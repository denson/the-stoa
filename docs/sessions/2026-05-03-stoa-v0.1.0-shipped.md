# Handoff brief — Stoa v0.1.0 shipped (2026-05-03)

**Audience:** the next POLYBIUS-equivalent seat (fresh user-tier Claude Code session) picking up where this session left off.
**Authored by:** the user-tier Chief-of-Staff seat ending this session, with PRINCIPAL Denson Smith.
**Companion:** sectioned session retrospective at `docs/sessions/2026-05-03-stoa-v0.1.0-shipped--retro.md` (semantic-chunked for hypergraph / vector-DB ingestion later).

---

## TL;DR

The Stoa shipped v0.1.0 + went public on GitHub. The Arc 19 sequence (initial entry-point skills + 10 iterative patches) closed cleanly. **Next priority is installing Stoa into `C:\Users\denso\claude_projects\ariadne-core-workspace` and authoring custom agents for that project — that's the project the PRINCIPAL paused to build distribution-readiness for Stoa.**

---

## Live state

| Surface | URL / path | Status |
|---|---|---|
| `the-stoa` repo | `https://github.com/denson/the-stoa` | **PUBLIC** |
| GH Pages — interactive presentation | `https://denson.github.io/the-stoa/case-study/architecture-kg.html` | **LIVE** (verified incognito + cold-clone) |
| GH Pages — case study (Jekyll-rendered) | `https://denson.github.io/the-stoa/case-study/case-study.html` | **LIVE** |
| `v0.1.0` release | `https://github.com/denson/the-stoa/releases/tag/v0.1.0` | **TAGGED** (anchor commit `d375987`) |
| Repo description + homepage URL | (set via `gh repo edit`) | **CURRENT** |
| `user-beadwork` (sister repo) | `https://github.com/denson/user-beadwork` | **PRIVATE** (intentional — case study `u--7yg` refs degrade as prose context) |
| Local working tree | `C:\Users\denso\claude_projects\the-stoa\` | clean against `origin/main` |
| Build session for `the-stoa` Arc 19 | none active | `bw` ticket `stoa--t3k` was the last — closed |
| Polling crons | none active | (Arc 18 polling cadence `*/5 * * * *` cancelled at engagement end) |

The cold-clone flow is verified end-to-end. A fresh user can `git clone https://github.com/denson/the-stoa.git`, open in Claude Code (Desktop or CLI), ask *"what is this?"*, and the agent reads `SKILL.md` → routes them to the visual tour / install / case-study.

---

## What's locked — DO NOT redo

Each lock has a one-line *why* (full context in the retrospective).

| Lock | Why |
|---|---|
| **Elevator pitch wording** (~220 words, in `README.md` + `SKILL.md`) | Iterated through 4 rounds with PRINCIPAL; v3 with the empirical-insight-integrated framing won. Field-notes voice. |
| **"Interactive presentation"** as the user-facing term, NOT "knowledge graph" | PRINCIPAL: "knowledge graph" is academic-CS jargon that puts off generic readers. (Arc 19.10) |
| **Chrome MCP as primary preview surface**, not Claude Preview | PRINCIPAL tested Preview empirically: clunky in practice — inline screenshots aren't the same UX as a live Chrome tab the user can interact with. (Arc 19.4 reverted Arc 19.3) |
| **Agent releases browser control proactively** after Beat 3's hand-off, before user asks | The Chrome extension's "Stop Claude" button is empirically buggy; releasing is the agent's responsibility. (Arc 19.6) |
| **Beat 1 file:// link must be absolute markdown link, surfaced eagerly in option (b)** | Cold-clone test caught the agent surfacing `docs/case-study/architecture-kg.html` (relative, not clickable). Now constructed before the surface message. (Arc 19.5) |
| **Teach/Cite narration shape** (concept-led, visual as italic side-citation) | PRINCIPAL: "the agent is just describing the screen." Beats 5/6/7 in `skills/stoa-intro/SKILL.md` enforce concept-leads-visual-cites. **Do NOT emit literal "Teach:" / "Cite:" labels in narration** — those are skill-author labels, not script labels. (Arc 19.7) |
| **Disciplines pitch as named examples** (Beat 8), not log-file ticket dumps | PRINCIPAL caught the failure mode mid-tour. Discipline name leads, ticket-id in parens as side-citation. (Arc 19.1) |
| **Standalone HTML transport-controls position: `bottom: 96px`, opaque background** | At tall viewports, original `bottom: 28px` + 92% opacity = invisible against BEADWORK band's striped pattern. (Arc 19.8) |
| **`user-beadwork` stays PRIVATE** | PRINCIPAL decision; case study links degrade gracefully as prose context. |
| **Three-skill structure**: root `SKILL.md` (about) + `skills/stoa-intro/` (visual tour) + `skills/install-stoa/` (install) | Established Arc 19; deliberately layered to separate routing from action. |
| **CLAUDE.md is short** (~5 sections, hands off to SKILL.md for the canonical pitch) | Auto-loaded every session — keeping it short matters for context budget. (Arc 19.9) |

---

## Operational state — nothing in flight

- No active polling crons
- No open `bw` tickets in the-stoa
- No in-flight build sessions (no MAJOR_PLINY-for-engagement currently activated)
- No outstanding directives in `substrate/arcs/`
- No surfaced questions awaiting PRINCIPAL adjudication

The board is clean. New POLYBIUS starts from rest.

---

## Pending in priority order

### 1. Install Stoa into `ariadne-core-workspace` + author custom agents

**Path:** `C:\Users\denso\claude_projects\ariadne-core-workspace`

**Why this is the load-bearing next item:** PRINCIPAL paused work on this project to build Stoa's distribution-readiness. With Stoa now publicly distributable, the actual deployment + customization story gets its first real test on a live project that has its own substantive work (vector database + hypergraph for searchable session transcripts).

**Concrete next steps:**

1. PRINCIPAL opens a fresh Claude Code session in `~/claude_projects/ariadne-core-workspace/`
2. Either: invokes `/install-stoa` skill (the guided walkthrough) — OR — runs `~/claude_projects/the-stoa/substrate/install.sh --target project --project-dir ~/claude_projects/ariadne-core-workspace --modify-claude-md` directly
3. After install: PRINCIPAL invokes POLYBIUS in that project session; project-tier POLYBIUS comes online with `MAJOR_POLYBIUS_ariadne_core_workspace`
4. Customization phase: author project-specific custom agents. The substrate's CAPTAIN roster (architect, plan-critic, executor, verifier, reviewer, scout, file-clerk, intake, synthesist, spec-checker) is generic; ariadne-core-workspace likely needs domain-specific seats (e.g., a CAPTAIN for graph-engineering-specific work, a CAPTAIN for vector-DB schema design). Use the `agent-author` skill (Arc 17) for this.

**This is the first real test of:**
- The cold-install flow on a project that wasn't authored alongside Stoa
- The custom-agent authoring story (sub-agent customization for a specific domain)
- The recursive sub-project pattern, if ariadne-core-workspace ends up spawning sub-projects (e.g., one for graph schema, one for vector index, one for ingestion pipeline)

**What's known about ariadne-core-workspace:**
- Stated purpose (per this session): vector database + hypergraph for searchable session transcripts
- The session retrospective + handoff documents in this directory (`docs/sessions/`) become input data for that system
- The PRINCIPAL hasn't shared deeper detail about ariadne-core-workspace's existing state in this session — PRINCIPAL will surface that to the new POLYBIUS at activation

### 2. Manuel feedback intake

PRINCIPAL pinged Manuel (beadworks team peer) at end of this session to look at Stoa. When his response comes in:

- Treat as load-bearing empirical signal — first external-audience reaction to the cold-clone flow
- Things to watch for in his response: which surface he hit first (visualization / case study / repo), whether the pitch landed, what he asked about (questions = signals about pitch gaps), any push-back (push-back is the most valuable signal)
- File any actionable feedback as `u--7yg.N+1` tickets or as inputs to a follow-up arc
- The case study `§11 Open questions` and the README's invitation explicitly invite his push-back

### 3. Hypergraph architectural arc

Forward-flagged in case study `§10 Where this is going`. When the PRINCIPAL signals readiness, this becomes a major architectural arc — likely involves spinning up a sub-project under either `the-stoa` or `ariadne-core-workspace` (since the hypergraph engineering domain trips at least two of the sub-project trip-wires per `MAJOR_POLYBIUS.md` §10.1).

Not active yet; do not pre-empt.

---

## Pointer map — where to read for what

| If asked / if you need | Read |
|---|---|
| The canonical Stoa elevator pitch | `the-stoa/SKILL.md` (root) — instructional version with framing |
| The full Arc 19 sequence retrospectively | This dir's `--retro.md` companion |
| Why three roles, recursion, voice discipline | `the-stoa/docs/case-study/case-study.md` §1-§9 |
| What's shipped after the case study was authored | `the-stoa/docs/case-study/case-study.md` Appendix (added Arc 19.9) |
| Specific officer behavior (POLYBIUS, PLINY, any CAPTAIN_*) | `the-stoa/substrate/MAJOR_*.md` and `substrate/CAPTAIN_*.md` (spec-authoritative) |
| KG / presentation vocabulary (node types, edge channels, modes) | `the-stoa/docs/case-study/kg-spec.md` |
| Architecture spec (canonical, lives in private sister repo) | `~/claude_projects/user-beadwork/plans/three-role-recursive-architecture.md` |
| Empirical record of disciplines (`u--7yg.N` tickets) | `~/claude_projects/user-beadwork/u--7yg` epic (private repo; `bw show u--7yg`) |
| Previous session retrospective (Arcs 1-14) | `~/claude_projects/user-beadwork/retrospectives/v2-arcs-1-14.md` (private) |
| Tour skill procedure detail | `the-stoa/skills/stoa-intro/SKILL.md` |
| Install skill procedure detail | `the-stoa/skills/install-stoa/SKILL.md` |
| `bw` (beadwork) tool itself | Web-search current install URL — separate project |

---

## Voice / cadence — how the PRINCIPAL works

- **Field-notes voice.** Working-notebook, peer-technical, no euphemism. Empirical observations preferred over declarative claims. The case study and substrate role files are the canonical voice register.
- **PRINCIPAL / HUMAN throughout** — never "user." Voice discipline is structural (`u--7yg.20`).
- **Small fixes ship same-session.** Fix-now discipline (`~/.claude/CLAUDE.md`). When something is small + mechanical, do it inline rather than scheduling for "next sprint."
- **Surface ambiguity, don't barrel.** Verify-then-execute (`u--7yg.10` / `.18`) — when a directive contradicts visible state, or when there's a load-bearing decision the directive didn't lock, surface to PRINCIPAL rather than guessing.
- **Empirical beat over a-priori reasoning.** The Arc 19 sequence shipped 10 patches, all from real testing, none from theoretical reasoning. PRINCIPAL is patient with iterative refinement IF each iteration was driven by a real observation.
- **Don't pre-empt PRINCIPAL's authority.** When the PRINCIPAL says "I want X," do X. When unclear, ask.
- **Autonomous-ship on clean PASS** (`u--7yg.11`). When work passes verification cleanly and no override flags fire, commit + push without routing through PRINCIPAL for approval. Saves round-trips.
- **PRINCIPAL is direct.** Frustration in messaging is honest signal, not personal. The right response to frustration is to acknowledge the actual issue (often a real failure mode) without over-defending.

---

## Activation — how to come online cleanly

When the PRINCIPAL says "POLYBIUS" or "chief of staff" in a fresh user-tier Claude Code session:

1. Read `~/.claude/MAJOR_POLYBIUS.md` (the user-tier role file) — assume the chief-of-staff role
2. Run `bw prime` in `~/claude_projects/the-stoa/` to surface this handoff document via its companion ticket
3. Read **this brief** (`docs/sessions/2026-05-03-stoa-v0.1.0-shipped.md`)
4. Read the Arc 19.x retrospective companion if you need the iterative-trail context (`--retro.md`)
5. Optionally: read `the-stoa/SKILL.md` to refresh the canonical pitch
6. Surface to PRINCIPAL: *"POLYBIUS online. Read the handoff brief — Stoa v0.1.0 shipped, board is clean, next priority is the ariadne-core-workspace install. Ready when you are."*

Then wait for PRINCIPAL's direction. They likely want to start the ariadne-core-workspace install + custom-agent authoring, but they may have something else in mind — verify rather than assume.
