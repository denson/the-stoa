Read .claude/MAJOR_PLINY.md and assume the orchestrator role for the-stoa.

Your immediate intent for this session: Build Arc 21 per the build directive at substrate/arcs/arc-21-build-directive.md (commit 7e0ea9e on origin/main, 812 lines, 6 Parts × 6 Phases × ~25 deliverables). The directive lands operating-modes-as-substrate-first-class-concept (HITL/Autonomous, orthogonal to Mode 1/Mode 2) plus the full autonomous-mode protocol stack (radio-check, adaptive cadence, unified polling, cross-tier write boundaries + routing, downstream-handoff discipline, bw orphan-branch storage model). Closes 10 cluster tickets (stoa--mdb, stoa--ljf, stoa--ivc, stoa--ay1, stoa--blg, stoa--xh2, stoa--m5m, stoa--0mr, stoa--cc0, stoa--1n5) plus the parent epic stoa--pbz; stoa--vz9 partial-landing is acknowledged but not closed (workspace-tier remainder is workspace-POLYBIUS's seat).

Operating mode for this dispatch: AUTONOMOUS. Per Phase D autonomous-aware ship discipline, run all six build phases heads-down without surfacing to PRINCIPAL between phases. Use the bw coordination ticket stoa--pbz for status updates as you progress. Surface to PRINCIPAL ONCE at end-of-arc with the final clean-PASS verdict for ship/no-ship. Mid-arc surfacing is reserved for the autonomous escalation triggers only: substance disagreement after one round of bw exchange, authorship/copyright/Denson-final-say content surfaces, irreducible ambiguity, peer (POLYBIUS_the_stoa) silent > 60 min, smoke-test failures that require POLYBIUS guidance per the directive's surface-back wording.

Coordination: POLYBIUS_the_stoa is polling stoa--pbz at */5 min via cron e6a08d54 (autonomous mode). Bidirectional radio-check pattern applies between you and POLYBIUS_the_stoa per directive §C.1. On dispatch, post an initialization handshake comment on stoa--pbz naming your cron id (set up your own polling cron via CronCreate if needed for tracking gauntlet phase transitions) and your cadence. Heartbeat every ≤30 min on stoa--pbz with one-line state ("DAEDALUS phase active" / "ARGUS critique returned" / "ADA Phase 2 of 6 complete" / etc.). Closure handshake on stoa--pbz close.

Cross-tier coordination: user-tier POLYBIUS is also polling stoa--pbz via unified cron ecd46dd8. If you need cross-project context (e.g., ariadne lived-experience anchors that aren't in the-stoa's bw, sanity check on a subtle judgment call), post a [for: user-tier POLYBIUS] tagged comment on a relevant stoa-- ticket. They respond within ~5 min poll fire.

Check beadwork (stoa-- prefix) for pending directives from MAJOR_POLYBIUS — start with: stoa--pbz (the parent arc-21 epic; the build directive at substrate/arcs/arc-21-build-directive.md is its load-bearing artifact).

If compaction or /clear erases your role, re-read this paste from HUMAN_paste-pliny-arc-21-instruction.md in the project root.
