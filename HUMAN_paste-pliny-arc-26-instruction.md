Read .claude/MAJOR_PLINY.md and assume the orchestrator role for the-stoa.

Your immediate intent for this session: Build Arc 26 per the build directive at substrate/arcs/arc-26-build-directive.md. Four phases × one gauntlet × ~1-2 files edited (substrate/skills/check-substrate-updates/check.sh + SKILL.md). The directive extends the existing substrate-update check skill so the FULL picture of substrate state surfaces at detection time — three drift categories (DRIFTED + MISSING + OBSOLETE) plus uncommitted .claude/-state pre-flight, plus a routing footer that names which tool to run per category. apply.sh stays narrow (DRIFTED only); install.sh handles MISSING + OBSOLETE; check.sh's output routes the operator. Closes one ticket: stoa--dxw (P2; LOAD-BEARING — the work-unit ticket itself, no children, body carries the 7 deliverables + 7 acceptance probes + hard-locked out-of-scope list).

**PRECONDITION:** Arc 25 (stoa--p5g) is CLOSED. ✓ Verified by user-tier POLYBIUS at dispatch authoring. You inherit post-Arc-25 main, currently 71ea092.

Operating mode for this dispatch: AUTONOMOUS. Run all four phases heads-down (Phase 1 DAEDALUS + ARGUS, Phase 2 ADA build on feature branch arc-26/build, Phase 3 VERA + CATO + ZENO parallel verification, Phase 4 smoke + PR + merge + close). Use stoa--dxw for status updates as you progress (no parent epic for this arc — stoa--dxw is the coherent work unit). Surface to PRINCIPAL ONCE at end-of-arc with the final clean-PASS verdict for ship/no-ship. Mid-arc surfacing is reserved for the autonomous escalation triggers only: substance disagreement after one round of bw exchange, authorship/copyright/Denson-final-say content surfaces, irreducible ambiguity, POLYBIUS silent > 60 min, smoke-test failures that require POLYBIUS guidance per the directive's surface-back wording.

Coordination: the-stoa PROJECT-TIER POLYBIUS is your radio-check peer for this arc (activated in a separate Claude Code session at the same time as you, paste at HUMAN_paste-polybius-arc-26-instruction.md). User-tier POLYBIUS dispatched the arc (the 2026-05-16 Arc-25 drift apply to railway--l7o, ariadne--kwo, s4--3jp surfaced the gap that motivates Arc 26). User-tier may post cross-workspace context comments on stoa--dxw periodically — but project-tier POLYBIUS is your peer. User-tier is exception-handler alongside PRINCIPAL. Bidirectional radio-check pattern applies per substrate/operating-disciplines.md §7. On dispatch, post an initialization handshake comment on stoa--dxw confirming you've read the directive + verified Arc 25 closed + naming your polling cron id (if you choose to set one). Heartbeat one-line state on stoa--dxw ("Phase 1 DAEDALUS dispatched" / "ARGUS PASS — ADA dispatched" / "Phase 3 VERA + CATO + ZENO returned CLEAN" / etc.). Closure handshake on stoa--dxw close.

The architectural decisions A1-A8 are LOCKED in the directive — you do NOT surface those as design questions. DAEDALUS treats stoa--dxw's body as primary input prose alongside the directive. Phase A locked decisions encode: one-arc one-gauntlet (A1), three detection categories + uncommitted-state pre-flight (A2), source-side enumeration with cite-comment mitigation + DAEDALUS sub-decision on SKILL_NAMES parsing approach (A3), OBSOLETE scope limited to substrate-deployable paths so user-added files aren't flagged (A4), uncommitted-state detection via git status --porcelain excluding .substrate-last-check (A5), output format with per-workspace verdict + per-category detail blocks + routing footer + uncommitted warning (A6), SKILL.md doc updates covering the new categories + the seam + the cite-comment coupling (A7), hard-locked out-of-scope list (A8).

**Cite-comment discipline:** wherever the new check.sh parses install.sh's SKILL_NAMES (or any other source-side enumeration that reads install.sh), place a cite-comment at the parse site pointing back to install.sh's line numbers. Same pattern as the existing apply_substitutions() cite-comment in check.sh / apply.sh. CATO will check this in cold-read; missing cite-comments will surface there if they slip through.

**CATO is MANDATORY** for this arc (not optional). The SKILL_NAMES parsing coupling is a wording-precision concern CATO catches well; the new output format will be the operator's interface for substrate state for the foreseeable future and warrants cold-reading.

Authorship attribution is IMMUTABLE per substrate/CLAUDE.md: all edits credit Denson Smith. No exception. Arc 26 edits existing files (no new files with fresh author-like fields), so the regression surface is smaller than Arc 25 — but if any CAPTAIN proposes adding a new file as part of build, check the author field before commit.

Check beadwork (stoa-- prefix) for pending directives from MAJOR_POLYBIUS — start with: stoa--dxw (the work-unit ticket; substrate/arcs/arc-26-build-directive.md is its load-bearing artifact, on disk at dispatch authoring time, will commit as part of arc-26/build feature branch).

If compaction or /clear erases your role, re-read this paste from HUMAN_paste-pliny-arc-26-instruction.md in the project root.
