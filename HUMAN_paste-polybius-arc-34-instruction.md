Read .claude/MAJOR_POLYBIUS.md and assume the project-tier chief-of-staff role for the-stoa.

Your immediate intent for this session: stand up as POLYBIUS peer for Arc 34 (canonification batch 2 — 4 small discipline tightenings bundled in stoa--y14: user-tier-to-main commit discipline + HUMAN_paste-*.md archival convention + template-title cosmetic fix + HITL-paused queue sweep discipline). PLINY is being activated in a separate session shortly. Work-unit ticket: **stoa--y14** (P2).

Your job is the radio-check counterpart per substrate/operating-disciplines.md §7. Surface to PRINCIPAL only on autonomous escalation triggers.

Cron hygiene FIRST (before any substantive work): this session may carry an orphaned cron from a prior /clear'd context. Run CronList; if any cron is present, CronDelete it. Then set up fresh cron at */5 * * * * (substrate-canonical default per operating-disciplines.md §7.2). Name the new cron id in your init handshake on stoa--y14.

**Read first (in order):**

1. **`substrate/arcs/arc-34-build-directive.md`** — load-bearing spec. A1-A12 LOCKED architectural decisions.
2. **`bw show stoa--y14`** — work-unit ticket bundling C1-C4. Body carries empirical anchors + fix-shape Options + out-of-scope hard-locks + §15 N=1 framing.
3. **Source tickets folded as candidates:**
   - `bw show stoa--k36` (C1 — user-tier-to-main)
   - `bw show stoa--f37` (C2 — paste accumulation)
   - `bw show stoa--3qi` (C3 — template-title cosmetic)
   - `bw show stoa--ize` (C4 source — HITL-paused queue surfaced via this sweep)
4. **`HUMAN_paste-pliny-arc-34-instruction.md`** in the project root — PLINY's activation paste; same content frame.
5. **`substrate/MAJOR_POLYBIUS.md`** — current section structure; C1 + C4 candidate loci.
6. **`substrate/MAJOR_PLINY.md` §5.9 + §5.10** — C2 arc-close discipline lands alongside; pre-branch hygiene + signoff-accuracy already there.
7. **`substrate/templates/paste-instruction-template.md`** — C3 title fix; C4 candidate locus if DAEDALUS picks Option γ.
8. **`substrate/templates/handoff-doc-template.md`** — C4 candidate locus.
9. **Arc 32 stoa--ewn precedent** — this arc mirrors its bundling pattern.

**Operating mode:**

- AUTONOMOUS with radio-check protocol per substrate/operating-disciplines.md §7.
- USER-TIER POLYBIUS dispatched + will do QA pass at arc close per PRINCIPAL's pattern.
- On init: post handshake comment on stoa--y14 confirming directive + ticket + source-tickets all read, naming polling cron id + cadence.
- Per-phase heartbeat on stoa--y14.
- Closure handshake on stoa--y14 close per §5.10. PLINY tags `[for: user-tier POLYBIUS]` invitation.

**What stays out of scope (per directive A8 — hard-locked):**

- stoa--32b.2 (script/agent-split) — shipped via Arc 33.
- stoa--ize Arc 22 disposition (stoa--jru) — separate Arc 36.
- stoa--vz9 universal disciplines — separate Arc 37.
- stoa--kjo per-agent git identity — separate Arc 35.
- u--7yg.16 + .21 — separate (audit + operational-truth).
- ariadne--1of + s4--bbz — separate.

If PLINY or any CAPTAIN surfaces a scope concern touching A8, treat as substance disagreement: confirm A8 wording from directive, file follow-up ticket if the concern has merit, do NOT expand this arc.

**Pre-branch hygiene per §5.9 + worktree convention per §5.9.4 (directive A10):** before PLINY creates arc-34/build, verify the two-check rule. Use separate worktree at `.claude/worktrees/arc-34-build/`. User-tier POLYBIUS confirmed at dispatch: local main = origin/main at `789496b`; no orphan arc-build branches.

**Signoff-accuracy per §5.10 + attestation-honesty per §19.6 (directive A11):** PLINY's signoff must live-verify cleanup claims per §5.10. Attestations cite live-verified state per §19.6.

**PRINCIPAL-gate awareness per §25:** if any DAEDALUS sub-decision (C1 Option A/B/C, C2 Option α/β/γ, C4 Option α/β/γ) surfaces as needing PRINCIPAL judgment rather than DAEDALUS discretion, treat as a PRINCIPAL-gate per §25 — halt + escalate immediately.

**Source-ticket closure per directive A12:** on Arc 34 ship, close source tickets in addition to stoa--y14: stoa--k36 + stoa--f37 + stoa--3qi + stoa--ize (each with cross-ref to merge commit).

**Authorship attribution is IMMUTABLE per substrate/CLAUDE.md.** Arc 34 edits existing files; no fresh author-like field exposure expected.

**Self-referential note:** Arc 34 canonifies disciplines that surfaced via today's stoa--ize sweep + end-of-session hygiene. C1 (user-tier-to-main) is literally about user-tier POLYBIUS direct-commit behavior the substrate hasn't yet documented. C4 (HITL-paused queue sweep) would have surfaced stoa--jru (Arc 22) much earlier had it been canon. Same substrate-shaping shape as Arcs 24-33.

If compaction or /clear erases your role, re-read this paste from HUMAN_paste-polybius-arc-34-instruction.md in the project root.
