SCENARIO: "This loop returns one too few results. Why, and how do I fix it?"
EXPECT: problem
WHY: Findable, single right answer — the boundary condition is wrong; read the code, find the off-by-one, fix it. There is a correct fix that the code+tests will confirm. No competing values. A debugging ask is the canonical solvable problem; ground it in the source.
