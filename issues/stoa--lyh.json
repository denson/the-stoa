{
  "assignee": "denson",
  "blocked_by": [],
  "blocks": [],
  "created": "2026-05-08T21:15:51Z",
  "description": "Surfaced 2026-05-08 by PRINCIPAL after Engagement B substrate ship + manual deploy to ariadne-core-workspace. Today's deploy required POLYBIUS to (a) realize the substrate had updated, (b) manually run install.sh --dry-run, (c) verify drift, (d) deploy. That sequence is fine for occasional updates but doesn't scale as the substrate matures or as consumer workspaces multiply.\n\n## Problem\n\n- the-stoa is canonical substrate source for MAJOR_PLINY.md, MAJOR_POLYBIUS.md, operating-disciplines.md, 10 CAPTAIN envelopes, templates, skills.\n- Consumer workspaces (ariadne-core-workspace, possibly agent-team-on-beadwork, agent-gauntlet, others) deploy via substrate/install.sh.\n- After deploy, if the-stoa is updated, consumer workspaces silently drift.\n- Current detection: manually `cd substrate \u0026\u0026 ./install.sh --target project --project-dir \u003cws\u003e --dry-run` per workspace, eyeball output. Doesn't scale; easy to forget; no aggregation across workspaces.\n\n## Proposed feature: skill that checks all consumer workspaces for drift\n\nNew skill at `substrate/skills/check-substrate-updates/`. Invokable via Skill tool or via a one-liner from any agent (POLYBIUS, PLINY, pair-programmer Major).\n\nBehavior:\n1. Read a workspace registry (substrate/consumer-workspaces.txt or auto-discovered via filesystem walk under ~/claude_projects/).\n2. For each registered workspace: invoke install.sh --dry-run, capture which files would change.\n3. Aggregate output:\n\n   ```\n   ariadne-core-workspace            STALE (4 files; last deploy 2026-05-04)\n     - operating-disciplines.md       +156 lines\n     - MAJOR_PLINY.md                  +86 lines\n     - MAJOR_POLYBIUS.md               +2 lines\n     - templates/paste-instruction-template.md  +2 lines\n   agent-team-on-beadwork            CURRENT\n   agent-gauntlet                    NOT-STOA-DEPLOYED (no .claude/operating-disciplines.md)\n   ```\n\n4. Optional flag --since to show substrate-side commits between last deploy and current: `git log --oneline \u003clast-deploy-sha\u003e..HEAD substrate/`. Helps PRINCIPAL decide whether the drift is worth deploying now or can wait.\n\n5. Optional flag --deploy to apply install.sh against any STALE workspace identified (with confirmation prompt or --yes).\n\n## Implementation shape\n\n- substrate/skills/check-substrate-updates/SKILL.md — skill metadata + frontmatter + trigger description\n- substrate/skills/check-substrate-updates/check.sh — bash implementation; reuses install.sh --dry-run as the drift primitive\n- substrate/consumer-workspaces.txt — explicit registry (one path per line); alternative is auto-discovery but explicit registry is more deterministic\n\nWorkspace registry seed (current consumers I'm aware of):\n- /c/Users/denso/claude_projects/ariadne-core-workspace\n- (others TBD per PRINCIPAL's actual workspace inventory — survey at implementation time)\n\n## Out of scope\n\n- Activation-time auto-check in MAJOR_PLINY / MAJOR_POLYBIUS (more intrusive; could be a v2 follow-up if drift becomes painful)\n- Auto-deploy without operator confirmation (security: substrate canon edits SHOULD be ratified by a human before propagating to consumer workspaces, especially since they affect agent role-files)\n- Cross-machine substrate sync (the-stoa is per-machine; multi-machine canon is a separate concern)\n\n## Gauntlet\n\nADA + CATO eyeball. New skill + small bash script + plain-text registry file. ~150-300 LOC total. No DAEDALUS / ARGUS / VERA / ZENO unless something substantive surfaces during build.\n\n## Sequencing\n\nP3. Not blocking; today's manual deploy was tractable. But useful soon — multi-workspace deploy reality is real now (post Engagement B), and the friction will compound as the substrate evolves. Bundle into next stoa substrate dispatch or ship standalone when convenient.\n\n## Empirical anchor\n\n2026-05-08 PRINCIPAL surface during Engagement B post-ship deploy. POLYBIUS manually ran install.sh --dry-run + install.sh against ariadne-core-workspace; no aggregated visibility across multiple workspaces; PRINCIPAL flagged the gap and asked for a check skill.",
  "id": "stoa--lyh",
  "labels": [],
  "comments": [
    {
      "text": "## v2 architecture (PRINCIPAL design 2026-05-08)\n\nOriginal ticket framed this as a centralized POLYBIUS-side check skill. PRINCIPAL re-architected based on the principle that **each workspace is expected to diverge from the core stoa substrate** — substrate is a starter framework, not canonical truth that consumer workspaces must mirror. Users will modify their agent teams as a feature, and any check-for-updates flow must respect that.\n\n### Core principle\n\nA working agent team is sacrosanct. The substrate update mechanism never modifies a deployed file without explicit consent, and treats any local modification as a signal that the user has invested in the divergence.\n\n### Architecture\n\n**1. Per-file source SHA marker (frontmatter/trailer)**\n\n`install.sh` writes a source SHA marker into each deployed file at deploy time:\n\n- For .md files: YAML frontmatter field, e.g. `stoa_source_sha: \u003ccommit-SHA\u003e`\n- For .sh / other files: trailer comment, e.g. `# stoa-source-sha: \u003ccommit-SHA\u003e`\n\nThe marker records 'this file was deployed from the-stoa at this commit.' Drift detection uses three comparisons:\n\n- Deployed content vs source-at-recorded-SHA → is there LOCAL modification?\n- Source-at-recorded-SHA vs current source → has UPSTREAM advanced?\n- Combine → categorize each file as one of:\n  - **CLEAN, CURRENT**: no local mods, matches latest source\n  - **CLEAN, UPSTREAM-ADVANCED**: no local mods, source has newer version → candidate for safe update\n  - **LOCALLY-MODIFIED, UPSTREAM-UNCHANGED**: user modified, source hasn't changed → leave alone\n  - **LOCALLY-MODIFIED, UPSTREAM-ADVANCED**: user modified AND source changed → INTEGRATION CANDIDATE; surface diff between user's mod and upstream change for discussion\n\n**2. POLYBIUS-side periodic check, bw-tracked state**\n\nState storage: marker file at `\u003cworkspace\u003e/.claude/.substrate-last-check` (simpler than bw — file-system state is closer to the right shape than ticket data). Contents: `last_check_timestamp` + `last_check_against_sha`.\n\nCadence: configurable; default 'daily.' On POLYBIUS activation, if last check \u003e 24h ago, run drift check in background, surface result if any drift.\n\nInitial-setup question (during onboarding): 'Want POLYBIUS to check substrate for updates daily? You can change this anytime.' If yes: schedule active. If no: only check on explicit ask.\n\nUser-prompt path: any agent can be asked 'check substrate for updates' and run on-demand.\n\n**3. FYI surface; non-blocking; ignorable indefinitely**\n\nDrift surface is informational, not modal:\n\n```\nSubstrate updates available (last check: 6 days ago):\n  CLEAN, UPSTREAM-ADVANCED:\n    operating-disciplines.md  +156 lines (12 commits since deploy)\n    MAJOR_PLINY.md            +86 lines (7 commits since deploy)\n  LOCALLY-MODIFIED, UPSTREAM-ADVANCED:\n    MAJOR_POLYBIUS.md         (your version differs from any source revision; upstream also has 2 new commits)\n\nRun 'review substrate updates' to discuss; or ignore — your team keeps running unchanged.\n```\n\nIf the user's working system is happy, they can ignore the surface for weeks, months, or forever. POLYBIUS doesn't nag.\n\n**4. Per-file consent with context-aware discussion**\n\nWhen user says 'review substrate updates,' POLYBIUS walks through file-by-file:\n\n- For CLEAN, UPSTREAM-ADVANCED: shows what changed; discusses pros and cons of taking the update vs staying put. Considers HOW the user is working with the team (e.g., 'you ship via PR-flow gauntlet, this update sharpens VERA's brief discipline — likely useful for you'). User decides per file.\n- For LOCALLY-MODIFIED, UPSTREAM-ADVANCED: surfaces both diffs (user's mod vs original; original vs upstream). Suggests integration if obviously compatible; recommends leaving alone if non-trivial. User decides per file.\n- For LOCALLY-MODIFIED, UPSTREAM-UNCHANGED: not surfaced (no action needed).\n\nUser can also say 'just do it' for CLEAN-class updates: POLYBIUS applies them all, with revert path ready if any regression surfaces.\n\n**5. Git-based revert**\n\nAssumption: `\u003cworkspace\u003e/.claude/` is tracked in workspace git (matches current ariadne-core-workspace pattern).\n\nPre-deploy: commit any uncommitted .claude/ changes (or stash them with explicit warning if the user has pending edits). Deploy. If user wants to revert: `git checkout HEAD~1 -- .claude/\u003cfile\u003e` for specific files, or full `git reset --hard HEAD~1` for full revert.\n\nIf `.claude/` is NOT tracked in the consumer workspace's git: install.sh writes per-file backups to `\u003cworkspace\u003e/.claude/.substrate-backups/\u003ctimestamp\u003e/` before overwrite. Single 'revert' command restores from most recent backup. (Lower priority — most consumer workspaces will track .claude/.)\n\n**6. Workspace-divergence as expected feature**\n\nThe substrate provides a starter framework; consumer workspaces are EXPECTED to fork and modify. The substrate update mechanism's job is to:\n\n- Notify when upstream has improvements available\n- Help the user decide whether to integrate\n- Never override user agency\n\nUsers versioning their own agent teams is the intended workflow. The check skill's role is to keep them informed about the upstream baseline they forked from — without forcing them back to it.\n\n## Implementation scope\n\n```\nsubstrate/skills/check-substrate-updates/\n  SKILL.md                  # invokable skill\n  check.sh                  # core drift detection logic\n  review.sh                 # interactive review flow (per-file discussion)\n  apply.sh                  # apply chosen updates, with git pre-commit\nsubstrate/install.sh        # extended to write per-file source SHA markers\nsubstrate/templates/        # may need updates for marker placement\n```\n\nPlus: pre-existing deployed substrate files need a one-time MIGRATION to add source SHA markers. install.sh should detect the absence of markers and either (a) refuse to drift-check until re-deployed (heavy-handed), or (b) backfill markers using current source SHA and treat as 'clean baseline' (assumes deployed copy matches current source — verifiable via byte-compare; treat as locally-modified if mismatch).\n\n## Gauntlet\n\nDAEDALUS-light + ADA + CATO. The 4-category drift classification is real design work; DAEDALUS sketches the shape. ADA implements; CATO eyeballs. Skip ARGUS / VERA / ZENO unless DAEDALUS surfaces real architectural risk.\n\n## Sequencing\n\nP3, but architecturally interesting. Could ship standalone OR fold into next stoa substrate dispatch. The DAEDALUS-light step adds modest gauntlet ceremony beyond ADA + CATO eyeball; bundle calculus depends on what other tickets are on the dispatch.\n\n## Out of scope\n\n- Auto-update without user consent (explicitly violates core principle)\n- Multi-machine substrate sync (cross-machine canon is separate)\n- Substrate-side validation that user's modifications are 'still compatible' with new role-file structure (semantic compatibility checking is way too ambitious; users own their forks)\n- Activation-time HARD-BLOCK on drift (FYI only; never blocks the working team from operating)",
      "timestamp": "2026-05-08T21:36:35Z"
    },
    {
      "text": "## Migration baseline confirmed (PRINCIPAL 2026-05-08)\n\nPRINCIPAL confirms: agent teams currently deployed across consumer workspaces have NOT been locally modified. After today's substrate deploy of ariadne-core-workspace via install.sh (commit c37cf5a as source), the deployed copies are byte-equal to the corresponding source files in the-stoa.\n\nImplication for the migration step in this ticket: the simple path is sufficient. When stoa--lyh implementation ships and a drift check runs against existing-deployed-substrate-without-markers, the byte-compare-against-current-source path will correctly classify all currently-deployed files as 'clean baseline.' install.sh's marker backfill can write the current source SHA confidently.\n\nNo edge case where 'unknown SHA + locally-modified-from-current-source' needs special handling for the post-2026-05-08 baseline. Future deployments may produce that state if a user modifies a deployed file between deploys, but the existing baseline is uniformly clean.\n\nVerifiable post-implementation: the first drift-check run against ariadne-core-workspace (currently the only confirmed Stoa-consuming workspace) should report 'all files CLEAN, CURRENT' on day-zero.",
      "timestamp": "2026-05-08T21:40:52Z"
    },
    {
      "text": "## Running-agent concern (PRINCIPAL 2026-05-08)\n\nPRINCIPAL flagged: 'we have the problem that we might need to update you.' When substrate updates include changes to role files (MAJOR_POLYBIUS.md, MAJOR_PLINY.md, CAPTAIN envelopes), the currently-running session is operating on the role file it loaded at activation, not the deployed file post-update. Mismatch is real for substantive role-file changes.\n\n### Mitigation: bake into the skill's UX\n\nWhen stoa--lyh's apply.sh writes role-file updates, it should surface explicitly:\n\n```\nSubstrate update applied. Role files affected:\n  - MAJOR_POLYBIUS.md (~50 line change to discipline section)\n  - MAJOR_PLINY.md (small clarification only)\n\nCurrently-running agent sessions are operating on the pre-update role files\nthey loaded at activation. They will continue working as before — their\nbehavior is in-context, not file-bound — until a /clear or new session.\n\nRecommendation: when you reach a natural break, /clear the running POLYBIUS\nsession and re-paste activation. The new role file's behavior takes effect\non the next session. PLINY sessions inherit the same pattern.\n\n(For minor changes, you can also choose to ignore — running sessions\ncontinue per their loaded behavior, and the change takes effect organically\non the next role-activation.)\n```\n\n### Implementation surface\n\napply.sh tracks which deployed files were role files (MAJOR_*.md or CAPTAIN_*.md). When the deploy includes any of those, the message above is surfaced as the apply step's tail output.\n\nThe skill does NOT auto-invalidate running sessions (no way to do that anyway — sessions are independent processes). It just informs the user about the mismatch and the recovery path.\n\n### Risk classification\n\n- **Minor changes** (clarifications, small additions, fixes that don't change agent behavior): user can safely ignore until next natural session boundary.\n- **Substantive changes** (new disciplines, new responsibilities, restructured pipelines): /clear sooner is recommended; the skill can flag these explicitly via diff-size or section-level analysis.\n\nDiff-size as a heuristic: \u003e 50 lines changed in a role file = 'substantive, /clear recommended sooner'; \u003c 50 lines = 'minor, /clear at convenience.' Heuristic only; user judgment overrides.\n\n### Out of scope (this ticket)\n\n- Auto-detecting backward-incompatible changes (semantic; too ambitious; user judgment is canonical).\n- Forcing a /clear on the running session (not technically possible from the skill; would be inappropriate even if it were).",
      "timestamp": "2026-05-08T21:43:19Z"
    },
    {
      "text": "## Dispatch notes for PLINY (POLYBIUS-side workflow recommendation, 2026-05-08)\n\nWhen this ticket gets dispatched, the recommended workflow shape is:\n\n### Gauntlet\n\nDAEDALUS-light + ADA + CATO eyeball. Skip ARGUS / VERA / ZENO unless DAEDALUS-light surfaces real architectural risk during sketch.\n\n### DAEDALUS-light scope\n\nThree architectural picks to resolve before ADA implements:\n\n1. **Per-file source SHA marker shape** — pick exact field name + format. YAML frontmatter for .md files (e.g., `stoa_source_sha: \u003csha\u003e`); trailer comment for .sh files (e.g., `# stoa-source-sha: \u003csha\u003e`).\n2. **4-category drift classification** — describe how each of (CLEAN-CURRENT / CLEAN-UPSTREAM-ADVANCED / LOCALLY-MODIFIED-UPSTREAM-UNCHANGED / LOCALLY-MODIFIED-UPSTREAM-ADVANCED) is computed and how each surfaces.\n3. **apply.sh + revert.sh seam** — git-based revert flow + the running-agent warning UX from the prior comment.\n\nOutput to whatever the-stoa's design-artifact convention is (likely under agents/design/ or substrate/design/). Surface picks-summary to POLYBIUS before ADA dispatch.\n\n### Implementation surface (from the v2 architecture comment)\n\n```\nsubstrate/skills/check-substrate-updates/SKILL.md\nsubstrate/skills/check-substrate-updates/check.sh\nsubstrate/skills/check-substrate-updates/review.sh\nsubstrate/skills/check-substrate-updates/apply.sh\nsubstrate/install.sh                                  # extended for SHA markers + migration backfill\nsubstrate/MAJOR_POLYBIUS.md                          # documents check-on-activation discipline + daily cadence\nsubstrate/operating-disciplines.md                   # cross-reference to the skill\n```\n\n### PR shape\n\nSingle PR for the bundle. Three disjoint layers (skill / install.sh / role-files); CATO's bundle-shape rule says safe.\n\n### Ship + handoff\n\n- Open PR; squash-merge per the-stoa's existing pattern.\n- Verify post-merge via the-stoa's standard (GitHub Pages deploy on merge).\n- Close stoa--lyh with ship reason.\n- Surface to POLYBIUS: 'stoa--lyh shipped. Downstream deploy to consumer workspaces (ariadne-core-workspace, etc.) is POLYBIUS-side. Note: the deploy will trigger the new role-file-update warning on the running POLYBIUS — POLYBIUS plans a /clear point per the apply.sh UX.'\n- STOP. Do not pre-emptively run install.sh against consumer workspaces — that's POLYBIUS's call after substrate ships.\n\n### Bounded engagement\n\nSingle ticket, single engagement. Surface mid-flight at the DAEDALUS-light picks-summary; otherwise proceed straight to ADA + CATO + ship.",
      "timestamp": "2026-05-08T21:46:32Z"
    },
    {
      "text": "DAEDALUS-light design written: agents/design/stoa--lyh/design.md\n\nverdict: pass\nticket: stoa--lyh\ndesign_artifact_path: agents/design/stoa--lyh/design.md\n\nPicks summary:\n- Pick 1 (marker shape): single canonical HTML-comment trailer block (shell-comment for .sh files), fields source-sha/source-path/deploy-tier/deployed-at; stripped from byte-compare. Trailer position avoids collision with existing YAML frontmatter on CAPTAIN_*.md and SKILL.md. Substitution wrinkle resolved Option A: marker written AFTER sed; check.sh re-derives substitution by mirroring install.sh lines 612-616 and 657 in an apply_substitutions() helper, with reciprocal cite-comments at both sites.\n- Pick 2 (4-category classification): two normalized comparisons (deployed vs source-at-recorded-SHA-substituted; source-at-recorded vs source-at-HEAD-substituted), pre-strip marker block + normalize line endings before compare. Edge cases: pre-migration baseline -\u003e byte-compare-against-HEAD path per ticket comment 2; unknown-recorded-SHA -\u003e same fall-through; renamed-workspace-slug surfaces honestly as LOCALLY-MODIFIED with annotation.\n- Pick 3 (apply.sh + revert + running-agent UX): apply.sh per-file-list; pre-deploy git auto-commit on --yes (uncommitted .claude/ refused without --yes) or backup-dir at \u003cws\u003e/.claude/.substrate-backups/\u003cts\u003e/; running-agent warning fires when any role file in apply set has \u003e50 line diff; revert.sh as separate ~80 LOC script for both git and backup-dir paths.\n\nSelf-assessed weak points (4): substitution-coupling between check.sh and install.sh; 50-line heuristic is brittle to semantic-only changes; marker block is fragile to hand-edits; consumer-workspaces.txt registry will silently drift on new-workspace creation. Each is defended as the lesser evil vs alternatives ruled out-of-scope by ticket.\n\nResidual question for ARGUS (gauntlet skips ARGUS but flagging anyway): is the substitution-coupling cite-comment durable enough? Option A vs B was the load-bearing pick.\n\nStatus: completed; ready for ADA dispatch.",
      "timestamp": "2026-05-08T21:54:33Z"
    },
    {
      "text": "## DAEDALUS-light picks-summary (PLINY 2026-05-08)\n\nDesign landed at `agents/design/stoa--lyh/design.md` (542 lines).\n\n### Pick 1 — Marker shape\n\nSingle canonical HTML-comment trailer (shell-comment variant for .sh files):\n```\n\u003c!-- stoa-substrate-marker\nsource-sha: \u003c40-char-SHA\u003e\nsource-path: substrate/\u003crepo-relative-path\u003e\ndeploy-tier: \u003cuser|project|subproject\u003e\ndeployed-at: \u003cISO-timestamp\u003e\n--\u003e\n```\n\nTrailer over header: avoids collision with existing YAML frontmatter on CAPTAIN_*.md and SKILL.md; keeps reader-attention paragraph at top of role files.\n\n**Substitution wrinkle resolution: Option A** — marker written AFTER sed substitution; `check.sh` re-derives substitutions via `apply_substitutions()` helper that mirrors install.sh lines 612–616 + 657. Reciprocal cite-comments at both sites.\n\n### Pick 2 — 4-category drift classification\n\nTwo normalized comparisons (marker-strip + LF-normalize before byte-compare):\n- `locally_modified  = (deployed != source_at_recorded_with_substitutions)`\n- `upstream_advanced = (recorded_sha != HEAD) and (source_at_recorded != source_at_HEAD)`\n\nEdge cases named:\n- Pre-migration baseline (no marker): byte-compare against HEAD; if equal → CLEAN_CURRENT + marker backfill on next apply; if not → LOCALLY_MODIFIED_UPSTREAM_UNCHANGED conservatively.\n- Unknown source-SHA (force-push): fall through to same byte-compare path; transparent self-heal on next apply.\n- Workspace deploy-context drift (project renamed): surfaces as LOCALLY_MODIFIED with surface annotation; remediation = re-run install.sh.\n\n### Pick 3 — apply.sh + revert + running-agent UX\n\n- apply.sh takes per-file list; writes individual files (does NOT re-run install.sh wholesale; avoids interactive prompts).\n- LOCALLY_MODIFIED_UPSTREAM_ADVANCED files: refuse without explicit `--overwrite-local`.\n- Pre-deploy snapshot: git path (auto-commit on `--yes` if uncommitted; refuse without `--yes`) OR backup-dir fallback at `\u003cworkspace\u003e/.claude/.substrate-backups/\u003ctimestamp\u003e/`.\n- Running-agent warning: literal text from ticket comment 3, gated by 50-line role-file diff heuristic (substantive vs minor split). Non-modal; FYI only.\n- **revert.sh** authored as separate ~80 LOC script (symmetric UX across git and backup-dir paths; 6th file in skill subtree).\n\n### File touchpoints\n\nNet-new (~770 LOC, 6 files): SKILL.md / check.sh / review.sh / apply.sh / revert.sh / consumer-workspaces.txt\nEdited: install.sh (write_marker helper, --migrate-baseline flag), MAJOR_POLYBIUS.md (daily-cadence discipline + onboarding hook), operating-disciplines.md (cross-ref).\n\n### Residual question for PRINCIPAL/POLYBIUS adjudication\n\nDAEDALUS flagged §8.1 (install.sh ↔ check.sh substitution coupling) as load-bearing. Dispatch note 4 says skip ARGUS unless something substantive surfaces. PLINY's read: substantive enough to optionally spot-engage ARGUS on this one decision; otherwise CATO's bundle review carries the load. Surfacing to PRINCIPAL for ratification before ADA dispatch.",
      "timestamp": "2026-05-08T21:55:50Z"
    },
    {
      "text": "## PRINCIPAL scope decision (2026-05-08): Option Small approved\n\nPRINCIPAL ratified Option Small after the v2 picks-summary surface. Build is single-bit 'differs?' check; per-file SHA markers and 4-category classification dropped.\n\n### Kept from v2\n- Per-file consent + diff display in apply.sh\n- Running-agent warning when role files touched (50-line substantive heuristic; verbatim text from ticket comment 3)\n- Git-based revert (separate revert.sh script)\n- Daily check cadence in `\u003cworkspace\u003e/.claude/.substrate-last-check`\n\n### Dropped from v2\n- Per-file SHA markers (HTML-comment trailer block)\n- 4-category classification (CLEAN-CURRENT / CLEAN-UPSTREAM-ADVANCED / LOCALLY-MODIFIED-UPSTREAM-UNCHANGED / LOCALLY-MODIFIED-UPSTREAM-ADVANCED)\n- install.sh marker write/read logic\n- The apply_substitutions() install.sh-coupling that was v2 §8.1's load-bearing weak point — coupling shrinks but does not vanish (see §10.1 of new design)\n- ARGUS spot-engage question dissolves with the marker\n\n### Active design\n- New: `agents/design/stoa--lyh/design.md` (Option Small, ~470 lines)\n- Preserved: `agents/design/stoa--lyh/v2-marker-architecture-future-work.md` (the v2 thinking; revisit trigger documented at top of file)\n\n### Gauntlet\nADA + CATO eyeball. DAEDALUS / ARGUS / VERA / ZENO skipped. No DAEDALUS rev2.\n\nDispatching ADA next.",
      "timestamp": "2026-05-09T06:01:24Z"
    },
    {
      "text": "## CATO verdict + final amendments (PLINY 2026-05-09)\n\nCATO returned **pass** with one recommended-revision (s1) and three minor findings.\n\n### Findings landed inline before commit\n- **s1 (recommended-revision):** stale install.sh line-number cites in apply_substitutions cite-comments (off-by-1 across 5 sites). The cite-comment is the load-bearing mitigation for the substitution-coupling weak point per design §10.1; tightened to actual line numbers (~613–617 + ~658 for sed; ~681, ~711 for cp). Fixed in check.sh:108,113; apply.sh:83,88; SKILL.md:35,147.\n- **m1 (minor):** dead CAPTAIN_NAMES array in apply.sh:76-78 (carried over from check.sh during inlining). Removed.\n- **m3 (minor):** design.md slug-rule cite was install.sh line 461 actual was 462. Corrected in design.md (2 occurrences).\n\n### Findings left as-is (defensible per CATO)\n- **m2:** operating-disciplines.md got a new §9.1 sub-section rather than an appended paragraph (design §7's preference). CATO: 'defensible — placement inside §9 (durable storage / cross-session) is thematically right, and the new heading gives MAJOR_POLYBIUS.md §14 a stable anchor to link to.' Left as-is.\n\n### Probes re-run by CATO\n- Probe 1 (clean → CURRENT): PASS, including the file-class-scoping regression test (consent-prompts.md and the new SKILL.md both contain literal {{NAME_SUFFIX}} in prose; both correctly classified CURRENT).\n- Probe 2 (modified → DIFFERS): PASS.\n- Probe 4 (substantive role-file → /clear recommended): PASS, verbatim warning text matches design §3.4 word-for-word.\n\n### Bundle-shape verification\nThree disjoint surfaces (skill subtree net-new; install.sh 1-line; role-file/disciplines doc cross-references) — bundle-safe per MAJOR_PLINY.md §6.3.\n\n### ADA's self-assessed concerns (CATO confirmed all four)\nAll four are documented operational risks, not blocking; mitigations in place.\n\nShipping next.",
      "timestamp": "2026-05-09T06:22:29Z"
    },
    {
      "text": "## Shipped — PR #2 open (PLINY 2026-05-09)\n\nBranch `claude/awesome-nash-190573` pushed to origin; PR opened: https://github.com/denson/the-stoa/pull/2\n\nCommit: 7002813 — 8 files / 1284 insertions.\n\n### What ships\n\n- New skill: substrate/skills/check-substrate-updates/ (SKILL.md / check.sh / apply.sh / revert.sh)\n- Registry seed: substrate/consumer-workspaces.txt (ariadne-core-workspace only per ticket)\n- install.sh edit: SKILL_NAMES += check-substrate-updates\n- MAJOR_POLYBIUS.md edit: §14 daily-cadence discipline\n- operating-disciplines.md edit: §9.1 cross-reference\n\n### What stays local (gitignored agents/design/)\n\n- agents/design/stoa--lyh/design.md (Option Small spec)\n- agents/design/stoa--lyh/v2-marker-architecture-future-work.md (preserved v2 thinking; revisit trigger documented at top)\n\n### Post-merge actions for POLYBIUS\n\n1. Squash-merge PR #2 per the-stoa's existing pattern.\n2. Verify GitHub Pages deploy on merge.\n3. Deploy to ariadne-core-workspace via `cd substrate \u0026\u0026 ./install.sh --target project --project-dir ~/claude_projects/ariadne-core-workspace`. Day-zero check.sh should report `CURRENT (all N deployed files match current substrate)`.\n4. **The deploy will trigger the new role-file-update warning on the running POLYBIUS** (MAJOR_POLYBIUS.md gained §14 + minor structural addition; substantive-vs-minor heuristic should classify; /clear at the operator's natural break point).\n5. `bw close stoa--lyh --reason \"shipped via PR #2; substrate-update check skill live on the-stoa main\"`.\n\nPLINY stopped per dispatch note bounded-engagement rule. Not pre-emptively running install.sh against consumer workspaces — that's POLYBIUS's call.",
      "timestamp": "2026-05-09T06:24:39Z"
    }
  ],
  "priority": 3,
  "status": "in_progress",
  "title": "Substrate-update check skill — detect drift between the-stoa canon and consumer workspaces",
  "type": "task",
  "updated_at": "2026-05-09T06:24:39Z"
}
