{
  "assignee": "denson",
  "blocked_by": [],
  "blocks": [],
  "created": "2026-05-23T02:45:47Z",
  "description": "ENFORCEMENT LAYER: reliability moves from memorized-instruction to HARNESS-OWNED triggers that carry self-contained instructions at the moment of action. Proven principle: cron survives compaction because the cron prompt is fresh input re-injected AFTER the wipe (the agent does not remember; the trigger re-tells it). Same property makes hooks reliable (harness-fired, not model-fired).\n\nVERIFIED HOOK MAPPING (docs pulled 2026-05-22, code.claude.com/docs/en/hooks):\n- PreToolUse on Bash (e.g. git commit): hard mechanical gate via exit 2 / permissionDecision:deny. FULLY DETERMINISTIC, no model in loop, COMPACTION-IMMUNE. For: author-field audit, clean-tree-before-branch, no -m in bw comment.\n- Stop: inject additionalContext (self-check \"did you do A,B,C?\") + exit 2 to force rework before the turn can end. Caveat: Claude can commit BEFORE Stop fires and may stop mid-question - so pre-action gates belong on PreToolUse, not Stop.\n- PostToolUse on the Agent tool: fires in the PARENT context when a sub-agent returns; injects \"sub-agent done - run the checker against its output\". (SubagentStop CANNOT inject additionalContext - verified - so use PostToolUse-on-Agent for the check-a-sub-agent pattern.)\n- cron: periodic poll; survives compaction by fresh re-injection. For standing checks.\n\nKEY LIMIT: hooks CANNOT directly dispatch a sub-agent (verified). They inject the instruction + block the turn so the dispatch is non-optional; the model still issues it. Strong, not absolute -\u003e the peer-checker backstop matters.\n\nCHECKER AGENT: judgment backstop, triggered by a hook. Orchestrator-fired (a leaf agent cannot dispatch a checker - can't-nest). Cross-check (each other's work) \u003e self-check (shared blind spots). Fills a real gap: orchestrator OUTPUTS (activation pastes, arc-closes, commits, directives) currently have no checker.\n\nDETECTION of compaction-derailment: do NOT try to detect \"compaction happened\". Detect \"output diverges from bw ground truth\" - the checking layer IS the derailment detector (a derailed agent produces non-conforming output regardless of cause). A derailed agent cannot self-detect; detection must be external + comparison-based vs durable bw. Ready-made canary: the bw seat-tag convention (operating-disciplines 7.7) - a dropped/mistagged [from: seat] tag is a cheap signal. Bounded by check cadence (one cron/peer cycle, not instant).\n\nREMEDIATION: re-decompose the failed unit into smaller pieces and re-dispatch (orchestrator job - can't-nest). Compaction-derailment correlates with task size; small-enough tasks never compact. System self-tunes granularity (prevention by sizing small up front + reaction by splitting; the reaction TEACHES the prevention). bw-as-memory makes fine decomposition cheap (state read from bw, not carried in-head, so splits don't fragment context). GUARDRAILS: (1) checker must distinguish size-derail (split) from wrong-spec/blocked/impossible (escalate, do NOT split) else infinite subdivision; (2) attempt cap -\u003e escalate to human; (3) granularity floor (too-small = handoff/coordination overhead). Slots into the existing MAJOR_PLINY 5.6 INCOMPLETE/UNVERIFIABLE verdict protocol.\n\nAUTHORING RULE for every trigger payload (cron AND hook additionalContext): state WHY it fired and WHAT to do, self-contained inline (not \"see 5.1.3\" - a pointer fails after compaction).\n\nVERIFY AT BUILD TIME: PostCompact reportedly cannot inject context (per docs pulled) - so no immediate auto-reprime the instant compaction finishes; the next trigger carries it. Re-confirm against the live version (hook surfaces shift).",
  "id": "stoa--xyb.5",
  "labels": [],
  "parent": "stoa--xyb",
  "comments": [
    {
      "text": "DAEDALUS activated on stoa--xyb.5 (Arc 46 enforcement-layer design). Reading brief + spec (.5/.1/.2) + composition-layer mechanism + install.sh. Next: WEB-VERIFY live hook surface before drafting (global rule + .5 verify-at-build-time).",
      "timestamp": "2026-05-23T08:08:14Z"
    },
    {
      "text": "[from: daedalus-the-stoa] brief+spec+constraints+composition-layer+install.sh absorbed. install.sh has NO settings.json/hooks handling today (net-new infra). cron idiom = self-contained inline prompt body via CronCreate (matches .5 authoring rule). Next: WEB-VERIFY live hook surface (PreToolUse deny, Stop additionalContext, PostToolUse-on-Agent, SubagentStop, PostCompact) per global rule before drafting.",
      "timestamp": "2026-05-23T08:08:51Z"
    },
    {
      "text": "[from: daedalus-the-stoa] WEB-VERIFY done (3 sources; authoritative = code.claude.com/docs/en/agent-sdk/hooks). RESULTS vs .5: (1) PreToolUse deny via permissionDecision:deny+permissionDecisionReason CONFIRMED. (2) PostToolUse additionalContext CONFIRMED ('append information to the tool result') — resolves a stale plugin-dev SKILL.md that omitted it. (3) Agent IS a matchable tool name CONFIRMED ('Bash...WebFetch, Agent'). (4) SubagentStop NO additionalContext CONFIRMED (validates PostToolUse-on-Agent choice). (5) DRIFT: PostCompact does NOT EXIST in canon event list (only PreCompact) — reprime job moves to SessionStart matcher:compact OR cron; .5 'PostCompact can't inject' is moot. (6) NEW since .5: SubagentStart CAN inject additionalContext (noted as alt). Drafting design-rev1 now.",
      "timestamp": "2026-05-23T08:10:05Z"
    },
    {
      "text": "[from: daedalus-the-stoa] CAPTAIN frontmatter idiom + agent-author conventions absorbed (new CAPTAIN = substrate/CAPTAIN_\u003cMNE\u003e.md + CAPTAIN_NAMES array entry + install.sh deploy loop covers it). Restatement-gate drafting; then design §1-§7. Proposed checker name: CAPTAIN_NOMOS (ground-truth auditor). Drafting design-rev1.md now.",
      "timestamp": "2026-05-23T08:10:15Z"
    },
    {
      "text": "[from: daedalus-the-stoa] pass: design-rev1 complete (345 lines, 11 sections) at agents/design/arc-46/design-rev1.md, committed f9b879e on arc-46/build w/ seat trailer. WEB-VERIFY: PreToolUse-deny / PostToolUse-additionalContext / Agent-matcher / SubagentStop-no-additionalContext all CONFIRMED vs .5; DRIFT: PostCompact does NOT exist (routed reprime to SessionStart/cron); SubagentStart-additionalContext noted new. Checker = CAPTAIN_NOMOS (GROUND-TRUTH AUDITOR, no Agent/Write/WebFetch). Staging recommend: 2 stages (deterministic PreToolUse+Stop+install first; NOMOS+checker-trigger+re-decompose second). 4 self-assessed weak points (JSON-parse fail-open, author-allow-list, hook-can't-dispatch KEY LIMIT, missing-PostCompact). 5 residual Qs for ARGUS. Returning.",
      "timestamp": "2026-05-23T08:13:22Z"
    },
    {
      "text": "[from: argus-the-stoa] ARGUS activated on stoa--xyb.5 (Arc 46 enforcement-layer audit). Read design-rev1 + spec (.5/.1/.2) end-to-end. Priority: HARD SAFETY CONSTRAINT (can a hook arm in the LIVE session?). Next: independently web-verify the load-bearing hook-surface claims against cited code.claude.com/docs.",
      "timestamp": "2026-05-23T08:15:09Z"
    },
    {
      "text": "[from: argus-the-stoa] WEB-VERIFY pass 1 (code.claude.com/docs/en/hooks, live 2026-05-23) surfaces DRIFT vs design-rev1's own verification: live doc shows (a) PostCompact DOES exist (side-effect only, no decision control) — contradicts design §2 'no PostCompact event'; (b) Stop does NOT support additionalContext — CONFIRMS design's PARTIAL-DRIFT correction (rides reason); (c) PreToolUse CAN return additionalContext per live doc — design didn't claim this but it widens options; (d) matcher is tool-name-only, arg-matching is via the 'if' field w/ permission-rule syntax (Bash(git *)) — design's 'script inspects tool_input.command' still works but the cleaner 'if' idiom exists. Cross-checking SDK doc next; then classifying whether any drift is load-bearing.",
      "timestamp": "2026-05-23T08:15:40Z"
    }
  ],
  "priority": 2,
  "status": "in_progress",
  "title": "Enforcement layer: verified hook mapping + checker agent + bw-ground-truth detection + re-decompose remediation",
  "type": "task",
  "updated_at": "2026-05-23T08:15:40Z"
}
