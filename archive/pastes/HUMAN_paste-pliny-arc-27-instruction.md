Read .claude/MAJOR_PLINY.md and assume the orchestrator role for the-stoa.

Your immediate intent for this session: Build Arc 27 per the build directive at substrate/arcs/arc-27-build-directive.md. Four phases × one gauntlet × ~1-2 files edited (substrate/MAJOR_POLYBIUS.md primarily; possibly substrate/operating-disciplines.md cross-ref; possibly new substrate/templates/handoff-doc-template.md per A6 deliverable 4). The directive encodes the POLYBIUS session lifecycle discipline as substrate canon: three modes (handoff+compaction default; new POLYBIUS session for mechanism changes; relay-channel-indefinitely with decay-not-termination) + multi-artifact handoff shape + POLYBIUS-as-collective conceptual lens + Ariadne-search-ready authoring as forward context. Closes one ticket: stoa--32b.3 (P2; LOAD-BEARING — the work-unit ticket itself, no children, body + one fold-in comment carry full intent). Parent epic: stoa--32b (sibling children stoa--32b.1 + stoa--32b.2 are SEPARATE future arcs — A8 hard-locked out-of-scope for THIS arc).

**PRECONDITION:** Arc 26 (stoa--dxw) is CLOSED. ✓ Verified by user-tier POLYBIUS at dispatch authoring. You inherit post-Arc-26 main.

**Cron hygiene check FIRST (before any substantive work):** this session was /clear'd by PRINCIPAL to transition from Arc 26 → Arc 27. Crons survive /clear (session-level, not conversation-level per CronCreate tool description). Run `CronList` once; if any cron is present, CronDelete it — Arc 26 PLINY used surface-and-wait (no cron), but defense-in-depth: don't trust your /clear'd context on this. CronList is canonical. Then proceed with substantive work.

Operating mode for this dispatch: AUTONOMOUS. Run all four phases heads-down (Phase 1 DAEDALUS + ARGUS, Phase 2 ADA build on feature branch arc-27/build, Phase 3 VERA + CATO + ZENO parallel verification, Phase 4 smoke + PR + merge + close). Use stoa--32b.3 for status updates as you progress. Surface to PRINCIPAL ONCE at end-of-arc with the final clean-PASS verdict for ship/no-ship. Mid-arc surfacing is reserved for the autonomous escalation triggers only: substance disagreement after one round of bw exchange, authorship/copyright/Denson-final-say content surfaces, irreducible ambiguity, POLYBIUS silent > 60 min, smoke-test failures that require POLYBIUS guidance per the directive's surface-back wording.

Coordination: the-stoa PROJECT-TIER POLYBIUS is your radio-check peer for this arc (activated in a separate Claude Code session at the same time as you, paste at HUMAN_paste-polybius-arc-27-instruction.md). User-tier POLYBIUS dispatched the arc + will do QA pass at arc end per PRINCIPAL's 2026-05-16 pattern ("hand off to the full team with their own polybius to do the changes and you check for mistakes at the end"). User-tier is exception-handler alongside PRINCIPAL during the arc; QA seat at close. Bidirectional radio-check pattern applies per substrate/operating-disciplines.md §7. On dispatch, post an initialization handshake comment on stoa--32b.3 confirming you've read the directive + verified Arc 26 closed + naming your polling cron id (if you choose to set one). Heartbeat one-line state on stoa--32b.3 ("Phase 1 DAEDALUS dispatched" / "ARGUS PASS — ADA dispatched" / etc.). Closure handshake on stoa--32b.3 close.

The architectural decisions A1-A8 are LOCKED in the directive — you do NOT surface those as design questions. DAEDALUS treats stoa--32b.3's body + the one fold-in comment as primary input prose alongside the directive. Phase A locked decisions encode: one-arc one-gauntlet (A1), three lifecycle modes with PRINCIPAL's exact phrasing where load-bearing (A2), multi-artifact handoff shape enumerated (A3), Ariadne-readiness as forward authoring discipline (A4), POLYBIUS-as-collective lens with DAEDALUS-discretion on rendering location (A5), deliverables sketch with DAEDALUS scoping specifics (A6), authorship immutable (A7), out-of-scope hard-locked including the two sibling children (A8).

**Self-referential note for this arc:** you are encoding the discipline that governs how POLYBIUS sessions operate. The canon you write will be read by the very seats that build it. Same self-referential dynamic as Arc 24 (heartbeat-discipline edit) and Arc 25 (credential-discipline edit by agents that handle credentials). Substrate working as designed; build heads-down.

**CATO is MANDATORY** for this arc (not optional). Wording-precision in substrate canon matters; future POLYBIUSes read this for life-of-the-substrate. CATO checks PRINCIPAL's exact phrasing per A2, §15 N=1 honesty, scope discipline, and authorship attribution.

Authorship attribution is IMMUTABLE per substrate/CLAUDE.md: all edits credit Denson Smith. No exception. Arc 27 edits existing MAJOR_POLYBIUS.md (no fresh author-like field exposure there). Possibly adds new substrate/templates/handoff-doc-template.md as A6 deliverable 4 — if so, check the `author:` field (or any frontmatter) before commit. Special care: the new template (if authored) is a fresh file; default-templated scaffolds often LLM-fill with placeholder names that look right but aren't. Verify before commit.

**Phase 4 close handshake:** in addition to standard closure, tag a [for: user-tier POLYBIUS] comment on stoa--32b.3 inviting QA pass. User-tier will read diff + cross-refs + scope + authorship; surfaces findings to project-tier POLYBIUS via comment for fix-now items, or to PRINCIPAL via push-notification for project-direction escalations.

Check beadwork (stoa-- prefix) for pending directives from MAJOR_POLYBIUS — start with: stoa--32b.3 (the work-unit ticket; substrate/arcs/arc-27-build-directive.md is its load-bearing artifact, on disk at dispatch authoring time, will commit as part of arc-27/build feature branch).

If compaction or /clear erases your role, re-read this paste from HUMAN_paste-pliny-arc-27-instruction.md in the project root.
