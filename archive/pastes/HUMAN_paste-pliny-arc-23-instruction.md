Read .claude/MAJOR_PLINY.md and assume the orchestrator role for the-stoa.

Your immediate intent for this session: Build Arc 23 per the build directive at substrate/arcs/arc-23-build-directive.md (commit 0d097c9 on origin/main, ~255 lines, 5 Phases × one gauntlet × ~30 substrate-prose deliverables across ~9 files). The directive lands the verification-complexity framework (2x2 quadrant classification + INCOMPLETE / UNVERIFIABLE verdict shapes) as the load-bearing scaffolding, plus seven supporting substrate-canon updates surfaced during the 2026-05-12 bw → Ariadne integration + STRABO fabrication + bw scaling-wall cluster. Closes eight tickets: stoa--4h7 (bw upgrade), stoa--tp1 (LOAD-BEARING — verification-complexity), stoa--fea (VERA verifies STRABO), stoa--nax (redundant-checks + N=1 + estimate-axes), stoa--148 (CATO empirical-env + probe-fallback), stoa--vmc (bw-fit matrix + layered-architecture), stoa--rno (fork-over-upstream + agent-time-latency), stoa--14u (substrate-edit smoke beats). Parent epic: stoa--pmp.

Operating mode for this dispatch: AUTONOMOUS. Run all five phases heads-down (Phase 0 bw upgrade, Phase 1 DAEDALUS + ARGUS, Phase 2 ADA build on feature branch arc-23/build, Phase 3 VERA + CATO + ZENO parallel verification, Phase 4 smoke + PR + merge + close). Use the Arc 23 epic stoa--pmp for status updates as you progress. Surface to PRINCIPAL ONCE at end-of-arc with the final clean-PASS verdict for ship/no-ship. Mid-arc surfacing is reserved for the autonomous escalation triggers only: substance disagreement after one round of bw exchange, authorship/copyright/Denson-final-say content surfaces, irreducible ambiguity, POLYBIUS silent > 60 min, smoke-test failures that require POLYBIUS guidance per the directive's surface-back wording.

Coordination: POLYBIUS is monitoring stoa--pmp. Bidirectional radio-check pattern applies per substrate/operating-disciplines.md §7. On dispatch, post an initialization handshake comment on stoa--pmp confirming you've read the directive + naming your polling cron id (set up your own via CronCreate if you want phase-transition pickup; otherwise heartbeat every ≤30 min unsolicited). Heartbeat one-line state on stoa--pmp ("Phase 0 complete — bw 0.13.0 installed" / "DAEDALUS surfaced design.md for ARGUS audit" / "ARGUS PASS — ADA dispatched" / "Phase 3 VERA + CATO + ZENO returned CLEAN" / etc.). Closure handshake on stoa--pmp close.

The eight child tickets are wired as 'blocks' deps on stoa--pmp. `bw show stoa--pmp` lists them. `bw show stoa--tp1` has a long POLYBIUS elevation comment (2026-05-12 18:24) that DAEDALUS treats as additional spec. Phase A architectural decisions A1-A11 are LOCKED pre-dispatch in the directive — you do NOT surface those as design questions.

Authorship attribution is IMMUTABLE per substrate/CLAUDE.md: all edits credit Denson Smith. No exception.

Check beadwork (stoa-- prefix) for pending directives from MAJOR_POLYBIUS — start with: stoa--pmp (the parent Arc 23 epic; the build directive at substrate/arcs/arc-23-build-directive.md is its load-bearing artifact).

If compaction or /clear erases your role, re-read this paste from HUMAN_paste-pliny-arc-23-instruction.md in the project root.
