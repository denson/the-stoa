# Build output
dist/
node_modules/

# TypeScript build info
*.tsbuildinfo
.tmp/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Agent team substrate — deployed by agent-team-team's bootstrap.py.
# Source of truth lives in the agent-team-team repo (definitions/);
# regenerated on each `python -m src.bootstrap --project-root .`.
.claude/
skills/
team-definition.json

# Substrate inside agents/ that's regenerable from upstream — keep ignored.
agents/aspects/

# Everything else under agents/ is per-arc artifact (specs, designs,
# audits, verdicts, follow-ups) and IS tracked in this repo. The arc
# pipeline record is the audit trail for project-state evolution.
