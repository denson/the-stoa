/design/

# Claude Code runtime locks (the-stoa runs its own team from .claude/, deployed from substrate/)
.claude/scheduled_tasks.lock

# Stoa install.sh single-shot backup of CLAUDE.md (overwritten each --modify-claude-md run)
CLAUDE.md.bak

# Python bytecode cache (introduced Arc 39 with save-verdict skill — first Python in substrate/)
__pycache__/
*.pyc

# Agent run-output SCRATCH — only the throwaway probe/test runs + tmp authoring files.
# The real per-ticket arc artifacts (agents/{design,verdicts,verification,save-verdict}/stoa--*) ARE tracked + pushed.
agents/**/r3test/
agents/**/zzz/
agents/**/ttt/
agents/**/tkt-A/
agents/**/tkt-B/
agents/**/tkt-wt/
agents/**/_body-*.tmp.md

# user-tier POLYBIUS housekeeping (Arc 61 pre-branch hygiene, 2026-06-17) — keep the-stoa main tree clean.
# HANDOFF_*/HUMAN_paste-*/PLAN_* tracked home is the beadwork branch (attach to a ticket), NOT main (§18).
/HANDOFF_*.md
/HUMAN_paste-*.md
/PLAN_*.md
# session backup dirs + Stop-hook runtime sentinels (runtime/throwaway)
.claude.bak-*/
.claude/hooks/.stop-sentinels/
# stray data file (not a repo artifact)
/LA_sea_level_data.csv
