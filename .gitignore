/design/

# Claude Code runtime locks (the-stoa runs its own team from .claude/, deployed from substrate/)
.claude/scheduled_tasks.lock

# Stoa install.sh single-shot backup of CLAUDE.md (overwritten each --modify-claude-md run)
CLAUDE.md.bak

# Python bytecode cache (introduced Arc 39 with save-verdict skill — first Python in substrate/)
__pycache__/
*.pyc

# Agent run-output SCRATCH — only the throwaway probe/test runs + tmp authoring files.
# The real per-ticket arc artifacts (agents/{design,verdicts,verification,save-verdict}/stoa--*) ARE tracked + pushed.
agents/**/r3test/
agents/**/zzz/
agents/**/ttt/
agents/**/tkt-A/
agents/**/tkt-B/
agents/**/tkt-wt/
agents/**/_body-*.tmp.md
