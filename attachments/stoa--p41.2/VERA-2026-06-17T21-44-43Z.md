status: completed
ticket: stoa--p41.2
verdict: pass
design_artifact_verified_against: agents/design/arc-63/design-rev2.md (§3 probe set)
build_verified: arc-63/build @ d08aef0585641742a74906b114bb7d7328b4caff (worktree …/arc-63-build), claude --version 2.1.170

probes_executed:
- probe_id: P-EMPIRICAL
  description: SessionStart type:"command" startup hook's additionalContext reaches the model on local v2.1.170, asserted on the DETERMINISTIC surface (debug log + transcript JSONL), model-echo soft secondary only.
  quadrant_classification: easy-hard (channel fires obviously; proving the additionalContext truly reaches model context is the hard half — answered deterministically via the transcript hook_additional_context attachment rather than fragile model-echo).
  command_or_method: mktemp throwaway harness; sentinel-emitting SessionStart startup hook + throwaway settings.json (NO live settings.json touched); `cd $TMP && claude --debug-file $TMP/dbg.log -p "reply READY" --max-turns 1`; grep sentinel in dbg.log + cwd-keyed transcript JSONL.
  expected: (a) debug log shows the SessionStart hook's additionalContext extracted/injected AND/OR (b) transcript JSONL records the injected context, independent of model echo.
  observed: PRIMARY PASS on BOTH surfaces. (a) dbg.log: "Hook SessionStart:startup (SessionStart) success" + "Hook SessionStart (…/probe-sessionstart.sh) provided additionalContext (71 chars)". (b) transcript JSONL: a record with `"type":"hook_additional_context","content":["STOA_PROBE_SENTINEL_7F3A is the sentinel token present in this context."]` — Claude Code v2.1.170 ingested the additionalContext as a context attachment. SECONDARY (model-echo) could NOT run: default model "claude-fable-5" unavailable in throwaway env → model-call exit 1. Per probe spec a SECONDARY miss with a PRIMARY pass is an overall PASS; the model error is downstream of (and irrelevant to) the channel test, which completed BEFORE the model call. Web-verified (gsearch) the v2.1+ behavior: SessionStart startup/resume additionalContext is silently appended to the model's active context window — matches the observed hook_additional_context attachment.
  result: pass
- probe_id: P-FALLBACK / F1-READER
  description: drift-signal file is WRITTEN by the hook on drift and READ by a real non-additionalContext carrier (stop-self-check.sh clause D on the Stop reason); silent-when-current; fail-open; dual-copy identical.
  quadrant_classification: easy-easy (direct script execution with seeded signal; PASS/FAIL on the emitted JSON).
  command_or_method: mktemp throwaway; seed .claude/.substrate-drift-signal; pipe a Stop event JSON to stop-self-check.sh; assert reason carries clause (D) with the signal body, decision:block, NO additionalContext key; re-fire with no signal (fresh session_id); re-fire with missing cwd; diff dual copies.
  expected: signal body surfaces under clause (D) via decision:"block"+reason (no additionalContext); graceful no-op A/B/C when no signal; exit 0 on missing cwd; byte-identical copies.
  observed: ALL 5 sub-checks PASS. (2) clause (D) present with signal body "substrate drift: …", "decision":"block" present, NO additionalContext key. (3) no signal → clause (D) absent, A/B/C intact. (4) missing cwd → exit 0, valid block, no abort. (5) `diff substrate/hooks/stop-self-check.sh .claude/hooks/stop-self-check.sh` byte-identical. The drift signal reaches the model on a channel structurally independent of additionalContext (the channel P-EMPIRICAL tests).
  result: pass
- probe_id: p-nosettings (design §3 name: NOSETTINGS)
  description: no build step writes a live settings.json (THREAT-ANCHORED for M1); legit operator arming intact.
  quadrant_classification: easy-easy.
  command_or_method: `git diff --name-only b5de0aa..HEAD`; grep changed-files for settings.json; grep repo for live settings.json with "hooks" (exclude templates); confirm pre-existing tracked .claude/settings.json unchanged by arc and does NOT register the new sessionstart-substrate-check hook.
  expected: only the design's expected file set changed; no live settings.json introduced/armed by this arc; --enable-hooks runbook still printed.
  observed: 28 changed files, all within the NOSETTINGS expected set; the only settings.json matches are settings-hooks.json under templates/ (both copies). The pre-existing tracked .claude/settings.json is UNCHANGED by the arc (empty b5de0aa..HEAD diff) and contains 0 references to the new sessionstart-substrate-check hook (it registers only the prior compact-reprime SessionStart + Stop). ATTACK BLOCKED: arc arms no new live settings.json hook. LEGIT UNAFFECTED: --enable-hooks operator arming path intact (candidate template only).
  result: pass
- probe_id: F2-MOOT
  description: check.sh ../.. state-file anchor unchanged; dirs still at substrate/skills/check-*/.
  quadrant_classification: easy-easy.
  command_or_method: grep anchor lines in both check.sh.
  expected: both scripts compute SCRIPT_DIR/../.. and dirs unmoved.
  observed: check-substrate-updates/check.sh:35 `SUBSTRATE_DIR="$(cd "${SCRIPT_DIR}/../.." && pwd)"`; check-bw-release/check.sh:46 `SKILLS_PARENT_DIR="$(cd "${SCRIPT_DIR}/../.." && pwd)"`. No anchor edit; dirs unmoved (F2 moot by construction).
  result: pass
- probe_id: F3-MOOT
  description: hook resolves check.sh at unchanged skills path (consumer .claude/skills/ + substrate/skills/ dogfood fallback); no maintenance/ path.
  quadrant_classification: easy-easy.
  command_or_method: grep hook (both copies) for the check.sh paths.
  expected: .claude/skills/check-*/check.sh consumer + substrate/skills/check-*/check.sh fallback; both exist post-arc.
  observed: both copies L107-110 resolve consumer .claude/skills/check-*/check.sh with substrate/skills/check-*/check.sh fallback (§2.1 EDIT 1); no maintenance/ path (the only "maintenance" match is a comment word). Both target paths exist on disk.
  result: pass
- probe_id: G-a
  description: TRAP CLOSED — gen-data renders NO check-* LIEUTENANT; gauntlet-setup present.
  quadrant_classification: easy-easy.
  command_or_method: `npm run gen-data`; grep generated agents.ts (real path app/src/data/generated/agents.ts per ADA's flagged drift) for a roster skill name = check-*.
  expected: no LIEUTENANT skill named check-substrate-updates/check-bw-release; gauntlet-setup present.
  observed: gen-data exit 0; roster 4 MAJOR / 12 CAPTAIN / 10 LIEUTENANT. NO `"name":"check-substrate-updates"` or `"name":"check-bw-release"` roster entry (the 5 grep hits are prose body refs in other agents). gauntlet-setup IS a roster skill (L280). Trap closed at the gen-data layer via SKILL.md deletion.
  result: pass
- probe_id: G-b
  description: CAPABILITY PRESERVED — check + apply/revert land in a consumer .claude/skills/ via the carve-out, not pruned.
  quadrant_classification: easy-easy.
  command_or_method: install.sh --target project --dry-run against mktemp parent (4 grep checks); + real non-dry-run deploy against a 2nd mktemp; ls the landed dirs.
  expected: carve-out deploys both check dirs to consumer .claude/skills/ carrying check.sh+apply.sh+revert.sh; gauntlet via SKILL_NAMES loop; check dirs not flagged obsolete/prune.
  observed: dry-run exit 0; (i) check-substrate-updates carve-out deploy planned; (ii) check-bw-release carve-out deploy planned; (iii) gauntlet-setup deploys via skill loop; (iv) neither check dir flagged obsolete/prune. Real deploy: check-substrate-updates landed apply.sh+check.sh+revert.sh; check-bw-release landed check.sh; NO SKILL.md in either (tool-dir, not skill); NO live settings.json deployed without --enable-hooks.
  result: pass
- probe_id: F1-MOOT-CHECK / NOSTALE-LIEUTENANT
  description: both check dirs present on disk, neither carries SKILL.md, scripts retained.
  quadrant_classification: easy-easy.
  command_or_method: ls both dirs.
  expected: dirs present, no SKILL.md, check.sh (both) + apply.sh + revert.sh (substrate-updates).
  observed: check-substrate-updates/ = apply.sh check.sh revert.sh (no SKILL.md); check-bw-release/ = check.sh (no SKILL.md). On-disk layer confirms the trap-closing deletion + capability retention.
  result: pass
- probe_id: APPGREEN
  description: Stoa app stays green on the FULL suite (regen re-derives whole roster).
  quadrant_classification: easy-easy.
  command_or_method: `cd app && npm run gen-data && npm run build && npm test`.
  expected: gen-data 0, build 0, full npm test green, LIEUTENANT non-empty at -1, gauntlet-setup parses clean.
  observed: gen-data 0; build 0 (vite built in 3.69s); npm test 41 passed (3 files). LIEUTENANT slot = 10 (was 11, net -1 per directive). gauntlet-setup frontmatter parses clean (rostered at L280 — the description:| block-scalar conversion ADA flagged works against js-yaml). No parse error.
  result: pass
- probe_id: SKILLNAMES
  description: net -2 +1 = 9 SKILL_NAMES entries.
  quadrant_classification: easy-easy.
  command_or_method: grep SKILL_NAMES block in install.sh.
  expected: 9 entries; both check skills absent; gauntlet-setup + team-launcher present.
  observed: 9 entries (credential-discipline, inspect-script-output, handoff-author, save-verdict, validate-spec, workflow-composer, interactive-html-preview, team-launcher, gauntlet-setup); both check skills absent; gauntlet-setup + team-launcher present.
  result: pass
- probe_id: LANDING
  description: gauntlet-setup deploys to a consumer .claude/skills via the SKILL_NAMES loop; no check skill via that loop.
  quadrant_classification: easy-easy.
  command_or_method: install.sh --target project --dry-run grep for deploy skill: gauntlet-setup; confirm check skills only via carve-out.
  expected: gauntlet-setup planned via skill loop; check skills NOT via skill loop.
  observed: "[dry-run] deploy skill: …/gauntlet-setup/ -> …/.claude/skills/gauntlet-setup/"; check skills appear only as the carve-out "deploy operator-tool (carve-out, non-SKILL_NAMES)", NOT via the deploy-skill loop. Real deploy landed gauntlet-setup/SKILL.md.
  result: pass
- probe_id: RECOMPOSE
  description: REAL non-dry-run subproject recompose passes Checks A-E (Arc-61 lesson — dry-run early-returns before the awk FAIL-LOUD).
  quadrant_classification: easy-hard (an obvious abort surfaces immediately, but a full non-dry-run run is required to exercise the awk Checks A-E — bounded by a single real recompose against a throwaway parent).
  command_or_method: `bash substrate/install.sh --target subproject --parent-dir $PARENT --subproject probe63` (NO --dry-run) against mktemp parent.
  expected: exit 0, no err()/FAIL-LOUD abort, role files recomposed.
  observed: exit 0; "install.sh: done (applied)"; OWNED module bodies re-inlined into MAJOR_POLYBIUS/PLINY/CHIRON/operating-disciplines/DAEDALUS (full recompose path executed, not early-returned); no FAIL-LOUD / err() / abort. This arc adds no owned module → recompose unaffected, confirmed via the real run.
  result: pass
- probe_id: GITIGNORE
  description: hook transients ignored in install.sh template + self-deployed .claude/.gitignore.
  quadrant_classification: easy-easy.
  command_or_method: grep transient names in install.sh + .claude/.gitignore.
  expected: both .substrate-check-hook-stamp + .substrate-drift-signal in the install.sh gitignore template AND self-deployed .claude/.gitignore.
  observed: install.sh L596-597 (heredoc) + L573 (dry-run echo); .claude/.gitignore L19-20. Both names present in both layers.
  result: pass
- probe_id: XREF
  description: no dangling skill-path reference (regression-guard under Option C — dirs did not move); R1(b) false skill-invocation prose removed; no dangling deleted-SKILL.md ref.
  quadrant_classification: easy-easy.
  command_or_method: grep skills/check-* refs across substrate/ + .claude/ (.md, excl arcs/ + design docs); verify each path exists on disk; grep for false-invocation prose + deleted-SKILL.md refs.
  expected: every check-* path resolves; zero "invoke via the Skill tool" / "via the check-bw-release skill" prose; zero refs to deleted SKILL.md.
  observed: all skills/check-* refs point at substrate/skills/check-substrate-updates|check-bw-release — both EXIST on disk (no dangling). R1(b) reworded prose present (substrate-update-check.md:12, bw-upgrade.md:15/47/59 + .claude/ copies now say "run check.sh directly / fires via the SessionStart hook … not a Skill-tool skill"). ZERO false-invocation prose; ZERO refs to deleted SKILL.md.
  result: pass

threat_coverage:
- mitigation: M1 (live-settings.json auto-write — gate-origin, ratified)
  threat: T-arm-live-settings (a build/install/hook-registration step writes/merges a hook block into a live .claude/settings.json, arming gates into the running session — no-experiments-on-real-agents violation)
  defeats_via_probe: p-nosettings (design §3 name: NOSETTINGS)
  probe_evidence: agents/verdicts/stoa--p41.2 verdict body p-nosettings/NOSETTINGS observed block; verification artifacts at the path below (git diff b5de0aa..HEAD changed-file scan + live-settings.json grep + pre-existing-settings.json unchanged-by-arc check).
  attack_path_exercised: Drove the attack path = "did any arc step write/arm a live settings.json hook?" via the changed-files scan + repo-wide grep for live settings.json carrying "hooks". (a) ATTACK BLOCKED — only settings-hooks.json under templates/ changed; the pre-existing tracked .claude/settings.json is unchanged by the arc and registers 0 references to the new sessionstart-substrate-check hook; the new hook ships INERT as a candidate. (b) LEGIT UNAFFECTED — install.sh --enable-hooks operator arming runbook still prints; operator-gated arming (DEFAULT OFF) intact.

methodology_concerns: |
  (1) P-EMPIRICAL SECONDARY (model-echo) could not execute: the default model in the throwaway env ("claude-fable-5") returned a model-access error, so the model-call leg failed (exit 1). This is irrelevant to the PRIMARY deterministic gate (the hook subsystem completed and recorded the additionalContext injection BEFORE the model call), and the probe spec explicitly treats a SECONDARY miss with a PRIMARY pass as an overall PASS. No impact on verdict; noted for transparency.
  (2) Cosmetic line-citation drifts in rev2 §3 vs ship reality, ALL pre-flagged by ADA, none defects: hard-abort actual L824-827, prune-scan custom-* skip actual L1725, gen-data output path actual app/src/data/generated/agents.ts (probe text said app/src/data/agents.ts). I built/ran every affected probe against ship reality; results unaffected.
  (3) The G-a probe text greps for any check-* substring in the generated roster, which returns 5 prose-body false-positives; the load-bearing assertion is "no roster skill whose NAME field is check-*", which I asserted directly (anchored grep `"name":\s*"check-(substrate-updates|bw-release)"` → zero). Per §5.11 (regex-anchoring), the substring grep alone would be a misleading count; the anchored name-field grep is the honest check. Non-blocking — the design's intent is unambiguous and the anchored check passes.

falsifying_evidence_summary:

verification_artifacts_path: probes executed inline in this session against mktemp throwaway dirs (all torn down post-run); no persistent artifact tree written. Recorded probe outputs (debug-log injection lines, transcript hook_additional_context attachment, Stop-reason clause-D JSON, install.sh dry-run + real-deploy listings, recompose output, app full-suite results) are pasted verbatim in the probes_executed: observed fields above.

summary: |
  Verified ADA's build d08aef0 on arc-63/build against design-rev2 §3's full 14-probe set. Every probe PASSES. The two directive-mandated load-bearing probes both pass on independent channels: P-EMPIRICAL proves on local v2.1.170 — via the DETERMINISTIC surface (debug log "provided additionalContext (71 chars)" + a transcript JSONL "hook_additional_context" attachment carrying the sentinel) — that a SessionStart type:"command" startup hook's additionalContext is ingested into the model's context (web-verified against current v2.1 docs); the model-echo secondary couldn't run only because the throwaway env's default model was unavailable, which is downstream of and irrelevant to the channel test. P-FALLBACK/F1-READER proves the drift-signal file is written by the hook and read by stop-self-check.sh clause (D) onto the Stop reason — a carrier structurally independent of additionalContext — with graceful no-op, fail-open, and byte-identical dual copies. The M1 threat (live settings.json auto-write) is defeated by NOSETTINGS: the arc arms no live settings.json hook (the new hook ships inert as a candidate; the pre-existing tracked settings.json is unchanged and references the new hook zero times) while operator --enable-hooks arming stays intact. Option C is sound: G-a confirms the gen-data trap closed (no check-* LIEUTENANT, gauntlet-setup rostered), G-b confirms capability preserved (carve-out deploys check.sh+apply.sh+revert.sh to a consumer with no SKILL.md, not pruned), F2/F3 moot by construction, XREF clean with R1(b) prose reworded. App is fully green (gen-data 0, build 0, 41 tests, LIEUTENANT -1, gauntlet-setup frontmatter parses clean), and a REAL non-dry-run subproject recompose passes Checks A-E. No FAIL, no INCONCLUSIVE, no live settings.json touched anywhere.
