status: completed
ticket: stoa--p41.2
verdict: pass
design_artifact_verified_against: agents/design/arc-64/design-rev2.md (fold delta over design-rev1.md)
build_verified: arc-64/build @ 56e20bb1493ded6c92471f46889c3fd602270946
probes_executed:
- probe_id: P1
  description: attach-at-write REAL (load-bearing, NET-NEW Arc-62 verdict-loss fix) — verdict bytes present ON the beadwork orphan branch, sha256-match to disk.
  quadrant_classification: easy-easy
  command_or_method: drove the full byte-aligned procedure for a synthetic verdict (printf author w/ apostrophe -> inline sha256 round-trip -> bw attach stoa--p41.2 "$DEST" --name verdicts/SYNTH-<ts>.md); then independently `git show beadwork:attachments/stoa--p41.2/verdicts/SYNTH-<ts>.md | sha256sum` vs on-disk sha256sum.
  expected: attach exit 0; bytes committed to beadwork branch; beadwork sha256 == on-disk sha256.
  observed: attach exit 0; file present at beadwork:attachments/stoa--p41.2/verdicts/SYNTH-2026-06-18T02-19-48Z.md; on-disk sha256 b2d5c2fd0f08ab54d70b9c36a59a2737361096c556dc9413ece53ac3efb8d1ee == beadwork-branch sha256 b2d5c2fd0f08ab54d70b9c36a59a2737361096c556dc9413ece53ac3efb8d1ee; apostrophe ("VERA's") survived intact. NOT asserted from "the module says it attaches" — exercised end-to-end and re-hashed off the orphan branch tree.
  result: pass
- probe_id: P2
  description: no-Write-seat authoring — Bash-only printf with embedded apostrophe escaped '\'' produces literal apostrophe + sha256 MATCH; dest-exists clobber guard (r6).
  quadrant_classification: easy-easy
  command_or_method: printf '%s' 'verdict: pass — VERA'\''s ... seat'\''s job' > DEST; sha256 round-trip; grep literal apostrophe. dest-guard: write to existing dest w/o override (expect exit 3, ORIGINAL preserved), then SAVE_VERDICT_OVERWRITE=1 (expect overwrite).
  expected: literal apostrophe in file; sha256 MATCH; exit 3 + ORIGINAL preserved w/o override; overwrite with SAVE_VERDICT_OVERWRITE=1.
  observed: on-disk content "verdict: pass — VERA's falsification check, the seat's job"; sha256 MATCH (eec4cfb0...); dest-guard exit 3 with ORIGINAL preserved; override -> NEW BODY. The '\'' escape is correct; no Write/Edit tool used.
  result: pass
- probe_id: P3
  description: sha256 negative — corrupt the on-disk verdict between write and re-hash (append a byte); inline round-trip exits 2 with mismatch diagnostic.
  quadrant_classification: easy-easy
  command_or_method: printf body > DEST; printf 'X' >> DEST (corrupt); WANT=$(printf body|sha256sum); GOT=$(sha256sum DEST); [ WANT = GOT ] || exit 2. Plus clean-write control (expect exit 0 MATCH).
  expected: corruption -> exit 2 + "SAVE-VERDICT FAIL: sha256 mismatch want=... got=..."; clean write -> exit 0 MATCH.
  observed: corruption -> exit 2, diagnostic "SAVE-VERDICT FAIL: sha256 mismatch want=22fbb41a... got=b21993b8..."; clean control -> exit 0 MATCH (want=22fbb41a... == got). Negative probe falsifies the happy path correctly.
  result: pass
- probe_id: P4
  description: threat-coverage empty-binding guard (Q-A preserved) + j2i lowercase-p fix + INLINE-block placement (r8) physically in three role files.
  quadrant_classification: easy-easy
  command_or_method: ran the verbatim empty-binding if-block from the byte-aligned region — Case A TRM_COUNT=1 empty THREAT_PROBE_IDS; Case B THREAT_PROBE_IDS=P-INJ (uppercase); Case C x9 (malformed); Case D TRM_COUNT=0 control. Then grep '\^[pP][0-9A-Za-z._-]+\$' in CAPTAIN_VERA/ARGUS/CATO confirming each match sits BETWEEN that file's BYTE-ALIGNED sentinels.
  expected: A->exit 4; B->exit 0 PASS (proves j2i lowercase-p NOT reproduced); C->exit 4; D->exit 0; assert physically inline in all three role files.
  observed: A exit 4 ("...declared but no threat-coverage probe-ids"); B exit 0 PASS (uppercase P-INJ accepted — j2i fix confirmed); C exit 4 ("probe-id 'x9' malformed (must match ^[pP][0-9A-Za-z._-]+$)"); D exit 0. grep: 1 match each in VERA(L299)/ARGUS(L280)/CATO(L245), each between BEGIN/END sentinels — resolves at subproject tier where module is absent.
  result: pass
- probe_id: P5
  description: SKILL_NAMES -3 + no dangling pointers (merged into P10 below). SKILL_NAMES has exactly 6 entries.
  quadrant_classification: easy-easy
  command_or_method: read install.sh L228-235 SKILL_NAMES array; grep retired-skill paths in substrate/ app/ excluding arcs/ + v1-historical/.
  expected: 6 entries (credential-discipline, handoff-author, workflow-composer, interactive-html-preview, team-launcher, gauntlet-setup); no live retired-skill-menu invocations.
  observed: SKILL_NAMES = exactly those 6; none of save-verdict/validate-spec/inspect-script-output present. See P10 for the dangling-pointer detail.
  result: pass
- probe_id: P6
  description: no recompose (C2) — VERA/ARGUS/CATO NOT in the install.sh recompose ownership-partition; no MODULE-INLINE markers for the 3 new modules.
  quadrant_classification: easy-easy
  command_or_method: grep recompose_module_inline call sites in install.sh; grep MODULE-INLINE in the 3 verdict-seat role files.
  expected: recompose owners = {POLYBIUS, OPDISC, PLINY, CHIRON, DAEDALUS} only; zero MODULE-INLINE markers in VERA/ARGUS/CATO.
  observed: 5 recompose calls (POLYBIUS L1208, OPDISC L1209, PLINY L1210, CHIRON L1215, DAEDALUS L1223). Zero MODULE-INLINE markers in the 3 role files. VERA/ARGUS/CATO never appear as recompose targets (the lone "ARGUS" hit is an unrelated Check-E code comment). C2 confirmed.
  result: pass
- probe_id: P7
  description: app green + count (REAL gen-data regen) — full suite green; LIEUTENANT count reconciled; -3 delta real and not masking pre-existing drift.
  quadrant_classification: easy-hard
  command_or_method: cd app && npm run gen-data && npm run build && npm test; enumerated the LIEUTENANT roster group via tsx import of generated/agents.ts; compared SKILL.md dirs git ls-tree HEAD~1 vs HEAD.
  expected: gen-data/build/test all green; LIEUTENANT drops by 3; 6 SKILL_NAMES-backed + reconcile the gen-data "7"; generated.test.ts length>0 holds; no masked roster drift.
  observed: gen-data "4 MAJOR / 12 CAPTAIN / 7 LIEUTENANT"; build green; 41/41 tests pass. LIEUTENANT roster group = 7 skills: credential-discipline, decision-surface, gauntlet-setup, handoff-author, interactive-html-preview, team-launcher, workflow-composer. RECONCILED: 7 = 6 SKILL_NAMES-backed + decision-surface (pre-existing SKILL.md dir, NOT in SKILL_NAMES; gen-data is directory+SKILL.md driven, not SKILL_NAMES driven). DELTA: parent HEAD~1 had 10 SKILL.md dirs -> HEAD 7 = exactly -3, and the diff is PRECISELY {save-verdict, validate-spec, inspect-script-output} (the intended trio). decision-surface existed in HEAD~1 (pre-existing, not new drift). Fresh regen byte-identical to committed agents.ts (git status clean) — no stale checked-in data, no masked drift. generated.test.ts L101 expect(slot.skills.length).toBeGreaterThan(0) holds (7>0).
  result: pass
- probe_id: P8
  description: byte-alignment — dest-guard -> printf -> sha256 -> empty-binding assert -> bw attach region byte-identical across CAPTAIN_VERA §7 + CAPTAIN_ARGUS §7 + CAPTAIN_CATO §7 + modules/save-verdict.md.
  quadrant_classification: easy-easy
  command_or_method: sed-extract the region between SAVE-VERDICT-BYTE-ALIGNED-REGION:BEGIN/END sentinels in all four files; diff pairwise (VERA vs ARGUS, vs CATO, vs module).
  expected: all four byte-identical (35 lines each), no drift.
  observed: VERA vs ARGUS IDENTICAL; VERA vs CATO IDENTICAL; VERA vs MODULE IDENTICAL; 35 lines each. The empty-binding assert AND the dest-exists guard are both inside the diffed region.
  result: pass
- probe_id: P9
  description: install.sh carve-out + prune-exemption (r1/r2) — CARVEOUT_SKILL_DIRS + prune-exemption contain BOTH validate-spec AND inspect-script-output; save-verdict in NEITHER; dry-run deploys correctly.
  quadrant_classification: easy-easy
  command_or_method: grep CARVEOUT_SKILL_DIRS (L1308) + prune-exemption case (L1796); bash substrate/install.sh --target user --dry-run; filter for the 3 names + modules + prune.
  expected: both lists contain validate-spec + inspect-script-output, not save-verdict; dry-run exit 0; 3 modules deploy via glob; 2 retained script dirs deploy via carve-out + not prune-flagged; save-verdict fully absent as a skill.
  observed: CARVEOUT_SKILL_DIRS=(check-substrate-updates check-bw-release validate-spec inspect-script-output); prune-exemption L1796 check-substrate-updates|check-bw-release|validate-spec|inspect-script-output) continue ;; — both contain the two, NEITHER contains save-verdict. dry-run exit 0; validate-spec + inspect-script-output deploy via operator-tool carve-out (cp -R + pycache cleanup); save-verdict.md + validate-spec.md + inspect-script-output.md deploy via the modules glob (40 modules); save-verdict NEVER deploys as a skill/operator-tool; no prune-flag for the retained dirs.
  result: pass
- probe_id: P10
  description: no dangling skill pointers (r3/r4 + extends rev1 P5) — opdisc L590 + PLINY §5.14 repointed to module; no --cwd skill-contract sentence; no live retired-skill-menu refs / deleted-file pointers.
  quadrant_classification: easy-easy
  command_or_method: grep retired-skill paths in substrate/ app/ excluding arcs/ + v1-historical/; classify each match; grep opdisc L590 + PLINY 5.14 for module pointer + absence of --cwd / SKILL.md.
  expected: opdisc L590 + PLINY 5.14 point at .claude/modules/save-verdict.md; no --cwd skill-contract sentence; remaining matches are intended (provenance note + retained check.sh operator-tool refs), none a live menu invocation or dangling deleted-file pointer.
  observed: opdisc L590 points at .claude/modules/save-verdict.md (no skill SKILL.md ptr); PLINY §5.14 L256 points at the module path-convention, no --cwd / "cwd contract" sentence; §5.16 attach hand-back section present. Sole save-verdict/ match is the module's own provenance note (history, not a live pointer). All validate-spec/inspect-script-output matches resolve to the RETAINED check.sh operator tool. Zero references to the removed SKILL.md menu entries; zero _save_verdict.py refs.
  result: pass
threat_coverage:
methodology_concerns: |
  None of consequence. Two observations (non-blocking, field notes):
  (1) The §35.5 process-hardening carve-out is correct for this arc — no threat-ratified mitigation, so threat_coverage: is legitimately empty (the §35.5-classified empty-state). I did NOT manufacture a threat-anchored probe. This empty list is the valid empty-state, not an absence-of-an-executed-probe finding.
  (2) W2 (the four-copy SSoT guarded only by the manual build-time P8 diff, no regen-time/CI alignment test) is a real documented C2-residual the design acknowledges (r7). P8 is green at this commit; the residual is that a FUTURE edit between P8 runs could drift. Out of scope for this arc per design §12; recorded here so the loop is durable. Not a falsification of this build.
falsifying_evidence_summary:
verification_artifacts_path: agents/verification/stoa--p41.2/ (p3-sha256-negative.sh, p3-clean.sh, p4-empty-binding.sh, p-dest-guard.sh + the synthetic P1 verdict at agents/verdicts/stoa--p41.2/SYNTH-2026-06-18T02-19-48Z.md attached to beadwork)
summary: |
  All 10 probes (P1-P10, rev2 §9 superseding rev1 §3 for P4/P7/P8 + adding P9/P10) executed against the built deliverable at 56e20bb and PASS. The load-bearing net-new probe P1 (attach-at-write, the Arc-62 verdict-loss fix) was exercised end-to-end: a synthetic verdict driven through the byte-aligned procedure landed its BYTES on the beadwork orphan branch and the on-branch bytes sha256-match the on-disk file (b2d5c2fd...) — durable, not asserted. The Q-A keystone P4 confirms the j2i lowercase-p bug is NOT reproduced (uppercase P-INJ passes, malformed x9 + empty-with-TRM>0 exit 4) AND the empty-binding assert lives physically inline in all three verdict-seat role files (resolves at subproject tier where the module is absent). P8 byte-alignment is exact across all four homes (35 lines, dest-guard + printf + sha256 + empty-binding + attach). P7 (real gen-data regen) is fully reconciled: -3 LIEUTENANT delta is real (10->7 SKILL.md dirs, precisely the retired trio), gen-data's "7" = 6 SKILL_NAMES-backed + decision-surface (pre-existing, not masked drift), 41/41 tests green. P9 confirms the r1/r2 silent-data-loss + deploy-gap fix (carve-out + prune-exemption now cover both operator-tool dirs; save-verdict in neither; dry-run exit 0). P10 confirms r3/r4 doc-coherence (opdisc L590 + PLINY §5.14 repointed; no --cwd sentence; no dangling pointers). This arc is correctly §35.5-carved-out (process hardening, no threat-ratified mitigation) so threat_coverage: is the legitimate empty-state. My own verdict save dogfooded P1/P2/P3 (Bash-only printf authoring + inline sha256 + bw attach). No probe falsified any design claim. Verdict: PASS.
