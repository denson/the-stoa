status: completed
ticket: stoa--p41.2
verdict: pass
diff_reviewed: d08aef0 (arc-63/build, b5de0aa..HEAD; 28 files, +1432/-686)
design_artifact_compared_against: agents/design/arc-63/design-rev2.md (Option C)

VERDICT: PASS (PASS-WITH-NITS). No blocking findings. 3 non-blocking nits, all follow-up-flavored. Authorship audit CLEAN. VERA meta-verdict: SOUND.

concerns:
- id: c1
  category: hygiene
  description: check-bw-release/check.sh issues curl -fsSL to the GitHub releases API with NO --max-time/--connect-timeout; the hook bounds it with timeout 10, so session boot is bounded at ~10s/check (~20s worst case for both) under a black-holed network, and an orphaned curl child can outlive the SIGTERM. The non-blocking guarantee HOLDS (boot returns in bounded time, fail-open), but the bound is 10-20s on the throttled once/day path rather than sub-second, and a stalled curl may orphan.
  evidence: substrate/skills/check-bw-release/check.sh:120 (curl -fsSL to api.github.com, no max-time); bounded by sessionstart-substrate-check.sh:115 timeout 10.
  severity: minor
  quadrant_classification: easy-easy
- id: c2
  category: consistency
  description: An UNTOUCHED referencing file, MAJOR_POLYBIUS.md:506 (both copies), still calls check-substrate-updates a base-team sync skill (L618 likewise); post-arc these are operator TOOLS (no SKILL.md), not Skill-tool skills. This is loose/historical usage of the word skill meaning the dir (the path ref still resolves under Option C), NOT the FALSE invoke-via-the-Skill-tool framing R1 targeted (that is fully cleared). Cosmetic terminology drift, outside the R1 condition and outside the diff.
  evidence: .claude/MAJOR_POLYBIUS.md:506,618 + substrate copies (untouched by this arc; not in the d08aef0 diff).
  severity: minor
  quadrant_classification: easy-easy
- id: c3
  category: hygiene
  description: The hook non-blocking + throttle correctness depend on coreutils timeout and BSD-style date -r FILE +%s being present in the consumer shell. Both are present and verified working in the-stoa git-bash env (empirically probed this review). On a consumer lacking timeout, each check.sh runs UNBOUNDED (fail-open still holds via the SUB_OUT-empty guard if check.sh errors, but a slow check.sh would not be cut off); on a consumer lacking date -r, stamp_fresh() always returns 1 so the throttle never engages and the check runs EVERY session start. Both degrade safely (no hang from the hook logic itself, no wrong output) but lose the intended boot-budget/throttle property on a minimal POSIX env. Acceptable for the substrate assumed targets; noted for the pass-B generic-mechanism work.
  evidence: sessionstart-substrate-check.sh:67 (date -r STAMP +%s), L114-115 (timeout 10); empirically confirmed working in the-stoa env this review.
  severity: minor
  quadrant_classification: easy-easy

follow_ups:
- c1 (curl --max-time on check-bw-release/check.sh) -- pre-existing check.sh behavior, not introduced by this arc; a one-line --max-time hardening belongs with the check.sh tools, not this hook diff. Schedule immediately after pass A lands.
- c2 (MAJOR_POLYBIUS.md skill -> tool terminology) -- outside the R1 condition and outside the diff; a cosmetic terminology sweep across untouched referencing files is a small follow-up, not a pass-A blocker.

verifier_coverage_assessment: |
  VERA executed all 14 design-rev2 section-3 probes and returned PASS with deterministic evidence on the two load-bearing probes. I independently cross-checked VERA most load-bearing claims and they hold: (1) NOSETTINGS -- confirmed myself that the b5de0aa..HEAD diff contains NO live settings.json (only the two templates/settings-hooks.json candidates), the pre-existing tracked .claude/settings.json is byte-unchanged across the arc, and it references the new sessionstart-substrate-check hook ZERO times. The M1 threat-coverage line is well-formed and genuinely exercised (attack-blocked: no live settings.json armed; legit-unaffected: --enable-hooks runbook intact) -- not a happy-path proxy. The section-6.1 item-11 binding holds: defeats_via_probe p-nosettings IS in probes_executed with non-empty probe_evidence. (2) G-a trap-closed -- confirmed myself via the anchored name-field grep on the check-* name field, zero roster entries; gauntlet-setup rostered at agents.ts:280; VERA methodology_concern #3 (substring grep gives 5 prose false-positives, anchored name grep is the honest check) is sound and I reproduce its result. (3) generated.test.ts -- confirmed the change is comment-only; the toBeGreaterThan(0) assertion is untouched (no weakening). BEYOND VERA: I ran an end-to-end empirical probe of the SessionStart hook itself in the live git-bash env (VERA tested the additionalContext channel and the Stop reader separately but not the hook own emit path): current-workspace yields empty stdout + no signal file; drift-workspace yields a valid JSON object on stdout (parses clean, first byte open-brace) + a signal file written with self-contained body; date -r throttle resolves; lib sources cleanly. No coverage gap found. VERA 3 methodology_concerns are all honestly disclosed, non-blocking, and correct. VERA meta-verdict: SOUND -- VERA falsified rather than rubber-stamped (drove the anchored name-grep instead of the misleading substring count, exercised the M1 attack path, ran a REAL non-dry-run recompose per the Arc-61 lesson).

summary: |
  d08aef0 moves check-substrate-updates + check-bw-release off the model-invokable skill menu onto a SessionStart(startup|resume) type:command substrate-check hook (throttle/silent/non-blocking/fail-open + a P-FALLBACK signal file read by stop-self-check.sh clause D), keeps the check/apply/revert scripts deployable via two narrow install.sh carve-outs (Option C: delete only the 2 SKILL.md), ports gauntlet-setup into the substrate, doc-fixes the stale SessionStart note, lands R1 xref fixes, and keeps the app green. The diff is CLEAN and tightly scoped -- confined to the 5 deliverables + R1 + design/verdict artifacts, no pass-B leakage, no out-of-scope canon edits. Cold-read craft is high: the hook is fail-open at every step (guarded stdin read, cwd-missing exits 0, throttle stamp refreshed BEFORE the checks to prevent loop-fire, timeout-bounded check calls, stdout discipline emits ONLY the JSON object or nothing -- empirically confirmed), every variable use is quoted (no command-injection/path-traversal surface from the event cwd -- it is only ever a quoted path prefix or a quoted --workspace arg, no eval, no constructed command), the Stop clause D rides the existing decision:block+reason JSON after the infinite-block guard (once-per-turn, graceful no-op when no signal, fail-open), the install.sh carve-outs are narrowly scoped to a literal 2-name allowlist mirroring the SKILL_NAMES loop mechanics with a paired prune-exemption, all dual-copy pairs are byte-identical, and gauntlet-setup description text + body are byte-verbatim from source (only the YAML wrapper changed to a block scalar, fixing a real gen-data parse abort). AUTHORSHIP AUDIT CLEAN: no author/owner/creator field in any changed file names a non-Denson person (gauntlet-setup has no author field; every author-keyword grep hit is prose about the authorship discipline itself). The doc-fix is honest and not over-claiming (narrows the working-channel claim to startup|resume, keeps the best-effort caveat for the still-broken PostToolUse/PreToolUse #55889 + compact #15174 channels, matches VERA observed v2.1.170 reality). VERA meta-verdict SOUND. The most important finding is c1 (a pre-existing curl without --max-time, bounding session boot at ~10-20s under network stall) -- non-blocking, follow-up-flavored. Overall posture: PASS, ship-ready, with 3 minor nits routed to follow-ups.
