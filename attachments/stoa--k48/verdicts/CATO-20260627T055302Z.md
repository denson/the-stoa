status: completed
ticket: stoa--k48
verdict: pass
diff_reviewed: arc-k48/build worktree uncommitted state — provision/ sub-package (9 modules) + tests/test_provision.py (31 probes) + tests/test_install_smoke.py + jd5 fold (data/ git-mv inside pkg, dataload.py/pyproject.toml/conftest.py/test_dataload.py) + agents/design/stoa--k48/relay-up/ (3 artifacts)
design_artifact_compared_against: agents/design/stoa--k48/design-rev2.md (+ design-formal §4-§7/§12 ground-truth)
concerns:
- id: c1
  category: consistency
  description: On the INITIAL provision, the S3 provision_db_service and provision_serving_service actions are wrongly recorded as created=False / SKIPPED. The mock methods self-register the id (mock.py:90 _created_flag db-service:slug; mock.py:94 _created_flag serving-service:slug) AND the steps layer re-checks the SAME key one line later (steps.py:95 / steps.py:101), so the steps-layer _created_flag always returns False — even on first run. Contrast ensure_project/ensure_sa (mock does NOT self-register; steps.py:29/35 is the sole registrant, correctly True). Design §3.1 created semantics say a FIRST create must be True. Reproduced live: first run both services created=False/skipped while ensure_project is correctly True.
  evidence: builder_deploy_core/provision/mock.py:90,94 + builder_deploy_core/provision/steps.py:95,101 (double-registration, same key); design-rev2.md §3.1 StepAction.created
  severity: recommended-revision
  quadrant_classification: easy-easy
- id: c2
  category: coverage
  description: META-VERIFY-VERA — the gap c1 slipped through. VERA idempotency probe p8 is ONE-DIRECTIONAL: it asserts the SECOND run widens nothing (zero create-or-get verbs created=True), but never asserts the FIRST run create-or-get verbs (incl provision_db_service/provision_serving_service) ARE created=True. test_idempotent_rerun_no_widen (test_provision.py:136) only checks the second run. The c1 bug makes the no-widen assert pass MORE easily (fewer created=True), so the suite is hollow for this direction. Not a falsification of any DoD VERA verified — a missed case (§6.4 meta-verifier coverage gap).
  evidence: tests/test_provision.py:136-155 (second-run-only); VERA-20260627T054115Z.md p8 (second-run no-widen asserted; first-run create-or-get=True not asserted)
  severity: recommended-revision
  quadrant_classification: easy-easy
- id: c3
  category: consistency
  description: DESIGN-vs-SHIPPED drift (CONFIRMED; design prose is the thing to fix at close, NOT the code) — design-rev2 §8.12/§4.5 wrote the M3 check_runtime_completeness missing shape as bare names; the shipped FROZEN function returns list[((api_kind,api_name),(secret_kind,secret_name))] tuple-pairs. ADA built the M3 probe (test_provision.py:445) + S6 (steps.py:160-172) to the SHIPPED tuple-pair shape — CORRECT (frozen shipped code is canon). The M3 probe drives both (a) attack-blocked (drop paired secret -> ok=False, tuple-pair in missing) AND (b) legit-unaffected (complete -> ok=True, missing==[]). The design PROSE needs the close design-canon-audit correction; the code is right.
  evidence: design-rev2.md §8.12/§4.5 (bare-name prose) vs steps.py:160-172 + tests/test_provision.py:445-457 (tuple-pair); ADA return-note + steps.py:161-164 ground-check comment
  severity: minor
- id: c4
  category: consistency
  description: op-string vocabulary drift — Choreographer._failed_action (choreographer.py:78) emits StepAction with op=fail. Design §3.1 pins the canonical op vocabulary to EXACTLY the §4.3 ABC abstractmethod names PLUS the engine-level verify; fail is a THIRD engine-level op the design did not enumerate. Also the failed action sets target=<step name> (e.g. S0) rather than the NAME the op acts on (§3.1 target contract). Benign for safety (value-free; assert_value_free walks it; detail=(reason,provisioner_raise)), but it is a design-§3.1-vs-shipped vocabulary gap. CATO rules: benign, fold into the close design-canon-audit prose (do not block).
  evidence: builder_deploy_core/provision/choreographer.py:78 (op=fail, target=step) vs design-rev2.md §3.1 op-vocabulary
  severity: minor
- id: c5
  category: consistency
  description: golden-fixture form deviation — design §8.1 specified golden files under tests/fixtures/provision-specs/<name>.toml; ADA built the 8/6/7 emit goldens as in-code ProvisioningSpec field assertions (test_provision.py:66-104) instead. Satisfies DoD#1 intent (byte-stable end-to-end emit, regression-pinned to resolve()/derive_sa_scope) and is arguably more robust (no external-file drift). Minor design-vs-shipped form note for the close audit; not a defect.
  evidence: design-rev2.md §8.1 (external TOML golden files) vs tests/test_provision.py:66-104 (in-code asserts); tests/fixtures/provision-specs/ does not exist
  severity: minor
follow_ups: |
  - design-canon audit at close (already PLINY-flagged): correct design-rev2 §8.12/§4.5 M3 missing-shape prose to the shipped tuple-pair shape (c3); enumerate the fail engine-op alongside verify in §3.1 or rule it out (c4); note the in-code golden-fixture form vs §8.1 external-TOML (c5). All design-prose-only; shipped code is canon.
  - c1+c2 (S3 first-run created=False) is a near-zero-cost fix (drop the redundant steps-layer _created_flag for the two S3 service ops and use the mock method return, like every other verb; OR have the two mock methods return their _created_flag) + add a first-run-create-or-get=True assertion to close the c2 one-directional gap. PLINY decides whether to route the one-line fix back to ADA in this envelope (PRINCIPAL fix-it-now calculus favors it) or as an immediate follow-up.
verifier_coverage_assessment: |
  VERA 12 probe-groups cover DoD 1-10 well and the 4 threat-anchored probes (M1/p8, M2/p6, M3/p9, M6/p5) are NOT hollow — each drives a real attack path with BOTH (a) attack-blocked AND (b) legit-unaffected (independently re-confirmed: M2/p6 RAISES on an injected int leaf; M3/p9 asserts the real tuple-pair shape on the drop-paired-secret attack; M6/p5 zero LEAK_TOKENS; M1/p8 no project-wildcard grant + sa_scope==derive_sa_scope). §6.1 item-11 mechanical cross-check PASSED: every cited defeats_via_probe id (p5/p6/p8/p9) is in probes_executed with non-empty probe_evidence. The ONE gap (c2): VERA idempotency probe p8 is one-directional — verifies the SECOND run widens nothing but never asserts the FIRST run create-or-get verbs are created=True, so the c1 S3 first-run-SKIPPED bug slipped through (§6.4 meta-verifier catch). Independently re-ran: full bdc suite 105 passed; W1 grep EMPTY whole-tree; W2/W3 confirmed; frozen surface byte-identical (resolve.py adade158); install-smoke real-venv PASS (not skipped); M3/W1-AST/grep-clean all executed (not skipped).
summary: |
  Clean, well-structured 9-module provision/ sub-package (pure emitter + port-driven engine + the ONLY concrete MockProvisioner) realizing design-rev2 faithfully, plus the jd5 packaging fold (data/ inside the importable package, four consistent path edits) and 3 value-free relay-up generators. The 3-wall TRIPWIRE holds independently (W1 grep EMPTY whole-tree incl tripwire.py/checklist.py via fragment-SSoT; W2 subclasses==[MockProvisioner] + no-default-port + mock instantiable; W3 emit takes no port); strict §7 boundary clean (/run absent everywhere; emitted T1 surface zero domain-verb leaks); value-free emit + relay-up artifacts carry zero credential values; frozen surface byte-unchanged (blob adade158, IDENTICAL main-vs-worktree); scope isolated to agents/ (no substrate/app/tooling creep); authorship clean (author=Denson Smith throughout; seat-built-by is a provenance breadcrumb, not an author field). ADA M3 built-to-ship-reality deviation (tuple-pair shape, not the design bare-name prose) is the CORRECT call. The single real bug is c1: the S3 db-service/serving-service actions are mislabeled created=False/SKIPPED on the INITIAL provision (a double _created_flag registration between mock.py and steps.py), which VERA one-directional idempotency probe missed (c2). It touches no security wall, no value-free property, no fail-closed path, no frozen surface — the ledger is a mock reporting artifact in a domain-empty choreography that provisions nothing — so recommended-revision, not blocking. Overall posture: PASS with one recommended-revision (near-zero-cost fix; PLINY routing call) + 3 minor design-canon-audit notes.
