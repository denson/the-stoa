---
author: Denson Smith
seat: CAPTAIN_ARGUS_the_stoa (PLAN-CRITIC)
ticket: stoa--k48 (u--9s2 Phase-2 increment 2.3 — provisioning choreography, ONE pass)
operating-mode: autonomous
as_of: 2026-06-27
---

status: completed
ticket: stoa--k48
verdict: revise
design_artifact_audited: agents/design/stoa--k48/design-rev1.md

OVERALL: PASS-WITH-CONCERNS leaning REVISE — 3 load-bearing findings, all easy-easy (concrete,
cheaply re-verifiable). The design is structurally sound: pure-emitter + port-driven-engine + 2-mode
value-free mock is the right architecture; the 3-wall TRIPWIRE concept is correct; the jd5 fold is
coherent + complete; the frozen surface is reused byte-unchanged; the threat map faithfully realizes
jw5 sec12 (M1-M6 + R-1/R-2/R-3 + M5-MOOT) and I CONFIRM/RATIFY it. The 3 findings are precise
buildable-spec gaps, not architecture flaws — each a targeted spec tightening, no redesign.

audit_block:
  risks:
  - id: r1
    description: The W1 grep ATTESTATION self-collides — render_tripwire_attestation() lives in provision/checklist.py (sec2) and sec5 directs it to cite the grep command; the grep command literal contains the bare tokens subprocess|socket|urllib|requests|httpx|gcloud|railway|os.system|popen, so the sec5 W1 grep scanning builder_deploy_core/provision/ MATCHES those tokens inside checklist.py and returns NON-EMPTY -> DoD#6 grep FAILS while the AST test (authoritative; a string-literal is not an import) PASSES. The design asserts the grep is the human-auditable echo of the AST; under this collision AST and grep DISAGREE, defeating that property.
    evidence: design-rev1.md sec5 (W1 grep token list L379 + render_tripwire_attestation cites the grep command L394-395) cross sec2 (checklist.py under provision/ L120); empirically confirmed — grep -nE of the token list matches bare subprocess/gcloud/railway in prose.
    load_bearing: true
    quadrant_classification: easy-easy
  - id: r2
    description: The M3 threat-anchored probe is NOT threat-anchored — sec12.B binds M3 (runtime-completeness; api-enabled-but-keyless -> 401) to test_emit_prospector_golden (a HAPPY-PATH spec-equality assert) + an unnamed S6 runtime-completeness assert. No sec8 probe exercises the M3 attack-path: a resolved set with a key-bearing gcp_api present but its paired gcp_secret DROPPED, where S6 check_runtime_completeness() must FAIL/report missing, asserting (a) attack-blocked AND (b) legit-unaffected. This is the op-disc sec35 / role sec6.9-clause-4 map-present/threat-anchored-probe-absent smell for M3 (contrast sec8.2 which DOES spec S2c-blocks + S0-abort negatives).
    evidence: design-rev1.md sec12.B M3 row (L652, names only the golden + an unnamed S6 assert); sec8.1 test_emit_prospector_golden (L431, spec-equality only); sec4.5 S6 (L341, describes the call but sec8 specs no attack-path probe for it).
    load_bearing: true
    quadrant_classification: easy-easy
  - id: r3
    description: Port-ABC contract gap — S4 (sec4.5) dispatches provisioner.select_base_image(needs_postgis_base_image), but select_base_image is NOT declared as an abstractmethod in the sec4.3 Provisioner ABC (which ends at stand_up_mesh_shape). W2 asserts Provisioner.__subclasses__()==[MockProvisioner] and relies on the ABC to pin the port contract; an undeclared port verb means the ABC does not force MockProvisioner to implement select_base_image -> a latent AttributeError at S4 or an ad-hoc mock method outside the audited port surface.
    evidence: design-rev1.md sec4.3 Provisioner ABC (L274-305, abstractmethods enumerated; no select_base_image) vs sec4.5 S4 row (L339, select_base_image(needs_postgis_base_image)) + sec3.1 op enumeration (L156-158) listing select_base_image as an op.
    load_bearing: true
    quadrant_classification: easy-easy
  - id: r4
    description: The W1 grep token list is FRAGILE against legitimate value-free deploy-help/attestation prose under provision/ — bare lowercase railway / gcloud-enable / any subprocess mention trips it, while underscore forms (railway_set_secret, railway_run) and capitalized Railway/GCP are safe only by accident of the word-boundary regex. The sec9 deploy-help table happens to use the safe forms, but that safety is an undocumented authoring constraint, not a structural guarantee; a future generic-prose edit naming the railway service or a subprocess in a provision/ docstring would silently fail DoD#6. Bounded (FAIL-LOUD at VERA grep time, not a runtime breach), so not promoted.
    evidence: design-rev1.md sec9 deploy-help table (L551-556 uses railway_set_secret/railway_run/Railway dashboard) vs sec5 grep (L379); empirical regex test confirms underscore + capital forms safe, bare-lowercase forms trip.
    load_bearing: false
    quadrant_classification: easy-easy
  - id: r5
    description: Op-string label drift — sec3.1 StepAction.op example list names require_population and verify_* as ops, but the sec4.3 port method for the S2c gate is is_populated and there is no verify_* port method (S6 verify is engine-level, not a port call). These are documentation-example strings on a free-form op field, not contract-bearing, so cosmetic.
    evidence: design-rev1.md sec3.1 op comment (L156-158) vs sec4.3 ABC (is_populated, L294) + sec4.5 S6 (engine-level verify, L341).
    load_bearing: false
    quadrant_classification: easy-easy
  non_findings:
  - Frozen surface reuse CONFIRMED byte-unchanged — resolve L69 / resolve_with_lint L106 / derive_sa_scope L147 / check_runtime_completeness L162 verified exact; jd5 touches NONE of these bodies (only dataload _DATA_ROOT L25 path expr + pyproject + test_dataload data-path lines). emit_spec calls the real frozen derive_sa_scope, not a re-derive (sec4.1 + test_sa_scope_is_derive_sa_scope sec8.4).
  - jd5 fold completeness CONFIRMED — data/ move + dataload.py:25 parent.parent->parent + pyproject:31 parent-relative glob -> intra-package glob + pyproject:28 packages += provision + install-then-import smoke. The DAEDALUS fold-coherence catch (test_dataload.py L61 + L84 flip parent.parent->parent) VERIFIED EXACT against the real file; without the flip test_dataload goes RED. conftest._PKG_ROOT stays valid (sys.path only). MANIFEST.in correctly flagged-not-required (wheel package-data primary; smoke test the guard).
  - Value-free emit / HW-1 DISCHARGED — every spec/ledger/StepAction field is a NAME/ref/bool/enum; populated is a typed set-of-str of slot NAMES walked by assert_value_free; assert_value_free walks spec + ledger + mock state (sec4.2, sec8.3). The port takes/returns no value (is_populated->bool; set_railway_var->source_slot ref).
  - DW-5 secret-backed railway vars CONFIRMED coherent — the sec8 resolved sets carry ONLY DATABASE_URL as a railway_var (VERIFIED in tests/fixtures/expected/{prospector,scienceclaw,labstat_bls}.toml: exactly one railway_var=DATABASE_URL each); secret-backed vars are correctly S3 ACTIONS naming a source_slot, not resolved railway_var entries. No value leaks.
  - acquisition derivation (HW-2) DISCHARGED — _derive_acquisition is total over the actual resolved sets (MAPS_API_KEY->mint-via-gcp-console, POSTGRES_PASSWORD->generate-locally, BLS_OEWS_API_KEY->mint-via-thirdparty); no new REQUIRED catalog field; sec8 fixtures untouched.
  - labstat thirdparty kind-dispatch CONFIRMED — labstat_bls expected set has BLS_OEWS_API_KEY as thirdparty_rest_key, not a gcp_api; test_labstat_skips_s1_for_thirdparty (sec8.7) carries the proof.
  - Per-builder isolation / M1 CONFIRMED — bijective sa=sa-<slug> + per-secret grant_secret_accessor (never project-wide) + sa_scope==derive_sa_scope (frozen) + S3 injects only this builder SA-key-ref. sec8.4 binds all four.
  - W2 subclasses==[MockProvisioner] non-finding on import-order — holds because provision/__init__ re-exports MockProvisioner (the only concrete subclass imported at package-import time); no other concrete Provisioner under provision/. List-equality is order-sensitive in principle but deterministic with exactly one subclass.
  - Credential discipline (role sec6.8) CONFORMANT — emit-then-apply, real applier OUT-OF-PACKAGE forever; 2.3 emits PLAN-TEXT only (none of the 5 rejected anti-patterns); keyring/WIF ENV/STDIN path matches canon; S2c BLOCKS + halts (no refusal-as-signal route-around).
  threat_coverage_assessment: Design-time probe-SPEC adequacy — M1 (sec8.4) ADEQUATE; M2 (sec8.3 + W1/W2/W3 sec8.6) ADEQUATE; M4 (inherited frozen badbuilder_pgvector_omit fixture, VERIFIED present) ADEQUATE; M6 (sec8.5) ADEQUATE. M3 INADEQUATE (see r2) — the bound probe is happy-path golden equality, not an attack-path probe exercising check_runtime_completeness at S6. 4 of 5 threat-ratified mitigations carry a spec-d threat-anchored probe; M3 does not. Honest-claim boundary (op-disc sec35.5): I verify named-threat COVERAGE, not threat-ENUMERATION completeness — the jw5 sec12 threat set is inherited + Grand-gated, not re-opened here.

RATIFICATION LIST (A3->A1; threat + attack-path per mitigation; for PLINY pre-ADA A1 restatement):
  I CONFIRM the design-rev1 sec12.A classification (DAEDALUS PROPOSED, ARGUS CONFIRMS) and RATIFY:
  - M1 RATIFIED — Threat: per-builder isolation / cross-builder lateral movement (compromised key or over-scoped SA in builder A reads/spends/exfiltrates builder B). Defeated: sec4.5 S0 bijective ensure_sa + sec4.5 S2 per-secret grant_secret_accessor (never project-wide) + sec3.2/sec4.1 sa_scope==derive_sa_scope (FROZEN union) + sec4.5 S3 injects only THIS builder SA-key-ref. Residual R-1 carried not defeated (t0 holds). Probe sec8.4 adequate.
  - M2 RATIFIED — Threat: credential-discipline / agents-never-hold-values (secret VALUE in agent transcript/argv/disk). Defeated: sec0.1 emit-then-apply (value-free spec; real applier out-of-package forever) + sec4.5 S2c BLOCK + sec4.2 assert_value_free + sec4.3/sec4.4 port carries no value + the 3-wall TRIPWIRE sec5. Probe sec8.3 + sec8.6 adequate, SUBJECT to r1 fixing the W1 attestation self-collision so DoD#6 actually holds.
  - M3 RATIFIED-WITH-PROBE-GAP — Threat: runtime-completeness (key-bearing API enabled-but-keyless -> 401). Defeated: sec4.5 S6 check_runtime_completeness (FROZEN). MECHANISM ratified (frozen fn correct + reused); PROBE binding inadequate (r2) — needs a spec-d attack-path probe before coverage can be attested at VERA.
  - M4 RATIFIED — Threat: non-omittable baseline silently omitted -> fail-open. Defeated UPSTREAM by resolve() (FROZEN) BaselineOmitError; 2.3 adds no re-introduction path. Probe: inherited frozen badbuilder_pgvector_omit fixture adequate.
  - M6 RATIFIED — Threat: agent-access mesh security / unauthorized mesh access. Defeated: sec6/sec4.5 S5 T1 SHAPE — 0600 AF_UNIX socket + tailscale serve header-trust (Tailscale-User-Login, never WhoIs-on-loopback) + deny-by-default + BUILDER_OPERATORS + group:operators + Funnel OFF. Probe sec8.5 adequate.
  CARVE-OUTS CONFIRMED (op-disc sec35.5 process-change, no runtime attack path; DAEDALUS PROPOSED, ARGUS CONFIRMS): the pure-emitter+engine architecture, the dataclass schemas, the 2-mode mock, the jd5 fold, the relay-up generators, the deploy-help PLAN emit — all correctly carved out (2.3 provisions NOTHING; pure code + mock). None has a runtime attack path; none is a mapless smell. CONFIRMED, not flagged.
  RESIDUALS CONFIRMED carried (NOT defeated): R-1 (M1 stale-grant), R-2 (manifest integrity), R-3 (catalog integrity), M5-MOOT (per-builder prepaid card = out-of-band hard cap; 2.3 REPRESENTS it at S0 via set_budget_boundary/budget_boundary_required, never creates a card). All surfaced-not-overclaimed.

RQ RULINGS:
  - RQ-1 (sa-<slug>/proj-<slug> emit granularity vs FQN in 2.4 applier): RULING — the BARE slug-derived stem is CORRECT at the emit; the FQN belongs in the 2.4 APPLIER. emit_spec is pure + cloud/HOME-agnostic (sec0.1); a bare stem is value-free, deterministic, derivable-to-FQN by the applier that knows the live project id. Baking the FQN into emit would couple the pure emitter to a GCP-specific convention and break HOME-agnosticism. design-formal sec5.A only fixes derived-from-the-slug, which the stem honors. NO isolation ambiguity: bijectivity preserved (one slug -> one stem -> one FQN). Downstream note (not a 2.3 change): the stem->FQN expansion must be the applier single deterministic fn so the bijection survives. NOT a 2.3 blocker.
  - RQ-2 (KEEP the strict substring sec7 leak-scan vs word-boundary refine): RULING — KEEP STRICT, as DAEDALUS leaned and Polybius_the_Stoa explicitly endorsed (the stronger form; /run IS a leak). The substring strictness is the POINT: a generic-prose false-positive (research contains search) is the ADA signal to REWORD the T1 surface to domain-neutral language (lookup not search; vectorize not embed), not a probe bug. The T1 surface is small + cookie-cutter generic, so rewording cost is bounded; word-boundary refine would let /run slip if embedded as a path segment and weakens the canonical mandate. ENDORSED PROBE FORM: strict substring scan of the ENTIRE emitted T1 surface (stand_up_mesh_shape output + any shipped template file under provision/) for the LEAK_TOKENS list (/run, collect, embed, ingest, search, newswire, scienceclaw, prospector) verbatim, asserting NONE appears; health the only concrete route; domain endpoint a VERB 501-stub. Correct AS-IS. ADA-facing note (not a spec change): scan the actual emitted SHAPE STRING + shipped template file (round-trip), not a paraphrase.
  - RQ-3 (deploy-help library COPY into the skill dir in 2.3 vs reference-only): RULING — this is a PLINY orchestration call, not an ARGUS risk; DAEDALUS correctly flagged not baked. On the merits for PLINY: reference-only (DAEDALUS formalization) is a CLEAN deferral and does NOT under-deliver the directive. Directive sec2 the builder-deploy skill deliverable is the CHOREOGRAPHY package + tests + the 3 relay-up generators; the deploy-help LIBRARY copy is a HOME-agnostic skill-wrapper concern that rides the close/2.4 A-home git mv. DoD#7 (deploy-help DESIGNED, mock-only) is satisfied by the PLAN-TEXT emit + sec8.8 plan-dispatch probe. DoD#1 end-to-end emit is package-internal, unaffected by where the reuse-source scripts live. NO DoD gap from reference-only. Residual exposure: the working-tree cite to the user-tier reuse source (the railway-keyring-deploy user-tier path) is a user-tier absolute path a fresh consumer clone lacks — but that is a 2.4 consume-path concern, out of 2.3 scope, not a 2.3 DoD gap. PLINY call; reference-only is sound.

summary: design-rev1.md folds the CHIRON ownership/tier half and the HAMILTON mechanics/mock half into one buildable spec for a provisioning choreography that EMITS a value-free spec (slot NAMES only) and walks S0-S6 against an injected Provisioner port whose only concrete implementation is a zero-I/O MockProvisioner — provisioning NOTHING real. The architecture is correct and the 3-wall TRIPWIRE (W1 no-I/O imports, W2 mock-only port, W3 pure emit) is the right shape; the frozen surface is reused byte-unchanged (line-refs verified exact); the jd5 packaging fold is coherent + complete including the test_dataload.py fold-coherence catch; the threat map faithfully realizes jw5 sec12 (M1-M6 + R-1/R-2/R-3 + M5-MOOT) and I ratify it. Three load-bearing findings keep this at REVISE, all easy-easy: (r1, most important) the W1 grep ATTESTATION self-collides — citing the grep command inside provision/checklist.py embeds the subprocess/gcloud/railway token literals under provision/, so the W1 grep returns non-empty and DoD#6 FAILS while the authoritative AST test passes, defeating the design own grep-is-the-echo-of-the-AST property; (r2) the M3 threat-anchored probe is happy-path golden equality, not an attack-path probe exercising check_runtime_completeness on an api-enabled-but-paired-secret-dropped set; (r3) select_base_image is dispatched by S4 but not declared in the sec4.3 Provisioner ABC, so the W2 subclasses contract does not pin it. None require redesign — each a targeted spec tightening. RQ-1: bare slug stem correct (FQN in the 2.4 applier). RQ-2: KEEP strict substring sec7 scan (false-positive = ADA reword signal). RQ-3: PLINY call; reference-only a clean deferral, no DoD gap. Posture: minor-revisions / structurally sound.
