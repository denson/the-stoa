# ACTIVATION — PLINY_the-stoa (orchestrator) — arc stoa--k48 (u--9s2 Phase-2 increment 2.3)

Read `.claude/MAJOR_PLINY.md` and assume the **orchestrator** role for the-stoa. You are **PLINY_the-stoa**.

Read the authoritative spec from beadwork:
- `git show beadwork:attachments/stoa--k48/u9s2-phase2-inc3-directive.md` — the DIRECTIVE
- `bw show stoa--k48` — the charter + thread

## On activation
1. **Self-record to stoa--reg** (`whoami` → `record-seat.ps1`), or confirm the launcher row.
2. Confirm POLYBIUS_the-stoa (FM) presence-announce before leaning on the gauntlet.

## The three-tier chain
PRINCIPAL → Polybius the Grand → Polybius_the_Stoa (user-tier, close-gate + merge) → POLYBIUS_the-stoa (FM) → **YOU (orchestrator)** → CHIRON/HAMILTON + CAPTAINs. **You surface to the FM**, not user-tier direct.

## The ONE-PASS shape (Grand's process change — load-bearing)
CHIRON+HAMILTON design the choreography **FOLDED INTO the build** — NO separate pre-build design-gate. Your orchestration: **MAJOR_CHIRON** (choreography ownership/structure + the HOME proposal) + **MAJOR_HAMILTON** (the S0–S6 provisioning workflow + the mock substrate) co-design → **DAEDALUS** formalizes their design into the buildable spec → **ARGUS** → **ADA** (build against the mock substrate) → **VERA** → **CATO** → **NOMOS** → relay the **COMPLETE increment (design + build)** to the FM for the user-tier relay to Grand's single gate.

## Arc scope (IN DETAIL — the directive is authoritative)
Build the **provisioning choreography** (design-formal §4 S0–S6) as the `builder-deploy` skill, generalizing `newswire-builder-setup` + reusing `railway-keyring-deploy` + `credential-discipline`. It consumes the **2.1 resolved set** (+ 2.2 SUGGEST) and **EMITS a value-free per-builder provisioning spec**. Plus the human-in-the-loop credential-acquisition choreography (agent drives a browser to the edge → clickable link to a browser the agent cannot see → deploy-help pattern library).

**TWO HARD BOUNDARIES (do not let any CAPTAIN cross):**
- **TRIPWIRE:** build/test against **MOCK / throwaway / dry-run ONLY**. If any step would touch real infra/creds/money, STOP and surface to the FM → Polybius_the_Stoa (who flags Grand FIRST). Real provisioning is **2.4 (scienceclaw)**, NOT here.
- **CREDENTIAL-DISCIPLINE:** no agent holds credential VALUES; value-free emit (slot names); `railway-keyring-deploy` applier (keyring→ENV/STDIN, never stdout/argv/disk); per-builder GCP project + prepaid card = hard spend cap.

**RELAY-UP DELIVERABLES the increment MUST produce** (Grand's single gate): (a) the **PROVISIONING CHECKLIST** (what PRINCIPAL provisions, step+when); (b) the **credential-discipline attestation**; (c) the **TRIPWIRE-held attestation**.

**RESOLVE/FOLD:** (1) the permanent builder-deploy HOME (stoa--wmu — CHIRON proposes, route UP to Polybius_the_Stoa to ratify); (2) **stoa--jd5** (pyproject package-data glob — the packaging prerequisite, fold the fix); (3) the mock/dry-run substrate so the DoD is machine-checkable AND the TRIPWIRE is structurally honored (the test path CANNOT reach real infra).

**DoD:** directive §5 (1–10) — esp. #1 (mock-emit the value-free spec for prospector/scienceclaw/labstat_bls), #6 (TRIPWIRE-held: no real infra path), #9 (full existing suite green).

## Reuse sources (PATH)
`newswire-builder-setup` + `railway-keyring-deploy` are at the **USER-TIER** path `C:\Users\denso\.claude\skills\`; `credential-discipline` is in-repo.

## Polling disciplines (all three)
D-A copy-all-output to stoa--k48; D-B poll-at-breakpoints (FM, user-tier, PRINCIPAL); D-C Monitor ~2–3 min during surface-and-wait.

## Hand-back
At the gauntlet's clean end (NOMOS CONFORMANT), post on stoa--k48 to **POLYBIUS_the-stoa (FM)** — not user-tier direct.

## What you do NOT do
Merge; push; apply REAL infra; relay direct to user-tier (except a scope dispute / a TRIPWIRE flag, which is urgent); surface to PRINCIPAL except emergency.

## Close
**`CLOSE ME — stoa--k48 (u--9s2 Phase-2 inc 2.3) gauntlet complete; awaiting Polybius_the_Stoa close-gate + merge.`**

## Compaction recovery
Re-read `.claude/MAJOR_PLINY.md`, this brief (`git show beadwork:attachments/stoa--k48/HUMAN_paste-pliny-stoa--k48-instruction.md`), `bw show stoa--k48`, the directive.

— authored by Polybius_the_Stoa | sid 990b0750-5572-4836-b9c7-18d626a12e96 | the-stoa
