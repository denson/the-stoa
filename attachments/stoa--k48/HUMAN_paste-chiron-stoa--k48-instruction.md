# ACTIVATION — MAJOR_CHIRON (team / choreography architect) — arc stoa--k48 (u--9s2 Phase-2 increment 2.3)

Read `.claude/MAJOR_CHIRON.md` and assume the **architect** role for the-stoa on arc stoa--k48. You are a design-time terminal seat answering to PLINY_the-stoa (parallel to HAMILTON), under POLYBIUS_the-stoa (FM) → Polybius_the_Stoa (user-tier).

Read the authoritative spec from beadwork:
- `git show beadwork:attachments/stoa--k48/u9s2-phase2-inc3-directive.md` — the DIRECTIVE
- `bw show stoa--k48` — the charter

## On activation
Self-record to stoa--reg (`whoami` → `record-seat.ps1`) or confirm the launcher row. Post a presence-announce on stoa--k48.

## Your design focus (ONE pass — your design is folded into the build)
You own the **choreography OWNERSHIP + STRUCTURE** of the provisioning choreography (design-formal §4 S0–S6, §7); HAMILTON owns the S0–S6 workflow mechanics + the mock substrate. Co-design with HAMILTON; PLINY drives the handoff to DAEDALUS who formalizes both into the buildable spec.

Deliver:
1. **The ownership/tier model** — for each of S0–S6 and the agent-access layer (§7), what is **T1 cookie-cutter (generic, built here)** vs **T3 project-seat (the product, NOT built here)**. The §7 SHAPE-vs-CONTENT boundary is load-bearing: the cookie-cutter ships a secured, mesh-reachable, **domain-empty** endpoint + a CLI-client skeleton; domain verbs/data are T3. The moment a domain verb leaks into T1, that is the boundary violation.
2. **The permanent builder-deploy skill HOME** (deferred from 2.1, decision-register `stoa--wmu`): a near consumer now exists (scienceclaw at 2.4). **PROPOSE** Option A (the-stoa forge-owned home) vs Option B (standalone builder-tooling location). This **routes UP to Polybius_the_Stoa to ratify** (escalate to Grand if load-bearing) — do NOT bake it.
3. **The emit-then-apply structure** — the choreography EMITS a value-free provisioning spec; a credential-disciplined applier executes it. Structure the boundary so agents never hold credential values.

## Hard constraints (non-negotiable)
- **TRIPWIRE:** the design provisions NOTHING real; build/test is MOCK/dry-run. If your design would require a real-infra/cred/money step to validate, flag it UP — do not bake a real action.
- **CREDENTIAL-DISCIPLINE:** value-free emit; per-builder GCP project + prepaid card = the hard spend cap.
- **Per-builder isolation:** one project ⟷ one builder; SA scope = the mechanical union of scope-bearing resolved entries (§5.A).

## Reuse source (PATH)
`newswire-builder-setup` (the ~60%-done structure reference) is at the **USER-TIER** path `C:\Users\denso\.claude\skills\newswire-builder-setup\`. Read it for the working structure to generalize.

## Surface UP
Route the HOME proposal + any load-bearing structural decisions UP through PLINY → FM → Polybius_the_Stoa. Hand your design to PLINY for the DAEDALUS formalization.

## Compaction recovery
Re-read `.claude/MAJOR_CHIRON.md`, this brief (`git show beadwork:attachments/stoa--k48/HUMAN_paste-chiron-stoa--k48-instruction.md`), `bw show stoa--k48`, the directive.

— authored by Polybius_the_Stoa | sid 990b0750-5572-4836-b9c7-18d626a12e96 | the-stoa
