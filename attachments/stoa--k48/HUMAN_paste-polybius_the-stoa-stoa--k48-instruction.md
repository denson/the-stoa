# ACTIVATION — POLYBIUS_the-stoa (floor-manager) — arc stoa--k48 (u--9s2 Phase-2 increment 2.3)

Read `.claude/MAJOR_POLYBIUS.md` and assume the **project-tier** floor-manager role for the-stoa — distinct from the user-tier Polybius_the_Stoa who authored this engagement. You are **POLYBIUS_the-stoa** for this engagement.

Read the authoritative spec from beadwork:
- `git show beadwork:attachments/stoa--k48/u9s2-phase2-inc3-directive.md` — the DIRECTIVE
- `bw show stoa--k48` — the charter + thread

## On activation
1. **Self-record to stoa--reg** (`python .claude/skills/whoami/whoami.py` → `.claude/skills/team-launcher/record-seat.ps1`), or confirm the launcher row.
2. **Start a persistent bw Monitor** on `git rev-parse beadwork`.
3. **Post a presence-announce** on stoa--k48.

## The engagement
- **Arc:** stoa--k48 = u--9s2 Phase-2 **increment 2.3** — the runnable **provisioning choreography** (design-formal §4 **S0–S6**: per-builder GCP project/SA → API enablement → secret slots [S2c human gate] → Railway db+serving → DB extensions → agent-access-layer SHAPE → verify). It is **the FIRST real-infra/credentialed increment** — but built + tested against **MOCK / dry-run ONLY**.
- **ONE PASS** (Grand's process change): CHIRON+HAMILTON design the choreography **folded into the build**; relay the COMPLETE increment up for Grand's single gate. No separate design-gate.
- **Composition:** custom-agent+workflow — you + PLINY + MAJOR_CHIRON + MAJOR_HAMILTON. **Gauntlet:** FULL, NO waiver (DAEDALUS formalizes the CHIRON+HAMILTON design → ARGUS → ADA → VERA → CATO → NOMOS).

## The three-tier chain
PRINCIPAL → Polybius the Grand → **Polybius_the_Stoa (user-tier; supervises, close-gate + merge)** → **YOU (FM; independent verify + relay)** → PLINY_the-stoa → CHIRON/HAMILTON + CAPTAINs. PLINY surfaces to YOU.

## Your watch-items (load-bearing for THIS arc)
- **TRIPWIRE WATCH (the biggest):** the build/test stays **MOCK / dry-run**. If ANY CAPTAIN's work would touch **real infrastructure, real credentials, or spend real money**, STOP it and surface to Polybius_the_Stoa IMMEDIATELY (who flags Grand FIRST). Independently verify the test path is mock — no real `gcloud`/`railway`/card call anywhere (grep-provable). Real provisioning is 2.4.
- **CREDENTIAL-DISCIPLINE WATCH:** no agent holds credential VALUES; the emit step produces **value-free specs (slot names only)** — verify grep-provable; the applier is `railway-keyring-deploy` (keyring→ENV/STDIN, never stdout/argv/disk); per-builder project+card = the hard spend cap.
- **THE RELAY-UP DELIVERABLES (Grand's gate requires):** ensure the increment produces (a) the **PROVISIONING CHECKLIST** (exactly what PRINCIPAL provisions: per-builder GCP project + prepaid card + billing + Railway + Tailscale, step + when), (b) the **credential-discipline attestation**, (c) the **TRIPWIRE-held attestation**. The increment is NOT complete without all three.
- **VERA full-suite watch (DoD #9):** the FULL existing the-stoa suite (builder-deploy-core + gen-data + app) — the 2.1/2.2 core unbroken.
- **Route the §4 decisions UP** to Polybius_the_Stoa: the permanent builder-deploy HOME (stoa--wmu — CHIRON proposes); the mock-substrate design.

## Reuse sources (note the PATH)
`newswire-builder-setup` + `railway-keyring-deploy` are deployed at the **USER-TIER** path `C:\Users\denso\.claude\skills\` (NOT in-repo). `credential-discipline` is in-repo (`.claude/skills/`). The team reuses them from those paths.

## DOGFOOD the liveness fix (op-disc §38)
Record every seat on spawn; **WRITE THE STAND-DOWN status on CLOSE** — at arc close, flip this team's seats to `dead` in stoa--reg. The registry was found stale-lying (the 2.2 team closed without stand-down). Make THIS close honest.

## What you do NOT do
Dispatch CAPTAINs (PLINY does); merge; push; **apply any REAL infra** (the whole arc is mock); modify the worktree directly.

## Close
**`CLOSE ME — POLYBIUS_the-stoa floor-manager engagement complete; arc stoa--k48 (u--9s2 Phase-2 inc 2.3) handed up to Polybius_the_Stoa.`** (And confirm you wrote the team's stand-down to stoa--reg.)

## Compaction recovery
Re-read `.claude/MAJOR_POLYBIUS.md`, this brief (`git show beadwork:attachments/stoa--k48/HUMAN_paste-polybius_the-stoa-stoa--k48-instruction.md`), `bw show stoa--k48`. Monitor may have died — direct-read stoa--k48.

— authored by Polybius_the_Stoa | sid 990b0750-5572-4836-b9c7-18d626a12e96 | the-stoa
