<!-- author: Denson Smith -->
<!-- seat: CAPTAIN_ARGUS_the_stoa (PLAN-CRITIC) — focused cold-audit of the rev4 to rev5 INV-RESP depth-fold delta -->
<!-- ticket: stoa--q7f | arc-77 | coordination stoa--po5 | u--9s2 Phase-1 increment 2.4 -->

# ARGUS re-audit verdict — arc-77 rev5 INV-RESP depth fix (stoa--q7f)

```
status: completed
ticket: stoa--q7f
verdict: pass-with-conditions
design_artifact_audited: agents/design/stoa--q7f/design-rev5.md (rev4 to rev5 INV-RESP depth-fold delta ONLY; rev4 shape NOT re-litigated)

audit_block:
  scope: FOCUSED cold-audit of the rev4 to rev5 delta — INV-RESP made depth-complete (redact_response recurses a NODE grammar OBJECT/SCALAR/PASS_WHOLE to schema depth; content the one documented PASS_WHOLE exception; loader refuses a legacy bare-list node; P-M2 extended with a nested-declared-field clause; M2 honest-claim updated). The already-cleared rev4 shape (multi-lane authz, INV-PRINCIPAL, seal-audit, INV-DEST/BIND/LANE, M1/M3-M7) was verified UNCHANGED, not re-audited.

  core_delta_confirmed_sound:
  - The OBJECT-node depth recursion makes the SCOPED VERA finding hold BY CONSTRUCTION: a credential in an undeclared sub-key at any depth inside a declared OBJECT node (candidates then groundingMetadata then undeclared) is dropped by the keep-only-declared-keys rule at that level. groundingMetadata is a RECURSED OBJECT node (verified in the section 5.1a schema, NOT PASS_WHOLE), so its undeclared sub-keys are dropped. Genuinely by-construction for the OBJECT-recursion path; no leak constructible through it.
  - P-M2(a.2) genuinely FALSIFIES a depth-2 build: it plants a credential at depth 3 or deeper in an undeclared sub-key of a declared OBJECT node and asserts ABSENCE from the caller body plus both audit phases. The CURRENT depth-2 redact.py (verified on disk: line 35 emits the declared sub-key VALUE whole) EMITS that value, so the absence-assertion fails against a depth-2 build. VERA already empirically demonstrated the depth-2 build emits the planted groundingMetadata leaked_bearer. Falsification is sound via the absence-assertion.
  - Loader guard fails CLOSED on regression: the extended _check_inv_resp recursively validates node types and REFUSES to load a bare-list (legacy depth-2) node (LoaderError). A regression to the whole-pass form fails at STARTUP, not at runtime. Build-coupling note (non-finding): the CURRENT registry.py uses bare-list nodes (verified on disk, lines 65 and 87), so ADA MUST migrate the schema to the node grammar in the SAME change as the loader guard, else the service fails to load (fail-closed, safe).

  risks:
  - id: r1
    description: The SCALAR no-object-at-any-depth by-construction guarantee is stated in the grammar but is NOT exercised by any threat-anchored probe. P-M2(a.2) plants the credential ONLY in an undeclared sub-key of a declared OBJECT node (groundingMetadata), never in a dict smuggled under a SCALAR slot. A shallow-SCALAR implementation (drop only a top-level dict; pass a list-containing-a-dict, or any array whose elements carry dicts) would PASS P-M2(a.2) while leaving the SCALAR-slot smuggle path open — the credential-bearing dict rides through under a SCALAR leaf. The GRAMMAR RULE is tight (drop any value carrying an object at any depth); the IMPLEMENTATION-fidelity (recursive dict-scan, not a shallow top-level check) and the PROBE-coverage of that rule are the open dependencies the delta does not close at design tier.
    evidence: design-rev5.md section 5.1a SCALAR grammar clause (passes iff it contains NO object at any depth ... a value carrying an object anywhere under a SCALAR node ... DROP) versus section 5.1a P-M2(a.2) (plants at depth 3 or deeper in an undeclared sub-key of a declared groundingMetadata OBJECT node — the OBJECT-recursion path, not the SCALAR-slot path). Per op-disc section 6.9 clause 4: the mapped M2 mitigation names an attack path (object rides a SCALAR slot — the design section 5.1a by-construction claim) for which NO threat-anchored probe is specified. Answers brief residual-question 4.
    load_bearing: uncertain
    quadrant_classification: easy-hard
  - id: r2
    description: PASS_WHOLE is an unaudited runtime whole-pass escape hatch. The extended loader guard validates node TYPES only (OBJECT/SCALAR/PASS_WHOLE well-formed); it does NOT restrict WHERE PASS_WHOLE may appear, nor mechanically enforce the per-field rationale the spec calls MUST. For a FUTURE provider that returns a credential IN its response body (unlike Vertex, whose ADC bearer is request-side and never in a response body), a PASS_WHOLE marker mistakenly placed on that credential-bearing field would bypass INV-RESP at runtime with NO mechanical catch — only reviewer convention plus the build-time (not runtime) seal-audit, which scans the tree/logs and does NOT scan runtime response bodies. This is precisely the future-provider case the general recursive form was chosen to protect, so the selling point of the general form carries a live, unguarded escape hatch in that case. For THIS arc the realistic surface is closed (content, the sole PASS_WHOLE, is correctly non-credential-bearing for Vertex; groundingMetadata is a recursed OBJECT).
    evidence: design-rev5.md section 5.1a PASS_WHOLE clause (MUST be declared per-field with a rationale ... never a default — a convention, NOT loader-enforced) plus the extended _check_inv_resp spec (validates node types only, does not constrain PASS_WHOLE placement) plus the rev5-changelog / section 5.1a rationale for choosing the general form (protecting future ops/providers whose declared nested fields could be credential-adjacent). Directly answers brief item 2.
    load_bearing: uncertain
    quadrant_classification: hard-hard

  non_findings:
  - content PASS_WHOLE is correctly BOUNDED to content alone for this arc — verified in the section 5.1a schema: PASS_WHOLE appears exactly once (candidates then content); groundingMetadata is a recursed OBJECT node, usageMetadata an OBJECT node, all leaves SCALAR. The content whole-pass rationale (model product, cannot be allow-listed without truncating the deliverable; NOT credential-bearing because the ADC bearer is request-side, never in a Vertex response body) is sound for the SCOPED M2 threat (core key-exfil). The future-provider generality gap is captured separately in r2.
  - KEY-allow-list versus VALUE-content-scanner residual (DAEDALUS wp3 residual i): a declared SCALAR leaf string value (e.g. a grounding title) or the content product passes whole; this is the CORRECT boundary, not a hole — the only core credential is the request-side ADC bearer (never in a response body); value-content artifact scanning is the seal-audit (M7) job. Discharged as correctly-placed.
  - Vertex grounding sub-tree staleness: the by-construction DROP property is INVARIANT to the exact grounding sub-keys (an undeclared new field is dropped by omission, fail-closed), and the exact shape is a build-time V-RESP-GROUNDING re-pin. The delta does NOT rest on a memorized upstream shape, so no web re-fetch is needed to clear the delta. Discharged.
  - P-M2(a.2) explicit depth-2 demonstration: unlike the VERA executed M4 approach (monkeypatched a broken permissive-default resolver to PROVE the probe catches it), P-M2(a.2) relies on the absence-assertion alone. This is LOGICALLY sufficient (an absence-assertion falsifies any emitting build) — non-load-bearing. An explicit companion (run the probe against a deliberately depth-2 build, mirroring the VERA M4 demonstration) would strengthen robustness but is not required for correctness.
  - No-other-regression (brief item 6): mechanically diffed rev4 versus rev5 (git diff --no-index: 186 insertions / 20 deletions). Every removed/changed line is INV-RESP-fold-consistent (header/changelog, M2 threat row, INV-RESP invariant plus map rows, section 4 INV-RESP bullet, section 5.3 DoD rephrasing, section 6 wp3). No line weakens multi-lane authz, INV-PRINCIPAL, seal-audit, INV-DEST/BIND/LANE, or M1/M3-M7. The section 5.3 INV-PRINCIPAL fail-closed DoD survives intact. Discharged.
  - Classification (brief item 6b; op-disc section 6.9 clause 2, A4 CONFIRM): DAEDALUS PROPOSES the depth fix is a TIGHTENING inside the already-threat-ratified M2 (no new M-number; reuses P-M2 / V-M2-RESP; content PASS_WHOLE a bounded carve-out not a new mitigation). ARGUS CONFIRMS: the depth fix introduces no new attack path or threat, deepens the existing M2 mitigation, correctly reuses the M2 A3 map row plus P-M2. Classification CORRECT.

  threat_coverage_assessment: PARTIAL (design-time probe-SPEC adequacy). M2 is a threat-ratified mitigation with an A3 map row present (section 5.2). A threat-anchored probe (op-disc section 6.13) IS specified for the OBJECT-node depth attack path — P-M2(a.2): attack-blocked (depth-3 credential in an undeclared sub-key of a declared OBJECT node is DROPPED) AND legit-unaffected (declared nested sub-key intact plus content passes whole). The ONE mapped-mitigation attack surface with NO threat-anchored probe specified is the SCALAR-slot smuggle path the section 5.1a grammar by-construction claim names (finding r1). No executed probe exists yet (design-time read); this assessment is about probe-SPEC adequacy only. Threat-ENUMERATION completeness stays ARGUS unmechanized judgment: the M-set is unchanged from the already-ratified rev4 model (no new threat introduced by the depth fix), so enumeration is not re-opened by this delta.

  items_1_to_6_explicit_answers:
  - item 1 (recursive serializer makes INV-RESP hold BY CONSTRUCTION): YES for the OBJECT-recursion path (the scoped VERA finding) — an undeclared sub-key at any depth inside a declared OBJECT node is dropped by construction; no leak constructible through the OBJECT-node path. PARTIAL for the SCALAR-slot path (r1): the grammar rule is tight but its by-construction property depends on a recursive dict-scan implementation and is not probe-guarded.
  - item 2 (content PASS_WHOLE bounded plus documented; groundingMetadata recursed; future mis-mark guarded): content is correctly bounded to content ALONE and documented; groundingMetadata is a RECURSED OBJECT node (not whole-passed); an OBJECT cannot ride a PASS_WHOLE slot for smuggling gain (PASS_WHOLE passes the value whole regardless — the field is already fully trusted). BUT a future op marking a credential-bearing field PASS_WHOLE by mistake is NOT mechanically guarded or audited (r2).
  - item 3 (P-M2 nested clause falsifies a depth-2 build): YES — the absence-assertion at depth 3 or deeper in a declared OBJECT node fails against the current depth-2 redact.py (which emits the declared sub-key value whole), and is satisfied by the recursive build. Genuine falsification, not merely that the recursive build drops it.
  - item 4 (loader refuses a legacy bare-list node so it fails closed at LOAD): YES — the extended _check_inv_resp recursively validates node types and raises LoaderError on a bare-list or unrecognized node. Regression fails closed at startup. Build-coupling note: the current registry.py bare-list schema MUST be migrated in the same change.
  - item 5 (DAEDALUS four residual questions): (1) P-M2(a.2) falsifies a depth-2 build = YES (item 3). (2) content PASS_WHOLE bounded to content alone = YES (item 2). (3) loader refuses a legacy bare-list node = YES (item 4). (4) SCALAR no-object-at-any-depth tight enough that an object cannot ride a SCALAR slot = RULE is tight by spec, but implementation-fidelity (recursive dict-scan, not shallow) and probe-coverage are open (r1).
  - item 6 (nothing else in rev4 regressed; classification correct): CONFIRMED both (see non_findings — no-other-regression plus classification).

summary: Focused cold-audit of the rev4 to rev5 INV-RESP depth-fold delta ONLY. The CORE of the delta is SOUND and closes the scoped VERA depth-2 finding BY CONSTRUCTION: redact_response recurses an OBJECT/SCALAR/PASS_WHOLE node grammar in lock-step with the value tree, so a credential in an undeclared sub-key at any depth inside a declared OBJECT node is dropped by the keep-only-declared-keys rule; groundingMetadata is a recursed OBJECT node (not whole-passed); P-M2(a.2) genuinely FALSIFIES a depth-2 build (its absence-assertion fails against the current redact.py, verified on disk, which emits a declared sub-key value whole); and the extended loader guard refuses a bare-list node so a regression fails CLOSED at load. content is the ONE correctly-bounded, documented PASS_WHOLE exception (model product, not credential-bearing for Vertex). Mechanically diffed rev4 versus rev5: the delta is confined to the INV-RESP fold — multi-lane authz, INV-PRINCIPAL, seal-audit, INV-DEST/BIND/LANE, and M1/M3-M7 are carried verbatim, none weakened; the tightening-inside-M2 with no-new-M-number classification is CONFIRMED. TWO load-bearing-UNCERTAIN residuals remain in the by-construction GENERALITY the general form claims (NOT in the scoped VERA finding, which is closed): (r1) the SCALAR no-object-at-any-depth guarantee is unprobed — P-M2(a.2) exercises the OBJECT-recursion path, not the dict-under-SCALAR-slot path, so a shallow-SCALAR implementation would pass the probe while leaving the smuggle open; (r2) PASS_WHOLE is an unaudited runtime whole-pass escape hatch with no mechanical guard on placement, so a FUTURE provider that returns a credential in its response body could have that field mistakenly marked PASS_WHOLE and bypass INV-RESP — the exact future-provider case the general form was chosen to protect. Both residuals leave the REALISTIC exfil surface CLOSED for this arc (no core credential is ever in a Vertex response body; content is correctly the sole PASS_WHOLE). Posture: PASS-WITH-CONDITIONS — the delta cleanly closes the VERA finding it was scoped to close and is cleared for ADA on the OBJECT-recursion path; the two residuals are refinements of the by-construction GENERALITY (r1 is a cheap probe-case addition; r2 is a design judgment on whether to mechanize a PASS_WHOLE placement guard now or document-and-defer with a ticket), routed to DAEDALUS and FM to decide per the fix-known-bugs posture. ARGUS surfaces; ARGUS does not fix.
```
