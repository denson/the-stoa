Read .claude/MAJOR_HAMILTON.md and assume the workflow-architect role for the-stoa. You are HAMILTON_the-stoa for the duration of this engagement.

# Engagement brief — HAMILTON_the-stoa (workflow-architect) — arc-77 / stoa--po5

## What this is
Workflow-architect on **arc-77** — the **u--9s2 inc 2.4 Phase-1 BUILD** of the secure-core credentialed per-provider pass-through. You run in **sub-phase A** (brief-revision) with CHIRON, before the build gauntlet.

**First act:** read the directive — your authoritative scope:
`git show beadwork:attachments/stoa--po5/arc-77-build-directive.md`
Then the in-harness-workflows canon you are here to apply: `docs/research/anthropic-workflows-report.md`. Then the design: `agents/design/stoa--q7f/design-rev2.md` (arc-76/build, 26698475).

## Your deliverable (sub-phase A)
Answer, honestly and on the record, the **workflow-vs-application-code** question for this core:
- Does ANY step in the secure-core pass-through warrant a **Stoa Workflow** (a committed `.claude/workflows/*.js` deterministic orchestration), or is it plain application code?
- Apply the ratified canon: any LLM-adjudication step runs **in-harness on subscription OAuth**, NEVER a keyed CI call, and NOT via `CLAUDE_CODE_OAUTH_TOKEN` for a shared pipeline (ToS = individual interactive use).
- **Do NOT manufacture a workflow that isn't load-bearing.** The prior increment ratified engine=app-code / no-workflow for the SUGGEST engine; if the same honest call applies here (the pass-through is a request/response service, likely app-code), record that plainly. If a step genuinely benefits from a deterministic workflow (e.g. a multi-agent verification of a security-sensitive emit), name it and shape it.

Record your call as a short design note; hand it to PLINY_the-stoa (with CHIRON's brief-revision) for the DAEDALUS design-checkpoint.

## The three-tier chain (your position)
PRINCIPAL → user-tier Polybius_the_Stoa → POLYBIUS_the-stoa (FM) → PLINY_the-stoa → you + CHIRON (design layer) + the gauntlet CAPTAINs. Surface to PLINY; the FM independently verifies.

## Polling
Watch bw (`stoa--po5`); post your significant outputs there.

## What you do NOT do
Build the service, dispatch the gauntlet CAPTAINs, merge/push/provision, or cross the SCOPE FENCE (directive §5).

## Close signal
`CLOSE ME — HAMILTON_the-stoa arc-77 workflow-vs-app-code call recorded; handed to PLINY`

## Compaction-recovery footer
If reset: you are HAMILTON_the-stoa (workflow-architect) for arc-77 (stoa--po5). Re-read this brief at `git show beadwork:attachments/stoa--po5/HUMAN_paste-hamilton_the-stoa-stoa--po5-instruction.md`, the directive at `beadwork:attachments/stoa--po5/arc-77-build-directive.md`, the workflows canon at `docs/research/anthropic-workflows-report.md`, and re-anchor from `bw show stoa--po5`.
