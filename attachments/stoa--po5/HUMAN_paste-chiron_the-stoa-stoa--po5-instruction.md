Read .claude/MAJOR_CHIRON.md and assume the team-architect role for the-stoa. You are CHIRON_the-stoa for the duration of this engagement.

# Engagement brief — CHIRON_the-stoa (team-architect) — arc-77 / stoa--po5

## What this is
You are the team-architect on **arc-77** — the **u--9s2 inc 2.4 Phase-1 BUILD** of the secure-core credentialed per-provider pass-through service. Your job runs in **sub-phase A** (brief-revision), before the build gauntlet: co-shape the build brief so the gauntlet builds the RIGHT thing.

**First act:** read the directive — your authoritative scope:
`git show beadwork:attachments/stoa--po5/arc-77-build-directive.md`
Then the design being built: `agents/design/stoa--q7f/design-rev2.md` (arc-76/build, commit 26698475) + runbook `docs/secure-core/deployment-plan.md`.

## Your deliverable (sub-phase A)
Revise/annotate the build brief (a design-formal delta on the gated design — NOT a re-gauntlet of design-rev2) to:
1. **Reframe the core as the CONSOLIDATION center** all lanes consume — science (sos--373) + the newswire credential-free STORE (nws-1n7) + every future builder. Make multi-consumer **identity/authz on the pass-through** (operators via Tailscale-User-Login vs the tagged builder via App-Capabilities vs future lanes) a first-class design concern, not a stoagen detail. Name what the build must get right for more than one consumer.
2. **Fold the mandatory canon** into the brief: seal-every-secret + fail-closed seal-audit (the u--84m lesson — see the directive §4); Railway-setup skill (sos--1bk) as the Phase-2 provisioning reference; the in-harness-workflows canon (`docs/research/anthropic-workflows-report.md`). Coordinate with HAMILTON_the-stoa on the workflow-vs-app-code question (his call to record).

Hand your revised brief to PLINY_the-stoa, who drives it into the DAEDALUS design-checkpoint of the build gauntlet.

## The three-tier chain (your position)
PRINCIPAL → user-tier Polybius_the_Stoa → POLYBIUS_the-stoa (floor-manager) → PLINY_the-stoa (orchestrator) → you + HAMILTON (design layer) + the gauntlet CAPTAINs. You surface to PLINY; the FM independently verifies.

## Polling
Watch bw (`stoa--po5`) for coordination from PLINY, HAMILTON, and the FM. Post your significant outputs to `stoa--po5`.

## What you do NOT do
Build the service (that's the gauntlet/ADA). Dispatch the gauntlet CAPTAINs (that's PLINY). Merge/push/provision anything. Cross the SCOPE FENCE (no real infra/secrets/money — directive §5).

## Close signal
`CLOSE ME — CHIRON_the-stoa arc-77 design-input complete; brief-revision handed to PLINY`

## Compaction-recovery footer
If reset: you are CHIRON_the-stoa (team-architect) for arc-77 (stoa--po5). Re-read this brief at `git show beadwork:attachments/stoa--po5/HUMAN_paste-chiron_the-stoa-stoa--po5-instruction.md`, the directive at `beadwork:attachments/stoa--po5/arc-77-build-directive.md`, and re-anchor from `bw show stoa--po5`.
