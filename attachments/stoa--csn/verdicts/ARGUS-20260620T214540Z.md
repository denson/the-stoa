status: completed
ticket: stoa--csn
verdict: revise
design_artifact_audited: .claude/worktrees/arc-69-build/agents/design/stoa--csn/design-rev1.md
overall: PROCEED-WITH-CONDITIONS

audit_block:
  risks:
  - id: r1
    description: ez9 allow-list PARSER is new, unspecified, security-critical code. The block form ("- publisher: CalGEM") is INVISIBLE to extract_author_fields, so the design claim that extraction reuses the md-frontmatter slice already in _hooklib.sh is FALSE; the gate needs a NEW dash-list YAML parser no existing function provides.
    evidence: design 2.B:102; live probe - frontmatter with a dashed allow-entry through extract_author_fields md emits ONLY "publisher CalGEM", the dashed "- author:" entry is NOT emitted. _hooklib.sh:160-177 md-mode regex cannot match a leading "- ".
    load_bearing: true
    severity: BLOCKER
    quadrant_classification: easy-easy
  - id: r2
    description: design narrative and design fixtures use DIFFERENT, divergently-parsed allow-list FORMS. 2.B narrative uses block form (stoa-author-gate-allow: then indented "- publisher: CalGEM"); fixtures ctl5/ctl6/tp12 use inline-array form (stoa-author-gate-allow: [publisher: CalGEM]). Form not pinned; a parser for one silently fails the other, and the SECURITY control tp12 is only proven for whichever form the test exercises.
    evidence: design 2.B:93-100 (block) vs 2.B:113-116 (inline-array). Live - the two forms parse differently under extract_author_fields.
    load_bearing: true
    severity: BLOCKER
    quadrant_classification: easy-easy
  - id: r3
    description: ez9 introduces a SECOND mirrored-not-sourced security-critical block in the test harness. compute_outcome (run-author-gate-tests.sh:123-140) already MIRRORS is_principal rather than sourcing it; the allow-filter (field-whitelist + exact-pair-match) must be ADDED as a second mirror, doubling gate-vs-harness drift surface for the keystone boundary. A harness that proves the allow-filter blocks does NOT prove the GATE copy does.
    evidence: run-author-gate-tests.sh:56-76 (is_principal mirror) + :120-140 (compute_outcome); design 2.B:118. The gate real allow-filter has NO live-probe backstop specified (only fixture-mirror coverage).
    load_bearing: true
    severity: MAJOR
    quadrant_classification: hard-easy
  - id: r4
    description: 3nh check.sh fix as written (count-guard + bare exit 2 inside enumerate_deployed) does NOT fail loud and is actively HARMFUL. exit-2 inside the process-substitution at check.sh:843 does not abort the parent; the parent reads the partial enumeration and continues rc=0. Because the MAJOR glob is FIRST in enumerate_deployed (414-419, before operating-disciplines/CAPTAINs/skills/hooks), a guard-exit there TRUNCATES all subsequent enumeration, and Pass-2 OBSOLETE (852+) could then flag legitimately-deployed files as obsolete.
    evidence: EMPIRICALLY PROVED - a while-read loop consuming a function that exits 2 via process-substitution reaches AFTER-LOOP with rc=0 (parent did not abort; trailing lines lost). check.sh:414-419 + :843 + :845-870. Design 2.C:182 flags this but defers to ADA "MUST verify" without committing the sentinel/caller-guard mechanism.
    load_bearing: true
    severity: BLOCKER
    quadrant_classification: easy-hard
  - id: r5
    description: 3nh install.sh and check.sh sites need DIFFERENT abort mechanisms but the design treats them with one fix-shape. write_substrate_manifest is called DIRECTLY (install.sh:1907) so err()/exit DOES abort the parent; enumerate_deployed is via process-substitution so it does NOT. The "mirror the same guard at both sites" conflates two structurally different abort surfaces.
    evidence: install.sh:1907 (direct) vs check.sh:843 (process-sub); design 2.C:160,180.
    load_bearing: false
    severity: MINOR
    quadrant_classification: easy-easy
  - id: r6
    description: luo proposes mechanizing attach_status as an in-block stdout echo "attach_status: OK" and reframing the prose to say it is NOW emitted BY THE BLOCK (mechanized) not by prose adherence. But save-verdict.md clause (d) clause 1 is marked LOCKED and specifies attach_status is a first-class field in the seat DISPATCH RETURN, explicitly NOT a scroll-past stderr echo. Block-stdout and dispatch-return are different surfaces; reframing risks weakening the LOCKED orchestrator-retry contract (PLINY keys retry off the dispatch-return field).
    evidence: save-verdict.md:97-104 (LOCKED clause 1); design 2.C:134-152 (in-block echo + prose reframe).
    load_bearing: true
    severity: MAJOR
    quadrant_classification: easy-easy
  - id: r7
    description: y12 cpat year-requirement residual + adversarial year-bearing prose. Design accepts that yearless "Copyright (c) Acme Corp" slips (documented tradeoff) but the end-anchor + case-sensitive name run is the only guard against year-bearing prose. Sound for probed cases but the corpus must include an adversarial year-bearing-prose-in-cfg control, which the design only suggests ("ADA: add such a fixture if cheap") rather than requires.
    evidence: design 2.A:70 + 2.A:83 (optional, not required). cpat at 2.A:60-67.
    load_bearing: false
    severity: MINOR
    quadrant_classification: easy-easy
  - id: r8
    description: ez9 allow mechanism has NO live in-repo consumer. Zero publisher/vendor/source/provider frontmatter fields exist in substrate today, so the ez9 fixtures are the ONLY exercise of the new security-critical path; no real-artifact regression surface catches a mis-integration the fixtures own assumptions miss.
    evidence: live grep for the four allow-fields in substrate returns nothing. New key stoa-author-gate-allow also absent (collision-free).
    load_bearing: false
    severity: MINOR
    quadrant_classification: hard-hard
  non_findings:
  - save-verdict 4-home byte-alignment VERIFIED all 4 byte-identical today (sha de485e336a73562c, 35 lines); design claim accurate; realign-to-42 risk is real but the design sha256 loop is the correct DC3 probe.
  - stoa-author-gate-allow key collision VERIFIED zero existing occurrences; new key is collision-free.
  - ez9 field-whitelist concept (publisher/vendor/source/provider allow-able; author/owner/creator/created_by/maintainer/by/copyright/holder NEVER allow-able) - the EXCLUSION boundary is sound IN PRINCIPLE; the load-bearing authorship fields are correctly excluded. The risk is in the PARSER (r1/r2), not the field-set choice.
  - p41.3 curl --max-time 8 VERIFIED the timeout 10 wrapper exists (sessionstart-substrate-check.sh:114-115); --max-time bounds whole transfer; 8<10 correct. Sound, low-risk.
  - p41.4/p41.3 .claude-copy-vs-regen (DAEDALUS residual 2) RESOLVED against ground truth - the-stoa git-TRACKS both substrate and .claude copies (git ls-files confirms). The arc commits both; editing both is correct. RESIDUAL drift-risk - hand-edits to both must match install.sh regen output or latent drift is introduced.
  - kr7 scope-split (DAEDALUS residual 3) - FIX (install.sh:896 echo) is one-spot + zero-behavior; the RE-SCOPED fresh-DEPLOY glob (:963-1007) already hand-enumerates all 4 current MAJORs correctly - split sound, RE-SCOPED half NOT half-done. Acceptable.
  - kr7 NEW frontmatter key acceptability (DAEDALUS residual 1) - collision-free; acceptability + doc-home is a process question for the FM, not a runtime risk.
  threat_coverage_assessment: ez9 carries an A3 M1 map (2.B:109-110) + a threat-anchored probe (tp12, 2.B:115). Probe-SPEC presence ADEQUATE at design time. BUT probe VALIDITY is contingent on r1/r2 (parser/form must be pinned first) and r3 (the gate REAL allow-filter, not just the harness mirror, must be what tp12 exercises downstream). VERA/CATO tier-ii - confirm tp12 drives the REAL gate allow-filter, not only compute_outcome mirror.
attach_status: OK
summary: |
  design-rev1 is a sound DC0 grounding (7 FIX / 3 ALREADY-FIXED / 2 RE-SCOPE; dispositions independently confirmed by NOMOS + FM) with a correctly-narrow FIX set. The non-keystone edits (y12, luo-mechanics, kr7-narrow, p41.3, p41.4) are buildable with minor conditions. The SECURITY KEYSTONE - ez9 reviewed-allow - is NOT YET BUILDABLE AS WRITTEN: the allow-list PARSER is new, unspecified, security-critical code (r1); the narrative and fixtures use two divergently-parsed allow-list forms (r2); the gate real allow-filter gains a second mirrored-not-sourced harness block with no live-probe backstop specified (r3). The single most important load-bearing risk is r4 - the 3nh check.sh FAIL-LOUD fix as written does NOT fail loud and is actively harmful (EMPIRICALLY PROVED exit-2 inside the process-substitution does not abort the parent, truncating enumeration and risking false-OBSOLETE flagging). luo (r6) must NOT reframe the LOCKED dispatch-return attach_status contract as satisfied by an in-block stdout echo. Posture - PROCEED-WITH-CONDITIONS: build the non-keystone source edits now; ez9 and 3nh-check.sh need a design revision pinning the allow-list form+parser and the abort mechanism (sentinel/caller-guard) before build. The field-whitelist field-SET choice is sound; the danger is entirely in the parser and the abort plumbing.
