status: completed
ticket: stoa--csn
verdict: pass
overall: PROCEED-WITH-CONDITIONS
design_artifact_audited: .claude/worktrees/arc-69-build/agents/design/stoa--csn/design-rev2.md
re_audit_of: agents/verdicts/stoa--csn/ARGUS-20260620T214540Z.md (rev1 risks r1-r8)

audit_block:

  rev1_risk_closure:
  - id: r1
    rev1_severity: BLOCKER
    status: CLOSED
    evidence: rev2 sec1.2 specs a NEW parse_allow_pairs (NOT a reuse of extract_author_fields md-slice). Built the rev2 program verbatim and live-probed 8 adversarial inputs through it. author-allow attempt -> EMPTY (author not in ALLOW_FIELDS set). The rev1 hole (block-form dash-entries invisible to the extractor) is moot - rev2 PINS inline-array, block-form yields EMPTY (fail-toward-enforcement). PROVED.
  - id: r2
    rev1_severity: BLOCKER
    status: CLOSED
    evidence: form PINNED to single-line inline-array in narrative sec1.1 AND all 5 ez9 fixtures (tp11/ctl5/tp12/ctl6/tp13). No residual block-form-in-prose vs inline-in-fixtures split. tp13 is the MANDATORY form-pinning regression control (block-form -> BLOCK). Divergent-parse risk eliminated.
  - id: r3
    rev1_severity: MAJOR
    status: CLOSED-WITH-RESIDUAL (acceptable)
    evidence: rev2 sec1.5 PINS parse_allow_pairs into _hooklib.sh; the harness already sources _hooklib.sh (run-author-gate-tests.sh:54) so the parser is SOURCED not mirrored (verified function inventory + source line). The 2nd-mirror is REDUCED to a 4-line is_pair_allowed skip-glue in compute_outcome (a reimplementation of the gate sub-check-2 loop, so the skip-glue cannot avoid being mirrored). MANDATORY P-ez9-live drives the REAL gate end-to-end as the DoD backstop; a mirror-only pass MUST NOT satisfy DoD. Residual mirror is bounded + backstopped -> acceptable.
  - id: r4
    rev1_severity: BLOCKER
    status: CLOSED
    evidence: rev2 sec2.1 replaces the bare in-process-sub exit (PROVED does not abort the parent) with a SENTINEL line emitted as the SOLE output of an empty-MAJOR-glob, caught by a caller-guard at the check.sh:786-843 consumer, which exits 2 in check_workspace scope (called directly from main, NOT via process-sub) -> propagates. Verified check.sh:414-419 (MAJOR glob FIRST) + :843 (process-sub consumer). The abort moves to a scope where exit actually aborts.
  - id: r5
    rev1_severity: MINOR
    status: CLOSED
    evidence: rev2 sec2.2 + the two-mechanism table correctly distinguish the two abort surfaces. install.sh write_substrate_manifest is a DIRECT call (verified install.sh:1918) inside a brace-group writer (opens :518, closes :564 - NOT a subshell), so err() (verified install.sh:274-277 does exit 2) propagates. maj_count incremented inside the brace group persists out (brace groups share parent scope). The assert lands AFTER the brace group closes. Sound.
  - id: r6
    rev1_severity: MAJOR
    status: CLOSED
    evidence: Read the full LOCKED clause-(d)-clause-1 (save-verdict.md:97-107). rev2 sec3 mechanizes ONLY attach_rc capture (same line as bw attach) + a stderr breadcrumb; does NOT add an attach_status echo to stdout, does NOT move the contract into the block. The LOCKED sentences (first-class attach_status field in the seat dispatch return, NOT a scroll-past stderr echo; the load-bearing signal PLINY keys its retry/escalate obligation off) are present VERBATIM and untouched; rev2 adds ONE additive sentence tying the field to the captured rc. r6 not reintroduced.
  - id: r7
    rev1_severity: MINOR
    status: CLOSED
    evidence: rev2 sec4 promotes the adversarial year-bearing-prose-in-cfg control (ctl-y12-year-prose-in-cfg) from if-cheap to MANDATORY. The trailing lowercase run breaks the name-run-to-EOL anchor so cpat must NOT match -> ALLOW. Now a required regression row.
  - id: r8
    rev1_severity: MINOR
    status: ACCEPTED-RESIDUAL (unchanged, acceptable)
    evidence: no live in-repo consumer (zero publisher/vendor/source/provider frontmatter fields in substrate). rev2 mitigates by making P-ez9-live drive the REAL gate; cannot manufacture a real consumer. Acceptable MINOR residual per residual-question 3.

  rev2_new_finding_audit:
  - id: CR-normalization
    daedalus_claim: CRLF/Windows checkout appends CR to the extracted value, false-BLOCKing legit ez9 citations unless tr -d CR lands on BOTH sides of the exact-pair compare.
    argus_finding: REAL and load-bearing - CONFIRMED at byte level. extract_author_fields md-mode emits values where git-bash command-substitution capture injects CR on the inter-record boundary, so the gate read loop sees val=CalGEM+CR. parse_allow_pairs strips CR (text.replace). Without normalization the exact-pair compare CalGEM+CR != CalGEM FAILS -> ctl5 false-BLOCKs. The rev2 fix (tr -d CR on the extracted field/val AND the allow-pair field/val) is COMPLETE - simulated is_pair_allowed with both-side tr -d CR: legit ALLOWs, mismatch correctly rejected. The allow-pair output ALSO carries CR on its separator and rev2 normalizes the allow side, so it is covered.
    precision_note: rev2 FRAMES this as a CRLF/Windows-checkout issue. My probe shows the CR appears on EVERY git-bash run regardless of file line endings (injected on the command-substitution stdout boundary), so the exposure is UNCONDITIONAL on the PRINCIPAL platform - not contingent on a CRLF checkout. The fix is in the right place; the framing understates how often the path is hot. The existing gate is unaffected because is_principal sed space-class trim already strips trailing CR; the NEW is_pair_allowed does EXACT equality with no trim, which is exactly why tr -d CR is load-bearing there. Raised as condition C2.

  risks:
  - id: c1
    description: rev2 INTERNAL CONTRADICTION on parser home. sec1.2 header (line 44) + line 46 state parse_allow_pairs lives in the GATE script (NOT _hooklib.sh); sec1.5 (line 141) + sec4 (346) + weak-point-1 (377) state the PINNED DECISION is _hooklib.sh. ADA could build to the stale sec1.2 text and re-introduce the 2nd mirror r3 closes.
    attack_path: an executor reading sec1.2 top-down implements the parser in the gate; the harness must then mirror is_pair_allowed AND parse_allow_pairs (the worse drift surface rev2 weak-point-1 names), and the r3 closure silently regresses.
    evidence: design-rev2.md:44,46 vs :141,346,377.
    load_bearing: true
    severity: CONDITION
    quadrant_classification: easy-easy
  - id: c2
    description: CR-normalization probe (P-ez9-live CR sub-case) must be MANDATORY-not-optional AND must run on the PRINCIPAL native git-bash, because the CR is injected unconditionally on every git-bash capture, not only on a CRLF checkout. rev2 sec1.6 RISK-IF-WRONG (c) + sec4 phrase it as run on a CRLF-checked-out fixture, which a builder could read as niche/optional.
    attack_path: ADA implements tr -d CR on only ONE side (the single most-likely mis-build per rev2 weak-point-2); the CR sub-case is treated as optional/skipped; ctl5 false-BLOCKs on the PRINCIPAL machine; the whole ez9 feature silently dies the first time a real publisher field ships.
    evidence: byte-probe author+TAB+Denson Smith+CR+LF+publisher+TAB+CalGEM; is_principal tolerates CR via sed trim but is_pair_allowed exact-equality does not; design-rev2.md:158,342,379.
    load_bearing: true
    severity: CONDITION
    quadrant_classification: easy-easy
  - id: c3
    description: P-fullsuite (rev2 sec4) proposes install.sh --dry-run to confirm 3nh manifest guard does not brick a normal deploy. But DRY_RUN early-returns at install.sh:515 BEFORE the MAJOR loop (:545) and the proposed maj_count guard (post-:564), so a dry-run CANNOT exercise the 3nh install.sh guard at all. The guard abort must be proved by a REAL (non-dry-run) execution against an empty SCRIPT_DIR.
    attack_path: VERA reads the P-fullsuite dry-run line as covering the install.sh manifest guard; the guard is never exercised by a real run; a regression that makes the guard never fire (future subshell refactor of the brace group, or assert misplaced inside the brace group) ships unverified. The directive-DoD-must-match-tool-reality lesson - a dry-run cannot verify a check it early-returns past.
    attack_path_note: P-3nh-abort (rev2 sec4) DOES specify the correct real-execution probe (empty SCRIPT_DIR -> err() fires -> install.sh exits 2). The condition is only that P-3nh-abort, not the P-fullsuite dry-run, is the authoritative install.sh-guard verification.
    evidence: install.sh:513-516 (DRY_RUN early return) vs :545 (MAJOR loop) + the post-brace-group assert site; design-rev2.md sec4 P-fullsuite + P-3nh-abort.
    load_bearing: true
    severity: CONDITION
    quadrant_classification: easy-easy
  - id: c4
    description: residual harness mirror - compute_outcome must still mirror the 4-line is_pair_allowed skip-glue (the parser itself is sourced, not this). A harness GREEN does NOT prove the GATE is_pair_allowed skip works; only P-ez9-live does. Bounded remnant of r3.
    attack_path: the skip-glue mirror in compute_outcome diverges from the gate copy (gate adds tr -d CR but mirror does not, or vice-versa); the harness passes 27/27 while the real gate ALLOWs or BLOCKs differently. Caught ONLY if P-ez9-live is run and treated as the DoD gate.
    evidence: run-author-gate-tests.sh:123-140 (compute_outcome reimplements sub-check-2); design-rev2.md:141.
    load_bearing: false
    severity: CONDITION
    quadrant_classification: hard-easy

  non_findings:
  - parse_allow_pairs adversarial closure - PROVED LIVE across 8 inputs (author-allow EMPTY, value-mismatch marker-only, substring exact-equality, newline-smuggle EMPTY, embedded-second-field verbatim-value-cannot-suppress-real-author, unicode-lookalike byte-distinct, body-marker EMPTY, block-form EMPTY). The rev2 sec1.4 threat table matches observed behavior exactly. Discharged.
  - ALLOW_FIELDS is a hard in-code Python set literal publisher/vendor/source/provider, not a file-read list - confirmed in the rev2 program; author/owner/creator/etc. dropped before entering the set. Field-set boundary sound.
  - 4-home byte-aligned region IDENTICAL today - sha de485e336a73562cdee59b42b8fcd7bbe734f7a23f6ff538bf4216602cb8c553 across all 4 (save-verdict.md, CAPTAIN_VERA/ARGUS/CATO.md). rev2 claimed sha matches. The DC3 sha256 loop is the correct post-edit gate; ADA keeps all 4 identical AND re-runs it; ARGUS/CATO re-run.
  - luo LOCKED sentences present VERBATIM (save-verdict.md:99-104) - the rev2 additive sentence does not reframe; r6 not reintroduced.
  - 3nh sentinel string has no .claude/ prefix so cannot collide with a real path - sound.
  - install.sh err() does exit 2 (install.sh:274-277); brace-group writer is not a subshell so maj_count persists out - both verified; the rev2 placement-subtlety note is correct.
  - harness baseline 16/16 GREEN today (DC1 do-no-harm) - verified by running run-author-gate-tests.sh. rev2 27-row target = 16 + 6 y12 + 5 ez9.

  threat_coverage_assessment: |
    ez9 carries an A3 M1 map (design-rev2.md sec1.6:156) + a threat-anchored probe SPEC-d in section 3/4 (tp12 the hole-closing control, with tp13 form-pinning + ctl6 exact-pair as supporting controls + P-ez9-live driving the REAL gate). Design-time probe-SPEC adequacy: ADEQUATE - a threat-anchored probe (exercises the named M1 attack path: author riding a forged allow-marker -> asserts BLOCK; and asserts legit publisher unaffected -> ctl5 ALLOW) IS spec-d per the mapped mitigation. The section 6.9/35.4 map-present-AND-probe-present test passes at tier-i (mechanical presence). The tier-ii judgment (does the EXECUTED probe genuinely drive the gate REAL allow-filter, not just the harness mirror) is VERA/CATO downstream and is GATED by C2 (CR-normalization both sides) + C4 (P-ez9-live is the authoritative backstop). Threat-ENUMERATION completeness for M1 (8 named variants) is my judgment - the enumerated set is strong and each variant is proved-defeated, but I do not represent coverage as proof of total threat-defeat - a variant outside the 8 (a future multi-line allow form, explicitly UNSUPPORTED + out-of-scope in rev2) would need its own re-proof. Honest-claim boundary held.

  residual_question_rulings:
  - q1 (parse_allow_pairs in _hooklib.sh acceptable vs the gate): ACCEPTABLE. Verified the harness already sources _hooklib.sh, so the lib home makes the parser SOURCED with zero new mirror - strictly better than gate-home (which would force the harness to mirror the parser too). The lib already hosts classify + extract primitives; one more pure stdin-to-stdout function is consistent. CONDITION C1 - resolve the sec1.2-header/line-46 stale text to the PINNED _hooklib.sh decision.
  - q2 (luo LOCKED sentences all present verbatim after the 1-sentence addition): YES. save-verdict.md:99-104 LOCKED sentences present verbatim; addition is additive-only, does not reframe the dispatch-return contract. r6 not reintroduced.
  - q3 (ez9 r8 no live consumer, fixtures+live-probe only): ACCEPTABLE MINOR residual. P-ez9-live driving the REAL gate is the best available exercise absent a real consumer. Flagged, accepted.

attach_status: OK
summary: |
  rev2 genuinely closes all 8 rev1 risks (3 BLOCKER + 2 MAJOR + 3 MINOR) - not by rewording but by mechanism, each independently re-verified against the REAL source this cycle. The ez9 keystone parser was built verbatim and live-probed: the author-attribution hole (r1) is PROVED closed across all 8 named adversarial variants through the actual rev2 program; the form is PINNED to inline-array consistently in narrative AND fixtures (r2); the parser is relocated to _hooklib.sh so the harness SOURCES not mirrors it, with a MANDATORY real-gate live-probe backstop (r3). The 3nh FAIL-LOUD fix now actually fails loud: a sentinel+caller-guard at the process-sub site (where I PROVED a bare exit does not abort) and a direct err()/exit at the brace-group direct-call site - two structurally-correct mechanisms (r4/r5, both source-verified). luo mechanizes ONLY the rc-capture and leaves the LOCKED dispatch-return attach_status contract verbatim (r6); the 4-home region is byte-identical today. The rev2 CR-normalization NEW FINDING is REAL, load-bearing, and its both-side tr -d CR fix is COMPLETE - confirmed at byte level; my one refinement is the CR exposure is UNCONDITIONAL on git-bash (every capture), broader than the CRLF-checkout framing, raising C2. Overall posture: PROCEED-WITH-CONDITIONS. No risk is left open - the keystone is clear to build. Four CONDITIONS carry into the ADA brief + VERA DoD: C1 resolve the rev2-internal parser-home contradiction to the PINNED _hooklib.sh decision; C2 both-side tr -d CR is mandatory and the P-ez9-live CR sub-case is a hard DoD gate run on native git-bash; C3 prove the install.sh 3nh guard via P-3nh-abort REAL execution NOT the --dry-run smoke which early-returns past it; C4 P-ez9-live (not the harness mirror) is the authoritative gate verification for the 4-line is_pair_allowed glue. The field-set boundary, the abort plumbing, the LOCKED contract, and the byte-alignment are all sound.
