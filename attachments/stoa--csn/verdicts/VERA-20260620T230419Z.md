status: completed
ticket: stoa--csn
verdict: pass
design_artifact_verified_against: agents/design/stoa--csn/design-rev2.md (+ rev1 for unchanged clean items)
build_verified: arc-69/build @ HEAD 34db584 (4 commits: e7d309a kr7-narrow/p41.3/p41.4 | d9fd259 y12+ez9 | 443edbf luo | 34db584 3nh); worktree C:\Users\denso\claude_projects\the-stoa\.claude\worktrees\arc-69-build

probes_executed:
- probe_id: C4-tp11
  description: ez9 tp11 (publisher, no marker) through the REAL gate pretooluse-author-field-audit.sh
  quadrant_classification: easy-easy
  command_or_method: synthetic PreToolUse git-commit event fed to the real gate; fixture staged at docs/x.md in a throwaway git repo
  expected: BLOCK (permissionDecision deny)
  observed: BLOCK -- deny, reason "author-like field \"publisher\" ... names \"CalGEM\", which is not the PRINCIPAL"
  result: pass
- probe_id: C4-ctl5
  description: ez9 ctl5 (publisher: CalGEM + [publisher: CalGEM]) -- legit reviewed-allow path through the REAL gate
  quadrant_classification: easy-easy
  command_or_method: same harness; fixture control/ctl5-publisher-allowed.md.fixture
  expected: ALLOW (gate exits 0, no deny JSON)
  observed: ALLOW -- stdout EMPTY, exit 0. Reviewed-allow path closes the hole WITHOUT false-blocking.
  result: pass
- probe_id: C4-tp12
  description: ez9 tp12 (author: Fenwick Galsworthy + [author: Fenwick Galsworthy]) -- the M1 attack path through the REAL gate
  quadrant_classification: easy-easy
  command_or_method: same harness; fixture tp/tp12-author-allow-attempt.md.fixture
  expected: BLOCK (parse_allow_pairs drops the author field BEFORE it enters the allow set)
  observed: BLOCK -- deny, reason "author-like field \"author\" ... names \"Fenwick Galsworthy\", which is not the PRINCIPAL". The forged allow-marker did NOT suppress the authorship-claim block. M1 HOLE CLOSED.
  result: pass
- probe_id: C4-ctl6
  description: ez9 ctl6 (publisher: CalGEM + [publisher: NOAA]) -- value-mismatch / stale marker through the REAL gate
  quadrant_classification: easy-easy
  command_or_method: same harness; fixture control/ctl6-publisher-mismatch.md.fixture
  expected: BLOCK (exact-pair compare: (publisher,CalGEM) not in allow set {(publisher,NOAA)})
  observed: BLOCK -- deny on publisher CalGEM. Stale/mismatched marker is NOT a blanket allow.
  result: pass
- probe_id: C4-tp13
  description: ez9 tp13 (publisher: CalGEM + UNSUPPORTED block dash-list marker) through the REAL gate
  quadrant_classification: easy-easy
  command_or_method: same harness; fixture tp/tp13-blockform-not-allowed.md.fixture
  expected: BLOCK (only the inline-array regex matches; block form yields zero allow pairs; fail-toward-enforcement)
  observed: BLOCK -- deny on publisher CalGEM. Wrong-form marker is fail-safe, not silently widened.
  result: pass
- probe_id: C2-cr-asymmetric
  description: prove the both-side tr -d CR strip does real work (asymmetric-CR false-block reproduced) on native git-bash
  quadrant_classification: easy-hard
  command_or_method: isolated is_pair_allowed test + full-gate run of ctl5 CRLF-staged (core.autocrlf true; staged blob carries 12 CR-lines) through real gate vs a CR-strip-removed gate variant
  expected: WITH extracted-side strip -> ALLOW (no false-block); WITHOUT strip + asymmetric CR -> FALSE-BLOCK
  observed: A (real gate, extracted-side strip present) CalGEM==CalGEM -> MATCH -> ALLOW (correct). B (no extracted strip, asymmetric CR) CalGEM\r != CalGEM -> NO-MATCH -> FALSE-BLOCK (design failure mode reproduced). Real gate ctl5 CRLF-staged on native git-bash -> ALLOW. NOTE: on this platform Windows-python text-mode stdout translates \n->\r\n so BOTH parse_allow_pairs and extract_author_fields emit a trailing \r on EVERY line; the both-side tr -d strip reconciles them. The full-gate symmetric-CR case cancels, but the asymmetric case (content-CR vs normalized) is the load-bearing one and is correctly defended.
  result: pass
- probe_id: C3-checksh-abort
  description: check.sh enumerate_deployed process-sub site aborts rc=2 on empty MAJOR glob (REAL exec, NOT --dry-run)
  quadrant_classification: easy-easy
  command_or_method: throwaway substrate (zero MAJOR_*.md) + project-tier workspace (.claude/MAJOR_POLYBIUS.md); bash check.sh --workspace <ws> (calls check_workspace direct from main)
  expected: FATAL "zero MAJOR_*.md" on stderr + rc=2 (sentinel emitted, caller-guard catches post-loop, exit 2 propagates)
  observed: "check.sh: FATAL: zero MAJOR_*.md globbed ... Aborting." rc=2. NO silent rc=0-with-truncated-enumeration.
  result: pass
- probe_id: C3-checksh-donoharm
  description: check.sh do-no-harm -- non-empty MAJOR substrate must NOT trip the guard
  quadrant_classification: easy-easy
  command_or_method: throwaway substrate with 2 MAJOR_*.md; bash check.sh --workspace <ws>
  expected: no FATAL, rc=0, normal drift output
  observed: rc=0, no FATAL message, normal MISSING-drift output. Guard does NOT false-fire.
  result: pass
- probe_id: C3-installsh-abort
  description: install.sh write_substrate_manifest direct-call site aborts rc=2 on empty SCRIPT_DIR MAJOR glob (REAL exec, NOT --dry-run)
  quadrant_classification: easy-easy
  command_or_method: extracted err() + write_substrate_manifest into a harness; DRY_RUN=0; SCRIPT_DIR=<empty>; called write_substrate_manifest real
  expected: err() "zero MAJOR_*.md globbed" + exit 2; no partial manifest left live (per design claim)
  observed: err() fired, rc=2 -- ABORT confirmed. HOWEVER a partial header-only manifest (379 bytes, zero NAME_SUFFIX rows) IS left at $DEST/.substrate-manifest because the brace-redirect `} > "$manifest"` writes the header BEFORE the post-brace guard fires. The design's secondary claim "the partial manifest is not left as the live artifact path" is FALSIFIED. Primary FAIL-LOUD objective (rc=2 + loud message) is met; the residual is a no-op header-only manifest (zero data rows -> apply.sh produces zero substitutions, never a wrong one).
  result: pass
- probe_id: C3-installsh-donoharm
  description: install.sh do-no-harm -- non-empty MAJOR SCRIPT_DIR must NOT trip the guard
  quadrant_classification: easy-easy
  command_or_method: harness with SCRIPT_DIR holding 2 MAJOR_*.md; real write_substrate_manifest
  expected: manifest written with MAJOR rows, rc=0
  observed: "wrote manifest", rc=0, 2 NAME_SUFFIX rows present. Guard does NOT false-fire.
  result: pass
- probe_id: y12-truepos
  description: y12 -- the original true-positive MISS (Copyright (c) <year> <foreign-name>) is now caught
  quadrant_classification: easy-easy
  command_or_method: extract_author_fields cfg on "Copyright (c) 2026 Fenwick Galsworthy"; tp8 (c-form) + tp9 (symbol-form) fixtures
  expected: extracts copyright<TAB><foreign> -> BLOCK; ctl4 (Denson) -> ALLOW
  observed: extracts "copyright<TAB>Fenwick Galsworthy" (tp8+tp9 same); ctl4 -> "copyright<TAB>Denson Smith" (ALLOW). Original MISS caught.
  result: pass
- probe_id: y12-yearprose-control
  description: y12 MANDATORY control ctl-y12-year-prose-in-cfg -- year-bearing prose that is NOT an authorship claim must still ALLOW (no false-block)
  quadrant_classification: easy-easy
  command_or_method: extract_author_fields cfg on "Copyright 2026 was a busy year for Acme and its many contributors."
  expected: no extraction -> ALLOW (name-run-to-EOL anchor fails on trailing lowercase run)
  observed: extract output EMPTY -> ALLOW. No false-block on year-prose.
  result: pass
- probe_id: luo-rc-capture
  description: luo -- rc-capture mechanized (attach_rc=$? on the bw attach line) in all 4 byte-aligned homes
  quadrant_classification: easy-easy
  command_or_method: grep "bw attach.*attach_rc=$?" across save-verdict.md + CAPTAIN_ARGUS/VERA/CATO.md
  expected: present in all 4
  observed: present in all 4 (save-verdict.md:76, CAPTAIN_VERA.md:311, CAPTAIN_ARGUS.md:292, CAPTAIN_CATO.md:257)
  result: pass
- probe_id: luo-4home-sha
  description: luo DC3 -- the 4 byte-aligned regions are byte-identical
  quadrant_classification: easy-easy
  command_or_method: awk-slice the BEGIN/END region in each of the 4 homes | sha256sum
  expected: all 4 sha256 identical
  observed: all 4 = 494b442ea512949906134c4318786acf7ad6f63785fef19e6afee8bc6d2842cb (matches NOMOS re-compute)
  result: pass
- probe_id: luo-no-stdout-echo
  description: luo -- zero `echo "attach_status"` to stdout (the rev1 r6 regression must NOT reappear) + LOCKED clause-d-clause-1 sentences verbatim + stderr-only breadcrumb
  quadrant_classification: easy-easy
  command_or_method: grep for echo "attach_status across 4 homes; grep LOCKED sentences; confirm WARN breadcrumb is >&2
  expected: zero stdout echo; LOCKED sentences present; breadcrumb to stderr
  observed: grep rc=1 (none found) for stdout echo; "first-class field in the seat's dispatch return", "NOT a scroll-past stderr echo", "load-bearing signal PLINY keys its retry/escalate obligation off" all present verbatim; WARN breadcrumb is `>&2`.
  result: pass
- probe_id: 3nh-covered-by-C3
  description: 3nh -- both abort sites covered by C3-checksh-abort + C3-installsh-abort above
  quadrant_classification: easy-easy
  command_or_method: see C3 probes
  expected: both sites abort rc=2 on empty + do-no-harm on non-empty
  observed: see C3 (one MINOR partial-manifest deviation at install.sh site, abort objective met)
  result: pass
- probe_id: kr7-narrow-roster
  description: kr7-narrow -- install.sh deploy-summary echo glob-derives the MAJOR roster == fresh-DEPLOY hand-enumeration (4 MAJORs)
  quadrant_classification: easy-easy
  command_or_method: read install.sh:906-917 (glob for ${SCRIPT_DIR}/MAJOR_*.md); ls substrate/MAJOR_*.md; compare to fresh-DEPLOY enum :963-1007
  expected: glob == enumeration == 4 (POLYBIUS, PLINY, CHIRON, HAMILTON)
  observed: glob yields 4 (CHIRON, HAMILTON, PLINY, POLYBIUS); fresh-DEPLOY enum lists POLYBIUS/PLINY/CHIRON/HAMILTON. Match. Prior hardcoded 2-MAJOR list (omitted CHIRON/HAMILTON) now glob-consistent.
  result: pass
- probe_id: p41.3-curl-maxtime
  description: p41.3 -- check-bw-release/check.sh curl carries --max-time 8
  quadrant_classification: easy-easy
  command_or_method: grep curl in substrate/skills/check-bw-release/check.sh
  expected: --max-time 8 on the curl
  observed: line 120 `curl -fsSL --max-time 8 https://api.github.com/...` -- previously-unbounded network call now bounded at 8s.
  result: pass
- probe_id: p41.4-doc-reword
  description: p41.4 -- MAJOR_POLYBIUS.md retired-tool cross-ref reworded; zero residual "base-team sync skill" string
  quadrant_classification: easy-easy
  command_or_method: grep "base-team sync skill" substrate/MAJOR_POLYBIUS.md
  expected: zero matches; reworded to "sync tool / check.sh"
  observed: grep rc=1 (none found); line 629 now reads "base-team sync tool / check.sh".
  result: pass
- probe_id: cg-author-gate
  description: close-gate -- full author-gate test suite
  quadrant_classification: easy-easy
  command_or_method: bash substrate/hooks/tests/run-author-gate-tests.sh
  expected: 27 passed / 0 failed (16 baseline + 6 y12 + 5 ez9; parse_allow_pairs SOURCED from _hooklib.sh, no second mirror)
  observed: 27 passed / 0 failed.
  result: pass
- probe_id: cg-gen-data
  description: close-gate -- npm run gen-data deterministic + Zod adapter stays valid
  quadrant_classification: easy-easy
  command_or_method: npm ci in app/; npm run gen-data x2; sha256 compare
  expected: no error (Zod valid), identical output across 2 runs
  observed: both runs rc=0, no Zod error (16 role files, 4 MAJOR/12 CAPTAIN); sha256 identical (9e32278e...73915) -> DETERMINISTIC. SEE methodology_concerns: the COMMITTED agents.ts is STALE (4 role bodies: POLYBIUS p41.4 reword + ARGUS/VERA/CATO luo region) -- needs ship-beat regen.
  result: pass
- probe_id: cg-vitest
  description: close-gate -- full app vitest suite
  quadrant_classification: easy-easy
  command_or_method: npm test (vitest run) in app/
  expected: all pass
  observed: 3 test files / 41 tests passed, 0 failed.
  result: pass
- probe_id: cg-stop-hook
  description: close-gate -- stop-self-check test suite
  quadrant_classification: easy-easy
  command_or_method: bash substrate/hooks/tests/run-stop-self-check-tests.sh
  expected: pass
  observed: 2 passed / 0 failed.
  result: pass
- probe_id: cg-bash-n
  description: close-gate -- bash -n syntax check on all 5 edited shell files
  quadrant_classification: easy-easy
  command_or_method: bash -n on install.sh, check-substrate-updates/check.sh, check-bw-release/check.sh, pretooluse-author-field-audit.sh, _hooklib.sh
  expected: all clean
  observed: all 5 OK (rc=0).
  result: pass
- probe_id: cg-install-donoharm
  description: close-gate -- install.sh dry-run + real deploy do-no-harm (3nh guard must not brick a healthy deploy)
  quadrant_classification: easy-easy
  command_or_method: install.sh --target project --project-dir <syn> --dry-run; then a real full deploy
  expected: dry-run rc=0; real rc=0 with manifest written
  observed: dry-run rc=0 "done (dry-run, no writes)"; real rc=0 "wrote manifest" + "done (applied)", 16 NAME_SUFFIX rows. Guard does NOT false-fire on a healthy 4-MAJOR substrate.
  result: pass

threat_coverage:
- mitigation: M1
  threat: false-attribution riding a reviewed-allow marker (author/value-mismatch/substring/newline-smuggle/embedded-field/block-form/body-placed variant slipping a real authorship violation past the gate)
  defeats_via_probe: C4-tp12
  probe_evidence: C4-tp12 executed output (above) -- real gate emits permissionDecision deny on author "Fenwick Galsworthy" despite the forged `stoa-author-gate-allow: [author: Fenwick Galsworthy]` marker; parse_allow_pairs hard-restricts allow-able fields to {publisher,vendor,source,provider} so the author field never enters the allow set.
  attack_path_exercised: drove the M1 author-allow-attempt attack path (committer forges an allow-marker naming a real authorship field) through the REAL gate end-to-end; observed (a) attack BLOCKED (deny on author Fenwick Galsworthy) AND (b) legit low-risk traffic NOT false-blocked (ctl5 publisher:CalGEM legit reviewed-allow -> ALLOW, C4-ctl5). Both halves confirmed against the real gate, not the harness mirror.

methodology_concerns:
- "[MINOR -- DEVIATION FROM DESIGN CLAIM, not a gate failure] install.sh write_substrate_manifest leaves a partial header-only manifest on disk when the empty-MAJOR guard fires. The design (rev2 sec2.2 line 571-572) and the brief C3 both assert 'no partial manifest is left as the live artifact path' / 'no partial manifest is left live'. EMPIRICALLY FALSIFIED: the brace-redirect `} > \"$manifest\"` (install.sh:565) writes the manifest header BEFORE the post-brace guard at :574 can fire, so a 379-byte header-only manifest (zero NAME_SUFFIX rows) remains at the workspace .claude/.substrate-manifest after the rc=2 abort. SEVERITY MINOR: the PRIMARY FAIL-LOUD objective (loud err() message + rc=2 + do-no-harm) is fully met; the residual partial file has ZERO data rows, so a subsequent apply.sh read produces zero substitutions (a no-op), never a wrong substitution; and a zero-MAJOR source means the substrate checkout is itself broken (install aborts, operator must re-checkout + re-run, which rewrites the manifest). The design's own reasoning is flawed here -- a post-brace assert structurally CANNOT prevent the brace-redirect from committing the partial file first; to actually satisfy the claim the writer would need to build into a temp path and mv-on-success, or assert maj_count INSIDE the brace before the redirect commits. FLAGGED for arc-owner: either fix (temp-file + mv-on-success) or amend the design claim to 'aborts loudly rc=2; a zero-data partial manifest may remain but is a harmless no-op'. Per discuss-before-fixing this is surfaced, not patched."
- "[SHIP-BEAT -- not a build defect] The committed app/src/data/generated/agents.ts is STALE vs the current substrate: a fresh `npm run gen-data` produces a 4-insertion/4-deletion diff in exactly the 4 arc-69-edited role bodies (MAJOR_POLYBIUS p41.4 'sync skill'->'sync tool' reword; CAPTAIN_ARGUS/VERA/CATO luo rc-capture region). ADA edited the substrate role files but did not regenerate agents.ts. The project CLAUDE.md says 'run npm run gen-data after substrate edits'; prior arcs carry a dedicated close-gate regen commit (e.g. 4a73edf). This is expected to be handled at the ship beat (the brief itself names 'scoped .claude/ regen + step-4 diff assert' as a ship-beat step), but the branch is CURRENTLY stale -- the ship beat MUST regen agents.ts + commit before merge or the app roster will not reflect the shipped substrate. I regenerated to detect this, then `git checkout --` restored agents.ts to preserve deliverable independence (the worktree is clean of my probe)."
- "[INFO] On this Windows/git-bash platform, Windows-python text-mode stdout translates \\n->\\r\\n, so BOTH parse_allow_pairs AND extract_author_fields emit a trailing \\r on EVERY line regardless of input line endings -- broader than the design's CRLF-checkout finding. The both-side tr -d '\\r' strip correctly reconciles this; the symmetric case cancels and the asymmetric case (content-CR vs normalized-allow) is the load-bearing one, which I reproduced as a real false-block when the extracted-side strip is removed (C2-cr-asymmetric). No action needed -- the build's both-side strip is correct and exercised on native git-bash."

falsifying_evidence_summary: ""

verification_artifacts_path: probe transcripts inline in this verdict (the C4/C2/C3 harnesses ran in throwaway mktemp dirs, torn down post-run; no persisted artifact dir). The build worktree was left CLEAN -- the one mutation (agents.ts regen for cg-gen-data detection) was reverted via `git checkout -- app/src/data/generated/agents.ts`.

attach_status: <set by save procedure>

summary: The complete 7-FIX Arc 69 substrate-hygiene build PASSES falsification. All four load-bearing DoD gates are green against the REAL artifacts (not mirrors): C4 P-ez9-live drove all 5 ez9 fixtures through the actual gate pretooluse-author-field-audit.sh end-to-end -- the two load-bearing catches both hold (tp12 M1 author-allow-attempt -> BLOCK, the hole stays closed; ctl5 legit publisher reviewed-allow -> ALLOW, no false-block) plus tp11/ctl6/tp13 -> BLOCK. C2 confirmed the both-side CR strip does real work: I reproduced the asymmetric-CR false-block when the extracted-side strip is removed, and the real gate ALLOWs ctl5 CRLF-staged on native git-bash. C3 proved BOTH 3nh abort sites fail loud (check.sh sentinel+caller-guard -> rc=2; install.sh err()->rc=2) on an empty MAJOR source via REAL execution (NOT --dry-run), with do-no-harm on non-empty sources at both sites. All 7 per-ticket repros pass (y12 true-positive now caught + year-prose control still ALLOWs; ez9 via C4; luo 4-home byte-identical sha 494b442e + zero stdout echo + LOCKED sentences verbatim; 3nh via C3; kr7-narrow glob==4-MAJOR enumeration; p41.3 curl --max-time 8; p41.4 zero residual 'base-team sync skill'). Full close-gate suite green: author-gate 27/0, gen-data deterministic (identical sha across 2 runs, Zod adapter valid), vitest 41/41, stop-hook 2/2, bash -n clean on all 5 edited shell files, install do-no-harm rc=0. Two items flagged for the ship beat / arc-owner, NEITHER falsifying a C1-C4 gate: (1) MINOR -- install.sh leaves a harmless zero-data partial manifest on the abort path, falsifying the design's secondary 'no partial manifest left live' claim though the FAIL-LOUD primary objective is fully met (surfaced, not patched, per discuss-before-fixing); (2) SHIP-BEAT -- the committed agents.ts is stale vs the 4 arc-69-edited role bodies and must be regenerated at the close-gate before merge.
