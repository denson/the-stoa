verdict: CONFORMANT
ticket: stoa--csn (Arc 69 keystone author-gate + save-verdict build)
checked_output: commits d9fd259 (Cluster A: stoa--y12 + stoa--ez9) + 443edbf (Cluster B: stoa--luo) on branch arc-69/build

PER-CLAIM:
1. Commit hygiene (BOTH) -- CONFORMANT. git show --format=fuller: both Author=denson <densonsmith2@gmail.com> (PRINCIPAL, NOT overridden); both bodies carry Co-Authored-By: CAPTAIN_ADA_the-stoa <captain-ada@the-stoa.local> (+ Claude Opus 4.8 trailer). Both reachable on arc-69/build (443edbf=HEAD, d9fd259=parent).
2. No foreign authorship field in a REAL surface -- CONFORMANT (CRITICAL). (a) Every foreign-author string in the diff lives in a .fixture-basename file under substrate/hooks/tests/fixtures/: Fenwick Galsworthy only in tp12-author-allow-attempt.md.fixture, tp8-license-c-form.fixture, tp9-license-copyright-symbol.fixture; CalGEM (publisher value) only in ctl5/ctl6/tp11/tp13 fixtures. (b) classify_author_file (_hooklib.sh:96) returns rc=1 for .fixture basenames: not in the cfg case-list, basename does not start LICENSE, path is not *.md (the .md.fixture ones end .fixture not .md) nor *.claude-plugin/* -> falls to return 1 (skipped). (c) No real author-encoding file (role-file/LICENSE/manifest/module) in the diff gained a foreign author/owner/creator/etc. field. The principal control Copyright (c) 2026 Denson Smith appears only in ctl4 fixture.
3. C1 parse_allow_pairs home -- CONFORMANT. Single definition home: substrate/hooks/_hooklib.sh:286 (grep -rn ^parse_allow_pairs() = 1 hit, _hooklib only). Gate pretooluse-author-field-audit.sh CALLS it (L168) + carries only the minimal is_pair_allowed skip-glue (gate L83); is_pair_allowed count in _hooklib = 0. Harness sources _hooklib (no 2nd parser mirror).
4. ez9 boundary -- CONFORMANT. _hooklib.sh:290 ALLOW_FIELDS = {publisher, vendor, source, provider} exactly; authorship fields {author,authors,owner,creator,created_by,maintainer,maintainers,by,copyright,holder} hard-dropped at L305 (field not in ALLOW_FIELDS -> continue, before entering allow set; closes M1). is_pair_allowed uses [ "$lf" = "$af" ] && [ "$lv" = "$aval" ] = EXACT field+value equality (not substring). C2 tr -d CR on BOTH sides: allow side _hooklib.sh:289 text.replace(CR); extracted side gate L174/175; is_pair_allowed also CR-strips lf/lv.
5. DC1 no-regression (RE-RUN) -- CONFORMANT. I ran bash substrate/hooks/tests/run-author-gate-tests.sh: observed 27 passed / 0 failed (16 baseline GREEN + 11 new). Matches ADA claim.
6. DC3 4-home byte-identity (RE-COMPUTE) -- CONFORMANT. sha256 of the SAVE-VERDICT-BYTE-ALIGNED-REGION in save-verdict.md, CAPTAIN_VERA.md, CAPTAIN_ARGUS.md, CAPTAIN_CATO.md = 494b442ea512949906134c4318786acf7ad6f63785fef19e6afee8bc6d2842cb (all 4 identical; matches ADA claim). LOCKED clause-(d) clause-1 sentence present VERBATIM (diff shows prior sentence preserved char-for-char + ONE additive trailing sentence). echo "attach_status: ..." to stdout in region: grep -c = 0 in all 4 homes (only a comment mention + a stderr >&2 breadcrumb; r6 anti-pattern avoided).
7. Scope containment -- CONFORMANT. The 2 commits touch ONLY: _hooklib.sh, pretooluse-author-field-audit.sh, run-author-gate-tests.sh, 11 fixtures, + the 4 save-verdict homes. git show --name-only grep for .claude/ = NONE; for install.sh/check.sh/kr7/p41 = NONE.
8. bw -- CONFORMANT. Both commit messages REF (not Close) stoa--y12 / stoa--ez9 / stoa--luo. bw show: all 3 tickets still OPEN (circle marker, status open).

OBSERVATION (NOT a divergence against the audited commits): the worktree has UNCOMMITTED modifications to substrate/install.sh + substrate/skills/check-substrate-updates/check.sh and untracked agents/ artifacts. These are NOT part of d9fd259 or 443edbf (name-only confirms). Flagged for PLINY as out-of-band worktree state, outside this audit scope.

ground_truth_consulted: git show --format=fuller d9fd259 443edbf; git show --stat/--name-only both; git merge-base --is-ancestor (both on arc-69/build); _hooklib.sh:96 classify_author_file, :286 parse_allow_pairs, :290 ALLOW_FIELDS, :305 hard-drop, :289 CR-strip; pretooluse-author-field-audit.sh:83 is_pair_allowed, :168 call, :174-175 CR-strip; bash run-author-gate-tests.sh (27/0); sha256sum of byte-aligned region x4; grep -c echo attach_status x4; bw show stoa--y12/ez9/luo (all open).

summary: Audited the Arc 69 keystone build (author-gate y12+ez9 at d9fd259, save-verdict luo rc-capture + 4-home re-align at 443edbf) against repo + bw ground truth across all 8 brief claims. ALL CONFORMANT. The three highest-value catch surfaces are clean: (1) no foreign-author field in any REAL author-encoding surface -- every Fenwick Galsworthy / CalGEM string lives in a .fixture-basename test file that classify_author_file skips (rc=1); (2) no DC1 regression -- re-ran the suite, 27 passed / 0 failed (16 baseline green); (3) no 4-home hash mismatch -- all four byte-aligned regions sha256 to 494b442e... identically. Commit hygiene preserves PRINCIPAL Author + ADA seat-trailer; ALLOW_FIELDS boundary holds (authorship fields hard-rejected, M1 closed) with exact-pair match + both-side CR-strip; scope is contained to author-gate + save-verdict surfaces with no .claude/ copies; commits REF (not Close) the 3 still-open tickets. One out-of-scope observation: uncommitted install.sh/check.sh edits + untracked agents/ artifacts in the worktree, not part of either audited commit -- surfaced for PLINY, not a divergence.
gap_or_blocker: none