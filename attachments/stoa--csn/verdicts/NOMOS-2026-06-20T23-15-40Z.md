verdict: CONFORMANT
ticket: stoa--csn
checked_output: CAPTAIN_VERA falsification verdict VERA-20260620T230419Z.md (overall PASS, HEAD 34db584) for Arc 69 substrate-hygiene 7-FIX build on arc-69/build. NOMOS cross-checked VERA factual/ground-truth claims against repo + bw, focus on the TWO ship-beat FLAGS VERA raised (partial-manifest + stale agents.ts) -- confirming each is REAL and accurately characterized (neither over- nor under-stated).

divergences: []   # none -- every checked claim CONFORMANT; both flags accurately characterized

flag_audit:
- flag: 1 (partial-manifest on empty-MAJOR abort)
  nomos_verdict: CONFORMANT (REAL + accurately characterized -- neither over- nor under-stated)
  evidence_structural: install.sh:565 brace-group redirect (close-brace > manifest) commits the header to the live manifest path BEFORE the post-brace guard at :574 ([ maj_count -ge 1 ] || err) can fire. The design comment at :571-572 (assert AFTER the brace group so the partial manifest is not left as the live artifact path) is structurally impossible as written -- a post-brace assert cannot un-write a redirect that already committed.
  evidence_empirical: extracted err()+write_substrate_manifest harness, DRY_RUN=0, empty SCRIPT_DIR MAJOR glob, REAL exec -> err() fired FATAL zero MAJOR_*.md globbed + rc=2 (FAIL-LOUD confirmed); AND a partial header-only manifest WAS left on disk (header line only, ZERO NAME_SUFFIX data rows, grep -c NAME_SUFFIX = 0). My harness header 22 bytes vs VERA real-gate 379 bytes -- structural finding identical: header committed, zero data rows.
  subclaims: (a) brace-redirect writes before guard CONFIRMED; (b) abort still FAIL-LOUD (err()+rc=2) CONFIRMED; (c) zero data rows -> zero substitutions in a subsequent apply.sh read, never a WRONG substitution CONFIRMED. The design rev2 sec2.2 sentence is exactly the one VERA falsified. VERA MINOR severity correct.
- flag: 2 (stale committed agents.ts)
  nomos_verdict: CONFORMANT (REAL)
  evidence: agents.ts is git-TRACKED (git ls-files confirms app/src/data/generated/agents.ts). At dispatch the tree showed it UNMODIFIED (VERA restore held). npm run gen-data from app/ produced exactly 4 insertions / 4 deletions (numstat: 4  4  app/src/data/generated/agents.ts), matching VERA claim verbatim; the diff lands in the role-body strings (POLYBIUS body carrying the p41.4 reword surfaced). I then git checkout -- agents.ts and re-confirmed the tree clean -- NOMOS left no stray regen either. CONFIRMED stale; a regen WOULD change it; VERA restored it correctly.

ground_truth_consulted:
- bw stoa--csn (charter + full comment trail + VERA verdict comment); bw stoa--y12/ez9/luo/3nh/kr7/p41.3/p41.4 (all open glyph -- VERA closed none)
- git rev-parse HEAD = 34db58457e02581976d4c5ff4d922656ce5cd051 (unchanged); log -4 = 4 build commits e7d309a/d9fd259/443edbf/34db584 all Author=denson (PRINCIPAL not overridden); log 34db584..HEAD empty (VERA committed nothing); git status --short = only untracked agents/ artifacts, zero modified tracked files
- npm run gen-data regen (4/4 numstat) then git checkout -- restore; install.sh write_substrate_manifest L540-576 + REAL-exec extracted harness on empty MAJOR glob
- luo 4-home byte-aligned region sha256 (save-verdict.md + CAPTAIN_VERA/ARGUS/CATO.md, BEGIN/END slice) all 4 = 494b442ea512949906134c4318786acf7ad6f63785fef19e6afee8bc6d2842cb
- ez9 fixtures tp11/ctl5/tp12/ctl6/tp13 all on disk under substrate/hooks/tests/fixtures; VERA-20260620T230419Z.md on disk (19751 bytes) at agents/verdicts/stoa--csn/; attach_status OK per VERA mechanized attach_rc=0

per_claim:
- claim_1_FLAG1_partial_manifest: CONFORMANT -- REAL + accurately characterized (header committed at L565 before guard at L574; rc=2 FAIL-LOUD intact; zero data rows -> no-op never wrong substitution; design rev2 sec2.2 sentence is the one VERA falsified)
- claim_2_FLAG2_stale_agents_ts: CONFORMANT -- REAL (tracked; regen = 4/4 diff in the 4 arc-69 role bodies; VERA restored, tree clean of stray regen)
- claim_3_worktree_clean: CONFORMANT -- git status --short shows only untracked agents/ artifacts; zero modified tracked files (no probe debris)
- claim_4a_luo_4home_sha: CONFORMANT -- all 4 == 494b442e... (matches VERA cite)
- claim_4b_ez9_fixtures: CONFORMANT -- all 5 (tp11/ctl5/tp12/ctl6/tp13) on disk
- claim_4c_verdict_attach: CONFORMANT -- verdict on disk (19751 bytes) at canonical path; attach_status OK per VERA mechanized rc-capture (bw CLI exposes no attachment-list section this version; on-disk artifact + attach_rc=0 are the durable ground truth)
- claim_5_commit_integrity: CONFORMANT -- HEAD 34db584 unchanged; no commit after it; 4 build commits Author=denson (PRINCIPAL); VERA committed nothing; no foreign author-like field
- claim_6_tickets_open: CONFORMANT -- all 7 FIX tickets (y12/ez9/luo/3nh/kr7/p41.3/p41.4) carry the open glyph; VERA closed nothing

ground_truth_summary: VERA overall PASS is sound against repo+bw ground truth, and BOTH ship-beat flags are accurately characterized -- neither over- nor under-stated. FLAG 1 (partial-manifest) is REAL: structurally confirmed (L565 brace-redirect commits the header before the L574 guard, making the design rev2 sec2.2 no-partial-manifest claim structurally impossible) AND empirically reproduced via a REAL-exec extracted harness (rc=2 FAIL-LOUD fires AND a header-only zero-data-row manifest remains). VERA characterization that the abort is still FAIL-LOUD and the residual is a harmless no-op (zero data rows -> zero substitutions downstream, never a WRONG one) is accurate; the falsified sentence is correctly identified. FLAG 2 (stale agents.ts) is REAL: the tracked file regenerates to a 4-insertion/4-deletion diff in exactly the four arc-69-edited role bodies, and VERA correctly restored it (worktree clean, no stray regen left by VERA or NOMOS). All headline PASS spot-checks reproduce (luo 4-home sha 494b442e identical across all 4 homes; 5 ez9 fixtures on disk; verdict attached/on-disk). Commit integrity holds (HEAD 34db584 unchanged, all-denson author, VERA committed nothing); all 7 FIX tickets remain OPEN. No divergence found.

gap_or_blocker: none
