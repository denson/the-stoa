status: completed
ticket: stoa--csn
verdict: revise
diff_reviewed: 7b271df..34db584 (4 commits: e7d309a kr7/p41.3/p41.4 | d9fd259 y12+ez9 | 443edbf luo | 34db584 3nh) on arc-69/build
design_artifact_compared_against: agents/design/stoa--csn/design-rev2.md (+ rev1 for unchanged items)

OVERALL: PASS-WITH-FINDINGS, routed verdict=revise for ONE must-fix-this-arc finding (c1, the FLAG-1 false-comment-in-canon, already concurred fix-now by POLYBIUS). The 7-FIX build is craft-sound, in-scope, source-only, authorship-clean, and the ez9 security keystone is airtight under independent adversarial probing. No BLOCKER defects. c1 is the only item that must change before ship; everything else is MINOR/follow-up or ship-beat.

concerns:
- id: c1
  category: hygiene
  description: install.sh:571-572 code comment is STRUCTURALLY FALSE as written -- it claims the post-brace assert prevents a partial manifest being left live, but the brace-redirect closes-to "$manifest" at L565 commits the header-only manifest to disk BEFORE the L574 guard fires. A false comment shipping into substrate canon is the defect (independent of the harmless zero-data residual).
  evidence: substrate/install.sh:565 (brace-group redirect to "$manifest") vs :571-572 (comment) vs :574 (guard); empirically reproduced -- zero-MAJOR abort leaves a 54-byte header-only manifest on disk with rc=2. Root: the false claim ORIGINATED in design-rev2 L226-232 + sec2.2; ADA faithfully transcribed it.
  severity: blocking
  quadrant_classification: easy-easy
  suggested_direction: make the L571-572 claim TRUE -- recommend option (a) PRE-REDIRECT count-check (POLYBIUS lean): glob-count the MAJOR set into maj_count BEFORE the brace-group redirect opens, err if zero, THEN open the redirect, so the manifest path is never truncated/written on the abort path (~4 lines). temp-file+mv-on-success (option b) also satisfies but is heavier. Whichever ADA/DAEDALUS pick: VERA C3-installsh-abort must add the NEW assertion -- zero partial artifact on disk after abort -- plus rc=2 + do-no-harm on non-empty. Do NOT write the fix (CATO has no Write/Edit).
- id: c2
  category: craft
  description: y12 cpat trailing-period OVER-CAPTURE -- the name-class includes "." so "Copyright (c) 2026 Denson Smith." captures the value "Denson Smith." WITH the sentence-final period. is_principal lowercases+trims whitespace but does NOT strip trailing punctuation, so the PRINCIPAL period-form copyright line would FALSE-BLOCK against the accepted token "denson smith".
  evidence: substrate/hooks/_hooklib.sh:197 (name-class with literal period) + pretooluse-author-field-audit.sh:102 (is_principal trims whitespace only); probed: extract_author_fields cfg on "Copyright (c) 2026 Denson Smith." emits "copyright<TAB>Denson Smith." (period retained).
  severity: minor
  quadrant_classification: easy-easy
  suggested_direction: self-inflicted + fail-toward-enforcement (over-blocks, never under-blocks an impostor) + non-standard MIT form (canonical MIT has no trailing period), so NOT blocking. If folded into the c1 fix pass: either strip a trailing "." from the cpat name capture, or strip trailing punctuation in is_principal before compare. The "." in the class is genuinely needed for middle initials ("Denson J. Smith") -- do not just remove it. Flag-only; owner/ADA decide whether to fold.
- id: c3
  category: coverage
  description: y12 cpat multi-author UNDER-MATCH -- "Copyright (c) 2026 X and Y" extracts NEITHER name (the lowercase " and " breaks the name-run-to-EOL anchor). A two-author copyright line with " and " silently passes the gate.
  evidence: substrate/hooks/_hooklib.sh:197 (name run anchored to EOL); probed: extract_author_fields cfg on "Copyright (c) 2026 Fenwick Galsworthy and Jane Doe" emits NOTHING.
  severity: minor
  quadrant_classification: easy-easy
  suggested_direction: this is the SAME EOL-anchor discriminator that (correctly) keeps year-prose out (ctl-y12-year-prose-in-cfg ALLOWs by exactly this mechanism), so it is an ACCEPTED design tradeoff, not a regression -- pre-y12 the gate missed ALL copyright forms. Follow-up only: if multi-author LICENSE coverage is wanted later, a separate ticket. Not this arc.
- id: c4
  category: consistency
  description: is_pair_allowed is MIRRORED (byte-copied) into the test harness (run-author-gate-tests.sh) from the gate. If the gate is_pair_allowed drifts, the harness mirror silently diverges. (parse_allow_pairs itself is correctly SOURCED, not mirrored -- only this trivial exact-pair compare is duplicated.)
  evidence: substrate/hooks/pretooluse-author-field-audit.sh:83-97 vs substrate/hooks/tests/run-author-gate-tests.sh (is_pair_allowed mirror block).
  severity: minor
  quadrant_classification: easy-easy
  suggested_direction: VERA P-ez9-live real-gate probe is the NAMED backstop for this glue (design sec1.5), and the mirrored fn is a 13-line literal copy of a trivial compare, so the divergence risk is bounded + caught. ACCEPTABLE as-is. Optional future tightening: relocate is_pair_allowed to _hooklib.sh so the harness sources BOTH halves (same r3 rationale that moved parse_allow_pairs to the lib-home). Follow-up, not this arc.

follow_ups:
- c3 (multi-author copyright coverage) -- separate ticket IF wanted; the EOL-anchor tradeoff is correct as shipped.
- c4 (is_pair_allowed lib-home relocation) -- optional consistency tightening; VERA live-probe backstop makes the current mirror safe.
- c2 (period over-capture) -- fold into c1 fix pass OR file follow-up; owner/ADA call.
- DESIGN CORRECTION (reference-material, non-blocking): design-rev2 L226-232 pseudocode + sec2.2 carry the same structurally-false "no partial manifest left" claim that seeded c1; ADA deviations_from_design should record that the design reasoning was flawed at this site. Design is reference material -- corrected on next revise, does not block ship.
- DESIGN STALE-PROSE (non-blocking): design-rev2 sec1.2 still says parse_allow_pairs lives in the GATE script "NOT _hooklib.sh", but the as-built (reviewed C1 decision) correctly placed it in _hooklib.sh so the harness SOURCES it. The as-built is STRICTLY BETTER (no second mirror of the security-critical parser); the design prose is stale, not the build.

verifier_coverage_assessment: VERA coverage is STRONG and its PASS is WARRANTED. C4 drove all 5 ez9 fixtures through the REAL gate (not the harness mirror); the M1 attack path (C4-tp12) exercised BOTH halves -- attack BLOCKED (forged author-marker -> deny) AND legit traffic NOT false-blocked (C4-ctl5 publisher reviewed-allow -> ALLOW). My independent section-6.1 item-11 cross-check confirms the threat_coverage line cites defeats_via_probe C4-tp12, which IS in probes_executed with non-empty probe_evidence + attack_path_exercised -- the binding holds. C3 ran REAL exec (not --dry-run) at both 3nh sites and VERA CAUGHT the partial-manifest deviation itself rather than papering over it (the meta-verifier-of-VERA win: the verifier surfaced the very gap a cold-read confirms). C2 reproduced the asymmetric-CR false-block to prove the CR-strip is load-bearing (not dead code). Full close-gate ran (author-gate 27/0, gen-data deterministic + Zod-valid, vitest 41/41, stop-hook 2/2, bash -n x5, install do-no-harm) -- satisfies full-suite+bespoke discipline. COVERAGE GAP a cold-read caught that VERA did NOT probe (coverage_concern per section-6.4, MINOR): VERA y12 probe set did not exercise the principal-name punctuation boundary (c2 trailing-period over-capture -> false-block of the PRINCIPAL own period-form copyright) nor the multi-author under-match (c3). Neither falsifies a DoD gate; both are fail-toward-enforcement; VERA y12 coverage should have driven at least the principal-period case. Not blocking VERA PASS; recorded so the next y12 touch closes it.

attach_status: <set by save procedure>

summary: A clean, well-bounded 7-FIX substrate-hygiene build. The ez9 security keystone -- the load-bearing surface -- is AIRTIGHT under independent adversarial probing: parse_allow_pairs hard-whitelists {publisher,vendor,source,provider} so a forged [author: X] / [owner:/copyright:/holder: X] marker yields ZERO allow-pairs (M1 closed); is_pair_allowed is exact-match-only (substring/prefix/suffix smuggle all rejected); the allow-marker line itself is not mis-parsed as an author field (the -gate-allow suffix breaks the separator anchor in both modes); the harness SOURCES the security-critical parser (only the trivial exact-pair compare is mirrored, with VERA real-gate probe as the named backstop); 27/27 green. y12 cpat is sound in its main path (x27 apostrophe correct + file-consistent; both prose controls ALLOW) with two MINOR fail-toward-enforcement edge cases (c2 period over-capture, c3 multi-author under-match). luo is byte-identical across all 4 homes (sha 494b442e) with the LOCKED dispatch-return contract un-reframed. 3nh FAIL-LOUD primary objective is met at both sites; the check.sh sentinel+caller-guard is the correct pattern for the process-sub site, and the CAPTAIN/template/skill globs being unguarded is a DOCUMENTED DC2 scope call (zero-CAPTAIN is a legitimate --no-captains state), not a gap. Scope is source-only with each commit mapping 1:1 to a named ticket -- no drive-by changes; authorship audit clean (fixtures are labeled FICTIONAL TEST INPUT; design author = Denson Smith). The ONE must-fix-this-arc item is c1: the install.sh:571-572 comment is structurally FALSE (the brace-redirect commits the partial manifest before the post-brace guard can fire), a false comment shipping into canon -- POLYBIUS has already concurred fix-now; recommend the pre-redirect count-check. Routed verdict=revise on c1 alone.
