status: completed
ticket: stoa--csn
verdict: pass
design_artifact_verified_against: agents/verdicts/stoa--csn/CATO-20260620T232459Z.md (the c1/c2 fix spec)
build_verified: arc-69/build HEAD=79b87ef (targeted re-probe of the consolidated CATO review-fix)

probes_executed:
- probe_id: c1
  description: install.sh write_substrate_manifest leaves NO partial manifest on a zero-MAJOR abort (the NEW assertion the original C3 probe lacked) + rc=2 FAIL-LOUD + do-no-harm full write.
  quadrant_classification: easy-easy
  command_or_method: Extracted err() (install.sh:274-277) + write_substrate_manifest() (install.sh:498-592) VERBATIM from the fix commit into an isolated harness; drove a REAL (DRY_RUN=0) execution against throwaway temp SCRIPT_DIRs. (a) empty MAJOR glob -> abort path; (b) full glob (4 MAJOR_*.md) -> do-no-harm; (c) adversarial: stale pre-existing manifest + empty glob.
  expected: (1) abort with err() rc=2; (2) NO manifest file on disk at target after abort; (3) non-empty glob writes FULL manifest (all 4 MAJOR mappings) rc=0.
  observed: |
    ABORT (empty_src): stderr "install.sh: error: write_substrate_manifest: zero MAJOR_*.md globbed ... refusing to write a manifest missing every MAJOR mapping." ; ABORT_EXIT_CODE=2 ; on-disk: dest_abort/.substrate-manifest ABSENT (ls shows empty dir). => (1) PASS rc=2, (2) PASS zero partial artifact.
    DO-NO-HARM (full_src, 4 MAJOR): rc=0 ; manifest WRITTEN (581 bytes) with exactly 4 {{NAME_SUFFIX}} MAJOR rows (.claude/MAJOR_{NOMOS,PLINY,POLYBIUS,TIRO}.md). => (3) PASS.
    ADVERSARIAL (stale manifest pre-exists + empty glob): pre-abort sha=bce048de... size=42 ; post-abort sha=bce048de... size=42 IDENTICAL ; rc=2. The pre-redirect check fires BEFORE the brace-group redirect opens, so the stale file is not even truncated -- proving the mechanism is genuinely pre-redirect (not delete-after).
  result: pass
- probe_id: c2
  description: y12 cpat trailing-terminal-punctuation strip -- both directions + internal-dot preservation + no new under-match, through the REAL extract_author_fields/gate path.
  quadrant_classification: easy-easy
  command_or_method: Drove the new c2 fixtures through the REAL gate via run-author-gate-tests.sh (ctl7 ALLOW, tp14 BLOCK); plus isolated extract_author_fields cfg-mode probes (real _hooklib.sh) for the sub-assertions; plus a pre/post-fix (34db584 vs 79b87ef) capture diff to confirm the fix actually changes behavior.
  expected: principal period-form -> "Denson Smith" ALLOW; foreign period-form -> "Fenwick Galsworthy" BLOCK; "J. R. R. Tolkien." -> internal dots kept, only terminal period stripped; no-period cases unchanged.
  observed: |
    PRINCIPAL period (ctl7 / "Copyright (c) 2026 Denson Smith."): extract -> [copyright<TAB>Denson Smith] (terminal . stripped) -> is_principal match -> gate ALLOW. Suite row PASS.
    FOREIGN period (tp14 / "Copyright (c) 2026 Fenwick Galsworthy."): extract -> [copyright<TAB>Fenwick Galsworthy] (strip did NOT turn the catch into a miss) -> is_principal mismatch -> gate BLOCK. Suite row PASS.
    INTERNAL-DOT: "J. R. R. Tolkien." -> [J. R. R. Tolkien] (internal dots preserved, only terminal . stripped). "Denson J. Smith." -> [Denson J. Smith] (middle initial intact).
    REGRESSION (no-period): "Denson Smith" -> [Denson Smith]; "Fenwick Galsworthy" -> [Fenwick Galsworthy] BLOCK -- both unchanged.
    FIX-EFFECT pre/post: PRE-FIX 34db584 captured "Denson Smith." WITH period (would false-BLOCK the PRINCIPAL); POST-FIX 79b87ef captures "Denson Smith" -> ALLOW. Foreign: PRE "Fenwick Galsworthy." / POST "Fenwick Galsworthy" -> still BLOCK. Fix demonstrably load-bearing and asymmetry-safe.
  result: pass
- probe_id: nr1-author-gate-suite
  description: No-regression -- full author-gate close-gate suite.
  quadrant_classification: easy-easy
  command_or_method: bash substrate/hooks/tests/run-author-gate-tests.sh
  expected: 29 passed / 0 failed (27 prior + 2 new c2).
  observed: "29 passed / 0 failed" (verbatim tail). All prior 27 rows green + ctl7 ALLOW + tp14 BLOCK.
  result: pass
- probe_id: nr2-keystone-untouched
  description: No-regression -- ez9 keystone gate + the 4 luo save-verdict homes UNCHANGED vs HEAD~1 (34db584); the ez9 keystone PASS must not be reopened.
  quadrant_classification: easy-easy
  command_or_method: git diff 34db584 79b87ef -- substrate/hooks/pretooluse-author-field-audit.sh (and the MAJOR_PLINY/CAPTAIN_ARGUS/CAPTAIN_VERA/CAPTAIN_CATO + save-verdict homes); git diff 34db584 79b87ef --name-only.
  expected: gate diff EMPTY; save-verdict-home diffs EMPTY; only 5 files changed (install.sh, _hooklib.sh, runner, 2 fixtures).
  observed: gate diff = 0 lines; save-verdict-home diff = 0 lines. --name-only = exactly substrate/install.sh, substrate/hooks/_hooklib.sh, substrate/hooks/tests/run-author-gate-tests.sh, ctl7 fixture, tp14 fixture. NOT the gate, NOT the save-verdict homes.
  result: pass
- probe_id: nr3-shell-syntax-and-gendata
  description: No-regression -- bash -n on edited shell files; gen-data + vitest unaffected.
  quadrant_classification: easy-easy
  command_or_method: bash -n on install.sh + _hooklib.sh + run-author-gate-tests.sh; inspected app/scripts/gen-data.ts input surface.
  expected: bash -n clean x3; gen-data inputs exclude the edited files.
  observed: bash -n CLEAN on all 3 edited shell files. gen-data.ts reads ONLY canonical role files (MAJOR_*.md / CAPTAIN_*.md from substrate/) -- it does NOT read install.sh, hooks/_hooklib.sh, or test fixtures, so the c1+c2 edits cannot perturb the roster derivation or the vitest suite that tests it. gen-data/vitest UNAFFECTED by reasoning (not run; no regen to restore).
  result: pass

threat_coverage: []
# No threat-ratified mitigation in scope for THIS targeted re-probe. The ez9 keystone (the M1-bearing surface) is byte-UNCHANGED by this fix (nr2 above); its threat-coverage was discharged in the prior VERA/CATO gauntlet pass on 34db584 and is not reopened. This re-probe verifies c1 (install hygiene, non-security) + c2 (a DC1 do-no-harm false-block correction in the author-gate). c2 fail-toward-enforcement: the strip only ever REMOVES a false-block (over-block -> correct), never weakens an impostor catch (tp14 confirms foreign still BLOCKs). Empty list is the correct state per the op-disc 35.5 not-threat-ratified carve-out for this dispatch.

methodology_concerns: |
  (MINOR, benign -- no action required) The c2 strip regex is r"[.,]\s*$" (strips a trailing terminal period OR comma). The terminal-PERIOD branch is load-bearing and verified (the "." is in the cpat name-class so it is captured then stripped). The terminal-COMMA branch is effectively dead at the cpat capture site: cpat anchors the name run to EOL via \s*$ over class [A-Za-z.\x27-], which EXCLUDES comma -- so "Copyright (c) 2026 Fenwick Galsworthy," fails to match cpat entirely (emits NOTHING) and never reaches the strip. This is the SAME accepted EOL-anchor behavior as the c3 multi-author under-match (a known/accepted coverage limit, not a regression -- the comma-form was never captured pre-fix either). The "," in the strip class is harmless defensive breadth, not a defect; flagged only for the record so a future y12 touch knows the comma branch is currently unreachable via cpat.

falsifying_evidence_summary: ""

verification_artifacts_path: probes executed in throwaway /tmp dirs (cleaned up post-run); harness extracted err()+write_substrate_manifest() verbatim from 79b87ef install.sh; all recorded outputs are inline in probes_executed above. No persistent artifact files (the worktree was left clean per dispatch).

attach_status: OK

summary: |
  TARGETED RE-PROBE PASS on fix commit 79b87ef. Both CATO fixes falsified-against and hold; no regression to the already-PASS'd build. c1 (install.sh no-partial-manifest): the pre-redirect maj_precount glob-count check err()s rc=2 BEFORE the brace-group redirect opens -- a REAL (non-dry-run) zero-MAJOR abort leaves ZERO manifest on disk (the new assertion the prior probe lacked; the prior build left a 54-byte header-only file), and an adversarial stale-manifest test proves the redirect never even opens (stale file sha unchanged). Do-no-harm: a non-empty 4-MAJOR glob writes the FULL manifest (4 mappings) rc=0. c2 (y12 cpat trailing-punctuation): the strip r"[.,]\s*$" turns "Denson Smith." -> "Denson Smith" (PRINCIPAL period-form now ALLOWs -- pre-fix it false-BLOCKED, verified by pre/post capture diff) while "Fenwick Galsworthy." -> "Fenwick Galsworthy" still BLOCKs (the strip does not turn a foreign catch into a miss), internal dots preserved (J. R. R. Tolkien. / Denson J. Smith. intact). Author-gate suite 29 passed / 0 failed (27 prior + 2 new). KEYSTONE UNTOUCHED: ez9 gate diff = 0 lines, 4 luo save-verdict homes diff = 0 lines, full diff is exactly the 5 expected files (install.sh, _hooklib.sh, runner, 2 fixtures) -- the ez9 PASS is not reopened. bash -n clean x3; gen-data/vitest unaffected (gen-data reads only role-file frontmatter, not the edited install/hooks/fixtures). Tree left CLEAN (probes ran in throwaway /tmp; only pre-existing untracked agents/ gauntlet artifacts remain; HEAD still 79b87ef). One MINOR benign methodology note: the comma branch of the c2 strip regex is unreachable via cpat's EOL anchor (same accepted behavior as c3) -- harmless, recorded for the record.
