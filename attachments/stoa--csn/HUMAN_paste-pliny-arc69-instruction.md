Read `.claude/MAJOR_PLINY.md` and assume the orchestrator role for the-stoa. You are **PLINY_the-stoa** for Arc 69.

Open in `C:\Users\denso\claude_projects\the-stoa\`. Run `bw prime` at activation.

**DOGFOOD turn one:** discover your sid via the `whoami` skill (`$CLAUDE_CODE_SESSION_ID`); sign every bw comment `[from: PLINY_the-stoa | sid <your-sid>]`.

**The three-tier chain (your position):** PRINCIPAL → user-tier Polybius_the_Stoa (owns arc; close-gate + merge) → **POLYBIUS_the-stoa (floor-manager)** → **you, PLINY_the-stoa** (run the gauntlet). You take direction from + surface to **POLYBIUS_the-stoa (the floor-manager) — NOT user-tier directly, and NOT the PRINCIPAL.**

**The spec (read it FULLY):** the directive at `substrate/arcs/arc-69-build-directive.md` (committed `7b271df`). Charter: **`stoa--csn`**. This is a **substrate-hygiene BUG-SWEEP** — ~12 small, independent substrate-tool/canon bugs in 4 clusters (A author-gate / B save-verdict / C install.sh-check / D tooling-doc). **Standard team — no CHIRON/HAMILTON.**

**DC0 is the gate on the whole sweep:** DAEDALUS GROUNDS each ticket FIRST — `bw show` it, confirm it is still LIVE (not already fixed by Arcs 66–68) and a SMALL independent fix → verdict FIX / ALREADY-FIXED (close with ref) / RE-SCOPE (ticket-and-skip, do not force). The sweep ships ONLY the FIX set. (`stoa--qsf` is already dispositioned: closed-superseded, NOT a target.) **DC1: Cluster A (author-gate) is LOAD-BEARING** — fixes must ADD the missed/allow case WITHOUT regressing the 16/16 author-gate tests; every Cluster-A change carries a new both-directions test fixture; `ez9`'s reviewed-allow path is the one genuine design question (no blanket bypass). **DC2: one ticket = one minimal change. DC3: save-verdict edits re-align all four byte-aligned homes.**

**Run the full gauntlet** (DAEDALUS → ARGUS → ADA → VERA → CATO → NOMOS). Phase A (DAEDALUS): the per-ticket DC0 verdict + the FIX-set edit plan per cluster; surface to the floor-manager for a go/no-go before build.

**Polling disciplines (run all three):**
- **D-A (bw-copy-all-output):** every CAPTAIN echoes significant outputs to bw on `stoa--csn`.
- **D-B (polling-at-breakpoints):** read bw between every CAPTAIN dispatch.
- **D-C (polling-during-surface-and-wait):** Monitor during surface-and-wait at ~2-3 min cadence.

**Hand-back protocol:** at CATO PASS, post on `stoa--csn` addressed to **POLYBIUS_the-stoa (floor-manager)** — NOT direct to user-tier. The floor-manager runs final verification + relays up.

**What you do NOT do:** merge; push; relay direct to user-tier (except a genuine scope dispute); surface to the PRINCIPAL except emergencies. You dispatch the gauntlet CAPTAINs — that IS your seat — but you do NOT run solo with one self-chosen checker.

**Close-signal at arc end:** `CLOSE ME — arc 69 gauntlet complete; awaiting user-tier Polybius_the_Stoa close-gate + merge`.

**Compaction recovery:** re-orient from this brief — `git show beadwork:attachments/stoa--csn/HUMAN_paste-pliny-arc69-instruction.md` — plus `bw show stoa--csn` and the directive.
