status: completed
ticket: stoa--x5t
verdict: pass
design_artifact_verified_against: agents/design/stoa--x5t/design-rev2.md
build_verified: 1c6655658ebe47fe61228ffe575c87cec2c16ddd on arc-74/build
probes_executed:
- probe_id: p1
  description: DC4 demonstration -- a verdict authored under the NEW format (attested body excludes attach_status) has committed-content-sha == attested/cited-sha; the sos--yn2 divergence cannot recur.
  command_or_method: printf %s BODY > sample.md; ATTESTED=sha256(BODY); commit in-tree; COMMITTED=sha256(git cat-file -p HEAD:sample.md); assert ATTESTED==COMMITTED; then negative control append attach_status:OK and assert sha changes.
  expected: committed-content == attested under new format; OLD-bug append diverges the sha.
  observed: ATTESTED==ON-DISK==COMMITTED-CONTENT == b46f950c2bdad6708bc771607e379c782c825ba3ad5355d862e22f7a62ed4d83. Negative control append attach_status:OK -> 5933482906ceab25166fab6461d0e6128583bdf3bfc57d07b59eddcbfc79dc60 (diverges, confirming the field WAS the sos--yn2 delta). Throwaway commit reverted (reset --hard HEAD~1), probe dir removed, worktree clean, HEAD back at 1c66556.
  quadrant_classification: easy-easy
  result: pass
- probe_id: p2
  description: DC1 field-removal -- the NEGATIVE assertion (zero attach_status/attach_failure INSIDE the verdict-format fenced block) for all 3 reviewers; the three legitimate post-fix survivor sites are EXPECTED, not defects.
  command_or_method: awk-extract the first fenced block after the verdict-format heading in each of VERA/ARGUS/CATO; grep attach_status|attach_failure inside it; full-file grep -n to confirm survivor sites.
  expected: zero attach_status/attach_failure inside the verdict-format fence in all three; survivors only at the allowed sites.
  observed: PASS x3 -- zero inside the fence. Survivors per file at the addendum (VERA L271-272), region attach_rc comment (L313+320, inside the untouched 494b442e region), post-region posture prose (L325), and the new frozen-body sentence (L266, new DC1c machinery, prose). All expected; no false-route-back.
  quadrant_classification: easy-easy
  result: pass
- probe_id: p3
  description: DC1 parallelism -- the new/replaced machinery (frozen-body sentence + addendum label + two relocated field lines + the full bw-comment replacement sentence) is byte-identical across VERA/ARGUS/CATO.
  command_or_method: awk-extract the machinery span from each of the 3 files; pairwise diff + sha256.
  expected: all three pairwise diffs empty; identical sha.
  observed: all three diffs EMPTY; sha256 == 50e8739f15590f5167ceea06d422b74f96ea1718e4b465dae718702e48dc8d89 for all three (10 lines each).
  quadrant_classification: easy-easy
  result: pass
- probe_id: p4
  description: DC3 byte-aligned region UNTOUCHED -- the SAVE-VERDICT-BYTE-ALIGNED-REGION is byte-identical across all four homes AND equal to the pre-fix baseline 494b442e.
  command_or_method: awk-extract the region between BEGIN/END sentinels; Form-1 pairwise diff of save-verdict.md vs each role file; Form-2 sha loop over all four homes vs baseline.
  expected: all three diffs empty; all four sha == 494b442ea512949906134c4318786acf7ad6f63785fef19e6afee8bc6d2842cb.
  observed: all three Form-1 diffs EMPTY; all four Form-2 sha == 494b442e baseline. Region UNTOUCHED.
  quadrant_classification: easy-easy
  result: pass
- probe_id: p5
  description: DC2 reinforcement + P8 doc-coherence repair present, contract intact -- zero P8 across the four files; DC2a body-freeze + DC2b manual-diff wording + DC4 invariant present; durability contract + exit-code map + attach-failure posture prose unchanged (additive only).
  command_or_method: grep -c P8 over four files; grep -c the new DC2a/DC2b/DC4 sentences + the existing durability/exit-code/posture headings in save-verdict.md.
  expected: zero P8 each; new additions present; contract prose intact.
  observed: P8 count 0/0/0/0. Body-freeze reinforcement (1), Attestation invariant DC4 (1), no-automated-cross-file-gate wording (1), manual-diff-not-automated-gate wording (1) all present. Durability contract heading (1), Exit-code map heading (1), Attach-failure posture HARDENED LOCKED (1) all intact.
  quadrant_classification: easy-easy
  result: pass
- probe_id: p6
  description: full-suite regression bar -- gen-data deterministic + the full app vitest suite + the substrate author-gate and stop-self-check hook test suites all green.
  command_or_method: npm run gen-data x2 (assert sha stable + committed==regen); npm test (vitest run); bash run-author-gate-tests.sh; bash run-stop-self-check-tests.sh.
  expected: gen-data deterministic no-op; full suites green.
  observed: gen-data run1==run2 sha bddfa7380be0676eb4f6c715f405ac42805deae6f036d6d630fd88c7ad7a3156, committed==regen (clean git status). vitest 3 files / 41 tests passed. author-gate 29 passed / 0 failed. stop-self-check 2 passed / 0 failed.
  quadrant_classification: easy-easy
  result: pass
- probe_id: p7
  description: no scope leak -- changed-file set is exactly the 4 substrate files + the gen-data agents.ts; v1-historical untouched.
  command_or_method: git diff --name-only merge-base(HEAD,main) HEAD; grep v1-historical.
  expected: exactly substrate/CAPTAIN_{VERA,ARGUS,CATO}.md + substrate/modules/save-verdict.md + app/src/data/generated/agents.ts; zero v1-historical.
  observed: exactly those 5 files (merge-base dacb9fd). v1-historical untouched.
  quadrant_classification: easy-easy
  result: pass
threat_coverage: []
methodology_concerns: none. The design probes were concrete and re-executable. The P2 three-site expected-survivor set (folded by the directive + rev2) correctly prevented false-route-back on the legitimate surviving attach_status occurrences; I additionally noted a fourth legitimate survivor per file (the new frozen-body prose sentence at DC1c) which the design P2 prose anticipates -- expected, not a finding.
falsifying_evidence_summary:
verification_artifacts_path: probes executed in-place from the worktree root C:/Users/denso/claude_projects/the-stoa/.claude/worktrees/arc-74-build; recorded outputs in this verdict body. No standing probe scripts left on disk (P1 throwaway commit/dir cleaned up).
summary: Built deliverable 1c66556 exercised against all of design-rev2 P1-P7 plus the directive DoD. Load-bearing probes -- P4 (DC3 four-home region == 494b442e baseline, run by hand; no automated gate), P1 (committed-content-sha == attested-sha under the new format, with a negative control reproducing the sos--yn2 divergence and proving it cannot recur), and P3 (DC1 new machinery byte-identical across the 3 reviewers, sha 50e8739f). The structural fix is sound: attach_status/attach_failure are removed from the attested verdict-format fenced block in all three reviewers and relocated to a dispatch-return-only addendum + frozen-body sentence (byte-identical across VERA/ARGUS/CATO); save-verdict.md carries the DC2a body-freeze reinforcement, the DC2b P8->manual-diff repair (zero P8 across all four files), and the DC4 attestation invariant, with the durability contract / exit-code map / attach-failure posture all preserved additively. Full regression bar green (vitest 41, author-gate 29, stop-self-check 2; gen-data deterministic). Scope clean (4 substrate + agents.ts; v1-historical untouched). VERDICT: pass.
gap_or_blocker:
