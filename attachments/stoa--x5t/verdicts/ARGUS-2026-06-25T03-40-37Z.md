status: completed
ticket: stoa--x5t
verdict: pass
design_artifact_audited: agents/design/stoa--x5t/design-rev1.md
audit_block:
  risks: []   # no load_bearing-true risk; all five load-bearing axes (DC1 correctness, DC1 parallelism, DC3 untouched, DC2/DC4/DC5, scope) clear. Non-load-bearing observations + rulings below.
  observations_not_load_bearing:
  - id: o1
    description: canonical-template-alignment.md explicitly scopes itself to the WITHIN-a-single-design.md case and disclaims cross-FILE templates. The directive + design lean on it as the authority for the cross-FILE four-home byte-identity, which it does not actually provide.
    evidence: substrate/modules/canonical-template-alignment.md L29-35 vs design sec2.3 / arc-74-build-directive.md L38
    load_bearing: false
    why_not: the design does NOT depend on that module providing a gate -- it correctly states there is no automated cross-file gate and supplies its own executable manual four-home diff (sec2.3 Form 1 + Form 2). Executability unimpaired. Latent doc-coherence wrinkle, not a fix defect.
  - id: o2
    description: residual-Q2 -- the design leaves VERA bw-comment trailing parenthetical differing from ARGUS/CATO, so the post-fix bw-comment sentence is parallel only on its load-bearing clause, not byte-identical across all three.
    evidence: design sec2.1(d) + sec4 weak-point-1; VERA L268 (no sec12 parenthetical) vs ARGUS L249 / CATO L214 (sec12 parenthetical inline)
    load_bearing: false
    why_not: if left as-is the fix is still correct -- the attested body/addendum separation does not depend on the bw-comment trailing convention. Cost is cosmetic non-parallelism only. ARGUS recommends tightening (ruling Q2) but does not block.
  rulings_on_residual_questions:
  - q: Q1 -- DC2(b) P8 to manual-diff doc-coherence repair (save-verdict.md L31 + L147) in scope or carve to follow-up?
    ruling: IN SCOPE. Inside the file DC2 already edits; removes a contradictory automated-gate-exists claim that would mislead the next verifier into skipping the mandatory manual four-home diff this arc relies on; directly serves the arc DC3 posture; leaving it is a known-defect-deferred. Core does not depend on it, so low-risk to fold. CONFIRMED in-scope, not scope-creep.
  - q: Q2 -- full bw-comment sentence byte-identical across the three, or only the load-bearing clause?
    ruling: FULL byte-identity recommended. Fold all three onto one identical sentence (carry the sec12 parenthetical on all three); cheap, removes the future-drift hazard + future-re-audit flag. NOT a blocker -- load-bearing-clause parallelism is acceptable if DAEDALUS declines; P3 guards the load-bearing machinery either way.
  - q: Q3 -- addendum placement -- semantic anchor vs exact per-file insertion line?
    ruling: SEMANTIC ANCHOR sufficient. The three files have genuinely different section tails, so one hard line-anchor cannot fit all three. The design anchor (immediately after the verdict-format fenced block + its definitions/notes, before the sec7 canonical-save-path prose, labeled post-attach) + the verbatim label is mechanically precise; P3 catches drift. No exact line needed.
  dc3_untouched_confirmation: CONCUR -- region UNTOUCHED is correct. Independently verified this Phase: the byte-aligned region (between BEGIN/END sentinels) contains NO attach_status field -- only the printf of the verdict-body slot, the sha round-trip, the threat-coverage guard, and bw attach with attach_rc-capture + stderr breadcrumb (the WARN echo references attach_status only in a comment string, not as a body field). Four-home region sha byte-identical at 494b442ea512949906134c4318786acf7ad6f63785fef19e6afee8bc6d2842cb (re-measured), matching the design baseline. The bug lives entirely in what the seat copies into the verdict-body slot, so a prose-only redefinition leaves the region untouched. The four-home diff spec (Form 1 awk-extract + pairwise diff; Form 2 sha-loop) is correct + executable -- awk sentinel idiom matches on-disk markers; Form-2 loop reproduces the 494b442e baseline.
  dc1_correctness_confirmation: SOUND. Removing attach_status/attach_failure from the attested verdict-body and relocating them to a post-attach dispatch-return-only addendum makes committed-sha == attested-sha hold BY CONSTRUCTION: the attested body then contains no field whose value is knowable only after the attach, so nothing mutates the body post-hash. Traced the full seat flow (construct block to printf as verdict-body to sha to bw attach to learn attach_rc) and found no other post-round-trip body-write path; the frozen-body sentence + the bw-comment attested-body-not-addendum fix close the two surfaces (post-edit + comment-from-wrong-block) that could re-import it. Field text moves verbatim (role-file L254-255 == addendum proposed text == save-verdict.md clause-d text), no content drift.
  dc1_parallelism_confirmation: SOUND in shape. The new machinery (addendum label + frozen-body sentence + the two relocated field lines + the bw-comment replacement sentence) is specified byte-identical across VERA/ARGUS/CATO and guarded by P3 (automatic route-back if non-empty). Pre-existing seat-specific body-field differences (VERA probes_executed, ARGUS audit_block, CATO concerns) correctly excluded from the parallelism requirement. Only softness is the bw-comment trailing parenthetical (o2 / ruling Q2) -- non-load-bearing.
  dc5_threat_concurrence: CONCUR -- not threat-ratified, no named threat M-n. This is the sec35.5 self-reference carve-out class verbatim: a process/role-file verdict-attestation-hygiene change with NO runtime attack path. The divergence is between two copies of the seat OWN verdict, both authored by the trusted seat under the trusted PLINY brief; no runtime attacker injects a divergent field. The degraded property is per-verdict tamper-EVIDENCE, but exploiting it needs an in-tree-commit-mutator attacker outside the substrate threat model (git history + PR review is that control, not the per-verdict sha). DAEDALUS carve-out classification CONFIRMED (ARGUS owns A4 confirmation per sec35.1/sec35.5). The A1 restatement beat is a NO-OP for this arc; no A3 map, no threat-anchored probe required.
  non_findings: |
    - post-hash body mutation via some other field -- traced full seat flow; attach_status/attach_failure were the ONLY post-attach-knowable fields; no other mutation path. Discharged.
    - P2 would false-positive on the relocated addendum or the region attach_rc comments -- P2 scopes its assertion to BETWEEN the verdict-format fences, so the relocated addendum + the region attach_rc comment-strings + the post-region attach-failure-posture prose paragraph are correctly outside scope. Discharged.
    - save-verdict.md DC2 reinforcement contradicts the durability contract -- the reinforcement is purely additive (body-freeze + attested-artifact statement); clause-(d) durability contract, exit-code map, attach-failure posture untouched in substance. Discharged.
    - DC4 leaves a real gap by not adding a mechanical gate -- defensible: DC1 removes the defect structurally so committed==attested holds by construction; a standing guard would defend a now-structurally-prevented defect; the directive itself warns against bolting a check into the byte-aligned region; the heavier NOMOS-on-merge assertion is correctly named as a deferred follow-up, not silently dropped. Sound non-load-bearing call.
    - scope leak beyond 3 role files + save-verdict.md -- design edit set is exactly the 3 reviewers + save-verdict.md; v1-historical/ explicitly fenced out; P7 guards it. DC2(b) is inside save-verdict.md (already in-set). No leak. Discharged.
    - wrong author field in any edit-target -- none of the four edit targets carry an author/owner/copyright field; the substrate authorship convention (PRINCIPAL = Denson Smith) is unaffected. Discharged.
  threat_coverage_assessment: N/A -- this arc is not threat-ratified (sec35.5 carve-out, ARGUS-confirmed); no threat-ratified mitigation in scope, so no threat-anchored probe is required and the empty threat_coverage list is the correct carve-out state.
summary: |
  The design is a clean, correctly-scoped structural fix. It splits the conflated reviewer verdict-format block into an attested verdict-body (post-attach fields removed) plus a dispatch-return-only addendum, adds a verbatim frozen-body rule, and redirects the bw-comment instruction at the attested body -- which makes committed-sha == attested-sha hold BY CONSTRUCTION. All five load-bearing audit axes clear: DC1 correctness sound (no residual post-hash mutation path), DC1 parallelism correctly specified + P3-guarded, DC3 region-UNTOUCHED independently CONFIRMED (region carries no attach_status field; four-home sha re-measured at 494b442e matching baseline; the four-home diff spec correct + executable), DC2/DC4 additive and do not contradict the durability contract, DC5 not-threat-ratified CONCURRED (sec35.5 self-reference carve-out, no runtime attack path, no M-n, A1 beat a no-op). No load-bearing risk surfaces. Two non-load-bearing observations + three rulings (Q1 DC2b IN SCOPE; Q2 full byte-identity recommended not blocking; Q3 semantic anchor sufficient). Posture: PASS -- ready for build.
gap_or_blocker:
