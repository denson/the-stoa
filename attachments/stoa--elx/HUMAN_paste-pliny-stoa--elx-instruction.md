# Engagement brief v2 — PLINY_the-stoa (orchestrator) — stoa--elx RESUME: execute the locked polish fold

Read .claude/MAJOR_PLINY.md and assume the orchestrator role for the-stoa. You succeed the closed e9a93d58 seat.

**NARROW RESUME.** Gauntlet complete; user-tier + Grand gates PASSED on b698f4ef (held unmerged on stoa--elx/build). Your ONE job: execute your predecessor's LOCKED fold plan (its 00:57 comment on stoa--elx — read it verbatim; it is your work order) with ONE amendment from the Grand's ratification: **the polish commit gets its own bounded CATO re-glance** (this supersedes the prior 'not required' call — Decider relay comment has the record).

## Chain

... → Polybius the Decider (user-tier) → POLYBIUS_the-stoa (FM — **your only up-channel**) → YOU → CAPTAINs. Fire only on the FM's dispatch.

## The fold (one shot, from the locked plan + amendment)

1. ADA one polish commit on stoa--elx/build: c1 (finally { if () { .Close() } } on the win_path.ps1 catch path), c2 (remove the dead -Broadcast stub), c3 (guard mktemp), c4 (local rc), + sync design-rev3's DC2 win_path.ps1 body byte-exact-to-shipped. Author = Denson Smith + ADA seat trailer per §28.
2. VERA TARGETED re-verify: P2/P2b (append/present/fail_read exit-3 no-write) + P3 (fail_length exit-4 no-write) against the THROWAWAY key (finally teardown; real HKCU Environment Path untouched — one real-key mutation = auto route-back) + bash -n bootstrap-bw.sh + P7 byte-unchanged all 3 targets + P10 authorship + P9 FULL SUITE (gen-data deterministic + vitest).
3. CATO bounded re-glance on the polish diff (REQUIRED — Grand-ratified).
4. NOMOS re-conformance on the FINAL polished commit.
5. Hand back to the FM with per-commit coverage stated precisely. NOT merged, NOT pushed — the Decider merges.

## Disciplines

Worktree: recreate from the existing branch if your predecessor's teardown already removed it (`git worktree add .claude/worktrees/arc-75-build stoa--elx/build` — check first; the arc-75-build worktree existed at last sight). D-B: re-read bw immediately before the commit. 10-min liveness on pickups. bw positional comments, no backticks/`$()`.

## Close signal

`CLOSE ME — stoa--elx polish folded + re-verified; awaiting FM verification + Decider merge`

## Compaction recovery

`git show beadwork:attachments/stoa--elx/HUMAN_paste-pliny-stoa--elx-instruction.md`
