verdict: CONFORMANT
ticket: stoa--elx (arc-75 — bw bootstrap into the Stoa install process)
checked_output: build commit b698f4ef683f411e81f24068853c30a8fa4f1030 on branch stoa--elx/build (the orchestrator output — the built artifact handed to Phase D for relay-up to the Grand second gate). Audited against arc-75 directive DC1-DC8 + DoD, the GATED plan-bw-bootstrap-v2.md, design-rev3, and bw/repo/upstream ground truth.
divergences: []   # none blocking; two dispositioned non-divergences recorded below

audit_items:
- item: 1 — commit reality
  result: CONFORMANT
  evidence: git show b698f4ef — SHA confirmed; 4 changed files match deliverables exactly (A substrate/win_path.ps1 53L NEW; A substrate/bootstrap-bw.sh 391L NEW; M substrate/install.sh +17/-10; M skills/install-stoa/SKILL.md +21). b698 IS ancestor of stoa--elx/build; b698 NOT on main; main tip unchanged cfd683d7 == origin/main cfd683d7; ls-remote for refs/heads/stoa--elx/build returns NOTHING (branch NOT pushed). NOT merged, NOT pushed, main untouched.
- item: 2 — authorship (FM P10 gate)
  result: CONFORMANT
  evidence: Author = denson <densonsmith2@gmail.com> (git-configured PRINCIPAL, NOT overridden); committer identical. Section 28.9 seat trailer PRESENT — Co-Authored-By CAPTAIN_ADA_the-stoa <captain-ada@the-stoa.local> (plus the standard Claude Opus 4.8 trailer). Author-like-field sweep over all 4 changed-file blobs (author/authors/owner/creator/created_by/maintainer/by/copyright/holder/vendor/publisher) — the only true author FIELDS are the header comment "Author: Denson Smith" in win_path.ps1 L15 and bootstrap-bw.sh L28, both naming Denson Smith. All other lexical hits are prose (by / authoring / deployed-by). The SKILL.md ~L217 "agent-author" hit is the LIEUTENANT skill-NAME reference (1 LIEUTENANT skill (agent-author)), NOT an authorship field — CATO classification verified correct. ZERO foreign-author.
- item: 3 — spec conformance (directive DC1-DC8 + DoD + gated v2 plan + design-rev3)
  result: CONFORMANT
  evidence: DC2/win_path.ps1 mechanism is the PowerShell .NET registry API — [Microsoft.Win32.Registry]::CurrentUser + OpenSubKey (writable) + SetValue Path,newUser,kind; NO real setx and NO reg.exe invocation (grep for non-comment setx/reg.exe/reg-add = NONE; the only textual hits are the negating header comment "never reg.exe, never setx"). Invoked via -File (no argv-mangling). DC6 flag BOOTSTRAP_BW=0 default (install.sh L153, DEFAULT OFF), --bootstrap-bw parsed L743-744, pre-flight L760-765 sits AFTER the [ -n TARGET ] || err target validation (L758) and BEFORE case TARGET in (L774) — target-independent, exactly per rev3 N4; documented in --help usage L26-27. DC7 reversal: SKILL.md Beat 1 + must-NOT entries reversed with recorded justification; the bw-init separation line "Do not auto-initialize bw after install ... POLYBIUS job" (L246) is UNCHANGED (absent from the diff). Scope in-slice: only substrate/ + skills/install-stoa touched — no arc-19 edit, no u--9s2 call-site, no bw-upgrade change, install-stoa NOT moved. The 4th file (win_path.ps1) is a DC2 implementation split ratified by the FM design-GO (strictly-safer implementation of the DC2 crux, an implementation choice not new scope, Approved) — not scope creep.
- item: 4 — bw ground truth (ticket state + gauntlet verdicts)
  result: CONFORMANT
  evidence: stoa--elx status open/in-progress (header circle-open) — consistent with an in-progress Phase-D arc (design GATED-GO, build done, not closed). Both gauntlet verdicts exist AND are attached — attachments/stoa--elx/verdicts/VERA-2026-07-10T000737Z.md and CATO-2026-07-10T001519Z.md on the beadwork branch + on-disk in the worktree. VERA verdict = PASS (all 10 probes P1-P10 executed vs b698f4ef; hard-safety held, real HKCU Environment Path byte-identical sha256 0951f1e4; vitest 41/41; gen-data deterministic). CATO verdict = pass (4 minor hygiene NITs c1-c4, all shippable; security read POSITIVE). Both consistent with a passing gauntlet.
- item: 5 — PRINCIPAL-gate ratification
  result: CONFORMANT
  evidence: FM design GO/NO-GO = GO on stoa--elx by POLYBIUS_the-stoa (floor-manager) at 2026-07-09T23:36:47Z, with independent ground-checks (anchor re-ground, F1/F2 cleared, 4->5 deliverable-flag CONCUR). The arc SECOND gate (the Grand, on the built artifact) is still PENDING per the FM EXECUTE-AS-WRITTEN conditions (nothing merges/pushes until the Grand gates the built artifact). No premature merge/push: b698 not on main, origin/main unchanged cfd683d7, branch not pushed.

dispositioned_non_divergences:   # named per the dispatch brief; NOT build DIVERGENT
- CATO NITs c1-c4 (win_path.ps1 catch-path no wk.Close(); -Broadcast no-op stub; mktemp -d unguarded; rc global) — behavior-neutral hygiene, CATO-rated shippable, folded into a documented Phase-D polish. Not blocking.
- DC2.0 stale-machine-state claim (design-rev3 narrates bw >= 0.13.2 present; the reference machine actually carries bw 0.13.1, below floor) — a design-NARRATIVE inaccuracy only. VERA unit-verified the shipped version_ge/HAVE_BINARY logic correctly does NOT skip on a below-floor version. Phase-D design-canon-audit correction item, NOT a build defect.

ground_truth_consulted: bw show stoa--elx (status open; Grand PROBLEM 2026-06-27; FM design-GO 2026-07-09T23:36:47Z; VERA PASS comment). git ls-tree beadwork attachments/stoa--elx (v2 plan + VERA + CATO verdicts). agents/verdicts/stoa--elx/{VERA,CATO}-*.md on-disk. git show b698f4ef (-s format + --name-status + --stat + per-file blobs). git rev-parse main / origin/main (cfd683d7). git merge-base --is-ancestor (b698 on build, not main). git ls-remote origin main + refs/heads/stoa--elx/build (branch absent on origin). git worktree list (arc-75-build registered @ b698f4ef). substrate/arcs/arc-75-build-directive.md (DC1-DC8 + Deliverables + DoD + Out-of-scope). Author-like-field sweep over the 4 changed-file blobs.

summary: Audited arc-75 build commit b698f4ef (branch stoa--elx/build) — the Phase-D built artifact — against directive DC1-DC8 + DoD, the GATED v2 plan, design-rev3, and bw/repo/upstream ground truth across all five dispatch audit items. Every checkable claim matches ground truth: commit reality (4-file deliverable set exact, not merged/pushed, main + origin/main unchanged at cfd683d7); authorship clean (Author = Denson Smith git-configured, ADA section-28.9 seat trailer present, zero foreign-author — the only author FIELDS name Denson Smith, the agent-author hit is a skill-name not an authorship field); spec conformance (DC2 registry .NET API with no real setx/reg.exe, DC6 opt-in default-OFF pre-flight in the target-independent window, DC7 reversal preserving the bw-init separation, scope in-slice); bw ground truth (VERA PASS + CATO pass both attached and consistent); PRINCIPAL-gate (FM design-GO recorded, the Grand second gate still correctly PENDING with no premature merge/push). No divergence beyond the two dispositioned non-divergences (CATO c1-c4 hygiene NITs; the DC2.0 design-narrative stale-machine claim). VERDICT CONFORMANT — the built artifact may propagate to the Grand second gate.
