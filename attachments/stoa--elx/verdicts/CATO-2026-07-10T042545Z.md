status: completed
ticket: stoa--elx
verdict: pass
diff_reviewed: cc325c6f (polish; 3 files +8/-11) — bounded re-glance ONLY, not a re-review of b698f4ef
design_artifact_compared_against: agents/design/stoa--elx/design-rev3.md
concerns: []
follow_ups: []
verifier_coverage_assessment: >
  VERA PASS is methodologically sound and sufficient for a behavior-neutral polish.
  VERA drove the load-bearing paths: P2b fail_read exit3 (proves c1 finally-guard skips
  the Close when $wk=$null so NO spurious write on the fail path), P3 fail_length exit4
  (proves the finally double-Close is an idempotent no-op preserving exit4 + no-write),
  P2 a-e happy append/present/token paths (exit0, kind + %tokens% + idempotency preserved),
  P7 install.sh byte-identical across all 3 targets, and the HARD-SAFETY real
  HKCU\Environment Path sha256 (0951f1e4.../LEN800) byte-identical before/after every probe
  with throwaway keys torn down. One assessed-and-cleared coverage note: VERA did not
  fault-inject the exit5 (fail_exception/catch) path — the single path where the finally
  behavior genuinely CHANGES (was: no Close, OS releases handle on exit; now: explicit
  guarded Close). This is NOT a blocking gap: (1) the exit5 path is exception-triggered
  and hard to inject deterministically (hard-to-verify quadrant); (2) it is statically
  proven behavior-neutral given the two web-verified capability facts below — the finally
  performs NO registry write (Close != SetValue) and cannot throw (RegistryKey.Close is
  idempotent and returns without work on an already-closed key; on a still-open key it is a
  normal Close identical to lines 38/44/46/48); (3) its constituent operations — guarded
  Close of a valid open key AND idempotent double-Close — are each empirically exercised by
  VERA on the exit0 appended and exit4 fail_length paths. Coverage judged sufficient.
c1_finally_crux: >
  BEHAVIOR-NEUTRAL, web-verified against Microsoft docs (CLAUDE.md current-docs rule).
  Fact 1: PowerShell runs the finally block on exit inside try — so the present/fail_length/
  would_append/appended paths (which already Close $wk then exit) DO hit a second Close.
  Fact 2: Microsoft.Win32.RegistryKey.Close is idempotent — does nothing on an already-closed
  key, raises no ObjectDisposedException. Path-by-path: exit3 fail_read ($wk=$null) the guard
  if ($null -ne $wk) correctly SKIPS the Close (no write, no NullReference throw); exit0 present
  / exit4 fail_length / exit0 would_append / exit0 appended each explicitly Close then the
  finally double-Closes = no-op; exit5 catch now explicitly Closes an open-or-null $wk (guard
  handles null) — the only genuinely-new operation, and it is a normal Close, no write. The
  SINGLE SetValue site (line 47) remains the ONLY write and is still gated behind the
  idempotent-present early exit (38), composed-length early exit (44), and dry-run early exit
  (46) — the finally added nothing to the write path. Registry / output / exit-code semantics
  unchanged.
c2_broadcast: >
  TRULY DEAD. grep -rn Broadcast substrate/ = zero matches (only past verdict + design records
  reference it). The wrapper win_ensure_on_path never passed -Broadcast; the removed
  if ($Broadcast){} stub never executed. Removal changes nothing observable.
c3_mktemp_guard: >
  FAIL-CLOSED and correct. [ -d "$tmp" ] || fail "mktemp -d failed" sits BEFORE any use of
  $tmp; on mktemp failure $tmp is empty, [ -d "" ] is false, fail() (line 56, exit 1) fires.
  No unguarded rm -rf on an empty $tmp reachable.
c4_local_rc: >
  CORRECT, load-bearing rc-capture rule preserved. The edit is a BARE declaration local out rc
  (no =$?), so it does NOT reset $?. The out=$(...) assignment and rc=$? remain on separate,
  immediately-adjacent lines with no pipe / no || true — rc still captures the powershell.exe
  exit. No other function reads a global rc (line 220 $rc_file is unrelated), so scoping rc
  local is behavior-neutral.
design_sync: >
  BYTE-EXACT confirmed. Extracted the powershell fenced block from design-rev3.md and diffed
  the param-onward body against substrate/win_path.ps1 (lines 16-51) — exact string match, zero
  diff. The header comment (incl. Author: Denson Smith) is not embedded, as expected; the
  byte-exact claim covers the executable body. Phase-D errata correctly marked DISCHARGED.
scope: >
  CLEAN. The diff touches ONLY the 4 nits (c1+c2 in win_path.ps1, c3+c4 in bootstrap-bw.sh) and
  the design-rev3 sync/errata-discharge. Every hunk inspected; no drive-by, no scope creep.
authorship: >
  CLEAN. Author fields = Denson Smith across all 3 files (win_path.ps1:15, bootstrap-bw.sh:28,
  design-rev3.md:1). Zero foreign author-like fields. Co-Authored-By seat trailer present per
  substrate convention (layered on top of, not replacing, the PRINCIPAL Author field).
summary: >
  Bounded re-glance of the post-gate polish cc325c6f. All four CATO nits c1-c4 are correctly and
  minimally addressed and the polish is genuinely behavior-neutral: c1 the guarded finally-Close
  is proven neutral against every exit path (web-verified: PowerShell runs finally on exit +
  RegistryKey.Close is idempotent), with the SINGLE SetValue write still behind all three gates;
  c2 -Broadcast was dead (zero callers); c3 the mktemp guard is fail-closed; c4 local out rc is a
  bare declaration that preserves the rc=$? capture rule. Design body is byte-exact to the shipped
  file, scope is confined to the 4 nits + sync, and authorship is all Denson Smith. VERA PASS is
  methodologically sound; the one uninjected exit5 catch path is statically neutral and its
  operations are exercised elsewhere — sufficient. No new craft/hygiene/security/scope issue
  introduced. Posture: CLEAN — PASS.
attested_by: CAPTAIN_CATO_the-stoa (subagent) | caller-sid 38b42688-9024-46cf-8b9a-fb68aa3526f5