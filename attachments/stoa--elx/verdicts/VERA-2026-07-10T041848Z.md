status: completed
ticket: stoa--elx
verdict: pass
design_artifact_verified_against: agents/design/stoa--elx/design-rev3.md (§3 Verification probes; committed probe scripts agents/verification/stoa--elx/)
build_verified: stoa--elx/build @ cc325c6f (behavior-neutral post-gate polish folding CATO c1-c4 onto fully-gauntleted b698f4ef)
scope: TARGETED re-verify — falsify the claim that the c1-c4 polish changed NO observable behavior. Real HKCU\Environment Path held byte-identical throughout.
probes_executed:
- probe_id: p2
  description: registry-safe append driving the SHIPPED win_path.ps1 against the THROWAWAY key Environment_stoa_test — present/append/token-preserve/no-double-append/fail_read (P2b). Falsifies c1 (finally-close) altering exit codes or causing a spurious write on the fail path.
  quadrant_classification: easy-easy — code probe (drive win_path.ps1, assert exit code + registry read-back); detection and verification both mechanical.
  command_or_method: bash agents/verification/stoa--elx/p2_registry_safe.sh
  expected: (a) appended once exit 0; (b) present exit 0 no write; (c) ExpandString kind + %USERPROFILE% token preserved, appended, exit 0; (d) token-form present, NO double-append, exit 0; (e/P2b) key absent -> RESULT=fail_read exit 3, absent key NOT created (NO write); teardown GONE.
  observed: (a) RESULT=appended EXIT=0, value became C:\Some\Other\dir;C:\Users\denso\.local\bin; (b) RESULT=present EXIT=0, after==before; (c) RB_KIND=ExpandString, %USERPROFILE%\Documents;C:\Users\denso\.local\bin, EXIT=0; (d) RESULT=present EXIT=0, unchanged; (e) RESULT=fail_read EXIT=3, ABSENT_KEY_STILL_ABSENT=True; TEARDOWN_GONE=True. On the exit-3 path $wk is $null so the c1 guarded finally-Close is skipped and no write occurs — c1 behavior-neutral on the fail path.
  result: pass
- probe_id: p3
  description: composed-length fail-loud (M2 + M4) against the throwaway key with -Ceiling 10 -> fail_length exit 4 NO write; legit half at 4095 appends. Falsifies c1 finally double-close (on this path step-5 already Closed $wk, then finally Closes again) perturbing exit 4 or the no-write.
  quadrant_classification: easy-easy — drive win_path.ps1 with a small ceiling, assert exit 4 + registry read-back unchanged.
  command_or_method: bash agents/verification/stoa--elx/p3_length.sh
  expected: -Ceiling 10 -> RESULT=fail_length exit 4, after==before (no write); -Ceiling 4095 -> RESULT=appended exit 0; wrapper routes rc 3/4/5 -> fail_loud_path GUI step.
  observed: RESULT=fail_length composed=1362 EXIT=4, after==before (C:\Some\Other\dir unchanged — no write); legit half RESULT=appended EXIT=0; bootstrap-bw.sh:204 case arm 3|4|5) -> fail_loud_path; GUI "Edit environment variables for your account" step present; TEARDOWN_GONE=True. The c1 finally's idempotent second Close on the exit-4 path did NOT perturb exit 4 or the no-write.
  result: pass
- probe_id: p_bashn
  description: bash -n syntax validation of bootstrap-bw.sh after c3 (mktemp guard) + c4 (local out rc).
  quadrant_classification: easy-easy — static syntax check.
  command_or_method: bash -n substrate/bootstrap-bw.sh
  expected: rc 0 (syntax valid)
  observed: rc=0
  result: pass
- probe_id: p_psast
  description: PowerShell AST parse (no exec) of win_path.ps1 after c1 (finally) + c2 (-Broadcast removal).
  quadrant_classification: easy-easy — static AST parse via [Parser]::ParseFile.
  command_or_method: [System.Management.Automation.Language.Parser]::ParseFile(win_path.ps1)
  expected: 0 parse errors
  observed: PS_AST_PARSE_OK (0 errors)
  result: pass
- probe_id: p_c2
  description: c2 confirmation — the dead -Broadcast switch is fully removed and was never passed by the bootstrap wrapper (so removal is behavior-neutral).
  quadrant_classification: easy-easy — grep-against-source.
  command_or_method: grep -rn 'Broadcast' substrate/win_path.ps1 substrate/bootstrap-bw.sh ; inspect wrapper invocation args
  expected: zero -Broadcast references; wrapper passes only -Dir/-KeyName/-Ceiling/[-DryRun]
  observed: grep rc=1 (no matches); wrapper line 200 invokes -Dir "$dir_win" -KeyName ... -Ceiling 4095 "${extra[@]}" where extra only ever adds -DryRun. $Broadcast was always false -> the removed stub `if ($Broadcast){...}` never executed. Behavior-neutral.
  result: pass
- probe_id: p7
  description: install.sh absent-flag --dry-run output byte-unchanged across user|project|subproject (install.sh NOT touched by the polish — regression bar must still hold) + no-target pre-flight-ordering probe.
  quadrant_classification: easy-easy — diff built vs pre-arc (cfd683d7) dry-run output, normalize only the inherent deployed_at time field.
  command_or_method: bash agents/verification/stoa--elx/p7_byte_unchanged.sh ; then re-run with only deployed_at=<UTC> normalized (sed) and assert byte-identical
  expected: byte-identical across all 3 targets after normalizing the sole inherent time field (deployed_at); no-target --bootstrap-bw -> exit non-zero on "target is required", pre-flight does NOT fire, real key sha256 unchanged.
  observed: raw diff showed ONLY the deployed_at wall-clock timestamp differing per target (substrate_sha=cc325c6f identical both sides); after normalizing deployed_at only -> [user] BYTE-IDENTICAL (220 lines), [project] BYTE-IDENTICAL (184), [subproject] BYTE-IDENTICAL (78). No-target probe: exit=2, "install.sh: error: --target is required", pre-flight fired? NO, real-key sha256 unchanged? YES.
  result: pass
- probe_id: p10
  description: authorship audit of the 3 changed files (substrate/win_path.ps1, substrate/bootstrap-bw.sh, agents/design/stoa--elx/design-rev3.md).
  quadrant_classification: easy-easy — grep author-like fields against source.
  command_or_method: grep -rniE 'author|owner|creator|copyright|maintainer|vendor|publisher|created_by' on the 3 files; confirm shipped files carry '# Author: Denson Smith'
  expected: every authorship field = Denson Smith, ZERO foreign names; both shipped files carry # Author: Denson Smith.
  observed: substrate/win_path.ps1:15 '# Author: Denson Smith'; substrate/bootstrap-bw.sh:28 '# Author: Denson Smith'; design-rev3.md:1 '<!-- author: Denson Smith -->'. All other matches are prose ("authored", "authorship", "Author field / attribution: Denson Smith"). Zero foreign names.
  result: pass
- probe_id: p9_gendata
  description: gen-data determinism — regen the roster in the worktree, assert the worktree git diff is EMPTY (no roster drift from the polish).
  quadrant_classification: easy-easy — run gen-data, assert worktree git diff --quiet.
  command_or_method: (app) npm run gen-data ; git -C <worktree> diff --quiet
  expected: worktree git diff EMPTY after regen.
  observed: gen-data discovered 16 role files, wrote agents.ts (4 MAJOR / 12 CAPTAIN / 9 LIEUTENANT); worktree git diff EMPTY (deterministic).
  result: pass
- probe_id: p9_vitest
  description: FULL app vitest suite green (the full-suite regression bar).
  quadrant_classification: easy-easy — run vitest, assert all green.
  command_or_method: (app) npm run test  (vitest run)
  expected: all tests green (prior pass 41/41 — report actual count).
  observed: Test Files 3 passed (3); Tests 41 passed (41); duration 1.56s. Matches prior pass count.
  result: pass
- probe_id: p_realkey_safety
  description: HARD SAFETY — real HKCU\Environment Path held byte-identical across the entire probe run; no throwaway keys leaked.
  quadrant_classification: easy-easy — read-only .NET registry read + sha256 before/after.
  command_or_method: read-only OpenSubKey('Environment',$false) + GetValue(DoNotExpandEnvironmentNames) + SHA256(UTF16LE) BEFORE and AFTER all probes; enumerate throwaway keys.
  expected: BEFORE == AFTER (sha256 0951f1e4…, LEN=800, FIRST=C:\Users\denso\.local\bin — corroborating the prior VERA pass); Environment_stoa_test + Environment_stoa_test_absent GONE.
  observed: BEFORE LEN=800 FIRST=C:\Users\denso\.local\bin SHA256_UTF16LE=0951f1e40b95b37998f598d7b0483be64f512eb6eabf0c05d4ac6b5a3abf1a99; AFTER identical (REAL_KEY_BYTE_IDENTICAL=YES); Environment_stoa_test_GONE=True, Environment_stoa_test_absent_GONE=True.
  result: pass
threat_coverage:
- mitigation: M2
  threat: Windows USER PATH clobber/truncation (failed-read-mistaken-for-empty overwrite; setx truncation/expansion/kind-downgrade)
  defeats_via_probe: p2
  probe_evidence: agents/verification/stoa--elx/p2_registry_safe.sh recorded output (this verdict, p2 observed) — P2(e)/P2b fail_read exit 3 with ABSENT_KEY_STILL_ABSENT=True.
  attack_path_exercised: Drove the read-failure attack path (key absent -> would-be "empty PATH -> overwrite" clobber): observed RESULT=fail_read exit 3 and NO write (absent key not even created), so a failed read is never mistaken for empty; and (legit low-rate side) present/append/token cases wrote correctly and idempotently without double-append. The c1 finally leaves this unchanged ($wk=$null -> guarded Close skipped).
- mitigation: M4
  threat: fail-loud-not-firing (unsafe PATH condition silently proceeds behind a green result)
  defeats_via_probe: p3
  probe_evidence: agents/verification/stoa--elx/p3_length.sh recorded output (this verdict, p3 observed) — RESULT=fail_length composed=1362 exit 4, after==before, wrapper routes rc 3/4/5 -> fail_loud_path.
  attack_path_exercised: Drove the composed>ceiling breach (attack path: a genuine append would exceed the login-truncation cliff): observed exit 4 + NO write + fail_loud_path GUI manual step; legit half at ceiling 4095 wrote normally. The c1 finally's idempotent double-Close on this path did not perturb exit 4 or the no-write.
methodology_concerns: |
  (1) SCOPE NOTE, not a gap: this is a TARGETED behavior-neutrality re-verify of the c1-c4 polish, not a fresh full threat-ratification gate. The design's threat-anchored probes P5 (M1 SHA256 fail-closed) and P4 (M3 false-green) were NOT re-executed in this dispatch — they were out of the brief-scoped targeted set and their threat-defeating logic is untouched by the polish: c1/c2 are confined to win_path.ps1's PATH-mutation crux (M2), and c3 is a fail-closed guard `[ -d "$tmp" ] || fail` placed UPSTREAM of the unchanged SHA256-verify block in win_obtain (it only fires when mktemp -d produces no dir; bash -n confirms syntax validity; on the success path it is a pass-through). M1 and M3 remain covered by the b698f4ef full-gauntlet VERA PASS. threat_coverage above therefore cites only the mitigations whose code the polish touched AND whose threat-anchored probes were executed here (M2/p2, M4/p3), each backed by a probe present in probes_executed.
  (2) c3's fail path (mktemp -d failure) is not force-exercised — it is a fail-closed guard verified by syntax + read; forcing an mktemp failure was out of scope and low-value (the guard's only effect is to abort loudly before any download, which is strictly safer than the pre-c3 unguarded state).
falsifying_evidence_summary: ""
verification_artifacts_path: agents/verification/stoa--elx/ (committed probe scripts p1-p7 reused; p2/p3/p7 driven against the shipped cc325c6f artifacts; recorded outputs captured in this verdict's per-probe observed fields)
summary: |
  I attempted to FALSIFY the claim that the cc325c6f polish (CATO c1-c4) is behavior-neutral, and could not. The build was exercised as follows. c1 (win_path.ps1 finally-close): P2(e)/P2b drove the fail_read attack path (exit 3, absent key NOT created) — on that path $wk is $null so the new guarded finally-Close is skipped and NO spurious write occurs; P3 drove the fail_length path (exit 4, no write) — where the finally double-closes an already-closed $wk, an idempotent no-op that did not perturb exit 4 or the no-write; the happy append/present/token paths (P2 a-d) preserved exit 0, kind, %tokens%, and idempotency. c2 (-Broadcast removal): grep confirms zero remaining references and the wrapper never passed -Broadcast, so the removed `if ($Broadcast){}` stub was dead code that never executed — behavior-neutral. c3 (mktemp guard) + c4 (local out rc): bash -n rc 0 and PS AST parse 0 errors; the rc=$? capture line is unchanged and still immediately follows the powershell.exe command-substitution, so making rc function-local does not change the value the next case reads. The install.sh regression bar held: P7 byte-identical across user|project|subproject after normalizing only the inherent deployed_at timestamp (substrate_sha identical), no-target --bootstrap-bw errors before the pre-flight fires, real key untouched. Full suite green: gen-data deterministic (worktree diff empty) and vitest 41/41 (3 files). The most load-bearing checks were P2(e) (proving c1's finally does not write on the fail path) and the real-key HARD-SAFETY sha256, which was byte-identical before and after every probe (0951f1e4…, LEN=800, FIRST=C:\Users\denso\.local\bin — corroborating the prior VERA pass) with both throwaway keys torn down. Verdict: PASS — the polish is behavior-neutral and the real Windows USER PATH was never touched.
