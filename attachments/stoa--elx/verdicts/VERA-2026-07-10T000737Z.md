status: completed
ticket: stoa--elx
verdict: pass
design_artifact_verified_against: agents/design/stoa--elx/design-rev3.md (§Verification probes P1-P10; §DC2/DC3; §DC8 threat_coverage M1-M4)
build_verified: stoa--elx/build @ b698f4ef (substrate/win_path.ps1, substrate/bootstrap-bw.sh, substrate/install.sh, skills/install-stoa/SKILL.md)
probes_executed:
- probe_id: P1
  quadrant_classification: easy-easy
  description: idempotent skip on THIS machine — --check then --dry-run report state / plan without mutation; real-key sha256 guard.
  command_or_method: bash substrate/bootstrap-bw.sh --check ; bash substrate/bootstrap-bw.sh --dry-run ; real HKCU\Environment Path sha256 before/after. version_ge floor logic unit-tested in isolation.
  expected: --check + --dry-run emit no mutation; win_ensure_on_path RESULT=present (read-only); zero real-key change.
  observed: --check reported PowerShell-can-resolve-bw=YES, no changes; --dry-run logged would-download + win_path RESULT=present + would-verify DC3; real key sha256 IDENTICAL before/after (0951f1e4..., KIND=String LEN=800). version_ge correct incl 0.13.10>=0.13.2 and 0.13.1<0.13.2. GROUND-TRUTH DRIFT (see methodology_concerns 1): machine bw is 0.13.1 at ~/bin (below floor) so HAVE_BINARY=0 correctly; the download-SKIP branch (HAVE_BINARY=1) is not naturally exercised here — covered by version_ge unit test + static branch trace + P6 dry-run HAVE_BINARY=1 skip.
  result: pass
- probe_id: P2
  quadrant_classification: easy-easy
  description: registry-safe append driving the SHIPPED substrate/win_path.ps1 via -File against the THROWAWAY key Environment_stoa_test; trap teardown.
  command_or_method: agents/verification/stoa--elx/p2_registry_safe.sh — subcases (a)-(f); powershell -File win_path.ps1 -KeyName Environment_stoa_test.
  expected: (a) appended-once; (b) present no-write; (c) REG_EXPAND_SZ+%USERPROFILE% kind+token preserved; (d) token-form entry vs expanded -Dir present no double-append (F8); (e/P2b) absent key RESULT=fail_read exit 3 no-write (F2); (f) no setx/%PATH%/reg add|query.
  observed: (a) RESULT=appended exit0 value=OTHER;DIR appended once; (b) RESULT=present exit0 unchanged; (c) RESULT=appended, RB_KIND=ExpandString, %USERPROFILE% token intact + ;DIR; (d) RESULT=present exit0 unchanged (no double-append); (e/P2b) RESULT=fail_read exit3, absent key still absent (not created); (f) all setx/%PATH%/reg matches are negative-statement PROSE only, sole writer is SetValue at win_path.ps1:47. Teardown GONE=True.
  result: pass
- probe_id: P3
  quadrant_classification: easy-easy
  description: composed-length fail-loud — small -Ceiling forces composed>ceiling on the throwaway key; wrapper fail_loud_path route.
  command_or_method: agents/verification/stoa--elx/p3_length.sh — powershell -File win_path.ps1 -KeyName Environment_stoa_test -Ceiling 10; then -Ceiling 4095 legit; static grep of win_ensure_on_path + fail_loud_path.
  expected: RESULT=fail_length exit 4 NO write; legit ceiling appends; wrapper routes rc 3/4/5 to fail_loud_path GUI step, no write.
  observed: RESULT=fail_length composed=1362 exit4, throwaway value UNCHANGED; ceiling 4095 RESULT=appended exit0 write happened; bootstrap-bw.sh:204 case 3|4|5) -> fail_loud_path which prints "No change was made" + "Edit environment variables for your account" GUI step + exit 1. Teardown GONE=True.
  result: pass
- probe_id: P4
  quadrant_classification: easy-easy
  description: DC3 false-green discrimination (M3; F5; rev3 N3 POSIX export) — naive powershell inherits git-bash PATH vs correct reassign-from-registry.
  command_or_method: agents/verification/stoa--elx/p4_falsegreen.sh — mktemp -d POSIX stub dir, unique-named .cmd stub, export PATH=<posix-stub>:$PATH; naive `powershell -Command "<name> --version"` then the SHIPPED reassign-from-registry technique with the same name.
  expected: stub absent from registry; naive EXITS 0 (false-green live); correct reassign-from-registry EXITS 1 (caught).
  observed: STUB_IN_REGISTRY=False; NAIVE exit=0 (resolved the git-bash-inherited stub — false-green LIVE); CORRECT reassign-from-registry exit=1 (stub not seen). Discrimination proven. FIXTURE ADAPTATION (see methodology_concerns 2): literal command name adapted bw->bwveraprobe$$ because this machine is now TRUE-green for bw (a bw.cmd shim at ~/.local/bin is on the registry PATH), so the shipped verbatim-bw correct check returns exit 0 (real bw resolvable) — the false-green state the design assumed no longer holds. The reassign-from-registry MECHANISM the shipped _ps_callable_probe uses is exercised verbatim except the command name.
  result: pass
- probe_id: P5
  quadrant_classification: easy-easy
  description: SHA256 fail-closed (M1) driving the SHIPPED win_obtain via a curl-shim local mirror; target dir overridden to TEMP (never real ~/.local/bin); real upstream v0.13.2 asset for the legit half.
  command_or_method: agents/verification/stoa--elx/p5_sha256.sh — source bootstrap-bw.sh (main-stripped), shadow curl() to serve mirror, DIR_POSIX=temp; cases legit / byte-flipped zip / tampered checksums.
  expected: legit verifies+extracts+places bw.exe; corrupt-zip and wrong-hash checksums ABORT before extraction, exit non-zero, NO bw.exe placed.
  observed: LEGIT SHA256 OK -> extracted -> placed bw.exe -> `bw 0.13.2`, exit0; CORRUPT ZIP SHA256 MISMATCH -> "aborting, NO extraction" exit1, bw.exe NOT placed; WRONG-HASH checksums MISMATCH -> abort exit1, no placement. Fail-closed confirmed on the shipped win_obtain.
  result: pass
- probe_id: P6
  quadrant_classification: easy-easy
  description: Unix delegates to upstream (DC4) — static trace + dry-run drive of the shipped unix_obtain (real linux end-to-end un-exercisable on this Windows host; design weak point 6).
  command_or_method: agents/verification/stoa--elx/p6_unix.sh — grep for INSTALL_DIR pin + upstream pipe + absence of tar/unzip reimpl; source + unix_obtain in dry-run OS=linux HAVE_BINARY 0 then 1.
  expected: pins INSTALL_DIR=$HOME/.local/bin, pipes upstream install.sh, NO tar -xzf/unzip reimplementation; idempotent skip when >=floor.
  observed: unix_obtain pipes UPSTREAM_INSTALL with INSTALL_DIR="$DIR_POSIX" sh; NO tar/unzip anywhere (Windows uses Expand-Archive; Unix delegates); dry-run HAVE_BINARY=0 -> "would run curl upstream | INSTALL_DIR=... sh" + no-mutation shell-rc; HAVE_BINARY=1 -> "bw >= 0.13.2 present — skipping upstream install".
  result: pass
- probe_id: P7
  quadrant_classification: easy-easy
  description: install.sh absent-flag byte-unchanged (DC6/N4) across user|project|subproject + the no-target pre-flight-ordering probe; baseline from git show cfd683d7.
  command_or_method: agents/verification/stoa--elx/p7_byte_unchanged.sh — diff pre-arc vs built --dry-run per target (pre-arc staged in substrate/ so SCRIPT_DIR resolves identically; deployed_at runtime timestamp normalized); then `install.sh --bootstrap-bw --dry-run` with NO --target + real-key guard.
  expected: byte-identical --dry-run output all 3 targets; no-target errors on target-required with NO pre-flight fire and NO mutation.
  observed: all 3 targets BYTE-IDENTICAL after normalizing the inherent runtime deployed_at timestamp (user 220 / project 184 / subproject 78 lines; substrate_sha identical, only diff was the fresh wall-clock TS). No-target: exit 2, "install.sh: error: --target is required", NO [bootstrap-bw] line (pre-flight sits AFTER the target check), real-key sha256 unchanged.
  result: pass
- probe_id: P8
  quadrant_classification: easy-easy
  description: flag documented + wired — --help documents --bootstrap-bw; --target user --dry-run --bootstrap-bw delegates the pre-flight in dry-run with no mutation.
  command_or_method: bash install.sh --help | grep -- --bootstrap-bw ; bash install.sh --target user --dry-run --bootstrap-bw with real-key sha256 guard.
  expected: --bootstrap-bw present in --help; pre-flight delegates to helper in dry-run; no real-key mutation.
  observed: --bootstrap-bw present in both user+project usage lines; pre-flight emitted the [bootstrap-bw] dry-run plan (would download/verify/extract; win_path RESULT=present read-only; would verify DC3); exit0; real-key sha256 UNCHANGED (0951f1e4... before==after).
  result: pass
- probe_id: P9
  quadrant_classification: easy-easy
  description: standing regression bar — gen-data deterministic (worktree git diff empty) + FULL vitest suite green. Deps installed via npm ci first.
  command_or_method: cd app && npm ci ; npm run gen-data ; git -C <worktree-root> diff --quiet ; npm test (vitest run).
  expected: gen-data produces no worktree diff; full vitest suite green.
  observed: npm ci added 75 packages; gen-data discovered 16 role files, wrote agents.ts, `git diff --quiet` EMPTY (DETERMINISTIC — regen re-derived the whole roster, no change); vitest: 3 files, 41/41 tests PASSED (1.40s).
  result: pass
- probe_id: P10
  quadrant_classification: easy-easy
  description: authorship + §28.9 seat trailer (close-gate observation; NOMOS makes the final conformance call).
  command_or_method: git show -s b698f4ef Author+trailers ; grep author-like fields across the 4 changed files incl the new win_path.ps1.
  expected: Author = Denson Smith (not overridden); §28.9 seat trailer present; zero foreign author-like field.
  observed: Author=denson <densonsmith2@gmail.com>; trailers Co-Authored-By: CAPTAIN_ADA_the-stoa <captain-ada@the-stoa.local> + Claude Opus 4.8; new files carry "# Author: Denson Smith" (win_path.ps1:15, bootstrap-bw.sh:28); no frontmatter author in SKILL.md; all other grep hits are "authoring/agent-author" prose, NOT authorship-claim fields; install.sh diff added no author field. Zero foreign name in any author-like field.
  result: pass
threat_coverage:
- mitigation: M1
  threat: T-supply-chain-download-tamper
  defeats_via_probe: P5
  probe_evidence: agents/verification/stoa--elx/p5_sha256.sh recorded output (SHA256 OK->extract legit; MISMATCH->abort no-placement corrupt+wrong-hash)
  attack_path_exercised: drove the SHIPPED win_obtain against a byte-flipped zip AND a tampered checksums.txt — (a) attack blocked: both abort fail-closed exit1 with NO bw.exe placed; (b) legit unaffected: real upstream v0.13.2 asset verifies+extracts to `bw 0.13.2`.
- mitigation: M2
  threat: T-windows-user-path-clobber-truncation
  defeats_via_probe: P2
  probe_evidence: agents/verification/stoa--elx/p2_registry_safe.sh + p3_length.sh recorded output (throwaway key, SHIPPED win_path.ps1)
  attack_path_exercised: seeded read-failure (absent key), token-form entry, REG_EXPAND_SZ, and small-ceiling composed-breach — (a) attack blocked: fail_read exit3 NO overwrite, fail_length exit4 NO write, no double-append, type+token preserved, no setx/%PATH%; (b) legit unaffected: absent-dir seed appends exactly once. (P3 co-cites the length-gate no-write.)
- mitigation: M3
  threat: T-powershell-blind-false-green
  defeats_via_probe: P4
  probe_evidence: agents/verification/stoa--elx/p4_falsegreen.sh recorded output (naive exit0 / reassign-from-registry exit1)
  attack_path_exercised: planted a git-bash-PATH-visible stub ABSENT from the registry — (a) attack blocked: the shipped reassign-from-registry check exits 1 (does not see the git-bash-only stub); (b) legit/false-green demonstrated: naive check exits 0 on the same stub, proving the discrimination is live and the shipped form catches what the naive form would miss.
- mitigation: M4
  threat: T-fail-loud-not-firing
  defeats_via_probe: P3
  probe_evidence: agents/verification/stoa--elx/p3_length.sh + p2_registry_safe.sh recorded output
  attack_path_exercised: forced composed>ceiling and an absent-key read failure — (a) unsafe conditions FAIL LOUD: fail_length exit4 (P3) and fail_read exit3 (P2b) both non-zero with NO write; the bash wrapper routes rc 3/4/5 to fail_loud_path (exact GUI manual step, no write); (b) no silent green-over-unsafe-state observed.
methodology_concerns:
- P1 GROUND-TRUTH DRIFT (not a build defect): design-rev3 DC2.0 + P1 assert the reference machine has bw >= 0.13.2 present (idempotent full-skip as the reference path). The ACTUAL machine has bw 0.13.1 at C:\Users\denso\bin (below the 0.13.2 floor) and NO bw.exe at ~/.local/bin, so HAVE_BINARY=0 — which is CORRECT behavior (version_ge unit-verified). Consequence the arc team should note: the download-SKIP branch is not naturally exercised on this host; I covered it via the version_ge unit test + static branch trace + the P6 dry-run HAVE_BINARY=1 skip. The design's DC2.0 empirical "bw detected >= 0.13.2" claim is stale relative to current machine state.
- P4 FIXTURE DRIFT (not a build defect): FM condition 3 / design P4 specify the literal command name bw and expect the correct check to exit 1. This machine is now TRUE-green for bw — a bw.cmd shim at C:\Users\denso\.local\bin (first on the registry User PATH) makes the shipped verbatim-bw reassign-from-registry check exit 0, not 1 — the git-bash-green/registry-blind false-green state the design assumed no longer holds (the Grand's session shim fixed it). I demonstrated the exact reassign-from-registry DISCRIMINATION mechanism with an isolated unique-named stub (strictly cleaner isolation of the mechanism); only the fixture command name was adapted. The load-bearing property (naive false-green vs registry-reassign catch) holds.
- P6 COVERAGE BOUND (design weak point 6, inherited): a real linux/darwin end-to-end obtain is un-exercisable on this Windows host; P6 verified delegation-not-reimplementation via static trace + the shipped unix_obtain dry-run drive. Residual is test-coverage, not design.
falsifying_evidence_summary:
verification_artifacts_path: C:/Users/denso/claude_projects/the-stoa/.claude/worktrees/arc-75-build/agents/verification/stoa--elx/ (p1_idempotent.sh, p2_registry_safe.sh, p3_length.sh, p4_falsegreen.sh, p5_sha256.sh, p6_unix.sh, p7_byte_unchanged.sh)
summary: All 10 design probes executed against b698f4ef; verdict PASS. The load-bearing crux was exercised on the SHIPPED artifacts against the THROWAWAY registry key Environment_stoa_test: P2 drove win_path.ps1 through append-once / present-no-write / type+token preservation / F8 token-idempotent / F2 fail_read-exit3-no-write / no-forbidden-mechanism; P3 confirmed the composed-length gate fails loud (exit4, no write) and the wrapper routes 3/4/5 to fail_loud_path; P5 drove the shipped win_obtain to fail-closed on a byte-flipped zip and a tampered checksums.txt while verifying+extracting the real v0.13.2 asset; P4 proved the DC3 reassign-from-registry check discriminates a git-bash-only stub (naive exit0 false-green vs correct exit1). P7 confirmed install.sh --dry-run is byte-unchanged across all three targets (timestamp-normalized) with the pre-flight sitting after the --target check (no-target errors first, no mutation). P9 gen-data is deterministic and the full vitest suite is 41/41 green. HARD SAFETY HELD: the real HKCU\Environment Path is byte-identical before/after the entire run (KIND=String LEN=800 SHA256=0951f1e40b95b37998f598d7b0483be64f512eb6eabf0c05d4ac6b5a3abf1a99); both throwaway keys torn down (GONE=True). Two probe-premise-vs-machine-state drifts (P1 bw-below-floor, P4 machine-now-true-green) were worked around while still exercising the load-bearing mechanism; neither falsifies the build. All four FM 6 conditions and the design threat_coverage M1-M4 are satisfied with executed probes.
