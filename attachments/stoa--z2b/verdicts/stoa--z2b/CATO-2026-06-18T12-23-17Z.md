status: completed
ticket: stoa--z2b
verdict: pass
diff_reviewed: arc-65-build worktree (working-tree only, uncommitted) vs main 20267e4 — substrate/hooks/{_hooklib.sh, pretooluse-author-field-audit.sh, README.md} + byte-identical .claude/hooks/ mirrors + NEW untracked substrate/hooks/tests/ corpus
design_artifact_compared_against: agents/design/arc-65/design-rev2.md

# ============================================================================
# HEADLINE — CATO cold-read + meta-verification of VERA
# ============================================================================
# THE SECURITY PROPERTY HOLDS IN BOTH DIRECTIONS. I did not take this on VERAs
# assertion — I re-ran the corpus (16/16 green), independently re-derived every
# load-bearing claim (C4 single-def, OLD-name-removed, cfg byte-identity, mirror
# byte-identity, the P2 threat-coverage binding), AND drove adversarial attack
# shapes that NO fixture in the corpus covers. NO TRUE-POSITIVE LEAK EXISTS.
# The narrowing restricts only the .md SCAN WINDOW; it never relaxes the value
# check (is_principal) and never touches the config path.
#
# META-VERIFY VERA: SOUND. VERA drove the attack path (not the happy path), the
# P2 threat-anchored binding is real (defeats_via_probe:P2 in probes_executed,
# result pass), the live P5/P6 commits captured verbatim deny JSON, and the
# tp3-drift + separator-gap-pre-existing rulings hold up under my own re-run.
#
# VERDICT: pass. P10 is a landing/process matter (PLINYs seat), not a code
# defect — I concur it is not a security regression. One recommended-revision
# (documentation honesty note) + two follow-ups, none blocking.
# ============================================================================

concerns:
- id: c1
  category: coverage
  description: "The corpus tests/README.md does not carry the LICENSE separator-coverage honesty note that VERA and the floor-manager both recommended — a reader of the green corpus could misread it as proving ALL LICENSE author/copyright forms BLOCK, when the classic separator-less Copyright (c) <year> <name> form is a known un-matched gap (tracked stoa--y12)."
  evidence: "substrate/hooks/tests/README.md Why-this-exists section lists LICENSE/NOTICE/CITATION.cff TP coverage with no note that coverage is the field:value/field=value SEPARATOR form only; VERA-2026-06-18T12-12-35Z.md tp3-ruling (c) + POLYBIUS bw comment 2026-06-18T12:02:41Z both recommend the note; stoa--y12 tracks the gap."
  severity: recommended-revision
  quadrant_classification: easy-easy
- id: c2
  category: hygiene
  description: "tp5 (CITATION.cff) BLOCKs on the messy value family-names: Galsworthy rather than the clean person name — the line-anchored cfg regex catches the authors: key + YAML continuation. The BLOCK is real (a non-Denson value reaches the deny path, security holds) but the deny-message value text is ugly. This is PRE-EXISTING (cfg byte-identical to 20267e4), NOT introduced by Arc 65 — a minor follow-up, not a build concern."
  evidence: "VERA probes_executed[P2] observed line; my own run: classify(CITATION.cff)->cfg, cfg-extract emits authors<TAB>family-names: Galsworthy. cfg clean_one/emit block byte-identical to git show 20267e4:substrate/hooks/_hooklib.sh (diff empty)."
  severity: minor
  quadrant_classification: easy-easy

follow_ups:
- "stoa--y12 (already filed): the separator-less classic Copyright (c) <year> <name> LICENSE form is a true-positive MISS. CONFIRMED genuinely pre-existing by my own run: the OLD extractor at 20267e4 emits nothing on that form; Arc 65 cfg branch is byte-identical, so this arc did NOT widen the hole. Correctly out-of-scope for z2b (the arc narrows .md, does not touch separator semantics). Follow-up not block: the directive TP set is the structured field:value form, which IS covered."
- "CITATION.cff YAML-list value cleanliness (c2) — the deny value family-names: Galsworthy is ugly but functionally correct; a cleaner YAML-aware extraction for the authors:/family-names: list form is a polish item, separable from this security fix. Pre-existing, not an Arc-65 regression."
- "save-verdict --threat-coverage-probe-ids regex is ^p... (lowercase) while design probes are P1..P11 (uppercase) — VERA passed p2 to satisfy the mechanical check for an uppercase P2 probe-id. A small tooling casing-quirk worth a follow-up (POLYBIUS noted they will assess/file); the binding itself is sound."

verifier_coverage_assessment: |
  VERAs coverage is SOUND and COMPLETE for both directions of the load-bearing
  security property — and I verified this independently rather than re-reading
  VERAs claims.

  (1) ATTACK PATH GENUINELY DRIVEN (not happy-path-relabeled). P2 is the threat-
  anchored probe; it asserts BLOCK across EVERY covered class incl the two .md-
  suffixed config-class collisions tp6 (LICENSE.md) + tp7 (*.claude-plugin/*.md),
  AND asserts classify returns cfg (not md) for both — the exact carve-out that
  defeats the rev1 attack path. I re-ran the corpus: tp6 [rc=0 mode=cfg BLOCK] +
  tp7 [rc=0 mode=cfg BLOCK] reproduce. Live P5/P6a/P6b/P6c captured verbatim deny
  JSON against a REAL armed throwaway gate — the real runtime contract, not a stub.
  Both halves of op-disc 6.13 (attack-blocked + legit-unaffected) are observed.

  (2) THREAT-COVERAGE BINDING MECHANICALLY VALID (my 6.1 item-11 grep, seat-side
  not skill-enforced): defeats_via_probe:P2 — and grep probe_id: P2 in VERAs
  probes_executed = 1 occurrence with result: pass and a non-empty probe_evidence.
  The cited id IS in the executed set with real evidence. Binding holds.

  (3) tp3-DRIFT + SEPARATOR-GAP RULINGS HOLD under my own re-run. I diffed the cfg
  clean_one/emit/finditer block against 20267e4 (byte-identical) and the cfg regex
  pattern against the OLD pattern (same (?<![A-Za-z0-9_]) + slash-s[:=]slash-s form,
  char-for-char). The separator-less Copyright form emitting nothing is therefore a
  genuine OLD property Arc 65 did not change. VERAs pre-existing ruling is correct.

  (4) GAPS VERA DID NOT PROBE — I drove them myself, ALL route as designed:
    - md frontmatter with an INDENTED author key -> still emitted (^[ tab]* anchor)
      -> would BLOCK. Attacker cannot hide a wrong-person in nested-indent FM.
    - md frontmatter single-quoted value -> emitted -> BLOCK.
    - md UNCLOSED frontmatter -> fm regex fails -> text empty -> emits nothing
      (under-enforcement, but it is the Q-B delegated residual: a broken-FM docs body
      author line is out of mechanical scope by design, same as any body line — NOT a
      new hole; the OLD gate had no such doc class structured-covered either).
    - md =-separator in FM -> NOT matched (md uses : only) — documented design choice
      (design 2.3 note), narrows toward YAML-correctness, not a leak of a structured
      field the repo uses.
    - classify ordering under adversarial paths (.claude-plugin/foo.md;
      x.claude-plugin/sub/deep.md; LICENSE.txt; frontend/package.json) -> config arms
      WIN over the bare *.md arm in every case (cfg). The case-arm-ORDER carve-out
      (design weak-point 2) holds and is corpus-guarded (tp6/tp7 + the NOTICE.md->md
      guard go RED on any reorder).
    - value check NOT relaxed: a Denson FM value emits the pair (is_principal in the
      gate ALLOWs); md mode only narrows the scan window.
  None of these surfaced a true-positive leak. VERAs corpus + live re-probe set was
  sufficient; my extra shapes confirm the property is robust beyond the fixtures, not
  merely true on the chosen fixtures.

  (5) P10 FAIL is correctly classified by VERA as a landing/process defect, not a
  security regression. I concur: the design doc is itself a z2b false-positive (the
  NARROWED gate ALLOWs it — classify->md, no frontmatter, emits nothing); it is denied
  only by the OLD live-armed gate. That is the chicken-and-egg landing problem,
  PLINYs seat to resolve. NOT a code defect.

summary: |
  Arc 65 narrows the authorship gates .md matcher via a unified classify_author_file
  (membership + extraction-mode in ONE decision, config arms tested BEFORE the bare
  *.md arm) + a mode-aware extract_author_fields (md = leading-frontmatter-only YAML
  scan; cfg = byte-identical OLD whole-blob). The diff is tightly scoped, well-
  commented, and faithfully mirrored (substrate/hooks <-> .claude/hooks byte-identical
  x3). The build matches the design contract; nothing was built beyond design and
  nothing in the design was dropped (the tp3 fixture deviation is correctly scoped,
  documented, and ruled sound by ARGUS/VERA/POLYBIUS and re-confirmed by me).

  THE SECURITY PROPERTY HOLDS IN BOTH DIRECTIONS, verified independently: false-
  positive .md body prose now ALLOWs (corpus fp/ + my re-run) and a real structured
  non-PRINCIPAL author field STILL BLOCKs across every class incl the two collision
  classes (corpus tp/ + my adversarial shapes + VERAs live deny JSON). The narrowing
  touches only the SCAN WINDOW; is_principal is untouched; the cfg path is byte-
  identical to pre-Arc-65. I found NO true-positive leak, including in attack shapes
  no fixture covers (indented/quoted/unclosed frontmatter, config+.md collision
  paths, separator variants).

  Meta-verification of VERA: SOUND — VERA drove the attack path (not happy-path), the
  P2 threat-coverage binding is real and mechanically valid (defeats_via_probe P2 in
  probes_executed, result pass, non-empty evidence), the live probes used the real
  runtime contract, and the tp3/separator rulings hold under my own re-run. The lone
  VERA FAIL (P10) is a landing/process matter (PLINYs seat), correctly classified as
  NOT a security regression — I concur.

  OVERALL POSTURE: CLEAN. One recommended-revision (add the LICENSE separator-coverage
  honesty note to the corpus README so green is not misread as total LICENSE coverage)
  and two minor/pre-existing follow-ups (CITATION.cff value cleanliness; save-verdict
  probe-id casing quirk), none blocking. This arc can ship the code as-is; the
  recommended README note is a cheap honesty improvement best applied before merge or
  tracked in stoa--y12.
gap_or_blocker:
