status: completed
ticket: stoa--z2b
verdict: pass
design_artifact_audited: agents/design/arc-65/design-rev2.md
audit_block:
  risks: []
  non_findings:
  - "C1 CLOSES r1 (the load-bearing question) RULED YES. rev2 collapses author-encoding membership AND extraction mode into ONE classify_author_file in _hooklib.sh, deriving mode from the SAME case arms as membership in the SAME priority order. I transcribed the rev2 predicate (design 2.3 Change A) and the OLD is_author_encoding_file (gate lines 113-125) and ran both over a 24-path battery: NEW rc=0 IFF OLD-member yes for EVERY path (membership byte-preserved), AND LICENSE.md to mode cfg, .claude-plugin/docs/overview.md to mode cfg. End-to-end deny-set proof: a LICENSE.md BODY line Copyright: Fenwick Galsworthy emits (Copyright, Fenwick Galsworthy) under cfg (BLOCK) and emits NOTHING under md (the rev1 hole), so routing the collision classes to cfg is exactly what re-closes the regression. The .claude-plugin/*.md body Author line is the same: cfg BLOCKS, md would ALLOW. r1 is closed BY CONSTRUCTION: mode and membership cannot disagree because there is no second list."
  - "C2/r7 CLOSED RULED YES. tp6-license-md (intended LICENSE.md, body Copyright Fenwick Galsworthy, assert mode cfg + BLOCK) and tp7-claude-plugin-doc (intended .claude-plugin/docs/overview.md, body Author line, assert mode cfg + BLOCK) are both present in the 3.5 fixture list. These are the exact two collision classes r7 demanded. They go RED on the r1-reopen path: a reorder routing LICENSE.md to md would emit nothing, ALLOW, the fixture BLOCK expectation fails loud. The corpus can now PROVE both-directions-green for the classes r1 named."
  - "Carve-out boundary EXACTLY right (no over/under-correction) RULED YES. The collision SET is exactly the two classes I ruled at rev1: LICENSE.md + *.claude-plugin/*.md. Battery proof: NOTICE.md to mode md (NOT over-corrected to cfg; basename NOTICE.md not equal to literal NOTICE); package.json.md to mode md (correct, basename not literal). ctl3 (LICENSE.md body Copyright Denson Smith) to cfg to value Denson to ALLOW (proves cfg-routing does not OVER-block a legit copyright). NOTICE.md body author line to md, emits nothing, the accepted Q-B residual, identical OLD-vs-new behavior. The P3 NOTICE.md-to-md carve-out guard fixture pins the no-over-correction boundary; ctl3 pins the no-over-block boundary. Both boundary guards present."
  - "cfg-path byte-identity PRESERVED RULED YES (with one precision note in summary). I compiled the OLD cfg regex (_hooklib.sh:94-99) and the rev2 else/cfg-branch regex (design 194-199) and ran both over a battery (Copyright line, compact JSON author, TOML author=, maintainer, identifier-prefixed xauthor lookbehind, bold Authored-by, CITATION.cff authors list): MATCHING is byte-identical on every case. clean_one/emit/array-flatten are reused unchanged by both branches (design line 159). The cfg path did not drift; only the md path narrows."
  - "No NEW fail-open / default-mode gap RULED clean. (a) The caller mode-capture idiom propagates the function rc correctly under set -uo pipefail: a non-author-encoding file (rc 1) is SKIPPED exactly as the OLD is_author_encoding_file skip did, verified by running the exact idiom. (b) extract_author_fields MODE default-cfg fails toward MORE enforcement: the parameter-default substitutes cfg on BOTH unset AND empty arg (verified in bash) so even the impossible-by-construction empty-mode path runs the broadest whole-file scan, never a silent skip. (c) classify_author_file on an out-of-class path returns rc 1 + prints nothing, default-skip, fail-open preserved (P7 asserts this). No error path fails closed; no path silently drops a true-positive."
  - "Membership of ALL config classes preserved: package.json/pyproject.toml/Cargo.toml/setup.py/Gemfile/composer.json/plugin.json/marketplace.json/metadata.json/manifest.json/CITATION.cff/NOTICE/LICENSE/LICENSE.* all to rc0 mode cfg (battery-confirmed). notes.txt/src/foo.py to rc1 out-of-class. tp6-license-md.fixture to rc1 (basename ends .fixture, not LICENSE.md) so it commits clean through the OLD gate (r6 still sound)."
  - "RELOCATE (C4) correct and stronger than rev1, confirmed. rev2 relocates the UNIFIED classify_author_file (membership+mode), not just membership. The gate defines no classifier inline (Change C deletes the OLD function block). P11 asserts the classify_author_file definition count is 1 in _hooklib.sh and 0 in the gate, and is_author_encoding_file count is 0 across substrate/hooks/ (OLD name fully removed). These greps are well-formed against the rev2 source shape. My rev1 RELOCATE-over-source-guard ruling holds: a pure no-I/O function move, not a new early-return fail-surface in the live gate."
  threat_coverage_assessment: "M1 (a real structured author field naming a non-Denson PERSON slips the narrowed gate) now has a SOUND probe-SPEC binding at design time. The how-defeated claim (design 5) is now TRUE for the collision classes: I proved cfg whole-file scan is tested BEFORE the bare *.md arm, so LICENSE.md + *.claude-plugin/*.md are scanned whole-file and a body Copyright/Author non-Denson line is extracted and BLOCKED. The threat-anchored probe P2 exercises BOTH halves of 6.13: (a) attack-blocked across ALL classes incl tp6+tp7 with an explicit assert that classify_author_file returns cfg for tp6/tp7 (the exact carve-out that defeats the rev1 attack path); (b) legit-unaffected via P1 (body prose ALLOWS) + P3 control (ctl3 LICENSE.md-Denson cfg ALLOWS, NOTICE.md stays md). defeats_via_probe P2 is set. Tier-i (design-time, mine) probe-SPEC presence check: PASS, a threat-anchored probe is SPECd per the mapped mitigation, exercising the named attack path across every covered class incl both r1 collision classes. HONEST BOUNDARY: I read PRE-BUILD, so no EXECUTED probe exists yet; M1 has a PASSING threat-coverage binding only after VERA EXECUTES P2 (+ live P6 b/c) green. The design-time obligation, a specd threat-anchored probe per mapped mitigation, is now MET (it was NOT at rev1, where P2 omitted the collision classes). Threat-ENUMERATION completeness stays unmechanized judgment: M1 is the single named threat for this arc and is correctly the security frame; I do not represent coverage as proof of total threat-defeat."
summary: |
  VERDICT: RATIFY (verdict: pass). rev2 closes BOTH r1 and r7 cleanly, introduces no new gap, and gives M1 a sound design-time probe-SPEC binding. The design can LOCK.

  DOES rev2 CLOSE r1+r7? YES to both. The structural move, collapsing membership AND mode into one classify_author_file derived from the SAME case arms in the SAME priority order, closes r1 BY CONSTRUCTION: mode can never disagree with membership because there is no second list to drift. I did not take this on assertion; I transcribed the rev2 predicate and the OLD is_author_encoding_file and ran both over a 24-path battery (membership rc byte-preserved for every path), then ran an end-to-end deny-set proof showing the two collision classes (LICENSE.md, *.claude-plugin/*.md) emit a non-Denson value under cfg (BLOCK) and emit NOTHING under md (the rev1 hole), so cfg-routing is exactly the fix. r7 is closed by tp6 (LICENSE.md) + tp7 (.claude-plugin/*.md) fixtures, each asserting mode cfg + BLOCK; both go RED on any reorder that re-opens r1.

  CASE-ARM-ORDER RESIDUAL (the open question DAEDALUS asked me to rule): CORPUS-GUARD IS SUFFICIENT, I do NOT require an explicit is-config flag refactor. Reasoning: (1) the order-dependence is not NEW, the OLD is_author_encoding_file ALREADY depended on the identical case-arm order for membership (config arms above the bare *.md arm); rev2 inherits that exact ordering, it does not add fragility. (2) The regression is now MECHANICALLY caught, which the OLD code never had: tp6/tp7 go RED if any reorder routes the collision classes to md (md emits nothing, ALLOW, BLOCK-expectation fails loud), and the P3 NOTICE.md-to-md guard goes RED if the order over-corrects. A reorder cannot land green. (3) An explicit per-arm is-config flag is a larger refactor of a live SECURITY gate than this fix warrants, and would itself need the SAME corpus guard to prove it correct, the guard, not the flag, is the actual proof. The design correctly scopes the flag refactor OUT (section 7) with a reconsider-if-the-arms-grow trigger; that is the right call. So: corpus-guard-sufficient, ratified.

  M1 BINDING SOUND? YES at design time. The how-defeated claim is now TRUE for the collision classes (proven), P2 is threat-anchored across both halves of 6.13 and exercises both collision classes with an explicit mode-cfg assertion, defeats_via_probe P2 is set. The PASSING binding materializes once VERA executes P2 green, that is the correct pre-build tier-i ruling (a specd threat-anchored probe per mapped mitigation is the design-time obligation, and it is now met; it was NOT at rev1).

  NEW GAP CHECK: none found. Caller skip-rc propagates correctly (no fail-open regression); MODE default fails toward MORE enforcement on empty/unset; out-of-class returns fail-open default-skip as before. cfg path byte-identical with one precision note: the rev2 cfg-branch matches the OLD regex byte-for-byte in BEHAVIOR; the verbose-mode comment TEXT differs trivially (a separator comment), which is insignificant under re.X and does not perturb the deny-set. The VERA P11 deny-set assertion tests behavior, which is the correct test; the literal-source byte-identity claim should be read as behavioral/deny-set identity, not character-identity of the comment lines. Not a finding, flagged so VERA does not chase a non-existent char-diff.

  Two carried conditions for VERA (both already in the rev2 probe set, restated so they are not lost downstream): (1) execute P4/P5/P6 against a REAL armed throwaway gate, the runner mirrors the unchanged is_principal, so the LIVE probes are the backstop that exercises the real is_principal (the floor-manager note, which I endorse). (2) P11 deny-set byte-identity across the FULL corpus + classify_author_file defined exactly once + OLD name fully removed. Both are VERA execution obligations, not design gaps.

  POSTURE: clean. rev2 is a structurally superior fix to rev1 (closes r1 by construction rather than by a bolt-on carve-out), all five floor-manager conditions are folded, the carve-out boundary is exact in both directions, and no new gap is introduced. RATIFY, the design can lock.
gap_or_blocker:
