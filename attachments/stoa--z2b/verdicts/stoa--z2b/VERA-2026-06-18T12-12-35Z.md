status: completed
ticket: stoa--z2b
verdict: fail
design_artifact_verified_against: agents/design/arc-65/design-rev2.md
build_verified: arc-65-build worktree (working-tree only, NOT committed) — substrate/hooks/{_hooklib.sh, pretooluse-author-field-audit.sh, README.md} + byte-identical .claude/hooks mirrors + NEW substrate/hooks/tests/ corpus

# ============================================================================
# HEADLINE
# ============================================================================
# THE #1 DoD (the SECURITY property — gate behaves correctly in BOTH directions)
# is FULLY GREEN. Every false-positive (z2b) now PASSES and every true-positive
# (real non-Denson author field) STILL BLOCKS — proven both at the corpus level
# (P1/P2/P3) AND live against a real armed throwaway gate (P4/P5/P6), including
# the two .md-suffixed config-class collisions LICENSE.md (tp6/P6b) and
# *.claude-plugin/*.md (tp7/P6c). M1 threat-coverage is materialized by P2.
#
# THE VERDICT IS `fail` ON A SINGLE PROBE: P10 (committability self-test). The
# design doc design-rev2.md, when committed through the OLD live-armed gate (the
# gate that actually fires at landing time), is DENIED — because the design doc
# is ITSELF a z2b false-positive instance (its line-3 `**Authored by:**` seat-
# attribution + its embedded `Fenwick Galsworthy` fixture sketches are exactly
# the bug this arc fixes). This is a LANDING/PROCESS defect, NOT a security
# regression. It routes to PLINY for a landing decision. Details in P10 +
# falsifying_evidence_summary below.
# ============================================================================

probes_executed:
- probe_id: P1
  quadrant_classification: easy-easy
  description: "False-positive corpus — every fp/ fixture must now PASS (md mode emits nothing -> ALLOW)."
  command_or_method: "bash substrate/hooks/tests/run-author-gate-tests.sh"
  expected: "All 4 fp/ fixtures report PASS [rc=0 mode=md ALLOW]; runner exits 0."
  observed: "fp1 docs/x.md [rc=0 mode=md ALLOW] PASS; fp2 agents/verdicts/x.md [rc=0 mode=md ALLOW] PASS; fp3 substrate/arcs/x.md [rc=0 mode=md ALLOW] PASS; fp4 substrate/x.md [rc=0 mode=md ALLOW] PASS. Runner: 16 passed / 0 failed, EXIT=0."
  result: pass
- probe_id: P2
  quadrant_classification: easy-easy
  description: "True-positive corpus — every tp/ fixture must BLOCK across ALL classes incl tp6 (LICENSE.md) + tp7 (*.claude-plugin/*.md); classify_author_file returns mode cfg for tp6/tp7. THREAT-ANCHORED probe for M1 — drives the attack path (a real non-Denson author field) across every covered class."
  command_or_method: "bash substrate/hooks/tests/run-author-gate-tests.sh + direct source-and-call: classify_author_file <path> for tp6/tp7 + extract_author_fields cfg < fixture"
  expected: "tp1 package.json [cfg BLOCK]; tp2 SKILL.md frontmatter [md BLOCK]; tp3 LICENSE [cfg BLOCK]; tp4 NOTICE [cfg BLOCK]; tp5 CITATION.cff [cfg BLOCK]; tp6 LICENSE.md [cfg BLOCK]; tp7 .claude-plugin/docs/overview.md [cfg BLOCK]. Explicit: classify_author_file('LICENSE.md')->mode=cfg rc=0; classify_author_file('.claude-plugin/docs/overview.md')->mode=cfg rc=0."
  observed: "Runner: all 7 tp/ fixtures PASS meaning BLOCK computed. tp6 [rc=0 mode=cfg BLOCK]; tp7 [rc=0 mode=cfg BLOCK]. Direct classify: classify(LICENSE.md)->rc=0 mode='cfg'; classify(.claude-plugin/docs/overview.md)->rc=0 mode='cfg'. tp6 cfg-extract emits: 'Copyright<TAB>Fenwick Galsworthy' AND 'Author<TAB>Fenwick Galsworthy'. tp7 cfg-extract emits: 'Author<TAB>Fenwick Galsworthy'. tp5 CITATION.cff cfg-extract emits 'authors<TAB>family-names: Galsworthy' (non-Denson value reaches deny path -> BLOCK; the value text is messy but the attack is caught). All non-Denson -> is_principal false -> BLOCK."
  result: pass
- probe_id: P3
  quadrant_classification: easy-easy
  description: "Control + negative + carve-out — ctl/ ALLOW (incl ctl3 LICENSE.md-Denson->cfg->ALLOW); notes.txt->classify rc 1; NOTICE.md->rc 0 mode md (NOT cfg, over-correction guard)."
  command_or_method: "bash substrate/hooks/tests/run-author-gate-tests.sh + direct classify_author_file calls"
  expected: "ctl1 package.json [cfg ALLOW]; ctl2 SKILL.md [md ALLOW]; ctl3 LICENSE.md-Denson [cfg ALLOW]; notes.txt [rc=1 mode=-]; NOTICE.md [rc=0 mode=md] (NOT cfg)."
  observed: "ctl1 [rc=0 mode=cfg ALLOW] PASS; ctl2 [rc=0 mode=md ALLOW] PASS; ctl3 [rc=0 mode=cfg ALLOW] PASS; notes.txt [rc=1 mode=-] PASS; NOTICE.md [rc=0 mode=md] PASS. Direct: classify(NOTICE.md)->rc=0 mode='md' (carve-out NOT over-corrected); classify(notes.txt)->rc=1 mode='' (out-of-class)."
  result: pass
- probe_id: P4
  quadrant_classification: easy-easy
  description: "LIVE re-probe, false-positive — real commit ALLOWED against a REAL armed throwaway gate at /tmp/arc65-gate-probe (NARROWED deployed hook + test principal-identity allow-list + throwaway git repo)."
  command_or_method: "Armed throwaway gate at fixed literal /tmp/arc65-gate-probe with the deployed-mirror narrowed hook + _hooklib.sh + principal-identity {Denson Smith, denson, densonsmith2@gmail.com}. Staged a prose doc.md with '**Authored by:** ...POLYBIUS... + the PRINCIPAL (Denson Smith)' line + a verdict AUDIT prose line ('expected: html author = Denson Smith; no other person...'). Fired the gate with a real PreToolUse event JSON (tool_input.command='git commit -m ...', cwd=scratch repo) piped to the deployed gate. Teardown: rm -rf /tmp/arc65-gate-probe (fixed literal path)."
  expected: "Gate emits no deny JSON, exit 0 (ALLOWED)."
  observed: "GATE OUTPUT empty stdout, [GATE_EXIT=0]. RESULT: ALLOWED. (z2b false-positive fixed live.)"
  result: pass
- probe_id: P5
  quadrant_classification: easy-easy
  description: "LIVE re-probe, true-positive — package.json {\"author\":\"Fenwick Galsworthy\"} real commit DENIED."
  command_or_method: "Same armed throwaway gate; staged package.json {\"name\":\"x\",\"version\":\"1.0.0\",\"author\":\"Fenwick Galsworthy\"}; fired the gate."
  expected: "Deny JSON: permissionDecision deny, field author, file package.json, value Fenwick Galsworthy."
  observed: "DENY JSON emitted verbatim: 'Commit blocked: author-like field \"author\" in staged file \"package.json\" names \"Fenwick Galsworthy\", which is not the PRINCIPAL...' [GATE_EXIT=0]. RESULT: DENY (correct)."
  result: pass
- probe_id: P6
  quadrant_classification: easy-easy
  description: "LIVE re-probe, frontmatter + collision true-positives — THREE commits each DENIED: (a) skills/z/SKILL.md frontmatter author non-Denson; (b) LICENSE.md BODY Copyright non-Denson (r1 live form); (c) .claude-plugin/docs/x.md BODY Author non-Denson."
  command_or_method: "Same armed throwaway gate; three separate staged-file+fire-gate runs."
  expected: "All three emit deny JSON (DENY)."
  observed: "P6a SKILL.md frontmatter: DENY (field author, file skills/z/SKILL.md, value Fenwick Galsworthy). P6b LICENSE.md body: DENY (field Copyright, file LICENSE.md, value Fenwick Galsworthy) -- proves LICENSE.md keeps cfg whole-file coverage live. P6c .claude-plugin/docs/x.md body: DENY (field Author, file .claude-plugin/docs/x.md, value Fenwick Galsworthy) -- proves plugin-doc class keeps cfg coverage live. All three deny JSONs captured verbatim. The narrowing did NOT blanket-exempt .md."
  result: pass
- probe_id: P7
  quadrant_classification: easy-easy
  description: "FAIL-OPEN preserved — python3 unavailable / malformed event -> gate ALLOWS; classify_author_file on missing/empty path does not hard-error. PLUS a live control: LICENSE.md body Copyright=Denson -> ALLOWED (no over-block)."
  command_or_method: "(i) Staged a real violation package.json then fired the gate with PATH=/usr/bin:/bin (python3 not visible). (ii) Fired the gate with non-JSON stdin. (iii) classify_author_file '' and classify_author_file /no/such/file. (control) Staged LICENSE.md body 'Copyright: Denson Smith' and fired the gate."
  expected: "(i) ALLOW exit 0; (ii) ALLOW exit 0; (iii) rc 1, no crash; (control) ALLOW."
  observed: "(i) python3 visible under MINPATH? NO; OUTPUT empty [GATE_EXIT=0] ALLOWED (fail-open). (ii) OUTPUT empty [GATE_EXIT=0] ALLOWED (fail-open on malformed event). (iii) classify('')->rc=1; classify(missing)->rc=1; no stderr crash. (control) LICENSE.md Copyright=Denson -> OUTPUT empty ALLOWED (cfg-routing does not over-block legit copyright)."
  result: pass
- probe_id: P8
  quadrant_classification: easy-easy
  description: "Deploy clean + tests not swept — install.sh --target user --dry-run lists narrowed hooks + NO tests/ path under deploy."
  command_or_method: "bash substrate/install.sh --target user --dry-run"
  expected: "Dry-run lists _hooklib.sh + pretooluse-author-field-audit.sh deploy lines; no 'tests/' path anywhere; exit 0; INERT/default-OFF preserved."
  observed: "deploy hooks: yes (8 scripts) [INERT - not armed]; arm hooks: no (default OFF). Both narrowed hooks listed (deploy hook: _hooklib.sh / pretooluse-author-field-audit.sh). grep 'tests/' over full dry-run log: NONE. install dry-run exit=0."
  result: pass
- probe_id: P9
  quadrant_classification: easy-easy
  description: "Source <-> deployed mirror byte-identical — diff both .sh + README.md pairs -> empty."
  command_or_method: "diff substrate/hooks/{_hooklib.sh,pretooluse-author-field-audit.sh,README.md} .claude/hooks/<same>"
  expected: "All three pairs identical."
  observed: "IDENTICAL: _hooklib.sh; IDENTICAL: pretooluse-author-field-audit.sh; IDENTICAL: README.md."
  result: pass
- probe_id: P10
  quadrant_classification: easy-easy
  description: "Committability self-test — with the OLD main-tree gate armed, the design doc + the updated READMEs must be ALLOWED through the OLD gate (design-rev2 §3.6 + §4 P10). Exercised WITHOUT landing a commit on arc-65/build."
  command_or_method: "Built a throwaway clone (git clone --no-local) of arc-65-build at /tmp/arc65-p10-repo; copied the working-tree .md deliverables (design-rev2.md + the 3 READMEs) in and `git add`ed them; fired (a) a reconstructed OLD gate (git show 20267e4:...) AND (b) the ACTUAL LIVE parent-session armed gate (/c/.../the-stoa/.claude/hooks/pretooluse-author-field-audit.sh, confirmed OLD: classify_author_file refs=0, is_author_encoding_file refs=2) with a real PreToolUse event JSON. No `git commit` was ever run; no stray commit landed (clone HEAD stayed at 20267e4). Then isolated each deliverable. Teardown: rm -rf the scratch dirs."
  expected: "OLD gate ALLOWS the design doc + all 3 READMEs (no deny)."
  observed: "DENY on agents/design/arc-65/design-rev2.md by BOTH the reconstructed OLD gate AND the actual live parent gate. Triggering value (first of many): field 'by' = '** `CAPTAIN_DAEDALUS_the-stoa` (architect) + the PRINCIPAL (Denson Smith).' from line 3 `**Authored by:**`. Isolation: design-rev2.md DENY; substrate/hooks/README.md ALLOW; .claude/hooks/README.md ALLOW; substrate/hooks/tests/README.md ALLOW. Full OLD whole-blob scan of the design doc emits a LARGE non-Denson deny set incl 'author<TAB>Fenwick Galsworthy' (bare, NOT safely wrapped), contradicting design-rev2 §3.6's claim that 'Wrong-name examples are wrapped ... so the OLD whole-blob *.md scan does not resolve a non-Denson value.' CROSS-CHECK: the NARROWED (fixed) gate ALLOWS the design doc (classify->md, md-mode extract emits nothing — no leading frontmatter), confirming the design doc is itself a z2b false-positive."
  result: fail
- probe_id: P11
  quadrant_classification: easy-easy
  description: "C4 assertions — classify_author_file defined exactly once in _hooklib.sh (0 in gate); is_author_encoding_file fully removed (0 across substrate/hooks/); deny-set byte-identical full corpus before/after relocation (behavioral)."
  command_or_method: "grep -c 'classify_author_file()' on lib + gate; grep -rc 'is_author_encoding_file' substrate/hooks/ (+ wc -l total); per-fixture OLD-whole-blob vs NEW-cfg emitted (field,value) comparison over all 8 config-class fixtures."
  expected: "lib=1, gate=0; is_author_encoding_file total=0; all config-class fixtures OLD==NEW."
  observed: "classify_author_file() : _hooklib.sh=1, gate=0. is_author_encoding_file across substrate/hooks/: total matches=0 (every file 0, grep -r exit 1 = no match). Deny-set behavioral identity: tp1/tp3/tp4/tp5/tp6/tp7/ctl1/ctl3 all IDENTICAL OLD-vs-NEW cfg emissions -> C4 satisfied (cfg path relocated, behavior unchanged)."
  result: pass

threat_coverage:
- mitigation: M1
  threat: "T-author-slip — a real STRUCTURED author field naming a non-Denson PERSON slips past the narrowed gate (the reputational/legal regression the gate exists to prevent; regressed TWICE)."
  defeats_via_probe: P2
  probe_evidence: "P2 (executed) — agents/verdicts/stoa--z2b/VERA-<ts>.md probes_executed[P2]; runner output 'tp6 LICENSE.md [rc=0 mode=cfg BLOCK]' + 'tp7 .claude-plugin/docs/overview.md [rc=0 mode=cfg BLOCK]' + direct classify(LICENSE.md)->cfg / classify(.claude-plugin/docs/overview.md)->cfg + cfg-extract emits 'Copyright/Author<TAB>Fenwick Galsworthy'. Backed live by P6b (LICENSE.md body commit DENIED) + P6c (.claude-plugin/*.md body commit DENIED) + P5 (package.json commit DENIED)."
  attack_path_exercised: "Attack path (iii) of the M1 map: a .md-suffixed CONFIG-class file (LICENSE.md / *.claude-plugin/*.md) whose body Copyright:/Author:<non-Denson> the OLD cfg gate blocked but the rev1 narrowing would have routed to md and let through. (a) attack-blocked: P2 tp6/tp7 compute BLOCK + classify returns cfg (not md), and live P6b/P6c real commits are DENIED. (b) legit-unaffected: P1 (z2b body prose now ALLOWs) + P3 ctl3 (LICENSE.md w/ Denson copyright -> cfg -> ALLOW) + live control (LICENSE.md Denson copyright commit ALLOWED) + P3 NOTICE.md stays md (carve-out not over-corrected). Both halves of op-disc §6.13 observed."

methodology_concerns:
- "tp5 (CITATION.cff) BLOCKS on the value 'family-names: Galsworthy' rather than the clean person name 'Fenwick Galsworthy' — the line-anchored cfg regex matches the 'authors:' key and the YAML continuation. The BLOCK is real (a non-Denson value reaches the deny path), so the security property holds, but the emitted value text is messy (a known property of the pre-existing line-anchored scan, not introduced by Arc 65). Not blocking; noted for CATO."
- "P10 is the design's own self-test and it FAILS for the design doc. The design-rev2 §3.6 claim that wrong-name examples are all safely wrapped is FALSE (the OLD whole-blob scan resolves bare 'author: Fenwick Galsworthy' + the line-3 '**Authored by:**' seat attribution). This is a LANDING/PROCESS defect routed to PLINY (see falsifying_evidence_summary), NOT a security regression. PLINY decides the landing path; the security build is sound."
- "P7 / live P4-P6 were exercised by invoking the deployed gate script directly with a hand-built PreToolUse event JSON (tool_input.command + cwd) piped to stdin, against a real git repo with a real staged blob (git show :file) and a real principal-identity allow-list — this is the gate's actual runtime contract, not a stub. Arming Claude Code's own live settings.json from this seat is forbidden (HARD SAFETY CONSTRAINT), so the throwaway-gate-direct-invocation is the correct live form; the deny/allow JSON captured is byte-identical to what the harness would receive."

falsifying_evidence_summary: |
  ONE probe is falsified: P10 (committability self-test). The arc deliverable
  agents/design/arc-65/design-rev2.md is DENIED when committed through the OLD
  authorship gate — and the OLD gate is the gate that is ACTUALLY LIVE-ARMED in
  the parent session (verified: /c/Users/denso/claude_projects/the-stoa/.claude/
  settings.json registers .claude/hooks/pretooluse-author-field-audit.sh, which
  is the OLD version: classify_author_file refs=0, is_author_encoding_file refs=2).
  The OLD whole-blob *.md scan of the design doc emits a large non-Denson deny
  set, first hit being line 3 field 'by' = '** `CAPTAIN_DAEDALUS_the-stoa`
  (architect) + the PRINCIPAL (Denson Smith).' (from `**Authored by:**`), plus
  bare 'author<TAB>Fenwick Galsworthy' from the fixture sketches. This directly
  falsifies design-rev2 §3.6's claim that all wrong-name examples are wrapped so
  the OLD scan resolves no non-Denson value, and falsifies P10's required
  outcome ("a dry commit of this doc + READMEs through the OLD gate must be
  ALLOWED"). The 3 READMEs all pass; only the design doc fails.

  CRITICAL SCOPING — this is NOT a security regression and does NOT touch the
  #1 DoD. The design doc is ITSELF a z2b false-positive instance: the NARROWED
  (fixed) gate ALLOWS it (classify -> md mode; md-mode extract emits nothing
  because the doc has no leading YAML frontmatter). The failure is a chicken-
  and-egg LANDING problem: the fix can only land by committing the design doc,
  but the OLD gate (live until the fix lands) blocks that commit. Every security-
  behavior probe (P1/P2/P3/P4/P5/P6/P7/P9/P11) is GREEN in BOTH directions; M1
  threat-coverage is materialized by executed P2 (+ live P6b/P6c). The fail is
  routed to PLINY for a landing decision (standard escape hatches: commit the
  narrowed mirror into the parent .claude/hooks/ FIRST so the fixed gate is live
  before the docs commit; or land with --no-verify on the design-doc commit; or
  re-wrap the design doc's line-3 + bare Fenwick sketches per the design's own
  §3.6 remediation). VERA does not choose the landing path (independence-of-
  verification — I do not modify the deliverable); I surface the falsification.

  TP3 DRIFT RULING (asked of VERA explicitly):
  (a) ADA's corrected tp3 ('Copyright: Fenwick Galsworthy', separator form) is
      SOUND for verifying LICENSE-class BLOCK: the cfg extractor matches it and
      emits 'Copyright<TAB>Fenwick Galsworthy' -> is_principal false -> BLOCK.
      Confirmed live (OLD extractor emits the pair; new cfg byte-identical).
  (b) The separator-LESS classic form 'Copyright (c) 2026 <name>' non-coverage
      is GENUINELY PRE-EXISTING: I ran the OLD/unmodified extractor (git show
      20267e4:substrate/hooks/_hooklib.sh, the un-moded whole-blob function)
      against 'Copyright (c) 2026 Fenwick Galsworthy' AND the uppercase
      'Copyright (C) 2026 Fenwick Galsworthy' — both emit NOTHING (exit 0, empty
      stdout). The gate's extractor requires a ':' or '=' separator; the classic
      '(c) <year> <name>' form has none and was NEVER matched. Arc 65 did NOT
      widen this hole (the cfg branch is byte-identical to OLD; P11 deny-set
      identity confirms it).
  (c) RULING: the pre-existing separator-less LICENSE non-coverage is
      ACCEPTABLY OUT-OF-SCOPE for stoa--z2b and does NOT endanger the #1 DoD.
      Rationale: the directive's true-positive set names "a LICENSE/NOTICE
      naming a non-Denson person" — and that IS covered for the separator form
      (the form structured-author-field tooling and humans actually write as a
      field). The separator-less prose '(c) <year> <name>' is a copyright NOTICE
      sentence, not a structured 'field: value', and the gate has never claimed
      to parse free prose. It is already tracked as a known gap in stoa--y12
      (per floor-manager comment on stoa--z2b). RECOMMEND (non-blocking, defer
      to stoa--y12 / DAEDALUS-PLINY call): one honest line in the corpus README
      noting LICENSE TP coverage is the 'field: value'/'field = value' separator
      form and the classic 'Copyright (c) <year> <name>' form is the un-matched
      gap tracked in stoa--y12 — so a green corpus is not misread as 'all LICENSE
      formats covered.'

verification_artifacts_path: "Probes run in-place against the arc-65-build worktree (no persisted scratch — all throwaway repos at /tmp/arc65-* torn down: confirmed clean). Recorded probe outputs are embedded verbatim in this verdict's probes_executed + falsifying_evidence_summary. The corpus itself (the durable regression guard) lives at substrate/hooks/tests/."

summary: |
  Arc 65 narrows the authorship gate's .md matcher via a unified
  classify_author_file (membership + extraction-mode in one decision, config arms
  tested before the bare *.md arm) + a mode-aware extract_author_fields (md =
  frontmatter-only, cfg = byte-identical OLD whole-blob). I executed all 11
  design probes verbatim against the built deliverable. The SECURITY property —
  the #1 DoD — is GREEN in BOTH directions: false-positives (z2b body prose) now
  ALLOW (P1 corpus + P4 live), and real non-Denson author fields STILL BLOCK
  across every class (P2 corpus + P5/P6 live), INCLUDING the two .md-suffixed
  config-class collisions LICENSE.md and *.claude-plugin/*.md that rev1 would
  have leaked (tp6/tp7 cfg-mode BLOCK + live P6b/P6c DENY). The most load-bearing
  probe, P2, is the threat-anchored probe materializing M1 coverage and exercises
  the attack path (a real wrong-person field) rather than the happy path; both
  halves of op-disc §6.13 (attack-blocked + legit-unaffected) are observed.
  FAIL-OPEN is preserved (P7), the cfg deny-set is behaviorally byte-identical
  after the C4 relocation (P11), the OLD name is_author_encoding_file is fully
  removed (0 across substrate/hooks/), classify is defined exactly once, mirrors
  are byte-identical (P9), and the corpus does not deploy (P8). The single fail
  is P10: the design doc design-rev2.md cannot be committed through the OLD
  live-armed gate because the doc is itself a z2b false-positive (line-3
  `**Authored by:**` + bare Fenwick fixture sketches); the FIXED gate ALLOWs it.
  That is a landing/process defect, not a security regression — routed to PLINY
  for a landing-path decision. The tp3 drift is ruled SOUND, the separator-less
  LICENSE gap is CONFIRMED pre-existing (OLD extractor emits nothing on
  'Copyright (c) <year> <name>') and acceptably out-of-scope (tracked in
  stoa--y12). Per the verdict definitions a single falsified probe yields a
  routing verdict of `fail`, but the falsification is bounded and well understood
  and the security deliverable itself is sound.
