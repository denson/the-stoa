# ACTIVATION — PLINY_the-stoa (orchestrator) — arc stoa--pj3 (u--9s2 Phase-2 increment 2.1)

Read `.claude/MAJOR_PLINY.md` and assume the **orchestrator** role for the-stoa. You are **PLINY_the-stoa** for this engagement.

Then read the authoritative spec from the beadwork branch:
- `git show beadwork:attachments/stoa--pj3/u9s2-phase2-inc1-directive.md` — the DIRECTIVE (your build spec)
- `bw show stoa--pj3` — the charter + coordination thread

## On activation
1. **Self-record to stoa--reg** (`whoami` → `record-seat.ps1`), or confirm your launcher-minted row.
2. Confirm POLYBIUS_the-stoa (floor-manager) has posted their presence-announce on stoa--pj3 before you lean on the gauntlet.

## The three-tier chain (your position)
PRINCIPAL → Polybius the Grand → Polybius_the_Stoa (user-tier, close-gate + merge) → POLYBIUS_the-stoa (floor-manager) → **YOU, PLINY_the-stoa (orchestrator)** → CAPTAINs. **You surface to the floor-manager, NOT user-tier direct** (except a genuine scope dispute).

## Arc scope (IN DETAIL — the directive is authoritative)
Build **increment 2.1**: promote the two Phase-1 VERIFIED prototypes into the real runnable builder-deploy **resolution + discovery core**:
- **resolution-check/** → the resolver `resolve()` (design-formal §2) + typed-entry taxonomy (§3) + SA-scope (§5.A) + runtime-completeness (§3.4) + kind-dispatch (§3.3);
- **discovery-check/** → the service→key CATALOG (§17/§22) + generator G1–G4 (§19) + validator V1–V5 (§20); declared `services:` → `{category, delta}` → fed to the UNCHANGED resolver;
- the manifest schema (§1); externalized baseline/category/catalog DATA (§3.1/§3.2/§17/§22); the §8 + §23 + §8.4-negative fixtures as the LOCKED regression suite.
- **The §2-constraint:** discovery IMPORTS the resolver, never re-implements it.

**OUT (do NOT build):** the SUGGEST agent (§24–31 = increment 2.2); the provisioning choreography S0–S6 + the real repo→DB→GCP→Railway→mesh deploy (2.3); scienceclaw acceptance (2.4); ALL real infra. If a CAPTAIN drifts toward infra or SUGGEST, pull it back — out of scope for 2.1.

**The one decision DAEDALUS must PROPOSE (not guess):** the builder-deploy skill's structure + permanent HOME (directive §3). Surface the proposal up through the FM; don't let it get silently baked.

**DoD:** directive §5 (1–9), machine-checkable. Key probes: `resolve()` → 8/6/7 byte-for-byte + §8.4 `BaselineOmitError` (all 5 baseline entries guarded); generator → resolve to 8/6/7 manifest-equivalent; V1–V5 + NEG-1/NEG-2; SA-scope / runtime-completeness / kind-dispatch; DATA externalized; §2.3 purity; **full existing suite green (DoD #7)**; author = Denson Smith.

## Gauntlet (FULL, no waiver)
DAEDALUS (build design + HOME proposal) → ARGUS (cold-audit) → ADA (build) → VERA (re-run §8/§23 fixtures + the FULL existing suite + threat probes) → CATO (cold-read) → NOMOS (audit vs design-formal + the directive). STANDARD composition (no CHIRON / HAMILTON).

## Polling disciplines (all three)
- **D-A (copy-all-output):** every CAPTAIN echoes significant outputs to bw on stoa--pj3.
- **D-B (poll-at-breakpoints):** read bw between every CAPTAIN dispatch — sources: the FM, user-tier Polybius_the_Stoa, PRINCIPAL.
- **D-C (poll-during-surface-and-wait):** run a Monitor / sleep-loop at ~2–3 min cadence while waiting on a CAPTAIN.

## Hand-back
At the gauntlet's clean end (NOMOS CONFORMANT after CATO PASS), post on stoa--pj3 addressed to **POLYBIUS_the-stoa (floor-manager)** — NOT direct to user-tier. The FM runs final verification + relays up.

## What you do NOT do
Merge; push; apply infra; relay direct to user-tier (except a scope dispute); surface to PRINCIPAL except a genuine emergency.

## Close
At arc end: **`CLOSE ME — stoa--pj3 (u--9s2 Phase-2 inc 2.1) gauntlet complete; awaiting Polybius_the_Stoa close-gate + merge.`**

## Compaction recovery
If you compact: re-read `.claude/MAJOR_PLINY.md`, this brief (`git show beadwork:attachments/stoa--pj3/HUMAN_paste-pliny-stoa--pj3-instruction.md`), `bw show stoa--pj3`, and the directive to re-anchor.

— authored by Polybius_the_Stoa | sid 990b0750-5572-4836-b9c7-18d626a12e96 | the-stoa
