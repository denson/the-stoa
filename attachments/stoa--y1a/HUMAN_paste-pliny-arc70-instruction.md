Read `.claude/MAJOR_PLINY.md` and assume the orchestrator role for the-stoa. You are **PLINY_the-stoa** for Arc 70.

Open in `C:\Users\denso\claude_projects\the-stoa\`. Run `bw prime` at activation.

**DOGFOOD turn one:** discover your sid via the `whoami` skill (`$CLAUDE_CODE_SESSION_ID`); sign every bw comment `[from: PLINY_the-stoa | sid <your-sid>]`.

**The three-tier chain (your position):** PRINCIPAL → user-tier Polybius_the_Stoa (owns arc; close-gate + merge) → **POLYBIUS_the-stoa (floor-manager)** → **you, PLINY_the-stoa** (run the gauntlet). You take direction from + surface to **POLYBIUS_the-stoa (the floor-manager) — NOT user-tier directly, and NOT the PRINCIPAL.**

**The spec (read it FULLY):** the directive at `substrate/arcs/arc-70-build-directive.md` (committed `dd70b86`, NOMOS-CONFORMANT). Charter: **`stoa--y1a`**. Design input: **`docs/self-correction-doctrine-DRAFT.md`** (the full doctrine — read it; §2 is the "wire triggers, not a dilemma-detector" stance, §3 lock-spine/free-tact, §4 the diagnostic tree).

**The build (SLICE 1 — the detection front):**
1. a composable dilemma-detection **CLASSIFIER module** — problem-vs-dilemma check + plain-language explanation + route rule + lock-spine/free-tact delivery discipline;
2. **two DETERMINISTIC triggers ONLY** — (a) explicit "detect dilemma" call (+ a small synonym set), (b) the identified workflow/agent checkpoints (DAEDALUS scopes the slice-1 subset);
3. a **both-directions SEED CORPUS** — problem / dilemma / **camouflaged-dilemma** fixtures + a runner, mirroring the author-gate fixture pattern at `substrate/hooks/tests/`.

**Standard team — no CHIRON/HAMILTON** (it's a module + triggers + corpus, not a custom agent/workflow).

**Phase A (DAEDALUS) resolves DC0–DC5** (see directive): module form + composition homes (DC0); explicit-trigger mechanism (DC1); the slice-1 checkpoint subset (DC2); corpus structure + floor (DC3); **DC4 lock-spine/free-tact — the LOAD-BEARING design risk (so a sycophancy-trained consumer cannot collapse the spine)**; DC5 plain-language delivery. Surface the design to the floor-manager for a go/no-go BEFORE build.

**OUT OF SCOPE (do NOT fold in — named follow-on arcs):** the meta-trigger auto-detect-circling counter; the bw decision-register + complaint-callback + re-verify gate (arc 2); graduating decision-surface from DRAFT (`stoa--ida`); the broader checkpoint rollout.

**Run the full gauntlet** (DAEDALUS → ARGUS → ADA → VERA → CATO → NOMOS).

**Polling disciplines (run all three):**
- **D-A (bw-copy-all-output):** every CAPTAIN echoes significant outputs to bw on `stoa--y1a`.
- **D-B (polling-at-breakpoints):** read bw between every CAPTAIN dispatch.
- **D-C (polling-during-surface-and-wait):** Monitor during surface-and-wait at ~2–3 min cadence.

**Hand-back protocol:** at CATO PASS, post on `stoa--y1a` addressed to **POLYBIUS_the-stoa (floor-manager)** — NOT direct to user-tier. The floor-manager runs final verification + relays up.

**What you do NOT do:** merge; push; relay direct to user-tier (except a genuine scope dispute); surface to the PRINCIPAL except emergencies. You dispatch the gauntlet CAPTAINs — that IS your seat — but you do NOT run solo with one self-chosen checker (the AR-7 failure).

**Close-signal at arc end:** `CLOSE ME — arc 70 gauntlet complete; awaiting user-tier Polybius_the_Stoa close-gate + merge`.

**Compaction recovery:** re-orient from this brief — `git show beadwork:attachments/stoa--y1a/HUMAN_paste-pliny-arc70-instruction.md` — plus `bw show stoa--y1a` and the directive.
