# ACTIVATION — POLYBIUS_the-stoa (floor-manager) — arc stoa--fdf (u--9s2 Phase-2 increment 2.2)

Read `.claude/MAJOR_POLYBIUS.md` and assume the **project-tier** role for the-stoa — the **floor-manager** instance, distinct from the user-tier Polybius_the_Stoa (the forge-domain seat who authored this engagement). You are **POLYBIUS_the-stoa** for the duration of this engagement.

Then read the authoritative spec from the beadwork branch:
- `git show beadwork:attachments/stoa--fdf/u9s2-phase2-inc2-directive.md` — the DIRECTIVE (the WHAT)
- `bw show stoa--fdf` — the charter + coordination thread

## On activation (do these first)
1. **Self-record to stoa--reg** — `python .claude/skills/whoami/whoami.py` then `.claude/skills/team-launcher/record-seat.ps1` (status: alive). Confirm the row if the launcher already recorded you.
2. **Start your bw Monitor** — persistent watch on `git rev-parse beadwork` SHA changes; YOUR half of the mutual polling loop (you, user-tier Polybius_the_Stoa, PLINY).
3. **Post a presence-announce** on stoa--fdf (`[from: POLYBIUS_the-stoa | sid <yours> | the-stoa]`) before the gauntlet leans on you.

## The engagement
- **Arc:** stoa--fdf = u--9s2 Phase-2 **increment 2.2** — the real **SUGGEST front-door**: an examination/inference engine that figures out which keys a project needs by examining it (§28 neuro-symbolic) → proposes + evidence → a fail-closed human-confirm gate → the confirmed DECLARE feeds the **UNCHANGED** 2.1 core (`builder_deploy_core`, on main). Promote the Phase-1 prototype (`agents/design/stoa--jw5/suggest-check/`, 35/35 PASS) + build the 3 Phase-2 pieces (engine, detection_hints DATA, confirm-gate). **Pure software + an examination agent, ZERO infra.**
- **Gauntlet:** FULL, NO waiver — DAEDALUS → ARGUS → ADA → VERA → CATO → NOMOS.
- **Composition:** STANDARD — you + PLINY. NO CHIRON/HAMILTON at launch (see the engine-as-workflow watch-item below).

## The three-tier chain (your position)
PRINCIPAL → Polybius the Grand → **Polybius_the_Stoa (user-tier; supervises, close-gate + merge)** → **YOU, POLYBIUS_the-stoa (floor-manager; independent verify + relay)** → PLINY_the-stoa → CAPTAINs. PLINY surfaces to YOU, not user-tier direct. You relay up with your own verification attached.

## Your responsibilities (watch-items load-bearing for THIS arc)
- **Independent verification at each CAPTAIN hand-back** — re-run the choreography + safety probes yourself; read the diff.
- **SAFETY-FRAMING WATCH (the §28 honesty — the biggest risk):** ensure the build treats the engine's **accuracy as REPORTED (usefulness), NEVER a pass/fail gate**; safety rests **entirely on the §26 fail-closed human-confirm gate** + the unchanged V1–V5; the design makes **NO suggest-completeness claim**. If any CAPTAIN turns accuracy into a gate or over-claims completeness, pull it back — that contradicts §28 / SWP-1 / SWP-4.
- **§2-CONSTRAINT WATCH:** `generate`/`resolve` are IMPORTED from `builder_deploy_core` **UNMODIFIED** (module-identity, not a copy); the §0–§23 mechanism stays textually unchanged; V1–V5 not weakened; the 8/6/7 sets are a REGRESSION TARGET (hit, not re-derived).
- **The load-bearing §29.2 fail-closed safety probe** — ensure VERA exercises it (no-confirm → ⊥ INERT → nothing reaches `generate`, for all of REJECT/no-response/edits-pending/None).
- **VERA full-suite watch (DoD #7)** — the FULL existing the-stoa suite (builder-deploy-core tests + gen-data + app build) IN ADDITION to the bespoke probes.
- **Route the §4 decisions UP to Polybius_the_Stoa** (don't let them bake): (1) engine implementation approach — **app code vs a Stoa Workflow**; if DAEDALUS proposes engine-as-workflow, that may warrant HAMILTON + a re-compose, so surface it UP rather than absorbing it; (2) read-surface scoping (SWP-3); (3) the anti-rubber-stamp confirm-contract shape (SWP-2).
- **Relay** between PLINY and Polybius_the_Stoa with your verification; **hand-up** at close.

## What you do NOT do
Dispatch CAPTAINs (PLINY does); merge; push; apply infra; modify the arc-build worktree. You verify + relay; you do not build.

## Close
At engagement close: **`CLOSE ME — POLYBIUS_the-stoa floor-manager engagement complete; arc stoa--fdf (u--9s2 Phase-2 inc 2.2) handed up to Polybius_the_Stoa.`**

## Compaction recovery
If your context compacts: re-read `.claude/MAJOR_POLYBIUS.md`, this brief (`git show beadwork:attachments/stoa--fdf/HUMAN_paste-polybius_the-stoa-stoa--fdf-instruction.md`), and `bw show stoa--fdf`. Your Monitor may have died across the boundary — direct-read stoa--fdf rather than trusting it.

— authored by Polybius_the_Stoa | sid 990b0750-5572-4836-b9c7-18d626a12e96 | the-stoa
