# ACTIVATION — PLINY_the-stoa (orchestrator) — arc stoa--fdf (u--9s2 Phase-2 increment 2.2)

Read `.claude/MAJOR_PLINY.md` and assume the **orchestrator** role for the-stoa. You are **PLINY_the-stoa** for this engagement.

Then read the authoritative spec from the beadwork branch:
- `git show beadwork:attachments/stoa--fdf/u9s2-phase2-inc2-directive.md` — the DIRECTIVE (your build spec)
- `bw show stoa--fdf` — the charter + coordination thread

## On activation
1. **Self-record to stoa--reg** (`whoami` → `record-seat.ps1`), or confirm your launcher-minted row.
2. Confirm POLYBIUS_the-stoa (floor-manager) has posted their presence-announce before you lean on the gauntlet.

## The three-tier chain (your position)
PRINCIPAL → Polybius the Grand → Polybius_the_Stoa (user-tier, close-gate + merge) → POLYBIUS_the-stoa (floor-manager) → **YOU, PLINY_the-stoa (orchestrator)** → CAPTAINs. **You surface to the floor-manager, NOT user-tier direct** (except a genuine scope dispute).

## Arc scope (IN DETAIL — the directive is authoritative)
Build the real **SUGGEST front-door** (design-formal §24–§31), strictly UPSTREAM of DECLARE. Promote the Phase-1 VERIFIED prototype (`agents/design/stoa--jw5/suggest-check/`, 35/35 PASS) into runnable code + build the 3 Phase-2 pieces:
- **The examination/inference engine** (§28 neuro-symbolic: parse config/manifests + AST outbound-call scan + LLM dynamic-endpoint resolution + README/OpenAPI reconcile → extract the 4 signal surfaces → match §25 `detection_hints` → propose + per-candidate evidence; recommendation-only, INERT);
- **The `detection_hints` catalog DATA** (§25.3 Phase-2 fill);
- **The confirm-gate** (§26 fail-closed `D := confirm(P)`) with an **anti-rubber-stamp, evidence-rich confirm-contract** (present each proposed service WITH its cited evidence — reasons, not a blind yes/no).
- The confirmed DECLARE feeds the **UNCHANGED** 2.1 core (`builder_deploy_core` generate→resolve), regression target **8/6/7**. The §29.1 worked-example **project fixtures** are the machine-checkable surface for the engine.

**THE SAFETY FRAMING (load-bearing, §28):** the engine's **accuracy is USEFULNESS, NOT safety.** Safety rests ENTIRELY on the §26 fail-closed gate + the unchanged V1–V5. Make **NO suggest-completeness claim** and do **NOT gate the arc on an accuracy number**. The engine's accuracy is REPORTED (DoD #9, run against scienceclaw's real repo), not gated.

**THE §2-CONSTRAINT:** `generate`/`resolve` IMPORTED from `builder_deploy_core` UNMODIFIED — never reimplemented/edited; §0–§23 textually unchanged; V1–V5 not weakened.

**OUT (do NOT build):** the provisioning choreography S0–S6 + real deploy (2.3); scienceclaw end-to-end stand-up (2.4); ALL real infra.

**The §4 decisions DAEDALUS must PROPOSE (not guess), surfaced UP through the FM:** (1) engine impl approach — **app code vs a Stoa Workflow** (if engine-as-workflow, it may warrant HAMILTON + a re-compose — surface up, do not bake); (2) read-surface scoping (SWP-3); (3) the anti-rubber-stamp confirm-contract shape (SWP-2) as a Phase-1-named contract.

**DoD:** directive §5. GATED (machine-checkable): choreography → 8/6/7; the §29.2 fail-closed no-confirm branch → ⊥ INERT (all four no-confirm actions) + over/under-proposal caught; §2-constraint (import unmodified); confirm-gate evidence-rich contract; detection_hints DATA + P3 provenance; engine runs on the project fixtures; full existing suite green; author=Denson Smith. REPORTED (NOT gated): the engine's accuracy baseline on scienceclaw's repo.

## Gauntlet (FULL, no waiver)
DAEDALUS (build design + the §4 proposals) → ARGUS (cold-audit; confirm SWP-1..4, esp. "safety = the gate, not the engine's accuracy") → ADA (build) → VERA (re-run choreography + the load-bearing §29.2 fail-closed safety probe + over/under-proposal + the FULL existing suite) → CATO (cold-read) → NOMOS (audit vs design-formal §24–31 + the directive). STANDARD composition.

## Polling disciplines (all three)
- **D-A (copy-all-output):** every CAPTAIN echoes significant outputs to bw on stoa--fdf.
- **D-B (poll-at-breakpoints):** read bw between every CAPTAIN dispatch — sources: the FM, user-tier Polybius_the_Stoa, PRINCIPAL.
- **D-C (poll-during-surface-and-wait):** Monitor / sleep-loop at ~2–3 min cadence while waiting on a CAPTAIN.

## Hand-back
At the gauntlet's clean end (NOMOS CONFORMANT after CATO PASS), post on stoa--fdf addressed to **POLYBIUS_the-stoa (floor-manager)** — NOT user-tier direct. The FM runs final verification + relays up.

## What you do NOT do
Merge; push; apply infra; relay direct to user-tier (except a scope dispute); surface to PRINCIPAL except a genuine emergency.

## Close
At arc end: **`CLOSE ME — stoa--fdf (u--9s2 Phase-2 inc 2.2) gauntlet complete; awaiting Polybius_the_Stoa close-gate + merge.`**

## Compaction recovery
If you compact: re-read `.claude/MAJOR_PLINY.md`, this brief (`git show beadwork:attachments/stoa--fdf/HUMAN_paste-pliny-stoa--fdf-instruction.md`), `bw show stoa--fdf`, and the directive.

— authored by Polybius_the_Stoa | sid 990b0750-5572-4836-b9c7-18d626a12e96 | the-stoa
