status: completed
ticket: stoa--p41.1
verdict: fail
design_artifact_verified_against: agents/design/arc-61/design-rev1.md §"Verification probes" (V1–V8)
build_verified: arc-61/build @ 937aceec4adee945e0e6be32148300ada2c27a51 (worktree .claude/worktrees/arc-61-build)
probes_executed:
- probe_id: V1
  description: user-tier dry-run emits CHIRON deploy line, no recompose error, exit 0
  quadrant_classification: easy-easy
  command_or_method: bash substrate/install.sh --target user --dry-run 2>&1 | grep -E "MAJOR_CHIRON|deploy: .*CHIRON"
  expected: a [dry-run] deploy line for MAJOR_CHIRON.md; no recompose error; exit 0
  observed: exit 0; "[dry-run] deploy: .../substrate/MAJOR_CHIRON.md -> /c/Users/denso/.claude/MAJOR_CHIRON.md (substitute {{NAME_SUFFIX}} -> '')"; no recompose error
  result: pass
- probe_id: V2
  description: subproject dry-run shows 5 recompose owners; CHIRON owns pair-programmer-authoring; POLYBIUS line does not
  quadrant_classification: easy-easy
  command_or_method: bash substrate/install.sh --target subproject --parent-dir /tmp/arc61-v2-parent --subproject testsub --dry-run 2>&1 | grep "[dry-run] recompose (subproject):"
  expected: 5 dry-run recompose lines (POLYBIUS, op-disc, PLINY, CHIRON, DAEDALUS); CHIRON owned:pair-programmer-authoring; POLYBIUS line lacks pair-programmer-authoring; exit 0
  observed: exit 0; 5 owner lines present; CHIRON line "MAJOR_CHIRON_testsub.md ... (owned:pair-programmer-authoring)"; POLYBIUS line "(owned:onboarding sub-project-spawning pair-programming-prototyping substrate-update-check)" — pair-programmer-authoring NOT present
  result: pass
- probe_id: V3
  description: REAL subproject recompose exercises awk Checks A–E; body lands in CHIRON, absent from POLYBIUS (the drop/double-host falsifier)
  quadrant_classification: easy-easy
  command_or_method: bash substrate/install.sh --target subproject --parent-dir /tmp/arc61-recompose-probe --subproject probesub (dry-run OFF); then grep over recomposed MAJOR_CHIRON_probesub.md + MAJOR_POLYBIUS_probesub.md
  expected: 5 "recomposed (subproject):" success lines, exit 0, no "install.sh: error: recompose:" line; CHIRON contains module body; POLYBIUS does not
  observed: exit 0; all 5 "recomposed (subproject):" success lines incl. MAJOR_CHIRON_probesub.md and MAJOR_POLYBIUS_probesub.md; NO recompose error. BODY PARITY — grep -c "Trigger recognition" CHIRON=1, POLYBIUS=2 (BUT both POLYBIUS hits are heading-phrase collisions in the POLYBIUS-OWNED sub-project-spawning + pair-programming-prototyping module bodies, NOT the pair-programmer body — see methodology_concerns). Module-UNIQUE phrase probe is definitive: "Pair-programmer Major authoring — instruction module" CHIRON=1/POLYBIUS=0; "meta-team editorial pair-programmer in agent-gauntlet" CHIRON=1/POLYBIUS=0; "Asymmetric beadwork visibility" CHIRON=1/POLYBIUS=1 (POLYBIUS hit = its own §10.3 sub-project-spawning heading, a collision). Marker region: CHIRON open L154 / close L251 = 96 inlined body lines == 96-line module source (full body, not truncated). POLYBIUS has ZERO pair-programmer-authoring markers.
  result: pass
- probe_id: V4
  description: app green — gen-data + build + npm test all exit 0; deterministic gen-data (no agents.ts diff)
  quadrant_classification: easy-easy
  command_or_method: cd app && npm run gen-data && npm run build && npm test
  expected: all three exit 0; generated.test.ts passes with L92 LIEUTENANT assertion removed; discoverSkillFiles finds 11 skills; no unstaged agents.ts diff after gen-data
  observed: gen-data exit 0 (roster 3 MAJORs incl CHIRON, 12 CAPTAINs, 11 LIEUTENANTs); no unstaged agents.ts diff (deterministic regen — confirmed); build exit 0 (chunk-size warning non-fatal/pre-existing). npm test EXIT 1 — 2 tests FAILED in app/src/data/__tests__/generated.test.ts: (1) "seats POLYBIUS and PLINY at MAJOR rank" L55 — got ['CHIRON','PLINY','POLYBIUS'] != ['PLINY','POLYBIUS']; (2) "seats the canonical 10 CAPTAINs" L65 — toHaveLength(10) but got 12. LIEUTENANT test (Arc 17.1) passes; the L92 assertion removal is correct in isolation, but it was NOT the complete app-break set.
  result: fail
- probe_id: V5
  description: no dangling skill refs — skills/agent-author path-pointer empty; dir gone; app examples swapped to handoff-author
  quadrant_classification: easy-easy
  command_or_method: grep -rn "skills/agent-author" substrate/ app/ | grep -v substrate/arcs/ | grep -v substrate/v1-historical/ ; test ! -d substrate/skills/agent-author ; grep agent-author app/src/data/types-v2.ts app/scripts/gen-data-lib.ts
  expected: scoped skills/agent-author path-pointer grep EMPTY (load-bearing); dir removed; both app examples now handoff-author; remaining agent-author matches are intended CHIRON-§7 prose
  observed: scoped "skills/agent-author" grep EMPTY (load-bearing PASS — all remaining skills/agent-author hits are in substrate/arcs/, correctly excluded). Dir GONE (test ! -d passes). R2 swaps landed — app/src/data/types-v2.ts L148 and app/scripts/gen-data-lib.ts L81 now reference handoff-author, zero agent-author. Remaining scoped "agent-author" matches all intended (CHIRON.md L3/L32/L85, POLYBIUS.md L624, module L37/41/85, op-disc L955, handoff-author SKILL.md L185 = repointed CHIRON-§7 prose; credential-discipline L3 = "agent-authors-workflow" false-positive compound word; app daedalus-brief.md L47/67/99 = pre-existing app design-doc UI-"form" refs, not skill-dir pointers). None point at the retired dir.
  result: pass
- probe_id: V6
  description: voice audit — colonel only reserved-rank; "the user" in CHIRON only the §7 voice-rule defs
  quadrant_classification: easy-easy
  command_or_method: grep -rni "colonel" substrate/ --exclude-dir=v1-historical (then scope-filter reserved-rank + arcs); grep -ni "the user" substrate/MAJOR_CHIRON.md
  expected: colonel matches only deliberate reserved-rank; "the user" in CHIRON only at the §7 voice-rule definition lines
  observed: CHIRON "the user" = exactly L103 + L109, both inside the §7 voice-rule definition (the rule itself + the grep-command example). In-canon (excluding arcs+v1-historical) the only colonel match is substrate/README.md:55, a CHANGELOG line documenting a past Colonel-ref retirement — pre-existing, NOT touched by 937acee, not a live Colonel-as-human use. arc-directory colonel matches are historical directives. No new voice violation introduced.
  result: pass
- probe_id: V7
  description: authorship — html meta+footer = Denson Smith; CHIRON.md/module/agents.ts carry no false author field
  quadrant_classification: easy-easy
  command_or_method: grep author/denson/copyright docs/major-chiron.html ; grep author-like frontmatter in CHIRON.md + module
  expected: html author = Denson Smith; no other person in any author field
  observed: docs/major-chiron.html L6 <meta name="author" content="Denson Smith" />; footer L255 "Author: Denson Smith · 2026-06-16". No author-like frontmatter in MAJOR_CHIRON.md or pair-programmer-authoring.md. No other person named anywhere. ("§7 author" nav / "agent-author capability" are role-capability prose, not authorship attribution.)
  result: pass
- probe_id: V8
  description: HTML/.md parity — §1–§11 headings mirrored; draft-banner removal mirrored; §11 rendered without MODULE-INLINE markers
  quadrant_classification: easy-easy
  command_or_method: grep "^## " substrate/MAJOR_CHIRON.md vs grep "<h2" docs/major-chiron.html; grep DRAFT/MODULE-INLINE in html
  expected: every §1–§11 + Tools heading in .md has a corresponding rendered HTML section; non-draft provenance mirrored; no MODULE-INLINE markers in html
  observed: 1:1 parity §1–§11 + Tools (12 .md headings, 12 html <h2>, same titles); nav bar L69-71 lists §1-§11+Tools. Draft-banner removal mirrored — html L78 renders "v1 — landed Arc 61." (footer L254 too); the DRAFT banner is gone (remaining "draft" hits are a CSS class + body prose, not the banner). ZERO MODULE-INLINE markers in html; §11 rendered as a clean section (L239).
  result: pass
threat_coverage: []  # arc classified "not threat-ratified (process change: substrate canon + deploy-tooling refactor, no runtime attack path)" per design §2 / §35.5 carve-out — ARGUS-confirmed. No threat-ratified mitigation in scope; empty list valid under the carve-out.
methodology_concerns:
- "V3 probe-spec regex under-discrimination (§5.11). The design's V3 asserts grep -c \"Trigger recognition\" on recomposed POLYBIUS == 0, but \"Trigger recognition\" is NOT unique to the pair-programmer-authoring body — it collides with the §10.1 heading of the POLYBIUS-owned sub-project-spawning module AND a §11-cross-ref line in the pair-programming-prototyping module. The raw probe returns POLYBIUS=2 (a false-positive vs the design's expected 0), which would mislead a verifier into a spurious FAIL. I substituted module-UNIQUE phrases ('Pair-programmer Major authoring — instruction module', 'meta-team editorial pair-programmer in agent-gauntlet') which give CHIRON=1/POLYBIUS=0 cleanly, plus a 96==96 marker-region line-count parity check. RECOMMENDATION: DAEDALUS revise V3's grep to an anchored module-unique phrase (or assert on the marker-region body extent) rather than 'Trigger recognition'. This did NOT change the V3 verdict (still PASS — body is correctly hosted), but the spec as written is misleading."
- "V4 app-break set was under-specified by the directive/design (O3 named ONLY generated.test.ts:92 LIEUTENANT). gen-data discovers MAJOR_*.md, so landing MAJOR_CHIRON.md adds a 3rd MAJOR and breaks the L54-55 MAJOR-membership toEqual — flagged by POLYBIUS_the-stoa pre-commit (11:43:55Z) but the fix did NOT land in 937acee. SEPARATELY, the L65 CAPTAIN-count toHaveLength(10) is broken by a PRE-EXISTING latent: the committed agents.ts at parent df0ca9d already carried NOMOS (Arc 50) + TIRO (Arc 38) = 12 CAPTAINs, so that test was already red before Arc 61 and the directive never noticed. The complete app-break set is THREE assertion sites (L55 MAJOR membership, L65 CAPTAIN length, L66 CAPTAIN mnemonic list), not the one (L92) the directive named."
falsifying_evidence_summary: |
  V4 (app green) is FALSIFIED — `npm test` exits 1 with 2 failing tests in app/src/data/__tests__/generated.test.ts, so the arc DoD "app green / npm test passes" is UNMET on build 937acee. (1) "seats POLYBIUS and PLINY at MAJOR rank" (L55): AssertionError: expected ['CHIRON','PLINY','POLYBIUS'] to deeply equal ['PLINY','POLYBIUS'] — landing MAJOR_CHIRON.md makes gen-data ingest a 3rd MAJOR, which the unmodified L55 toEqual rejects. This is precisely the break POLYBIUS_the-stoa surfaced pre-commit at 2026-06-17T11:43:55Z with the exact fix (L55 -> ["CHIRON","PLINY","POLYBIUS"], L51 title update); that fix was NOT applied in the build commit. (2) "seats the canonical 10 CAPTAINs" (L65): AssertionError: expected length 10 but got 12 — the committed agents.ts at parent df0ca9d ALREADY contained NOMOS (Arc 50) + TIRO (Arc 38), so this assertion was latently red before Arc 61; Arc 61's gen-data regen surfaces it on npm test. Either way npm test is red and V4 fails. ALL OTHER probes pass: notably the high-stakes V3 (real subproject recompose) is clean — the pair-programmer-authoring 96-line body is inlined exactly once into MAJOR_CHIRON's markers (96==96 source parity) and is wholly absent from MAJOR_POLYBIUS (the two raw "Trigger recognition" hits in POLYBIUS are heading collisions in OTHER POLYBIUS-owned modules, confirmed by context — not a dropped/double-hosted body). The recompose machinery, install wiring, dangling-ref cleanup, voice, authorship, and HTML/.md parity are all correct. The single blocking defect is the incomplete app-test-fix set.
verification_artifacts_path: probe outputs captured inline; transient probe artifacts written to git-bash /tmp/v1.out /tmp/v2.out /tmp/v3.out /tmp/v4-*.out and scratch recompose dirs /tmp/arc61-v2-parent + /tmp/arc61-recompose-probe (cleaned after V3). No persistent verification artifact dir required for these mechanical probes.
summary: |
  Verified build 937acee (arc-61/build) against design-rev1.md probes V1–V8 from the worktree root. 7 of 8 probes PASS; V4 (app green) FAILS, so the overall verdict is FAIL. The load-bearing high-stakes probe V3 — a REAL (non-dry-run) subproject recompose into a fixed-literal scratch parent — exercised the FAIL-LOUD awk Checks A–E and is fully clean: exit 0, all 5 recompose success lines, no recompose error, and the pair-programmer-authoring module body is inlined exactly once into MAJOR_CHIRON's markers (96 body lines == 96-line source, balanced markers L154/L251) and is entirely absent from MAJOR_POLYBIUS (verified with module-unique phrases after catching that the design's "Trigger recognition" grep collides with two other POLYBIUS-owned module headings — a methodology concern logged for DAEDALUS, not a build defect). install wiring (V1/V2: CHIRON as 5th recompose owner, ungated, suffixed), dangling-ref retirement (V5: skills/agent-author path-pointer empty, dir gone, R2 app examples swapped), voice (V6), authorship (V7: Denson Smith), and HTML/.md parity (V8: §1–§11 mirrored, draft-banner removal mirrored, no MODULE-INLINE in html) are all correct. The single blocking break is the app test suite: npm test exits 1 because the directive/design under-specified the app-break set to ONLY generated.test.ts:92 (LIEUTENANT). In fact ingesting MAJOR_CHIRON adds a 3rd MAJOR that breaks the L55 MAJOR-membership toEqual (the exact break POLYBIUS_the-stoa flagged pre-commit, whose fix did not land), and a pre-existing latent (NOMOS+TIRO already in committed agents.ts at df0ca9d) breaks the L65 CAPTAIN-count assertion. Both must be fixed for the arc's "app green" DoD; route back to ADA. No deliverable was modified during verification (independence preserved).
