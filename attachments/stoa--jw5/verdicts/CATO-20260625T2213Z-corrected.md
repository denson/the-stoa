---
author: Denson Smith
ticket: stoa--jw5 (u--9s2 Phase-1)
seat: CAPTAIN_CATO_the_stoa (REVIEWER)
gauntlet_stage: 5/6
reviews: agents/design/stoa--jw5/ (design-formal.md + resolution-check/ harness + vera/argus verdicts)
as_of: 2026-06-25
---

# CATO -- cold-read verdict: u--9s2 Phase-1 design + resolution harness

status: completed
ticket: stoa--jw5 (u--9s2 Phase-1)
verdict: pass
diff_reviewed: agents/design/stoa--jw5/ (design-formal.md; resolution-check/{resolve,fixtures,run,vera_probes}.py + RESULTS.md), working tree @ main HEAD 4160b14 (design arc, no branch -- untracked)
design_artifact_compared_against: agents/design/stoa--jw5/design-formal.md + directive (beadwork:attachments/stoa--jw5/u9s2-phase1-directive.md sec6 DoD)

concerns:
- id: c1
  category: consistency
  description: design-formal.md formalizes: frontmatter (L5) and sec15 Provenance (L846) cite agents/design/stoa--jw5/design-codesign.md by a WORKING-TREE path that does not resolve on-disk. CORRECTED AFTER FM GROUND-TRUTH (verified first-hand by me via git cat-file): the converged doc is NOT missing -- it EXISTS on the beadwork branch at attachments/stoa--jw5/design/design-codesign.md (23129 bytes, the unified CO-DESIGN CONVERGED doc sec0-sec15 incl sec11 held fork + sec12.B.1 relay-up package). The defect is a DANGLING WORKING-TREE CITE / co-location gap: the authoritative converged artifact lives in the beadwork attachment store, but design-formal.md cites it as if co-located beside it in the working tree, so a reader landing in agents/design/stoa--jw5/ cannot resolve the formalizes:/sec15 pointer.
  evidence: design-formal.md:5 + design-formal.md:846 (working-tree path cite); git cat-file -e beadwork:attachments/stoa--jw5/design/design-codesign.md = EXISTS (23129 bytes); its header names design/chiron-ownership-model.md + design/choreography-hamilton.md as the half-lenses it unified (both also under attachments/stoa--jw5/design/ on beadwork).
  severity: minor
  quadrant_classification: easy-easy

follow_ups: (provenance co-location, NOT a redesign; routed as follow-up so the Grands gate package has a resolvable provenance chain) DAEDALUS / the writing seat should make the formalizes: + sec15 cite RESOLVE -- EITHER (a) co-locate design-codesign.md (and the design/chiron-ownership-model.md + design/choreography-hamilton.md lenses) into the working tree alongside design-formal.md, OR (b) repoint the cite to the beadwork-attachment path attachments/stoa--jw5/design/design-codesign.md. DISPOSITION GUARD (per FM ground-truth, which I confirmed first-hand): the unified design-codesign.md is the AUTHORITATIVE converged artifact (it carries the held fork + the relay-up residual package the Grand gates on) -- the fix MUST preserve it in provenance; do NOT repoint formalizes:/sec15 to the half-lens files as if the unified converged doc never existed (my earlier draft suggested that option; it is WITHDRAWN -- it would erase the authoritative convergence artifact).

verifier_coverage_assessment: VERAs threat-coverage is GENUINE and complete, not a happy-path proxy set. I ran the sec6.1 item-11 mechanical cross-check myself: all 5 defeats_via_probe ids (p3-M4, p4-M1, p5-M3, p6-M2, p7-M6) appear in probes_executed with non-empty probe_evidence and an attack_path_exercised recording BOTH (a) attack-blocked AND (b) legit-unaffected -- no empty binding, no sec36 T-a/T-b trigger shape. Tier-ii substance (my meta-verifier judgment): each probe drives the named attack, not the happy path -- M4 drives omit of EACH of the 5 baseline entries + the omit-cap-add step-0-bypass attempt (all fail-closed) while a legit category-omit (postgis) is correctly permitted (over-blocking falsified); M3 STRIPS MAPS_API_KEY and asserts runtime-completeness FAILS naming the exact missing pairing (catches the gap, not just passes happy path); M1 drives cross-builder scope derivation and asserts neither builders unique scope-bearing entry reaches the other; M2 probes for ANY value-materialization point (none exists); M6 is a correct design-property grep for Phase-1 (no live mesh). I re-ran vera_probes.py first-hand (30/30, exit 0) AND ADAs run.py (19/19, exit 0). VERAs self-reported false-positive (the sec5.11 PASSWORD-substring anchoring trap) is confirmed real and confirmed fixed IN THE PROBE, not the build: the corrected param-name inspection lives at vera_probes.py L274-279 with the trap documented in the comment, and resolve.py carries no patch to dodge it (the build inspects no signatures). Independence preserved. M5 / R-1 / R-2 are correctly NOT claimed defeated -- sec35.5 named residuals in sec12.B/sec12.B.1, carried UP; sec36 correctly does not fire. VERAs PASS is justified by the evidence.

summary: The Phase-1 design + resolution harness is a clean, well-crafted, internally-consistent deliverable; NO blocking concern. CRAFT/HYGIENE: resolve.py implements sec2.5 verbatim -- precedence (step-0 guard, union category, subtract omit, union add), (kind,name) tuple identity, add-wins (add last), BaselineOmitError guard derived from the baseline record (generalizes to all 5 + future entries), deterministic sorted output; harness genuinely pure (no env/network/clock/fs reads, provisions nothing), no dead code, no silent except-swallow (warn() is an intentional documented no-op hook; resolve_with_lint captures lints explicitly). SECURITY: credential-discipline holds -- (kind,name) SLOTS only, no secret VALUE anywhere in harness or spec; per-builder isolation genuinely derived (derive_sa_scope = mechanical union of scope-bearing resolved entries, no foreign-entry leak path); no provisioning side-effect. SCOPE: nothing exceeds Phase-1 -- no provisioning, cookie-cutter NOT built (Phase-2), held isolation-UNIT fork carried UNCLOSED to the Grand, R-1/R-2/M5 honestly deferred not silently solved. HONESTY AUDIT (the watched drift-class): clean -- M1 correctly qualified to INITIAL PROVISION (t0) with the subtractive stale-grant gap named as R-1; M5 explicitly NOT claimed a hard defeat (no GA hard-dollar cap at any granularity); no surviving hard-cap over-claim; sec12.C not-threat-ratified classifications honest. AUTHOR ATTRIBUTION: author: Denson Smith confirmed on all 9 arc artifacts (design-formal + 4 harness .py + RESULTS.md + 3 verdicts); zero author-like field naming anyone else (author/co-author/authorship sweep hits are all prose body-text, not metadata fields). META-VERIFY-VERA: PASS justified, build not silently patched, no empty threat-coverage binding. The single finding (c1, minor) is a provenance co-location defect: design-formal.md cites design-codesign.md by a working-tree path that does not resolve on-disk, but the converged doc EXISTS on beadwork (attachments/stoa--jw5/design/design-codesign.md, 23129 bytes -- I verified first-hand via git cat-file after the FM ground-truth correction); fix = co-locate or repoint to the beadwork-attachment path, preserving the authoritative unified doc. Overall posture: CLEAN -- pass with one minor provenance co-location fix routed as a follow-up.

gap_or_blocker: none (status completed).

- [from: CAPTAIN_CATO_the_stoa (subagent) | caller-sid 8040be7f-a1ba-4917-b953-75947d464abf]
