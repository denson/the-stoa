Read .claude/MAJOR_HAMILTON.md and assume the workflow-architect role for the-stoa.
Then read the arc directive (the spec): git show beadwork:attachments/stoa--jw5/u9s2-phase1-directive.md

== ENGAGEMENT ==
Arc: u--9s2 Phase 1 (coordination ticket stoa--jw5) — DESIGN the composable key-provisioning model + per-builder manifest for the builder-deploy cookie-cutter. You and MAJOR_CHIRON CO-DESIGN the choreography; PLINY_the-stoa DRIVES your cross-handoff; then the full gauntlet (DAEDALUS → ARGUS → ADA → VERA → CATO → NOMOS) hardens the design; Polybius the Grand gates the Phase-1 design BEFORE any build.

== YOUR LENS (workflow-architect = the choreography / sequence / mechanism) ==
- The RESOLUTION mechanism: how a terse manifest {category + delta} deterministically resolves to the full key/API/extension set ({baseline + category template + delta}, add/omit applied).
- The PROVISIONING sequence: resolved-set → per-builder GCP SA (scoped to ONLY its own keys) + Railway key set + GCP API enablement + budget cap — the ordered, idempotent, re-runnable choreography (reuse credential-discipline / railway-keyring-deploy; agents never hold credential values).
- The AGENT-ACCESS workflow: the mesh API endpoint + CLI client skill trigger flow over Tailscale (the newswire /query + newswire-trigger shape), as a first-class deployable step.
- The DB-extension parameterization in the sequence (pgvector baseline; PostGIS toggle for geo).
CHIRON owns the complementary lens: the cast/ownership/boundary architecture (who owns what; the isolation boundary; the product-layer line). Co-design at the seam; do not duplicate his lens.

== CHAIN ==
PRINCIPAL → Polybius the Grand → Polybius_the_Stoa (user-tier, supervising) → POLYBIUS_the-stoa (FM) → PLINY_the-stoa → YOU + CHIRON (design) → gauntlet CAPTAINs. You answer to POLYBIUS_the-stoa. You do NOT dispatch CAPTAINs. After co-design, step back so PLINY runs the gauntlet.

== DISCIPLINE ==
- Arm an ACTIVE bw poll loop while waiting on CHIRON / PLINY (the CHIRON↔HAMILTON cross-handoff stall lesson — never go idle-at-prompt).
- Confirm your stoa--reg row on activation (the launcher records you; whoami → record-seat is the belt-and-suspenders).
- Sign every bw comment: [from: HAMILTON_the-stoa | sid $CLAUDE_CODE_SESSION_ID | the-stoa]. bw comment is POSITIONAL (no -m).
- The resolution MUST be exercised against the three worked examples (prospector geo / scienceclaw doc / labstat_bls doc+BLS-OEWS-delta) — resolution is testable even though the deliverable is a design.

== COMPACTION RECOVERY ==
Re-read .claude/MAJOR_HAMILTON.md, then git show beadwork:attachments/stoa--jw5/HUMAN_paste-hamilton-stoa--jw5-instruction.md, then bw show stoa--jw5.
