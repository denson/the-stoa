status: completed
ticket: stoa--xa4
verdict: pass
diff_reviewed: git diff main..arc-72/build @ a973f31 (29 files, +1491/-17)
design_artifact_compared_against: agents/design/stoa--xa4/design-rev1.md + design-rev2.md (rev2 wins on conflict)

=== r1 SEMANTIC READ (the named load-bearing item) — called out FIRST ===
input-not-verdict: YES
lean-inside-guardrail: YES

Cold-read of the DEPLOYED substrate/skills/decision-surface/SKILL.md, both reframed lean sites + the Q5 clause:

1. Spine paragraph (deployed L29): "if you have a lean — you expose it as an INPUT to their deciding, not a
   recommendation to defend: 'here is where I would lean and exactly why, so you can push on the reasoning.'
   The lean is a window into your reasoning the human can interrogate, never a verdict you hold the line on;
   the value-call stays the human's." This READS as an interrogable input with its WHY exposed. The verbs are
   expose / interrogate / push-on-the-reasoning, and the explicit negation "never a verdict you hold the line
   on" forecloses the defend-the-label posture. Not a position the agent states and would defend.

2. Procedure step 6 (deployed L143): "surface it as a reasoned input the human can push on ('here is where I
   would lean and exactly why'), never a verdict to defend. If the human pushes to HARDEN the lean into your
   decision, that triggers the cave-trap guardrail." Input-framed, WHY exposed, and it actively routes the
   harden-push into the guardrail. Sound.

3. Q5 cave-trap clause (deployed L81), inside the "When the human pushes hard for a verdict" subsection
   (heading L71, next heading L83 — confirmed within bounds): a push to "harden a stated lean into a verdict"
   is named a push-for-a-verdict and routed through the SAME dilemma-classifier §2 self-check: name
   new-information-vs-pressure; on pressure HOLD and re-expose the lean as a reasoned input (do not promote to
   a decision); on genuine new-info update honestly. This is NOT cosmetic — it genuinely wires the lean into
   the anti-cave machinery, and it leans on the "naming it out loud makes a cave a detectable lie" mechanism
   the rest of §2/Q5 uses. The lean is structurally and semantically inside the guardrail.

VERDICT on the load-bearing property: the deployed artifact READS as input-not-verdict at both sites, and the
lean is genuinely inside the anti-cave guardrail. The reframe did not re-introduce a verdict-to-defend. This
is the property the FM pinned to CATO; it holds.

concerns:
- id: c1
  category: other
  description: P-DC3-ida-closed is INCONCLUSIVE in VERA's verdict (stoa--ida still OPEN); the bw-close ledger action is correctly deferred to the close-gate per the execute-ledger-before-push discipline. Surfaced so the close-gate (NOMOS/PLINY) actually executes the close with the landing SHA — root cause is shipped+verified (SKILL_NAMES membership), so this is a ledger action, not a build defect.
  evidence: VERA-2026-06-24T02-45-37Z.md probe P-DC3-ida-closed; stoa--ida status OPEN
  severity: minor
  quadrant_classification: easy-easy
- id: c2
  category: consistency
  description: README L43 class table summarizes cave as "hold x4 / update x1" while the body sentence (L46) and manifest enumerate cave5 (harden-the-lean) as hold; the two are consistent (cave1/2/3/5 hold = 4, cave4 update = 1) but the "x4/x1" shorthand reads slightly tersely next to the per-fixture detail elsewhere. Cosmetic; no correctness impact. Recorded for completeness, not for revision.
  evidence: substrate/modules/tests/decision-surface/README.md L43 vs L46 + manifest.tsv
  severity: minor
  quadrant_classification: easy-easy

follow_ups:
- Execute the stoa--ida bw-close at the close-gate/merge with the landing SHA (deferred ledger action, not a build fix). This is the correct sequencing per execute-ledger-before-push; flagged so it is not dropped.

verifier_coverage_assessment: VERA exercised the load-bearing cases. I independently re-ran the two mechanical
probes that are cheap to reproduce — the exactly-nine schema guard (grep -cE on the SKILL.md side = 9; full
label-list diff against decision-register.md §2 = empty) and check-corpus (PASS 19/19, exit 0) — and both
reproduce VERA's observations exactly. I independently confirmed classifier §1/§2 (L37-83) and §4/§5/§6 are
byte-unchanged (single §3 hunk), the bare-labeled-lean count is 0, the Q5 harden clause sits inside the Q5
subsection, and the honest-stance grep hits all sit inside explicit "NOT guaranteed" disclaimers (SKILL L12,
L118) or are the pre-existing honesty-LOCK warnings in the classifier intro (pre-§3, unchanged by this diff).
VERA correctly and explicitly scoped P-DC1-lean-framed to the STRUCTURAL half and deferred the SEMANTIC read
to CATO — exactly the meta-verifier division the FM pinned; no rubber-stamp, no over-count. The one INCONCLUSIVE
(P-DC3-ida-closed) is correctly reasoned as a deferred ledger action, not a build defect. VERA's honest-stance
wording is clean (dogfood-proxy, seed-bar-not-coverage, no guaranteed/enforced claims). I did not re-run the
app/gen-data probe (P-DC3-gendata, npm-bound); VERA's deterministic-sha256 + roster method is the right shape
and I have no reason to doubt it. No coverage gap found.

attach_status: PENDING

summary: A clean, disciplined graduation arc. The diff is confined to the five named deliverables — classifier
§3 retune (single hunk, §1/§2/§4/§5/§6 byte-unchanged), the graduated SKILL.md (Q1 detection table, Q4 sharp
render threshold, Q5 cave-trap guardrail reusing classifier §2, Q2 expert-referral v1 with the bias hard-part
NAMED, DRAFT stripped, the nine-field decision-record template), the worked-example frontmatter
(forthcoming->shipped), the install.sh SKILL_NAMES one-line addition, and the new decision-surface corpus
(runner, manifest, 19 realistic difficulty-spread fixtures, README with four explicit honesty statements). The
load-bearing r1 semantic property HOLDS: the deployed lean reads as an interrogable input-with-WHY at both
sites and is genuinely inside the §2/Q5 anti-cave machinery — no verdict-to-defend re-introduced. DC2
schema-share is literally one-to-one with decision-register §2 (exactly nine labels, same order, zero
translation). The honest stance is the deliverable and it is held everywhere — every guarantee-flavored word
sits inside an explicit "NOT guaranteed" disclaimer; cave/ is named a seed bar over an unbounded vector space;
--judge is named a dogfood proxy. The cave fixtures are genuine (cave5 directly exercises the r1 lean-harden
vector; cave4 is a well-constructed new-info negative control proving "hold" is not reflexive). Author=Denson
Smith preserved throughout; no authorship regression. VERA's PASS is sound and I reproduced its two cheapest
load-bearing probes independently. The only open items are non-blocking follow-ups for the close-gate (execute
the stoa--ida bw-close at merge) and one cosmetic README shorthand. Overall posture: clean — PASS with two
minor (non-blocking) nits.