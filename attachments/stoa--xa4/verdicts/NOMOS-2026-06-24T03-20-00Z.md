verdict: DIVERGENT
ticket: stoa--xa4
checked_output: Arc 72 gauntlet build commit a973f31 on branch arc-72/build (decision-surface graduation + flag-and-guide retune + one-loop wiring + corpus), audited against substrate/arcs/arc-72-build-directive.md (DC0-DC4 + DoD), charter stoa--xa4, design-rev1/rev2, and the three verdicts ARGUS (PASS-WITH-CONDITIONS) / VERA (PASS) / CATO (PASS).

divergences:
- type: commit-sha
  detail: The committed build artifact app/src/data/generated/agents.ts is STALE. Commit a973f31 changed substrate/skills/decision-surface/SKILL.md from DRAFT to graduated (the body that gen-data embeds into the decision-surface LIEUTENANT slot), but did NOT regenerate the tracked agents.ts. The committed agents.ts still embeds the OLD DRAFT skill body (the DRAFT banner + the old Open-questions section + the pre-r1 labeled-lean prose). A fresh "npm run gen-data" produces a 1-file diff against the committed artifact.
  evidence: |
    git show --stat a973f31 -- app/src/data/generated/agents.ts  =>  EMPTY (agents.ts NOT in the 29-file commit).
    git show a973f31:app/src/data/generated/agents.ts | grep -c "being refined as we use it"  =>  1 (stale DRAFT body present in committed artifact).
    cd app && npm run gen-data ; git status --porcelain app/src/data/generated/agents.ts  =>  " M ..." (1 insertion / 1 deletion; the decision-surface slot body+description flip DRAFT->graduated).
    Established arc pattern: Arc 70 (5f941bf) and Arc 71 (bf57c40) BOTH regenerated agents.ts IN the build commit; Arc 68/69 carry explicit regen-agents.ts chore commits. Directive DoD line 68 lists gen-data deterministic in the close-gate; a deterministic gen-data over the shipped substrate yields NO diff only if the artifact is current.
    (Working tree restored via git checkout after the regen probe -- auditor left no dirty state.)
  classification: SIZE-DERAIL
  note: |
    Mechanically trivial to fix (one npm run gen-data + stage agents.ts, either as a trailing commit on arc-72/build or as the standard post-merge regen). Surface-divergence cause = the build omitted the established regen-agents.ts step for a SKILL.md body change; the artifact and the substrate it ships beside disagree. NOT a logic defect in any deliverable. Per the directive DoD gen-data-deterministic this must be reconciled before the close-gate passes. Routing is PLINYs call (incomplete-unverifiable-routing): the cheapest sound route is a trailing regen-agents.ts commit on the build branch (mirrors Arc 68/69 chore commits) so the committed artifact matches the shipped substrate. VERA reported gen-data deterministic + clean roster from its OWN in-worktree regen (which silently re-derived the graduated body) and did not compare the COMMITTED artifact against a fresh regen -- that is the gap this probe catches.

ground_truth_consulted:
  bw_tickets: stoa--xa4 (charter + full comment thread), stoa--ida (OPEN, deferred-close confirmed)
  git_refs: a973f31 (built commit, branch arc-72/build), origin/main (baseline for spine + install.sh diffs), commit history of app/src/data/generated/agents.ts (5f941bf Arc70, bf57c40 Arc71, e157592 Arc69, 4a73edf Arc68)
  commands: |
    git log -1 (author=denson, seat trailer Co-Authored-By: CAPTAIN_ADA_the-stoa present), git show --stat a973f31 (29 files +1491/-17);
    git diff origin/main..a973f31 -- dilemma-classifier.md (single hunk, confined to S3); L1-83 byte-identical (S1/S2) + tail-compare byte-identical (S4/S5/S6, +7 net offset); S3 AFTER-text guarantee-word grep (none);
    SKILL.md read (no status:DRAFT frontmatter; author Denson Smith; Q1/Q4/Q5 resolved, Q2 v1, Q6 done, Q3 open; consumer-tier line intact; guarantee-words only in disclaimers L12/L118);
    decision-register S2 labels vs SKILL.md DC2 template: EXACTLY 9 line-start labels, identical set + order (DR-ID,WHEN,CHECKPOINT,DILEMMA,WARNING,OPTIONS,CHOSEN,COUNTER-HYPOTHESIS,CONTEXT-LINK);
    install.sh SKILL_NAMES +decision-surface (single +1 line L233); non-recursive module glob /*.md L896 (+ L891-893 comment: modules/tests/ deploy-safe);
    corpus: 19 fixtures (dilemma5/solved5/cave5/render4), manifest 19 data rows with per-row want (cave4=update, cave5=hold), README honesty (seed-bar/dogfood-proxy/not-a-guarantee/never guaranteed-enforced-non-collapsible L121);
    re-ran: decision-surface --check-corpus PASS 19/19, dilemma PASS 19/19, decision-register PASS 18/18, author-gate 29/0, stop-self-check 2/0, vitest 41/41, gen-data clean roster (4 MAJOR/12 CAPTAIN/8 LIEUTENANT/COLONEL reserved/HUMAN stub);
    stray-ticket grep on diff: only stoa--xa4 (17) + stoa--ida (6).

summary: |
  Nine of ten probes CONFORMANT and clean. Probe 1 (SHA/author/trailer): a973f31 real on arc-72/build, Author=denson (PRINCIPAL, not overridden), seat trailer present. Probe 2 (spine): single diff hunk confined to dilemma-classifier S3; S1 LOCKED + S2 self-check + S4/S5/S6 byte-identical to main. Probe 3 (graduation): no status:DRAFT frontmatter, Qs resolved per spec, consumer-tier line intact. Probe 4 (schema-share): the DC2 decision-record template has EXACTLY 9 line-start labels, identical set AND order to decision-register S2 -- re-derived, not trusted. Probe 5 (deploy): decision-surface in SKILL_NAMES; non-recursive modules glob keeps the tests corpus source-only. Probe 6 (corpus): 19 fixtures 5/5/5/4 + manifest per-row want + runner + README honesty; --check-corpus green 19/19. Probe 7 (regression): dilemma 19/19, register 18/18, decision-surface 19/19, author-gate 29/0, stop-hook 2/0, vitest 41/41 -- all re-run green. Probe 8 (honest stance): no hard-gate over-claim in deployed text or verdicts; every guarantee-word inside an explicit NOT-guaranteed disclaimer. Probe 9 (stoa--ida): OPEN, correct deferred-ledger state (close deferred to ship per DC3; root-cause SKILL_NAMES fix shipped+verified; VERA INCONCLUSIVE + CATO nit c1 consistent). Probe 10: charter id correct throughout, no stray ids, three verdicts internally consistent and consistent with the repo.

  THE LOAD-BEARING DIVERGENCE (gen-data determinism): the build commit changed the SKILL.md body that gen-data embeds but did not regenerate the tracked app/src/data/generated/agents.ts, so the committed artifact carries the stale DRAFT skill body. A fresh gen-data produces a real 1-file diff. This contradicts the directive DoD (gen-data deterministic) and breaks the Arc 70/71 pattern (agents.ts regenerated in the build commit). Classified SIZE-DERAIL: a trivially-fixable omitted build step, NOT a logic defect. The five deliverables (DC0 retune, DC1 graduation, DC2 schema, DC3 deploy, DC4 corpus) are all CONFORMANT.

  RECOMMENDATION (orchestrator-owned routing): reconcile the committed agents.ts (npm run gen-data + stage) so the artifact matches the shipped substrate, then the build + verdicts may propagate. Do NOT propagate as-is: the public commit would carry a tracked build artifact that disagrees with its own substrate.

gap_or_blocker: n/a (all probes verifiable; nothing UNVERIFIABLE).
