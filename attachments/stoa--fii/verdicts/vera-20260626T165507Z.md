status: completed
ticket: stoa--fii
verdict: pass
design_artifact_verified_against: agents/design/stoa--fii/design-rev2.md
build_verified: commit a4f80c6 on stoa--fii/radio-check-liveness (worktree .claude/worktrees/stoa--fii-build)
probes_executed:
- probe_id: P1
  description: end-to-end radio-check happy path (2 live seats ack + 1 dead seat silent).
  quadrant_classification: easy-easy
  command_or_method: jq-free registry resolve (git show beadwork:.../seat-registry.jsonl | grep project | grep status:alive); post [radio-check] ping on stoa--fii naming POLYBIUS_the-stoa + PLINY_the-stoa + CHIRON_the-stoa with a 10min window; collect acks; bw show stoa--fii; tally by eye + audit-only sid-match.
  expected: both live seats produce a [radio-check-ack] whose sid matches the registry session_id (tallied alive); the dead seat produces no ack (presumed dead); tally derivable from comments+registry with no passive field and no jq.
  observed: REAL live demo. POLYBIUS_the-stoa posted a genuine [radio-check-ack] sid 4f60c468-9286-441f-8ffe-bf5979a65470 = registry session_id -> MATCH, tallied alive. PLINY_the-stoa (heads-down orchestrator, simulated per brief) ack sid 62a49ed0-cf37-4b6a-896d-3cebda142d5c = registry session_id -> MATCH, tallied alive. CHIRON_the-stoa carries status:dead in registry (session 046d128e-...); posted NO ack in the window -> tallied PRESUMED dead. Resolve + tally fully jq-free.
  result: pass
- probe_id: P2
  description: M1 threat-anchored probe - audit-only sid-match (accidental wrong/stale sid flagged, NOT impersonation defeated).
  quadrant_classification: easy-easy
  command_or_method: (1) post POLYBIUS_the-stoa ack carrying wrong sid 00000000-...; apply 38.3 audit-only sid-match. (2) post POLYBIUS_the-stoa ack carrying true registry sid 4f60c468-...; apply match. (3) grep op-disc 38 / stoa--reg note for audit-only / not-authentication / does-not-defeat-impersonator wording.
  expected: (1) wrong sid != registry session_id -> NOT tallied, flagged for escalation. (2) true sid -> tallied alive. (3) canon states audit-only boundary, makes NO authentication claim.
  observed: (1) 00000000-... != 4f60c468-... -> MISMATCH -> not tallied alive, flagged (accidental-wrong-sid slice caught). (2) 4f60c468-... = registry -> MATCH -> tallied alive (legit ack unbroken). (3) op-disc 38.3 L1849-1850 states the sid-match is "AUDIT-ONLY: it does NOT defeat a deliberate impersonator who copies the registry sid, and is NOT an authentication or authorization control"; mirrored in stoa--reg honest-claim note. No authentication/impersonation-defeat claim made. VERA asserts M1 defeats ONLY the accidental stale/typo/wrong-sid slice, NOT impersonation.
  result: pass
- probe_id: P3
  description: M2 threat-anchored probe - honest-claim boundary holds in both homes + confirm-the-presumption pre-relaunch step + recovery executable.
  quadrant_classification: easy-easy
  command_or_method: grep op-disc 38 for "PRESUMED dead, NOT proven" + a pre-relaunch confirm-the-presumption/re-ping step; grep stoa--reg for the mirrored WHO-vs-WHETHER + presumed-not-proven note; confirm 38.5 recovery has a reachable relaunch command after step 1.
  expected: both homes carry the boundary; recovery remains executable for genuine deaths.
  observed: op-disc 38.4 (L1858) states non-answer is "PRESUMED dead, NOT proven dead"; 38.5 step 1 (L1871) is "Confirm the presumption (re-ping with a widened window...)"; 38.5 step 2 (L1873-1876) carries a concrete team-launcher relaunch command reachable after confirmation. stoa--reg ADA comment carries the mirrored WHO-vs-WHETHER + "PRESUMED dead, NOT proven dead" + audit-only note. Both halves present; recovery executable.
  result: pass
- probe_id: P4
  description: M3 threat-anchored probe - no passive machinery / scope-fence holds; registry JSONL key set unchanged.
  quadrant_classification: easy-easy
  command_or_method: git show a4f80c6 of changed substrate files | grep added lines for last_seen/TTL/sweep/continuous monitor/passive liveness/periodic - assert every occurrence is in a NEGATING/OUT/scope-fence context; registry key grep (head -1 | grep -o keys) for unchanged 12-key set; confirm 38.1 SCOPE-FENCE clause present.
  expected: zero in-scope passive-machinery re-introduction; 12 keys unchanged; scope-fence present.
  observed: all passive-machinery vocab in added lines is in NEGATING/OUT context only - 38.1 SCOPE-FENCE: "NO last_seen field, NO TTL, NO passive liveness field" (L1767), "NO periodic sweep, NO continuous monitor" (L1770), "never a baked constant ... IS the TTL this scope scraps" (L1771); the "PERIODIC" occurrences are the 7-vs-38 disambiguation (negating, not introducing periodic machinery into 38). Registry key grep returns exactly 12 keys (seat,name,session_id,project,machine,role,tier,composition,gauntlet,chain_role,launched_at,status) - unchanged, no liveness key. 38.1 SCOPE-FENCE clause present as a positive anchor.
  result: pass
- probe_id: P5
  description: regression backstop - author audit + deploy-plan + gen-data + full existing suite.
  quadrant_classification: easy-easy
  command_or_method: (a) grep changed files for author-like fields; (b) confirm op-disc deploys via install.sh cp (informational); (c) npm run gen-data; (d) npm test (full vitest suite) from the worktree.
  expected: (a) any author field = Denson Smith; team-launcher SKILL frontmatter author unchanged. (c) gen-data green, no Zod failure. (d) full suite green, no regression.
  observed: (a) only author-like metadata field across changed files is team-launcher SKILL.md frontmatter author: Denson Smith, UNCHANGED; the two "author" string hits in op-disc added lines are prose ("author identity", "authorization control"), not metadata - no foreign author. (b) op-disc deploys via plain cp (install.sh) - inline 38 needs no array edit. (c) npm run gen-data ran green - 16 role files, roster 4 MAJOR / 12 CAPTAIN / 8 LIEUTENANT, no Zod failure; the only generated diff is the legit team-launcher Recovery cross-ref re-derived into agents.ts (a build artifact regenerated on merge per 18.1) - NOT non-determinism, restored after check. (d) npm test: 3 test files passed, 41/41 tests passed, no regression.
  result: pass
threat_coverage:
- mitigation: M1
  threat: false-alive via accidental stale/typo/wrong sid
  defeats_via_probe: P2
  probe_evidence: stoa--fii comments (P2-step1 wrong-sid mismatch flagged; P2-step2 true-sid match tallied); op-disc 38.3 L1849-1850 audit-only/no-authentication text
  attack_path_exercised: drove the accidental-wrong-sid ack path - wrong sid 00000000-... mismatched registry and was flagged-not-tallied (a) attack-blocked; true sid 4f60c468-... matched and tallied alive (b) legit-unaffected. Honest scope - NOT impersonation-defeat (canon states so).
- mitigation: M2
  threat: honest-claim erosion (non-answer treated as proven dead -> wrongful replacement)
  defeats_via_probe: P3
  probe_evidence: op-disc 38.4 L1858 PRESUMED-not-proven + 38.5 step1 L1871 confirm-the-presumption + 38.5 step2 relaunch command; stoa--reg ADA comment mirrored note
  attack_path_exercised: drove the one-silent-window-as-proof path - canon in BOTH homes gates wrongful-immediate-replacement behind confirm-the-presumption (a) attack-blocked; 38.5 step2 keeps a reachable relaunch after confirmation (b) legit-unaffected.
- mitigation: M3
  threat: scope-creep back to scrapped passive machinery (last_seen/TTL/sweep/passive field)
  defeats_via_probe: P4
  probe_evidence: git show a4f80c6 added-line grep (all passive vocab in NEGATING/OUT context); 38.1 SCOPE-FENCE L1767/L1770/L1771; registry 12-key grep unchanged
  attack_path_exercised: drove the convenience-edit-adds-passive-field path - zero in-scope passive machinery in the diff; 12-key JSONL schema unchanged (a) attack-blocked; 38.1 SCOPE-FENCE present as the named flag-anchor (b) legit-unaffected.
methodology_concerns: |
  None of consequence. Notes: (1) P1's PLINY ack was simulated (PLINY is the heads-down orchestrator/caller per the brief) - the POLYBIUS_the-stoa ack was a REAL live ack, so P1 is a genuine live end-to-end demo on at least one seat plus a wire-shape-faithful sim on the other; the load-bearing assertion (coordinator tally + audit-only sid-match logic) is fully exercised either way. (2) CHIRON_the-stoa was named as the dead target; it is present in the registry with status:dead (session 046d128e-...) rather than absent - a stronger demonstration than "absent" (a real dead seat that does not poll/answer), still tallied PRESUMED dead. (3) gen-data regenerated app/src/data/generated/agents.ts (build artifact); the diff is exactly the legit arc cross-ref - restored after verifying, must be regenerated on merge per 18.1.
falsifying_evidence_summary: ""
verification_artifacts_path: stoa--fii probe outputs recorded inline in this verdict + as executed bw comments on stoa--fii (the [radio-check] ping, the live POLYBIUS ack, the P2-step1/step2 acks, the PLINY sim ack)
summary: |
  The build (commit a4f80c6) was exercised against design-rev2 P1-P5 jq-free. P1 ran as a GENUINE LIVE end-to-end demo: POLYBIUS_the-stoa answered a real [radio-check-ack] whose sid matched its registry session_id (tallied alive), PLINY_the-stoa (heads-down) was simulated wire-shape-faithfully with its registry sid (tallied alive), and CHIRON_the-stoa (status:dead) stayed silent (tallied PRESUMED dead) - tally fully derivable from comments+registry, no passive field, no jq. The load-bearing threat-anchored probes all hold: P2 proved the audit-only sid-match flags an accidental wrong sid while passing a legit one AND canon makes NO authentication/impersonation-defeat claim (38.3); P3 proved the PRESUMED-not-proven honest-claim boundary lives in both homes with a confirm-the-presumption pre-relaunch gate and an executable recovery; P4 proved zero in-scope passive machinery (every scrapped term in a NEGATING/OUT context), the 12-key JSONL schema unchanged, and the SCOPE-FENCE present. P5 regression backstop is green: author audit clean (Denson Smith, no foreign author), gen-data green (no Zod failure), full vitest suite 41/41 passed. ADA's 0.5 drift call is VERIFIED CORRECT: 0.5 indexes ONLY relocated/cut content and inline 36/37 (like new inline 38) carry no 0.5 row - the omission is correct, not a gap; 37 confirmed prior-highest so 38 is the correct next integer. No falsification. VERDICT: pass.
gap_or_blocker: ""
