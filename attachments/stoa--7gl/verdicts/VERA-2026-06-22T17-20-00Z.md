status: completed
ticket: stoa--7gl
verdict: pass
design_artifact_verified_against: agents/design/stoa--7gl/design-rev3.md (entry; authoritative-rev map: rev3 DC3+P11, rev2 DC0+P7, rev1 the rest)
build_verified: arc-71/build @ bf57c40 (worktree HEAD 31eec4b = design commit atop the build commit)

probes_executed:
- probe_id: P1
  description: decision-register composes (two-owner MODULE-INLINE) into BOTH deployed POLYBIUS and PLINY; module source exists.
  command_or_method: grep MODULE-INLINE:decision-register in both source role files; REAL throwaway subproject install (install --dry-run early-returns past recompose per project memory) then assert full body inlined into both deployed hosts.
  expected: marker pair in each source host; module source present; full 250-line body inlined into both recomposed hosts.
  observed: source POLYBIUS L141/142 + PLINY L330/331 marker pairs; substrate/modules/decision-register.md present (17128 bytes); both deployed hosts carry the 250-line body inlined; both module lists (install.sh L1264 POLYBIUS_MODULES, L1266 PLINY_MODULES) include decision-register; recompose ran exit 0.
  quadrant_classification: easy-easy
  result: pass
- probe_id: P2
  description: --check-corpus deterministic PASS, 18/18 well-formed.
  command_or_method: bash substrate/modules/tests/decision-register/run-decision-register-corpus.sh --check-corpus
  expected: exit 0, check-corpus PASS (well-formed), 18 manifest entries / 18 fixture files.
  observed: checked 18 manifest entries / 18 fixture files, check-corpus PASS (well-formed), EXIT=0.
  quadrant_classification: easy-easy
  result: pass
- probe_id: P3
  description: vacuity detector negative control. A hollow counter-hypothesis is detectably hollow.
  command_or_method: in-place mutate decided/dec1 COUNTER-HYPOTHESIS to a denylist phrase (we-will-see), run --check-corpus (expect FAIL flagging THAT fixture hollow), restore via git checkout, re-run (expect PASS). Method note in methodology_concerns.
  expected: check-corpus FAILS exit 1 flagging dec1 COUNTER-HYPOTHESIS empty-or-vacuous; after restore PASS 18/18 exit 0; tree clean.
  observed: FAIL fixtures/decided/dec1-ship-now-vs-slip.md write fixture COUNTER-HYPOTHESIS is empty-or-vacuous, EXIT=1; after git checkout, git status clean, --check-corpus PASS 18/18 EXIT=0.
  quadrant_classification: easy-easy
  result: pass
- probe_id: P4
  description: --judge floor eval (DOGFOOD PROXY, judge==verifier; NOT trigger-verified).
  command_or_method: bash run-decision-register-corpus.sh --judge; classify n=18 from the module text ALONE; bash run-decision-register-corpus.sh --judge --score <calls>.
  expected: decided RECALL >=4/5, illuminated SPECIFICITY >=3/4 (load-bearing), problem >=2/3, incidental >=2/3, exit 0.
  observed: decided 5/5, illuminated 4/4 (load-bearing floor met), problem 3/3, incidental 3/3; judge PASS (all per-class floors met) -- DOGFOOD PROXY, not independent verification, EXIT=0.
  quadrant_classification: hard-easy
  result: pass
- probe_id: P5
  description: live nine-field entry lands intact; throwaway ticket created + deleted (NOT the real standing register ticket, per brief override of rev3 standing-ticket wording).
  command_or_method: bw create throwaway (stoa--0fy); bw comment with all 9 single-line fields via the DC3 robust single-quote form; bw show then confirm every field present + unmangled; bw delete --force; confirm gone.
  expected: all 9 fields present + unmangled on re-read; ticket deleted and absent from bw show / bw list --all.
  observed: all 9 fields (DR-ID, WHEN, CHECKPOINT, DILEMMA, WARNING, OPTIONS, CHOSEN, COUNTER-HYPOTHESIS, CONTEXT-LINK) present + intact on bw show; stoa--0fy deleted (exit 0), error no issue found matching stoa--0fy, 0 hits in bw list --all.
  quadrant_classification: easy-easy
  result: pass
- probe_id: P6
  description: over-write guard holds both directions (illuminated/undecided AND problem). NO entry emitted.
  command_or_method: reason from the module section-1 predicate (write iff checkpoint + DILEMMA + path-chosen); corroborate via P4 floors + confirm throwaway ticket carried EXACTLY the 2 deliberate decided entries, zero over-write entries.
  expected: predicate withholds on illuminated/undecided (cond 3 fails) and on problem (cond 2 fails); no spurious entry.
  observed: illuminated direction cond 3 (path-chosen) fails -> no-write (P4 illuminated 4/4 load-bearing); problem direction cond 2 (DILEMMA) fails -> no-write (P4 problem 3/3); throwaway ticket DECISION-REGISTER ENTRY count = exactly 2 (P5+P11), zero over-write entries.
  quadrant_classification: easy-hard
  result: pass
- probe_id: P7
  description: schema is 2b-re-readable by the bare Arc-70 field() INCLUDING full-OPTIONS recovery (FAILS if any option dropped or stored multi-line).
  command_or_method: bare field() (verbatim run-dilemma-corpus.sh:71-80) over all 9 fields of the landed entry (bw-show quote prefix stripped); split OPTIONS on the space-tilde-space delimiter; assert exactly N=3 non-empty tokens equal to the known set + N-1=2 delimiters + no label-shadow.
  expected: all 9 fields recover non-empty; OPTIONS splits to exactly 3 non-empty known tokens; 2 delimiters; no field value begins with a reserved LABEL token.
  observed: all 9 fields recovered non-empty (FIELD_FAILS=0); OPTIONS recovered whole, split to 3 non-empty tokens matching the known set, 2 delimiters; no label-shadow. OPTIONS stored single-line, the rev2 r1 fix holds; P7 would FAIL on multi-line OPTIONS.
  quadrant_classification: easy-easy
  result: pass
- probe_id: P8
  description: source-only. A subproject install deploys NO modules/tests/decision-register/ path and no standalone module file.
  command_or_method: REAL throwaway subproject install; find for tests/decision-register, a modules/ dir, and a standalone decision-register.md under the deploy.
  expected: none of the three present (non-recursive modules/*.md glob; subproject inlines, does not deploy modules/).
  observed: no tests/decision-register path; no modules/ dir deployed; no standalone decision-register.md. Module is inlined, corpus is source-only.
  quadrant_classification: easy-easy
  result: pass
- probe_id: P9
  description: byte-alignment. The schema/template region (and the full body) is byte-identical across source and both recomposed hosts.
  command_or_method: extract the 10-line schema fenced block from source + both deployed hosts and diff; strengthened by diffing the full 250-line inlined body vs source for both hosts.
  expected: schema region identical across all three; full body identical in both hosts.
  observed: schema region diff rc=0 both hosts (10 lines each, IDENTICAL); full 250-line body diff rc=0 both hosts (FULL-BODY IDENTICAL POLYBIUS + PLINY).
  quadrant_classification: easy-easy
  result: pass
- probe_id: P10
  description: honesty-posture grep. No AFFIRMATIVE over-claim that the writing-discipline/counter-hypothesis is mechanically guaranteed.
  command_or_method: grep -iE enforced OR guaranteed OR non-collapsible over decision-register.md + its README.
  expected: only negation-idiom / prohibition / prose-enforced compound hits (same precedent as dilemma-classifier.md); no affirmative over-claim.
  observed: every hit is a negation (NOT a non-collapsible enforcement; NOT a hard non-collapsible gate; not a non-collapsible gate), an explicit prohibition (any phrasing that this is enforced/guaranteed/non-collapsible, do not write it), or the template + prose-enforced + corpus regression-guard NOT a hard gate compound. No affirmative over-claim.
  quadrant_classification: easy-easy
  result: pass
- probe_id: P11
  description: live dollar-AND-apostrophe round-trip. A body with BOTH an apostrophe AND a bare 50k cost figure lands intact (must be able to FAIL on the dollar-mangle).
  command_or_method: bw comment (throwaway stoa--0fy) via the DC3 robust single-quote form, WARNING+others carrying contractions AND the bare cost figure; bw show then assert apostrophe present AND literal cost figure present AND no bare 0k mangle; negative control naive double-quote baseline mangles the cost figure to 0k.
  expected: re-read contains the contractions + literal cost figure + NO bare 0k; double-quote baseline produces 0k (proves the probe can fail).
  observed: re-read WARNING line carried both contractions intact and the literal 50k cost figure (3x in entry) with no bare 0k mangle; negative control double-quote form rendered accepts a 0k revert cost (mangle confirmed). Robust single-quote contract is what passes.
  quadrant_classification: easy-easy
  result: pass

full_close_gate_suite:
- gen-data: DETERMINISTIC + succeeds. Run twice, agents.ts byte-identical AND matches committed build (no drift); roster 4 MAJOR / 12 CAPTAIN / 8 LIEUTENANT, no Zod break, exit 0 both runs.
- vitest: GREEN. 3 files, 41 tests passed, exit 0.
- author-gate tests: GREEN. 29 passed / 0 failed.
- stop-self-check tests: GREEN. 2 passed / 0 failed.
- check-corpus: GREEN. 18/18 well-formed, exit 0 (P2).
- pre-existing-red distinction: NONE red. No this-arc-broke-it finding; full suite clean on arc-71/build.

threat_coverage: [] empty-state carve-out. Design section-1 classifies the arc as not threat-ratified (process/role-file + corpus-tooling change, no runtime attacker, no attack path) per operating-disciplines section-35.5; ARGUS-CONFIRMED. No A3 threat-to-mitigation map; no section-6.13 threat-anchored probe applies. TRM_COUNT=0.

methodology_concerns:
- L1252 stale module-count annotation (install.sh): the comment reads all 35 module sources minus README but the actual count is 41 (verified: ls substrate/modules/*.md minus README = 41; decision-register.md is one of the 41 added this arc). Annotation on a line in a file THIS arc edits (the TWO-OWNER block sits directly below it at L1253-1261); runtime existence count comes from the source glob (L958), so functional impact is NONE. Does NOT falsify any probe; recorded as a methodology/accuracy concern, not a fail. Build-hygiene/CATO domain; PLINY has an open routing thread (gdj) on it. Surfaced, not fixed (independence-of-verification).
- P3 method substitution: used in-place-mutate-then-git-restore on an existing decided fixture rather than the brief literal clone-to-a-fixed-throwaway-path, because ADDING a fixture file trips the corpus orphan + count-mismatch checks and would fail for the WRONG reason (not as hollow). The mutation isolates the DC4 hollow-detect signal exactly, which is P3 design intent; destructive op restored via git, tree confirmed clean.
- P5/P11 ticket-surface: the brief explicitly overrides rev3 wording (write to the standing decision-register-labeled ticket), requiring a throwaway ticket and NO real standing register ticket. Honored: throwaway stoa--0fy created, used, deleted, confirmed gone. The live grounding is on a throwaway surface; bw comment storage is ticket-id-agnostic so no real-register-only mangle is plausible.
- install --dry-run does NOT exercise recompose: per project memory the recompose machine only runs at TARGET=subproject and is not gated by --dry-run (the --dry-run path at install.sh ~L1032 echoes deploy actions and returns before recompose). P1/P8/P9 were verified via a REAL throwaway subproject install (throwaway-clone discipline), the honest exercise; a literal --dry-run would have verified nothing for these three.

falsifying_evidence_summary: empty. Verdict is pass; no probe falsified a design claim and the full close-gate suite is green.

verification_artifacts_path: agents/verification/stoa--7gl/ (p7-entry.txt, schema-src/pol/ply.txt, body-pol/ply.txt, vera-judge-calls.tsv; the throwaway-parent subproject install was created to exercise P1/P8/P9 then removed)

attach_status: PENDING (set by dispatch return after bw attach)

summary: The build was exercised end-to-end against all eleven design probes (P1-P11) across rev1/rev2/rev3 plus the full close-gate suite. The load-bearing probes were P4 (the over-write/over-fire judgment floors, reported strictly as a DOGFOOD PROXY since VERA is both judge and verifier; decided 5/5, illuminated 4/4 load-bearing, problem 3/3, incidental 3/3), P7 (the rev2 r1 fix: single-line delimiter-separated OPTIONS recovers in FULL via the bare Arc-70 field() and splits to exactly N tokens; would have failed on multi-line storage), and P11 (the rev3 DC3 hardening: a body with both an apostrophe and a bare cost figure round-trips intact through the robust single-quote bw-comment contract, with the naive double-quote mangle demonstrated as the failing baseline). Composition (P1) inlines a byte-identical 250-line body into both deployed hosts via a real throwaway subproject install; the corpus is source-only (P8); the schema/template + full body are byte-aligned across source and both hosts (P9); the DC4 vacuity detector fires on hollow then restores clean (P3); the honesty posture carries no affirmative over-claim (P10). The full suite is green with no pre-existing reds and no this-arc-broke-it finding. The single accuracy concern (install.sh L1252 stale 35 vs actual 41 module-count annotation) is a comment-only, zero-runtime-impact item on a file this arc edits, recorded as a methodology concern, surfaced not fixed (independence-of-verification), already in PLINY open routing thread. PASS.
