status: completed
ticket: stoa--7gl
verdict: revise
design_artifact_audited: .claude/worktrees/arc-71-build/agents/design/stoa--7gl/design-rev1.md
audit_block:
  risks:
  - id: r1
    description: DC0 claims all nine schema fields (incl. the multi-line OPTIONS field) are re-readable by "the SAME proven leading-LABEL awk the Arc-70 corpus field() helper uses" — but that field() helper reads ONLY the same line as the label and exits at the first match, so a field() OPTIONS call would silently drop every indented continuation line. The central DC0 forward-constraint (the whole point of the highest-leverage DC) is FALSE for the one multi-line field as currently specified.
    evidence: design-rev1 DC0 schema line "OPTIONS: <... one per line under this label>" + design note "multi-line values for OPTIONS are indented continuation lines under the label ... extracts a field by the same leading-LABEL match the Arc-70 field() awk uses" (design section 2 DC0); CONTRADICTED by substrate/modules/tests/dilemma-classifier/run-dilemma-corpus.sh:71-80 field() — v=substr line, then exit, same-line-only, first-match-only, no continuation handling.
    load_bearing: true
    quadrant_classification: easy-easy
  - id: r2
    description: The DC0 same-field forward-constraint is asserted but its own design probe (P7) does not actually exercise the multi-line-OPTIONS failure mode — P7 runs "the Arc-70 field()-style leading-LABEL awk extractor against a real landed entry for each of the nine fields, each returns its value", which would PASS on a single-line OPTIONS value and never expose the r1 gap. The probe as spec'd cannot falsify the very claim DC0 makes load-bearing.
    evidence: design-rev1 section 3 P7 (schema is 2b-re-readable) asserts each field returns its value but does not assert OPTIONS returns ALL of its option lines; pairs with r1.
    load_bearing: true
    quadrant_classification: easy-easy
  - id: r3
    description: The carve-out rests on "no runtime attack path," which is correct as a security claim. But in the same correctness family as r1, the design writes agent-filled free-prose field values into a shared bw ticket that the deferred 2b reader will machine-parse by leading-LABEL — a future entry whose value text begins a line with a colon-terminated token (e.g. a pasted "WARNING: ..." line inside OPTIONS) could be mis-parsed as a new field by 2b. This is forward parse-robustness, NOT a security attack path; it does NOT defeat the section 35.5 carve-out (no attacker, no privilege/credential/data-exfil surface). Surfaced once for completeness.
    evidence: design-rev1 DC3 (schema field values are plain prose) + DC0 leading-LABEL parse contract; the write is one positional bw comment, no -m, no backticks/dollar-paren (DC3 footgun lock), so no injection/credential surface exists — this is parse-robustness not threat.
    load_bearing: false
    quadrant_classification: easy-hard
  non_findings: |
    - DC2 two-owner composition claim — VERIFIED TRUE against install.sh:1097-1278. dilemma-classifier is already in BOTH POLYBIUS_MODULES (L1262) and PLINY_MODULES (L1264); recompose_module_inline runs once-per-file with that file's own owned-set (L1270/L1272); Check A builds the GLOBAL existence set from the filesystem glob modules/*.md (L1121) so a new decision-register.md source is picked up automatically; Checks B/D are per-file owned-scoped so two independent marker pairs never collide. Adding decision-register to both owned-sets + a marker pair per host needs NO recompose-awk change. No Check-A/B/D/E collision hazard. The second-two-owner-module claim holds.
    - P9 byte-alignment claim — sound; recompose copies the source verbatim (L1169 getline loop), so the two inlined copies are byte-identical by construction; the build-time diff over the schema/template region mirrors save-verdict P8 correctly.
    - P8 source-only (no deploy leak) — sound; install.sh globs modules/*.md NON-RECURSIVELY (L1121), so modules/tests/decision-register/ never deploys, identical to the Arc-70 tests/dilemma-classifier/ precedent.
    - DC1 over-write guard — 3-condition predicate is sound; the load-bearing illuminated-but-undecided direction is distinguished by condition (3) "a path was actually chosen", honestly scored as a judge floor not a shell check (A1). Guard does not fire on problem / illuminated-undecided / incidental. No guard hole found.
    - DC4 honesty posture — HELD. Template+prose-enforced + corpus regression-guard, explicitly NOT a hard non-collapsible gate; vacuity denylist honestly framed as a SEED (WP-2); no enforced/guaranteed/non-collapsible language; P10 grep guards the deliverable. Honest-stance intact.
    - DC5 hollow/ negative control — correct; the hollow class is a should-write-but-vacuous fixture set that --check-corpus must FLAG, proving the DC4 detector catches what it should (P3 negative control). check-corpus(deterministic)/judge(judgment) split is honest and mirrors Arc-70.
    - 2b boundary — NO reach. Section 5 defers callback / re-verify gate / dose-calibration / per-user visibility scoping; DC0 specifies the schema-for-reading and explicitly builds NO reader. The forward-constraint is named, not built. Clean.
    - DC3 inline-template-vs-helper — value-weighted call (keep field-writing visible for DC4 + resolves-at-subproject) honestly named; the save-verdict helper is correctly judged over-engineering for a single bw comment (no file/attach/sha256). Acceptable as a named tradeoff.
  threat_coverage_assessment: N/A — section-35.5-carved-out arc (process/role-file + corpus-tooling change, ARGUS-CONFIRMED carve-out, see adjudication (a)); no threat-ratified mitigation, so no threat-anchored probe is required. Coverage layer is a no-op here by design.
attach_status: <SET-FROM-attach_rc>
summary: |
  The design is a clean, honest capture-only slice that correctly mirrors the Arc-70 read module's
  shape and honesty posture: a new two-owner decision-register.md module writing one structured
  leading-LABEL bw entry per DECIDED dilemma, an over-write guard tested both directions, and a
  source-only mirror corpus. Five of six load-bearing seams RATIFY against ground truth (DC1 guard,
  DC4 honesty device, the 2b non-reach, DC2 composition mechanics verified line-by-line against
  install.sh, and DC5 corpus/floors). The one load-bearing problem is at the HIGHEST-leverage DC:
  DC0's central forward-constraint claim — that all nine fields are re-readable by the Arc-70 field()
  awk — is FALSE for the multi-line OPTIONS field, because that helper is same-line/first-match-only
  and silently drops continuation lines (r1); and the design's own P7 probe is spec'd in a way that
  cannot expose the gap (r2). This is exactly the seam DC0 exists to get right (the schema the
  deferred 2b reader keys on), so it is revise-blocking — but it is a narrow, easy-easy fix surface,
  not a structural problem with the design. Posture: minor structural revision at DC0; everything
  else is build-ready. All three residual questions adjudicated: (a) carve-out CONFIRMED; (b) DC2
  weighting RATIFIED; (c) DC5 floor set RATIFIED with one watch (the load-bearing illuminated
  SPECIFICITY floor at >=3/4 is the most corrosive miss direction and is defensibly the right seed
  bar; a case for >=4/4 exists but n=4 granularity makes 3/4-vs-4/4 a one-fixture flip, so the
  current floor is an honest seed, not a defect).
