status: completed
ticket: stoa--pk4
verdict: revise
design_artifact_audited: C:\Users\denso\claude_projects\the-stoa\.claude\worktrees\arc-68-build\agents\design\stoa--pk4\design-rev1.md
verdict_label_for_orchestrator: PASS-WITH-CONDITIONS

audit_block:
  risks:
  - id: r1
    description: P6(a) does not force the bash-fallback branch of record-seat fix (a); on a machine where bw IS on the pwsh PATH it passes via the Get-Command branch and never exercises the fallback the actual bug requires. The bash-fallback branch passes a Windows backslash temp path (Join-Path GetTempPath, e.g. C:\Users\...\Temp\stoa-seat-registry.jsonl) into git-bash bw via bash -lc; whether git-bash bw accepts that C:\... backslash path is NOT verified by the design. The FM success-proof was a bash-NATIVE invocation, NOT the pwsh->bash -lc path the fix proposes. On the affected machine Get-Command bw returns nothing in pwsh, so the bash-fallback IS the primary path and its path-format handling is the load-bearing unknown.
    evidence: design-rev1.md S2.6 fix(a) L177-184 + S4 P6(a) L257; record-seat.ps1 L108; ground-truth this turn.
    load_bearing: true
    quadrant_classification: easy-hard
  - id: r2
    description: The design S2.6 rationale that tmp is a fixed-literal path with no spaces by construction is WRONG -- tmp = GetTempPath()+filename, and GetTempPath() includes the user-profile dir, which CAN contain spaces (a Windows username with a space). The fix CODE is safe (it single-quotes tmp inside the bash -lc string; Windows paths cannot contain a single-quote), but the safety derives from the quote-wrapping, NOT from the path being space-free. The build must not rely on the stated false rationale: a future edit dropping the quotes reintroduces the break. The mis-stated rationale is the hazard, not the current code.
    evidence: design-rev1.md S2.6 L187 parenthetical; record-seat.ps1 L108; ground-truth GetTempPath this turn.
    load_bearing: false
    quadrant_classification: easy-easy
  - id: r3
    description: P6(b) error-attribution probe relies on forcing ticket to a bogus id via a test shim or a no-such-ticket workspace -- but ticket is a hardcoded constant (stoa--reg, record-seat.ps1 L58), NOT a parameter, so it cannot be forced bogus without editing the script under test. The probe has no clean injection point to exercise the exit-N (real attach failure / wrong-ticket) branch distinctly from the PATH-miss branch; it risks testing only the PATH-miss path and tautologically passing the distinction it claims to verify.
    evidence: design-rev1.md S4 P6(b) L258; record-seat.ps1 L58.
    load_bearing: false
    quadrant_classification: easy-easy
  - id: r4
    description: RQ1/W2 -- forcing -Layout Windows whenever the chain preamble is injected makes Windows the de-facto default for EVERY routine say-trigger launch (preamble prepended on all activation paths per S2.1 Edit B), because a multi-word positional prompt cannot pass robustly through wt split-pane without quoting the launcher does not do. This silently abolishes the documented DEFAULT panes UX (launch-team.ps1 L72 default Panes; SKILL.md L10 Panes DEFAULT). Web-verified: a multi-word wt split-pane prompt needs stop-parsing/escaping the say-path does not apply (wtArgs += p unquoted, L190). The break is TRANSPORT (pane arg re-split), NOT trigger semantics -- the say-trigger is SEMANTIC (CLAUDE.md L1625 When the PRINCIPAL invokes POLYBIUS read that file), so a preamble that ends with the bare word still fires IF delivered intact. The build must make this choice EXPLICITLY rather than defaulting into a UX regression.
    evidence: design-rev1.md S2.1 Edit B L58-62 + W2 L281 + RQ1 L288; launch-team.ps1 L72/L122-125/L185-190; install.sh L1625; SKILL.md L10; gsearch this turn.
    load_bearing: true
    quadrant_classification: easy-easy
  - id: r5
    description: P5 demonstrates ONLY that clause (E) text is PRESENT in the emitted decision-block reason JSON on the working Stop channel -- the same bar clauses A/D meet, which DoD bullet 4 honestly accepts as the advisory floor. But the DoD bullet-4 PRIMARY ask is demonstrate the mechanism actually fires on the AR-7 failure shape. Clause (E) injects on EVERY orchestrator-class Stop turn regardless of whether CAPTAINs were dispatched-without-PLINY (no transcript classification, W1; W4: cannot distinguish a legit solo build-session, which PLINY L44 blesses). So fires on the AR-7 shape is met only weakly; the selective sense is NOT met (the named W1 residual). The design is honest about this -- a coverage/expectation-precision flag, not concealment -- but the build + FM go/no-go must record that DoD bullet 4 is met via the accepted advisory branch, NOT selective detection.
    evidence: design-rev1.md S2.2 L85 + P5 L253-254; directive DoD L77; W1 L280; W4 L283 + MAJOR_PLINY.md L44.
    load_bearing: true
    quadrant_classification: hard-hard
  - id: r6
    description: The design says S37 is the next free number and S36 is the last -- accurate that S36 is the last NUMBERED section, but operating-disciplines.md has UN-numbered appendix sections AFTER S36 (Agent-regime inverses L1660, Empirical lineage L1671). The new S37 must be inserted BETWEEN S36 and those appendices, NOT appended at EOF, or the numbered sequence is broken by the trailing appendices.
    evidence: ground-truth grep this turn -- operating-disciplines.md L1476/L1660/L1671, 1681 lines; design-rev1.md S2.5 L153.
    load_bearing: false
    quadrant_classification: easy-easy
  - id: r7
    description: P5 claim that the tests dir has the pattern overstates the scaffolding -- substrate/hooks/tests/ has only run-author-gate-tests.sh (PreToolUse author-gate) + fixtures + README; there is NO existing stop-self-check test to pattern P5 on. P5 is still feasible (echo json | bash stop-self-check.sh; _hooklib.sh provides read_stdin_event/event_field/allow), but the build should author the stop-hook fixture fresh, not assume one exists.
    evidence: ground-truth this turn -- substrate/hooks/tests contents; _hooklib.sh L22/L70/L258; design-rev1.md S4 P5 L254.
    load_bearing: false
    quadrant_classification: easy-easy

  non_findings:
  - RQ2 / S5 bash -lc INJECTION SURFACE -- DISCHARGED. ticket, tmp, attachName interpolated into fix(a) bash -lc are ALL script-internal constants (record-seat.ps1 L58/L63/L108) -- none a parameter or caller/agent-controlled. No untrusted content reaches the bash string. DAEDALUS pre-empt correct; the S35.5 not-threat-ratified (process change, no runtime attack path) carve-out CONFIRMED.
  - DC3 chain-placement faithfulness -- DISCHARGED. CHIRON + HAMILTON role files literally say Lives at top-level Claude Code session engaged at design-time (terminal seats, not Agent sub-agents -> launcher-spun correct, PLINY-dispatch impossible), You answer to MAJOR_POLYBIUS, and step back so POLYBIUS/PLINY run it. Chain diagram (S2.3) faithful.
  - CHIRON/HAMILTON have-no-say-trigger -- DISCHARGED. Deployed CLAUDE.md (install.sh L1625 block) carries ONLY the POLYBIUS bare-word trigger; no chiron/hamilton bare-word in any CLAUDE.md. The claim they need a full seeded prompt is correct.
  - stop-self-check working-channel claim -- DISCHARGED. Emits decision-block+reason (L120); A/B/C in REASON (L90-94) + D appended (L106-115); infinite-block guard via per-(session,transcript-size) sentinel (L73-85) + stop_hook_active (L48-49); FAIL-OPEN on any error. Clause (E) on the same channel inheriting once-per-turn is sound. PostToolUse additionalContext-broken matches the hook header (L12-15).
  - DC4 additive-fields non-divergence -- DISCHARGED. No existing Composition/Gauntlet/ChainRole params (no collision); ordered-hashtable JSONL row (L92-102) tolerant of heterogeneous rows; optional params + safe defaults mean omitting-callers still write valid rows. Per-seat (no composition-id) shape (W3) is additive-later, not a divergence.
  - Scope-adherence -- DISCHARGED. Inside all out-of-scope lines: no Arc-67 rebuild, no u--9s2 BUILD, no hook-arming (ENABLE_HOOKS=0, A3), no stoa--tg7. install.sh needs no name-list edit (record-seat intra-skill, clause E intra-hook).
  - od S36/S37-free -- confirmed S36 last NUMBERED, S37 free (see r6 placement caveat re trailing appendices).

  threat_coverage_assessment: N/A -- Arc 68 is launcher-tooling + process/role-file hardening with no runtime attack path; ARGUS CONFIRMS the S35.5 carve-out not-threat-ratified (process change, no runtime attack path). AR-7 is a correctness/discipline failure shape, not a security threat. No threat-ratified mitigation -> no threat-anchored probe required. Honest-claim boundary (S35.5): coverage assessed, not threat-defeat-in-general.

attach_status: pending-save-procedure
summary: >
  Arc 68 design-rev1 is sound, honest, and well-grounded; ARGUS verified its ground-truth claims line-by-line
  against the actual files (launcher L122-125/L169-173/L226-252; record-seat L58/L108/L118-128; stop-self-check
  decision-block channel + clause A/D + infinite-block guard + fail-open; CHIRON/HAMILTON role files; od S36-last).
  The DC0 layered structural model is honest about its advisory residual (W1) and is DoD-conformant. DC1-DC4
  mechanics, DC3 chain-placement, and the S35.5 process-change carve-out (RQ2) hold. VERDICT: PASS-WITH-CONDITIONS
  (revise). Top load-bearing risk r1: the record-seat PATH fix bash-fallback branch -- the PRIMARY path on the
  very machine the bug manifests -- passes a Windows backslash temp path into git-bash bw, and whether git-bash bw
  accepts that path is UNVERIFIED; P6(a) as written can pass without ever exercising that branch (exactly the
  FM/user-tier WI-1 concern, independently confirmed). Conditions: C1(r1) VERA P6(a) MUST force+prove the
  bash-fallback branch writes a real registry row through git-bash bw on a pwsh-bw-unreachable shim; C2(r4/RQ1)
  make an EXPLICIT recorded decision on forcing -Layout Windows as the new say-launch default vs preserving panes
  via a shorter/robustly-quoted preamble; C3(r5) record DoD bullet 4 is met via the accepted advisory branch,
  NOT selective firing on the AR-7 shape (W1); C4(r2) do not carry the false tmp-no-spaces rationale into code;
  C5(r3) give P6(b) a real injection point since ticket is a hardcoded constant; C6(r6) insert S37 before the
  trailing un-numbered appendices; C7(r7) author a fresh stop-hook test fixture. r1/r4/r5 load-bearing;
  r2/r3/r6/r7 bounded. No structural BLOCK -- buildable once the conditions are honored.
gap_or_blocker: none (status completed)
