status: completed
ticket: stoa--pk4
verdict: pass
design_artifact_verified_against: agents/design/stoa--pk4/design-rev3.md (§2.6 + §4 P6)
build_verified: arc-68/build @ ec72ed5 (substrate/skills/team-launcher/record-seat.ps1)
probes_executed:
- probe_id: p6a
  description: C1 GATE — FORCE the git-derived resolution THROUGH the real WSL-shadow PATH + PROVE a real stoa--reg row round-trips. The proof rev2 could not produce.
  quadrant_classification: easy-easy — the WSL-shadow failure surfaces immediately (Get-Command bash = system32 shadow) and the fix is verified by a single mechanical round-trip (run + git-show + assert).
  command_or_method: "pwsh -NoProfile -File agents/verification/stoa--pk4/p6a-probe.ps1 — strips bw-dir (C:\\Users\\denso\\bin) from $env:PATH so Get-Command bw=null; prepends C:\\windows\\system32 so Get-Command bash=WSL shadow; dot-sources Resolve-GitBashWithBw (lines 172-206) from the file under test; runs record-seat.ps1 -Seat probeFallback ...; asserts row round-trips via git show beadwork:attachments/stoa--reg/seat-registry.jsonl | ConvertFrom-Json | Where seat -eq probeFallback. FALSE-PASS-GUARD-CLEAN: no git-bash fronting, no git-bash prepended ahead of system32, no bw shim onto pwsh PATH."
  expected: "Get-Command bw=null; Get-Command bash=C:\\windows\\system32\\bash.exe; Resolve-GitBashWithBw returns a git-bash != the WSL shadow whose command -v bw exits 0; record-seat.ps1 exit 0 + success Write-Host; probeFallback row round-trips."
  observed: "Get-Command bw=<null>; Get-Command bash=C:\\windows\\system32\\bash.exe (the broken WSL shadow); Get-Command git=C:\\Program Files\\Git\\mingw64\\bin\\git.exe (un-shadowed). Resolve-GitBashWithBw -> C:\\Program Files\\Git\\bin\\bash.exe (NOT the WSL shadow); command -v bw via -lc -> /c/Users/denso/bin/bw, exit 0. record-seat.ps1 exit 0; emitted 'attached seat-registry.jsonl to stoa--reg' + the green success Write-Host. probeFallback row round-tripped from beadwork with session_id matching the probe uuid (72052dfc-a003-4f09-858f-7eaf49c0c7e0)."
  result: pass
- probe_id: p6b-bucket2
  description: resolution-failure bucket (the NEW, load-bearing case) — no bw on pwsh PATH AND no git-derivable git-bash carrying bw -> Resolve-GitBashWithBw returns $null -> LOUD resolution-failure message, NOT a bw/ticket error. Kills CONSEQUENCE 2.
  quadrant_classification: easy-easy — failure forced deterministically by a throwaway git-shim env; message asserted by a single grep over the captured warning.
  command_or_method: "pwsh -NoProfile -File agents/verification/stoa--pk4/p6b-bucket2-probe.ps1 — keeps git RESOLVABLE via a throwaway git.cmd shim (so record-seat line-93 'git show' survives) under a bash-less shim tree, strips bw, leaves only WSL system32 bash reachable -> Resolve-GitBashWithBw finds zero candidates -> $null."
  expected: "exit 1; warning contains 'PATH/RESOLUTION failure' + 'is NOT git-bash and is intentionally skipped'; warning does NOT contain the bw-error 'bw resolved and ran' language NOR the old benign 'expected on a workspace without the registry ticket' language."
  observed: "exit 1; warning = 'bw could NOT be resolved: not on the pwsh PATH, and no git-bash carrying bw was found ... This is a PATH/RESOLUTION failure, NOT a missing-ticket or bw-error case ... WSL's C:\\windows\\system32\\bash.exe (if present) is NOT git-bash and is intentionally skipped (Arc 68 / stoa--pk4 rev3).' PATH/RESOLUTION msg=True, WSL-skip note=True, NOT bw-error=True, NOT benign=True, exit1=True."
  result: pass
- probe_id: p6b-bucket3
  description: bw-error bucket — bw resolvable, attach genuinely errors (invalid ticket id, confirmed exit 1 on bw 0.13.1) -> bw-error message, distinct from the resolution-failure message + carries the auto-create NOTE.
  quadrant_classification: easy-easy — trigger pre-confirmed to exit 1 on the live bw version; message asserted by grep.
  command_or_method: "pwsh -NoProfile -File agents/verification/stoa--pk4/p6b-probe.ps1 (bucket-3 leg) — record-seat.ps1 ... -Ticket 'not a valid id!!!'. Pre-verified live: bw attach with that ticket -> 'error: invalid ticket id' exit 1 on bw 0.13.1 (and -Ticket bogus--nope auto-creates exit 0, so the design's methodology fix is confirmed)."
  expected: "exit 1; warning contains \"bw attach to 'not a valid id!!!' exited 1\" + the 'may AUTO-CREATE' NOTE; does NOT contain the resolution-failure 'PATH/RESOLUTION failure' language."
  observed: "exit 1; warning = \"record-seat: could not record seat 'probeBwErr' ... bw attach to 'not a valid id!!!' exited 1 (bw resolved and ran, but the attach failed ...). NOTE: a non-existent ticket may AUTO-CREATE on this bw version ...\" bw-error msg=True, auto-create NOTE=True, NOT resolution msg=True, exit1=True. DISTINCT from bucket 2 (resolution msg present in b2, absent in b3; bw-error msg present in b3, absent in b2)."
  result: pass
- probe_id: p4
  description: DC4 composition/gauntlet/chain_role round-trip + idempotent re-record, exercised THROUGH the new rev3 resolution fallback (bw stripped from pwsh PATH).
  quadrant_classification: easy-easy — record then read-back then assert field equality; mechanical.
  command_or_method: "pwsh -NoProfile -File agents/verification/stoa--pk4/p4-probe.ps1 — under the WSL-shadow/bw-stripped PATH: record probeP4 with Composition='custom-agent+workflow' Gauntlet='waived:probe-test' ChainRole='team-architect'; read back; re-record same seat+machine with different fields; assert row count stays 1 and fields replaced."
  expected: "first record exit 0; composition/gauntlet/chain_role round-trip; re-record exit 0; exactly 1 probeP4 row after re-record; re-record replaces fields (composition=standard, chain_role=orchestrator)."
  observed: "first record exit 0; composition='custom-agent+workflow', gauntlet='waived:probe-test', chain_role='team-architect' all round-tripped True. Re-record exit 0; rows before=1 after=1 (idempotent, no dup); fields replaced (composition=standard, chain_role=orchestrator)=True. All through the git-bash fallback resolution (bw not on pwsh PATH)."
  result: pass
- probe_id: cleanup
  description: restore stoa--reg to EXACT baseline (2 real rows) after the probe-row writes; verify sha + no stray attachments.
  quadrant_classification: easy-easy — read-modify-rewrite-attach + sha256 round-trip.
  command_or_method: "pwsh -NoProfile -File agents/verification/stoa--pk4/cleanup.ps1 (drops probe seats, re-attaches the 2 real rows verbatim); then authoritative Bash check 'git show beadwork:attachments/stoa--reg/seat-registry.jsonl | sha256sum' (identical method to baseline capture); plus git ls-tree scan for stray probe attachments."
  expected: "registry = 2 real rows (POLYBIUS_the-stoa + PLINY_the-stoa); sha256 = baseline f29a2166371cd31d1ba8646ffe282e6e69c712d31d17bd71b581c79d80465e97; no stray probe attachments."
  observed: "registry restored to exactly POLYBIUS_the-stoa + PLINY_the-stoa; authoritative git-show|sha256sum = f29a2166371cd31d1ba8646ffe282e6e69c712d31d17bd71b581c79d80465e97 (EXACT baseline match); only attachments/stoa--reg/seat-registry.jsonl present under stoa--reg; no probe-named attachments created (probe rows were registry-row mutations, fully reverted)."
  result: pass
threat_coverage: []
# threat_coverage empty: arc is not threat-ratified (process change, no runtime attack path) — §35.5 carve-out.
# design-rev3 §2.6 / RQ2' confirms the resolution reads only the system-controlled git install location
# (Get-Command git + filesystem walk-up) and invokes the derived bash with POSITIONAL, script-internal/
# launcher-set args; no untrusted/agent input reaches the resolution or the bash string. No threat-anchored
# probe required. (CATO independently re-confirms RQ2' per the PLINY/POLYBIUS routing.)
methodology_concerns:
- "DESIGN PROBE-SPEC DEFECT (non-blocking, code is correct): design-rev3 §4 P6(b) bucket-2 literal setup ('set $env:PATH to a minimal value that contains neither bw nor git' OR 'remove git from PATH') does NOT reach the resolution-failure bucket. record-seat.ps1 line 93 runs '& git show beadwork:...' FIRST under $ErrorActionPreference='Stop'; with git absent that throws a terminating 'term not recognized' error and the script aborts BEFORE Resolve-GitBashWithBw / the fix(b) buckets ever run (observed: script aborts, $LASTEXITCODE empty, no warning, no bucket). To exercise bucket 2 faithfully, git must stay RESOLVABLE (line-93 read survives) while Resolve-GitBashWithBw returns $null (no git-derived bash carries bw, bw not on pwsh PATH). I used a throwaway git-shim trigger that satisfies this; the bucket then fires correctly. RECOMMENDATION: DAEDALUS revise the P6(b) bucket-2 spec to 'git resolvable but no git-bash carrying bw derivable' (e.g. shim git / point candidates away from bw) rather than 'remove git'. This is a probe-spec wording fix, not a code defect — the resolution-failure bucket itself is correctly wired and fires (proven)."
- "FIDELITY NOTE (not a concern, recorded for transparency): the Bash-tool-launched pwsh has bw on PATH (C:\\Users\\denso\\bin\\bw.exe) and a -NoProfile Get-Command bash resolves git-bash, UNLIKE a real desktop fresh terminal where bw is login-profile-only and the WSL shadow leads. P6(a)/P4/p6b-bucket2 reproduced the real failing conditions explicitly (strip the bw dir -> Get-Command bw=null; prepend system32 -> Get-Command bash=WSL shadow), so the REAL resolution path was exercised regardless of the launching shell's profile state. The false-pass-guard is satisfied: no shim fronts git-bash and no bw is shimmed onto the pwsh PATH; the test installs the BROKEN state and forces the rev3 derivation to do the work."
falsifying_evidence_summary:
verification_artifacts_path: agents/verification/stoa--pk4/ (p6a-probe.ps1, p6b-probe.ps1, p6b-bucket2-probe.ps1, p4-probe.ps1, cleanup.ps1)
attach_status: PENDING
summary: "RE-PROVE of the Arc 68 C1 gate after the rev3 fix (ec72ed5). The load-bearing P6(a) gate PASSES through the REAL WSL-shadow PATH: under Get-Command bw=null + Get-Command bash=C:\\windows\\system32\\bash.exe (the exact broken shadow that failed rev2), Resolve-GitBashWithBw derives git-bash FROM the un-shadowed git, skips the WSL shadow, selects C:\\Program Files\\Git\\bin\\bash.exe where command -v bw succeeds via -lc (/c/Users/denso/bin/bw, exit 0), and record-seat.ps1 writes a real probeFallback row that round-trips from beadwork at exit 0 — the proof rev2 could not produce, false-pass-guard-clean. P6(b) confirms the 4-bucket fix(b): the NEW resolution-failure bucket fires LOUDLY (kills CONSEQUENCE 2 — a wrong/broken-bash failure now reports AS a PATH/RESOLUTION failure naming the WSL skip, never as a bw/ticket error) and is distinct from the bw-error bucket (genuine invalid-ticket-id attach error, exit 1, with the auto-create NOTE). P4 confirms the DC4 composition/gauntlet/chain_role fields round-trip + idempotent re-record THROUGH the new resolution. stoa--reg restored to its EXACT baseline (sha256 f29a2166, 2 real rows, no stray attachments). One non-blocking methodology concern: the design's literal P6(b) bucket-2 setup aborts at line-93 git-show before the bucket; the corrected trigger (git resolvable, no bw-bash derivable) fires the bucket correctly — a probe-spec wording fix for DAEDALUS, not a code defect. P1-P3/P5/P7-P9 + full app suite cited as PASS at 8f3f2f9 per the brief: rev3 touched ONLY record-seat.ps1's bw-resolution block; P4 (the only DC4 probe whose data path runs through record-seat) was re-run and passes through the new resolution, so no other probe's premise is disturbed."
