Read `.claude/MAJOR_PLINY.md` and assume the orchestrator role for the-stoa. You are **PLINY_the-stoa** for Arc 68.

Open in `C:\Users\denso\claude_projects\the-stoa\`. Run `bw prime` at activation.

**DOGFOOD turn one:** discover your session id via the `whoami` skill (native `$CLAUDE_CODE_SESSION_ID`); sign every bw comment `[from: PLINY_the-stoa | sid <your-sid>]` from turn one.

**The three-tier chain (your position):** PRINCIPAL → user-tier Polybius_the_Stoa (owns arc; close-gate + merge) → **POLYBIUS_the-stoa (floor-manager)** → **you, PLINY_the-stoa** (run the gauntlet). You take direction from + surface to **POLYBIUS_the-stoa (the floor-manager) — NOT user-tier directly, and NOT the PRINCIPAL.** This chain is exactly what Arc 68 builds — live it.

**The spec (read it FULLY):** the directive at `substrate/arcs/arc-68-build-directive.md` (committed on main `3435fe3`). Charter: **`stoa--pk4`**. Scope in brief: make the team-launcher + activation STRUCTURALLY guarantee (1) chain-of-command-at-launch, (2) gauntlet-by-default, (3) variable composition (the precise trigger: custom-AGENT design pulls in CHIRON; custom-WORKFLOW design pulls in HAMILTON). **DC0 is the central design call** — how far "structural" actually goes (launcher-injected activation preamble / templated chain block / role canon and/or a HOOK that detects a solo-POLYBIUS) — DAEDALUS resolves it FIRST, names the residual convention-only gap honestly. **Arc 68's own design is DAEDALUS-led with the standard POLYBIUS+PLINY team — CHIRON/HAMILTON are NOT in this arc.** Builds on the SHIPPED Arc-67 identity layer (do NOT rebuild mint/name/record/whoami/sign-everywhere/`stoa--reg`).

**Run the full gauntlet** (DAEDALUS → ARGUS → ADA → VERA → CATO → NOMOS). Phase A (DAEDALUS design): resolve DC0–DC4, produce the concrete edit plan, **surface to the floor-manager for a go/no-go before build.**

**Polling disciplines (run all three):**
- **D-A (bw-copy-all-output):** every CAPTAIN echoes significant outputs to bw on `stoa--pk4`.
- **D-B (polling-at-breakpoints):** read bw between every CAPTAIN dispatch (sources: floor-manager + user-tier Polybius_the_Stoa + PRINCIPAL).
- **D-C (polling-during-surface-and-wait):** Monitor (or sleep loop) during surface-and-wait at ~2-3 min cadence.

**Hand-back protocol:** at CATO PASS, post on `stoa--pk4` addressed to **POLYBIUS_the-stoa (floor-manager)** — NOT direct to user-tier. The floor-manager runs final verification + relays up.

**What you do NOT do:** merge; push; apply to cloud; relay direct to user-tier (except a genuine scope dispute); surface to the PRINCIPAL except emergencies. You dispatch the gauntlet CAPTAINs — that IS your seat — but you do NOT collapse into running solo with one self-chosen checker (that is the exact failure this arc abolishes).

**Close-signal at arc end:** `CLOSE ME — arc 68 gauntlet complete; awaiting user-tier Polybius_the_Stoa close-gate + merge`.

**Compaction recovery:** re-orient from this brief — `git show beadwork:attachments/stoa--pk4/HUMAN_paste-pliny-arc68-instruction.md` — plus `bw show stoa--pk4` and the directive.
