# Engagement brief — PLINY_the-stoa (orchestrator) — Arc 62 (MAJOR_HAMILTON build)

Read `.claude/MAJOR_PLINY.md` and assume the orchestrator role for the-stoa. You are **PLINY_the-stoa** for this engagement. Run `bw prime` at activation.

## Three-tier chain (your position)

PRINCIPAL → user-tier POLYBIUS (close-gate + merge) → POLYBIUS_the-stoa (floor-manager) → **YOU, PLINY_the-stoa (orchestrator)** → CAPTAINs. You surface to the **FLOOR-MANAGER**, NOT to user-tier direct.

## Scope

Build the **MAJOR_HAMILTON** seat — D1–D5 in **`stoa--yh2.1`** (the epic body is authoritative): author `substrate/MAJOR_HAMILTON.md` + render `docs/major-hamilton.html` + wire `install.sh` deploy (all tiers, `HAMILTON_<slug>`) + app gen-data regen & test (now **4 MAJORs**) + reciprocal CHIRON↔HAMILTON cross-refs. IN/OUT-of-scope exactly as the epic states — resist drifting into the skills-housekeeping pass (separate, after) or touching CHIRON (landed Arc 61).

**TEMPLATE:** `MAJOR_CHIRON.md` + `docs/major-chiron.html` + the `install.sh` CHIRON deploy wiring (main @24b79cc). HAMILTON mirrors the SHAPE; domain differs (choreography/workflows, not cast/roster). Charter: `stoa--yh2` + the `p41` "MAJOR_HAMILTON charter" comment. Forward work on an **`arc-62/build`** feature branch (pre-branch hygiene first).

## Run

Full gauntlet DAEDALUS design → ARGUS → ADA → VERA → CATO → NOMOS. Surface the **DESIGN-LOCK** to the floor-manager post-ARGUS / pre-ADA (the Colonel call). ARC-61 verification lesson: the recompose FAIL-LOUD Checks A–E can ONLY be exercised by a **REAL (non-dry-run) recompose** — put a real-recompose probe in VERA's plan (HAMILTON owns no module, so it should be no-op-clean — confirm it, don't assume).

## Polling (all three disciplines)

- **D-A** (copy-all-output): every CAPTAIN echoes significant outputs to bw on `stoa--yh2.1`.
- **D-B** (poll-at-breakpoints): read bw between every CAPTAIN dispatch (floor-manager + user-tier POLYBIUS + PRINCIPAL).
- **D-C** (poll-during-surface-and-wait): Monitor/sleep at ~2–3 min cadence during surface-and-wait.

## Hand-back

At CATO/NOMOS PASS, post on `stoa--yh2.1` addressed to **POLYBIUS_the-stoa (floor-manager)** — NOT direct to user-tier. The floor-manager runs final verification + relays up.

## You do NOT

Merge, push, apply to deployed instances, relay direct to user-tier (except scope disputes), or surface to PRINCIPAL except emergencies.

## Close-signal

At arc end: `CLOSE ME — stoa--yh2.1 gauntlet complete; awaiting user-tier POLYBIUS close-gate + merge`.

## Compaction-recovery

Re-fetch this brief: `git show beadwork:attachments/stoa--yh2.1/HUMAN_paste-pliny-yh2.1-instruction.md`. bw syntax: positional `bw comment <id> "text"` (never `-m`).
