<!-- author: Denson Smith ; generated by builder_deploy_core.provision.render_tripwire_attestation (stoa--k48) -->

# TRIPWIRE-held attestation (the 3 structural walls)

W1 — NO I/O imports anywhere under provision/:
  - AST test (authoritative): every module's top-level imports are a subset of
    {builder_deploy_core.*, dataclasses, enum, typing, abc, __future__} (+ sys for __main__ only).
  - The forbidden-I/O token set is tripwire.w1_forbidden_tokens() (the fragment SSoT); it currently resolves to 9 tokens at runtime: subprocess | socket | urllib | requests | httpx | gcloud | railway | os.system | popen.
  - Grep attestation: the §5 W1 grep over provision/ for those tokens returns EMPTY (exit 1) over the WHOLE tree (checklist.py + tripwire.py INCLUDED) — the source carries no joined alternation literal, so grep AND the AST test AGREE (both clean).

W2 — only the mock concrete port exists:
  - Provisioner.__subclasses__() == [MockProvisioner]; Choreographer(None) raises TypeError (the constructor requires a port); MockProvisioner is instantiable (it implements every @abstractmethod incl select_base_image).

W3 — the emit side has no port at all:
  - emit_spec takes no provisioner parameter; it is a pure transform (no I/O).
