status: completed
ticket: stoa--p41.2
verdict: revise
design_artifact_audited: agents/design/arc-64/design-rev1.md

OVERALL: RATIFY-WITH-CONDITIONS (verdict: revise). Core mechanisms are sound and ground-verified: bw attach is real and byte-durable, the Q-A bash assert fixes the j2i lowercase-p bug, A3/Q-B reconciliation is correct, Q-C=C2 lands on the right call. But FOUR load-bearing gaps (two are directive-DoD MUST-FIX) are under-specified and would fail the P5 dangling-pointer and install.sh probes if built as written.

audit_block:
  risks:
  - id: r1
    description: prune-scan exemption (install.sh L1780, case for check-substrate-updates and check-bw-release) is an EXPLICIT list NOT containing validate-spec or inspect-script-output; after their SKILL.md is git-removed they leave SKILL_NAMES, so a --prune-obsolete run DELETES the two retained script dirs, destroying the check.sh and _check_runner.py the directive Q2 requires kept callable.
    evidence: substrate/install.sh L1780 case-arm continue; design 2.5 mentions CARVEOUT_SKILL_DIRS L1297 but NEVER the prune-scan exemption at L1780.
    load_bearing: true
    quadrant_classification: easy-easy
  - id: r2
    description: operator-tool DEPLOY carve-out (CARVEOUT_SKILL_DIRS, install.sh L1297) is an EXPLICIT list of check-substrate-updates and check-bw-release only; it does NOT contain validate-spec or inspect-script-output. Design 1 and 2.2 prose asserts this is the carve-out install.sh ALREADY handles, which is false; 2.5 correctly hedges verify-and-add, but the two statements conflict and the build must ADD both dirs or the retained scripts never deploy to consumers.
    evidence: substrate/install.sh L1297 CARVEOUT_SKILL_DIRS list; design 2.2 already-handles vs 2.5 if-explicit-add-them.
    load_bearing: true
    quadrant_classification: easy-easy
  - id: r3
    description: operating-disciplines.md L590 (spec-authoritative) asserts the save-verdict SKILL mechanically validates 15.4 shape (INCOMPLETE and UNVERIFIABLE required fields) and exit-4s BEFORE any file write. The design Q-A HYBRID DROPS 15.4 to seat-side acceptance. The design plans NO edit to op-disc L590, leaving canon claiming a mechanical exit-4 enforcement that will no longer exist: a stale-canon contradiction in a spec-authoritative file.
    evidence: substrate/operating-disciplines.md L590; design 2.4 picks accept-seat-side for 15.4 but lists no op-disc edit; edit-site list in 2.3 and 2.5 omits op-disc.
    load_bearing: true
    quadrant_classification: easy-easy
  - id: r4
    description: MAJOR_PLINY.md L257 references substrate/skills/save-verdict/SKILL.md and the --cwd write mechanism; the skill dir is git-removed and the Bash module has no --cwd. The design names only VERA, ARGUS, CATO sec-7 as repoint sites (2.3), so PLINY L257 becomes a dangling pointer to a deleted file describing a vanished mechanism: fails the directive DoD no-dangling-pointers grep (P5).
    evidence: substrate/MAJOR_PLINY.md L257; directive DoD line 59 and design P5 require no dangling pointers to retired skill dirs; design 2.3 edit-site list omits PLINY.
    load_bearing: true
    quadrant_classification: easy-easy
  - id: r5
    description: W2 attach-failure posture (FAIL-LOUD-but-write-preserving): the verdict write does NOT hard-fail on bw-attach failure; disk artifact kept, failure surfaced and reported up. Defensible judgment call, but it leaves a window where a verdict exists ONLY on a worktree that teardown can destroy: exactly the Arc-62 loss the attach is meant to close. Whether that residual window is acceptable vs hard-fail-until-attached is a posture decision for the floor-manager, not a defect; flagged as the most-likely-promoted residual.
    evidence: design 2.1(d) and W2; durability is non-atomic with the disk write, so a teardown between write and a failed or never-retried attach reproduces the loss class.
    load_bearing: uncertain
    quadrant_classification: easy-hard
  - id: r6
    description: Bash-only assert drops MORE than just 15.4: the Python also enforced dest-exists and --overwrite (exit 3), request-id and caller path-traversal validation, and emitted the record receipt artifact (agents/save-verdict/...). The design preserves only sha256 and threat-coverage empty-binding and is silent on whether the overwrite-collision guard and the record artifact are intentionally dropped. A silent same-path overwrite (two verdicts, same ticket and officer and second) now clobbers with no exit-3.
    evidence: _save_verdict.py L387-390 exit-3 dest-exists, L134-198 record artifact, L236-240 request-id regex; design 2.1 enumerates only sha256 and empty-binding as preserved.
    load_bearing: false
    quadrant_classification: easy-easy
  - id: r7
    description: Q-C C2 SSoT is four byte-aligned copies (3 role files plus module) guarded only by a build-time P8 diff. The design itself flags this (W1); the structural fragility is real: any future arc editing one copy without re-running P8 reintroduces drift, and P8 is a manual probe not an install.sh FAIL-LOUD check (unlike the MODULE-INLINE recompose Checks A-E). Bounded, not blocking; the C2-over-C1 tradeoff is correctly reasoned.
    evidence: design 2.7, W1, P8; contrast install.sh L1084-1135 Checks A-E (install-time FAIL-LOUD) vs P8 (one-shot build probe).
    load_bearing: false
    quadrant_classification: easy-hard
  non_findings:
  - A2 bw attach mechanism CONFIRMED live: bw attach id file --name path stores bytes at attachments/id/path, commits to orphan branch (survives worktree teardown). Command shape in design 2.1(d) exactly matches the tool.
  - A3 7b1.1 shared-layer CONFIRMED: only ARGUS and CATO carry BOTH sec-4-no-Write AND sec-7-save; ZENO L62 and NOMOS L62 have NO sec-7 verdict-save (dispatch-return plus bw only), so the latent-carve-out framing is correct. VERA carries Write and Edit (frontmatter L4).
  - Q-B printf-vs-Write reconciliation coherent: sec-4 forbids the TOOL, sec-7 satisfied by a Bash printf redirect within the existing Bash grant. No residual contradiction for ARGUS or CATO; latent for ZENO and NOMOS.
  - Q-C=C2 subproject premise web-confirmed: subagent relative Read resolves vs spawn-cwd and CLAUDE_PROJECT_DIR is empty in subagents; AND the module is not deployed at subproject tier (DEST_MODULES_DIR empty). Design conflates the two failure modes but the conclusion (inline-fallback) is correct.
  - gen-data and app green confirmed: discoverSkillFiles is directory plus SKILL.md driven (gen-data-lib L100); generated.test.ts asserts only length greater-than 0 plus per-skill description and body length greater-than 0 (L96-101), zero toContain for the 3 names. display-extras save-verdict entry present and correctly flagged for kind-module fix.
  - Q-A j2i fix confirmed: Python regex is lowercase-p-only (the live bug); design bash assert leading bracket-pP accepts uppercase P-INJ. Behavior change is correct and intentional.
  - Authorship: design header Author Denson Smith; no non-PRINCIPAL author-like field in any cited edit target.
  threat_coverage_assessment: OMITTED, arc is 35.5-carved-out. ARGUS CONFIRMS the classification: skill-to-module rehoming plus verdict-durability attach plus doc-coherence are process and role-file hardening with no runtime attacker and no attack path. No threat-ratified mitigation in scope, so no threat-anchored probe is required. The path-traversal regexes the Python enforced are input-hygiene on trusted PLINY-brief inputs, not a sec-35 mitigation. Carve-out confirmed, not self-asserted.
  count_note: SECONDARY (low severity): design 2.5 and 1 say SKILL_NAMES 10 to 7 (delta -3). Ground truth: SKILL_NAMES has NINE entries (install.sh L228-238). 9-3=6, not 7. The delta -3 is correct; the absolute count and LIEUTENANT baseline (9 to 6) are off by one in the design prose. The build DoD and P7 baseline assertion must use 6, not 7.
summary: The design re-homes save-verdict, validate-spec, inspect-script-output off the skill menu, rewrites save-verdict as a Bash-only printf plus sha256 plus bw-attach module, and resolves 7b1.1 at the shared read-only-review-seat layer. Headline mechanisms are sound and ground-verified. The most important load-bearing risk is r1: the prune-scan exemption at install.sh L1780 is an explicit list the design never mentions, so --prune-obsolete would DELETE the two retained operator-tool dirs the directive requires kept (silent data-loss). r2, r3, r4 are sibling under-scoped edit sites (deploy carve-out, stale op-disc L590 canon, dangling PLINY L257 pointer) that fail directive DoD probes as written. None design-fatal; all concrete easy-verify additions to build scope. Posture: minor revisions, RATIFY-WITH-CONDITIONS.
