status: completed
ticket: stoa--p7c
verdict: pass
design_artifact_verified_against: agents/design/stoa--p7c/design-rev4.md (§3 Verification probes P1..P9 + Full-suite belt-and-suspenders)
build_verified: arc-67/build @ 11b51f9 (worktree .claude/worktrees/arc-67-build)

probes_executed:
- probe_id: P1
  description: Launcher dry-run command shape — each launched seat's pwsh line carries --session-id <36-char-guid> AND --name <space-free>; two seats => two --session-id.
  quadrant_classification: easy-easy
  command_or_method: pwsh -NoProfile -File substrate/skills/team-launcher/launch-team.ps1 -DryRun -ArcId arc-67 -Slug the-stoa; then grep -oE -- "--session-id [0-9a-f]{8}-...-[0-9a-f]{12}" and grep -oE -- "--name '[^' ]+'"
  expected: exactly 2 launch lines, each with one --session-id <GUID> and one space-free --name; GUIDs valid 36-char shape.
  observed: 2 launch lines. --session-id 8e4605a1-3858-4797-9e89-36e279fc0e38 (POLYBIUS_the-stoa) and --session-id 2ea0451c-092c-4f04-b3ec-e08ddc44f856 (PLINY_the-stoa); --name 'POLYBIUS_the-stoa' and --name 'PLINY_the-stoa' (both space-free). Count of --session-id <guid> = 2.
  result: pass
- probe_id: P2
  description: Dry-run PRINTS the record-seat step as [dry-run] but does NOT execute it; registry unchanged (no synthetic row).
  quadrant_classification: easy-easy
  command_or_method: same dry-run run; grep -c "[dry-run] record-seat:"; then git show beadwork:attachments/stoa--reg/seat-registry.jsonl | wc -l (before/after).
  expected: 2 "[dry-run] record-seat:" lines; registry still exactly 2 production rows, no TESTSEAT/synthetic row.
  observed: 2 "[dry-run] record-seat:" lines printed. Registry after dry-run = exactly 2 rows (POLYBIUS 88cd8c37 + PLINY a3e23bca), unchanged from baseline. No synthetic row written.
  result: pass
- probe_id: P3
  description: REAL registry round-trip via record-seat.ps1 (no live spawn) — insert synthetic row, read back, re-record same (seat,machine) with a DIFFERENT SessionId => idempotent replace (exactly ONE TESTSEAT_probe row, NOT append). MANDATORY verified cleanup back to exactly 2 production rows.
  quadrant_classification: easy-easy
  command_or_method: pwsh record-seat.ps1 -Seat TESTSEAT_probe -Name Test_Probe -SessionId 00000000-... ; read-back via git show beadwork:...; re-run with -SessionId 11111111-... ; grep -c TESTSEAT_probe; then rebuild manifest with TESTSEAT_probe filtered out, bw attach stoa--reg <restore> --name seat-registry.jsonl (fixed literal --name); read-back.
  expected: row 1 present with all 8 fields; after second record exactly ONE TESTSEAT_probe row carrying the NEW sid (1111...); 3 rows total not 4; after cleanup exactly 2 production rows, zero TESTSEAT residue.
  observed: Insert OK — TESTSEAT_probe row with all fields (seat/name/session_id/project/machine/role/tier/launched_at/status), total 3 rows. Re-record with 1111... => exactly ONE TESTSEAT_probe row carrying sid 11111111-1111-1111-1111-111111111111, total 3 rows (idempotent REPLACE, not append). CLEANUP: re-attached the 2 production rows via fixed-literal --name seat-registry.jsonl; READ-BACK confirmed exactly 2 rows (POLYBIUS 88cd8c37-f3ff-4904-9b0e-efafaad48f70 + PLINY a3e23bca-779d-4ffc-b720-58262af14cdb), TESTSEAT residue count = 0, both production sids present (each grep -c = 1). Registry restored.
  result: pass
- probe_id: P4
  description: whoami terminal-seat path reads $CLAUDE_CODE_SESSION_ID and returns the bare GUID under the terminal label (sid=<v>); known terminal sids are PLINY a3e23bca and FM 88cd8c37 (both live-confirmed; both are the production registry rows).
  quadrant_classification: easy-easy
  command_or_method: python substrate/skills/whoami/whoami.py --role terminal (run from VERA's session; VERA's caller-sid equals PLINY's terminal sid, so the shape + value are directly assertable).
  expected: stdout "sid=<36-char-guid>", exit 0; value matches the known terminal sid a3e23bca.
  observed: stdout "sid=a3e23bca-779d-4ffc-b720-58262af14cdb", exit 0. Terminal-label path returns the bare GUID under the sid= label as designed. (FM 88cd8c37 + PLINY a3e23bca are the two live-confirmed terminal sids and the two production registry rows.)
  result: pass
- probe_id: P5
  description: whoami sub-agent REAL (the clean win) — VERA, itself an Agent-tool sub-agent, reads $CLAUDE_CODE_SESSION_ID and gets its CALLER PLINY's sid (NOT a per-agent id, NOT empty). REAL execution, no simulation.
  quadrant_classification: easy-easy
  command_or_method: echo $CLAUDE_CODE_SESSION_ID; python substrate/skills/whoami/whoami.py --role subagent; --role subagent --json — all run from VERA's own sub-agent session.
  expected: stdout equals VERA's dispatching terminal (PLINY) sid a3e23bca-779d-4ffc-b720-58262af14cdb, exit 0; JSON {"kind":"subagent","caller_sid":"..."}.
  observed: bare echo => a3e23bca-779d-4ffc-b720-58262af14cdb. whoami --role subagent => "caller-sid=a3e23bca-779d-4ffc-b720-58262af14cdb", exit 0. --json => {"kind": "subagent", "caller_sid": "a3e23bca-779d-4ffc-b720-58262af14cdb"}. Matches PLINY's caller-sid exactly; not empty, not a per-agent id.
  result: pass
- probe_id: P5-fail
  description: whoami FAIL-LOUD on empty/unset $CLAUDE_CODE_SESSION_ID (the re-homed ARGUS FIX1 MUST) — non-zero exit, clear stderr naming the var, NO sid on stdout, NO fallback to nonce-grep/path-parse.
  quadrant_classification: easy-easy
  command_or_method: env -u CLAUDE_CODE_SESSION_ID python whoami.py; CLAUDE_CODE_SESSION_ID= python whoami.py; CLAUDE_CODE_SESSION_ID="   " python whoami.py — stdout and stderr captured separately.
  expected: exit 2; stderr "whoami: $CLAUDE_CODE_SESSION_ID is not set; cannot determine session identity"; stdout EMPTY; no filesystem traversal.
  observed: All three cases (unset / empty / whitespace-only) => exit 2, stdout EMPTY, stderr "whoami: $CLAUDE_CODE_SESSION_ID is not set; cannot determine session identity". Source read (whoami.py) confirms a single os.environ.get + FAIL-LOUD branch — NO derivation, NO filesystem traversal, NO nonce-grep/path-parse fallback path exists in the code. (Whitespace-only also FAILs LOUD via the .strip() guard.)
  result: pass
- probe_id: P6
  description: §28.9 broadening present in operating-disciplines.md (env var + both sign formats + FAIL-LOUD line + Author-stays-PRINCIPAL absolute restated) AND the 14 role files (12 CAPTAIN + 2 MAJOR) carry the §28.9 pointer.
  quadrant_classification: easy-easy
  command_or_method: grep -n "28.9|CLAUDE_CODE_SESSION_ID|caller-sid|subagent|Author:" substrate/operating-disciplines.md; grep -rl "28.9" across role files | wc -l.
  expected: §28.9 exists with terminal format "[from: <Name> | sid <id> | <project>]", sub-agent format "[from: CAPTAIN_<MNEMONIC>_<slug> (subagent) | caller-sid <caller-sid>]" (no agent-id), FAIL-LOUD line, Author-stays-PRINCIPAL restated; 14 role-file pointers.
  observed: §28.9 present (op-disc L1211 "Session-identity sign-everywhere"). Terminal format L1219; sub-agent format L1220 with "(subagent) | caller-sid" and explicit "NO agent-id"; FAIL-LOUD line L1224 ("whoami MUST FAIL LOUD ... non-zero exit ... no workaround fallback"); Author-stays-PRINCIPAL absolute restated L1230 ("Author: remains the PRINCIPAL's configured identity"). Role-file pointer count = 14 (all 12 CAPTAIN_*.md + MAJOR_POLYBIUS.md + MAJOR_PLINY.md).
  result: pass
- probe_id: P7
  description: No cross-machine-resume promise — grep whoami + team-launcher + §28.9 for "resume"; every hit must be same-machine-correct, zero cross-machine-resume claims.
  quadrant_classification: easy-easy
  command_or_method: grep -rni "resume" substrate/skills/whoami/ substrate/skills/team-launcher/; grep "resume" §28.9 region.
  expected: zero cross-machine-resume claims.
  observed: ZERO "resume" hits in substrate/skills/whoami/ and substrate/skills/team-launcher/ (grep exit 1). The only op-disc "resume" (L700) is about agent-dispatch continuity ("you do not resume an agent — you dispatch a new one"), not cross-machine session resume. (Note: handoff-author SKILL.md carries a pre-existing "claude --resume <session-id>" — same-machine session resumption in a handoff template, NOT in whoami/launcher scope and NOT a cross-machine promise; pre-existing, not touched by this build.)
  result: pass
- probe_id: P8
  description: Substrate session-id reference clean-check AND nonce-grep / path-parse RESIDUE grep (#1 swap risk) — the retired approach must be FULLY gone from whoami, §28.9, and team-launcher SKILL.md, not half-swapped.
  quadrant_classification: easy-easy
  command_or_method: grep -rn "session-id|--session-id|whoami|caller-sid|CLAUDE_CODE_SESSION_ID" substrate/ (refs only intended); grep -rni "nonce|--classify-path|subagents/agent-|transcript.*grep|flush.*retry|path.parse" substrate/skills/whoami/ substrate/operating-disciplines.md substrate/skills/team-launcher/SKILL.md.
  expected: session-id refs only in intended files (launcher, whoami, §28.9, team-launcher SKILL.md, role-file pointers, install.sh); residue grep returns ZERO.
  observed: Residue grep = ZERO hits (exit 1) across whoami skill, op-disc, and team-launcher SKILL.md — nonce-grep / --classify-path / subagents/agent- / transcript-grep / flush-retry / path-parse are FULLY GONE. session-id refs land in intended files only; the additional hits (arc-37/38/59 directives, handoff-author SKILL.md) are PRE-EXISTING (confirmed NOT in the build diff 11b51f9~1..11b51f9) and unrelated to whoami/registry.
  result: pass
- probe_id: P9
  description: install.sh deploys whoami (in SKILL_NAMES) AND the launcher interim-note is RESOLVED (the "interim ... when stoa--p7c lands" phrasing gone, replaced with §28.9 + registry pointer); install.sh recompose A-E checks pass on a REAL run (full-suite, not "this arc touched no X").
  quadrant_classification: easy-easy
  command_or_method: grep SKILL_NAMES + whoami in install.sh; grep -ni "interim|when stoa--p7c lands|adopt the formal" launcher + SKILL.md; install.sh dry-run (project + subproject) AND a REAL throwaway-synthetic subproject install to exercise the A-E recompose checks (the dry-run early-returns past the recompose block per the banked directive-DoD-vs-tool-reality lesson).
  expected: whoami in SKILL_NAMES; interim phrasing gone, §28.9+stoa--reg pointers present; both dry-runs exit 0; REAL install exit 0 with recompose A-E checks executing cleanly (no awk fail()).
  observed: whoami in SKILL_NAMES (install.sh L235). "interim ... when stoa--p7c lands / adopt the formal" phrasing GONE (grep exit 1); launcher carries §28.9 + stoa--reg pointers instead (L62-65). Project-tier dry-run exit 0 (whoami deployed). Subproject-tier dry-run exit 0 (whoami deployed). REAL throwaway-synthetic subproject install exit 0: recompose re-inlined OWNED module bodies across MAJOR_POLYBIUS / op-disc / MAJOR_PLINY / MAJOR_CHIRON / CAPTAIN_DAEDALUS etc. with ZERO Check A-E failure (no fail(), no "marker has no module source", no unbalanced/corrupt); whoami SKILL.md + whoami.py actually landed. op-disc with new §28.9 recomposed without breaking module-inline integrity.
  result: pass
- probe_id: full-suite
  description: Belt-and-suspenders (banked lesson) — run the project's existing install.sh smoke (project + subproject tier, REAL where the recompose checks live) AND npm run gen-data; gen-data re-derives the WHOLE roster and surfaces pre-existing drift. Assert from a FULL suite run, NOT "this arc edited no agent frontmatter" (the 14 role-file pointer edits touch the gen-data + recompose input surface).
  quadrant_classification: easy-easy
  command_or_method: npm install + npm run gen-data (app/); install.sh project+subproject dry-run + REAL subproject install (see P9).
  expected: gen-data exit 0, full roster re-derived with no Zod/parse error; install.sh real recompose clean.
  observed: gen-data exit 0 — "discovered 16 role file(s); roster: 4 MAJOR(s), 12 CAPTAIN(s), 8 LIEUTENANT(s); COLONEL reserved-empty; HUMAN stub". No Zod schema error, no parse failure. Only output change = regenerated app/src/data/generated/agents.ts (a generated artifact; node_modules gitignored). The 14 role-file pointer edits broke NEITHER the gen-data input surface NOR the recompose module-inline checks. install.sh full-suite per P9 (real recompose clean).
  result: pass

threat_coverage: []
# Empty-list carve-out is VALID here: this arc is classified `not threat-ratified` (§35.5 carve-out)
# — ARGUS-CONFIRMED in design-rev4 §"A3 threat->mitigation map" (process + tooling / role-file hardening;
# no runtime attack path; the env-var swap changes the SID's SOURCE, not its security role — still
# audit-only forgeable provenance, not an authn/authz control). No M<n> rows; no threat-anchored probe
# required (design §6.13 self-carve-out, ARGUS-confirmed). threat-ratified-mitigation-count = 0.

methodology_concerns: >
  (1) P9's "install.sh --dry-run passes its A-E recompose checks" assertion is NOT satisfiable by --dry-run alone:
  the recompose block (with the awk Check A-E module-inline integrity checks) lives in install.sh's non-dry-run
  (else) branch (L995+ / the 2a-recompose block), and --dry-run early-returns past it (matches the project's banked
  "directive-DoD-must-match-tool-reality" + "gen-data-regen-re-derives-whole-roster" lessons). I therefore executed a
  REAL (non-dry-run) subproject install against a THROWAWAY synthetic parent (mktemp -d) to actually exercise the A-E
  checks — they passed. This is a probe-spec precision note (the verifiable A-E check needs a REAL run, not a dry-run),
  NOT a build defect; the build is correct. Recommend DAEDALUS tighten P9's wording on a future arc to say "REAL install
  (throwaway parent)" rather than "--dry-run" for the recompose-check half. (2) WEAK-1 residual (un-probable here):
  the whole mechanism rests on $CLAUDE_CODE_SESSION_ID keeping its terminal-own-sid / sub-agent-caller-sid SEMANTICS;
  FAIL-LOUD catches the var's ABSENCE loudly but cannot catch a future-CC SEMANTIC change (var still set, now means
  something different). This is an un-mechanizable future-runtime residual (I cannot probe a future CC version); the
  whoami SKILL.md carries the "re-verify NOMOS C1/C2/C3 if CC changes semantics" note, which is the adequate honest-claim
  boundary for v1. Not a this-arc gap.
falsifying_evidence_summary: >
  None. Every probe (P1, P2, P3, P4, P5, P5-fail, P6, P7, P8, P9, full-suite) PASSED with recorded evidence. The
  load-bearing wins: P5 is REAL (not simulated) — VERA's own sub-agent session returned its caller PLINY's sid
  a3e23bca via $CLAUDE_CODE_SESSION_ID, dogfooding the exact mechanism. P5-fail confirmed the FAIL-LOUD MUST (exit 2,
  empty stdout, no fallback). P8's residue grep confirmed the retired nonce-grep/path-parse approach is FULLY gone
  (zero hits) — the #1 swap risk did not materialize. P3's REAL registry round-trip proved idempotent replace AND its
  cleanup was executed + read-back-verified (registry restored to exactly 2 production rows, zero TESTSEAT residue).
verification_artifacts_path: agents/verdicts/stoa--p7c/ (recorded probe outputs captured inline in this verdict; transient probe scratch under /tmp on the verifying machine — not persisted to the repo per independence)
summary: >
  The Arc 67 build (11b51f9) was exercised against design-rev4 §3 probes P1..P9 + the full-suite belt-and-suspenders,
  with REAL execution wherever the design/DoD demanded it (NOT the launcher -DryRun, which early-returns before any
  registry write / live session; and NOT install.sh --dry-run, which early-returns past the recompose A-E checks).
  The load-bearing probes were P5 (the clean win — VERA, an actual Agent-tool sub-agent, read its caller PLINY's sid
  a3e23bca directly from $CLAUDE_CODE_SESSION_ID via whoami: REAL, no simulation), P5-fail (the re-homed ARGUS FIX1
  MUST — FAIL-LOUD exit 2 with empty stdout and zero fallback, confirmed by both runtime behavior and a source read of
  whoami.py showing a single os.environ.get with no filesystem traversal), P3 (the REAL registry round-trip proving
  idempotent (seat,machine) replace — exactly one TESTSEAT_probe row across two records with different sids — with a
  MANDATORY cleanup that was executed AND read-back-verified to exactly 2 production rows / zero TESTSEAT residue),
  and P8 (the #1 swap risk: the residue grep returned ZERO nonce-grep/path-parse hits — the retired approach is fully
  gone, not half-swapped). P1/P2 confirmed the HYBRID DC1 launcher still mints+pins --session-id per terminal seat
  and prints (does not execute) the record step in dry-run. P6 confirmed §28.9 with both sign formats, the FAIL-LOUD
  line, the Author-stays-PRINCIPAL absolute, and all 14 role-file pointers. The full-suite gen-data re-derived the
  whole roster cleanly (4 MAJOR / 12 CAPTAIN / 8 LIEUTENANT, no Zod/parse error) and a REAL throwaway-synthetic
  subproject install exercised the A-E recompose checks with zero failures — so the 14 role-file pointer edits broke
  neither the gen-data nor the recompose input surface. Author-field discipline: clean (the build commit Author stays
  PRINCIPAL with the CAPTAIN_ADA trailer; §28.9 restates the absolute). One methodology note (P9's dry-run wording for
  the recompose half) and one un-mechanizable WEAK-1 future-CC-semantics residual recorded — neither is a build defect.
  Overall falsification verdict: PASS. I operated only on the correct registry path; the known stray doubled-path
  (attachments/stoa--reg/attachments/stoa--reg/seat-registry.jsonl, blob 8b13789) was confirmed untouched and remains
  PLINY's to dispose.
