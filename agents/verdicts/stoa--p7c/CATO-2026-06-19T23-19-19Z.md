status: completed
ticket: stoa--p7c
verdict: revise
diff_reviewed: bd15fc3..HEAD (Arc-67 full diff: 11b51f9 build + e7df701 graceful-absence + 1c4b86c gen-data regen) on arc-67/build, HEAD 1c4b86c
design_artifact_compared_against: agents/design/stoa--p7c/design-rev4.md

concerns:
- id: c1
  category: consistency
  description: MAJOR_POLYBIUS.md has a DUPLICATE section number — the new session-identity pointer was inserted as `### 7.6` directly above the pre-existing `### 7.6 Orchestrator background-dispatch hygiene (Arc 24)`, producing two `### 7.6` headings in one spec-authoritative role file.
  evidence: substrate/MAJOR_POLYBIUS.md:259 (`### 7.6 Session-identity sign-everywhere ...`) collides with :262 (`### 7.6 Orchestrator background-dispatch hygiene (Arc 24)`). At baseline bd15fc3 the latter was the sole `### 7.6`; this arc added a second. The parallel insert in MAJOR_PLINY.md was correctly numbered `### 5.17` (no collision) — POLYBIUS is the odd one out. Fix: renumber the new pointer to `### 7.7` (and verify nothing downstream cross-refs the old `7.6`).
  severity: blocking
  quadrant_classification: easy-easy

follow_ups:
- f1: launch-team.ps1:63 (.NOTES prose block) summarizes the terminal sign-format with a generic `<id>` placeholder — `[from: <Name> | sid <id> | <project>]` — while every NORMATIVE locus (op-disc §28.9 table, the 2 MAJOR pointers, whoami SKILL.md L53) uses the literal `$CLAUDE_CODE_SESSION_ID`. This is a non-normative comment-block summary, not a second SSoT, so it is cosmetic-only; align the placeholder to `$CLAUDE_CODE_SESSION_ID` on a future touch for tidiness. NOT a block.
- f2: Seeded registry rows on the beadwork branch carry inconsistent `name` forms — POLYBIUS row `name=Polybius_the_Stoa` (underscored human-friendly form per record-seat.ps1 param doc) vs PLINY row `name=PLINY_the-stoa` (hyphenated = the seat name). Both are valid per the `-Name` param contract (free display string), so not a code defect; this is seed data, not arc build surface. Normalize at the PLINY close-time registry tidy (alongside the known stray doubled-path disposal). NOT a block.

verifier_coverage_assessment: >
  VERA exercised the DoD probes thoroughly and REAL where it matters — P3 (REAL registry round-trip via record-seat.ps1 with idempotent (seat,machine) replace + MANDATORY read-back-verified cleanup to exactly 2 production rows), P4/P5/P5-fail (REAL whoami terminal + sub-agent caller-sid + FAIL-LOUD on empty/unset/whitespace), P8 (nonce-grep residue = ZERO), and the full-suite gen-data + a REAL throwaway-parent install for the recompose A-E checks (correctly NOT relying on --dry-run, applying the banked directive-DoD-vs-tool-reality lesson). I independently re-ran P1/P2 (dry-run prints-not-executes both steps), P4/P5/P5-fail (all match), P8 (zero residue), and the gen-data determinism (byte-faithful, empty re-diff) — all confirm VERA. ONE COVERAGE GAP, now FILLED by me: VERA verified 11b51f9 ONLY (verdict ts 22:53Z preceded e7df701 17:00 local + 1c4b86c 17:08 local), so the graceful-absence fix (e7df701) had NO functional probe by VERA. NOMOS audited it for scope/authorship (CONFORMANT) but not behavior. I empirically reproduced both failure modes (§6.8): a non-zero record-seat exit AND a terminating throw each Write-Warning + loop-continue for every seat, neither aborts the launch — the e7df701 intent (record failure never breaks the consumer launch) is functionally correct on both paths. With that gap filled, the DoD is fully covered.

summary: >
  The Arc-67 diff is a clean, well-crafted session-identity deployment pattern: launcher mints a per-seat
  --session-id + records to the bw registry (best-effort, graceful on consumer workspaces); whoami.py reads
  $CLAUDE_CODE_SESSION_ID with a correct FAIL-LOUD MUST (empty/unset/whitespace -> exit 2, empty stdout, no
  fallback); §28.9 broadens the sign-everywhere convention with both sign-formats, the Author-stays-PRINCIPAL
  restatement, and the forgeable-provenance honest-claim boundary (ARGUS R1, not re-litigated). Consistency —
  the #1 risk for an identity-convention arc — is strong: the sub-agent sign-format is BYTE-IDENTICAL across all
  15 sub-agent loci (12 CAPTAINs + 2 MAJORs + whoami SKILL), the terminal format matches across the 2 MAJOR
  pointers + whoami SKILL + op-disc table, the registry JSONL schema matches record-seat.ps1 output exactly, and
  whoami runtime output matches its docs. Native env-var premise WEB-VERIFIED against the CC v2.1.132 changelog.
  Author-field audit CLEAN (both SKILL.md author: Denson Smith; no author-like field in .py/.ps1; all 3 commits
  Author=denson + CAPTAIN_ADA seat trailer). Scope clean — no dead code, no TODOs, no commented-out code, nonce
  residue fully gone, gen-data regen byte-faithful. ONE blocking finding: a duplicate `### 7.6` heading in
  MAJOR_POLYBIUS.md (the new pointer was inserted in front of the pre-existing 7.6 without renumbering to 7.7) —
  a low-cost must-fix in a spec-authoritative role file that propagates on every install, surfaced for ADA per
  the fix-known-bugs-now directive. Two cosmetic follow-ups (the .NOTES <id> placeholder; seed-row name-form
  drift) are non-blocking. Posture: clean build, one trivial-to-fix canon-numbering defect blocking ship.
