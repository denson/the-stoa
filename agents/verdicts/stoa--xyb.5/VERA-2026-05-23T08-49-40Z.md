status: completed
ticket: stoa--xyb.5
verdict: pass
design_artifact_verified_against: agents/design/arc-46/design-rev1.md (STAGE 1 components only)
build_verified: arc-46/build @ .claude/worktrees/arc-46-build, commit d70f7e2

HEADLINE — SAFETY (the load-bearing constraint, verified hardest):
  Live ~/.claude/settings.json sha256 was c79ae66148732c4c714edced6a8c80a6dd8ff276508baf9c770b1d00438b21b0
  at dispatch ENTRY and BYTE-IDENTICAL at dispatch EXIT, re-checked after EVERY probe including two
  real (non-dry-run) --enable-hooks runs. NO probe armed a hook in any live settings.json.
  Project-tier the-stoa/.claude/settings.json: ABSENT before, during, and after (worktree + main).
  --enable-hooks DEFAULTS OFF: a normal install.sh (no flag) registers ZERO hooks anywhere.
  User-tier --enable-hooks NEVER auto-writes settings.json — it prints a manual-merge runbook
  (verified via redirected-HOME throwaway with a pre-seeded settings.json: byte-identical after).
  This is a PASS on the brief's primary verification target.

probes_executed:
- probe_id: P1
  description: Deploy to a throwaway target; assert hook scripts exist + are +x and the settings-hooks.json template exists.
  command_or_method: bash install.sh --target project --project-dir /tmp/vera-arc46/proj-real (NO --enable-hooks)
  expected: all 5 scripts (_hooklib + 3 PreToolUse gates + Stop self-check) deployed +x; README + principal-identity present; templates/settings-hooks.json present
  observed: all 5 *.sh deployed -rwxr-xr-x; README.md + principal-identity present; templates/settings-hooks.json (1830 bytes) present
  quadrant_classification: easy-easy
  result: pass
- probe_id: P2
  description: STRUCTURAL SAFETY (design §8). Assert install.sh did NOT write/modify any LIVE settings.json (sha256 before/after) and wrote NO target settings.json on the default path.
  command_or_method: sha256sum ~/.claude/settings.json before/after default deploy; ls target .claude/settings.json
  expected: live settings.json byte-identical; NO target settings.json on default (OFF) path
  observed: live sha BEFORE==AFTER (c79ae66...); target .claude/settings.json absent on default deploy ("NO target settings.json written — correct"). Final re-check after the whole battery: still c79ae66, zero "hooks" keys in live ~/.claude/settings.json.
  quadrant_classification: easy-easy
  result: pass
- probe_id: P3
  description: author-field gate DENIES a commit staging a non-PRINCIPAL author with a self-contained reason.
  command_or_method: throwaway git repo + allow-list seeded in HOOK_DIR; stdin {tool_input.command:"git commit -m wip",cwd:scratch} with staged package.json {"author":"Nate Jones"} -> pretooluse-author-field-audit.sh
  expected: stdout hookSpecificOutput.permissionDecision="deny" + self-contained reason (field/file/value + fix + source-citation guidance)
  observed: DENY emitted; reason names field "author", file "package.json", value "Nate Jones", states the fix, the source-vs-author distinction, and how to widen the allow-list. Inline self-contained (no bare pointer). Also confirms defect#3 fix: "Nate Jones" extracted from compact JSON {"author":"Nate Jones"} (field-anchored, not "everything after the colon").
  quadrant_classification: easy-easy
  result: pass
- probe_id: P4
  description: author-field gate ALLOWS a commit whose author field = the PRINCIPAL (no false-positive).
  command_or_method: same harness, package.json {"author":"Denson Smith"}; also clean tree w/ git author = PRINCIPAL and no author files staged
  expected: ALLOW (exit 0, no deny JSON)
  observed: empty stdout, exit 0 (ALLOW) for both. Additional defect#3 depth: TOML authors=["Wrong Person","Denson Smith"] -> DENY on "Wrong Person" only (array flattening + per-element audit, PRINCIPAL element passes); YAML frontmatter author: in .md -> DENY; git author override "Impostor Dev" -> DENY, restore PRINCIPAL -> ALLOW; allow-list absent -> FAIL-OPEN (allow).
  quadrant_classification: easy-easy
  result: pass
- probe_id: P5
  description: clean-tree-before-branch gate DENIES branch-create on a dirty tree (lists dirty paths) and ALLOWS on a clean tree; discriminates list-vs-create.
  command_or_method: throwaway git repo; stdin {command:"git checkout -b arc-99/build",cwd:scratch} dirty vs clean -> pretooluse-clean-tree-before-branch.sh; plus git status / git branch -a / git branch <name>
  expected: deny on dirty create (paths listed inline); allow on clean create; allow on non-create + list forms
  observed: dirty + checkout -b -> DENY listing "M base.txt" + "?? new.txt" (count=2) inline; clean + checkout -b -> ALLOW; dirty + git status -> ALLOW; dirty + git branch -a (list) -> ALLOW; dirty + git branch newfeature (create) -> DENY. Correct list-vs-create discrimination.
  quadrant_classification: easy-easy
  result: pass
- probe_id: P6
  description: no-`-m`-bw-comment gate DENIES `bw comment <id> -m "x"`, ALLOWS the positional form, and does NOT false-flag a body that merely MENTIONS -m.
  command_or_method: stdin {command:"bw comment stoa--abc -m \"hello\""} vs positional vs body containing "git commit -m" vs non-bw -> pretooluse-no-dash-m-bw-comment.sh
  expected: deny only on the -m-as-flag-after-id case; allow positional + mentions-only + non-bw
  observed: `-m` flag after id -> DENY (self-contained reason: positional form, silent-data-loss explanation, exact re-run command); positional "hello world" -> ALLOW; body "use git commit -m for git" -> ALLOW (no false-flag, defect#2 fix confirmed); "echo hello -m world" -> ALLOW.
  quadrant_classification: easy-easy
  result: pass
- probe_id: P7
  description: Stop self-check BLOCKS once-per-turn via decision:"block"+reason carrying the inline A/B/C checklist, then RELEASES (sentinel) on the next stop; honors stop_hook_active.
  command_or_method: stdin Stop events {session_id,transcript_path:"",cwd,stop_hook_active} -> stop-self-check.sh (first call, repeat call same turn-key, stop_hook_active:true)
  expected: first -> decision:"block" + reason with A/B/C inline; second (sentinel present) -> allow; stop_hook_active:true -> allow
  observed: first Stop -> {"decision":"block","reason":...A/B/C...}; reason rides `reason` NOT additionalContext (correct per §2 Stop drift); NOMOS forward-ref degrades vacuously ("If NOMOS is not yet deployed... treat as satisfied"); brief's seat-tag clause-A correctly ABSENT (deferred Stage 2). Second Stop same turn-key -> ALLOW (no infinite block). stop_hook_active:true -> ALLOW (also proves defect#4: JSON bool serialized to lowercase "true" so the shell compare matches).
  quadrant_classification: easy-easy
  result: pass
- probe_id: FAIL-OPEN (brief item 3)
  description: simulate python3 broken (exit 127) on PATH and confirm every gate fail-OPENs (allows), never bricks.
  command_or_method: prepend a python3 stub that exits 127; feed each gate its bad case (bad-author commit / -m / dirty checkout -b / Stop)
  expected: all gates allow (empty stdout, exit 0); none deny/block
  observed: author-field (bad author) -> ALLOW; no-dash-m (-m present) -> ALLOW; clean-tree (dirty create) -> ALLOW; stop-self-check -> ALLOW (no block). Fail-open inversion (design §4 / ARGUS R5) holds across all four scripts: degrades to today's no-enforcement, never blocks the live session. (Note: an earlier symlink-based no-PATH harness was invalid — msys bash couldn't load DLLs — and was re-run correctly with a python3-only stub.)
  quadrant_classification: easy-hard
  result: pass
- probe_id: P11
  description: install.sh --dry-run plan lists the hooks deploy, the settings-hooks.json template, and the CAPTAIN count.
  command_or_method: bash install.sh --target project --project-dir <tmp> --dry-run
  expected: plan lists hooks deploy + settings-hooks.json + arm-hooks status + CAPTAIN count
  observed: "deploy hooks: yes (5 script(s)) [INERT — not armed]"; "arm hooks: no (default OFF — scripts deploy inert; no settings.json written)"; settings-hooks.json template enumerated; all 5 hook scripts + principal-identity seed enumerated; "deploy CAPTAINs: yes (11 envelopes)". NOTE: design probe P11 said "12 CAPTAINs (NOMOS added)" but NOMOS is Stage 2 (deferred per brief); 11 is CORRECT for Stage 1. Not a defect.
  quadrant_classification: easy-easy
  result: pass
- probe_id: P12
  description: bash -n syntax check every substrate/hooks/*.sh + install.sh; shellcheck if available.
  command_or_method: bash -n on each of the 5 scripts + install.sh; shellcheck probe
  expected: exit 0 on all; shellcheck if installed
  observed: bash -n exit 0 on _hooklib.sh + 3 gates + stop-self-check.sh + install.sh. shellcheck NOT installed (the optional "if available" clause); bash -n is the available syntax gate.
  quadrant_classification: easy-easy
  result: pass
- probe_id: P13
  description: no bare "see §X" pointer in any emitted trigger payload (the authoring rule); every payload carries an actionable WHAT-to-do.
  command_or_method: static scan of emit_deny/REASON payload strings for bare-pointer patterns + actionable-verb presence; cross-checked against the live payload outputs from P3/P5/P6/P7
  expected: no bare pointer; each payload states WHY + WHAT inline
  observed: zero bare "see §"/"see section" pointers in emitted payloads; all four payloads carry actionable instructions (Fix / Re-run / "Commit, stash" / "fix it now / restate"). Payloads name the discipline inline ("the authorship-attribution discipline", "the arc-build branch-hygiene discipline") AND state the fix — the correct pattern, not a lookup pointer. op-disc §34 codifies the rule (WHY+WHAT inline, never a bare pointer), detail in hooks/README.md (dogfoods the composition layer).
  quadrant_classification: easy-easy
  result: pass
- probe_id: ENABLE-HOOKS-SAFETY (brief item 1, deeper than P2)
  description: confirm user-tier --enable-hooks does NOT auto-write ~/.claude/settings.json (prints runbook) and project-tier --enable-hooks writes TARGET-only + preserves an existing target settings.json.
  command_or_method: HOME redirected to throwaway w/ pre-seeded settings.json for the user-tier real run; fresh + existing project-tier targets
  expected: user-tier -> runbook printed, settings.json untouched; project-tier fresh -> writes valid TARGET settings.json ({{HOOKS_DIR}} substituted, 3 PreToolUse + 1 Stop); project-tier existing -> not overwritten
  observed: user-tier --enable-hooks (real) -> "MANUAL merge required (safety) ... will NOT auto-write it"; pre-seeded settings.json byte-identical after; scripts still deployed inert. project-tier fresh -> wrote valid-JSON settings.json, 0 remaining {{HOOKS_DIR}}, command paths absolute, 3 PreToolUse inner-hooks + 1 Stop. project-tier existing {"keepMe":...} -> "Not overwriting (would drop your other settings)", byte-identical after. REAL ~/.claude/settings.json byte-identical throughout.
  quadrant_classification: easy-hard
  result: pass

methodology_concerns:
- P11 wording vs Stage-1 scope: the original design probe P11 asserts "12 CAPTAINs (NOMOS added)", but NOMOS is a Stage-2 component explicitly deferred per the dispatch brief. The Stage-1 build correctly ships 11 CAPTAINs. I scored P11 PASS against the Stage-1 reality (11), not the full-build figure (12). This is a probe-text/staging mismatch, not a build defect; flagged so PLINY knows the "12" figure belongs to Stage 2's verification.
- P8 (PostToolUse-on-Agent trigger) and P9/P10 (NOMOS) are Stage-2 probes; their components are not in this build and were correctly out of scope. Not run.
- shellcheck unavailable in this environment; P12 was satisfied by bash -n (the design's "shellcheck if available" makes it optional). A future run on a shellcheck-equipped host could add lint depth.
- Platform note (not a defect): probes ran under msys bash on Windows; absolute /tmp paths required cygpath to validate JSON with the Windows-native python3. The hook scripts' OWN python3 calls work because they pass paths the interpreter receives directly; on a real POSIX substrate target this concern does not arise. Substituted command paths in the project-tier armed settings.json are msys-style /tmp paths only because the install ran under msys; on a POSIX target DEST_HOOKS_DIR resolves to the correct absolute path.

falsifying_evidence_summary: (none — verdict is pass)

verification_artifacts_path: probes executed against throwaway targets under /tmp/vera-arc46/ (proj-dry, proj-real, proj-arm-fresh, proj-arm-existing, fake-home, scratch-author, scratch-tree, shadow-bin) — all deleted at run end per ISOLATION-ONLY. No persistent artifacts written outside this verdict (independence-of-verification: VERA never mutates the build).

summary: >
  All Stage-1 probes (P1-P7, P11-P13) plus the brief's explicit safety/fail-open/enable-hooks checks
  PASS against commit d70f7e2. The load-bearing SAFETY result is the headline: the live
  ~/.claude/settings.json is byte-identical (c79ae66...) from dispatch entry through every probe to exit,
  --enable-hooks defaults OFF (a normal install registers zero hooks), user-tier --enable-hooks prints a
  manual runbook and never auto-writes its settings.json, project-tier --enable-hooks writes the TARGET
  only and refuses to clobber an existing target settings.json — NO path arms a hook in the running
  session. The three PreToolUse gates each BLOCK their bad case and ALLOW their good case with
  self-contained deny reasons: author-field denies non-PRINCIPAL author (git-identity override + staged
  JSON/YAML/TOML/array fields) and allows the PRINCIPAL; clean-tree denies branch-create on a dirty tree
  (listing paths) and discriminates list-from-create; no-`-m` denies the -m-after-id footgun without
  false-flagging a body that mentions -m. The Stop self-check blocks once-per-turn via decision:block+reason
  (not additionalContext), releases via sentinel, honors stop_hook_active, and degrades the NOMOS forward-ref
  vacuously; the seat-tag clause is correctly dropped per brief. FAIL-OPEN holds: with python3 broken every
  gate allows rather than bricking. All 4 defects ADA reported fixed are independently confirmed: heredoc-
  consumed-stdin (json_field returns the command via -c), no-`-m` false-positive (mentions-only allowed),
  allow-list lookup + array/JSON/YAML/TOML extraction (per-element field-anchored audit), and JSON-bool
  serialization (true/false lowercase). bash -n clean on all scripts + install.sh; settings-hooks.json valid
  JSON; no bare-pointer trigger payloads; op-disc §34 codifies the authoring rule. The only methodology note
  is the P11 "12 CAPTAINs" figure belonging to Stage 2 (Stage 1 correctly ships 11) — a probe-text/staging
  mismatch, not a build defect. Verdict: pass.
