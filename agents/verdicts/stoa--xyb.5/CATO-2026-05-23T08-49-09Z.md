status: completed
ticket: stoa--xyb.5
verdict: pass-with-nits
diff_reviewed: main..arc-46/build @ d70f7e2 (substrate/ + agents/design/arc-46/design-rev1.md; agents/ artifacts excluded per brief)
design_artifact_compared_against: agents/design/arc-46/design-rev1.md (STAGE 1 scope)

SAFETY HEADLINE (independent meta-verify of VERA — can anything arm a live hook?): NO. No live-arming path exists.
- install.sh trace: the ONLY settings.json WRITE is install.sh:1219 (`sed s|{{HOOKS_DIR}}|...| "$_candidate" > "$DEST_SETTINGS_JSON"`). It is reachable ONLY in the project-tier branch (the `else` of `if [ "$TARGET" = "user" ]` at :1186). User-tier hits the print-only `elif [ "$TARGET" = "user" ]` (manual-merge runbook, never writes ~/.claude/settings.json). Subproject is guarded out at :1179 (`[ -z "$DEST_SETTINGS_JSON" ]`). Project-tier ALSO refuses to overwrite a pre-existing target settings.json (:1212 prints merge instruction, no write).
- Empirical: live ~/.claude/settings.json sha256 = c79ae66... BYTE-IDENTICAL before/after a project-tier `--enable-hooks` run, a user-tier `--enable-hooks --dry-run`, and ALL gate isolation probes. Re-confirmed byte-identical at review end.
- Deploying the SCRIPTS is INERT (Claude Code only fires hooks registered in a settings.json). Default-OFF holds: routine install deploys scripts + candidate template, arms nothing.
- ARGUS r4 (user-tier "never the running session" precision) is correctly IMPLEMENTED in the build: user-tier prints manual-merge, never auto-writes. VERA's redirected-HOME throwaway test independently corroborates.

concerns:
- id: c1
  category: craft
  description: User-tier install plan-line reads "arm hooks : YES (--enable-hooks) -> merge candidate block into ~/.claude/settings.json" but the actual user-tier behavior is print-only manual-merge (never writes). The plan-line phrasing overstates the action at user tier.
  evidence: substrate/install.sh:786 (`echo "  arm hooks        : YES (--enable-hooks) -> merge candidate block into ${DEST_SETTINGS_JSON}"`) vs the print-only handling at :1186-1200. Cosmetic — the safety behavior is correct; only the plan-line wording is imprecise.
  severity: minor
  quadrant_classification: easy-easy
- id: c2
  category: hygiene
  description: _hooklib.sh is deployed chmod +x (install.sh:5c deploy loop chmods every *.sh) although it is only ever sourced, never executed and never registered. Harmless (it carries a shebang; the template registers only the gate scripts) but the +x bit on a source-only lib is mildly misleading.
  evidence: substrate/install.sh deploy loop `chmod +x "$dest"` over `*.sh`; settings-hooks.json registers only the 4 gates, never _hooklib.sh. Confirmed deployed -rwxr-xr-x in throwaway.
  severity: minor
  quadrant_classification: easy-easy
- id: c3
  category: coverage
  description: Author-field gate scope is bounded to an enumerated author-encoding file set (basenames + *.md + .claude-plugin/*); a non-PRINCIPAL author-like field in a file OUTSIDE that set (e.g. notes.txt) is NOT caught. This is consistent with the global discipline's enumerated file set and the design's named weak point (§9.2) — a documented backstop boundary, not a defect. Surfaced for completeness; the prose discipline remains primary defense.
  evidence: substrate/hooks/pretooluse-author-field-audit.sh is_author_encoding_file(); design-rev1 §5.1a + §9 weak-point 2; empirically confirmed (notes.txt with non-PRINCIPAL author -> ALLOW).
  severity: minor
  quadrant_classification: easy-hard

follow_ups:
- (from c3) If a future arc wants tighter authorship coverage, consider whether the author-encoding file set should widen — but only on evidence; the current enumerated set matches the global discipline and over-broadening risks false-positive denies. Properly Stage-2-or-later, not a Stage-1 block.
- (ARGUS r4 already addressed in build) No follow-up needed; recorded that the user-tier safety precision ARGUS flagged is implemented.

verifier_coverage_assessment: >
  VERA exercised the load-bearing cases. I independently re-ran the SAFETY headline (P2-class: live settings.json byte-identical across project-tier --enable-hooks + user-tier dry-run), all 4 ADA-fixed defects (P3/P6/P7-class), the author-gate end-to-end (P3/P4 + bypass attempts), fail-OPEN on BOTH malformed-JSON and python3-absent (design §9.1 weak point — confirmed no spurious block, exit non-2 = non-blocking per platform contract), clean-tree gate (P5 + non-create-verb negatives), and the Stop sentinel once-per-turn loop-guard (P7). I ADDED two probes beyond the design's named set: (a) §6.8 environment-interactive — gates run correctly inside a git WORKTREE where .git is a FILE (the stoa--148 bug class is ABSENT; the gates use the porcelain `git -C` interface, not raw .git reads); (b) the extract_author_fields substring-trap (authority/byline correctly NOT matched via the left word-anchor). VERA's redirected-HOME user-tier test is a stronger user-tier safety probe than my dry-run. No coverage gap found; no INCOMPLETE/UNVERIFIABLE-class case is silently passed. VERA's coverage is sound.

summary: >
  Stage 1 ships the deterministic enforcement tier: 4 hook scripts (_hooklib + 3 PreToolUse gates + Stop self-check), a candidate settings-hooks.json template, install.sh hooks-deploy + --enable-hooks (DEFAULT-OFF) wiring, and a thin op-disc §34 authoring rule with detail dogfooded into hooks/README.md. The diff is exactly the 10 named files, no scope creep, clean hygiene (no TODO/debug/dead code), bash -n green on all 6 scripts. The SAFETY headline is CLEAR: there is no code path where any install run arms a hook in a live settings.json at either tier — the only write is project-tier-target-only, user-tier is print-only, and the live ~/.claude/settings.json is byte-identical across every probe. All 4 ADA-fixed build defects are independently confirmed fixed and correct. The author-field gate (the backstop for the global #1 authorship-regression concern) is sound: correct allow-list lookup, multi-format (JSON/YAML/TOML/array) extraction, substring-trap-immune field anchoring, and end-to-end denies on non-PRINCIPAL author fields, git identities, and .md frontmatter while allowing the PRINCIPAL. Fail-OPEN is correct (degrades to no-enforcement, never a spurious block) on both python3-absent and parse-error. §34 is thin and well-placed; the thin-canon + detail-in-README dogfood split is correct. The three findings are all minor/cosmetic (a plan-line wording overstatement at user tier; a +x bit on a source-only lib; the documented author-encoding-file scope bound). Overall posture: clean, ready for final gate. NO blocking concern.
