status: completed
ticket: stoa--xyb.5
verdict: PASS-WITH-RISKS
design_artifact_audited: .claude/worktrees/arc-46-build/agents/design/arc-46/design-rev1.md (committed f9b879e)

audit_block:
  risks:
  - id: r1
    description: NOMOS's PRIMARY canary (seat-tag) keys on a convention that is POLYBIUS-ONLY by canon, but NOMOS audits ORCHESTRATOR (PLINY) outputs, which are case-4 UNTAGGED by canon.
    evidence: operating-disciplines.md §7.7 case-4 + §7.1-beat-5 ("PLINY, CAPTAINs... are NOT required to author-tag"); design §5.4 probe1 + §5.5 + Stop clause-A (§5.2).
    load_bearing: true
    quadrant_classification: easy-easy
    tag: SHOULD-FIX
  - id: r2
    description: The whole hook layer IS "the mechanical-enforcement layer (pre-comment hook, CI lint)" that §7.7 Future-scope HARD-LOCKS OUT "until non-compliance recurs." Direct canon collision the design does not name.
    evidence: operating-disciplines.md §7.7 "Future scope" final paragraph; design §5 entire (the hook layer).
    load_bearing: true
    quadrant_classification: easy-easy
    tag: SHOULD-FIX
  - id: r3
    description: Design §2/§9 assert "there is no PostCompact event" — factually WRONG (PostCompact exists as a no-decision-control side-effect event). The OPERATIONAL conclusion (no instant reprime; route to SessionStart(compact)+cron) is correct because PostCompact cannot inject context.
    evidence: code.claude.com/docs/en/hooks + /agent-sdk/hooks (live 2026-05-23): PostCompact = "After context compaction completes... No decision control"; design §2 table row 6 + §9 weak-point 4.
    load_bearing: false
    quadrant_classification: hard-easy
    tag: SHOULD-FIX
  - id: r4
    description: --enable-hooks "even then only into the TARGET's settings.json, never the running session's" is imprecise at USER tier — at user-tier the TARGET settings.json IS the running session's (and every project's) live config. A user-tier ~/.claude/settings.json exists today.
    evidence: ~/.claude/settings.json exists (confirmed on disk); design §7.5(b) + §8 framing; P2 probe (§6) diffs only the project .claude/settings.json.
    load_bearing: true
    quadrant_classification: easy-hard
    tag: SHOULD-FIX
  - id: r5
    description: PostToolUse-on-Agent recursion guard (§5.3) depends on the PARENT-context event exposing the dispatched sub-agent's NAME to no-op on NOMOS-return. SDK doc confirms agent_id/agent_type populate WHEN the hook fires INSIDE a subagent — not clearly that the parent's PostToolUse-on-Agent event carries the returned agent's seat name. Design already flags a sentinel fallback.
    evidence: code.claude.com/docs/en/agent-sdk/hooks ("agent_id and agent_type are populated when the hook fires inside a subagent"); design §5.3 recursion-guard + parenthetical fallback.
    load_bearing: false
    quadrant_classification: easy-hard
    tag: CONSIDER
  - id: r6
    description: Stop self-check sentinel (§5.2) is keyed on session id and written to disk; staleness across sessions / sentinel-file cleanup is unspecified, and a stale sentinel silently disables the once-per-turn block.
    evidence: design §5.2 caveat-handling + §9 (sentinel staleness named as weak point, no lifecycle spec).
    load_bearing: false
    quadrant_classification: easy-easy
    tag: CONSIDER
  - id: r7
    description: The 'if' field (Bash(git *), permission-rule syntax) is a cleaner, doc-blessed arg-matching idiom than the design's "script inspects tool_input.command" string-parsing for all three PreToolUse gates. Not wrong, but more brittle than the available idiom.
    evidence: code.claude.com/docs/en/hooks live: matcher matches tool-name only; arg-filtering is the 'if' field; design §2 + §5.1 all gates inspect tool_input.command in-script.
    load_bearing: false
    quadrant_classification: easy-easy
    tag: CONSIDER

  non_findings:
  - HARD SAFETY CONSTRAINT (no live-session arming) — DISCHARGED. install.sh has no settings.json handling and no --enable-hooks today (grep-confirmed); no .claude/settings.json in worktree or main repo; deploying hook SCRIPTS to .claude/hooks/ is INERT (Claude Code only fires hooks registered in settings.json, which this arc never writes). Even a misdirected throwaway-target deploy drops dormant files, cannot arm a hook. P2 probe is sound for the project tier. No path where ADA arms a hook in the live session. NOT BLOCKING.
  - Stop additionalContext drift — design's PARTIAL-DRIFT correction is CORRECT (Stop = top-level decision/reason only; verified live).
  - PreToolUse deny + permissionDecisionReason — CONFIRMED live.
  - PostToolUse additionalContext — CONFIRMED live.
  - Agent as matchable tool name — CONFIRMED live.
  - SubagentStop no additionalContext — CONFIRMED live (validates the PostToolUse-on-Agent choice).
  - "11 envelopes -> 12" CAPTAIN count — CORRECT (11 CAPTAIN_*.md confirmed on disk; NOMOS is the 12th).
  - modules glob-deploy idiom the design mirrors — exists exactly as described (nullglob, DEST_*_DIR per tier, subproject-skip).
  - fail-OPEN choice (R5) — CORRECT for this layer; degrades to today's no-enforcement baseline; fail-closed would brick the live team.

summary: >
  A two-tier harness-owned enforcement layer (deterministic PreToolUse gates + judgment-tier
  Stop/PostToolUse-on-Agent checker + a new CAPTAIN_NOMOS) shipped as substrate SOURCE only,
  default-OFF, tested in isolation. The HARD SAFETY CONSTRAINT holds: no path in this arc arms a
  hook in the live session — install.sh writes no settings.json, the live tree has none, and hook
  SCRIPTS on disk are inert without settings.json registration. That is the load-bearing clearance
  and it is airtight at the project tier. The most important load-bearing risk is NOT a safety
  defect but a CANON COLLISION: §7.7 makes the seat-tag a POLYBIUS-only convention and explicitly
  HARD-LOCKS OUT "the mechanical-enforcement layer (pre-comment hook, CI lint)" until non-compliance
  recurs — yet NOMOS's primary canary keys on that tag against PLINY outputs that canon says are
  untagged, and this whole arc IS that hard-locked-out layer. PLINY must adjudicate the canon
  collision (it is PLINY's call whether xyb makes Arc 46 the "non-compliance recurs" trigger that
  unlocks §7.7's mechanical layer, and whether to scope the tag-convention to cover PLINY outputs).
  Web-verification surfaced one factual error (PostCompact DOES exist, side-effect-only) with an
  intact operational conclusion. Overall posture: minor revisions + one canon adjudication; the
  deterministic Stage 1 is buildable today. Verdict PASS-WITH-RISKS, no BLOCKING.
