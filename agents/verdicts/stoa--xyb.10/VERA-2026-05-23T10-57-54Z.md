status: completed
ticket: stoa--xyb.10
verdict: pass

LOSSLESS-COMPLETENESS HEADLINE (separate from the line count): PASS. Walked the
relocation ledger (§3) chunk-by-chunk against the build. Every CONDITIONAL chunk
(11) has a non-empty disk module home; every PROVENANCE rule (10 standalone C-1 +
1 C-2 + 9 move-with) has an Anchor and is recoverable (spot-checked 5 C-1 tickets
carry the deleted N=1 story; C-2 child stoa--xyb.10.1 carries the full §5.4 prose +
ariadne--b93 origin + matching module Anchor); the 1 DUPLICATE (§6.1 cookbook
tables) consolidated to the op-disc §12 pointer with the heading kept LIVE +
PLINY-framing residue (bw prime reflex + L256 bw-prime->op-disc§9 + TIRO delegation
+ SPEC cite-marker) retained; all KEEP always-on rules inline. NO LOST CANON. The
3-OWNER subproject recompose re-inlines all 11 PLINY bodies (869-line full band)
without regressing POLYBIUS (5) or op-disc (12), and FAIL-LOUD aborts the deploy
(exit 2, no partial file) on a missing module. The hard spec-xref gate holds: 7/7
at-risk PLINY cites PASS, zero flips.

LINE COUNT (reported, NOT the bar): MAJOR_PLINY.md = 379 lines (was 801; 53%
reduction). Above the design's ~250-330 estimate but within the 330-420
partial-not-failure band. Per the recalibrated PRINCIPAL-ratified criterion, the
floor is an OUTPUT not a target; 379 is the always-on orchestrator core's honest
weight. NOT a falsifier.

design_artifact_verified_against: C:/Users/denso/claude_projects/the-stoa/.claude/worktrees/arc-48-build/agents/design/arc-48/design-rev1.md
build_verified: arc-48/build @ 822cf34 (worktree C:/Users/denso/claude_projects/the-stoa/.claude/worktrees/arc-48-build)

probes_executed:
- probe_id: P-FLOOR
  description: empirical floor reported + sanity band + per-module re-bloat check
  command_or_method: wc -l substrate/MAJOR_PLINY.md ; wc -l each of 11 modules
  expected: floor REPORTED; sanity band, NOT a hard line target; no module a re-bloat monolith
  observed: MAJOR_PLINY=379 (band 330-420 partial-not-failure); largest module background-dispatch-hygiene.md=112 (the canonical poll-loop; largest existing op-disc autonomous-mode-setup=252, accepted); other 10 modules 17-75 lines
  result: pass
  quadrant_classification: easy-easy
- probe_id: P-COND
  description: every CONDITIONAL section has a real non-empty module home (11)
  command_or_method: test -s substrate/modules/<m>.md for all 11
  expected: all 11 module files exist non-empty
  observed: all 11 OK (ada-brief-preamble, sub-agent-watchdog, per-worktree-venv, post-strabo-vera, incomplete-unverifiable-routing, smoke-beat-deploy-check, background-dispatch-hygiene, pre-branch-hygiene, arc-close-hygiene, seat-identity-brief, pliny-polling-pattern)
  result: pass
  quadrant_classification: easy-easy
- probe_id: P-STUB
  description: every relocated section leaves a heading stub at its original number; §6.1 stays LIVE
  command_or_method: grep -nE '^### (5.2|...|6.2) ' MAJOR_PLINY.md ; grep '^### 6.1 '
  expected: §5.2/5.3/5.4/5.5/5.6/5.7/5.8/5.9/5.9.4/5.10/5.11/5.12/6.2 headings present (stubs); §6.1 present (live)
  observed: all 13 relocated headings present (L176-260); §6.1 present LIVE at L250
  result: pass
  quadrant_classification: easy-easy
- probe_id: P-SPEC-XREF
  description: HARD GATE - SPECIFICATION.md MAJOR_PLINY refs do NOT regress (r1-class)
  command_or_method: python substrate/skills/validate-spec/_lib/spec_refs.py --spec SPECIFICATION.md --repo-root . ; python adjudicator over at-risk set
  expected: all 7 at-risk PLINY cites (5.10x3 L448/562/625, 6.1 L533, 6.2x3 L264/573/595) PASS post-cut; zero flips; none missing
  observed: AT-RISK SEEN=all 7; FLIPPED-TO-FAIL=[]; NOT-FOUND=[]; RESULT=PASS. Only resolver FAIL is pre-existing N (L7) reading-note prose (FAILs pre- and post-cut, not in at-risk set, not a regression)
  result: pass
  quadrant_classification: easy-easy
- probe_id: P-XREF
  description: every MAJOR_PLINY §N cited from an active substrate file resolves post-cut
  command_or_method: grep substrate cites; assert heading line exists for each (re-run with corrected anchored regex - see methodology_concerns)
  expected: every cited §N (5,5.8,5.9,5.9.4,5.10,6.1,6.2,7,7.2,8,9) resolves to a heading line
  observed: all 11 cited §N have heading lines present (## 5./### 5.8 etc). Design's verbatim probe regex false-MISSING'd all 11 (em-dash inside POSIX bracket + headings carry no § symbol the probe optionally expected); corrected anchored regex confirms all OK; the authoritative spec resolver independently confirms the 3 spec-cited subsections PASS
  result: pass
  quadrant_classification: easy-easy
- probe_id: P-C1
  description: C-1 content-check gate honored - cited tickets carry the deleted N=1 story (sampled)
  command_or_method: bw show <id> for stoa--nvl, stoa--ads, stoa--nax, stoa--bxx, stoa--dyb
  expected: each ticket body materially carries the deleted inline story (not a thin stub)
  observed: stoa--nvl=§5.8 background-dispatch empirical (full); stoa--ads=§5.10 base-vs-custom (full); stoa--nax=§7.8 redundant-checks discipline (full); stoa--bxx=§5.2/§6.3 ADA-brief story (full); stoa--dyb=§5.3 watchdog (full). 5/5 carry the story
  result: pass
  quadrant_classification: hard-easy
- probe_id: P-C2
  description: C-2 archive-first executed BEFORE deletion in a dedicated child ticket (§5.4)
  command_or_method: bw list --all | grep xyb.10.N ; bw show stoa--xyb.10.1 ; grep module Anchor
  expected: §5.4 child ticket exists (parent stoa--xyb.10), carries verbose per-worktree-venv prose + ariadne--b93 origin, module Anchor cites correct child id
  observed: stoa--xyb.10.1 exists (parent stoa--xyb.10); description carries full §5.4 reflex prose + detection + ariadne--b93 origin + why-C-2-not-C-1; per-worktree-venv.md L18 Anchor cites stoa--xyb.10.1 + restates cross-repo origin; §5.4 stub present in MAJOR_PLINY with body relocated
  result: pass
  quadrant_classification: hard-easy
- probe_id: P-SPLIT
  description: SPLIT LIVE cross-refs preserved inline; PROVENANCE folded into Anchors
  command_or_method: grep LIVE pointers (MAJOR_POLYBIUS §13/§4.3.1, op-disc §10/§11/§19/§6.7.1) ; grep folded Anchors
  expected: every LIVE pointer resolves inline; PROVENANCE bw-ids in §N Anchor lines (not orphaned bullets)
  observed: §5.1 SPLIT - MAJOR_POLYBIUS §13 + op-disc §10/§11 at L174 (LIVE); §7.2 SPLIT - op-disc §19 + MAJOR_POLYBIUS §4.3.1 at L309 (LIVE) with stoa--ioy/u--7yg.10/.18/stoa--ezj folded into the §7.2 Anchor; §7.8 SPLIT - op-disc §6.7.1 at L333 (LIVE) with stoa--nax folded into the §7.8 Anchor (L343). (Design verbatim grep used § directly adjacent; flexible re-check confirms all present)
  result: pass
  quadrant_classification: easy-easy
- probe_id: P-ROUTING-INDEX
  description: orchestrator gets BOTH routing map AND relocation index; every relocation covered
  command_or_method: grep 'Routing map' / 'Relocation index' / '### 4.2 ' ; grep each module appears
  expected: §4.2 present; both tables present (orchestrator shape, assumption 1); all 11 modules in routing + index
  observed: §4.2 at L80; Routing map at L84; Relocation index at L102; all 11 modules appear in routing/index rows
  result: pass
  quadrant_classification: easy-easy
- probe_id: P-LYW
  description: stoa--lyw /resume INVOCATION fold landed (separable slice)
  command_or_method: grep '### 4.1 ' + decision rule + stale-id fall-through + multi-gen nav + Anchor stoa--lyw
  expected: §4.1 with all 4 lyw load-bearing elements
  observed: §4.1 at L64; decision rule OK; stale-id fall-through OK; multi-gen nav OK; Anchor: stoa--lyw OK
  result: pass
  quadrant_classification: easy-easy
- probe_id: P-RECOMPOSE
  description: subproject recompose completeness for MAJOR_PLINY (LOST-CANON-at-subproject)
  command_or_method: install.sh --target subproject against THROWAWAY mktemp; assert 11 paired markers, all bodies, full band, POLYBIUS/op-disc intact
  expected: install exit 0; 11 PLINY paired markers no empty pair; all 11 bodies present; recomposed in full band (~800+); POLYBIUS 5 + op-disc 12 intact
  observed: exit 0; opens=closes=11, no empty pairs; all 11 module first-heading bodies present; recomposed MAJOR_PLINY_myproj.md=869 lines (full band); POLYBIUS markers=5, op-disc markers=12 (no regression). Throwaway target cleaned up
  result: pass
  quadrant_classification: easy-easy
- probe_id: P-RECOMPOSE-NEG
  description: FAIL-LOUD asserted - losslessness depends on err() firing per-owner
  command_or_method: mv away background-dispatch-hygiene.md; install.sh against throwaway; restore immediately
  expected: install exits non-zero with Check-A 'no module source' error; NO partial/slim MAJOR_PLINY written to subproject
  observed: exit 2; error 'recompose: marker MODULE-INLINE:background-dispatch-hygiene has no module source'; NO partial MAJOR_PLINY_myproj2.md written; module source restored, worktree git-clean. (POLYBIUS+op-disc recomposed first then PLINY aborted the deploy - slim PLINY correctly NOT shipped)
  result: pass
  quadrant_classification: easy-easy
- probe_id: P-OWNERSHIP
  description: 3-owner partition correct - no cross-owner Check-B false-positive
  command_or_method: install.sh against throwaway (covered by P-RECOMPOSE happy path)
  expected: exit 0 - all three recompose calls pass, no owner trips Check B on another's modules
  observed: exit 0; POLYBIUS scoped to 5, op-disc to 12, PLINY to 11; no Check-B false-positive across owners (the 3-owner recompose completed cleanly)
  result: pass
  quadrant_classification: easy-easy
- probe_id: P-OWNERSHIP-NOCOLLIDE
  description: no module basename collides across the three owner-sets (NEW failure surface this arc)
  command_or_method: python parsing the LIVE install.sh POLYBIUS/OPDISC/PLINY_MODULES vars (per ARGUS r2, parse-not-hardcode) + source-file existence
  expected: zero collisions across 28 basenames; all three vars defined distinctly; every PLINY basename has a real source
  observed: POLYBIUS=5, OPDISC=12, PLINY=11; TOTAL=28 DISTINCT=28 COLLISIONS=[] RESULT=PASS; all 11 PLINY source files exist. Parsed from live install.sh (not the hardcoded probe copy - closes ARGUS r2)
  result: pass
  quadrant_classification: easy-easy
- probe_id: P-AUTH
  description: no author-field regression (CLAUDE.md authorship discipline)
  command_or_method: grep author-like fields across MAJOR_PLINY + 11 modules
  expected: no author-like field names anyone (MAJOR_PLINY none; 11 modules none)
  observed: grep exit 1 (zero matches) across MAJOR_PLINY + all 11 modules. No author-field regression
  result: pass
  quadrant_classification: easy-easy
- probe_id: P-KEEP
  description: always-on KEEP rules still inline (orchestrator availability-losslessness)
  command_or_method: grep each always-on rule
  expected: all always-on orchestrator rules resolve inline
  observed: §1 what-you-are OK; §3 don'ts OK; §5 gauntlet diagram OK; §5.1 operating-mode OK; §7.2 verify-then-execute OK; §7.1 one-job OK; §7.4 autonomous-ship OK; §6.3 bundle-shape OK; §6.1 bw-prime residue OK; §9 checklist OK. All 10 inline
  result: pass
  quadrant_classification: easy-easy
- probe_id: CONSIDER-1 (ARGUS r1)
  description: §6.1 keep-list L616 (bw prime errors -> op-disc §9) survived the cut
  command_or_method: grep 'bw prime' MAJOR_PLINY.md ; inspect §6.1 body
  expected: the bw-prime-errors->op-disc§9 pointer kept (ARGUS r1 flagged it unnamed in keep-list but outside delete range)
  observed: L256 '`bw prime` errors? See `operating-disciplines.md` §9.' KEPT; §6.1 slim residue correct (cookbook tables gone, heading live, §12 pointer + TIRO delegation + SPEC cite-marker present)
  result: pass
  quadrant_classification: easy-easy
- probe_id: bash-n
  description: install.sh syntax-valid after the 3rd owned-set + recompose call
  command_or_method: bash -n substrate/install.sh
  expected: exit 0
  observed: exit 0
  result: pass
  quadrant_classification: easy-easy

methodology_concerns:
- "P-XREF probe-regex defect (NOT a build defect; canon intact). The design's verbatim P-XREF resolution regex `^(#+) §?${n//./\\.}( |\\.|—|-|:)` false-MISSING'd ALL 11 cited §N: (a) the literal em-dash `—` inside the POSIX bracket expression breaks matching in this grep build, and (b) MAJOR_PLINY headings carry NO `§` symbol (they are `## 5.` / `### 5.8`), so the optional `§?` plus the bracket mismatch yields zero matches. A corrected anchored regex (`^#{2,4} <n>([. ]|$)`) confirms all 11 headings resolve, and the authoritative spec resolver (spec_refs.py) independently confirms the 3 spec-cited subsections PASS. Per CAPTAIN_VERA.md §5.11 (probe-spec regex anchoring): the probe author (DAEDALUS) should anchor + de-em-dash this regex on next touch so the probe verifies what it intends rather than uniformly false-failing. Non-blocking for this ship (corrected re-check + authoritative resolver both PASS)."
- "P-SPLIT probe-grep adjacency nit (NOT a build defect). The design's verbatim P-SPLIT first grep used `§13` (etc.) directly adjacent, which missed the live `§13.2`/`§13 ` spacing variants present at L174. A flexible re-check (`§13` with bounded gap) confirms every LIVE pointer is present inline and every PROVENANCE bw-id is folded into the §N Anchor (not orphaned). Same §5.11 anchoring-discipline class as the P-XREF nit; non-blocking."
- "ARGUS r2 (NOCOLLIDE probe hardcodes the owned-set strings) was addressed at verification time: I ran P-OWNERSHIP-NOCOLLIDE by PARSING the live install.sh vars rather than the hardcoded probe copy, confirming the probe and install.sh agree (28 distinct, 0 collisions). The design's probe-as-written still hardcodes; recommend DAEDALUS fold the parse-from-install.sh form into the canonical probe so future drift between probe copy and install.sh is caught. Non-blocking (verified equivalent this arc)."

falsifying_evidence_summary: (empty - verdict pass)

verification_artifacts_path: C:/Users/denso/claude_projects/the-stoa/.claude/worktrees/arc-48-build/agents/verdicts/stoa--xyb.10/ (probe outputs recorded inline in this verdict; throwaway recompose targets under mktemp dirs were cleaned up per ISOLATION; spec_refs_postcut.jsonl removed)

summary: MAJOR_PLINY.md (801-line ORCHESTRATOR role file, deploys at all 3 tiers) was cut to 379 lines via the proven 3-relocation-class method (11 CONDITIONAL -> disk modules; 14 PROVENANCE -> bw cites incl. 1 C-2 child; 1 DUPLICATE -> op-disc §12 pointer) plus a separable lean stoa--lyw /resume-invocation fold (§4.1). I verified LOSSLESS-ON-CANON by walking the relocation ledger chunk-by-chunk against the build: every CONDITIONAL chunk has a non-empty module home; every PROVENANCE rule has a recoverable Anchor (5/5 C-1 tickets spot-checked carry the deleted N=1 story; the C-2 child stoa--xyb.10.1 holds the full §5.4 prose with a matching module Anchor); the DUPLICATE §6.1 consolidated to op-disc §12 while keeping its spec-cited heading LIVE and its PLINY-framing residue (incl. the ARGUS-r1-flagged L256 bw-prime->op-disc§9 pointer); all 10 always-on KEEP rules remain inline. NO LOST CANON. The load-bearing probes were the HARD GATE P-SPEC-XREF (7/7 at-risk PLINY cites PASS, zero flips - only FAIL is the pre-existing §N reading-note) and the 3-OWNER recompose against a THROWAWAY mktemp target: P-RECOMPOSE re-inlined all 11 PLINY bodies (869-line full band) without regressing POLYBIUS (5) or op-disc (12), P-RECOMPOSE-NEG confirmed FAIL-LOUD (exit 2, no partial deploy) on a missing module, and P-OWNERSHIP-NOCOLLIDE (parsed from live install.sh, closing ARGUS r2) confirmed 28 disjoint basenames. Two probe-regex defects surfaced (P-XREF em-dash-in-bracket + P-SPLIT adjacency) - both probe underspecification, NOT build defects; canon confirmed intact by corrected re-checks + the authoritative spec resolver, and flagged to DAEDALUS per §5.11. The 379-line landing is above the ~250-330 estimate but within the 330-420 partial-not-failure band; per the recalibrated criterion the line count is reported not targeted and is NOT a falsifier - it is the always-on orchestrator core's honest weight. Verdict: pass.
