status: completed
ticket: stoa--xyb.10
verdict: pass
diff_reviewed: main..arc-48/build (commit 822cf34), excluding agents/ — substrate/MAJOR_PLINY.md (801->379), substrate/install.sh (3-owner partition), 11 new substrate/modules/*.md, + stoa--lyw §4.1 fold
design_artifact_compared_against: agents/design/arc-48/design-rev1.md (DAEDALUS rev1) + ARGUS PASS verdict agents/verdicts/stoa--xyb.10/ARGUS-2026-05-23T10-40-46Z.md

concerns:
- id: c1
  category: hygiene
  description: VERA's verifier scratch (spec_refs_arc48.jsonl/.err, spec_refs_main.jsonl, spec_refs_postcut.jsonl) sits untracked in the worktree root; NOT in the committed diff and NOT gitignored. ADA's design Step 6 mandates cleanup of spec_refs_postcut.jsonl; the others are VERA in-flight at my read. No effect on the shipped artifact.
  evidence: git status -s in worktree shows 4 untracked spec_refs_* files at repo root; commit 822cf34 contains none (git show --stat clean)
  severity: minor
  quadrant_classification: easy-easy

follow_ups:
- §5.1 operating-mode cross-ref line (MAJOR_PLINY.md L174) is one dense restatement-heavy line; losslessly tightenable. Correctly DEFERRED to stoa--xyb.9 (always-on prose-compression, not a missed relocation). reason: prose-compression of a KEEP section, out of scope for a relocation-only cut.
- §7.2 verify-then-execute (L295-309) is dense always-on prose; every sub-rule load-bearing, ~30% tightenable. Correctly DEFERRED to .9. reason: same — KEEP-section prose density, not orphaned/missed content.
- §9 activation checklist (27 lines) operationally re-states §4 (79 lines); collapse-to-cross-ref is a .9 judgment call. reason: deliberate checklist redundancy; compression is a reviewed-capstone decision, not this cut's scope.
- op-disc §18 L662 "(canonical inline)" parenthetical for §5.8 poll-loop template is now slightly stale (canonical home moved to background-dispatch-hygiene.md module). Still RESOLVES (top-level §5.8 -> stub -> module); not a regression. reason: a one-line op-disc touch would expand scope into a file this arc does not own; design §9 already flags it for .9.

verifier_coverage_assessment: VERA's COMPLETION verdict was NOT on the dispatch ticket or on disk at my read time (last VERA beat 10:53Z "probe set executing"; only ARGUS's verdict file present). I therefore meta-verified against design-rev1's probe spec by INDEPENDENTLY re-running the load-bearing probes rather than auditing VERA's recorded outputs. Coverage of the design's weak points is GOOD and I found no gap: (1) the hard gate P-SPEC-XREF re-ran clean (7/7 at-risk PLINY spec cites PASS via the live spec_refs.py resolver, zero PASS->FAIL flips, only the pre-existing §N L7 reading-note FAILs); (2) the load-bearing environment-interactive probe — live 3-owner recompose on a THROWAWAY subproject (mktemp, cleaned) — exited 0 with all 11 PLINY marker pairs re-inlined to an 869-line full band, POLYBIUS(5)+op-disc(12) intact (no third-owner regression); (3) FAIL-LOUD (P-RECOMPOSE-NEG) fired exit 2 with a clear Check-A error and shipped NO partial/slim PLINY to the subproject (the losslessness-at-subproject guarantee); (4) P-OWNERSHIP-NOCOLLIDE parsed from install.sh (not hardcoded) = 28 distinct basenames, zero collisions, AND the owned-set union EXACTLY equals the substrate/modules/*.md glob (no orphan module unowned, no phantom owned-set entry without a source — stronger than the design's probe asserts). ZENO independently posted SPEC-MET (11/11 mechanical checks) which corroborates. The single coverage caveat is process-not-substance: VERA's verdict-of-record should be confirmed present before PLINY closes the gauntlet; my independent re-runs cover the same surface VERA's design probes name.

summary: A clean, disciplined relocation cut. MAJOR_PLINY.md 801->379 (~53%), 11 CONDITIONAL sections relocated to well-formed disk modules (each: stable # Title first line, design+epic provenance header, NO author field, no stray MODULE-INLINE in body), 1 DUPLICATE (§6.1 cookbook tables) consolidated to op-disc §12 with the spec-cited heading kept live + the PLINY-framing residue (bw prime / TIRO / cite-marker) + the L256 bw-prime->op-disc§9 pointer preserved, 1 C-2 cross-repo provenance archived to child stoa--xyb.10.1 with full prose + matching cite-back. The §6.1 hybrid (the design's flagged mis-execution risk, weak point 2) is executed precisely: original L594-614 tables deleted, L588/592/616/618 residue kept — neither over- nor under-deleted. SPLIT cross-ref tails (§7.2 verified) keep LIVE pointers verbatim and fold PROVENANCE ids into the section Anchor. install.sh extension is the minimal Arc-47-designed change: +PLINY_MODULES var + 3rd recompose call + comment updates, ZERO function-body logic change, bash -n clean, Check A owner-agnostic-glob. The orchestrator-gets-BOTH-tables call (design Q1, ARGUS concurred) is materialized with full 3-way completeness — every one of the 11 modules in the routing map AND the relocation index AND a paired marker. The stoa--lyw §4.1 fold is lean (~14 lines), separable, and complete (decision rule + stale-id fall-through + dormant-vs-lost + multi-gen navigation + Anchor: stoa--lyw). Authorship: Author=PRINCIPAL (denson/densonsmith2@gmail.com), Co-Authored-By: CAPTAIN_ADA_the-stoa seat trailer present, no author-like field touched anywhere. THE 379-FLOOR JUDGMENT: 379 is the honest no-rules-cut floor and the gauntlet should accept it. It lands in the design's 330-420 partial-not-failure band, ABOVE the ~250-330 estimate precisely because the cut correctly REFUSED to compress always-on prose (kept §7.2 full, kept BOTH index tables, kept §9 as a real checklist) — that is the recalibrated criterion working as intended, not a shallow cut. P-KEEP-class always-on rules all resolve inline; P-FLOOR low-end guard (<200 = dropped-rule) is not tripped. The §5.1/§7.2/§9 sections ADA flagged for .9 are genuinely PROSE-COMPRESSION of KEEP sections, NOT relocation this cut missed — I spot-checked each: §5.1 is one already-folded cross-ref line, §7.2 keeps every load-bearing sub-rule, §9 is a deliberate checklist restatement of §4. Deferring them to the single reviewed .9 capstone is correct; pulling them into a relocation-only cut would mix scopes. One minor hygiene nit (untracked verifier scratch, not in the diff). Overall posture: CLEAN — ready for the final gate.
