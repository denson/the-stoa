status: completed
ticket: stoa--xyb.10
verdict: pass
design_artifact_audited: .claude/worktrees/arc-48-build/agents/design/arc-48/design-rev1.md
audit_block:
  risks:
  - id: r1
    description: "Section 6.1 keep/delete line-enumeration (design 3.5/2.5) names keep 592/618 + the op-disc 12 pointer; delete 594-614, but does not explicitly name L616 (the bw prime errors -> op-disc 9 pointer), which is PLINY-seat operational framing that must be KEPT."
    evidence: "source MAJOR_PLINY.md L616 vs design 3.5/2.5; L616 falls OUTSIDE the delete range (594-614) so survives by default, but the keep-list does not name it -> ambiguous for ADA on the least-mechanical deletion in the cut."
    load_bearing: false
    quadrant_classification: easy-easy
  - id: r2
    description: "P-OWNERSHIP-NOCOLLIDE python hardcodes the three owned-set strings rather than parsing them from install.sh, so the probe could pass even if install.sh PLINY_MODULES drifts from the probe copy."
    evidence: "design 4 P-OWNERSHIP-NOCOLLIDE (L541-554) hardcodes poly/opd/pliny lists in the python heredoc; the trailing grep prints the install.sh vars but does not assert equality with the hardcoded lists."
    load_bearing: false
    quadrant_classification: easy-easy
  - id: r3
    description: "op-disc 18 L662 parenthetical (canonical inline) for the bw-poll-loop template becomes imprecise once 5.8 relocates to background-dispatch-hygiene.md. Cross-ref still RESOLVES (top-level 5.8 -> stub -> module), so not a break."
    evidence: "op-disc 18 L660-664 (5 cites, all top-level 5.8) + design 7-weakpoint-3/9-out-of-scope. Verified all 5 cites are top-level 5.8 -> resolve to the stub. Design correctly defers the one-word wording fix to the capstone."
    load_bearing: false
    quadrant_classification: easy-easy
  non_findings:
  - "SPEC xref completeness: independently ran live spec_refs.py -- at-risk PASS set = 5.10 x3 (L448/562/625), 6.1 (L533), 6.2 x3 (L264/573/595); 3 distinct anchors; only FAIL is N (L7) reading-note prose (pre-existing, not a regression). Census COMPLETE -- no other relocated subsection is spec-cited."
  - "Heading-pattern resolver: _heading_pattern_for_anchor (L63-70) matches stub headings; prose-only would NOT match (Arc-47-r1 guard). Resolves by exact heading line not line-number, so heading-line preserve is the correct discipline."
  - "Substrate xref: distinct cited set {5,5.8,5.9,5.9.4,5.10,6.1,6.2,7,7.2,8,9} -- every number homed (KEEP / stub-heading / live 6.1 / Anchor)."
  - "install.sh 2-set machinery: Check A globs filesystem owner-count-agnostic (L944-946); Checks B/D owned-scoped (L947-949). Third owner = +PLINY_MODULES var + 3rd call, ZERO function-body change. L1006-1008 forward-note + DEST_PLINY@L849-before-L893 verified."
  - "P-OWNERSHIP-NOCOLLIDE: 11 PLINY basenames independently verified disjoint from the 17 existing (5 POLYBIUS + 12 op-disc)."
  - "C-1 recovery: spot-checked stoa--nvl (5.8 story), stoa--ads (5.10), stoa--nax (7.8) -- all carry the deleted inline prose. Per-deletion content-check gate + VERA re-sample is the correct downstream guard."
  - "C-2 (5.4): ariadne--b93 independently confirmed NON-resolving in the-stoa bw; stoa--bxx (the 5.2/6.3 co-anchor) DOES resolve. C-2-child correct because 5.4 has NO stoa-- co-anchor to fold into. Child ticket correctly not-yet-created (ADA creates at build)."
  - "Q1 (orchestrator gets BOTH tables): CONCUR. Arc-45 POLYBIUS (orchestrator) shipped both; Arc-47 op-disc (non-orchestrator) index-only with reasoning routing-map-is-an-orchestrator-artifact. PLINY IS the dispatcher -> Arc-45 precedent governs. Consistent application, not divergence."
  - "Q2 (6.1 DUPLICATE-body/KEEP-heading): CONCUR. modules/README.md L213-214 explicitly names MAJOR_PLINY.md 6.1 -> op-disc 12 consolidation (canon-directed). Design refinement (keep spec-cited heading + non-12 PLINY residue) is MORE careful than the README bare delete+pointer."
  - "Q3 (5.4 C-2 child): CONCUR. See C-2 non-finding."
  - "Q4 (lyw 4.1 always-on 4 vs CONDITIONAL module): CONCUR always-on-lean. /resume-vs-spawn fires at every successor activation + a CONDITIONAL module has the bootstrap problem. lyw ticket 4 questions map exactly to 4.1 4 elements. Cross-ref targets (op-disc 30, handoff-author step 6) verified to exist. Stale-id fall-through is NOT a refusal-as-signal violation (mechanical error, not refusal)."
  - "Q5 (NO-COLLIDE complete new failure surface): CONCUR. Walked all 5 checks A-E: A owner-count-agnostic (filesystem glob); B/D per-call owned-scoped; C/E structural per-file. ONLY new three-owner interaction is a basename in two owned-sets (NO-COLLIDE guards exactly this). No second interaction missed."
  - "Ambient-vs-conditional: 5.8 (run_in_background fire), 5.9 (branch creation), 5.10 (arc close) verified beat-specific from source headers -- frequently-used across an arc lifecycle but each fires at a SPECIFIC beat, not every turn. Relocate-not-keep correct; the routing map is the recovery mechanism (ties to Q1). 6.2 anti-pattern guard correctly travels WITH the procedure + named in the stub."
  - "Floor: 5.8=103, 5.9+5.9.4=92, 5.10+5.11=95 source lines independently confirmed. Largest existing module autonomous-mode-setup.md=252 (Arc-47 accepted); largest PLINY module ~103+header well under. Floor estimate ~250-330 + P-FLOOR band (>420 shallow / <200 suspicious) internally consistent."
  - "lyw separability: 1A (own design section) + 4.1 (own build step) + P-LYW (own probe). ADDS canon (not relocates); not part of the line-reducing cut. Genuinely separable + lean (~22 lines)."
  - "Check E hazard: ZERO literal MODULE-INLINE marker lines in MAJOR_PLINY source -- none of the 11 relocating bodies carries one. Build-time caveat for ADA (6.6) appropriately cautious."
  - "P-AUTH: MAJOR_PLINY carries no author-like frontmatter field; design mandates the 11 new modules carry none. No authorship regression risk."
summary: "MAJOR_PLINY.md (801 lines, ORCHESTRATOR role file, deploys at all 3 tiers) is cut via the proven 3-relocation-class method (11 CONDITIONAL -> modules; 14 PROVENANCE -> bw cites; 1 DUPLICATE -> op-disc 12 pointer) extended to a THIRD install.sh owned-set, plus a separable lean lyw /resume-invocation fold. I walked the file section-by-section against the relocation ledger: every chunk is homed (module / numbered-or-heading-line stub / live-KEEP / Anchor / C-2-child) and recoverable -- NO LOST CANON found. Headline: cross-ref integrity -- I independently re-ran the live spec_refs.py resolver and CONFIRMED the at-risk census is COMPLETE (5.10/6.1/6.2 are the only spec-cited subsections, 7 PASS cites, all preserved via real heading lines; only resolver FAIL is the pre-existing N reading-note; zero PLINY refs flip PASS to FAIL). Substrate xref set fully homed. install.sh 2-set machinery extends correctly to the third owner with zero function-body change; P-OWNERSHIP-NOCOLLIDE (independently verified disjoint) is the COMPLETE new failure surface. I CONCUR on all five residual questions Q1-Q5. The three risks are all NOT load-bearing: a 6.1 keep-list enumeration gap (L616 unnamed but survives), a NOCOLLIDE-probe robustness nit, and the op-disc 18 wording staleness (cross-ref still resolves; correctly deferred). Overall posture: CLEAN -- ready for build with optional minor polish ADA can absorb."
gap_or_blocker: none
