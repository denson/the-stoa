# CAPTAIN_ARGUS verdict - Arc 45 (Arc 2 debloat) - MAJOR_POLYBIUS.md cut design-rev2 (FOCUSED re-confirm)

status: completed
ticket: stoa--xyb.6
verdict: PASS-WITH-RISKS
design_artifact_audited: .claude/worktrees/arc-45-build/agents/design/arc-45/design-rev2.md (committed 4fa6c57)

## Headline

rev2 cleanly resolves r1-r4 + residual-2. The recompose mechanism (the LOST-CANON-at-subproject vector, audited hardest) is now specified to a buildable, FAIL-LOUD, idempotent shape with no edge case that drops a body THIS arc. r2 SPLIT classifications spot-checked against live source - both flagged calls (16.7 sources-cluster, 19.7 L1295 intra-line) are CORRECT. r3 arithmetic now self-consistent (4 SPLIT / 15 PROVENANCE confirmed against live source). residual-2 child-ticket gate is sound. rev2 is ADA-buildable. Two NEW non-blocking spec-precision findings (Check-D wording + a forward-arc module-body constraint) - neither drops canon this arc; both are SHOULD-FIX/CONSIDER precision, not REWORK.

## Confirm/deny of rev1 findings + residual-2

- r1 (recompose stub-marker format) - RESOLVED. The paired HTML-comment sentinel (full-line-anchored open ... close) + awk state-machine + 4 FAIL-LOUD checks (err exits 2) + idempotency genuinely close the silent-drop risk. See edge-case audit below. CLEARED.
- r2 (per-line SPLIT enumeration) - RESOLVED. CLEARED. Both DAEDALUS-flagged least-certain calls spot-checked against live source and CORRECT (detail below).
- r3 (ledger arithmetic) - RESOLVED. Live source has exactly 4 SPLIT cross-ref subsections (16.7/17.6/18.6/19.7, grep-confirmed). 9 C-1 + 2 C-2 + 4 SPLIT = 15 PROVENANCE. Self-consistent. CLEARED.
- r4 (lean-0hl deviation acknowledged) - RESOLVED. section 2.4 + section 3.2 F1 state the deviation from 0hl mirror-verbatim explicitly and by-design, with the build commit message naming it. CLEARED.
- residual-2 (C-2 to dedicated child tickets) - RESOLVED + sound. bw create supports --parent ID + -d TEXT (confirmed live). The archive-FIRST / content-CHECK (bw show child-id confirms verbose prose landed in the description) / THEN-delete ordering in section 3.4 + Step 3 guarantees losslessness. P7 asserts it. The cite-back-uses-ACTUAL-assigned-id instruction handles a non-.6.1/.6.2 id. CLEARED.

## r1 edge-case audit (the LOST-CANON-at-subproject vector - audited hardest)

1. Module body containing a literal MODULE-INLINE string. The 5 module bodies are sourced verbatim from POLYBIUS sections 5/10/11/12/14; live source POLYBIUS has ZERO MODULE-INLINE occurrences (grep-confirmed). modules/README.md does NOT document the marker convention (the brief self-referential-collision worry does NOT materialize - README carries no MODULE-INLINE literal). So no relocated body can contain the sentinel THIS arc. DISCHARGED for this arc. (Latent forward constraint - see NEW finding F-A.)
2. awk regex anchoring. The full-line (^...$) anchoring is a robustness STRENGTH: a sentinel mentioned mid-prose, indented, or inside a code fence with surrounding text does NOT match the state machine. Only a standalone full-line marker triggers. DISCHARGED.
3. Marker split across deploy-time sed. install.sh sed substitutes ONLY NAME_SUFFIX and USER_TIER_DIR placeholders; neither token appears in the marker. recompose runs POST-sed on DEST_POLYBIUS (after L757-765 write). The sed cannot split or alter a marker. DISCHARGED.
4. FAIL-LOUD covers every drop-shaped condition. marker-without-module to Check A; module-without-marker to Check B (the precise LOST-CANON guard); unbalanced to Check C; zero-markers-with-modules to Check D. All four call err() which exits 2 = aborts deploy, never writes a partial/slim file. DISCHARGED.
5. Idempotency. Algorithm keeps OPEN+CLOSE markers and skips between them on re-read, re-inlining fresh from source. Re-deploy safe. Sound for this arc BECAUSE no module body contains a literal close-marker line (confirmed). Forward constraint flagged (F-A).
6. P10 asserts canon-EQUIVALENCE not just marker-absence. P10(a) zero-markers-remain alone would pass a buggy delete-without-inline recompose, BUT P10(b) (each module first-heading present) catches an absent body and P10(c) (FULL line band ~1200+) catches the silent no-op. The (b)+(c) pairing is adequate. Minor: (b) asserts only the first-heading line, not full-body byte-equivalence; (c) full-band check bounds a truncated body. Acceptable as specified (easy-hard quadrant - full byte-equivalence is the INCOMPLETE-probe shape; VERA throwaway-target probe exercises it end-to-end). P10-negative (deliberate break = err fires) covers the FAIL-LOUD assertion. DISCHARGED.

## r2 SPLIT spot-check (the DAEDALUS-flagged least-certain calls)

- section 19.7 L1295 intra-line split. Live source line 1295 genuinely carries TWO payloads on one line: stoa--86k (2026-05-13 empirical anchor, PROVENANCE) AND the section 17.5 + section 18.5 per-class path convention and user-tier housekeeping carve-out clause (LIVE in-file pointer). DAEDALUS split - fold stoa--86k to the section 19 Anchor, keep 17.5/18.5 LIVE inline - is CORRECT. Also confirmed: line 1289 section 17.4 disambiguation parenthetical (op-disc section 17 is unrelated OSS-dep calculus and is NOT the intended target) is correctly classed LIVE and kept VERBATIM - it prevents a same-number cross-file mis-resolution; classing it provenance would have dropped a load-bearing operational note. Correct.
- section 16.7 sources cluster (1037-1040) classed PROVENANCE. Live source labels these Load-bearing sources for THIS arc content - stoa--32b.3 body is the primary source, HANDOFF_POLYBIUS_2026-05-16.md is a canonical worked-example source, retro.md is a load-bearing source for the broader epic and adjacent context (not the primary source for this arc). These are source-of-truth-FOR-CONTENT cites, not operational pointers a reader follows to a discipline. PROVENANCE classification is CORRECT. The Within-this-file 4.5/6/7/14/15 cluster (1041-1046) is correctly LIVE. No LIVE operational pointer is mis-classed as PROVENANCE in the spot-checked subsections.

## audit_block.risks (NEW findings only - rev1 r1-r4 cleared above)

### F-A [CONSIDER] - module-body literal-close-marker constraint is unstated as a forward guard
- description: The idempotency + state-machine correctness depends on NO module body containing a standalone close-marker line (a body close-marker would terminate the skip-region early on re-recompose and re-emit the rest of the body as live lines = corruption). True for THIS arc 5 modules (confirmed - zero MODULE-INLINE in source). But the design states it as DAEDALUS judgment rather than a named guard for the data-driven recompose that the NEXT arc (PLINY cut) reuses. A future module authored with a literal close-marker in a code fence would be a silent corruption vector.
- evidence: design section 6.5 idempotency paragraph + section 8 residual-1; live grep zero MODULE-INLINE strings in substrate/MAJOR_POLYBIUS.md.
- load_bearing: false
- quadrant_classification: hard-easy - hard to anticipate (a future module author choice), trivial to verify (grep module sources for the sentinel before recompose). The natural home is a 5th FAIL-LOUD check or a one-line module-author note; ARGUS does not prescribe which.

### F-B [SHOULD-FIX] - section 6.5 Check-D bullet wording is ambiguous (global vs per-file); section 7.4 has the correct form
- description: section 6.5 Check D reads as the slim core having NO MODULE-INLINE markers but substrate/modules/*.md non-empty triggers err. As literally worded (global modules glob) this FALSE-POSITIVES on PLINY at subproject tier - PLINY has zero markers (not cut yet, grep-confirmed) and the modules glob is non-empty, so a global Check D would abort every subproject deploy. section 7.4 resolves it correctly (scope Check D to this role file has zero markers but references modules that exist for it), but the section 6.5 bullet and section 7.4 read inconsistently. ADA implementing literally from section 6.5 would ship the broken global form.
- evidence: design section 6.5 Check D bullet vs section 7.4 weak point vs section 6.5 generality note; install.sh L762 deploys PLINY from same path; grep zero MODULE-INLINE in substrate/MAJOR_PLINY.md.
- load_bearing: false (this arc scopes recompose to DEST_POLYBIUS only per residual-3 rec below, so the global-Check-D footgun does not fire this arc - but the wording should be fixed so it cannot fire when the next arc wires the both-files loop)
- quadrant_classification: easy-easy - tighten the section 6.5 Check D bullet to key on THIS role file (DEST_POLYBIUS) having zero MODULE-INLINE markers so it matches section 7.4. Trivial spec edit, no redesign.

## non_findings (re-confirmed / discharged this pass)

- Whole-cut losslessness: NOT re-walked (already PASS in rev1; brief scoped this re-confirm to rev2 deltas). rev2 carries the rev1 ledger verbatim - no re-audit needed.
- SPLIT subsection count: re-confirmed 4 (grep). r3 arithmetic self-consistent.
- CONDITIONAL section headings: re-confirmed sections 5/10/11/12/14 exist at cited source lines (159/538/638/729/885).
- err() FAIL-LOUD: confirmed exits 2 (non-zero) - all 4 checks genuinely abort deploy.
- C-2 child tickets: confirmed NOT yet created (correct - ADA creates at build); bw create --parent + -d confirmed live.
- Author-field regression: no author-like field in source POLYBIUS; modules carry none by design; P9 guards. DISCHARGED.

## Adjudication of residual-3 (Check-D scoping: POLYBIUS-only this arc vs forward-compatible both-files loop now)

RECOMMEND: Option 1 - scope recompose + checks to DEST_POLYBIUS ONLY this arc - WITH the F-B wording fix carried regardless.

Rationale:
- The acceptance bar is LOSSLESS-ON-CANON. Option 1 meets it with the SMALLEST new code path on the highest-risk surface (install.sh, today pure sed+cp, no body-injection). r1 residual is implementation-correctness of a wholly new mechanism; minimizing this arc blast radius is the audit-favorable, conservative call. CONCUR with DAEDALUS instinct.
- Option 2 (the both-files loop now, for a zero-install.sh-edit PLINY cut) is a real generality benefit (r5), but it requires re-architecting Check D from global-glob to per-file-markers NOW - MORE new logic on the risky surface this arc, to serve a future arc. The next arc is itself gauntlet-gated and WILL touch install.sh anyway (author PLINY markers + verify recompose vs PLINY); adding DEST_PLINY to the loop there is a trivial in-scope edit at that time. Defer the loop.
- REQUIRED regardless of option: Check D must key on markers-in-THIS-file, not the global glob (F-B). section 7.4 already names the correct form; the fix is making the section 6.5 Check D bullet match section 7.4 so ADA cannot implement the global form. This is the F-B SHOULD-FIX.

## Quadrant note (ARGUS self-classification per section 6.6)

The recompose-completeness property is easy-hard at full generality (byte-exact equivalence across all future source edits is intractable). ARGUS surfaced the concrete edge cases it DID consider (6, enumerated in the r1 edge-case audit) and stopped; it does NOT claim exhaustion of the failure-mode space. Downstream VERA runs the P10/P10-negative throwaway-target probes as the bounded (INCOMPLETE-shape) verification of the happy path + FAIL-LOUD path. No risk in this audit returned UNVERIFIABLE - all edge cases were checkable within probe budget against the live source.

## summary

rev2 is a clean, targeted resolution of rev1 r1-r4 + residual-2 - it adds the build-spec precision the cut needed without re-cutting. The load-bearing fix (r1, the recompose mechanism for the hardest-audited subproject tier) is now a buildable, FAIL-LOUD, idempotent, machine-marker-keyed shape: I walked six edge cases (literal-sentinel bodies, regex anchoring, sed interaction, FAIL-LOUD coverage, idempotency, P10 canon-equivalence) and found no vector that drops a body THIS arc - the source carries zero MODULE-INLINE literals, the full-line regex anchoring resists prose-mention false-triggers, and the four err-backed checks abort rather than ship a partial file. The two r2 SPLIT calls DAEDALUS flagged as least-certain are both correct against the live source. r3 arithmetic is self-consistent. residual-2 archive-first/content-check/then-delete child-ticket gate is sound. Two NEW findings are precision-not-redesign: F-B SHOULD-FIX (the section 6.5 Check-D bullet reads as a global-glob check that would false-positive on marker-less PLINY; section 7.4 has the correct per-file form - make 6.5 match it), and F-A CONSIDER (the unstated module-body-must-not-contain-a-close-marker constraint that the next arc data-driven recompose reuse should guard). On residual-3 I recommend scoping recompose to DEST_POLYBIUS only this arc (minimal blast radius on the riskiest surface) and deferring the both-files loop to the gauntlet-gated PLINY cut. Posture: minor revisions - PASS-WITH-RISKS. rev2 is ADA-buildable; ADA should land F-B one-line Check-D wording fix and may fold F-A as a 5th FAIL-LOUD check or module-author note. Neither new finding drops canon this arc.

gap_or_blocker: none (status completed).
