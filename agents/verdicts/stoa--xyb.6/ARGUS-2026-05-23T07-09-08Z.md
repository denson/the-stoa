# CAPTAIN_ARGUS verdict - Arc 45 (Arc 2 debloat) - MAJOR_POLYBIUS.md cut design-rev1

status: completed
ticket: stoa--xyb.6
verdict: PASS-WITH-RISKS
design_artifact_audited: .claude/worktrees/arc-45-build/agents/design/arc-45/design-rev1.md (committed ea56fd7)

## Headline

LOSSLESS-ON-CANON holds for the CUT. I independently walked all 19 sections (87 headers) of the 1299-line source against the relocation ledger: EVERY chunk maps to a named home (module path / bw Anchor / live pointer / KEEP-TIGHTEN inline). NO dropped empirical anchor, discipline rule, cross-ref, or worked example was found in the cut itself. The 9 C-1 already-in-bw deletions are sound (6 spot-checked live; all recover the inline story via bw show). The subproject-tier probe is sound and confirmed against the real ariadne deploy plus current web evidence. The residual risks are all in BUILD-SPEC precision and the NEW recompose mechanism, not in the cut losslessness math - hence PASS-WITH-RISKS, not REWORK.

## audit_block.risks

### r1 [SHOULD-FIX] - install.sh subproject recompose stub-marker format under-specified
- description: section 6.2 says recompose cat-concatenates the 5 module bodies into the role file at their stub markers but never specifies the marker FORMAT. The section 2.7 stub is a human-prose heading (## 5. Onboarding flow + 3 pointer lines). If recompose greps on heading text and that text drifts (a future tighten edit), a body silently fails to inline at subproject tier - the exact LOST-CANON-at-the-hardest-tier failure DAEDALUS names as weak point 1.
- evidence: design section 6.2 (lines 263-267) + section 2.7 (lines 74-79); install.sh deploy is today a pure sed substitution (install.sh:758-762) with NO body-injection code path - recompose is wholly new.
- load_bearing: true
- quadrant_classification: easy-hard - the failure (a dropped body) is easy to detect on recompose output (grep each module first heading); full verification across all future source edits that might drift a marker is intractable. Mitigation is a MACHINE marker, not a prose heading. ARGUS does not propose the fix; the gap is the design leaves marker-format to ADA discretion and the loss-vector lives precisely in that discretion.

### r2 [SHOULD-FIX] - SPLIT class under-specified per-line; live-vs-provenance split is ADA judgment
- description: The 4 SPLIT rows (16.7/17.6/18.6/19.7) instruct live refs kept / provenance to Anchor but do not enumerate WHICH refs are live vs provenance. Each mixes bw-id provenance, source-doc citations, AND load-bearing live in-file + sibling-file cross-refs (e.g. 16.7 carries live refs to 4.5/6/7/14/15 + op-disc 21/30 + handoff-author skill). A mis-split drops a live cross-ref = lost canon.
- evidence: design section 3.3 SPLIT rows (lines 118/121/123/125); source 16.7 (1034-1050) confirmed to mix all three ref types.
- load_bearing: true
- quadrant_classification: easy-easy - once the design enumerates the live refs per subsection (or instructs ADA to preserve EVERY in-file/sibling ref and only Anchor-collapse the bw-id story lines), the downstream VERA probe is a trivial grep-diff. The risk is real only because the per-line split is currently implicit.

### r3 [SHOULD-FIX] - ledger arithmetic typo: section 3.7 claims 5 SPLIT / 16 PROVENANCE; actual 4 / 15
- description: section 3.3 contains exactly 4 SPLIT rows (16.7, 17.6, 18.6, 19.7). The section 3.7 summary says 5 SPLIT and 16 PROVENANCE chunks (9 C-1 + 2 C-2 + 5 SPLIT). Correct total is 9+2+4=15. In a losslessness-PROOF artifact, an off-by-one in the proof own count is a precision defect worth correcting - it does not drop canon (every actual subsection has a home) but erodes the ledger authority as the audit surface.
- evidence: design section 3.3 (4 SPLIT rows) vs section 3.7 (line 153).
- load_bearing: false
- quadrant_classification: easy-easy

### r4 [SHOULD-FIX] - section 4.5 F1 lean-2-sentence deviates from stoa--0hl mirror-verbatim instruction
- description: stoa--0hl Edit 1 instructs the 4.5 fold be the VERBATIM ~7-sentence paragraph already in the deployed user-tier copy (mirror it verbatim ... text is identical-by-construction). The design section 3.2 F1 instead specifies a LEAN 2-sentence paragraph. This is a deliberate defensible scope choice (the arc whole point is to write the fold lean, section 2.4) and does NOT drop canon (rule + say-vs-paste contrast + 2026-05-21 cite all preserved). But it is a NAMED divergence from the source ticket explicit instruction - ADA should land it knowingly, not silently, and the divergence should be visible in the build commit so a future auditor reconciling 0hl against the built 4.5 is not confused by the length mismatch.
- evidence: stoa--0hl Edit 1 (mirror it verbatim) + deployed ~/.claude/MAJOR_POLYBIUS.md:106 vs design section 3.2 F1 (LEAN 2-sentence).
- load_bearing: false

### r5 [CONSIDER] - recompose generality vs the epic roadmap (PLINY is next to be cut)
- description: The recompose code path is scoped (correctly for THIS arc) to the 5 POLYBIUS modules. But install.sh deploys BOTH MAJORs at subproject tier from one code path, and the epic explicitly plans to cut MAJOR_PLINY.md next. If recompose is written POLYBIUS-specific (hardcoded 5 module names), the next arc re-opens install.sh to generalize it - re-touching the highest-risk deploy code twice. A recompose data-driven from the relocation-index/stub markers (not a hardcoded module list) absorbs the PLINY cut without an install.sh re-edit, mirroring Arc-1 glob-deploy no-install.sh-edit property.
- evidence: design section 6.2 (POLYBIUS-only recompose) + stoa--xyb.6 (After POLYBIUS proves the method, repeat for MAJOR_PLINY.md) + install.sh:730-762 (single MAJOR deploy path for both).
- load_bearing: false
- quadrant_classification: hard-easy

### r6 [CONSIDER] - section 13 line-target pressure (DAEDALUS weak point 3) real but correctly scoped
- description: section 13 + 16-19 stay inline as KEEP-TIGHTEN; these are dense (16-19 ~70-110 lines each). If tighten is timid the core lands at 380-420 not 300-350. I concur with DAEDALUS these rules ARE always-on operational core (lifecycle, base-vs-custom, two-team routing, escalation triggers) and relocating off-disk degrades availability-losslessness. A 360-400 landing is a partial-not-failure. No section is wrongly KEEP that should be relocate, and none wrongly relocate that should be KEEP - I checked section 13.1 escalation triggers (826-836): read every autonomous engagement, MUST stay inline. The KEEP/relocate boundary is correctly drawn.
- evidence: source 13.1 (826-836) escalation triggers; design section 3.1 note + section 7 weak point 3.
- load_bearing: false
- quadrant_classification: easy-hard (whether prose tightens enough to hit 300-350 without dropping a rule is bounded only by ADA discipline; P1 >400 falsifier is the right guard).

## non_findings (considered and DISCHARGED)
- C-1 fabrication risk (highest drop-risk per brief): DISCHARGED for 6 sampled cites. stoa--32b.3, ads, k36, 86k, ezj, 3cs all recover the deleted inline story via bw show. ezj load-bearing 4-option-to-5th-option anchor lives in a COMMENT (body canonical-phrasing code block is empty) but bw show surfaces comments, so it IS recoverable; and section 4.3.1 3-step probe stays inline (KEEP) anyway. The per-deletion C-1 content-check GATE (section 3.3) is the right structural guard for the 3 unsampled cites (nax, ariadne--8fd, 32b/p5g/dxw).
- Subproject probe soundness (DAEDALUS top weak point): DISCHARGED. Confirmed against the REAL ariadne deploy - self-contained 64KB role file, NO modules dir at parent OR subproject tier (install.sh:624 DEST_MODULES_DIR empty). Web-check confirms #56686 current (~2 wks) + NEW corroborators #29423 (subagents skip project config) + #29610 (bypassPermissions no help for bg subagents). Strategy 2b (recompose-inline) is correct; 2a (extend glob) is strictly worse as DAEDALUS argues.
- Ledger completeness: DISCHARGED. All 87 source headers map to a ledger row or KEEP-TIGHTEN. The section 3 What-you-DO-do block, section 16.8 empirical registry caveat, section 15 cross-ref line - all have homes.
- Section-number preservation cross-ref coverage: DISCHARGED. Found NO cross-ref the preservation misses.
- DUPLICATE 7.3: DISCHARGED. op-disc section 12 confirmed canonical (self-declares do-not-duplicate, op-disc:729). POLYBIUS-specific prime+TIRO framing correctly stays.
- Author-field regression: DISCHARGED. No author-like field in source or planned modules; P9 guards.
- CONDITIONAL line ranges (10/11/12/14): DISCHARGED - all accurate. (section 5 159-379 excl 5.6 is harmless shorthand: 5.6 is a NEW fold, not present in source to exclude.)

## Adjudication of DAEDALUS 4 residual questions (section 8)

1. section 13 KEEP vs RELOCATE: KEEP, as chosen. CONCUR. section 13.1 universal escalation triggers (826-836) are read every autonomous engagement = always-on operational core; relocating off-disk degrades availability-losslessness. Do NOT split 13.2/13.3/13.4 to a mode-transitions module - the mechanics are read together with the triggers at mode-declaration time, and a 4-subsection section is below the line-savings threshold that justifies a new module + routing row + recompose-stub surface. Tighten section 13 prose inline; keep it whole.

2. C-2 archive home (xyb.6 comment vs dedicated child ticket): USE A DEDICATED CHILD TICKET each (stoa--xyb.6.1, stoa--xyb.6.2), not bare comments on xyb.6. The two C-2 blocks (5.1.1.1 N=2 cross-project-leak; 5.1.3 cron-hygiene N=0) are the ONLY genuinely not-yet-in-bw provenance in the cut - their bw record IS the sole surviving copy. A titled child ticket is retrievable by a future auditor via bw show of a NAMED id (discoverable from the relocation-index Anchor); a comment buried in xyb.6 stream (which already carries this whole audit heartbeats) is materially less discoverable, and DAEDALUS own weak point 2 concedes this. Cost (2 bw create calls) is trivial against sole-surviving-copy stakes. modules/README.md section 5.2 sanctions bw comment for SHORT prose, but sanctioned is the floor, not the right call when the prose is the only copy. P6 archive-before-delete ordering still applies, against the child-ticket bodies.

3. Section-number PRESERVE-via-stubs vs RENUMBER: PRESERVE, as chosen. CONCUR. Checked for a cross-ref the preservation might miss - found none. ~10 stub lines buy zero cross-ref churn; strongly net-positive. Renumber would re-open every sibling file. Keep preservation.

4. install.sh recompose vs 2-body fork at subproject: RECOMPOSE, as chosen - but with the r1 hardening as a build requirement. CONCUR with recompose over fork: the fork re-introduces the exact duplication the epic exists to kill (two role-file bodies). Recompose keeps single-source-of-truth. The LOST-CANON-at-subproject risk is REAL (r1) but mitigable by (a) a machine-readable stub marker rather than prose-heading match and (b) VERA throwaway-subproject recompose probe (post-recompose grep that EVERY module content is present). The risk lives in the marker-format gap, not the recompose strategy. Fix the marker spec; keep recompose.

## summary

The design is a structurally sound, losslessness-preserving cut. Shape: relocate 5 CONDITIONAL whole-sections to disk modules (with subproject-tier recompose-inline at deploy), collapse 9 C-1 + 2 C-2 + 4 SPLIT provenance blocks to bw Anchors, delete 1 DUPLICATE cookbook to an op-disc section 12 pointer, fold stoa--0hl in lean as a separable slice, keep ~13 sections inline (tightened) with section numbers PRESERVED via stubs for zero cross-ref churn. I independently walked every source section and found NO dropped canon in the cut - the losslessness bar is met by the design structure, and the 6 spot-checked C-1 cites all recover their deleted prose via bw show. The most important load-bearing risk is r1: the install.sh subproject RECOMPOSE mechanism (the one genuinely new code path) leaves its stub-marker FORMAT unspecified, and a prose-heading-match marker is exactly where a module body could silently fail to inline at subproject tier - the hardest-audited tier. That risk is mitigable at the build level (machine marker + a VERA recompose-completeness probe), not a redesign. Overall posture: minor revisions - PASS-WITH-RISKS. The cut is buildable; ADA should land r1 (marker spec) and r2 (per-line SPLIT enumeration) as build-precision requirements, take the dedicated-child-ticket call on the 2 C-2 blocks (residual 2), and land the section 4.5 F1 lean-vs-verbatim divergence (r4) knowingly. None of the four DAEDALUS residuals require a redesign.

gap_or_blocker: none (status completed).
