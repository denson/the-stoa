status: completed
ticket: stoa--xyb.6
verdict: pass-with-nits
diff_reviewed: main..arc-45/build (commits ee654d7 + 2074525), files: substrate/MAJOR_POLYBIUS.md, substrate/install.sh, substrate/modules/{onboarding,sub-project-spawning,pair-programmer-authoring,pair-programming-prototyping,substrate-update-check}.md (agents/ design+verdict artifacts excluded per brief)
design_artifact_compared_against: agents/design/arc-45/design-rev2.md (4fa6c57)

=== LOAD-BEARING ANSWER TO Q2 (line-count judgment): 590 is the HONEST LOSSLESS FLOOR for the 300-350 target band, with ~30-50 lines of residual prose headroom that would land ~540-560, NOT 300-350. The target is structurally unreachable without relocating §16-19 always-on rule-prose off-disk (design forbids; ARGUS weak-point#2 flags it as its own future arc). NOT timid in aggregate — but tightening depth is UNEVEN; 3 named shallow sections (c4) could shave ~30 lines. This is recommended-revision at most, NOT blocking. Ship at 590. ===

concerns:
- id: c1
  category: coverage
  description: VERA's P10(a) probe asserts "expect 0 markers" in the recomposed subproject file, but design §6.5 algorithm steps 2-3 MANDATE keeping the paired open/close markers as idempotency anchors — the assertion contradicts the algorithm it verifies. The BUILD correctly followed §6.5 (keeps markers); the contradiction is in the spec/probe text, not the artifact. My independent recompose probe found 11 marker lines in the recomposed file (5 kept open + 5 kept close + 1 §3.5 prose mention in backticks at L96 — the latter correctly NOT matched by the ^...$ full-line regex). Surfacing as coverage note so the spec's P10(a) wording is corrected before it misleads a future verifier into flagging a correct build. ZENO independently caught the same contradiction.
  evidence: design-rev2.md P10 step (a) "grep -cE '<!-- /?MODULE-INLINE:' ... expect 0" vs §6.5 algorithm steps 2-3 (emit OPEN marker / emit CLOSE marker "kept"); empirical: recomposed throwaway file had 11 MODULE-INLINE lines, all kept-anchor pairs with bodies between them (verified no empty/unexpanded pairs)
  severity: minor
  quadrant_classification: easy-easy
- id: c2
  category: craft
  description: Uneven tightening depth across KEEP-TIGHTEN sections. §13 (80->48, 40%), §7 (111->69, 38%), §16 (175->60, 66%) got genuine deep tightening. But §4.3.1 (19->17, 11%), §9 (26->22, 15%), §17 (77->57, 26%) are comparatively shallow. §4.3.1 specifically still carries a verbose Anchor line (L134) that retells the entire ezj 4-option-to-5th-option story inline EVEN THOUGH the Anchor cites stoa--ezj for recovery (and I confirmed ezj carries that story richly). That inline retelling is exactly the provenance-prose the debloat thesis targets. NOT blocking (no rule dropped; lossless holds) but it is the clearest single example of prose that could compress to rule+Anchor without relocating always-on core.
  evidence: substrate/MAJOR_POLYBIUS.md:134 (§4.3.1 Anchor retells ezj inline); per-section depth measured: §4.3.1 source 19 -> cut 17; cf §16 source 175 -> cut 60
  severity: recommended-revision
  quadrant_classification: easy-easy
- id: c3
  category: consistency
  description: P10 example invocation in design-rev2.md §4 uses non-matching CLI flag syntax (--target=subproject --parent="$TMP" --subproject=myproj --yes) vs install.sh's actual flags (--target subproject --parent-dir <path> --subproject <slug>, no --yes). ADA used the REAL flags in the build (P10 passed), so this is a design-doc nit not a build defect — but a future verifier copy-pasting the design's P10 literal would hit an arg error. Surfacing for design-doc correction.
  evidence: design-rev2.md:351 (P10 step 0) vs install.sh:28/492/497 (real flag forms)
  severity: minor
  quadrant_classification: easy-easy

follow_ups:
- §16-19 always-on rule-prose relocation to its own module(s) to reach 300-350 — out of scope here (would relocate always-on core off-disk; needs own design). Already named by ADA + ARGUS weak-point#2 as a future arc. Concur it is correctly deferred.
- Correct design-rev2.md P10(a) "expect 0 markers" wording + P10 CLI flag syntax (c1 + c3) — doc-only, no artifact change; fold into the next arc's design or a doc-fix housekeeping commit.
- Optional shallow-section tightening pass (c2) if a future arc wants to claw ~30 lines toward the band without relocating core — would land ~540-560, still not 300-350.

verifier_coverage_assessment: VERA exercised the load-bearing cases. I independently re-ran the LOST-CANON-at-subproject probe (the design's named highest-risk surface): deployed a throwaway subproject, confirmed exit 0 + recompose to 1098 lines FULL band + all 5 module bodies inlined + no empty/unexpanded marker pairs + byte-identical idempotent re-run. I independently induced 4 of 5 FAIL-LOUD checks (A marker-no-module / B module-no-marker / D zero-markers / E close-marker-in-body) — ALL fired exit 2 with correct diagnostic AND removed the partial file (never ships LOST CANON). Residual-3 confirmed: PLINY deployed untouched at 801 lines, zero markers, NOT recomposed, does NOT false-trip Check D (recompose scoped to $DEST_POLYBIUS only). C-1 cites independently spot-checked (32b.3 carries the two verbatim PRINCIPAL quotes; ezj carries the 4-to-5th anchor; nax/86k/ads/k36 are substantial bodies not thin stubs). C-2 child tickets xyb.6.1/.6.2 independently confirmed to carry the verbose provenance prose verbatim (archive-first honored). SPLIT folds spot-checked: §16.7 keeps all 9 LIVE pointers and explicitly names the folded provenance ("Provenance folded into §16.6's Anchor: stoa--32b.1/.2 ... sources HANDOFF..., retro..."). No coverage gap found; VERA's LOSSLESS-ON-CANON-holding verdict is corroborated. The one coverage nit (c1) is a spec-probe-text contradiction VERA flagged honestly as drift, not a missed case.

summary: Clean debloat cut of MAJOR_POLYBIUS.md (1299->590 slim core, ~55%) with 5 faithful CONDITIONAL module relocations, lossless PROVENANCE compression (9 C-1 to Anchors + 2 C-2 to verbatim child tickets + 4 SPLIT per-line folds), 1 DUPLICATE to op-disc §12 pointer, and a new gauntlet-gated install.sh subproject-recompose (awk state-machine + 5 FAIL-LOUD checks, scoped to $DEST_POLYBIUS per residual-3). LOSSLESS-ON-CANON — the hard bar — is MET at all tiers, independently confirmed by my own empirical recompose + FAIL-LOUD-induction probes and C-1/C-2/SPLIT spot-checks. Scope is clean (exactly the 7 named files, no drive-by changes); Author = PRINCIPAL on both commits with the CAPTAIN_ADA seat-id Co-Authored-By trailer; no author-like field touched; the 0hl fold is genuinely separable (18-line slice) with a commit message that honestly names the lean-vs-verbatim deviation by-design. The most important concern is the line-count: 590 vs the 300-350 target. My independent read is that 590 IS the honest lossless floor for that band — the line mass is dominated by structure (70 headings + 91 structural lines + 89 mandated table rows + 219 blanks) and dense always-on KEEP-TIGHTEN rule-prose (§13/§16-19), and tightening WAS genuinely applied (§16 cut 66%, §13 40%, §7 38%). There is ~30-50 lines of residual prose headroom in 3 shallower sections (§4.3.1 the clearest, c2), which would land ~540-560 — still nowhere near 300-350. The 300-350 target is unreachable without relocating §16-19 always-on rule-prose off-disk, which the design forbids and ARGUS flagged as its own future arc. Overall posture: PASS-WITH-NITS — ship at 590; the line-count "miss" is an honest structural floor, not timid tightening, and the residual headroom is a recommended-revision follow-up, not a blocker.
