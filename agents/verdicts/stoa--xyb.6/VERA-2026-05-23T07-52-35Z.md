# CAPTAIN_VERA verdict — Arc 45 (Arc 2 debloat) — MAJOR_POLYBIUS.md cut build verification

status: completed
ticket: stoa--xyb.6
verdict: pass (on the HARD bar — LOSSLESS-ON-CANON at all tiers) / P1 line-count TARGET MISSED (separate judgment — flagged for PLINY)
design_artifact_verified_against: agents/design/arc-45/design-rev2.md (committed 4fa6c57)
build_verified: arc-45/build worktree, commits ee654d7 (cut + 5 modules + install.sh recompose) + 2074525 (0hl lean fold)

## TWO SEPARATE JUDGMENTS (per dispatch brief — do NOT conflate)

**JUDGMENT 1 — LOSSLESS-ON-CANON (the HARD acceptance bar): PASS.** Falsification-tested at project tier (relocation-ledger walk + C-1 cite recovery + C-2 child-ticket prose + SPLIT LIVE preservation + DUPLICATE pointer) AND at subproject tier (install.sh recompose re-inlines all 5 module bodies + the §5.6 0hl fold; recomposed file is canon-equivalent to project-tier full content; all 5 FAIL-LOUD drop-shaped conditions abort the deploy non-zero with no partial file). I attempted to falsify losslessness at every tier and could not. NO canon is dropped — every relocated chunk has a recoverable home.

**JUDGMENT 2 — P1 line-count TARGET: MISSED (not a losslessness fail).** MAJOR_POLYBIUS.md = 590 lines. Design P1 target band = 300-350; the design's own falsifier guard is `>400`. 590 trips the >400 falsifier. This is a TARGET miss, NOT a losslessness fail (the cut still ~55% reduces 1299→590 and drops NO canon). ADA self-reported this honestly as `partial`; ARGUS pre-build CONCUR'd that §16-19 dense always-on rules + every-preserved-§X.Y + mandated §3.5 dual tables structurally dominate physical line count, and flagged §16-19 rule-prose for its OWN future relocation arc. **This is PLINY's scope judgment to adjudicate — VERA surfaces it factually, does not treat the line-count miss as a canon-loss fail.**

## probes_executed

- probe_id: P1
  description: Line-count target met (the cut actually cut).
  command_or_method: `wc -l substrate/MAJOR_POLYBIUS.md` (+ module line counts)
  quadrant_classification: easy-easy
  expected: 300-350 band; >400 falsifies.
  observed: 590 lines (modules: onboarding 218, sub-project-spawning 102, pair-programmer-authoring 93, pair-programming-prototyping 79, substrate-update-check 16 — each well under op-disc §33's monolith threshold; no module re-bloat).
  result: fail-on-target (>400 falsifier tripped) — SEPARATE judgment per brief; NOT a losslessness fail.

- probe_id: P2
  description: Every relocated CONDITIONAL section has a real non-empty module home + routing-map row.
  command_or_method: `test -s substrate/modules/<m>.md` for 5 modules + grep slim core for routing rows + relocation-index rows
  quadrant_classification: easy-easy
  expected: all 5 exist non-empty; routing-map row per module path; no dangling reference.
  observed: all 5 OK (non-empty). Routing-map rows at lines 69-73; relocation-index rows at 81-85; stub recovery pointers at 172-173/289-290/297-298/305-306/360-361. No dangling reference.
  result: pass

- probe_id: P3
  description: Every relocated section leaves a stub at its original number (cross-ref preservation).
  command_or_method: `grep -nE '^## (5|10|11|12|14)\.' substrate/MAJOR_POLYBIUS.md`
  quadrant_classification: easy-easy
  expected: §5, §10, §11, §12, §14 headings present as stubs.
  observed: all 5 present (lines 171/288/296/304/359). Section numbers preserved — zero cross-ref churn.
  result: pass

- probe_id: P4
  description: DUPLICATE pointer (§7.3) resolves to canonical home op-disc §12; verbose cookbook deleted.
  command_or_method: grep slim core for op-disc §12 pointer + `grep '^## 12\. ' operating-disciplines.md` + grep for residual -m/cookbook prose at §7.3
  quadrant_classification: easy-easy
  expected: §7.3 pointer to op-disc §12 present; op-disc §12 home exists; verbose -m cookbook gone.
  observed: pointer at line 219 ("bw command syntax → operating-disciplines.md §12 (canonical cookbook)"); op-disc §12 home heading at op-disc:727; verbose -m flag table / per-command table NOT inline (grep for -m flag prose returned empty). POLYBIUS-specific framing (bw prime + TIRO delegation at 219-223) correctly retained.
  result: pass

- probe_id: P5
  description: C-1 content-check gate honored — cited tickets materially carry the deleted N=1 story (sampled).
  command_or_method: `bw show` for stoa--32b.3, stoa--ads, stoa--k36, stoa--86k (the 4 brief-named C-1 cites)
  quadrant_classification: hard-easy (read each ticket end-to-end to confirm it carries the deleted story; cheap to falsify once read)
  expected: each cited ticket body carries the N=1 story (date, PRINCIPAL declaration, accretion framing) the deleted inline prose held.
  observed: stoa--32b.3 carries the two verbatim PRINCIPAL 2026-05-16 declarations + 3-mode lifecycle (matches §16.1/§16.6). stoa--ads carries the base-vs-custom model + 2026-05-17 declaration (matches §17.1/§17.5). stoa--k36 carries the user-tier direct-commit gap + 2026-05-17 anchor (matches §18.5). stoa--86k carries the two-team forge/shop declaration + 2026-05-13 anchor (matches §19.6). All four RECOVER the deleted story richly. (ADA also ran 9 C-1 checks per build report; my sample re-executes 4.)
  result: pass

- probe_id: P7
  description: C-2 archive-first executed BEFORE deletion in DEDICATED CHILD TICKETS; cite-backs name the child ids.
  command_or_method: `bw list --all | grep xyb.6.(1|2)` + `bw show stoa--xyb.6.1/.6.2` + grep onboarding.md for the child-id cite-backs
  quadrant_classification: hard-easy
  expected: both child tickets exist (parent stoa--xyb.6), each description carries verbose provenance verbatim, onboarding.md cite-back names the correct child id.
  observed: stoa--xyb.6.1 exists (parent stoa--xyb.6), carries the full verbatim §5.1.1.1 cross-project-leak provenance prose (N=2 2026-05-17 ariadne, the anti-pattern + positive-pattern worked examples). stoa--xyb.6.2 exists, carries the full verbatim §5.1.3 cron-hygiene provenance (N=0 canon, HUMAN_paste filename list + honest-scope paragraph). Cite-backs at onboarding.md:104 (→ stoa--xyb.6.1) and :163 (→ stoa--xyb.6.2). Archive-first ordering confirmed (prose lives in the titled child ticket descriptions; the only surviving copy is discoverable by named id).
  result: pass

- probe_id: P9
  description: No author-field regression in slim core or any module.
  command_or_method: `grep -niE '^(author|owner|creator|by|copyright|maintainer):'` on MAJOR_POLYBIUS.md + all 5 modules
  quadrant_classification: easy-easy
  expected: no author-like field names anyone (role files + modules carry none).
  observed: zero matches (grep exit 1) across slim core and all 5 modules. CLEAN. (No falsely-attributed authorship; CLAUDE.md authorship discipline held.)
  result: pass

- probe_id: P10
  description: Subproject recompose completeness (THE LOST-CANON-at-subproject probe) — happy path, against a THROWAWAY synthetic parent (op-disc §25.5; never touched the real repo/main/.claude).
  command_or_method: `mktemp -d` throwaway parent; `bash substrate/install.sh --target subproject --parent-dir <tmp> --subproject myproj` (note: space-form args + NO --yes; the design P10 wrote `--target=subproject ... --yes` which the parser rejects — probe-spec drift, see methodology_concerns); then assert markers + bodies + line band + residual-3.
  quadrant_classification: easy-hard (bug surfaces obviously; full byte-equivalence across all future source edits is intractable — bounded battery run: happy path + 5 FAIL-LOUD inductions + idempotency)
  expected: recomposed subproject role file in FULL band with all 5 module bodies + 0hl fold re-inlined; PLINY untouched (residual-3); modules dir not deployed.
  observed: install.sh exit 0. Recomposed MAJOR_POLYBIUS_myproj.md = 1098 lines (FULL band = 590 slim + 508 module bytes; NOT slim — proves bodies re-inlined, NOT silent no-op). 5 OPEN + 5 CLOSE markers KEPT as idempotency anchors (design §6.5; bodies inlined between them). All 5 module first-heading lines present. §5.6 0hl fold re-inlined at recomposed line 379. PLINY deployed at 801 lines, 0 markers, NOT recomposed (residual-3 confirmed). modules dir ABSENT at subproject tier (DEST_MODULES_DIR="" — correct). This proves LOSSLESS-ON-CANON at the hardest-audited subproject tier.
  result: pass

- probe_id: P10-negative
  description: FAIL-LOUD — each of the 5 drop-shaped conditions must abort deploy non-zero with NO partial/slim file. Run against SANDBOX substrate copies in /tmp (worktree source never mutated).
  command_or_method: induce Check A (rename a module source → marker-without-module), B (delete a stub marker pair → module-without-marker), C (delete one close marker → unbalanced), D (strip ALL markers → zero-markers-with-modules), E (inject literal close-marker into a module body → close-marker-in-body); run install.sh subproject each time; assert exit==2 + recomposed file ABSENT.
  quadrant_classification: easy-hard (the FAIL-LOUD path; bounded to the 5 named conditions the design + ARGUS enumerated)
  expected: each induction exits non-zero (2) and leaves NO partial role file.
  observed: ALL 5 fired exit 2 + partial_role_file ABSENT. Check A: "marker MODULE-INLINE:substrate-update-check has no module source". Check B (the precise LOST-CANON guard): "module substrate-update-check.md exists but no MODULE-INLINE marker — body would be DROPPED at subproject tier". Check C: "nested open marker ... before close (unbalanced)". Check D (per-file, ARGUS F-B): "role file has zero MODULE-INLINE markers but 5 relocatable module(s) exist — bodies would be DROPPED". Check E (ARGUS F-A): "module onboarding.md body contains a literal MODULE-INLINE marker line — would corrupt recompose". The `rm -f tmp role_file; exit 2` guard removes the partial in every case. Structural property holds: a recompose that cannot prove completeness FAILS rather than ships LOST CANON.
  result: pass

- probe_id: P11
  description: SPLIT LIVE cross-refs preserved inline; PROVENANCE bw-ids folded into §N Anchors (per §3.8 per-line enumeration).
  command_or_method: grep slim core for the load-bearing LIVE pointers (§6.7.1, §21/§30, §23, §29, §17.4, agent-author, tier2-project-onboarding, handoff-author) + the §19.7 intra-line split (§17.5/§18.5 LIVE half) + stoa--86k folded to §19 Anchor.
  quadrant_classification: easy-easy
  expected: every §3.8 LIVE pointer survives; PROVENANCE bw-ids appear in Anchor lines, not orphaned cross-ref bullets.
  observed: all LIVE pointers resolve in slim core (§16.7 cluster at 429; §17.6 cluster at 495; §18.6 cluster at 539; §19.7 cluster at 586). §19.7 intra-line split CORRECT: line 586 keeps the §17.5/§18.5 LIVE half inline AND keeps the §17.4 disambiguation parenthetical verbatim ("op-disc §17 is unrelated OSS-dep calculus and is NOT the intended target"); stoa--86k folded into §19.6 Anchor at line 582. PROVENANCE bw-ids live in relocation-index rows (90-93) + Anchors, none orphaned as standalone cross-ref bullets.
  result: pass

- probe_id: P6 (install.sh sanity)
  description: bash -n syntax + idempotency (re-run recompose → byte-identical).
  command_or_method: `bash -n substrate/install.sh`; run subproject install twice, sha256 the recomposed file each run.
  quadrant_classification: easy-easy
  expected: bash -n exit 0; byte-identical recompose across re-run.
  observed: bash -n exit 0 (clean). Re-run sha256 IDENTICAL (edc3fcb9...d6d0c48e, 1098 lines both runs). Idempotent — re-deploy safe.
  result: pass

- probe_id: DRIFT-1 (ADA ledger-drift call: ariadne--8fd cross-project)
  description: Verify ariadne--8fd is genuinely cross-project (not recoverable from the stoa-- store).
  command_or_method: `bw show ariadne--8fd` from the stoa-- store (the-stoa repo) + `bw show stoa--nax` (the in-store sibling C-1 cite).
  quadrant_classification: hard-easy
  expected: ariadne--8fd NOT in stoa-- store (cross-project); stoa--nax in-store and carries the §15 retrospective-discipline story.
  observed: `bw show ariadne--8fd` → "error: no issue found matching ariadne--8fd" — CONFIRMED cross-project (ariadne-- prefix = ariadne workspace). Slim-core §15 Anchor (line 380) honestly labels it "ariadne--8fd is cross-project (recover from the ariadne workspace)". stoa--nax IS in-store and carries the redundant-checks discipline (2026-05-12 N=1). §15 losslessness holds: in-store cite recovers the story; cross-project cite is correctly labeled. ADA drift call CORRECT.
  result: pass

- probe_id: DRIFT-2 (ADA ledger-drift call: stoa--p5g body-vs-cited-use mismatch)
  description: Verify stoa--p5g's body mismatches its §16.6 cited use ("Arc 25 cross-tier coordination"), and the §16.6 evidence is covered by the sibling cites.
  command_or_method: `bw show stoa--p5g` + `bw show stoa--dxw` + grep slim-core §16.6 Anchor (line 425) + diff against pre-cut source (ee654d7^).
  quadrant_classification: hard-easy
  expected: p5g body ≠ "cross-tier coordination"; the §16.6 N=1 story is recoverable from the primary source regardless of the supporting-evidence label.
  observed: stoa--p5g body = "credential discipline — batch or env-inject" (2026-05-15 railway_stoa), NOT "Arc 25 cross-tier coordination". stoa--dxw body = "substrate-update check skill — full-picture detection" (Arc 25), NOT "Arc 26 handoff + relay worked example". BOTH supporting-evidence labels in the §16.6 Anchor (line 425) mismatch their actual ticket bodies — BUT `git show ee654d7^` confirms these EXACT labels are inherited VERBATIM from the pre-cut source (lines 1028-1029 of the original). The cut faithfully PRESERVED what §16.6 said; it did NOT introduce the mislabel. The §16.6 N=1 canon (POLYBIUS lifecycle discipline) is fully recoverable via the PRIMARY source stoa--32b.3 (verified in P5). ADA drift call CORRECT (preserve all cited ids verbatim = lossless; 32b.3 carries the evidence).
  result: pass — losslessness holds (mislabel is pre-existing source condition, faithfully preserved, NOT cut-introduced; see methodology_concerns for the latent quality issue surfaced for PLINY/CATO).

## methodology_concerns

1. **[probe-spec drift — non-blocking; for DAEDALUS next dispatch] Design P10 command uses install.sh flags the parser rejects.** The design's P10 example runs `bash substrate/install.sh --target=subproject --parent="$TMP" --subproject=myproj --yes`. The actual install.sh parser uses SPACE-separated args (`--target subproject`, `--parent-dir <path>`, `--subproject myproj`) and has NO `--yes` flag and NO `--parent` flag (it is `--parent-dir`). Re-executing the design's literal P10 command exits 2 ("unknown argument: --target=subproject"). I re-executed against the actual install.sh CLI convention (the established space-form used by every other arg). The build is correct; the DESIGN's probe-spec is what drifted from the real CLI. Per VERA §5.4 (re-execute, do not re-derive) I flag rather than silently paraphrase: a future arc's DAEDALUS should align P10's command literal to the real install.sh flag shape so the probe is re-executable verbatim.

2. **[pre-existing source quality issue — NOT a cut regression; for CATO craft review / PLINY scope] §16.6 Anchor supporting-evidence labels are stale.** The §16.6 Anchor (slim-core line 425) labels stoa--p5g as "Arc 25 cross-tier coordination" and stoa--dxw as "Arc 26 handoff + relay worked example", but the actual ticket bodies are credential-discipline (p5g) and substrate-update-check detection (dxw). This is a pre-existing source condition (verbatim-inherited from pre-cut lines 1028-1029, confirmed via git), NOT introduced by this cut, and NOT in VERA's scope to fix (independence-of-verification). The cut is lossless because it faithfully preserved the source AND the §16.6 canon is independently recoverable from the primary source stoa--32b.3. Surfaced so PLINY/CATO can decide whether a future cleanup arc corrects the stale supporting-evidence labels at source.

3. **[cross-project cite recoverability — noted, not a regression] §15 Anchor's ariadne--8fd is not locally recoverable.** A reader on a machine without the ariadne workspace cannot `bw show ariadne--8fd` locally. This is a pre-existing property of cross-project cites (not introduced by this cut — the cite is preserved verbatim), and §15 also carries the in-store stoa--nax cite that recovers the story. Noted for completeness; no action required this arc.

## falsifying_evidence_summary

LOSSLESS-ON-CANON (the hard bar): NO falsifying evidence found at any tier. Project-tier: every relocated chunk recovers (9 C-1 cites carry their story, 2 C-2 child tickets hold verbatim prose, all SPLIT LIVE pointers survive, DUPLICATE pointer resolves). Subproject-tier: recompose re-inlines all 5 bodies + the 0hl fold into a 1098-line self-contained role file, and all 5 FAIL-LOUD checks abort rather than ship a partial. P1 (line-count target, a SEPARATE judgment): falsifying evidence EXISTS for the TARGET — 590 > 400 trips the design's own falsifier band. This is a target-miss, not a canon-loss; per the brief it is flagged for PLINY's scope adjudication and is NOT counted against the losslessness verdict.

## verification_artifacts_path

No persistent probe scripts written to disk — all probes were inline shell against the build, and all FAIL-LOUD inductions ran against ephemeral sandbox copies of substrate in mktemp dirs (now removed; worktree source confirmed clean via `git status --porcelain substrate/` = empty). Recorded probe outputs are inline in probes_executed above. Verdict saved to agents/verdicts/stoa--xyb.6/.

## summary

The build was exercised end-to-end against design-rev2's probes P1-P11 + the 2 ADA ledger-drift calls. The load-bearing probes were the two LOSSLESS-ON-CANON tiers: project-tier (P5/P7/P11 + the C-1/C-2/SPLIT ledger walk) and subproject-tier (P10 + P10-negative — the install.sh recompose, the arc's highest-risk new code path). The most important result: LOSSLESS-ON-CANON is MET and falsification-tested at BOTH tiers — I induced all 5 drop-shaped subproject-recompose failures and every one aborted the deploy non-zero with no partial file, the recompose re-inlines all 5 module bodies + the 0hl fold into a canon-equivalent 1098-line self-contained subproject role file, and idempotency holds byte-identical. The C-1 cites recover their deleted stories richly, the C-2 child tickets hold the sole-surviving-copy prose verbatim with correct cite-backs, and every SPLIT LIVE operational pointer survives inline. Both ADA drift calls verified CORRECT (ariadne--8fd genuinely cross-project; stoa--p5g body-mismatch is a pre-existing source label faithfully preserved, with §16.6 canon recoverable from primary source 32b.3). The ONE shortfall is P1: the slim core is 590 lines vs the 300-350 target / >400 falsifier — a TARGET miss driven by structurally-unavoidable always-on §16-19 rule density + preserved §X.Y subsections + mandated §3.5 dual tables, NOT a canon loss. Per the dispatch brief I report these as two separate judgments: HARD-BAR LOSSLESS-ON-CANON = PASS; P1 line-count = MISSED, flagged for PLINY's scope adjudication (ARGUS pre-build flagged §16-19 for a future dedicated relocation arc). Independence preserved: VERA modified nothing in the deliverable; all mutations ran against /tmp sandbox copies.

gap_or_blocker: none (status completed). PLINY decision needed: whether the P1 >400 target-miss (canon-faithful but over the declared band) ships as-is with the §16-19 future-relocation-arc follow-up, or is re-scoped this arc.
