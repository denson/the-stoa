status: completed
ticket: stoa--5ju
verdict: pass
design_artifact_verified_against: agents/design/stoa--5ju/design-rev2.md (§4 probes)
build_verified: arc-66/build @ 9e1768a (apply.sh glob-derive) on top of ff9f21e (check.sh/install.sh glob-derive)
probes_executed:
- probe_id: V2
  description: "THE PREVIOUSLY-FAILING PROBE — apply.sh --all-differing must now DELIVER a DRIFTED CHIRON/HAMILTON (project) and the SUFFIXED variant (subproject), not skip them 'not a substrate-derived path'."
  quadrant_classification: easy-easy
  command_or_method: |
    # PROJECT TIER
    bash substrate/install.sh --target project --project-dir /tmp/arc66-tw-project   # exit 0; 4 MAJORs deployed
    printf '\n# arc66-drift-probe-chiron\n'   >> /tmp/arc66-tw-project/.claude/MAJOR_CHIRON.md
    printf '\n# arc66-drift-probe-hamilton\n' >> /tmp/arc66-tw-project/.claude/MAJOR_HAMILTON.md
    bash substrate/skills/check-substrate-updates/check.sh --workspace /tmp/arc66-tw-project   # CHIRON+HAMILTON DRIFTED
    bash substrate/skills/check-substrate-updates/apply.sh --workspace /tmp/arc66-tw-project --all-differing --yes
    bash substrate/skills/check-substrate-updates/check.sh --workspace /tmp/arc66-tw-project   # re-check: 0 drifted
    # SUBPROJECT TIER
    bash substrate/install.sh --target project --project-dir /tmp/arc66-parent
    bash substrate/install.sh --target subproject --parent-dir /tmp/arc66-parent --subproject arc66tw
    printf '\n# arc66-sub-drift-chiron\n' >> /tmp/arc66-parent/arc66tw/.claude/MAJOR_CHIRON_arc66tw.md
    bash substrate/skills/check-substrate-updates/check.sh --workspace /tmp/arc66-parent/arc66tw  # MAJOR_CHIRON_arc66tw.md DRIFTED
    bash substrate/skills/check-substrate-updates/apply.sh --workspace /tmp/arc66-parent/arc66tw --all-differing --yes
  expected: "PROJECT: check reports CHIRON+HAMILTON DRIFTED ('  - '); apply.sh emits 'applied: .claude/MAJOR_CHIRON.md' AND '...HAMILTON.md' (exit 0); re-check 0 drifted. SUBPROJECT: SUFFIXED MAJOR_CHIRON_arc66tw.md DRIFTED then 'applied: .claude/MAJOR_CHIRON_arc66tw.md' (suffix-strip recovers source)."
  observed: |
    PROJECT check: "  - .claude/MAJOR_CHIRON.md (-2 lines)" + "  - .claude/MAJOR_HAMILTON.md (-2 lines)"
    PROJECT apply.sh: "applied: .claude/MAJOR_CHIRON.md" + "applied: .claude/MAJOR_HAMILTON.md", exit 0
    PROJECT re-check: "arc66-tw-project ... OBSOLETE (0 drifted, 0 missing, 4 obsolete...)" -> CHIRON/HAMILTON NO LONGER DRIFTED.
    SUBPROJECT check: "  - .claude/MAJOR_CHIRON_arc66tw.md (-98 lines)"
    SUBPROJECT apply.sh: "applied: .claude/MAJOR_CHIRON_arc66tw.md", re-check "0 drifted".
    (The 4 OBSOLETE = pre-existing unrelated skill dirs check-bw-release/check-substrate-updates/inspect-script-output/validate-spec; unchanged from prior run.)
  result: pass
- probe_id: V8
  description: "Shared-machinery non-MAJOR regression. apply.sh's edited source_path_for_deployed + apply_substitutions drive delivery for ALL categories; confirm non-MAJOR DRIFTED still delivers and a non-substrate .claude/MAJOR_FOO.md is NOT over-matched into a write; plus the design's check.sh detection probe (a)-(d)."
  quadrant_classification: easy-easy
  command_or_method: |
    bash substrate/install.sh --target project --project-dir /tmp/arc66-tw-regress
    printf '\n# arc66-regress-drift\n' >> /tmp/arc66-tw-regress/.claude/operating-disciplines.md   # (a) DRIFTED
    rm -f /tmp/arc66-tw-regress/.claude/agents/CAPTAIN_VERA_arc66_tw_regress.md                      # (b) MISSING (suffixed; see methodology note)
    mkdir -p /tmp/arc66-tw-regress/.claude/agents/custom; printf '# pp\n' > .../custom/CAPTAIN_PAIRBOT.md  # (c) must NOT OBSOLETE
    printf '# orphaned\n' > /tmp/arc66-tw-regress/.claude/agents/CAPTAIN_GHOST.md                    # (c) SHOULD OBSOLETE
    printf '# pp major\n' > /tmp/arc66-tw-regress/.claude/MAJOR_FOO.md                               # (d) SHOULD OBSOLETE, no over-match
    bash substrate/skills/check-substrate-updates/check.sh --workspace /tmp/arc66-tw-regress
    bash substrate/skills/check-substrate-updates/apply.sh --workspace /tmp/arc66-tw-regress --all-differing --yes
  expected: "(a) operating-disciplines.md DRIFTED; (b) CAPTAIN_VERA (suffixed) MISSING; (c) PAIRBOT NOT OBSOLETE + CAPTAIN_GHOST OBSOLETE; (d) MAJOR_FOO OBSOLETE, op-disciplines NOT mis-mapped as MAJOR. apply.sh delivers op-disciplines (non-MAJOR DRIFTED); MAJOR_FOO unchanged (never written)."
  observed: |
    check.sh: "  - .claude/operating-disciplines.md" ; "  + .claude/agents/CAPTAIN_VERA_arc66_tw_regress.md" ;
              "  ! .claude/agents/CAPTAIN_GHOST.md" ; "  ! .claude/MAJOR_FOO.md (dropped from source; manual-rm — see footer)" ;
              PAIRBOT NOT flagged; op-disciplines NOT mis-mapped as MAJOR. (all 4 conditions met)
    apply.sh: "applied: .claude/operating-disciplines.md" (non-MAJOR DRIFTED delivered; MAJOR_*.md arm did NOT shadow operating-disciplines.md arm); re-check "0 drifted".
    MAJOR_FOO.md after apply: still "# pair-programmer major" — UNCHANGED (apply.sh writes only DRIFTED harvest; OBSOLETE non-substrate MAJOR never over-matched into a write).
  result: pass
- probe_id: V7
  description: "Grep gate INCLUDING apply.sh — no surviving hardcoded POLYBIUS/PLINY-only MAJOR enumeration LIST at any of the SIX fix loci (4 check.sh/install.sh + 2 apply.sh)."
  quadrant_classification: easy-easy
  command_or_method: |
    grep -nE 'MAJOR_POLYBIUS|MAJOR_PLINY' substrate/skills/check-substrate-updates/check.sh
    grep -nE 'MAJOR_POLYBIUS|MAJOR_PLINY' substrate/install.sh
    grep -nE 'MAJOR_POLYBIUS|MAJOR_PLINY' substrate/skills/check-substrate-updates/apply.sh
    # + Read of each of the 6 fix loci to confirm glob-derivation.
  expected: "Each of the 6 fix loci uses .claude/MAJOR_*.md (or substrate/MAJOR_*.md glob); every literal POLYBIUS/PLINY hit classified as a pre-named legitimate survivor."
  observed: |
    6 fix loci ALL glob-derived:
      check.sh:118  apply_substitutions case  -> .claude/MAJOR_*.md|.claude/agents/CAPTAIN_*.md
      check.sh:296  source_path_for_deployed  -> MAJOR_*.md) arm (after operating-disciplines.md))
      check.sh:407-419 enumerate_deployed     -> for ... ${SUBSTRATE_DIR}/MAJOR_*.md, subproject-only suffix
      install.sh:537-556 write_substrate_manifest -> for ... ${SCRIPT_DIR}/MAJOR_*.md, POLYBIUS-only USER_TIER_DIR guard preserved
      apply.sh:97  apply_substitutions case   -> .claude/MAJOR_*.md|.claude/agents/CAPTAIN_*.md
      apply.sh:186 source_path_for_deployed   -> MAJOR_*.md) arm (after operating-disciplines.md))
    All literal POLYBIUS/PLINY hits classified as legitimate survivors:
      - detect_tier POLYBIUS tier-sentinel: check.sh:360-375, apply.sh:221-230 (POLYBIUS-only, no PLINY)
      - NOT-STOA-DEPLOYED sentinels: check.sh:728, apply.sh:279
      - USER_TIER_DIR POLYBIUS-only guard: install.sh:552 ([ "$majname" = "MAJOR_POLYBIUS" ])
      - doc cross-refs / comment EXAMPLES: check.sh 272-273/303/305/416/501/521/537/551; apply.sh 192/194/379; install.sh comments
      - install.sh:895 cosmetic plan-preview echo (pure echo, display-only; pre-named survivor, consciously LEFT)
      - install.sh:154-156 + 954-1003 fresh-DEPLOY block (out-of-scope per design §0/§6 — already enumerates all 4 MAJORs by name incl CHIRON/HAMILTON, so NOT the blind-spot bug class; the bug was the UPDATE path only).
    No POLYBIUS+PLINY-only enumeration list survives at any fix locus.
  result: pass
- probe_id: V6
  description: "App full suite — gen-data re-derives whole roster, build, test. Assert from the FULL run (Arc 61 lesson)."
  quadrant_classification: easy-easy
  command_or_method: "cd app && npm run gen-data && npm run build && npm test"
  expected: "gen-data discovers full roster; build succeeds; all tests pass."
  observed: |
    gen-data: "discovered 16 role file(s)" ; "roster: 4 MAJOR(s), 12 CAPTAIN(s), 7 LIEUTENANT(s); COLONEL reserved-empty; HUMAN stub"
    build: "✓ 1595 modules transformed" ; "✓ built in 3.40s" (only a standard >500kB chunk-size advisory, not an error)
    test: "Test Files 3 passed (3)" ; "Tests 41 passed (41)"
    gen-data dirtied app/src/data/generated/agents.ts with CRLF-ONLY noise (git diff --ignore-all-space = empty); restored via git checkout to keep worktree pristine.
  result: pass
- probe_id: V1
  description: "Spot-check (unchanged check.sh) — visibility flip: removed CHIRON/HAMILTON report MISSING at project tier."
  quadrant_classification: easy-easy
  command_or_method: |
    bash substrate/install.sh --target project --project-dir /tmp/arc66-v1spot
    rm -f /tmp/arc66-v1spot/.claude/MAJOR_CHIRON.md /tmp/arc66-v1spot/.claude/MAJOR_HAMILTON.md
    bash substrate/skills/check-substrate-updates/check.sh --workspace /tmp/arc66-v1spot
  expected: "CHIRON + HAMILTON reported MISSING ('  + ')."
  observed: |
    "  + .claude/MAJOR_CHIRON.md (new in source)" + "  + .claude/MAJOR_HAMILTON.md (new in source)"
  result: pass
- probe_id: V9
  description: "Standing full-suite step — author-gate hook tests (change touches neither hooks nor author fields; assert from a real run)."
  quadrant_classification: easy-easy
  command_or_method: "bash substrate/hooks/tests/run-author-gate-tests.sh"
  expected: "all green."
  observed: "16 passed / 0 failed"
  result: pass
threat_coverage: []
# §35.5 carve-out: design §7 declares this NOT threat-ratified (process/tooling completeness fix,
# no runtime attack path; check.sh read-only/exit-0-always; apply.sh writes only DRIFTED harvest).
# ARGUS confirmed the carve-out. No threat-anchored probe required. Empty list is the valid carve-out state.
methodology_concerns:
- "design-probe gap (V8(b)): the design's V8(b) spec does `rm -f .claude/agents/CAPTAIN_VERA.md` (UNSUFFIXED) at a PROJECT-tier workspace where CAPTAINs ARE suffixed (deployed as CAPTAIN_VERA_<slug>.md). Run verbatim, the rm removes nothing and check.sh reports 0 missing — the probe as written does NOT exercise the intended non-MAJOR-MISSING case. I re-ran with the suffixed name (CAPTAIN_VERA_arc66_tw_regress.md) and confirmed MISSING fires correctly. This is a probe-spec defect (wrong filename for the tier), NOT a build defect. DAEDALUS should correct the V8(b) command to use the tier-correct (suffixed) CAPTAIN name. Build behavior is correct."
- "out-of-arc residue (informational, not a fix-locus): install.sh:895 cosmetic plan-preview echo and install.sh:154-156/954-1003 fresh-DEPLOY block still spell POLYBIUS/PLINY by name. The deploy block already enumerates all 4 MAJORs (incl CHIRON/HAMILTON) so it is not the blind-spot bug class; install.sh:895 is a display-only echo. Both pre-named/out-of-scope per design §0/§6 and the dispatch. install.sh:895 won't list CHIRON/HAMILTON in the subproject summary preview (display-only, zero behavior impact) — tracked cosmetic residue, the close-gate's call to sweep-or-leave."
falsifying_evidence_summary: ""
verification_artifacts_path: "agents/verdicts/stoa--5ju/ (no separate probe scripts; all probes were inline shell against fixed-literal throwaways /tmp/arc66-* which were removed after the run; verbatim commands + outputs recorded in probes_executed above)"
summary: |
  The EXPAND slice (ADA commit 9e1768a) FIXES the V2 falsification from the prior run. The previously-FAILED
  V2 now PASSES on both tiers: apply.sh --all-differing DELIVERS a DRIFTED MAJOR_CHIRON and MAJOR_HAMILTON at
  project tier ("applied: .claude/MAJOR_CHIRON.md" / "...HAMILTON.md", exit 0, re-check 0 drifted) where the
  prior build skipped them as "not a substrate-derived path" and left them DRIFTED; and at subproject tier the
  SUFFIXED MAJOR_CHIRON_arc66tw.md is delivered (apply.sh's new MAJOR_*.md arm strips the _arc66tw suffix to
  recover source). The load-bearing probes were V2 (the falsifier flip) and V8 (shared-machinery regression):
  V8 proves the apply.sh edit drives delivery for ALL categories without regression — a non-MAJOR DRIFTED
  operating-disciplines.md still delivers (the generalized MAJOR_*.md arm, ordered AFTER operating-disciplines.md,
  does not shadow it), and a non-substrate .claude/MAJOR_FOO.md is OBSOLETE-flagged but never over-matched into a
  write (apply.sh writes only the DRIFTED harvest). V7 confirms all SIX fix loci are glob-derived with no surviving
  POLYBIUS/PLINY-only enumeration list; every literal hit is a pre-named legitimate survivor (tier sentinels,
  USER_TIER_DIR POLYBIUS guard, doc cross-refs, the cosmetic install.sh:895 echo, the out-of-scope fresh-deploy
  block that already names all 4 MAJORs). V6 full app suite is green (4 MAJOR / 12 CAPTAIN roster, build OK,
  41/41 tests). Spot V1 (visibility flip MISSING) and V9 (author-gate 16/16) confirm no collateral regression.
  One methodology concern: the design's V8(b) probe uses the unsuffixed CAPTAIN_VERA.md at a project tier where
  CAPTAINs are suffixed, so as-written it removes nothing — a probe-spec defect, not a build defect; I re-ran with
  the tier-correct suffixed name and MISSING fired correctly. Overall verdict: PASS.
