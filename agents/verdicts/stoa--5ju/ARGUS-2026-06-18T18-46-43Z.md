status: completed
ticket: stoa--5ju
verdict: revise
design_artifact_audited: agents/design/stoa--5ju/design-rev1.md
audit_block:
  risks:
  - id: r1
    description: V1/V3 probes use install.sh/apply.sh flags that do not exist in the actual interface, so the DoD probes will not run as written (fail at arg-parse, not at the logic under test).
    evidence: "design sec4 V1 step-1 uses --dest + --with-captains; install.sh has neither -- the path flag is --project-dir (install.sh:623,741) and captains deploy by DEFAULT, flag is --no-captains (install.sh:666). V3 step uses --dest (subproject path flags are --parent-dir + --subproject, install.sh:761-762)."
    load_bearing: true
    quadrant_classification: easy-easy
  - id: r2
    description: V1 step-5 (apply.sh delivers them) is INCOHERENT with the tool -- apply.sh --all-differing harvests ONLY DRIFTED-prefix lines, never MISSING. A MISSING CHIRON/HAMILTON is delivered by a re-run of install.sh, not apply.sh. The probe as written cannot pass even after a correct fix; bare apply.sh --workspace (no --all-differing/--files) hard-errors.
    evidence: "apply.sh:302-316 harvests only the DRIFTED two-space-dash prefix (per check.sh:947); check.sh routing footer routes MISSING to install.sh ... for missing (check.sh:990-1000), NOT apply.sh. apply.sh:323-325 errors if neither --all-differing nor --files given. Corroborated by POLYBIUS comment on stoa--5ju."
    load_bearing: true
    quadrant_classification: easy-easy
  - id: r3
    description: V1 falsification baseline (step-2 pre-fix run) is logically muddled and self-cancelling -- the design own inline comment (sec4 V1 lines 248-250) admits the pre-fix removal yields still-no-MISSING-line, so step-2 produces an empty/identical result whether or not the files are present. The genuine falsifying contrast is post-fix-only (remove -> MISSING appears), so step-2 as a baseline adds no falsification value and reads as confused; a builder may mis-encode it.
    evidence: "design sec4 V1 step-2 + the parenthetical at lines 248-250. To be a real baseline it must capture pre-fix output BEFORE the edit lands and contrast against post-fix output on the SAME removed-file state."
    load_bearing: false
    quadrant_classification: easy-easy
  - id: r4
    description: Probe set does not exercise the SHARED-machinery regression surface POLYBIUS flagged on-ticket -- the three edited check.sh functions drive DRIFTED/MISSING/OBSOLETE for ALL file categories, not just MAJORs. No probe proves the generalized .claude/MAJOR_*.md apply_substitutions case + the source_path_for_deployed MAJOR_*.md arm did not regress non-MAJOR detection (operating-disciplines.md byte-compare, a pair-programmer MAJOR false-OBSOLETE, a custom CAPTAIN). V2/V5 touch MAJOR-only paths.
    evidence: "design sec4 V1-V7 are all MAJOR-scoped; POLYBIUS comment on stoa--5ju names the gap (full check-substrate-updates exercise across ALL THREE categories to prove the MAJOR-glob change did NOT regress OBSOLETE/DRIFTED for non-MAJOR files). Shared functions: check.sh apply_substitutions (:107), source_path_for_deployed (:280), enumerate_deployed (:391)."
    load_bearing: true
    quadrant_classification: easy-easy
  - id: r5
    description: Locus 3 narrows the apply_substitutions case pattern to .claude/MAJOR_*.md. This is a genuine SUPERSET only if no future/other .claude/MAJOR_*.md file should be deployed VERBATIM. Today all 4 MAJOR sources are role files needing the sub, so correct now -- but a pair-programmer-added .claude/MAJOR_FOO.md at a workspace would now be force-substituted at check-time where before it fell to the verbatim default arm. Bounded (check.sh is informational; only affects byte-compare of a non-substrate MAJOR), but a behavior change worth ratifying with eyes open.
    evidence: "check.sh:118-124 (current case head) vs design sec2 Locus 3. Source confirms exactly 4 substrate/MAJOR_*.md, all role files. The default verbatim arm at check.sh:125-128 is what a non-substrate MAJOR currently hits."
    load_bearing: false
    quadrant_classification: easy-hard
  - id: r6
    description: V7 grep-gate carve-outs are correct AS CLASSIFIED, but the design under-states surviving legitimate loci. Beyond detect_tier (sec5.1) and the POLYBIUS-only USER_TIER_DIR guard, the doc-comment policy block (check.sh:101-103) and the install.sh writer comment (install.sh:537) also carry literal POLYBIUS/PLINY; Locus 3/4 say they update these, but V7 as written HITS them and a builder reading V7 as classify-each-hit needs them pre-named or the gate is ambiguous on whether a leftover comment is a fail.
    evidence: "check.sh:101-103 substitution-policy comment lists MAJOR_POLYBIUS*.md / MAJOR_PLINY*.md as separate lines; install.sh:537 comment says MAJOR_POLYBIUS.md. design sec5.5 names these but sec4 V7 PASS-condition enumerates only detect_tier + USER_TIER_DIR guard as legitimate survivors."
    load_bearing: false
    quadrant_classification: easy-easy
  - id: r7
    description: Empty-glob silent-zero-MAJORs hazard is real and the design self-flags it (sec5.4) but classifies it out of scope. For a FAIL-LOUD-adjacent update tool, a substrate checkout that globbed zero MAJOR_*.md would silently enumerate zero MAJORs (no MISSING ever reported) -- a silent-wrong-result, the exact class the skill exists to prevent. Naming it as a ratify-or-defer decision rather than silently accepting it is the honest call. Not a security threat (no attack path); a correctness-completeness hazard.
    evidence: "design sec2 Locus 1/4 use shopt -s nullglob so an empty MAJOR_*.md glob runs zero loop iterations; design sec5.4 acknowledges emits-zero-lines-silently, not FAIL-LOUD. The CAPTAIN glob (check.sh:416) already has the same property, so pre-existing, not newly introduced by this arc."
    load_bearing: false
    quadrant_classification: easy-hard
  non_findings:
  - "sec3 suffix-correctness table: VERIFIED CORRECT at every cell against source. MAJORs unsuffixed at user+project (check.sh:401-408 else-branch; install.sh:960-963 SUFFIX_MAJORS=0 branch), suffixed at subproject ONLY (install.sh:954-958; SUFFIX_MAJORS=1 set only at install.sh:813). The project-tier no-suffix cell (the attack target) is correct -- install.sh leaves SUFFIX_MAJORS=0 at project (no set in project branch :740-759). No CAPTAIN-rule leakage in proposed Locus 1 (gates major_suffix on tier=subproject, not on the project|subproject suffix var)."
  - "Locus 2 suffix-strip: CORRECT at every tier. At user/project the strip is gated on tier=subproject AND -n suffix, so mbase passes through unstripped = already-unsuffixed source name. At subproject, strips _slug. Mirrors the proven CAPTAIN arm (check.sh:303-312). Over-trim hazard (sec5.2) negligible and inherited from shipped CAPTAIN pattern, not newly introduced."
  - "Locus 3 legacy-vs-primary relationship: CONFIRMED intact. apply_substitutions is the fallback (called only when no manifest, check.sh:155-163); apply_substitutions_from_manifest (Arc 38 primary, check.sh:146) is untouched and reads the manifest that Locus 4 fixes. The generalization does not change which path runs."
  - "Locus 4 USER_TIER_DIR POLYBIUS-only guard: CONFIRMED. Only MAJOR_POLYBIUS source carries the USER_TIER_DIR placeholder; install.sh substitutes it into POLYBIUS only (install.sh:987-991; SRC_PLINY/CHIRON/HAMILTON seds at :992-994 do NAME_SUFFIX only). The proposed majname=MAJOR_POLYBIUS guard reproduces the existing else-branch (install.sh:545-546)."
  - "Locus 2 case-arm ordering: SAFE. operating-disciplines.md has its own arm (check.sh:300) preceding the MAJOR arm; the generalized MAJOR_*.md arm cannot shadow it. Confirmed only 4 substrate/MAJOR_*.md exist, all role files."
  - "sec6 V7 detect_tier carve-out (de-hardcoding the tier sentinel NOT in scope): CONFIRMED legitimate. detect_tier (check.sh:354-369) keys on MAJOR_POLYBIUS as the canonical always-deployed tier sentinel -- a different concern (which-tier vs enumerate-all). Correctly out of scope."
  - "sec7 threat classification: CONFIRMED. not-threat-ratified (process/tooling change, no runtime attack path) is the correct sec35.5 carve-out. check.sh is read-only/informational (exit 0 always; check.sh:27-28); apply.sh is the only writer and is UNTOUCHED. r7/suffix hazards are correctness, not security attack paths -- no threat-anchored probe required."
  threat_coverage_assessment: "N/A -- sec35.5-carved-out arc (process/tooling change, no threat-ratified mitigation). No mapped mitigation, so no threat-anchored probe SPEC required. Carve-out confirmed (A4 ownership): no runtime attack path -- check.sh read-only, apply.sh untouched."
summary: >
  The design is structurally sound at its load-bearing core: the sec3 suffix-correctness table is correct at every cell (verified against source, including the project-tier no-suffix cell a CAPTAIN-rule copy-paste would break), the four-locus glob shape mirrors the proven CAPTAIN auto-discovery pattern with the correct subproject-only MAJOR suffix rule, Locus 2 suffix-strip is tier-correct, the Locus 4 USER_TIER_DIR POLYBIUS-only guard reproduces existing behavior, the legacy/primary apply_substitutions relationship stays intact, and the sec7 not-threat-ratified classification is the correct sec35.5 carve-out. The problems are concentrated in the VERIFICATION section, not the edit design. The most important load-bearing risk is r2+r1 together -- the V1 DoD probe as written cannot run AND cannot pass against a correct fix: it invokes flags that do not exist (--dest, --with-captains) and asserts apply.sh delivers MISSING files when apply.sh structurally harvests only DRIFTED (MISSING is install.sh-re-run territory per check.sh own routing footer). Compounding, r4 names a real coverage gap POLYBIUS already flagged on-ticket: the three edited functions are shared machinery for ALL file categories and no probe proves non-MAJOR detection did not regress. Overall posture: minor-to-moderate revisions -- the edit logic clears, but the probe/DoD block needs a correctness pass (real flag names, MISSING-via-install.sh-not-apply.sh, a coherent falsification baseline, and a shared-machinery non-MAJOR regression probe) before VERA can execute it. r5/r7 are ratify-with-eyes-open items, not blockers.
