status: completed
ticket: stoa--5ju
verdict: pass
diff_reviewed: main...arc-66/build (ff9f21e check.sh+install.sh; 9e1768a apply.sh; design-rev1/rev2 docs in range)
design_artifact_compared_against: agents/design/stoa--5ju/design-rev2.md

# CATO verdict — PASS-WITH-NITS (no blocking concerns)

Cold-read of the full arc diff plus six empirical probes against the live working
tree (per CAPTAIN_CATO.md §6.8). The fix is faithful to design-rev2, scope-clean,
and the load-bearing behavior is empirically confirmed: CHIRON + HAMILTON now
enumerate at every tier, correctly suffixed at subproject only.

## Meta-verifier statement (CATO + VERA redundant pair)
I CONCUR with VERA r2 PASS (VERA-2026-06-18T19-39-37Z.md). I independently
re-ran the load-bearing checks and found nothing VERA missed. I also independently
confirmed VERA's V8(b) note: the design probe used an UNSUFFIXED CAPTAIN name at a
suffixed project tier — that is a probe-spec error, NOT a build defect. My own
strip probe with a SUFFIXED subproject MAJOR name (MAJOR_CHIRON_acme.md) round-trips
to MAJOR_CHIRON.md correctly, including a pathological slug=POLYBIUS case.

## concerns
(none blocking)

- id: c1
  category: hygiene
  description: install.sh:895 deploy-summary echo still hardcodes "MAJOR_POLYBIUS${NAME_SUFFIX}.md, MAJOR_PLINY${NAME_SUFFIX}.md" so the human-facing summary will not list CHIRON/HAMILTON when SUFFIX_MAJORS=1.
  evidence: substrate/install.sh:894-896
  severity: minor
  quadrant_classification: easy-easy
  note: Display-only, zero behavior impact. Consciously LEFT by ADA (comma-joined display-string generalization not trivially clean) and tracked as a follow-up by PLINY (r7). Defensible scope call, NOT a miss — the functional manifest writer at :537-555 IS glob-derived. Confirm the follow-up ticket lands at close.

- id: c2
  category: consistency
  description: The fresh-DEPLOY block (install.sh:954-1007) hand-enumerates all four MAJORs (SRC/DEST_POLYBIUS/PLINY/CHIRON/HAMILTON) rather than globbing — a different idiom from the glob-derive used at the six fix loci. A future 5th MAJOR would require editing this block too.
  evidence: substrate/install.sh:954-1007 (and SRC_* at :155-158)
  severity: minor
  quadrant_classification: easy-easy
  note: OUT OF SCOPE per design §0/§6 (VERA V7 classified it the same). It already correctly names all four current MAJORs, so it is not broken today; it is a latent maintenance asymmetry, not a defect. Worth a tracked follow-up to converge the fresh-DEPLOY block onto the same glob idiom so "add a MAJOR" is a one-file source-drop everywhere.

## follow_ups
- install.sh:895 deploy-summary echo: generalize to a glob loop so it lists all deployed MAJORs (display-only; r7 already named). Reason for follow-up not block: zero behavior impact, pre-named, scope-locked.
- install.sh:954-1007 fresh-DEPLOY block: converge onto the glob-derive idiom used at the six fix loci. Reason for follow-up not block: out-of-scope per design §0/§6; correct for all current MAJORs; latent maintenance asymmetry only.

## verifier_coverage_assessment
VERA exercised the load-bearing cases adequately. V2 (the previously-FAILED probe)
now PASSES both tiers: project apply.sh delivers DRIFTED CHIRON+HAMILTON (0 drifted
after); subproject delivers SUFFIXED CHIRON_arc66tw (suffix-strip worked). V8 confirms
no non-MAJOR regression and that MAJOR_FOO is not over-matched by the writer. V7
grep-gate confirms all six loci glob-derived. V6 full app suite green (4 MAJOR / 12
CAPTAIN, 41/41 tests). I independently re-verified the highest-risk paths myself:
(a) suffix-strip correct at all three tiers incl pathological slug; (b) case-arm
ordering ops-disc-BEFORE-MAJOR_*.md in BOTH source_path_for_deployed instances
(check.sh:293/296, apply.sh:185/186) so the MAJOR arm cannot shadow ops-disc;
(c) the apply.sh WRITER glob .claude/MAJOR_*.md is single-path-segment — subdir,
.bak, and .txt all fall through to verbatim passthrough, so blast radius is bounded
(r5 security item satisfied: writer touches only the DRIFTED harvest, never a
non-substrate file); (d) empirical enumerate_deployed against live substrate emits
all four MAJORs at every tier. VERA's one methodology concern (V8(b) probe-spec gap)
is real and correctly classified as a probe-spec issue, not a build defect. No
coverage gap requiring an INCOMPLETE verdict.

## scope + authorship audit
- Exactly 7 hunks (1 install.sh manifest writer + 2 apply.sh + 1 check.sh doc-comment
  sweep + 3 check.sh: apply_substitutions/source_path/enumerate_deployed). Nothing
  edited outside the named loci.
- detect_tier POLYBIUS tier-sentinel PRESERVED (check.sh:362/375, apply.sh:221/230) —
  correctly still uses MAJOR_POLYBIUS as the canonical subproject/project detector.
- USER_TIER_DIR POLYBIUS-only guard PRESERVED and correctly gated (install.sh:552
  emits {{USER_TIER_DIR}} only for majname=MAJOR_POLYBIUS at user-tier).
- is_role_file (apply.sh:243) and the two OBSOLETE-partition arms (check.sh:810/878)
  were ALREADY glob-based before this arc — the "6 loci" enumeration was therefore
  complete; nothing under-enumerated. Role files untouched.
- All surviving MAJOR_POLYBIUS/MAJOR_PLINY string literals are legitimate classified
  survivors (tier-sentinels, NOT-STOA-DEPLOYED messages, doc cross-refs, comment
  examples). Zero surviving hardcoded POLYBIUS+PLINY functional enumeration pair.
- Authorship CLEAN: Author=denson <densonsmith2@gmail.com> on both build commits;
  seat-identity Co-Authored-By: CAPTAIN_ADA_the-stoa + Claude Opus 4.8 trailers present;
  no author-like field falsely attributed anywhere in the diff.
- bash -n exit 0 on all three files.

## summary
A tight, well-bounded generalization fix: six loci converted from a hardcoded
POLYBIUS+PLINY MAJOR pair to a glob-derive over substrate/MAJOR_*.md, applying the
subproject-only suffix rule identically everywhere and faithfully mirroring the
adjacent CAPTAIN glob idiom (nullglob balanced, basename strip, local var scope all
consistent). The footgun the design names — MAJOR suffix rule DIFFERS from CAPTAINs
(subproject-only vs project+subproject) — is handled correctly at every locus, verified
empirically across all tiers including a pathological slug. The most important
positive finding: the apply.sh writer glob does not over-match (blast radius bounded),
and the case-arm ordering cannot shadow operating-disciplines.md in either file. The
two nits are display-only / out-of-scope-by-design maintenance asymmetries, both already
tracked as follow-ups. I concur with VERA's PASS. Overall posture: CLEAN with two
minor follow-ups. Ready for final gate.

threat_coverage: N/A — this arc ships no threat-ratified mitigation (a correctness/
generalization fix); §35.5 self-carve-out applies, threat_ratified_mitigation_count=0.
