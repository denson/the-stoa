status: completed
ticket: stoa--5ju
verdict: fail
design_artifact_verified_against: agents/design/stoa--5ju/design-rev2.md
build_verified: arc-66/build @ ff9f21e61d48bc0029bda2066acd05aec1a57e5c (worktree .claude/worktrees/arc-66-build)
probes_executed:
- probe_id: V1
  description: "Visibility flip (project tier) — removed CHIRON/HAMILTON report MISSING post-fix where pre-fix they were in neither category; MISSING delivered by install.sh re-run -> re-check clean."
  quadrant_classification: easy-easy
  command_or_method: |
    bash substrate/install.sh --target project --project-dir /tmp/arc66-tw-project
    rm -f /tmp/arc66-tw-project/.claude/MAJOR_CHIRON.md /tmp/arc66-tw-project/.claude/MAJOR_HAMILTON.md
    # PRE-FIX: parent-commit check.sh (ff9f21e^) copied into skill dir for SUBSTRATE_DIR resolution
    bash <prefix>check.sh --workspace /tmp/arc66-tw-project   # baseline
    bash substrate/skills/check-substrate-updates/check.sh --workspace /tmp/arc66-tw-project  # post-fix
    bash substrate/install.sh --target project --project-dir /tmp/arc66-tw-project  # delivery (re-run)
    bash substrate/skills/check-substrate-updates/check.sh --workspace /tmp/arc66-tw-project  # re-check
  expected: "PRE-FIX: CHIRON/HAMILTON in NEITHER MISSING nor DRIFTED. POST-FIX: both under MISSING ('  + '). After install.sh re-run: clean."
  observed: |
    PRE-FIX report: grep CHIRON|HAMILTON => NO MATCH (in neither category — the documented bug).
    POST-FIX report:
      + .claude/MAJOR_CHIRON.md                            (new in source)
      + .claude/MAJOR_HAMILTON.md                          (new in source)
    After install.sh re-run + re-check: CHIRON/HAMILTON CLEAN (no MISSING/DRIFTED).
  result: pass
- probe_id: V2
  description: "DRIFTED detection (Locus 2 maps deployed MAJOR back to source) + apply.sh --all-differing DRIFTED-only harvest round-trip on a stale MAJOR_CHIRON."
  quadrant_classification: easy-easy
  command_or_method: |
    printf '\n# arc66-drift-probe\n' >> /tmp/arc66-tw-project/.claude/MAJOR_CHIRON.md
    bash substrate/skills/check-substrate-updates/check.sh --workspace /tmp/arc66-tw-project       # V2a
    bash substrate/skills/check-substrate-updates/apply.sh --workspace /tmp/arc66-tw-project --all-differing --yes  # V2b
    bash substrate/skills/check-substrate-updates/check.sh --workspace /tmp/arc66-tw-project       # V2c
  expected: "V2a: MAJOR_CHIRON.md DRIFTED. V2b: apply.sh re-deploys it (exit 0). V2c: re-check clean for CHIRON."
  observed: |
    V2a PASS: '  - .claude/MAJOR_CHIRON.md  (-2 lines)' (DRIFTED — check.sh Locus 2 maps it to source correctly).
    V2b FAIL: apply.sh exit 0 BUT emitted 'apply.sh: skipping (not a substrate-derived path): .claude/MAJOR_CHIRON.md' — it did NOT re-deploy CHIRON.
    V2c FAIL: re-check STILL shows '  - .claude/MAJOR_CHIRON.md' DRIFTED (delivery never happened).
    ROOT CAUSE: apply.sh has its OWN source_path_for_deployed (apply.sh:185-186) + apply_substitutions case (apply.sh:97) that STILL hardcode MAJOR_POLYBIUS|MAJOR_PLINY only. apply.sh:185-186 returns '' for a deployed CHIRON -> the skip at apply.sh:384. Verified apply.sh is byte-identical pre/post-fix (the git-diff is pure CRLF/LF noise) => apply.sh was untouched by ff9f21e and carries the SAME hardcoded-MAJOR-enumeration bug the arc fixed in check.sh/install.sh. apply.sh:233 ALREADY uses .claude/MAJOR_*.md for the deployable-path check, so apply.sh is internally INCONSISTENT — which is why 'apply.sh is correct' slipped past design-time review.
  result: fail
- probe_id: V3
  description: "Subproject tier — REAL FAIL-LOUD recompose (not --dry-run); confirm install exits 0 (Checks A-E green) + MAJOR names suffixed; then suffixed-name MISSING slice."
  quadrant_classification: easy-easy
  command_or_method: |
    bash substrate/install.sh --target project --project-dir /tmp/arc66-parent          # establish parent
    bash substrate/install.sh --target subproject --parent-dir /tmp/arc66-parent --subproject arc66tw  # REAL recompose
    ls /tmp/arc66-parent/arc66tw/.claude/MAJOR_*.md
    rm -f /tmp/arc66-parent/arc66tw/.claude/MAJOR_CHIRON_arc66tw.md
    bash substrate/skills/check-substrate-updates/check.sh --workspace /tmp/arc66-parent/arc66tw
  expected: "install exit 0 (A-E green); 4 MAJORs suffixed _arc66tw incl CHIRON/HAMILTON; removed suffixed name reports MISSING; no unsuffixed false-MISSING/OBSOLETE footgun."
  observed: |
    subproject install exit: 0 (recompose ran on 5 files: POLYBIUS_arc66tw, op-disciplines, PLINY_arc66tw, CHIRON_arc66tw, CAPTAINs — Checks A-E inside recompose_module_inline (install.sh:1055+) all passed; any mismatch err()s non-zero).
    Deployed: MAJOR_POLYBIUS_arc66tw.md, MAJOR_PLINY_arc66tw.md, MAJOR_CHIRON_arc66tw.md, MAJOR_HAMILTON_arc66tw.md (all suffixed).
    MISSING slice: '  + .claude/MAJOR_CHIRON_arc66tw.md  (new in source)' — SUFFIXED name MISSING; NO unsuffixed MAJOR_CHIRON.md false-MISSING. Footgun absent.
  result: pass
- probe_id: V4
  description: "Project-tier no-false-suffix — zero MISSING + zero OBSOLETE for any MAJOR on a clean project deploy."
  quadrant_classification: easy-easy
  command_or_method: "bash substrate/skills/check-substrate-updates/check.sh --workspace /tmp/arc66-tw-project; grep -E '^  [+!] .*MAJOR_'"
  expected: "No MAJOR under MISSING ('  + ') or OBSOLETE ('  ! ') — a leaked CAPTAIN (project+subproject) suffix rule would produce one MISSING + one OBSOLETE per MAJOR."
  observed: "grep '^  [+!] .*MAJOR_' => NO MATCH. Summary line: '0 missing' for MAJORs (the 4 OBSOLETE in the summary are pre-existing skill-dir entries, NOT MAJOR — see methodology_concerns)."
  result: pass
- probe_id: V5
  description: "Manifest completeness (install.sh Locus 4) — exactly 4 MAJOR lines per tier; project unsuffixed + no USER_TIER_DIR line; subproject suffixed."
  quadrant_classification: easy-easy
  command_or_method: "grep 'MAJOR_' <ws>/.claude/.substrate-manifest  (project + subproject)"
  expected: "Project: 4 NAME_SUFFIX lines (CHIRON, HAMILTON, PLINY, POLYBIUS), unsuffixed dep-paths, no USER_TIER_DIR line. Subproject: 4 lines, _arc66tw suffix in dep-path + replacement."
  observed: |
    Project (4 lines): .claude/MAJOR_{CHIRON,HAMILTON,PLINY,POLYBIUS}.md <tab> {{NAME_SUFFIX}} <tab> _arc66_tw_project. No USER_TIER_DIR line (correct — only at user tier).
    Subproject (4 lines): .claude/MAJOR_{CHIRON,HAMILTON,PLINY,POLYBIUS}_arc66tw.md <tab> {{NAME_SUFFIX}} <tab> _arc66tw.
    CHIRON/HAMILTON now present in the manifest at both tiers (the Locus 4 fix).
  result: pass
- probe_id: V6
  description: "App full suite (gen-data re-derives whole roster, build, test) — assert from full run."
  quadrant_classification: easy-easy
  command_or_method: "cd app && npm ci && npm run gen-data && npm run build && npm test"
  expected: "gen-data clean, build green, all tests pass; no pre-existing roster drift surfaced."
  observed: |
    gen-data: discovered 16 role files; roster 4 MAJOR(s) [glob picks up all incl CHIRON/HAMILTON], 12 CAPTAIN(s), 7 LIEUTENANT(s). No error.
    build: vite — 1595 modules transformed, built in 8.41s (chunk-size advisory is pre-existing, not a failure).
    test: vitest run — Test Files 3 passed (3); Tests 41 passed (41).
    Note: gen-data regenerated app/src/data/generated/agents.ts but git diff was PURE line-ending (LF->CRLF) noise, zero content change => committed roster matches re-derivation. Restored agents.ts to keep the build pristine (verifier independence §5.1).
  result: pass
- probe_id: V7
  description: "No surviving hardcoded POLYBIUS/PLINY-only MAJOR ENUMERATION at the four fix loci (classified, not bare grep -c)."
  quadrant_classification: easy-easy
  command_or_method: "grep -nE 'MAJOR_POLYBIUS|MAJOR_PLINY' check.sh + install.sh; manual classification of each hit."
  expected: "No paired POLYBIUS+PLINY enumeration at the four fix loci; doc-comments #4 (check.sh ~101-103) + #5 (install.sh ~537) swept to MAJOR_*.md; only pre-named legit survivors remain."
  observed: |
    Four fix loci CLEAN of paired enumeration: Locus1 enumerate_deployed (glob), Locus2 source_path_for_deployed (MAJOR_*.md arm; only illustrative '# MAJOR_POLYBIUS' comments), Locus3 apply_substitutions (no POLYBIUS/PLINY hits), Locus4 manifest writer (only the POLYBIUS-only USER_TIER_DIR guard).
    Doc-comment #4 (check.sh:101) = '.claude/MAJOR_*.md : NAME_SUFFIX' (swept). Doc-comment #5 (install.sh:537) = glob-form comment (swept; POLYBIUS named only as the USER_TIER_DIR-placeholder carrier).
    Legit survivors confirmed: (#1/#3) detect_tier tier-sentinel (check.sh:360-375); (#2) POLYBIUS-only USER_TIER_DIR guard (install.sh:552). Out-of-scope but complete: install.sh deploy-path SRC_/DEST_ enumeration (155-157, 963-971) lists all 4 MAJORs (deploy path complete per design §0). Minor cosmetic: install.sh:895 summary echo still names only POLYBIUS/PLINY (status string, not a fix locus — methodology_concerns).
  result: pass
- probe_id: V8
  description: "SHARED-MACHINERY non-MAJOR regression — prove the 3 edited check.sh fns did not regress DRIFTED/MISSING/OBSOLETE for non-MAJOR categories, and MAJOR_*.md glob does not over-match."
  quadrant_classification: easy-easy
  command_or_method: |
    bash substrate/install.sh --target project --project-dir /tmp/arc66-tw-regress
    printf '\n# arc66-regress-drift\n' >> .../operating-disciplines.md     # (a) DRIFTED
    rm -f .../agents/CAPTAIN_VERA_arc66_tw_regress.md                       # (b) MISSING (suffixed name at project tier)
    mkdir -p .../agents/custom; printf '...' > .../agents/custom/CAPTAIN_PAIRBOT.md   # (c neg)
    printf '...' > .../agents/CAPTAIN_GHOST.md                              # (c pos)
    printf '...' > .../MAJOR_FOO.md                                         # (d) over-match boundary
    bash substrate/skills/check-substrate-updates/check.sh --workspace /tmp/arc66-tw-regress
  expected: "(a) op-disc DRIFTED; (b) CAPTAIN_VERA MISSING; (c) PAIRBOT NOT OBSOLETE + GHOST OBSOLETE; (d) MAJOR_FOO OBSOLETE under manual-rm footer, op-disc NOT mis-listed as MAJOR."
  observed: |
    (a) PASS: '  - .claude/operating-disciplines.md  (-2 lines)' DRIFTED — MAJOR_*.md) arm did NOT shadow operating-disciplines.md) arm.
    (b) PASS: '  + .claude/agents/CAPTAIN_VERA_arc66_tw_regress.md  (new in source)' MISSING. (Note: at project tier CAPTAINs ARE slug-suffixed; my first attempt removed unsuffixed CAPTAIN_VERA.md = no-op, corrected by removing the actual suffixed file — build behaved correctly throughout; probe-construction nuance, not a defect.)
    (c) PASS: CAPTAIN_PAIRBOT.md (custom/) NOT in OBSOLETE; '  ! .claude/agents/CAPTAIN_GHOST.md  (dropped from source)' OBSOLETE.
    (d) PASS: '  ! .claude/MAJOR_FOO.md  (dropped from source; manual-rm — see footer)' with the MAJOR-excluded-from-prune footer; operating-disciplines.md NOT mis-listed as a MAJOR.
  result: pass
- probe_id: V9
  description: "Author-gate hook tests (other existing suite the change could touch)."
  quadrant_classification: easy-easy
  command_or_method: "bash substrate/hooks/tests/run-author-gate-tests.sh"
  expected: "All green."
  observed: "16 passed / 0 failed; exit 0."
  result: pass
threat_coverage: []
# Empty list is valid: design §7 classifies this arc as the operating-disciplines.md §35.5 carve-out
# (process/tooling completeness fix; check.sh read-only/exit-0-always; apply.sh the only writer; no
# runtime attack path). ARGUS (rev1 verdict non_findings) + PLINY (A1 ratification-restatement beat)
# both confirmed: NOT threat-ratified, no threat-anchored probe required. threat_ratified_mitigation_count = 0.
methodology_concerns:
- "V2 DRIFTED-delivery slice is NOT met by the shipped tooling: apply.sh carries an UNFIXED 5th locus of the identical hardcoded-MAJOR-enumeration bug (apply.sh:97 apply_substitutions case + apply.sh:185-186 source_path_for_deployed). The design scoped apply.sh OUT (§6: 'apply.sh — correct; untouched'); V2 execution FALSIFIES the 'apply.sh is correct' premise. apply.sh:233 already uses MAJOR_*.md, so apply.sh is internally inconsistent. This is the load-bearing finding; floor-manager independently corroborated it; the scope-expansion-vs-defer decision is routed to PLINY/user-tier POLYBIUS (directive author/close-gate), NOT VERA's call."
- "Pre-existing, non-MAJOR, out-of-scope: 4 skills (check-bw-release, check-substrate-updates, inspect-script-output, validate-spec) report OBSOLETE in BOTH pre-fix AND post-fix reports despite existing in source AND being deployed. This is a separate check.sh skill-enumeration gap, unrelated to and unworsened by this arc. Noted so it is not mistaken for an arc regression; candidate for a separate ticket."
- "Cosmetic-only (not a fix locus): install.sh:895 deploy-summary echo still names only 'MAJOR_POLYBIUS${NAME_SUFFIX}.md, MAJOR_PLINY${NAME_SUFFIX}.md' — an incomplete status string (does not affect correctness; not one of the four enumeration loci). Minor; flag for a sweep when apply.sh is fixed."
falsifying_evidence_summary: |
  The arc's stated GOAL (per design §0) is to make the MAJOR set glob-derived so CHIRON/HAMILTON (and any
  future MAJOR) are visible to the check-substrate-updates UPDATE PATH and this bug class cannot recur. The
  four enumeration loci the design named (check.sh enumerate_deployed / source_path_for_deployed /
  apply_substitutions + install.sh write_substrate_manifest) are all built correctly and PASS V1/V3/V4/V5/V7/V8.
  BUT the update path has a FIFTH locus the design's locus-list missed: apply.sh — the WRITER half of
  check-substrate-updates — still hardcodes MAJOR_POLYBIUS|MAJOR_PLINY at apply.sh:97 (apply_substitutions
  case) and apply.sh:185-186 (its own source_path_for_deployed). Concrete falsification (V2):
    1. `printf '\n# arc66-drift-probe\n' >> .../MAJOR_CHIRON.md` ; check.sh => '  - .claude/MAJOR_CHIRON.md (-2 lines)' DRIFTED (correct).
    2. `apply.sh --workspace .../arc66-tw-project --all-differing --yes` => exit 0 but prints
       'apply.sh: skipping (not a substrate-derived path): .claude/MAJOR_CHIRON.md' — does NOT re-deploy.
    3. re-run check.sh => '  - .claude/MAJOR_CHIRON.md' STILL DRIFTED.
  Consequence: a DRIFTED (deployed-but-stale) CHIRON or HAMILTON CANNOT be brought current via the
  check-substrate-updates apply path — exactly the bug class the arc set out to eliminate, surviving in the
  writer. The arc HALF-meets its goal: MISSING visibility + install.sh-re-run delivery work (V1); DRIFTED
  delivery via apply.sh does not (V2). I confirmed apply.sh is byte-identical pre/post-fix (git diff = pure
  CRLF noise), so this is a pre-existing instance of the same bug the four-loci fix targets, not a regression
  introduced by ff9f21e. Verdict = FAIL because a stated-DoD slice (V2 DRIFTED round-trip for the auto-
  discovered MAJORs) is not met by the shipped tooling. The four built loci are sound; the deliverable is
  incomplete against its own goal. Routing/scope (expand-this-arc vs scoped-follow-up) is PLINY/close-gate
  territory, not VERA's.
verification_artifacts_path: /tmp/arc66-*.txt and /tmp/arc66-*.log (probe reports + install logs; throwaway deploy dirs cleaned per §8.6 fixed-literal-path discipline). Verdict body at agents/verdicts/stoa--5ju/.
summary: |
  Executed all nine design-rev2 §4 probes against the as-built tree (arc-66/build @ ff9f21e). Eight pass
  (V1, V3, V4, V5, V6, V7, V8, V9); one fails (V2). The CORE FIX is correct: the visibility flip is real and
  demonstrated (pre-fix CHIRON/HAMILTON in NEITHER category vs post-fix both MISSING, against an identical
  removed-file state, using the parent-commit check.sh for the genuine baseline), MISSING delivers via
  install.sh re-run, the subproject suffix is applied to the glob result (V3) without the project-tier
  false-suffix footgun (V4), the manifest carries all 4 MAJORs at both tiers (V5), the four enumeration loci
  are swept of paired hardcoded enumeration incl the two doc-comments (V7), the shared-machinery non-MAJOR
  detection is unregressed and the MAJOR_*.md glob does not over-match operating-disciplines.md (V8), and the
  full app suite + author-gate tests are green (V6, V9). The load-bearing FAIL is V2: apply.sh — the writer
  half of the same skill — carries an UNFIXED fifth locus of the identical hardcoded-MAJOR-enumeration bug
  (apply.sh:97 + :185-186), so a DRIFTED CHIRON/HAMILTON cannot be delivered through the apply path. The
  design scoped apply.sh out on the premise it was correct; execution falsifies that premise. This is the
  arc's pivotal decision (expand scope to fix the fifth locus vs land four loci + scoped follow-up) — routed
  to PLINY / user-tier POLYBIUS, not VERA. As built, the deliverable does not fully meet its own goal of
  removing the hardcoded MAJOR enumeration from the update path.
