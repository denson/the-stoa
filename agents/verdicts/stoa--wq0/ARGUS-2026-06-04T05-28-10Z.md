status: completed
ticket: stoa--wq0 (+ stoa--xxy + stoa--7b1.2 + stoa--7ap; Arc 56)
verdict: revise
design_artifact_audited: agents/design/arc-56/design.md

== ARGUS verdict: RATIFY-WITH-CONDITIONS ==
Design is structurally sound; its three ground-truth reframes (W4-split, 7b1.2 premise disproven, heredoc/tmp pattern emergent-not-taught) are CONFIRMED correct against the live tree. The W4 module-split premise PLINY ratified is FACTUALLY TRUE. Five load-bearing conditions must fold in before ADA builds; all easy/cheap, none structural.

audit_block:
  risks:
  - id: r1
    description: role-para byte-identical premise (design 2.5 + change C) is FALSE for VERA. CAPTAIN_VERA.md L255 carries an extra INCOMPLETE/UNVERIFIABLE sentence (530 chars vs ARGUS 375 / CATO 374). The byte-identical premise and the CATO verify-instruction (verify appended sentences differ ONLY in officer token) are wrong as written; ADA following 2.5 clobbers VERA extra sentence or produces a misleading diff.
    evidence: design.md 2.5 (L320-326) + change C (L156-172); CAPTAIN_VERA.md:255 vs CAPTAIN_ARGUS.md:227 / CAPTAIN_CATO.md:209
    load_bearing: true
    quadrant_classification: easy-easy
  - id: r2
    description: 7ap split hardens the §5.9.4 cleanup FENCE (L67-71 -> dir-gone assert) and §5.10 verify, but neither touches pre-branch-hygiene.md §5.9.4 L73 prose verify-list (still says only git-worktree-list should not show the path = stale registration-only check, the exact thing 7ap fixes). After build §5.9.4 self-contradicts and L73 contradicts hardened §5.10. The fix must ALSO harden §5.9.4 L73 to assert dir-gone.
    evidence: pre-branch-hygiene.md:73; design change H targets L65-73 fence but specifies no L73-prose edit; change I (3.2) hardens §5.10 only
    load_bearing: true
    quadrant_classification: easy-easy
  - id: r3
    description: xxy facet-1 change F leaves --artifact-path (required=True, _save_verdict.py:66) handling unresolved — ADA may keep-required-but-ignored OR relax-to-optional, AND separately the design offers dropping --artifact-path from the step-9 example. The combination keep-required + drop-from-example self-breaks the documented invocation (argparse exit-2 -> exit 4). Pin ONE coherent combination.
    evidence: _save_verdict.py:66 (required=True); design 2.3 L265-272 (W3) unresolved; SKILL.md:111 example currently passes --artifact-path
    load_bearing: true
    quadrant_classification: easy-easy
  - id: r4
    description: wq0 silent-green §34 hard-constraint (ANY path-mismatch must fail loud) is satisfied only for the /tmp-prefix form by guard B. A non-/tmp MSYS alias (/c/Users/...) or a relative path resolving differently under Write-tool vs Python cwd can still mismatch; if it points at an existing stale file the script reads it (exists->read_bytes) and writes green-with-wrong-content. Residual gap not a regression. Design should state §34 is satisfied for the /tmp form and the Write-tool-to-relative-path PROCEDURE (not the guard) closes the general case, so the claim is honest not over-broad.
    evidence: directive §34; design change B L132 (/tmp-only) + W1 L554-562; _save_verdict.py:267-270 (exists-then-read)
    load_bearing: true
    quadrant_classification: hard-easy
  - id: r5
    description: threat classifications (§5) CONFIRMED by ARGUS as §35 upstream-owner — wq0 guard = not-threat-ratified (correctness hardening); xxy/7b1.2/7ap = §35.5 process/doc carve-out. Path-traversal check on change F: receipt path uses ticket_id + officer, both regex-validated (_save_verdict.py:43-45 reject dotdot/leading-dash/lowercase) BEFORE composition; request_id is interpolated unvalidated into the receipt FILENAME but dest stays under cwd/agents/save-verdict/<validated-ticket>/, so request_id without a path-sep cannot escape. Minor non-load-bearing hardening recommended: validate request_id / reject path-sep in change F. No M<n> issued; carve-outs CONFIRMED.
    evidence: design §5 L533-548; _save_verdict.py:43-45 + 204-207; change F (request_id unvalidated)
    load_bearing: false
    quadrant_classification: easy-easy
  non_findings:
  - W4-split premise CONFIRMED TRUE: pre-branch-hygiene.md:65-73 holds the literal worktree-remove/branch-D/push-delete sequence; arc-close-hygiene §5.10 is verify-only. PLINY ratification rests on a TRUE premise.
  - 7b1.2 directive premise (committed pyc) DISPROVEN by git ls-files (4 source files, 0 pyc). Design reframe correct.
  - 7b1.2 .gitignore deploys: install.sh:1181 cp -R src/. dest/ copies dotfiles; post-copy strip 1193-1194 removes only __pycache__/+*.pyc not .gitignore. Design hedge resolves to YES.
  - 7b1.2 no-conflict: 3 agreeing layers (repo-root .gitignore:10-11, deployed .claude/.gitignore Arc55, skill-local) identical; git nearest-scope cannot conflict identical rules.
  - "#41740 citation web-confirmed current: Windows worktree-remove fails busy/perm-denied from in-worktree process lock. Not stale, not mischaracterized."
  - facet-1/facet-2 coupling: receipt path computed from cwd_abs (same cwd as verdict :258); pinning worktree --cwd (E) lands receipt in worktree too. Coherent.
  - rm -rf safety (op-disc §8.6): fixed literal arc-N-build (author-time substitution, no runtime VAR) — no VERA permission-pause.
  - author fields: design.md:3 + SKILL.md:4 both Denson Smith. Correct.
  - enforcement-hook scope: no hook exists/referenced; doc-only fix correct; hook-build OUT of scope.

== DAEDALUS residual questions answered ==
W1 (narrow /tmp guard vs broader): narrow-guard + Write-tool procedure is the RIGHT balance; a general bash-alias detector is undecidable from the string and risks false-positive rejection of legitimate absolute paths. Fold r4 to make the §34 claim honest. CONFIRMED-with-r4.
W2 (teaching fix, doc-discipline-dependent): acceptable this arc; stronger runtime enforcement OUT per no-new-hook lock; teaching at 4 surfaces removes the reason CAPTAINs reach for the heredoc. CONFIRMED.
W3 (artifact-path back-compat): relax to optional (cleaner) AND keep the step-9 example passing it as ignored; do NOT drop it from the example while leaving required=True. CONFIRMED-with-r3.
W4 (7ap different module than directive): premise TRUE, split correct, PLINY ratified on a true basis (avoids two-sources-of-truth). But the split is INCOMPLETE — it misses §5.9.4 L73 prose (r2). CONFIRMED-with-r2.
W5 (7b1.2 low marginal value): agreed — self-containment/vendoring value, not a live repo bug; cheap, cannot conflict. Ship it. Non-blocking.
W6 (P-xxy2 doc-checks the dispatch-discipline only): correct factoring; the skill cannot know the right tree; VERA runtime-verifies the skill half + doc-checks the discipline half. CONFIRMED.

summary: Arc 56 is a sound four-fix skill+discipline-hardening change across two disjoint surfaces. Every ground-truth reframe the design makes is verified correct against the live tree, the W4 module-split premise PLINY ratified is factually true, and the #41740 web citation is current. Verdict is REVISE (not pass) on five load-bearing conditions, all cheap: r1 the role-paragraph byte-identical premise is false for VERA and the CATO verify instruction is wrong; r2 (most important) the 7ap split hardens two of three verify surfaces but leaves pre-branch-hygiene §5.9.4 L73 prose stating the stale registration-only check, self-contradicting the hardened fence and §5.10; r3 the --artifact-path required-vs-optional + drop-from-example choice is unresolved and one combination self-breaks the documented example; r4 the §34 silent-green claim is honestly only /tmp-narrow and should say so; r5 (non-load-bearing) threat carve-outs CONFIRMED with a minor request-id-validation hardening noted. No structural problem; ADA can fold all five before build.

gap_or_blocker: (none — completed)
