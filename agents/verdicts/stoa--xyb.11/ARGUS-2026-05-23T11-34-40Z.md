status: completed
ticket: stoa--xyb.11
verdict: pass
design_artifact_audited: C:/Users/denso/claude_projects/the-stoa/.claude/worktrees/arc-49-build/agents/design/arc-49/design-rev1.md

# CAPTAIN_ARGUS verdict — Arc 6 (Arc 49) CAPTAIN_DAEDALUS.md cut (LAST relocation cut)

VERDICT: PASS

The design is lossless-complete, the cross-ref census is correct and complete, and the one genuinely-new install.sh mechanic (the no-DEST_DAEDALUS-var path-construction + ordering) is correctly specified with a probe that catches both error modes. No [BLOCKING] findings. No LOST-CANON drop. No broken cross-ref. The findings below are all [CONSIDER] — ADA-build notes, not design defects.

## Headline: cross-ref integrity — CLEAN (independently re-run)

I re-ran the census independently (backtick-tolerant grep across all of substrate/ md/sh/py excluding source+arcs; the validate-spec spec_refs.py resolver against SPECIFICATION.md live). Results MATCH the design exactly:

- External section-N cite set = {6 (top-level KEEP), 6.7, 6.8, 6.9, 6.9.3'', 6.11}. No cite the design missed.
  - 6.7: CAPTAIN_ADA.md:145/151, CAPTAIN_VERA.md:170, operating-disciplines.md:1045/1088 (x5)
  - 6.8: CAPTAIN_VERA.md:208 (x1)
  - 6.9: validate-spec check.sh:33, _check_runner.py:15/452, bw_tickets.py:14, drift_check.py:44, spec_refs.py:23 + spec_refs.py:55 (the bare form-c CAPTAIN_DAEDALUS section-6.9 WITHOUT .md) = x8. Heaviest external cite, as the design states.
  - 6.9.3'': CAPTAIN_VERA.md:212/217 (x2). The literal double-prime is present in source heading L377; design preserves it exactly in the stub (P-STUB escapes the apostrophes).
  - 6.11: CAPTAIN_ADA.md:183/186 (x2)
  - 6 top-level: modules/mechanical-inspection-split.md:47 resolves to the KEEP heading.
- All five cited-relocated subsections are marked for REAL-HEADING-LINE stub preservation. No spec-cited-or-substrate-cited relocated subsection is left unpreserved. NO [BLOCKING] cross-ref finding.
- 6.2.1' / 6.6 / 6.9.3' / 6.10 have ZERO external cites (independently confirmed) — uniformity stubs are sufficient.
- SPEC resolver: ZERO refs RESOLVE TO a DAEDALUS file. Baseline PASS=156 FAIL=56 confirmed live. The at-risk set is genuinely —PTY. P-SPEC-XREF as a standing guard is the right call.
  - Subtlety the design slightly understates but does not get wrong: SPEC L553 section-6.8 IS processed by the resolver — but via the bare-section (form-b) path, where it resolves to operating-disciplines.md (NOT DAEDALUS) and is ALREADY FAILing (part of baseline 56). The cut does not touch op-disc, so this ref cannot regress. The design claim is literally true (zero DAEDALUS-RESOLVING refs); the L553 6.8 bare-section is an already-failing op-disc-resolving ref, untouched. Net: no regression surface.

## Lossless-completeness (architect-seat) — VERIFIED

Walked the 633-line source heading-map against the ledger. Every heading line range is accounted for: KEEP-inline (1-5, 6.0-new, 6.1-6.5, 7-9, frontmatter+identity table) or RELOCATE-to-module (6.2.1', 6.6, 6.7, 6.8, 6.9 cluster, 6.10, 6.11). No silent gap. No dropped rule.

- 'OVENANCE: confirmed 0 standalone C-1 + 0 firm C-2. Empirical blocks are embedded INSIDE the disciplines that all relocate, so they travel WITH their discipline into the module (the module IS the surviving copy). Correct lossless home — Q2 CONCUR.
- All stoa-- ticketed empiricals resolve live (stoa--mn3, stoa--1lm, stoa--5sr, stoa--dxw, stoa--501, stoa--utn — all OK). C-1 losslessness holds.
- The 6.8 Anchor note (use stoa--5sr as the empirical home, NOT stoa--utn) is a correct, well-caught distinction. ADA should honor it.
- Check E (literal MODULE-INLINE full-lines in source bodies): independently confirmed ZERO in source. The cite-comment shapes that travel into modules are NOT MODULE-INLINE markers. Clean.

## The install.sh mechanic (top risk) — AUDITED HARDEST, CORRECT

Verified against live install.sh:

- Path construction DEST_DAEDALUS set to DEST_AGENTS_DIR / CAPTAIN_DAEDALUS + NAME_SUFFIX + .md mirrors the loop L1023 dest construction (name=DAEDALUS) EXACTLY. Both DEST_AGENTS_DIR and NAME_SUFFIX are in scope at the recompose region. Construction is CORRECT.
- DEST_DAEDALUS is NOT an existing variable anywhere in install.sh — no collision introduced.
- Ordering hazard is REAL and correctly diagnosed: the recompose block (if TARGET = subproject) spans L894-1010 and CLOSES at L1010; the CAPTAIN deploy loop is L1014-1037 (loop at L1021). So the recompose block currently runs BEFORE CAPTAINs deploy — the suffixed file does not exist when the L894 block runs. The four calls MUST move after L1037. Confirmed.
- The precedent DEST_* vars (POLYBIUS L818/821, PLINY L819/822, OPERATING_DISCIPLINES L866) are all set BEFORE the recompose block — DAEDALUS uniquely lacks one. The wrinkle is genuine and is the seat-specific reason this is not a literal copy of Arc-48.
- Option (i) (move only the four call lines + the 4 MODULES vars + DEST_DAEDALUS to a NEW if-subproject block after L1037, leave the function definition at L895) is sound: the function-defining block and the new call block both gate on TARGET=subproject, so the function is always defined before the calls run. Lower-risk than moving the deploy loop. CONCUR with option (i).
- P-RECOMPOSE-PATH catches BOTH error modes: static grep asserts DEST_DAEDALUS set to the suffixed path; the awk ordering check asserts call-after-loop. With P-RECOMPOSE (live: 7 markers re-inline non-empty bodies on a throwaway target) and P-RECOMPOSE-NEG (FAIL-LOUD), a mis-construction that silently no-ops is caught. P-RECOMPOSE-PATH is the right NEW probe — Q3 CONCUR.
- The 4-owner partition is correct: Check A tests the GLOBAL existence set (now 35 minus README), owner-agnostic (verified in function body L946-948); Checks B/D iterate the per-call owned set (L949-951). No function-body change needed. P-OWNERSHIP-NOCOLLIDE: independently confirmed all 7 DAEDALUS basenames disjoint from the 28 existing (0 collisions); credential-flow-design vs op-disc credential-discipline-detail are distinct.

## Adjudication of Q1-Q5

- Q1 (CAPTAIN to index-only, no routing map; standard for future CAPTAIN cuts): CONCUR, and YES it should be the standard. Grounded in modules/README section 4.1 (routing map = dispatch-time ORCHESTRATOR artifact) + section 1/4 (orchestrator core, NEVER a module). op-disc (Arc 47, non-orchestrator) is index-only and ARGUS CONCURRED there; deployed op-disc has a 0.5 Relocation index and no routing map. PLINY (Arc 48, orchestrator) carries BOTH, keyed on the BEAT. DAEDALUS is a non-orchestrator CAPTAIN (no Agent tool) — index-only is the correct, precedent-grounded shape. The design added nuance is right: DAEDALUS has exactly ONE reader (itself), and that reader IS the seat hitting every design-task-type trigger, so the per-stub Read pointer is the dispatch-time signal at point-of-need; this makes index-only CLEANER than op-disc, not weaker. All future CAPTAIN cuts (ARGUS/ADA/VERA/CATO/etc.) are non-orchestrators and should follow this shape.
- Q2 (move-with-verbatim for in-prose narrative anchors, not C-2 child): CONCUR. The in-prose stellation-Pass-10 anchors have no standalone bw ticket, but the discipline they anchor RELOCATES WHOLE into its module and the anchor travels INSIDE it verbatim — the module is the surviving copy. C-2 archive is for empiricals being DELETED from a KEEP section with no lossless home, or cross-repo ids not resolvable from this bw (Arc-48 ariadne--b93). Neither applies. A child ticket would duplicate, not preserve.
- Q3 (no-DEST_DAEDALUS path-construction + ordering = minimal-correct; P-RECOMPOSE-PATH the right probe): CONCUR on both, per the install.sh audit above. Constructing the path at/near the recompose call site (mirroring L1023) is correct and avoids special-casing one CAPTAIN in the generic deploy loop. A dedicated DEST_DAEDALUS deploy step would introduce asymmetry for no benefit.
- Q4 (6.2-extension-pointer repoint vs drop): CONCUR repoint-not-drop. Source L104-106 carries two cite-comments + one prose line pointing at 6.2.1' (canonical-code-block-fix) and 6.10 (ssot-with-why). These are always-relevant signposts (a reader at the always-on 6.2 weak-points gate benefits from knowing the two extensions exist + where to recover them) even though the detailed disciplines are conditional. Repoint at the module + 6.0 index row. The single SPLIT in the cut; P-SPLIT covers it.
- Q5 (ambient-vs-conditional for 6.6/6.7 [severity != frequency] + the 6.9 cluster): CONCUR all three RELOCATE. I read the source bodies: 6.6 opens When a brief involves credentialed operations against any third-party API or cloud service; 6.7 opens When designing a directive or spec that contains a 'INCIPAL-gating clause. Both explicitly design-task-type-gated — neither fires on a typical feature/refactor/debloat design. load-bearing in the headings = cost-when-it-fires, not read-frequency. Note: relocating DAEDALUS 6.6 does NOT weaken the always-on credential canon, which lives at operating-disciplines.md section 20 (a different file, unaffected by this cut); DAEDALUS 6.6 is the design-time APPLICATION read only on credentialed-ops briefs. The 6.9 probe-grounding cluster is correctly CONDITIONAL: the section-3-probes RULE is in always-on section 3; the GROUNDING DETAIL (anchor the regex / live round-trip / ground-check the tool surface) is read only when probes are regex/tool-shaped. Co-locating the three subsections in one probe-grounding.md is right (6.9.3'/6.9.3'' explicitly extend 6.9 clause 3; mirrors Arc-48 co-location precedent).

## The aggressive floor (~72 percent) — HONEST, not cut-too-deep

The ~150-200 floor estimate is the most aggressive ratio of the four cuts, but it is honest: section 6 is 511/633 lines (81 percent), and section 6 bulk is design-task-type-conditional (verified by reading the gate-opening sentence of each subsection). The always-on architect core is genuinely a tight contract (brief, restate, design, self-assess, verdict). The KEEP set holds every always-on architect rule — P-KEEP asserts each of the 13 by name; the load-bearing trio (3 design-output contract + 6.1 restatement gate + 6.2 weak-points gate) is guarded explicitly. P-FLOOR low-end guard (under 120 = suspicious) protects against an accidental KEEP drop. Sound.

## Buildability — sound

The 7-step build sequence is ordered correctly (modules first, then slim core, then install.sh, then probe sweep). The probe set (13 probes) is comprehensive and each probe is concretely executable. The throwaway-subproject discipline (op-disc section 25.5) is correctly applied for the recompose probes.

## audit_block

risks (all [CONSIDER] — build-execution notes, not design defects; zero [BLOCKING], zero [SHOULD-FIX]):

- r1 [CONSIDER] — quadrant_classification: easy-easy. The 6.9 stub must carry the literal double-prime in the 6.9.3'' heading EXACTLY (two ASCII apostrophes), or the VERA:212/217 cross-ref breaks. evidence: source L377; CAPTAIN_VERA.md:212/217. load_bearing: false (P-STUB escapes + checks the apostrophes; design weak-point 5 flags it). Surfaced so ADA does not normalize the primes to a single Unicode prime or drop one. If the literal double-prime is lost, this WOULD become [BLOCKING] at build — VERA must verify byte-exactly.

- r2 [CONSIDER] — quadrant_classification: easy-easy. The recompose FUNCTION DEFINITION stays at L895 inside the L894-1010 block under option (i); only the CALLS move after L1037. Sound (both blocks gate on TARGET=subproject), but ADA must NOT move the function definition out of / past its current block, and must keep the four calls together in ONE new if-subproject block. evidence: install.sh L894-1010 (function + block), L1014-1037 (CAPTAIN loop). load_bearing: false (P-RECOMPOSE/P-OWNERSHIP exercise the live path end-to-end; a broken move fails them loudly).

- r3 [CONSIDER] — quadrant_classification: easy-easy. The 6.8 module Anchor must be stoa--5sr (the wording-alignment empirical) NOT stoa--utn (a save-verdict-skill promotion, a shipped-in-arc cite for a different topic). evidence: design section 3.3.1; bw show stoa--utn (resolves but wrong topic). load_bearing: false (design already caught this and instructs ADA correctly; surfaced so VERA spot-checks the module Anchor line).

- r4 [CONSIDER] — quadrant_classification: easy-easy. Comment hygiene: install.sh L887-893 + L900-901 + L1000-1001 say THREE files / 28 modules / three owners. ADA must update them to FOUR files / 35 modules / four owners (design section 5 Step 4 names this but it is easy to skip — documentation-losslessness; a stale THREE-files comment misleads the next maintainer). evidence: install.sh L887-893, L900-908. load_bearing: false (comments do not affect runtime; flagged per fix-known-bugs-now discipline).

- r5 [CONSIDER] — quadrant_classification: hard-easy. SPEC L553 6.8 cite is human-readable but invisible to the spec resolver form-a (backtick-before-section miss; it IS picked up as a bare-section form-b that resolves to op-disc and already FAILs). 'E-EXISTING SPEC formatting nuance, NOT a regression this cut introduces — and the 6.8 heading title is preserved verbatim in canonical-template-alignment.md so the L553 prose stays accurate. Design correctly flags it out-of-scope for the capstone (.9) one-edit fix. evidence: SPEC L553; spec_refs.py form-a/form-b. load_bearing: false. Surfaced to confirm I independently verified it is pre-existing, not cut-introduced (cheap re-fetch: re-running spec_refs.py confirms L553 6.8 resolves to op-disc and was already failing).

non_findings:
- Did the design miss a cross-ref? Discharged: independent backtick-tolerant + form-c census surfaced the identical external-cite set; no missed cite.
- Is the path-construction wrong / could it silently no-op? Discharged: mirrors L1023 exactly, both vars in scope, P-RECOMPOSE-PATH + P-RECOMPOSE catch both error modes.
- Could DEST_DAEDALUS collide with an existing var? Discharged: grep confirms DEST_DAEDALUS absent from install.sh.
- Is the index-only call right for a CAPTAIN? Discharged: grounded in modules/README section 4 + the op-disc (Arc 47) non-orchestrator precedent ARGUS already concurred; PLINY (orchestrator) carries both, DAEDALUS (non-orchestrator) carries index only.
- Are the in-prose narrative anchors a C-2 case being silently dropped? Discharged: the discipline relocates whole and the anchor travels verbatim inside the module; the module is the surviving copy.
- Does relocating 6.6 weaken the always-on credential rule? Discharged: the universal credential canon is op-disc section 20 (different file, untouched); DAEDALUS 6.6 is the design-time application, conditionally read.
- Is there a literal MODULE-INLINE marker in a source body that would corrupt recompose (Check E)? Discharged: zero in source.
- Do the empirical ticket ids resolve (C-1)? Discharged: all 6 spot-checked resolve.
- Is the floor cut-too-deep, dropping an always-on rule? Discharged: section 6 is 81 percent of the file and conditional; P-KEEP asserts all 13 always-on rules by name; P-FLOOR guards the low end.

summary: The design is a clean, precedent-grounded application of the proven 3-bucket relocation method + multi-owner subproject-recompose to the architect role file — the 4th and last relocation cut. Shape as I read it: KEEP the always-on architect core (frontmatter, 1-5, the 6.1/6.2 gates + 6.3/6.4/6.5 reflexes, 7-9) + a new 6.0 relocation-index (NO routing map, correctly, since DAEDALUS is a non-orchestrator CAPTAIN); RELOCATE section 6 nine design-task-type-conditional subsections to 7 disk modules (the 6.9 cluster co-located); preserve all section numbers via stubs, with real-heading-line stubs for the five substrate-cited subsections. The most important thing I audited — and the design own top risk — is the no-DEST_DAEDALUS-var install.sh mechanic; I verified the path-construction mirrors L1023 exactly, the ordering hazard (recompose block closes at L1010 before the CAPTAIN loop at L1014-1037) is real and correctly fixed by moving the four calls after L1037, DEST_DAEDALUS does not collide, and P-RECOMPOSE-PATH catches both error modes. Cross-ref integrity is clean: my independent census matches the design exactly, no spec-cited-or-substrate-cited relocated subsection is left unpreserved, and the spec resolver has zero DAEDALUS-resolving refs (baseline 156/56 confirmed live). Lossless-completeness holds: every source line range is homed, 0 C-1 / 0 C-2 / 0 dropped rules, all empirical tickets resolve. Overall posture: CLEAN — PASS, ready for ADA build. The five findings are all [CONSIDER] build-execution notes — none blocks the build.
