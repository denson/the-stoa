status: completed
ticket: stoa--xyb.11
verdict: revise
diff_reviewed: main..arc-49/build (commit 854f89d), excluding agents/ — substrate/CAPTAIN_DAEDALUS.md (633->229), substrate/install.sh (+60), 7 new substrate/modules/*.md
design_artifact_compared_against: agents/design/arc-49/design-rev1.md (+ ARGUS PASS verdict agents/verdicts/stoa--xyb.11/ARGUS-2026-05-23T11-34-40Z.md)

concerns:
- id: c1
  category: security
  description: The new DAEDALUS recompose call in install.sh §3b is gated only on TARGET=subproject, NOT on WITH_CAPTAINS — but CAPTAIN_DAEDALUS deploys only when WITH_CAPTAINS=1, so `install.sh --target subproject --no-captains` now aborts (exit 2 "recompose: role file not found: CAPTAIN_DAEDALUS_<sub>.md") AND leaves a partial deploy (the 3 MAJOR/op-disc files deployed+recomposed before the abort). This is a commit-introduced regression: the same flag combo succeeded at main (main's recompose calls targeted only POLYBIUS/op-disc/PLINY, all deployed regardless of --no-captains).
  evidence: substrate/install.sh L1048 (`if [ "$TARGET" = "subproject" ]`) wraps L1062 (`recompose_module_inline "$DEST_DAEDALUS" ...`) with no WITH_CAPTAINS guard; CAPTAIN loop L1015 is gated `if [ "$WITH_CAPTAINS" -eq 1 ]`; recompose fn L934 `[ -f "$_role_file" ] || err ...` -> err() L227-230 exit 2. EMPIRICALLY REPRODUCED (§6.8): `bash install.sh --target subproject --parent-dir <tmp> --subproject probeN --no-captains` -> exit code 2, MAJORs left on disk, agents/ never created. Independently reproduced by ZENO (same throwaway result). Baseline confirmed: `git show main:substrate/install.sh` has zero DAEDALUS recompose call.
  severity: blocking
  quadrant_classification: easy-easy
- id: c2
  category: craft
  description: The §6.0 relocation-index prose (2 paragraphs, L84+L86) is slightly verbose for an index header — but it encodes the load-bearing index-vs-routing-map rationale + the subproject-recompose mechanism a future CAPTAIN cut needs to reuse. Not a defect; flagged only as a candidate the .9 prose-compression capstone could tighten (it is the right deferral target, not an in-cut miss).
  evidence: substrate/CAPTAIN_DAEDALUS.md L84, L86 (the two explanatory paragraphs above the 8-row index table)
  severity: minor

follow_ups:
- The c1 fix is a one-line guard. Two clean shapes for ADA on the next dispatch (CATO does not prescribe): (a) gate the DAEDALUS recompose call on `[ "$WITH_CAPTAINS" -eq 1 ]` inside §3b; or (b) make the recompose fn skip-with-warn (not err) when the role file is absent. Shape (a) is narrower (matches the deploy gate exactly) and preserves the err-on-genuinely-missing-file safety for the always-deployed files. Out-of-scope for THIS diff only because it is the catch this gauntlet exists to surface; belongs in the immediate revise dispatch, not a later arc.
- §6.5 heartbeat (~19L) + §7 verdict-format (~29L) are the two largest always-on inline blocks and the natural prose-compression targets for the .9 capstone. Correctly KEPT verbatim here (P-KEEP); noted only so the capstone has the candidate list.

verifier_coverage_assessment: VERA's coverage of the design's NAMED weak points is strong and independently corroborated: exhaustive losslessness (all 384 relocated §6.x body lines present in the 7-module union, zero lost), r1 double-prime byte-exact in BOTH slim stub + module + survives into the recomposed file (od 27 27), the 4-owner LIVE recompose against a throwaway returning 770 lines (DAEDALUS-path NOT no-op'd — the top LOST-CANON risk falsified), other-3-owner non-regression, P-RECOMPOSE-NEG fail-loud. I independently re-verified the move-with-verbatim claim (§6.9/§6.9.3'/§6.9.3'' bodies diff to ZERO content delta vs source-at-main; only added trailing blank lines) and the happy-path + project-tier deploys (project tier = slim 229 with inert markers; full subproject = 770 recomposed). COVERAGE GAP (the §6.4 meta-verify catch): VERA's 4-owner recompose exercised only the happy path (WITH_CAPTAINS on). Neither VERA nor ADA exercised `--no-captains --target subproject` — which is exactly the path the c1 regression lives in. ZENO reproduced the failure but explicitly routed the correctness call to CATO's lane. Recommendation for the redundant pair going forward: VERA's recompose probe set should include the --no-captains flag combination (or note it as out-of-coverage), since the CAPTAIN-file recompose calls are now flag-conditional in a way the MAJOR/op-disc calls never were. This is a coverage-set gap, not a VERA error — VERA's verdict will correctly PASS the spec-as-written; the gap is that the spec-as-written has a flag-interaction defect the happy-path probe cannot see.

summary: A clean, well-executed relocation cut marred by one commit-introduced install.sh regression. The losslessness is excellent: 7 modules are byte-verbatim relocations (zero content lost, double-prime preserved byte-exact, Check E clean, zero author-like fields), the §6.0 shape is correctly a relocation-INDEX not a routing-map (right call for a non-orchestrator CAPTAIN), scope is exactly the 9 named files, Author=PRINCIPAL with the correct ADA seat trailer, r3 (stoa--5sr anchor) and r4 (THREE->FOUR / 28->35 comments) landed. The 229-floor is HONEST: no KEEP rule was cut to hit a number (all 13 always-on rules inline), and the gap above the ~150-200 estimate is legitimately the richer §6.0 index + the full §7 verdict block + the always-on §6.5 heartbeat — all correctly KEPT, with the real prose-compression candidates correctly DEFERRED to the .9 capstone rather than missed in-cut. The one blocking concern (c1): the DAEDALUS recompose call is gated on TARGET=subproject but not WITH_CAPTAINS, so `--target subproject --no-captains` regresses from clean (at main) to abort-with-partial-deploy (after this commit), empirically reproduced and independently confirmed by ZENO. The fix is one line; per the no-deferral discipline it belongs in the immediate revise dispatch. Overall posture: revise — strong cut, one trivially-fixable deploy regression.
