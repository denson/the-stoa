status: completed
ticket: stoa--04n
verdict: pass
design_artifact_verified_against: agents/design/stoa--04n/design-rev2.md
build_verified: arc-53/build @ d93548a11b1b8c3ecfa1b3965f9404abf267ab6b (worktree .claude/worktrees/arc-53-build)

probes_executed:
- probe_id: P0
  description: verbatim promotion — SKILL.md tracked + sha256 byte-identical (no re-author)
  quadrant_classification: easy-easy
  command_or_method: git ls-files substrate/skills/workflow-composer/SKILL.md ; sha256sum same
  expected: path returned (tracked); sha256 = 03de64e5...073af
  observed: tracked; sha256 = 03de64e5b4eb63503723741f1bdcec0a8e7d09da5f634971c9307b82f4e073af
  result: pass
- probe_id: P1
  description: workflow-composer wired inside SKILL_NAMES block
  quadrant_classification: easy-easy
  command_or_method: grep -n 'workflow-composer' substrate/install.sh + sed 195-212p
  expected: exactly 1 hit between lines 198 and 208
  observed: single hit at line 207, inside SKILL_NAMES=( @198 ... ) @208
  result: pass
- probe_id: P2
  description: array-parse returns 9 names including workflow-composer
  quadrant_classification: easy-easy
  command_or_method: awk anchored on ^SKILL_NAMES=\( extracting entries; wc -l
  expected: 9 names, incl workflow-composer
  observed: 9 names; workflow-composer present
  result: pass
- probe_id: P3
  description: dry-run deploy into a TRUE throwaway clone lists workflow-composer; exit 0
  quadrant_classification: easy-easy
  command_or_method: fresh git clone --no-local of arc-53/build to /tmp; install.sh --target project --dry-run
  expected: dry-run lists workflow-composer skill deploy; exit 0
  observed: "9 skills"; explicit "[dry-run] deploy skill: .../workflow-composer/"; EXIT=0
  result: pass
- probe_id: P4
  description: real deploy into throwaway target lands SKILL.md byte-identical (LF-normalized)
  quadrant_classification: easy-easy
  command_or_method: install.sh --target project (real) into /tmp target; LF-normalized sha compare
  expected: deployed file exists; LF-sha == source LF-sha
  observed: deployed; both = 03de64e5...073af; MATCH; EXIT=0
  result: pass
- probe_id: P5
  description: drift — workspace missing only this skill reports it MISSING (+)
  quadrant_classification: easy-easy
  command_or_method: copy full install, rm workflow-composer dir; check.sh --workspace --registry
  expected: workflow-composer reported MISSING (+)
  observed: ".claude/skills/workflow-composer/SKILL.md (new in source)" under MISSING
  result: pass
- probe_id: P6
  description: no false OBSOLETE — deployed skill classified substrate-derived
  quadrant_classification: easy-easy
  command_or_method: check.sh on workspace WITH skill deployed
  expected: workflow-composer NOT in OBSOLETE block
  observed: workspace = CURRENT (0 drifted, 0 missing, 0 obsolete); no workflow-composer OBSOLETE line
  result: pass
- probe_id: P7
  description: zero survivors of stale install.sh:140 cite (zero-match = PASS per carry-in 1)
  quadrant_classification: easy-easy
  command_or_method: grep -c 'install\.sh:140' check.sh || true
  expected: count 0
  observed: count = 0
  result: pass
- probe_id: P8
  description: 5 corrected install.sh:198 cites at 76/228/236/385/435; line-236 bare form
  quadrant_classification: easy-easy
  command_or_method: grep -n 'install\.sh:198' check.sh ; per-line text inspect
  expected: 5 hits; 236 reads "install.sh:198 region", other 4 read "install.sh:198-208"
  observed: 5 hits at 76/228/236/385/435; 236="install.sh:198 region", 76/228/385/435="install.sh:198-208"
  result: pass
- probe_id: P9
  description: currency delta 1 — four-primitive (agent team) framing
  quadrant_classification: easy-easy
  command_or_method: grep -in 'agent team' SKILL.md
  expected: hits at L30 + L34
  observed: hits at L30 + L34
  result: pass
- probe_id: P10
  description: currency delta 2 — ultracode keyword
  quadrant_classification: easy-easy
  command_or_method: grep -n 'ultracode' SKILL.md
  expected: hit at L159
  observed: hit at L159 ("invocation keyword is ultracode ... workflow applied before v2.1.160")
  result: pass
- probe_id: P11
  description: currency delta 3 — args parameterization section
  quadrant_classification: easy-easy
  command_or_method: grep -n 'Parameterizing a saved workflow with .args.' SKILL.md
  expected: hit at L146
  observed: hit at L146
  result: pass
- probe_id: P12
  description: currency delta 4 — allowlist / stoa--x4j stall-fix
  quadrant_classification: easy-easy
  command_or_method: grep -in 'allowlist' ; grep -n 'stoa--x4j' SKILL.md
  expected: allowlist L156; stoa--x4j at L156 + L198
  observed: allowlist L156; stoa--x4j at L156 + L198
  result: pass
- probe_id: P13
  description: currency delta 5 — overnight != workflow (remote routine)
  quadrant_classification: easy-easy
  command_or_method: grep -in 'remote routine' SKILL.md
  expected: hits at L158 + L185
  observed: hits at L158 + L185
  result: pass
- probe_id: P14
  description: stale 'workflow'-as-live-keyword framing GONE (zero-match treatment per carry-in 1)
  quadrant_classification: easy-easy
  command_or_method: grep -n 'keyword is .*workflow' SKILL.md ; grep 'invocation keyword'
  expected: only L159 survives (historical "applied before v2.1.160")
  observed: only L159 matched; it is the historical mention. No line presents workflow as the live keyword.
  result: pass
- probe_id: P15
  description: gen-data frontmatter guard — run, assert exit 0 + workflow-composer record, then restore
  quadrant_classification: easy-easy
  command_or_method: npm run gen-data in app/ ; grep generated agents.ts ; git restore agents.ts
  expected: exit 0; workflow-composer record present; worktree restored clean
  observed: EXIT=0; workflow-composer record present (Zod-valid, 2 hits); agents.ts restored; tree clean
  result: pass
- probe_id: P16
  description: authorship — author: Denson Smith at SKILL.md:7; no foreign author-like field in diff
  quadrant_classification: easy-easy
  command_or_method: grep -n 'author:' SKILL.md ; git show d93548a field-shaped author-key grep
  expected: author: Denson Smith at L7; only field-shaped author add is Denson Smith
  observed: author: Denson Smith @L7; sole field-shaped author addition = "author: Denson Smith" (diff-line 226). No foreign field.
  result: pass
- probe_id: P17
  description: no-WORKFLOW scope guard (zero-match treatment per carry-in 1)
  quadrant_classification: easy-easy
  command_or_method: grep -c 'WORKFLOW_NAMES' install.sh || true ; grep -ic 'workflows/' check.sh || true
  expected: both 0
  observed: WORKFLOW_NAMES=0; workflows/=0
  result: pass
- probe_id: P18
  description: out-of-scope cites (install.sh:60-69, :766-770) untouched vs main
  quadrant_classification: easy-easy
  command_or_method: grep current worktree vs git show main:check.sh
  expected: identical to main (not edited)
  observed: worktree 462/858/1013 byte-identical to main; not edited
  result: pass
- probe_id: P19
  description: diff scope (Option C) — exactly 4 paths; agents.ts ABSENT
  quadrant_classification: easy-easy
  command_or_method: git diff --stat/--name-only main...d93548a ; grep agents.ts
  expected: exactly 4 paths (SKILL.md new, install.sh, check.sh, design-rev2.md); 0 agents.ts
  observed: 4 paths, +326/-5; agents.ts count = 0 (correct Option-C state)
  result: pass

methodology_concerns: |
  None of consequence. Two notes for the record:
  (1) The design's P19 text mentions "plus any app/ generated-data file gen-data writes" — that clause is N/A under Option C and the brief carry-in 3 confirms agents.ts ABSENCE is the correct state; verified as such (not a defect). The agents.ts staleness (1/9 -> 9/9 resync) is deferred to follow-up stoa--g38, not an Arc 53 verification failure.
  (2) P5's throwaway-install run also flagged the slug-renamed CAPTAIN agent files as MISSING — this is the install-time slug-rename-on-deploy behavior of a fresh synthetic target, not relevant to the workflow-composer probe; the load-bearing assertion (workflow-composer correctly classified MISSING when absent) held.

falsifying_evidence_summary: ""

verification_artifacts_path: agents/verification/stoa--04n/ (probe commands + recorded outputs captured inline in the dispatch transcript; throwaway clone built+verified+removed under /tmp/arc53-throwaway)

summary: |
  All 20 falsification probes (P0-P19) executed against committed d93548a; every one PASSES. The build was exercised mechanically (grep/sha/awk against the committed tree), via a live gen-data run-and-restore (P15), and via a TRUE throwaway clone of the arc-53/build branch into /tmp for the install + drift probes (P3-P6) per op-disc §25.5 — never against a real workspace, and the worktree was never modified (independence-of-verification preserved; tree left clean at d93548a). The load-bearing catches all landed correctly: (a) the highest-risk WP1 line-236 dual-trap — cite 236 reads the bare "install.sh:198 region" form, NOT "install.sh:198-208 region", and the other four read the range form; (b) verbatim promotion — the now-tracked SKILL.md is byte-identical (sha 03de64e5...073af) with no re-author, confirmed both in-tree and as deployed to the throwaway target; (c) all 5 currency deltas present at the design-claimed lines with the only surviving 'workflow'-keyword mention being the correct historical L159; (d) authorship Denson Smith intact with no foreign author-like field introduced; (e) Option-C diff scope is exactly the 4 paths with agents.ts correctly absent. No falsifying evidence found. Overall verdict: PASS.
