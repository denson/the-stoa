status: completed
ticket: stoa--k48
verdict: pass
design_artifact_verified_against: agents/design/stoa--k48/design-rev2.md (DoD->probe map §8 + §12.B threat-coverage)
build_verified: arc-k48/build worktree (uncommitted build) @ C:/Users/denso/claude_projects/the-stoa/.claude/worktrees/arc-k48-build
probes_executed:
- probe_id: p1
  description: Re-run the full builder-deploy-core bespoke suite independently (DoD#9 bespoke half; ADA claimed 105).
  command_or_method: python -m pytest agents/builder-deploy-core/tests/ -q
  quadrant_classification: easy-easy (mechanical run; pass/fail from output)
  expected: all green; 105 passed
  observed: 105 passed in 22.15s (re-run independently; matches ADA). 32 of those are the new provision+install-smoke probes named in design §8.
  result: pass
- probe_id: p2
  description: TRIPWIRE W1 — no I/O imports anywhere under provision/ (verbatim §5/§15.1 grep over whole tree incl checklist.py+tripwire.py) + AST test agrees.
  command_or_method: grep -rnE '\b(subprocess|socket|urllib|requests|httpx|gcloud|railway|os\.system|popen)\b' builder_deploy_core/provision/  (verbatim, from agents/builder-deploy-core/) + pytest test_tripwire_no_infra_imports + test_w1_attestation_grep_clean
  quadrant_classification: easy-easy
  expected: grep EMPTY (exit 1) over whole tree; AST test green; grep==AST
  observed: grep exit 1, NO output (all 9 provision/*.py scanned incl checklist.py+tripwire.py). Canonical grep WITHOUT --include (would catch a stale .pyc) also clean. test_tripwire_no_infra_imports + test_w1_attestation_grep_clean PASS. AST and grep AGREE (both clean). Methodology note: the §8.6 CI guard runs the grep with --include=*.py (excludes __pycache__) while the canonical VERA grep has no --include; both returned empty here, so no divergence.
  result: pass
- probe_id: p3
  description: TRIPWIRE W2 — only MockProvisioner subclasses Provisioner; Choreographer requires injected port (no default); mock instantiable (every @abstractmethod incl select_base_image).
  command_or_method: python -c import + inspect; Provisioner.__subclasses__(); Choreographer(None); inspect.signature(Choreographer.__init__); MockProvisioner() + pytest test_tripwire_only_mock_port
  quadrant_classification: easy-easy
  expected: __subclasses__ == [MockProvisioner]; no default; Choreographer(None) raises TypeError; mock instantiable
  observed: __subclasses__()==[MockProvisioner] True; provisioner param has NO default; Choreographer(None) raises TypeError ("Choreographer requires a Provisioner (no default) — W2"); MockProvisioner() instantiable; has select_base_image. test_tripwire_only_mock_port PASS.
  result: pass
- probe_id: p4
  description: TRIPWIRE W3 — emit_spec takes NO provisioner param (pure transform).
  command_or_method: inspect.signature(emit_spec).parameters + pytest test_tripwire_emit_is_pure
  quadrant_classification: easy-easy
  expected: params == [resolved, builder_slug]; no provisioner
  observed: emit_spec params == ['resolved','builder_slug']; provisioner absent. test_tripwire_emit_is_pure PASS.
  result: pass
- probe_id: p5
  description: STRICT §7 boundary (DoD#5) — independently grep the emitted T1 SHAPE surface for /run + collect/embed/ingest/search + builder names; confirm only health + <VERB> placeholder allowed; confirm build's own boundary test enforces.
  command_or_method: emit stand_up_mesh_shape('prospector'); scan reusable template surface for LEAK_TOKENS + adversarial grep of all provision/ source for builder names / '/run' + pytest test_s7_boundary_no_domain_verb_leak
  quadrant_classification: easy-easy
  expected: zero leak tokens in the GENERIC template surface; only health route; <VERB> placeholder
  observed: reusable-template surface clean of all 8 LEAK_TOKENS; health route present; <VERB> placeholder present; test_s7_boundary_no_domain_verb_leak PASS. The build's own test deliberately EXCLUDES builder_slug (the builder's own value-free identity) from the scanned surface with a documented rationale (the leak guarded is a domain verb / builder NAME baked into the GENERIC template) — defensible, matches design intent. ADVERSARIAL: 'scienceclaw' appears in provision/__main__.py:35 as a CLI dry-run repro slug->category map (design §2 repro surface), NOT in the T1 SHAPE; '/run' absent from all provision/ source. Cleared.
  result: pass
- probe_id: p6
  description: value-free emit (DoD#3) — assert_value_free walks spec+ledger+mock state with teeth; grep the 3 relay-up artifacts + emitted spec for any credential value.
  command_or_method: assert_value_free over spec/ledger/mock; adversarial inject non-name leaf (int) into StepAction.detail + populated set; grep relay-up for value-looking strings + pytest §8.3
  quadrant_classification: easy-easy
  expected: passes on legit; RAISES on a non-name leaf; no value in relay-up
  observed: assert_value_free passes on all 3 legit specs+ledger; RAISES ValueLeakError on an injected int leaf in action.detail AND in the mock populated set (structural teeth confirmed). relay-up value-grep (AIza/sk-/base64) EMPTY. §8.3 (emit_value_free x3 + ledger + mock_populated + raises) PASS. NOTE the guard is a STRUCTURAL/TYPE assertion (every leaf must be str/bool/enum; no value-pattern matching) — correct per design §4.2/HW-1; there are no values in the system to leak; the port takes/returns none.
  result: pass
- probe_id: p7
  description: 3 worked examples (DoD#1) — prospector/scienceclaw/labstat_bls emit byte-stable goldens; confirm 8/6/7 resolved-set entry counts.
  command_or_method: count resolve() entries per example + pytest test_emit_*_golden + test_emit_is_deterministic + test_labstat_skips_s1_for_thirdparty
  quadrant_classification: easy-easy
  expected: 8/6/7 entries; goldens green; deterministic
  observed: prospector 8, scienceclaw 6, labstat_bls 7 (match design-formal §8). All 3 goldens PASS; deterministic; labstat thirdparty_rest_key skips S1 PASS.
  result: pass
- probe_id: p8
  description: fail-closed + isolation + SHAPE (DoD#2/#4/#5) — S2c BLOCKS on unpopulated slot (no S3/S5); S0-abort materializes no key; idempotent re-run adds no create-or-get; per-secret accessor; sa_scope==derive_sa_scope; SHAPE stands up domain-empty mode-2.
  command_or_method: direct Choreographer runs (default mock / fail_at=S0:ensure_sa / populated / double-run) + pytest §8.2/§8.4/§8.5
  quadrant_classification: easy-easy
  expected: BLOCKED no-S3/S5; FAILED zero-S2; OK with S5; no create-or-get widen; per-secret accessor; sa_scope match
  observed: DEFAULT MOCK -> terminal BLOCKED, blocked_slots=(MAPS_API_KEY,POSTGRES_PASSWORD), steps {S0,S1,S2} only (S3/S5 ABSENT). S0-ABORT -> terminal FAILED, ZERO S2 actions (no key before the lock). MODE-2 -> terminal OK, stand_up_mesh_shape present. IDEMPOTENT re-run -> the only 3 created=True are the fixed-emit ops (set_budget_boundary/select_base_image/stand_up_mesh_shape); ZERO create-or-get verbs widened. All §8.2/§8.4/§8.5 PASS.
  result: pass
- probe_id: p9
  description: M3 threat-anchored probe (§8.12) — drop the paired gcp_secret, keep the key-bearing gcp_api; confirm BOTH attack-blocked AND legit-unaffected against the FROZEN function's ACTUAL return shape (tuple-pairs, built-to-ship-reality).
  command_or_method: pytest test_m3_runtime_completeness_drops_paired_secret + independent check_runtime_completeness(attack) and (complete)
  quadrant_classification: easy-hard reduced to easy-easy (the FROZEN function is deterministic; the attack path is a single bounded input — falsification is mechanical)
  expected: (a) ok=False with ((gcp_api,google-maps),(gcp_secret,MAPS_API_KEY)) in missing; (b) ok=True, missing==[]
  observed: (a) attack set -> ok=False, missing=[(('gcp_api','google-maps'),('gcp_secret','MAPS_API_KEY'))] (the enabled-but-keyless->401 attack is REPORTED). (b) complete set -> ok=True, missing=[]. Probe asserts against the REAL shipped tuple-pair shape (NOT design-rev2 §8.12 bare-name prose) — ADA's built-to-reality deviation is CORRECT against shipped frozen code. test PASS. This is the EXECUTED probe the M3 threat-coverage line cites.
  result: pass
- probe_id: p10
  description: jd5 fold (DoD#8) — data/ inside package; _DATA_ROOT=.parent/data; pyproject package-data data/**/*.toml + .provision in packages; test_dataload updated; real-venv install-then-import smoke proves data/ bundled.
  command_or_method: ls builder_deploy_core/data + grep _DATA_ROOT/pyproject/test_dataload + pytest test_install_smoke.py -rs
  quadrant_classification: easy-easy
  expected: data/ inside pkg; paths flipped; install-smoke green (not skipped)
  observed: builder_deploy_core/data present; _DATA_ROOT = Path(__file__).resolve().parent / "data"; pyproject builder_deploy_core=["data/**/*.toml"] + builder_deploy_core.provision in packages; test_dataload lines 61+84 -> .parent/data. test_install_then_import_smoke PASS in 21.5s with NO skip (-rs reported none) -> real venv install + import-from-outside-tree + load_baseline succeeded, data/ BUNDLED.
  result: pass
- probe_id: p11
  description: FULL existing the-stoa suite (DoD#9) — builder-deploy-core (p1) + gen-data + app, catching what the jd5 data/ move might break elsewhere.
  command_or_method: confirm diff isolated to agents/; npm run gen-data + determinism check; npm test (vitest) in app/
  quadrant_classification: easy-easy
  expected: all green; gen-data Zod-valid + deterministic; app suite green
  observed: build diff isolated to agents/builder-deploy-core + agents/design + agents/verdicts (substrate/ + app/ UNTOUCHED; worktree substrate byte-identical to main). gen-data; discovered 16 role files, wrote agents.ts, roster 4 MAJOR/12 CAPTAIN/8 LIEUTENANT (Zod schema valid), output DETERMINISTIC (no drift to generated agents.ts). app vitest; 3 files, 41 passed. builder-deploy-core 105 passed (p1). The 2.1/2.2 core + broader repo UNBROKEN.
  result: pass
- probe_id: p12
  description: frozen surface (DoD §0.3) + authorship (DoD#10) — resolve.py byte-unchanged vs main (blob-hash); no foreign-person author field in the diff.
  command_or_method: git hash-object resolve.py vs git rev-parse main:...resolve.py; git diff --stat main -- resolution/suggest/discovery; grep author-like fields across the diff
  quadrant_classification: easy-easy
  expected: resolve.py blob identical; resolution/suggest/discovery zero diff; every author field = Denson Smith
  observed: resolve.py blob adade158... IDENTICAL main-HEAD vs worktree (so resolve/derive_sa_scope/check_runtime_completeness byte-unchanged); resolution/+suggest/+discovery zero diff. Authorship; every new file (11 source + 3 relay-up) author=Denson Smith; pyproject authors=[{name="Denson Smith"}] untouched (jd5 changed only packages + package-data + a pytest marker); NO foreign author field anywhere.
  result: pass
threat_coverage:
- mitigation: M1
  threat: T-cross-builder-lateral-movement (per-builder isolation)
  defeats_via_probe: p8
  probe_evidence: agents/verdicts/stoa--k48/VERA-20260627T054115Z.md p8 observed (test_isolation_per_secret_accessor + test_sa_scope_is_derive_sa_scope, §8.4) — re-run green in p1/p8
  attack_path_exercised: over-scoped/cross-builder grant — (a) every grant_secret_accessor names exactly one secret, no project wildcard, sa_scope==derive_sa_scope(resolved) (no extra grant); (b) the builder's OWN resolved secrets still get per-secret accessors (isolation didn't break the feature).
- mitigation: M2
  threat: T-credential-value-in-transcript (agents-never-hold-values)
  defeats_via_probe: p6
  probe_evidence: agents/verdicts/stoa--k48/VERA-20260627T054115Z.md p6 observed (assert_value_free RAISES on injected int leaf in detail + populated set) + p2/p3/p4 (the 3-wall TRIPWIRE)
  attack_path_exercised: inject a value into the surface — (a) assert_value_free RAISES on any non-name leaf; the port takes/returns no value; the W1 grep is empty AND agrees with the AST test; real applier structurally unreachable on the test path; (b) a legitimate value-free emit + mock walk PASSES.
- mitigation: M3
  threat: T-enabled-but-keyless-401 (runtime-completeness)
  defeats_via_probe: p9
  probe_evidence: agents/verdicts/stoa--k48/VERA-20260627T054115Z.md p9 observed (test_m3_runtime_completeness_drops_paired_secret, §8.12) — independently re-run against frozen check_runtime_completeness
  attack_path_exercised: drop the paired gcp_secret MAPS_API_KEY while keeping the key-bearing gcp_api google-maps — (a) check_runtime_completeness(attack) -> ok=False with (('gcp_api','google-maps'),('gcp_secret','MAPS_API_KEY')) in missing (the 401 attack REPORTED, not shipped); (b) complete set -> ok=True, missing==[] (legit paired builder unaffected).
- mitigation: M6
  threat: T-unauthorized-mesh-access (agent-access mesh security)
  defeats_via_probe: p5
  probe_evidence: agents/verdicts/stoa--k48/VERA-20260627T054115Z.md p5 observed (test_s7_boundary_no_domain_verb_leak + test_s5_shape_stands_up, §8.5)
  attack_path_exercised: domain-verb leak / open mesh — (a) deny-by-default + Tailscale-User-Login header-trust (no WhoIs-on-loopback) + Funnel OFF SHAPE present; a domain-verb leak trips the strict grep (none present); (b) generic health route + <VERB> placeholder stand up (the SHAPE works).
methodology_concerns:
- DESIGN-PROSE-vs-BUILD-REALITY (benign, design-canon audit item); design-rev2 §8.12/§4.5 prose names check_runtime_completeness 'missing' as bare (api,secret) names, but the FROZEN shipped function returns list[((api_kind,api_name),(secret_kind,secret_name))] tuple-pairs. ADA correctly built the M3 probe + S6 to the SHIPPED shape (frozen code is canon). The DESIGN PROSE should be corrected at the close design-canon audit (already flagged by PLINY). NOT a build defect — the probe is correct against shipped reality.
- DESIGN-DoD-PROSE overstatement (benign); DoD#2 / §8.2 prose says the idempotent re-run adds 'ZERO created=True actions (all SKIPPED/created=False)'. The build's idempotency model leaves the FIXED-EMIT ops (set_budget_boundary/select_base_image/stand_up_mesh_shape) created=True on re-run (they are not create-or-get resources). The build's test correctly scopes the no-widen assertion to the create-or-get verbs (which ARE all SKIPPED). The load-bearing property (no resource widens on re-run) HOLDS; the design prose slightly overstates. Design-canon audit nit, not a defect.
- W1 grep-clean CI guard (§8.6 test_w1_attestation_grep_clean) runs grep with --include=*.py (excludes __pycache__) while the canonical §5/§15.1 VERA grep has no --include. Both returned EMPTY here (I ran the canonical bare grep independently, also clean), so AST==grep holds. Minor; a future stale .pyc carrying a forbidden token would be caught by the canonical VERA grep but NOT by the CI guard. Non-load-bearing (source is the contract; .pyc is a build artifact).
- Choreographer._failed_action op='fail' is outside the canonical §3.1 op vocabulary (which is the ABC method names + engine 'verify'). Value-free and benign (a terminal failure marker, not a port verb). PLINY pre-flagged for CATO to rule. Surfaced here as a vocabulary-consistency nit; not a value/TRIPWIRE concern.
falsifying_evidence_summary:
verification_artifacts_path: agents/verdicts/stoa--k48/ (this verdict; all probes are re-runnable; pytest agents/builder-deploy-core/tests/, the verbatim §5 W1 grep, the python import probes, npm run gen-data + npm test in app/)
summary: The build realizes design-rev2 across all 10 DoD items, verified by 12 probe-groups run adversarially against the uncommitted arc-k48/build worktree (mock/grep/pytest ONLY — TRIPWIRE held, no real infra/creds/money touched). The most load-bearing elements all hold; (1) the 3-wall structural TRIPWIRE — W1 grep EMPTY over the whole provision/ tree (incl checklist.py+tripwire.py via the fragment-SSoT) AND agreeing with the authoritative AST test; W2 __subclasses__()==[MockProvisioner] + no-default port + mock-instantiable; W3 emit_spec has no provisioner; (2) fail-closed verified DIRECTLY not just by the test — default mock BLOCKS at S2c with no S3/S5, S0-abort materializes ZERO S2 keys; (3) the M3 threat-anchored probe drives the real enabled-but-keyless attack path against the FROZEN check_runtime_completeness and confirms BOTH attack-blocked (ok=False, the missing pairing named) and legit-unaffected (ok=True, missing==[]), asserted against the REAL shipped tuple-pair shape; (4) value-free emit has structural teeth (assert_value_free RAISES on an injected non-name leaf in detail + mock state); (5) the jd5 fold's real-venv install-then-import smoke PASSES (not skipped) proving data/ is bundled; (6) the FULL repo suite is green (builder-deploy-core 105 + gen-data deterministic/Zod-valid + app vitest 41) with the diff isolated to agents/, so the jd5 data/ move broke nothing; (7) frozen surface byte-unchanged (resolve.py blob adade158 identical); (8) authorship clean. The 4 methodology_concerns are benign design-prose-vs-shipped-reality / vocabulary nits for the close design-canon audit (M3 return-shape prose, idempotency 'ZERO created' overstatement, the CI grep --include delta, the 'fail' op vocab) — none falsify a DoD claim; ADA's built-to-shipped-reality deviation on M3 is CORRECT (frozen code is canon). Verdict; PASS.
