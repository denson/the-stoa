status: completed
ticket: stoa--h2z
verdict: revise
design_artifact_audited: agents/design/stoa--h2z/design.md
audit_block:
  risks:
  - id: r1
    description: The central named-dependency premise that the workflow-composer is on-disk-and-UNTRACKED / forge-promotion-pending and that the 04n promotion arc itself ends at design+HARD STOP not yet built is FACTUALLY STALE against committed HEAD; the SKILL is tracked AND deployed.
    evidence: "design.md header/sec1/sec2.5/sec5/sec6 HS-1 all assert untracked. Counter-evidence at committed HEAD: git log commit c6ae899 (Arc 53: forge-promote workflow-composer skill into substrate canon); git ls-files returns substrate/skills/workflow-composer/SKILL.md (tracked, clean); git show HEAD:substrate/install.sh line 218 = workflow-composer inside the SKILL_NAMES deploy array. The skill is BOTH committed AND install.sh-deployed at HEAD."
    load_bearing: true
  - id: r2
    description: The Tier-1/Tier-2 split rests its gating logic on the stale premise (r1); the Tier-2 dependency the design hangs on 04n forge-promotion landing is one 04n explicitly scoped OUT and was never going to deliver, while the genuine residual (no WORKFLOW_NAMES / no deployable defeat-threat script) is a DIFFERENT un-named dependency.
    evidence: "design.md sec2.5 + sec6 HS-1 gate Tier-2 on 04n forge-promotion landing. 04n design-rev2 sec2.5 OUT-of-scope: No WORKFLOW_NAMES array, no .claude/workflows/ deploy/drift case, Ship only the SKILL (locked decision 2). Confirmed at HEAD: grep WORKFLOW_NAMES / defeat-threat in substrate/install.sh = 0 hits. The composer SKILL (composition guidance) IS shipped; workflow-DEPLOY infra is not, and 04n is not the arc that delivers it."
    load_bearing: true
  - id: r3
    description: WP2 residual (goal-lock leans on seat-side greps for defeats_via_probe-in-probes_executed AND attack-path-exercised, not tool-strength) is honestly INHERITED from shipped Arc B not newly introduced, but the design states the anti-drift property as structurally-cannot-drift which is stronger than the inherited enforcement supports for the Tier-2 script form.
    evidence: "design.md sec2.3 goal-lock para + sec1 (structurally cannot drift) vs design.md WP2 + CAPTAIN_ARGUS.md sec6.9 item-4 closing note (exit-4 empty-binding is skill-enforced; defeats_via_probe-in-probes_executed is seat-side grep, NOT tool-strength) + CAPTAIN_VERA.md sec5.2. Residual correctly inherited; claim-wording overreaches the inherited mechanism."
    load_bearing: false
  - id: r4
    description: sec35.5 self-carve classification (not threat-ratified, process change, no runtime attack path) is CORRECT and is CONFIRMED here; recorded as a non-finding (the ARGUS-confirmed explicit classification IS the required record per sec35.5), surfaced only to make the A4 confirmation auditable.
    evidence: "design.md sec2.6 + HS-2. h2z ships canon markdown + a (Tier-2) orchestration script that runs no privileged operation, no credential flow, no PRINCIPAL-gate beyond spawn-authorization, no runtime-exploitable surface; same self-reference class as Arc A/B named verbatim in operating-disciplines.md sec35.5. No M<n> attaches to any change h2z makes. CONFIRMED, not a mapless-mitigation smell."
    load_bearing: false
  non_findings:
  - "Trigger predicate soundness (focus 1): SOUND. Reuses sec35.1 named-threat classification verbatim, imports no fuzzy criticality scale; T-a/T-b are the two already-shipped Arc-B failure shapes. Fuzziness does not re-enter: trigger fires on the binary sec35.1 fact (named-and-undefeated); sec35.1 ARGUS-confirmed ownership is what makes it non-self-exemptable. Verified against op-disc sec35.1/35.4/35.5 + the four Arc-B seat surfaces; all cited claims hold."
  - "Threat-to-mitigation map for THIS arc: correctly ABSENT. Arc is sec35.5-carved (no threat-ratified mitigation means no map required means no map-present-but-probe-absent smell can fire). sec6.9 smell correctly does NOT fire on a confirmed carve-out per sec6.9 item-3."
  - "Honest-claim boundary (focus 4): NO overclaim. Design repeatedly states threat-ENUMERATION completeness remains ARGUS unmechanized residual (sec1, WP1, sec5, HS-5, probe P9 enforces 0 hits on all-threats/every-threat-defeated/catches-unnamed). Claims named-threat-DEFEAT-as-locked-goal only."
  - "Anti-drift integrity (focus 5): structurally true for the Tier-1 / classic-PLINY-run form (goal IS the whole arc; no larger deliverable to sub-bullet under; real, tool-independent, closes MAJOR-1). For the Tier-2 SCRIPT form the property is goal-schema-enforced for empty-binding only; residual honestly INHERITED from Arc B (WP2), not newly introduced. See r3 for the non-load-bearing claim-wording overreach."
  - "Arc-thrash / batching (focus 6 / WP3 / HS-4): correctly OUT of scope, NOT load-bearing. A security bug IS the load-bearing human-attention moment; one-stop-per-threat is the right N=0 default; args-parameterization makes each remediation cheap to RUN. Batching is a correct candidate-follow-up. I decline to promote it."
  - "WP4 sec36-vs-sec35 smear: not load-bearing. New sec36 citing sec35 as detection source keeps coverage-within-arc distinguishable from escalation-into-a-dedicated-arc. HS-3 correctly routes canon-home to ratification."
  - "Authorship (sec8): no wrong author field. design.md + workflow-composer SKILL.md + 04n design-rev2 all name Denson Smith. Clean."
  threat_coverage_assessment: "Design-time probe-SPEC adequacy: PASS for a sec35.5-carved-out arc. h2z carries NO threat-ratified mitigation (carve-out CONFIRMED, r4), so NO threat-anchored probe is required per DAEDALUS sec6.13 / ARGUS sec6.9, and none is specd; correct. sec3 probes P1-P11 falsify the BUILD canon-presence/anchoring/honesty, NOT a runtime threat-defeat. threat_ratified_mitigation_count = 0. No map-present-but-probe-absent smell (no map exists to be probe-orphaned)."
summary: >
  h2z designs a RESPONSE layer: on detection of a sec35.1 named-threat surfaced-but-not-yet-defeated, STOP + surface to PRINCIPAL, then spawn a dedicated goal-locked defeat-threat workflow whose single success criterion is the B2 executed-probe assertion, so threat-defeat is the WHOLE goal of a dedicated arc and cannot drift the way the origindex-trw sub-goal bullet did. Core thesis sound: trigger reuses sec35.1 verbatim with no new criticality judgment, honest-claim boundary holds with no enumeration overclaim, anti-drift is real for the Tier-1/classic-PLINY form, and the sec35.5 self-carve is CONFIRMED. The single load-bearing problem is FACTUAL not design-logic: the entire 04n dependency framing (sec1/sec2.5/sec5/sec6 HS-1) asserts the workflow-composer is on-disk-but-UNTRACKED / forge-promotion-pending, but at committed HEAD the SKILL is tracked (Arc 53 c6ae899) AND wired into install.sh SKILL_NAMES (line 218); it is shipped. The stale premise propagates into the split (r2): the Tier-2 gate points at 04n forge-promotion landing, but 04n explicitly scoped OUT the workflow-DEPLOY infra (locked decision 2: ship only the SKILL), so the real Tier-2 residual (no WORKFLOW_NAMES / no deployable defeat-threat script) is an un-named DIFFERENT dependency. Tier-1 carries genuine standalone load-bearing value (anti-drift / MAJOR-1-closing property comes from the goal being the whole arc, PLINY-enforceable without any script), so the split CONCLUSION (ship Tier-1 decoupled now) survives, but its STATED RATIONALE rests on a wrong fact and must be corrected before the canon it produces inherits the error. Posture: structural design clean; revise to correct the dependency facts and re-derive the Tier boundary against true HEAD state. No proposed fix (not this seat).
