status: completed
ticket: stoa--elx
verdict: pass
diff_reviewed: b698f4ef (substrate/win_path.ps1 NEW, substrate/bootstrap-bw.sh NEW, substrate/install.sh, skills/install-stoa/SKILL.md)
design_artifact_compared_against: agents/design/stoa--elx/design-rev3.md
concerns:
- id: c1
  category: hygiene
  description: win_path.ps1 catch block (exit 5, fail_exception) does not Close the open registry handle; every other exit path Closes it.
  evidence: substrate/win_path.ps1:53 (catch) vs the $wk.Close() at :38/:44/:46/:48
  severity: minor
  quadrant_classification: easy-easy
  note: Cosmetic only. The powershell process exits immediately on the exception path, so the OS releases the handle; no lock persists and no mutation occurs (SetValue is never reached on the catch path). Byte-exact from the ARGUS-cold-audited design, so a fix touches the frozen crux; surface-only. A one-line finally { if ($wk) { $wk.Close() } } would close it if ADA revisits.
- id: c2
  category: hygiene
  description: -Broadcast switch param is an implemented no-op (comment-only body); the helper never passes it.
  evidence: substrate/win_path.ps1:51 (if ($Broadcast) { <# ... #> })
  severity: minor
  quadrant_classification: easy-easy
  note: Intentional stub, documented as best-effort and non-fatal because DC3 reads the registry directly. A param that does nothing if passed is a mild footgun, but nothing in-repo passes it. Byte-exact from design.
- id: c3
  category: hygiene
  description: win_obtain does not guard the mktemp -d result; a mktemp failure leaves tmp empty.
  evidence: substrate/bootstrap-bw.sh:289 (tmp="$(mktemp -d)" with no || fail)
  severity: minor
  quadrant_classification: easy-easy
  note: Low-probability and non-damaging. Every downstream rm -rf "$tmp" is quoted, so rm -rf on an empty var is a harmless no-op, not a wildcard delete. A trailing [ -d "$tmp" ] || fail would harden it.
- id: c4
  category: hygiene
  description: rc in win_ensure_on_path is captured to a non-local global (bare rc=$? not declared local).
  evidence: substrate/bootstrap-bw.sh:336
  severity: minor
  quadrant_classification: easy-easy
  note: rc is read on the immediately-following case, so no cross-call leak matters in practice. Matches the design N2 sketch verbatim (design-rev3:349 is also a bare rc=$?), so build-to-design fidelity is intact; this is a pre-existing design choice, not build drift.
follow_ups:
- Correct the design-rev3 DC2.0 empirical claim ("bw detected >= 0.13.2" on the reference machine) for the record: the machine actually carries bw 0.13.1 (below floor) at ~/bin. Belongs in a design-artifact-of-record correction, not this build diff. Reason for follow-up: the build handles below-floor correctly; only the frozen design narrative is stale. VERA already surfaced this as methodology_concern 1.
- Optionally file the c1 finally-close and c3 mktemp-guard as a trailing hygiene polish on the crux if ADA is re-dispatched; all four c1-c4 items are minor and shippable as-is.
verifier_coverage_assessment: >
  VERA PASS is SOUND; nothing hidden. The load-bearing crux was exercised on the SHIPPED artifacts against the throwaway key Environment_stoa_test (P2/P3) and a temp target dir (P5), never the real HKCU\Environment Path (proven byte-identical before/after, KIND=String LEN=800, sha256 0951f1e4...). Independent item-11 cross-check PASSES: all four threat_coverage defeats_via_probe ids (P5, P2, P4, P3) appear in the probes_executed set (P1-P10) with non-empty probe_evidence; the p1-p7 verification scripts are on disk. Tier-ii substance: each cited probe genuinely drove the named attack path (both attack-blocked AND legit-unaffected), not a relabeled happy path -- P5 flips a zip byte AND tampers checksums.txt (both abort fail-closed, no bw.exe placed) while the real v0.13.2 asset verifies; P2 seeds an absent key (fail_read exit3, no overwrite) plus REG_EXPAND_SZ/token/double-append cases; P3 forces composed>ceiling (fail_length exit4, no write) and confirms the wrapper routes 3/4/5 to fail_loud_path; P4 proves the reassign-from-registry check exits 1 on a git-bash-only stub while the naive check exits 0. Two honest probe-premise drifts do not falsify: (1) the machine is below-floor (bw 0.13.1), so the download-SKIP branch (HAVE_BINARY=1) is not naturally exercised -- covered adequately for an easy-easy branch by the version_ge unit test (0.13.1<0.13.2, 0.13.10>=0.13.2), a static branch trace, and the P6 dry-run HAVE_BINARY=1 skip; (2) the machine is now true-green (bw.cmd shim on the registry PATH), so P4 exercised the discrimination mechanism with a unique-named stub rather than literal bw -- acceptable because the shipped _ps_callable_probe (Get-Command bw, verbatim) WAS run against the real registry in P1/--check (returned YES), so combined coverage of the literal-bw path plus the mechanism is complete. No coverage gap rises to INCOMPLETE; no easy-hard or hard-hard finding is present.
summary: >
  Clean, tightly-scoped build that lands the 4-file slice (win_path.ps1 NEW, bootstrap-bw.sh NEW, install.sh flag, install-stoa reversal) with no drive-by edits. SECURITY READ of the download + PATH-mutation surface is POSITIVE: win_path.ps1 is byte-identical to the ARGUS-cold-audited design DC2 body -- fail-closed on read failure (key-absent exit3 no write; exception exit5 no write), provably-absent-vs-read-failure discrimination intact, type-and-token preserving SetValue (never setx, never %PATH% expansion -- the write value $newUser is the raw unexpanded string; ExpandEnvironmentVariables is used only for the idempotent-compare and the composed-length gate, never for the written value), EXPANDED-composed 4095 gate correct with no write on breach, -File invocation (no argv-mangling surface), ExecutionPolicy Bypass appropriately bounded to the single -File call. bootstrap-bw.sh SHA256 verify is genuinely FAIL-CLOSED: on mismatch (or multi-match, or wrong-hash checksums) it rm -rf the temp and exits BEFORE any Expand-Archive or cp, no bw.exe placed; no TOCTOU between verify and extract (same private mktemp -d file, no external writer); curl is -fsSL over HTTPS with no -k/--insecure. No set -e is a deliberate style -- every critical curl/Expand-Archive/cp path carries an explicit || { rm -rf "$tmp"; fail; } guard, and the win_ensure_on_path rc capture is pipe-free and ||-true-free per design N2. SCOPE confirmed: pre-flight sits after the L758 target check and before the L774 case dispatch (target-independent, exactly per rev3 N4); absent-flag --dry-run is byte-unchanged (VERA P7, all 3 targets); SKILL.md L246 bw-init separation PRESERVED; no touch to arc-19 / bw-upgrade / install-stoa location. AUTHORSHIP clean: Author=denson <densonsmith2@gmail.com> (git-configured, not overridden), both new files carry "# Author: Denson Smith", the §28.9 CAPTAIN_ADA seat trailer is present, and the only author-word hit in SKILL.md is the "agent-author" skill-name reference (not an authorship field). The DC2.0 stale-machine-state note is a design-narrative inaccuracy only -- the shipped version_ge/HAVE_BINARY logic correctly does NOT skip on below-floor 0.13.1. Four minor hygiene NITs (c1-c4), all shippable. POSTURE: clean / minor nits only / no blocking concern.