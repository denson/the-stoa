status: completed
ticket: stoa--xyb.7
verdict: pass
diff_reviewed: main..arc-50/build @ 6de84aa (excluding agents/) - 8 substrate files: CAPTAIN_NOMOS.md NEW, 2 new hooks NEW, hooks/README.md, hooks/stop-self-check.sh, install.sh, templates/settings-hooks.json, modules/incomplete-unverifiable-routing.md
design_artifact_compared_against: agents/design/arc-50/design-rev1.md (DAEDALUS e8b2215) + ARGUS PASS-WITH-RISKS + VERA PASS

== HARD SAFETY (independent meta-verify) ==
CLEAN - no live-arm path. Traced install.sh end-to-end: arming is the SINGLE step 5d (lines 1266-1310), entirely behind ENABLE_HOOKS=1 (default 0, line 124). At ENABLE_HOOKS=0 (every routine install + every Stoa arc) the arming block is skipped entirely; scripts deploy inert; no settings.json written. User tier: PRINT-ONLY manual-merge runbook, NEVER auto-writes the running ~/.claude/settings.json config. Project tier: writes only the TARGET settings.json (never the running session), and only if it does NOT already exist (else print manual-merge - never clobbers existing config). The 2 NEW hooks ride this SAME single gate; THIS diff adds ZERO new write path to the arming logic (arming block is unchanged pre-existing Arc-46 code). The 2 new hooks deploy only as candidate registrations in settings-hooks.json - inert until armed. Independent of VERA P5, sound by construction.

== r1 JSON nesting (load-bearing, ARGUS-flagged) ==
CORRECT. PostToolUse + SessionStart registrations are siblings of PreToolUse + Stop INSIDE the top-level hooks{} object (top-level keys = _comment + hooks; hooks children = PreToolUse, Stop, PostToolUse, SessionStart). JSON parses valid. Would arm correctly under --enable-hooks. Confirmed at parse level AND matches VERA HS2 behavioral arm-in-throwaway.

== Findings ==

[SHOULD-FIX] c1 - hygiene/consistency - recursion-guard layer-2 is documented as an active fallback but is currently an INERT dead branch.
  Evidence: posttooluse-agent-checker-trigger.sh:85 reads the sentinel (test -f SENTINEL then allow) but NOTHING in the substrate writes a .nomos-sentinels/<session> file. The ONLY sentinel-write site (colon-redirect into SENTINEL) is stop-self-check.sh:88, which writes its OWN .stop-sentinels (a different dir). Empirically confirmed (CATO 6.8): layer-2 READ path fires correctly WHEN a sentinel is pre-created, but in production the file never comes to exist, so the read is always false and layer-2 never suppresses. Hook header (lines 31-37) and README.md:94 present layer-2 as a session-sentinel fallback that suppresses re-firing when a NOMOS dispatch is in flight - which overstates a guard that is presently never armed. Honest framing: layer-2 is a forward-compatible scaffold, inert until an orchestrator-writes-sentinel write-site lands (a future arc, when #55889 makes the hook live).
  quadrant_classification: easy-hard (symptom obvious - no write-site; full proof of guard behavior across all parent-event population states intractable since agent_type parent-population is upstream-unconfirmed)
  Why SHOULD-FIX not BLOCKING: the comment is misleading, not the code unsound. Bounded-failure (item-6 read) means the inert layer-2 has NO live consequence. The fix is a comment/README correction (cheap, fix-now-eligible), not a code change.

[NIT] c2 - consistency - SessionStart timeout=10 variance not named in the settings-hooks.json _comment.
  Evidence: settings-hooks.json SessionStart entry uses timeout 10 vs the canonical 30 on the other 3 registrations. Variance IS documented in the hook header + design 5.6(c) + README 4, but not at the registration-site _comment. VERA flagged the same as a non-blocking methodology note. (Design P15 permits surrounding prose so this passes; polish nit.)
  quadrant_classification: easy-easy

== Verifier coverage assessment (meta-verifier of VERA, 6.4) ==
VERA PASS is sound; the 15-probe coverage is adequate for the claims made. P5/HARD-SAFETY (live settings.json byte-identical c79ae6...b21b0 across the full --enable-hooks battery), r1 JSON-nesting (parse + behavioral arm-in-throwaway), and the REAL gen-data run (12 CAPTAINs, hyphenated GROUND-TRUTH-AUDITOR token validates, spaced-form counter-check) are the load-bearing probes and all PASS. One coverage-scope OBSERVATION, NOT a VERA defect: P7 tested the layer-2 guard READ path by pre-creating the sentinel and correctly proved the read fires; P7 did NOT test whether the production flow WRITES the sentinel - the c1 gap. This is a design-probe-scope limit (DAEDALUS scoped P7 to name-absent + a session-sentinel-present = read path), not a VERA error. Because design WP3 explicitly accepts the bounded-failure AND the hook rides the upstream-broken additionalContext channel (no live consequence), the gap does NOT warrant INCOMPLETE - VERA PASS stands. c1 is the honest surfacing of the layer-2-inert reality neither P7 nor the hook comments currently expose.

== Design-level read: the layer-2-no-self-write follow-up (brief item 6) ==
BOUNDED-FAILURE IS ACCEPTABLE FOR THIS ARC. Do NOT pull orchestrator-writes-sentinel in-scope now.
  (1) The hook rides the upstream-BROKEN additionalContext channel (#55889/#18427, no fix through v2.1.150) - even when it emits, nothing reaches the model today, so the inert layer-2 has ZERO live consequence on current versions.
  (2) Even on a future fixed platform, if layer-1 (agent_type) ALSO misses, worst case is ONE NOMOS-on-NOMOS reminder. NOMOS is a leaf (tools = Bash/Read/Grep/Glob, NO Agent - confirmed) and CANNOT dispatch another NOMOS. No loop. Exactly the bounded one-shot WP3 accepts.
  (3) The fix (orchestrator writes the sentinel when it dispatches NOMOS) needs a write-site in MAJOR_PLINY/MAJOR_POLYBIUS dispatch flow - a change to ORCHESTRATOR ROLE FILES, OUTSIDE this arc named 8-file scope, and it only matters once #55889 closes. Bundling it now widens scope + diff for a benefit that cannot manifest until the upstream channel is fixed.
RECOMMENDATION: ship this arc; do c1 comment-accuracy correction (cheap, in-scope); track orchestrator-writes-sentinel as the forward-compat follow-up that lands WHEN #55889 closes (same tripwire arc that promotes the best-effort hooks to load-bearing, per README 6 When-to-revisit). Follow-up names: (a) add the sentinel write-site to the orchestrator NOMOS-dispatch flow, (b) correct the layer-2 comments then, (c) re-test the full closed loop.

== follow_ups ==
- f1 [out-of-scope, forward-compat]: orchestrator-writes-sentinel write-site for layer-2. Belongs in the future arc that promotes the best-effort additionalContext hooks to load-bearing when #55889/#18427/#15174 close (README 6 tripwire). Bounded-failure makes it non-urgent; not a block on THIS arc.
- f2 [out-of-scope, brand-doc ripple]: 12th-CAPTAIN size references in docs/case-study/ + app/ roster prose. Already tracked separately per design 11.

== Craft / scope / authorship / consistency (clean) ==
- CAPTAIN_NOMOS.md (159 lines): LEAN (authored slim, not a cut target). Voice clean - PRINCIPAL throughout, the single Colonel mention is the reserved-rank disclaimer itself, no second-person-to-PRINCIPAL drift. Tools = Bash/Read/Grep/Glob (audit-only leaf; no Write/Edit/Agent/Web - correct, more restricted than VERA by design). CONFORMANT/DIVERGENT/UNVERIFIABLE + per-divergence classification contract clear (7). r2 APPLIED: section 4 line 64 cites ARGUS+CATO as the no-Write/Edit siblings and explicitly notes VERA carries Write/Edit/Web - does NOT claim to mirror VERA for independence. No author-like frontmatter field (correct for substrate role files).
- GROUND-TRUTH-AUDITOR hyphenation deviation: SOUND + NECESSARY. gen-data adapter regex (gen-data-lib.ts:119) captures a single uppercase token with no space. Empirically confirmed: hyphenated form extracts as GROUND-TRUTH-AUDITOR; spaced counterfactual returns NO MATCH (adapter fail). Convention-consistent with ARGUS PLAN-CRITIC, ZENO SPEC-CHECKER, BARTLEBY FILE_CLERK (all single-token). VERA REAL gen-data run independently confirms it validates.
- 2 new hooks: fail-OPEN correct (allow/exit 0 on any error path). Broken-channel handling HONEST: README 6 caveat accurate and if anything conservative (ARGUS found the finding UNDERSTATED - README adds #18427 per r5, correct direction). stop-self-check.sh edit is COMMENT-ONLY: non-comment/non-blank lines sha256-identical between main and build (working decision-block+reason channel byte-unchanged).
- re-decompose remediation: 3 guardrails (size-derail-split vs wrong-spec/blocked/impossible-escalate; cap N=2 RE-DERIVED-from-bw not remembered; granularity floor) + fail-toward-escalation integrated cleanly INSIDE the existing INCOMPLETE/UNVERIFIABLE routing module; existing branches + A6-LOCK + op-disc 15 cross-refs preserved; NOMOS cross-ref added.
- Scope: diff touches EXACTLY the named 8 substrate files + design artifact. NO scope creep, no opportunistic refactors. Envelope count 11->12 consistent end-to-end: 0 stale 11-CAPTAIN/envelope strings, 5 12-sites updated, CAPTAIN_NAMES array = 12 members with NOMOS in gauntlet-pipeline order after CATO.
- Authorship: NO author-like FIELD added or touched anywhere in the diff (only prose mentions of authorship discipline in NOMOS 4/6.2/8). Commit Author = denson (densonsmith2@gmail.com) - PRINCIPAL identity, never overridden. Seat trailer Co-Authored-By: CAPTAIN_ADA_the-stoa present per 28. CLEAN.

verifier_coverage_assessment: VERA exercised the load-bearing cases (HARD SAFETY byte-identical, r1 JSON-nesting parse+behavioral, real gen-data adapter); all 15 probes PASS; verdict sound. The one coverage-scope observation (P7 tested the layer-2 sentinel READ path, not whether the production flow writes it) is a design-probe-scope limit, not a VERA error, and is the c1 finding - surfaced here, bounded-acceptable per WP3, no INCOMPLETE warranted.

summary: A clean additive Stage-2 build. Ships the 12th CAPTAIN (NOMOS, lean + voice-clean + correctly audit-only) plus two best-effort judgment-tier hooks and the re-decompose remediation, exactly to the named 8-file scope, with no author-field touch and a correct PRINCIPAL Author + seat trailer. HARD SAFETY is clean by construction (single default-OFF arming gate, no new write path, new hooks inert until armed) - independently traced, matching VERA byte-identical P5. The single substantive finding (c1, SHOULD-FIX) is that the recursion-guard layer-2 is a presently-inert dead branch (no sentinel write-site) while the hook comments + README present it as an active fallback - a comment-accuracy correction, not an unsoundness, because the bounded-failure property (leaf NOMOS cannot loop + broken additionalContext channel) means zero live consequence. On the brief design-level question: bounded-failure is ACCEPTABLE for this arc; the orchestrator-writes-sentinel fix belongs in the future arc that promotes the best-effort hooks when #55889 closes, NOT in-scope now. Overall posture: pass with minor revisions (c1 comment-accuracy fix-now-eligible; c2 nit).
