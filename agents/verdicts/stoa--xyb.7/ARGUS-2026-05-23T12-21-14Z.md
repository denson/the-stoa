status: completed
ticket: stoa--xyb.7
verdict: PASS-WITH-RISKS
design_artifact_audited: agents/design/arc-50/design-rev1.md (committed e8b2215, arc-50/build)
audit_block:
  risks:
  - id: r1
    description: settings-hooks.json registration snippet (5.6c) shows PostToolUse and SessionStart arrays at top level, but the shipped template nests all hook arrays inside a top-level hooks object. Literal copy by ADA lands them at the wrong JSON depth so the hooks never arm even under --enable-hooks.
    evidence: substrate/templates/settings-hooks.json (nests PreToolUse and Stop inside hooks) vs design 5.6c L289-298 (bare top-level keys)
    load_bearing: true
    quadrant_classification: easy-easy
  - id: r2
    description: 4.4 and 3 frame NOMOS tool restriction (no Write/Edit/Web) as mirrors VERA independence, but VERA actually HAS Write Edit WebSearch WebFetch. NOMOS is MORE restricted than VERA; the true no-Write/Edit siblings are ARGUS and CATO. Inaccurate precedent if it propagates into the role file.
    evidence: substrate/CAPTAIN_VERA.md L4 vs design 4.4 L150 and 3 L75
    load_bearing: false
    quadrant_classification: easy-easy
  - id: r3
    description: 5.6a names only the dry-run echo (install.sh L762) for 11-to-12 envelopes, but the count also appears in the header comment (L42, 11 CAPTAIN sub-agent envelopes). Second occurrence left stale is a doc defect; P3 only probes the echo.
    evidence: substrate/install.sh L42 and L762; design 5.6a L282 names L762 only
    load_bearing: false
    quadrant_classification: easy-easy
  - id: r4
    description: Recursion-guard primary mechanism (no-op when agent_type is NOMOS) keys on a field whose parent-on-return population is unconfirmed. SDK docs say agent_type/agent_id populate when the hook fires INSIDE a subagent, not confirmed on the parent on-return event. Design correctly carries the session-sentinel fallback as the real guard; failure is bounded (NOMOS is a leaf; worst case one no-op reminder). Confirmation, not a defect.
    evidence: code.claude.com/docs/en/agent-sdk/hooks; design 5.2 L213 and WP3 L361
    load_bearing: false
    quadrant_classification: hard-easy
  - id: r5
    description: additionalContext-broken finding is correct and if anything UNDERSTATED. Independent web check surfaced #18427 (PostToolUse cannot inject context visible to Claude, CLOSED-not-planned Jan 2026) a broader matcher-agnostic confirmation DAEDALUS did not cite. Conservative posture is correct and robust to the broader scope. No must-happen behavior depends solely on the broken channel.
    evidence: github.com/anthropics/claude-code/issues/18427; #55889 OPEN deny/reason working; #15174 CLOSED-dup CLAUDE.md workaround; changelog v2.1.150 no fix; design 2 L52-67 omits #18427
    load_bearing: false
    quadrant_classification: hard-easy
  non_findings:
  - Broken-upstream-channel finding (target 1) INDEPENDENTLY VERIFIED CORRECT. #55889 OPEN v2.1.123 (deny/reason confirmed working by reporter); #15174 CLOSED-dup v2.0.72-76 (CLAUDE.md workaround confirmed); changelog v2.1.150 no fix. Stop decision block plus reason is a distinct working channel. No must-happen Stage-2 behavior rests solely on the broken channel. NOT a BLOCKING reliability gap.
  - R3 ship-vs-defer (target 2) RECOMMEND SHIP. Both best-effort hooks are harmless when broken, forward-compatible, fail-OPEN, DEFAULT-OFF source-only. Shipping documents the dependency as a tripwire for the future arc that revisits on issue-close. Deferring loses forward-compat for zero current-reliability gain. Ships-dead-code is overstated: correct and dormant, not dead.
  - Section 7.7 DROP (target 3) GENUINELY CLEAN on both grounds. Verified vs two-polybius-coordination.md 7.7: case-4 confirms PLINY/CAPTAIN outputs untagged-by-canon (DROP avoids r1 false-positive); Future-scope confirms the dual hard-lock. Section 27 confirms agent-inspection is the blessed form distinct from the locked mechanical layer. NOMOS-DROP never touches the seat-tag convention (subject) and is a judgment CAPTAIN not a pre-comment-hook/CI-lint (mechanism). Resolves Stage-1 r1. No PRINCIPAL unlock; DROP-no-gate.
  - NOMOS is a CAPTAIN (target 4) SOUND. The classification field (SIZE-DERAIL vs WRONG-SPEC/BLOCKED/IMPOSSIBLE) driving re-decompose routing is genuine judgment a skill cannot make; independence requires a separate context window. agent-author taxonomy backs the call. Seat spec lean and correctly locked. Inline fact-extraction is the right no-extra-handoff call.
  - re-decompose cap robustness (target 5) SOUND. Re-derive-from-bw self-corrects a mis-incremented write on next read; fail-toward-escalation when the count read is UNVERIFIABLE is the safe direction. Integrated alongside existing INCOMPLETE/UNVERIFIABLE branches and A6-LOCK without regression.
  - HARD SAFETY (target 7) CLEAN. No path arms a hook live: scripts deploy inert, registrations in CANDIDATE template, --enable-hooks DEFAULT-OFF, user-tier prints a manual-merge runbook. NOMOS INERT until deployed AND invoked (no Agent, leaf). P5 is the before/after settings.json diff probe.
  - Buildability (target 8) BUILDABLE. NOMOS adds no recompose call (owns no modules) so cannot reintroduce the Arc-6 --no-captains regression; P4 still probes it. Hooks glob-deploy. Verdict shape mirrors VERA verdict plus quadrant. 15 falsification probes. design Author is Denson Smith; no author-field defect found.
  - op-disc 34 (trigger-payload authoring) both new payloads obey it (WHY plus WHAT inline, no bare pointer); P6/P8/P14 probe it. No new always-loaded rule added.
summary: >
  Stage 2 adds the judgment tier (CAPTAIN_NOMOS ground-truth auditor, PostToolUse-on-Agent NOMOS-dispatch trigger, SessionStart-compact reprime, re-decompose remediation with 3 guardrails), all source-only, default-OFF, inert-until-invoked. The load-bearing move (build the two new hooks best-effort on the upstream-broken additionalContext channel while backing every must-happen behavior with a WORKING channel: Stop decision block plus reason for NOMOS-dispatch; CLAUDE.md plus cron for reprime) is correct and INDEPENDENTLY VERIFIED. Confirmed #55889 (OPEN, deny/reason working), #15174 (CLOSED-dup, CLAUDE.md workaround), changelog v2.1.150 no fix; additionally found #18427 (broader, CLOSED-not-planned) which STRENGTHENS the finding. No Stage-2 must-happen behavior rests solely on the broken channel, so there is no BLOCKING reliability gap. Section 7.7 DROP is genuinely clean on both subject and mechanism grounds (verified vs 7.7 and 27 canon); no PRINCIPAL unlock needed. NOMOS-as-CAPTAIN is sound. The recursion-guard agent_type uncertainty is real but correctly handled by the load-bearing session-sentinel fallback with a bounded failure (NOMOS is a leaf). HARD SAFETY is clean. The one load-bearing risk (r1) is a buildability footgun not a soundness defect: the 5.6c JSON snippet shows the two new registrations at the wrong nesting depth relative to the shipped hooks wrapper; ADA must nest them inside hooks or they never arm. Posture: minor revisions for the build (fix snippet nesting; add #18427 to README caveat; update both install.sh count strings; drop the mirrors-VERA framing in the role file). RECOMMENDATIONS R2: NOMOS is a CAPTAIN, confirm. R3: SHIP both best-effort hooks. R4: re-decompose cap robust. R5: recursion guard sufficient. Section 7.7: DROP-no-gate.
gap_or_blocker: none (status completed)
