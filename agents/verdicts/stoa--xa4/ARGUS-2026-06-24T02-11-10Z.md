status: completed
ticket: stoa--xa4
verdict: revise
design_artifact_audited: agents/design/stoa--xa4/design-rev1.md
note_on_verdict_word: Seat verdict vocab is pass|revise|refused. PLINY brief asked PASS/PASS-WITH-CONDITIONS/ROUTE-BACK. This maps to PASS-WITH-CONDITIONS: NOT a route-back (reframe held, DC0 has no verdict-to-defend, DC2 schema-share literal). It is `revise` only in that one load-bearing risk needs resolution before ADA builds. The design does NOT need a re-draft; resolve r1 (small clarification) and carry r2-r4 as build constraints.

audit_block:
  risks:
  - id: r1
    description: The decision-surface SKILL still ships a "labeled lean" verdict affordance (SKILL.md L30 "your lean if you have one (labeled as a lean, not a verdict)"; L78 "tradeoff + labeled lean"). The design (design-rev1 S1 L120-123) deliberately keeps the lean OUT of the retuned classifier S3 but KEEPS it inside the GUIDE. A labeled lean is a soft verdict-shaped object living INSIDE the process the reframe says has no label to be argued off. A dilemma-avoiding PRINCIPAL can latch onto the lean and push the agent to harden it ("you leaned X, just commit to X") - the exact cave-vector the arc exists to close, relocated from S3 into the guide body. The design no-verdict honesty stops at the S3 boundary and does not follow the lean into the guide.
    evidence: substrate/skills/decision-surface/SKILL.md L30, L78; design-rev1 S1 L120-123; directive L86.
    load_bearing: true
    quadrant_classification: hard-hard
    what_must_be_true_before_build: The design must NAME how the in-guide labeled-lean reconciles with the no-verdict-to-defend reframe - either (a) the Q5 cave-trap self-check is explicitly wired to fire when the PRINCIPAL pushes on the LEAN (not only on a fresh verdict ask), so the lean cannot be hardened into a laundered value-call, OR (b) the lean affordance is retuned to a process frame. ARGUS does not prescribe which; it must be named, not a silent seam. This is the one place a verdict-to-defend genuinely survives in the loop.
  - id: r2
    description: P-DC2-schema robustness on the SKILL.md side is asserted, not yet guaranteed. The register side is verified-clean (exactly 9 line-start LABEL: matches in order; parse-contract list copies are indented and do not match the caret anchor - ARGUS confirmed by grep). But the SKILL.md template does not exist yet; if ADA places a second labeled example/template anywhere in SKILL.md, the head -9 could capture the wrong nine.
    evidence: design-rev1 S4 P-DC2-schema L373-377 + weak-point 3 L440-447; decision-register.md L91-99 (9 matches in order, ARGUS-verified).
    load_bearing: false
    quadrant_classification: hard-easy
    what_must_be_true_before_build: ADA emits the nine LABEL: tokens at line-start EXACTLY ONCE in SKILL.md (the canonical decision-record template, un-indented inside its fence), with no other line-start LABEL: occurrence. VERA additionally asserts the SKILL.md side has EXACTLY 9 line-start matches (not >=9 then head) so a stray match cannot false-pass.
  - id: r3
    description: The new corpus runner must score the render/ class against the per-fixture MANIFEST label, not a per-dir literal. The proven Arc-70 runner dispatches by PATH (*/pressure/* scored vs literal "hold"; */problem/* scored vs $want). The render/ dir holds BOTH labels (render(2)+prose(2)) in one dir, so a copy-the-shape implementation scoring */render/* against a literal "render" would mis-score the 2 prose fixtures as failures.
    evidence: run-dilemma-corpus.sh L246-251 (path-based dispatch, literal vs $want split); design-rev1 S4 corpus table L336-341 (render/ carries render(2)/prose(2)).
    load_bearing: false
    quadrant_classification: easy-easy
    what_must_be_true_before_build: The decision-surface runner scores the render/ class against the manifest label per fixture (the $want path, like */problem/*), not a per-dir literal.
  - id: r4
    description: Honest-stance leakage - the design body is clean, but S1 L114 "ensure the deciding happens with the tradeoff visible" reads close to a guarantee if ADA carries it verbatim into the deployed module. It must stay probabilistic, matching the Arc-70 honest-claim block.
    evidence: design-rev1 S1 L114; dilemma-classifier.md L8-14 (no enforced/guaranteed/non-collapsible); directive L51/L69.
    load_bearing: false
    quadrant_classification: easy-easy
    what_must_be_true_before_build: ADA renders the S3 retune so no phrasing implies the deciding is guaranteed/enforced; "ensure" softened to a probabilistic frame in the deployed module text.
  - id: r5
    description: Threat-enumeration completeness on the cave-trap is irreducibly model judgment. The cave/ corpus (4 fixtures, floor >=3/4) exercises the named pressure-vs-new-info vector plus one new-info negative control (cave4). Whether 4 fixtures cover the cave SURFACE (multi-turn slow erosion, authority pressure, flattery-shaped pressure, sunk-cost pressure) is not provable by the corpus - it is a seed bar, honestly stated. Named so no downstream seat reads a green cave/ class as proof the cave-trap is closed.
    evidence: design-rev1 S4 L340/L382-385 (cave/ 4 fixtures, dogfood-proxy honesty); S6 weak-points 4/5.
    load_bearing: uncertain
    quadrant_classification: hard-hard
    domain_blind_spot: Exhaustive cave-vector enumeration is unbounded; ARGUS surfaces the instances it considered (slow multi-turn erosion, authority/expert-pressure, flattery, sunk-cost) and stops per disc-S6.6 rather than manufacturing an exhaustion claim. A 4-fixture floor measures the rule, not the surface; VERA returns dogfood-proxy, not capability verification.
  non_findings:
  - DC0 spine-weakening (orchestrator priority 1) - DISCHARGED. The AFTER S3 text (design S1 L83-105) opens a PROCESS ("walk the deciding with you and keep the cost of each side on the table") and asserts NO recommendation. No verdict-to-defend in S3. The firmness relocation does not drop a floor because the anti-cave machinery is S2 (byte-unchanged), re-wired into the guide via Q5. Only residual verdict-shaped object is the in-guide lean -> r1.
  - DC2 literal-schema-share (priority 2) - DISCHARGED one-to-one. Mapping (design S3 L266-276) is one-to-one vs the real register S2; ARGUS confirmed the register has exactly 9 line-start labels in canonical order (decision-register.md L91-99). The DILEMMA-only-fields seam (design S3 L286-292) is an HONEST limitation, not a hidden gap: the register S1 over-write guard already withholds on problems + undecided dilemmas (existing Arc-71 contract). Probe robustness -> r2.
  - DAEDALUS Q1 (Skill tool LIVE or shim?) - LIVE, no shim. WebSearch (current Claude Code / Agent SDK) confirms the main-agent orchestrator invokes the Skill tool (auto+manual), injecting SKILL.md into context. POLYBIUS+PLINY are main-agent orchestrators. Corroborated 3 ways: role-file L258, install.sh L227 ("POLYBIUS able to invoke them"), web. STRONGER than the design own evidence. The loop does not even depend on the live Skill-call for load-bearing behavior: the GUIDE runs AS A METHOD via the S3 prose pointer (same consulted-module pattern as S3.6 reading dilemma-classifier.md); the Skill-tool call is needed ONLY for the optional Part-2 render, gated behind Q4. Fallback shim genuinely unnecessary.
  - DAEDALUS Q2 (corpus under modules/tests/ vs skills/tests/) - RATIFIED as modules/tests/. Either is deploy-safe (non-recursive modules/*.md glob keeps tests/ source-only). Reusing the proven runner + field() awk + established source-only deploy-safety verbatim is lower-risk; a new skills/tests/ precedent buys nothing this arc.
  - DAEDALUS Q3 (P-DC2-schema robust?) - see r2: robust register-side (verified), conditionally robust SKILL.md-side pending the exactly-once build constraint.
  - DAEDALUS Q4 (retuned S3 re-introduce a verdict?) - NO in S3 itself (see DC0); only surviving verdict-shaped object is the in-guide lean -> r1.
  - Scope-lock - HELD. No DC reaches consumer-tier (Q3 line 61 kept verbatim), slice-2b, meta-trigger, broader checkpoint rollout, or Part-2 rebuild. Diff scoped to classifier S3 + skill + SKILL_NAMES + new corpus; classifier S1/S2/S4/S5/S6 and all decision-register logic untouched, guarded by the both-corpora bar.
  - Regression bar - REAL. P-fullsuite (design S4 L398-402) runs BOTH existing corpora (dilemma 19/19 + decision-register 18/18) PLUS the new corpus PLUS gen-data + vitest + author-gate + stop-hook. Matches directive DoD L68.
  - Author attribution - CLEAN. SKILL.md frontmatter author: Denson Smith (L7); design credits Denson Smith as repo author. No wrong-author field encountered.
  threat_coverage_assessment: N/A - skill-graduation + process-delivery retune + wiring + corpus. Addresses NO named runtime security threat; carved out under op-disc S35.5 (process/doctrine change, no runtime attack path). No threat-ratified mitigation declared, so the save-verdict empty-binding guard (TRM_COUNT=0) is correctly skipped. The cave-TRAP is a behavioral-honesty concern, not a S35 security threat.
attach_status: SET-FROM-ATTACH-RC
summary: >
  Sound design; both orchestrator-flagged seams are clean on direct read. DC0 retuned S3 opens a PROCESS and re-introduces NO verdict-to-defend (anti-cave machinery is S2, byte-unchanged, re-wired into the guide via Q5); DC2 schema-share is literally one-to-one against the verified register S2 nine-field schema. PASS-WITH-CONDITIONS, not route-back. The single load-bearing risk is r1: the decision-surface SKILL still carries a "labeled lean" (soft verdict-shaped object) INSIDE the guide, and the no-verdict honesty stops at the classifier S3 boundary without following the lean into the guide body - that lean is the cave-vector a dilemma-avoiding PRINCIPAL can push on, relocated rather than removed. The design must name how the Q5 self-check covers pushback on the LEAN (or retune the lean) before build. Remaining risks are build-time conditions, not design defects: probe-robustness SKILL.md-side (r2 - 9 labels at line-start exactly once), render-class manifest-label scoring (r3), and softening one "ensure the deciding happens" phrase (r4). r5 names the cave-trap irreducible enumeration limit honestly (seed bar, not proof). Posture: minor revisions - resolve r1, carry r2-r4 into ADA dispatch.
