status: completed
ticket: stoa--2i5
verdict: pass
design_artifact_verified_against: agents/design/stoa--2i5 (Arc 55 directive) — MC1 fix re-verify of --help surface
build_verified: arc-55/build @ 1cdae32 (atop acf944c) in worktree .claude/worktrees/arc-55-build/
probes_executed:
- probe_id: P4-RE
  description: usage() sed-anchor tightening — --help must be bounded AND still contain AC2 transient-paths doc AND not be truncated.
  quadrant_classification: easy-easy
  command_or_method: bash substrate/install.sh --help | wc -l ; grep -niE 'transient|gitignore' ; grep -nE 'Usage:|--target|--dry-run|Dry-run:' ; head/tail coherence check.
  expected: bounded output (~100-130 lines, NOT ~1753), exit 0; AC2 transient-paths doc present; Usage block + flag descriptions + Dry-run terminator all present and coherent (not truncated).
  observed: 117 lines (was ~1753), exit 0. AC2 doc present at lines 106-108 ("Transient-path .gitignore (Arc 55 / stoa--2i5)..."). Usage: block at lines 23-27 with all flag descriptions. Head opens at header line 3 coherently; tail ends cleanly at "Dry-run: --dry-run prints every action..." (line 117). Full content whole, no mid-sentence truncation.
  result: pass
- probe_id: P4-RE-anchor
  description: Mechanical confirmation the new sed start anchor matches exactly one line (the fix's mechanism).
  quadrant_classification: easy-easy
  command_or_method: grep -nE '^# install\.sh — ' substrate/install.sh (new) vs grep -nE '^# install\.sh' substrate/install.sh (old); grep -nE '^# Dry-run:' (terminator).
  expected: new anchor matches exactly 1 line; old anchor matched 2 (lines 3 + 169); single terminator at 119.
  observed: new anchor /^# install\.sh — / matches ONLY line 3. Old anchor matched lines 3 + 169 (body comment "# install.sh edit —", which had no Dry-run terminator after it, so the old range ran to EOF). Terminator at line 119. Range now bounded 3->119.
  result: pass
- probe_id: P1-RE
  description: Regression sanity — fix must not disturb gitignore behavior (gitignore content is on untouched commit acf944c).
  quadrant_classification: easy-easy
  command_or_method: fresh `git init` project, `bash substrate/install.sh --target project --project-dir <tmp>`; assert .claude/.gitignore written with bare-relative entries; git check-ignore on 5 transient paths; confirm .substrate-manifest NOT ignored.
  expected: install exit 0; .claude/.gitignore written, all entries bare-relative (no leading slash); scheduled_tasks.lock, worktrees/, .substrate-last-check, __pycache__/, *.pyc all git-ignored; .substrate-manifest visible.
  observed: install exit 0; .claude/.gitignore written (786 bytes), all 5 entries bare-relative (no leading-slash entries). git check-ignore confirms all 5 transient paths IGNORED; .substrate-manifest NOT ignored (visible). Unchanged from prior PASS.
  result: pass
methodology_concerns: MC1 (the prior dispatch's concern) is now RESOLVED by 1cdae32 — re-verified closed. No new concerns. The fix is a single-line sed start-anchor tightening; em-dash anchor confirmed to match exactly the header line, with no other body-comment collision (only :3 and :169 ever started with "# install.sh"; :169 no longer matches).
falsifying_evidence_summary: (empty — pass)
verification_artifacts_path: probes run in-place in worktree against ephemeral /tmp probe dirs (cleaned up); no persisted artifacts beyond this verdict.
summary: Focused re-verify of ADA's MC1 fix (commit 1cdae32 atop acf944c). The fix tightens usage()'s sed start anchor from /^# install\.sh/ to /^# install\.sh — / (literal em-dash). P4-RE confirms --help is now bounded at 117 lines (down from ~1753), exits 0, and crucially STILL contains the AC2 transient-paths documentation (lines 106-108) with the Usage block, all flag descriptions, and the Dry-run terminator intact — the fix bounded the output WITHOUT truncating any legitimate help content. The anchor mechanically matches exactly one line (:3), eliminating the :169 body-comment double-match that caused the EOF overrun. The most load-bearing check was confirming the AC2 doc survives AND the help text is coherent end-to-end, since a naive fix could have over-tightened and cut content. Regression sanity confirms the gitignore behavior (on untouched commit acf944c) is undisturbed: fresh install writes bare-relative .claude/.gitignore, all 5 transient paths ignored, manifest visible. VERDICT: PASS. MC1 closed.
gap_or_blocker: (none)
