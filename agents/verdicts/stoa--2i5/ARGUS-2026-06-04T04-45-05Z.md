status: completed
ticket: stoa--2i5
verdict: revise
design_artifact_audited: agents/design/stoa--2i5/design.md
audit_block:
  risks:
  - id: r1
    description: The design says "document in --help" but never names WHERE the help text must live; install.sh's usage() scrapes ONLY the header comment block (lines 3-108, terminator "# Dry-run:" at :108) via sed. AC2/P4 --help match fails unless the new gitignore/transient-paths text is inserted as a "# "-prefixed paragraph INSIDE that scraped range.
    evidence: substrate/install.sh:225 (usage() = `sed -n '/^# install\.sh/,/^# Dry-run:.*$/p'`); header block bounded by line 3 `# install.sh` and :108 `# Dry-run:`. An echo after the summary block or a doc line below :108 is NOT in usage() output.
    load_bearing: true
    quadrant_classification: easy-easy
  non_findings:
  - "W1 (path-relativity): CONFIRMED CORRECT. A .gitignore at .claude/.gitignore ignores paths relative to .claude/. Design's §3.1 bare entries (worktrees/ not .claude/worktrees/) are right. Standard git nested-gitignore semantics."
  - "W2 (.substrate-last-check write-site): GROUNDED — written at ${ws}/.claude/.substrate-last-check, INSIDE .claude/ (check.sh:678 write_state_file, :662 read_state_file). Bare entry `.substrate-last-check` matches it. AC3 holds for this path; the §2 decision to DROP the --manage-gitignore root-append is CORRECT and does NOT reopen. Residual Q1 = acceptable scope reduction."
  - "Insertion point 1 (new fn after :513): CONFIRMED. write_substrate_manifest closes at :513; `# ----- argument parsing` banner at :515. Adjacent placement exact."
  - "Insertion point 2 (call after :1591): CONFIRMED. install.sh:1591 = `write_substrate_manifest \"$DEST_DIR\" \"$TARGET\" \"$PROJECT_SLUG\"` in 7c region, before 7b block at :1593. Unconditional call after it is correct."
  - "Three-tier DEST_DIR non-empty: CONFIRMED at all 3 tiers — user :601 (${HOME}/.claude), project :620 (${PROJECT_DIR}/.claude), subproject :665 (${PARENT_DIR}/${SUBPROJECT}/.claude). No [ -n ] guard needed; matches manifest call. DEST_DIR mkdir -p at :806 precedes the :1591+ call so the dir exists at write time."
  - "DRY_RUN convention: CONFIRMED. DRY_RUN=0 default (:120), =1 via --dry-run (:554). Design's `[ \"$DRY_RUN\" -eq 1 ]` matches write_substrate_manifest's own guard (:468)."
  - "W4 (quoted heredoc <<'EOF' under set -euo pipefail :110): CORRECT. Quoted delimiter prevents expansion of *.pyc/$/# lines. cat>file + return 0 explicit, no pipeline failure surface."
  - "W3 (.claude/.gitignore shows untracked): correctly scoped out of AC3 (transient-paths only), consistent with .substrate-manifest already appearing untracked post-install. P1 greps the transient set specifically. Defensible AC3 reading."
  - "README target: CONFIRMED — `## What gets deployed` exists at substrate/README.md:20; no existing gitignore/transient text, so P4 README grep requires the new content. Design's named target is correct."
  - "Threat carve-out: CONFIRMED. This is a process/hygiene change writing a static ignore file into the installer's own subtree; no runtime attack path. `not threat-ratified (§35.5)` is correct — NOT a mapless-mitigation smell."
  - "Authorship: design author field = Denson Smith (correct). No author-field defects found in audited files."
summary: >
  The design (Option α: write an idempotent, full-overwrite, dry-run-honoring .claude/.gitignore
  with bare-relative entries; no new flag; drop the directive's optional --manage-gitignore
  root-append) is structurally sound and its biggest self-flagged risks resolve in its favor against
  ground truth. W2 — the one finding that could have changed the build — is decisively GROUNDED:
  .substrate-last-check is written INSIDE .claude/ (check.sh:678), so the bare entry matches and the
  root-append remains correctly declined; AC3 holds for all four transient paths. Both install.sh
  insertion points (:513, :1591) and the three-tier DEST_DIR non-emptiness are confirmed exact, and
  the heredoc/dry-run/idempotency approach is correct under set -euo pipefail. ONE load-bearing risk
  remains (r1): the design instructs "document in --help" without naming that install.sh's --help is
  produced by usage() scraping ONLY the header comment block (lines 3-108); ADA must place the new
  gitignore doc text as a "# "-prefixed paragraph inside that range or AC2/P4 silently fails. Overall
  posture: clean design, one build-note must be folded in before ADA builds — RATIFY-WITH-CONDITIONS.
