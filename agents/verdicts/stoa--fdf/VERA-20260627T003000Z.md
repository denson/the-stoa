status: completed
ticket: stoa--fdf
verdict: pass
design_artifact_verified_against: agents/design/stoa--fdf/design-rev2.md §3 (16-check probe table: 15 gated P1–P8 + 1 REPORTED P9)
build_verified: branch stoa--fdf/build, worktree C:/Users/denso/claude_projects/the-stoa/.claude/worktrees/stoa--fdf-build, HEAD a8cad9c
probes_executed:
- probe_id: P1-choreography
  description: confirm(suggest(examine(fixture)),CONFIRM) == §23 DECLARE byte-identical; generate(D)->resolve() len == 8/6/7
  command_or_method: independent harness (agents/verification/stoa--fdf/vera_probe.py) — examine->suggest->confirm->declare_from_confirm on the 3 real fixtures; cross-checked vs test_suggest.py:219 (P1)
  expected: prospector DECLARE [google-maps,spatial-db]->8; scienceclaw [document-parsing]->6; labstat_bls [bls-oews,document-parsing]->7
  observed: prospector DECLARE [google-maps,spatial-db] resolve len 8; scienceclaw [document-parsing] len 6; labstat_bls [bls-oews,document-parsing] len 7 — all byte-identical; P3 tag present on each
  quadrant_classification: easy-easy
  result: pass
- probe_id: P2a-failclosed-reject
  description: §29.2 fail-closed — human_action {reject} -> confirm INERT; is_declare False; generate called 0x (EXECUTION call-counter on the wired path)
  command_or_method: monkeypatch declare_mod.generate with a counting wrapper; confirm(proposed,{reject}); declare_from_confirm(...); assert counter==0
  expected: confirm INERT; is_declare False; declare_from_confirm None; generate call-counter == 0
  observed: INERT=True; is_declare False=True; declare_from_confirm None=True; generate call-counter=0
  quadrant_classification: easy-easy
  result: pass
- probe_id: P2b-failclosed-noresponse
  description: §29.2 fail-closed — {no_response} -> INERT, generate 0x
  command_or_method: same instrumented call-counter harness, action {no_response}
  expected: INERT; is_declare False; None; counter==0
  observed: INERT=True; is_declare False=True; None=True; counter=0
  quadrant_classification: easy-easy
  result: pass
- probe_id: P2c-failclosed-editspending
  description: §29.2 fail-closed — {edits_pending} -> INERT, generate 0x
  command_or_method: same instrumented call-counter harness, action {edits_pending}
  expected: INERT; is_declare False; None; counter==0
  observed: INERT=True; is_declare False=True; None=True; counter=0
  quadrant_classification: easy-easy
  result: pass
- probe_id: P2d-failclosed-none
  description: §29.2 fail-closed — human_action None -> INERT, generate 0x
  command_or_method: same instrumented call-counter harness, action None
  expected: INERT; is_declare False; None; counter==0
  observed: INERT=True; is_declare False=True; None=True; counter=0
  quadrant_classification: easy-easy
  result: pass
- probe_id: P2-counter-liveness (VERA-added)
  description: liveness check — the SAME call-counter must increment on a real CONFIRM, proving it is wired (not dead) so the P2a-d "==0" is meaningful
  command_or_method: instrumented harness, action {confirm}; assert counter==1
  expected: counter == 1
  observed: counter == 1 (on a real CONFIRM the call-counter increments — the 0-counts above are genuine, not a dead instrument)
  quadrant_classification: easy-easy
  result: pass
- probe_id: P2e-overproposal
  description: over-proposal fixture proposes google-maps (stray maps URL); human EDIT removes -> DECLARE [document-parsing]; resolve 6; NO (gcp_api,google-maps), NO MAPS_API_KEY in resolved
  command_or_method: suggest(examine(over-proposal)); confirm with edited set [document-parsing]; declare_from_confirm; inspect resolved (kind,name) tuples
  expected: over proposes google-maps; EDIT DECLARE [document-parsing]; resolve len 6; no google-maps/MAPS_API_KEY
  observed: over proposes google-maps=True; DECLARE [document-parsing]; resolve len 6; (gcp_api,google-maps) absent; MAPS_API_KEY absent
  quadrant_classification: easy-easy
  result: pass
- probe_id: P2f-underproposal
  description: under-proposal fixture misses bls-oews; human EDIT adds -> DECLARE [bls-oews,document-parsing]; resolve 7; BLS_OEWS_API_KEY present
  command_or_method: suggest(examine(under-proposal)); confirm with edited set [bls-oews,document-parsing]; declare_from_confirm; inspect resolved tuples
  expected: under misses bls-oews; EDIT DECLARE [bls-oews,document-parsing]; resolve len 7; BLS_OEWS_API_KEY present
  observed: under misses bls-oews=True; DECLARE [bls-oews,document-parsing]; resolve len 7; BLS_OEWS_API_KEY present
  quadrant_classification: easy-easy
  result: pass
- probe_id: P3-module-identity
  description: declare.generate IS the imported 2.1 callable; suggest/ defines no generate/resolve of its own — RUNTIME assert, not grep
  command_or_method: runtime `declare_mod.generate is core_generate`; check __module__; scan suggest/*.py for `def generate`/`def resolve`
  expected: identity True; __module__ under builder_deploy_core.*; suggest/ defines neither
  observed: declare.generate IS core_generate=True; generate.__module__=builder_deploy_core.discovery.generate; resolve.__module__=builder_deploy_core.resolution.resolve; suggest/ defines own generate/resolve=NONE
  quadrant_classification: easy-easy
  result: pass
- probe_id: P4a-evidence-present
  description: every proposed service carries non-empty evidence; each row is a (surface, observed_token, matched_hint) 3-tuple
  command_or_method: suggest() on the 3 fixtures; assert evidence[sid] non-empty + all rows len-3 tuples
  expected: non-empty 3-tuple evidence per proposed
  observed: prospector{google-maps:1,spatial-db:1}; scienceclaw{document-parsing:2}; labstat_bls{bls-oews:2,document-parsing:1} — all 3-tuples
  quadrant_classification: easy-easy
  result: pass
- probe_id: P4b-contract-shape
  description: §2.5 CandidatePresentation shape + 3 anti-rubber-stamp invariants (no-bulk-blind-accept; no-response->INERT; valid-pres+CONFIRM->DECLARE)
  command_or_method: build_presentation + check_contract; confirm_presented against empty presentation (inv1), {no_response} (inv3), valid+{confirm}
  expected: contract.ok; shape ok; inv1 empty-pres->INERT; inv3 no_response->INERT; valid->DECLARE
  observed: contract.ok=True; shape ok=True; inv1=True (empty pres -> INERT); inv3=True (no_response -> INERT); valid-pres+CONFIRM->DECLARE=True
  quadrant_classification: easy-easy
  result: pass
- probe_id: P5a-hints-data+tolerance-pin+neg
  description: load_detection_hints loads 4 services' hints (POS); real 4-rec catalog with [detection_hints] loads clean through UNCHANGED load_catalog with no detection_hints leak (PIN); unknown sub-key under [detection_hints] -> DataIntegrityError (NEG)
  command_or_method: load_detection_hints(data_root); load_catalog assert 4 recs + shape {entries,gcp_api,category}; synthetic TOML with mystery sub-key -> assert DataIntegrityError. Also confirmed both PIN test (test_load_catalog_admits_detection_hints) + NEG test present in suite.
  expected: POS 4 services; PIN clean load + no leak; NEG DataIntegrityError
  observed: POS hints for [bls-oews,document-parsing,google-maps,spatial-db]; PIN load_catalog clean 4 recs, record shape {category,entries,gcp_api} (no detection_hints leak); NEG raised DataIntegrityError "unknown sub-key(s) ['mystery'] under [detection_hints]"
  quadrant_classification: easy-easy
  result: pass
- probe_id: P5b-p3-provenance
  description: confirmed DECLARE record tagged P3 (agent-proposed + human-ratified), distinct from a hand-authored manifest
  command_or_method: declare_from_confirm -> inspect declare_record['provenance']
  expected: provenance == 'P3'
  observed: provenance=P3; services=[google-maps,spatial-db]
  quadrant_classification: easy-easy
  result: pass
- probe_id: P6-engine-runs-fixtures
  description: examine(fixture, neuro=False) Layer-S runs on all 5 §29.1 fixtures and extracts the expected signals feeding P1
  command_or_method: examine() on all 5 fixtures; assert dict output + signals present
  expected: Layer-S runs on all 5, produces evidence-bearing proposals
  observed: ran on all 5; e.g. prospector sdk_imports [@googlemaps/js-api-loader] + data_signals [spatial-data]; AST-not-grep confirmed (prospector's commented documentai import NOT flagged -> document-parsing correctly absent)
  quadrant_classification: easy-easy
  result: pass
- probe_id: P7-full-suite
  description: FULL existing the-stoa suite green — builder-deploy-core pytest + gen-data (deterministic) + app build + app test
  command_or_method: pytest (worktree .venv); npm run gen-data + git -C worktree diff; npm run build; npm test
  expected: pytest green (40 2.1-floor intact + new); gen-data clean diff; build OK; app test green
  observed: pytest 73 passed/0 fail (15 discovery + 17 resolution [both BYTE-UNCHANGED since 2.1] + 12 dataload [2.1-portion 8 intact, +4 additive] + 29 suggest); 2.1 floor 40 reconciled, NO 2.1 test dropped/errored; gen-data -> EMPTY worktree diff (deterministic, arc edited no substrate); npm run build green; npm test 3 files/41 passed
  quadrant_classification: easy-easy
  result: pass
- probe_id: P8-authorship
  description: every new/edited artifact carries author: Denson Smith; zero foreign author-like field
  command_or_method: grep author-like fields over suggest/*.py + test files + catalog TOMLs + dataload.py + pyproject; inspect commit Author/trailers
  expected: author: Denson Smith everywhere; no foreign field
  observed: all 6 suggest/ sources + both test files carry author: Denson Smith; pyproject authors=[{name=Denson Smith}]; catalog TOMLs are data (no author field, correct); commit Author=denson <densonsmith2@gmail.com> (NOT overridden) + Co-Authored-By seat trailer (substrate convention); only "author"-like greps are prose (hand-authored/fixture author/provisioning authority) — ZERO foreign field
  quadrant_classification: easy-easy
  result: pass
- probe_id: WATCH4-unknown-bucket-inert (FM watch-item 4)
  description: the INERT 'unknown' bucket (real-fixture uncataloged tokens: os, google.cloud parent, full-path urls) never leaks into proposed/DECLARE/8-6-7
  command_or_method: suggest() on the 3 real fixtures; assert unknown-token set disjoint from proposed set
  expected: unknown ∩ proposed == empty on all 3
  observed: prospector unknown []; scienceclaw unknown [google.cloud, documentai...:process]; labstat_bls unknown [os, google.cloud, api.bls.gov...timeseries/data] — ALL disjoint from proposed; recommendation-only INERT, never touch DECLARE/8-6-7
  quadrant_classification: easy-easy
  result: pass
- probe_id: P9-accuracy-baseline
  description: REPORTED-NOT-GATED accuracy baseline; honest Layer-S-only / symbolic-only label (Layer N unwired)
  command_or_method: examine(scienceclaw, neuro=False) -> suggest; recall/precision vs ground-truth {document-parsing}; examine(neuro=True) -> assert _sources has 0 neuro-tagged tokens
  expected: a measured baseline, REPORTED only (does not gate)
  observed: scienceclaw Layer-S-only recall=1.00 precision=1.00 (over=0, under=0); neuro=True contributes 0 neuro-tagged tokens (Layer N UNWIRED, no model in 2.2). EXPLICITLY symbolic-only — NOT a neuro-symbolic accuracy claim. Does NOT gate the arc.
  quadrant_classification: hard-hard
  result: inconclusive
threat_coverage: []
methodology_concerns: |
  - TRM_COUNT=0 for this arc: every SUGGEST element is the §35.5 self-carve-out (process/choreography/structure change, no new runtime attack path), ARGUS-RATIFIED not-threat-ratified in the rev1 audit; PLINY A1 confirmed the A2 gate vacuous. The threat_coverage: list is therefore correctly empty (the §35.5 carve-out empty-state, not an absent-probe finding). The §29.2 fail-closed no-confirm probe (P2a-d) is the load-bearing SAFETY probe but verifies the gate's fail-closed PROPERTY, not a threat-defeat — consistent with the design's §2.7/§5 and §35.5.
  - P9 is REPORTED-NOT-GATED and is recorded as `inconclusive` for routing-hygiene ONLY (it is an unbounded accuracy claim against a real repo — hard-hard quadrant — and the brief + design forbid gating on it). Its `inconclusive` does NOT lower the arc verdict: P9 is explicitly outside the gated set (DoD#9, design §3/§7). The 15 GATED probes (P1–P8 + the FM watch-items) are all PASS.
  - VERA-side harness note: my first harness run flagged P1-prospector/P5b as fail on a WRONG expected-DECLARE guess (["google-maps"]). The §23 DECLARE set for the prospector geo-fixture is ["google-maps","spatial-db"]->8 (test_suggest.py:70/181; prospector carries BOTH a Maps-JS import AND a spatial-data flow). Corrected expectation; build was correct. The build's resolve len (8) was right throughout — only my expectation string was wrong. No build defect.
falsifying_evidence_summary: ""
verification_artifacts_path: C:/Users/denso/claude_projects/the-stoa/.claude/worktrees/stoa--fdf-build/agents/verification/stoa--fdf/vera_probe.py
summary: |
  The SUGGEST front-door build (HEAD a8cad9c) PASSES independent verification against design-rev2 §3. All 15
  GATED probes pass; P9 is REPORTED-not-gated (recorded inconclusive for routing-hygiene only, does not lower
  the verdict). The build was exercised through its real wired path (examine->suggest->confirm->declare_from_confirm)
  via an independent monkeypatch harness, NOT trusting ADA's self-report. The load-bearing §29.2 fail-closed
  probe is the most important: for all 4 no-confirm actions {reject, no_response, edits_pending, None} the confirm
  gate returns INERT, is_declare is False, declare_from_confirm returns None, and an EXECUTION call-counter on the
  wired generate() reads 0 — verified live, not by grep, with a liveness check proving the counter increments to 1
  on a real CONFIRM (so the zeros are genuine). Choreography hits 8/6/7 byte-identical; over-proposal EDIT-remove
  resolves to 6 with no google-maps/MAPS_API_KEY; under-proposal EDIT-add resolves to 7 with BLS_OEWS_API_KEY
  present. The §2 constraint holds at the byte level: the 2.1 dataload.py is a STRICT PREFIX of the new file (every
  2.1 loader byte-identical; only appended function is load_detection_hints), generate/resolve/discovery/resolution
  are not in the changed set, catalog TOMLs gained additive [detection_hints] blocks only, P3 module-identity is a
  runtime IS-check. The INERT unknown bucket (os/google.cloud-parent/full-path urls) is disjoint from every
  proposed set. The FULL existing suite is green: pytest 73/0 (40 2.1-floor intact, no 2.1 test dropped/errored),
  gen-data deterministic (empty worktree diff), app build OK, app test 41/41. Authorship is clean (author: Denson
  Smith everywhere; commit Author not overridden). P9 is honestly Layer-S-only symbolic (Layer N unwired, 0 neuro
  tokens) — NOT over-claimed as neuro-symbolic and not gating the arc.
