status: completed
ticket: stoa--fdf (u--9s2 Phase-2 increment 2.2)
verdict: revise
design_artifact_audited: agents/design/stoa--fdf/design-rev1.md
audit_block:
  risks:
  - id: r1
    description: The design wires detection_hints onto the 2.1 catalog records by RELYING on load_catalog silently ignoring an unknown TOML key, but the 2.1 core own section-2.4.1 discipline is FAIL-CLOSED-on-unknown for every OTHER body (kinds/baseline/categories validate exact sets and reject extras) -- so detection_hints sits as the one un-contracted additive key in an otherwise strict-key package; a future hardening that extends the section-2.4.1 fail-closed posture to load_catalog (the natural next step for that package) would reject the [detection_hints] block and break the catalog load.
    evidence: dataload.py:243-275 (load_catalog reads ONLY service-id/entries/gcp_api/category, no extra-key rejection) vs dataload.py:84-174 (load_baseline/load_kinds reject extras by set-equality) ; design-rev1.md section-1 A-import-1, section-2.3, section-6 WP-D2
    load_bearing: true
    quadrant_classification: easy-easy
  - id: r2
    description: Directive piece #2 (populate hints BEYOND the four section-25.3 seed hints) uses a verb the 2.2 catalog cannot satisfy literally -- the 2.1 catalog ships EXACTLY the four seed-hint services and no others. The DAEDALUS faithful read (migrate the 4 seeds dict-to-TOML + establish the R-3 authoring + load_detection_hints validation contract as the durable fill MECHANISM) is the correct and only deliverable, but the directive-text/deliverable gap must be explicitly ratified UP so NOMOS does not later read it as under-delivery of piece #2.
    evidence: 2.1 catalog dir = exactly google-maps/spatial-db/document-parsing/bls-oews .toml (verified) == the 4 seed-hint services in suggest-check/catalog_hints.py:34-94 ; directive section-1 piece #2 + section-5 DoD#5 ; design-rev1.md section-2.3 note + section-6 WP-D3
    load_bearing: false
  - id: r3
    description: Layer-N (the REPORTED neuro path) sends project-repo excerpts to an external LLM -- a data-egress surface the gated path never touches and that NONE of the gated probes (P1-P8) exercise. It is correctly gate-inert and REPORTED-only (cannot mis-provision; section-26 gate stands), so it is NOT a new PROVISIONING trust boundary and does not change the section-31 not-threat-ratified verdict. But because it is a genuinely new external-egress surface distinct from the read-only-static gated path, it warrants ONE explicitly NAMED residual (Layer-N proposal-channel egress: repo excerpts to external LLM; REPORTED-only, gate-inert, no provisioning authority) so the egress is on the record rather than absorbed silently.
    evidence: design-rev1.md section-2.2 Layer N, section-4-Decision#2, section-5 5th row, section-6 WP-D4 ; design-formal section-31 SWP-3 row ; directive section-4.2 (names a distinct proposal-channel integrity residual)
    load_bearing: false
  non_findings: |
    - SAFETY-FRAMING (audit pt 1): CONFIRMED HOLDS. No accuracy-as-safety coupling anywhere. design-rev1 section-2.0 states safety rests ENTIRELY on the section-26 fail-closed gate + unchanged V1-V5; accuracy=USEFULNESS never safety; NO suggest-completeness claim; a weak/absent engine leaves the system SAFE. DoD#9/P9 is REPORTED-not-gated in BOTH the section-3 probe table and the section-7 DoD map. Nothing gates on an accuracy number. The Layer-S(gated/deterministic/hermetic) vs Layer-N(neuro/REPORTED-only/NEVER-gated) split is the correct STRUCTURAL realization of the section-28 USEFULNESS/safety boundary.
    - section-2 IMPORT-UNMODIFIED (audit pt 2): CONFIRMED at source. generate exported at builder_deploy_core.discovery.generate.generate, resolve at builder_deploy_core.resolution.resolve.resolve (__init__.py:13,25); both imported callables, not re-defined. The P3-module-identity probe (asserts the __module__ values resolve to builder_deploy_core.* AND that suggest/ defines NO generate/resolve of its own) is well-grounded against the ACTUAL module names -- a real identity check. section-0-23 textually unchanged; V1-V5 untouched; 8/6/7 a REGRESSION TARGET hit via the unchanged generate-to-resolve (P1), not re-derived.
    - SWP-1/WP-D1 (fixtures prove choreography not accuracy): correct + safe. The gated suite proving choreography-on-fixtures is the INTENDED shape; real recall/precision is the REPORTED DoD#9 baseline; the gate + V1-V5 make any proposal safe regardless of inference quality.
    - SWP-2/section-4-Decision#3 (anti-rubber-stamp confirm-contract): the Phase-1-named contract (CandidatePresentation shape + 3 machine-checkable invariants: no-bulk-blind-accept / per-candidate-ack / no-response==INERT) is the RIGHT Phase-1 boundary; defers only the visual UI. entries_preview (human sees the provisioning CONSEQUENCE) is a genuine anti-rubber-stamp strength. CONFIRMED sufficient.
    - SWP-3/section-4-Decision#2 (read-surface): CONFIRMED no new PROVISIONING trust boundary beyond R-2/candidate-R-3. Agent reads catalog hints + one project dir, writes nothing provisionable, output INERT until section-26; a biased read can only mis-PROPOSE (caught by the gate). The ONE refinement is r3 (name the Layer-N egress residual), not a trust-boundary change.
    - SWP-4 (DWP-3 honest scope): correctly stated as Phase-1 PARTIALLY closed by SUGGEST, fully closed only by the Phase-2 runtime-observer; the gate (not inference completeness) makes the residual safe. No over-claim.
    - WP-D5 (over/under fixtures test the GATE not the engine): CONFIRMED correct per section-28; P2e/P2f prove the gate CATCHES a constructed mis-proposal (the load-bearing safety claim); real mis-proposal rate is the deliberately-ungated DoD#9 baseline.
    - section-31/section-5 threat classifications (audit pt 4): ALL FIVE RATIFIED as PROPOSED not-threat-ratified (section-35.5 carve-out CONFIRMED each): detection_hints DATA, examine-to-propose engine, CONFIRM gate (fail-closed SAFETY gate parallel to V1-V5, defeats no NAMED threat), P3 provenance, agent-read surface incl. Layer-N egress. None has a runtime attack path; I (the section-35.1 upstream confirmer) RATIFY all five so none can be self-exempted downstream. No threat-anchored probe owed (coverage set empty); section-29.2 fail-closed branch (P2a-d) is the load-bearing SAFETY probe, not a threat-defeat.
    - section-4-Decision#1 (app-code vs Workflow, audit pt 6): CONFIRMED SOUND -- application code, NOT a Workflow, NO HAMILTON, NO re-compose. Layer S is a deterministic hermetic pure-function examiner matching the 2.1 idiom; the only agentic step (Layer N) is a single REPORTED-only enrichment call that does not warrant a workflow engine and would wrongly couple the gated suite to a workflow runtime. STANDARD composition correctly carries the arc.
  threat_coverage_assessment: N/A -- section-35.5-carved-out arc; zero threat-ratified mitigations (all 5 section-31 elements ratified not-threat-ratified); no threat-anchored probe owed; empty-binding guard not triggered (TRM_COUNT=0).
summary: |
  Faithful, well-grounded promotion of the 35/35 VERIFIED prototype into packaged code under builder_deploy_core, holding the load-bearing safety framing absolutely (accuracy=USEFULNESS-not-safety; DoD#9 REPORTED not gated; weak/absent engine leaves the system SAFE; safety rests entirely on the section-26 fail-closed gate + unchanged V1-V5) and the section-2 import-unmodified constraint at source (generate/resolve are imported module-identities; the P3 probe is a real identity check). I RATIFY all five section-31 not-threat-ratified classifications and the SWP-1..4 classifications, and CONFIRM section-4-Decision#1 (application code, no HAMILTON, no re-compose) as sound. ONE load-bearing risk to fold before ADA builds (r1): the design relies on load_catalog tolerating detection_hints as an additive key, but that tolerance is an ACCIDENT in an otherwise strict-key, fail-closed section-2.4.1 package -- it should be made a CONTRACT (a strict-key allowlist on load_catalog that explicitly includes detection_hints) so a future fail-closed hardening of load_catalog cannot silently break the catalog load. Per the no-fixes discipline I name the risk and stop. Two non-load-bearing risks (r2: ratify UP the directive piece-#2 verb-tension as migrate-4-seeds-to-DATA + establish R-3 contract, so NOMOS does not read it as under-delivery; r3: name the Layer-N repo-excerpt-to-LLM egress as one explicit residual). Posture: clean design, minor revisions -- one contract-hardening to fold + two ratify-up notes.
gap_or_blocker: none
