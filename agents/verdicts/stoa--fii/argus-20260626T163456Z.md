status: completed
ticket: stoa--fii
verdict: revise
design_artifact_audited: C:/Users/denso/claude_projects/the-stoa/.claude/worktrees/stoa--fii-build/agents/design/stoa--fii/design-rev1.md
audit_block:
  risks:
  - id: r1
    description: The protocol's target-resolution recipe and probes P1/P4 invoke jq, but jq is NOT installed on this machine (verified absent in both bash and PowerShell), so VERA cannot execute the probes as written and a coordinator cannot run the documented git-show-pipe-jq resolve step here.
    evidence: design-rev1.md L47, L118, L145 (jq in protocol Step 1 + P1 + P4); ground-truth `where jq`/`jq --version` both fail (exit 127); the pre-existing stoa--reg read-recipe in the ticket body ALSO uses jq (substrate-wide assumption, not arc-introduced, but it blocks THIS arc's probe execution).
    load_bearing: true
    quadrant_classification: easy-easy
  - id: r2
    description: M1 is framed as defeating "false-alive via stale-or-wrong-sid," but bw stores a comment as {text, timestamp} only — the [from:|sid] is self-declared, forgeable body text with no bw-enforced author identity, and the registry sid is world-readable JSONL. So sid-match catches an accidental/typo/stale-sid mismatch (which P2 proves) but does NOT defeat a deliberate impersonator who copies the registry sid. The M1 row overclaims past the honest-claim boundary stoa--reg's own contract already establishes (the provenance tag is audit-only, forgeable metadata, not an authentication or authorization control).
    evidence: design-rev1.md L103 (M1 row) + L130 P2; `bw show stoa--fii --json` comment schema = {text,timestamp} (no author field); stoa--reg ticket body HONEST-CLAIM BOUNDARY (audit-only). M1 should be honestly scoped to "audit-only sid-match; catches stale/typo/wrong sid, NOT a deliberate impersonator" to stay inside the registry's already-ratified forgeable-provenance boundary.
    load_bearing: true
    quadrant_classification: easy-easy
  - id: r3
    description: §7 (Coordinating two POLYBIUS seats) is RELOCATED to module .claude/modules/two-polybius-coordination.md with only stub anchors in the core op-disc file; the design's plan to ADD a one-line "Distinct from §38" cross-ref to §7 does not say whether the line lands in the core stub or the relocated module body. ADA could put it in the wrong home.
    evidence: design-rev1.md L162 (§7 cross-ref plan); op-disc L185 "## 7. ... Relocated to .claude/modules/two-polybius-coordination.md ... subsection headings below are stub anchors". Also the design adopts the SAME [radio-check] token the §7 memory pattern uses ([radio-check A]), so the §38 ping token and the §7 periodic-handshake token are textually identical — the disambiguating cross-ref is the only thing preventing a poll-filter / human conflation.
    load_bearing: false
  non_findings:
  - "install.sh deploy claim — CONFIRMED. op-disc deploys via plain cp (install.sh L1069); a new INLINE section needs no array change. OPDISC_MODULES (L1266) is for relocated modules only; design correctly flags a future module-ification WOULD need that entry."
  - "§38 section number — CONFIRMED. Highest current op-disc section is §37; §38 is the correct next integer."
  - "M1 sid both-halves obtainable — CONFIRMED. Registry sid in world-readable JSONL; ack sid in comment body text; coordinator can read+compare both. Mechanically achievable (the WHAT-it-defeats overclaim is r2, separate)."
  - "record-seat idempotency — CONFIRMED. record-seat.ps1 drops any existing (seat,machine) row (L102-108) and writes status=alive (L134); the recovery 'idempotent rewrite overwrites the dead row, no orphan' claim holds."
  - "team-launcher recovery cross-ref — clean add. SKILL.md has NO existing recover/relaunch/replace/resurrect text; no conflict."
  - "No passive-machinery drift — checked. M3 self-check + §6 hard fence + Assumption A keep liveness as the live ping; JSONL schema unchanged; window coordinator-stated per-ping (not a baked TTL); tally is a bw show read, not a sleeping process."
  - "Threat enumeration vs scope — brief-named extras (ack-storm, [for:]-tag spoofing, wrong-ticket, stale-registry-at-ping) mostly covered/OOS: ack-storm addressed (un-named targets do not answer, L59); [for:]-spoof collapses into r2 (same forgeable-body-text root); wrong-ticket + stale-registry are coordinator-judgment risks bounded by reading the live registry at ping-time (L45-47) — not load-bearing for a human-simple on-demand protocol."
  - "Helper-vs-protocol — the Grand anticipated 'a radio-check helper'; DAEDALUS made the no-helper call (§2.2). The Grand delegated weight ('your call given the smaller scope'); the justification (a sleeping script IS the passive-machinery smell; the answerer half is a polling discipline a skill cannot invoke) is sound and pro-human-simple."
  - "Over-engineering creep — none. Two bw comments + one bw show + a coordinator read. No new file, no install.sh array change, no schema change."
  threat_coverage_assessment: "Design-time probe-SPEC adequacy ADEQUATE for the three threat-ratified mitigations M1/M2/M3 — each has a §3 threat-anchored probe asserting attack-blocked + legit-unaffected (M1->P2, M2->P3, M3->P4), so a downstream EXECUTED probe exists for VERA's threat_coverage line to cite. CAVEAT: P2's attack-blocked half tests a RANDOM-GUID mismatch only; to faithfully exercise M1 as honestly scoped (r2) it should note a COPIED registry sid is NOT blockable (forgeable-text limit) — VERA must not assert M1 'defeats impersonation' when it defeats only stale/typo/wrong-sid. Threat ENUMERATION completeness stays my unmechanized judgment: the named-threat set is coverage-adequate for a human-simple on-demand protocol; no claim of total threat-defeat."
summary: |
  The design is a clean, genuinely human-simple documented bw protocol (ping -> ack -> coordinator-stated window -> coordinator-read tally) with NO new skill, NO schema change, and NO passive machinery — it sits squarely inside the re-forged on-demand scope and shows no drift back toward the scrapped sweep/TTL/last_seen. Deploy mechanics, section numbering, record-seat idempotency, and recovery-path cross-ref all verify clean. Two load-bearing risks hold it at REVISE, both easy-easy: (r1) the protocol and probes depend on jq, which is NOT installed on this machine, so VERA cannot execute P1/P4 as written and the documented resolve-step is non-runnable here — needs a jq-free fallback (a grep/Select-String resolve, or an explicit jq-prerequisite note); and (r2, the most important) M1's sid-match is framed as defeating false-alive impersonation, but bw comments carry no enforced author identity and the registry sid is world-readable, so the check is AUDIT-ONLY (catches stale/typo/wrong sid, not a deliberate impersonator) — the M1 row must be re-scoped to honor the forgeable-provenance boundary stoa--reg's own contract already ratified, rather than overclaiming past it. A third, non-load-bearing precision note (r3): the §7 cross-ref must specify the relocated-module home, and the §38 ping reuses the §7 [radio-check] token verbatim — the disambiguating cross-ref is load-bearing for avoiding poll-filter conflation. Overall posture: needs minor revisions; no structural problem.
