status: completed
ticket: stoa--rdh
verdict: PASS-WITH-RATIFICATIONS
design_artifact_audited: agents/design/stoa--rdh/design.md
audit_block:
  risks:
  - id: r1
    description: Design SS2.4 prose states "Final array (11 entries)" and "10 pre-existing entries + interactive-html-preview", but the actual SKILL_NAMES array (held install.sh + the literal block printed in SS2.4) has 10 entries total (9 pre-existing + interactive-html-preview). Off-by-one in the narration only.
    evidence: substrate/install.sh SKILL_NAMES block (10 entries, counted); agents/design/stoa--rdh/design.md SS2.4 line "Final array (11 entries)" + "Confirm: 10 pre-existing entries + interactive-html-preview". The printed array and P1 deploy-loop both correctly enumerate 10 names.
    load_bearing: false
    quadrant_classification: easy-easy
  - id: r2
    description: team-launcher SKILL.md documents "claude --dangerously-skip-permissions" (a real permission-bypass flag). It is held OUT of consumer deploy this arc, introduces no new runtime attack path (documents a flag the substrate team already uses by design), and is not a credential surface. Noted under the SS6.8 credential-discipline lens for completeness; no new attack path.
    evidence: substrate/skills/team-launcher/SKILL.md lines 4, 36; held out of SKILL_NAMES (verified absent) so not deployed to any consumer.
    load_bearing: false
    quadrant_classification: easy-easy
  - id: r3
    description: The team-launcher HOLD leaves a known consumer-incomplete skill version-controlled but un-generalized; a future agent skimming substrate/skills/ could mistake it for deployable and add it to SKILL_NAMES without first bundling launch-team.ps1 into the subtree. DAEDALUS self-flagged this (SS5.4); mitigation is the filed follow-up ticket + the-stoa-specific prose, no frontmatter marker (correctly out of scope). Surfaced for the ratification of the follow-up-ticket deliverable.
    evidence: design SS2.3 (follow-up ticket is an arc deliverable, to be filed before hand-back) + SS5.4; substrate/skills/team-launcher/SKILL.md cross-refs to ./launch-team.ps1 (repo root) and HUMAN_paste-*-init.md (repo root), verified to live OUTSIDE the skill deploy subtree.
    load_bearing: uncertain
    quadrant_classification: easy-easy
  non_findings:
  - team-launcher HOLD justification SOUND - launch-team.ps1 + HUMAN_paste-{pliny,polybius-the-stoa}-init.md verified at repo ROOT (not under substrate/skills/), and install.sh deploy unit is the skill subtree ONLY (lines 1209-1215), so deploying as-is WOULD hand a consumer an inert skill. Option (a) leaves a consumer install clean (skill never copied).
  - SKILL_NAMES correctness - held install.sh diff is EXACTLY "+interactive-html-preview" (one line, verified via git diff); every one of the 10 listed entries resolves to a real source dir; decision-surface and team-launcher both genuinely excluded from SKILL_NAMES AND (by exclusion) from deploy.
  - install.sh validation - the 811-813 gate hard-errs (install aborts) if any SKILL_NAMES entry has no dir or no SKILL.md; it never inspects UNLISTED dirs, so landing decision-surface/team-launcher unlisted cannot trip validation. Obsolete-sweep (1606/1695) only flags DEST subdirs not in SKILL_NAMES; both new skills are net-new (git log -S confirms never previously listed) so no obsolete warning.
  - gen-data / Zod - skillFrontmatterSchema = z.object({name, description}) is NOT .strict(), so decision-surface status: key passes (stripped). gray-matter parses all 3 frontmatters clean (the DRAFT status line with em-dash and embedded quotes parses as a single quoted string). All 3 frontmatter name: match their dir names (gen-data-lib 211-216 name-match assertion satisfied). All 3 already present in agents.ts.
  - P1-P6 falsifiable and sufficient. P6 author-grep weak point (DAEDALUS SS5.1): the second grep DOES fire on the 3 legit net-new "author: Denson Smith" additions, but the inline interpretation note correctly instructs VERA the finding is ONLY a non-Denson value or a change to a pre-existing field; the first grep (grep -v Denson) is the load-bearing check. Note adequate; no false-finding risk if VERA reads it. P5 node -e require(.ts) is best-effort/|| true (SS5.2) - real gates are gen-data exit 0 + build exit 0 + name-grep; acceptable.
  - Authorship invariant - all 3 SKILL.md author fields = Denson Smith (verified via gray-matter parse, not just grep); no author-like field in agents.ts; the diff is net-new files so author lines appear as additions (expected). No path by which a non-Denson author slips in.
  - Threat classification CONFIRMED not-threat-ratified (SS35.5 carve-out: additive skill landing + install-manifest wiring; no runtime attack path, no credentialed/network/PRINCIPAL-gate surface introduced). No credential/network/op-broker surface in any of the 3 skills (grep clean except the documented --dangerously-skip-permissions flag, r2). No named threat (SS35.1) hiding. The one safety-adjacent property (no consumer receives a dangling-ref skill) is a correctness/UX property covered by the SS2.3 hold + probe P4, not a security mitigation - so threat_coverage_assessment is omitted (SS35.5-carved-out arc, no threat-ratified mitigation).
summary: >
  Low-risk additive arc that version-controls 3 held LIEUTENANT skills and wires deployment per-skill by readiness. The design is sound and the one real design call - team-launcher hold-vs-generalize - is resolved correctly: option (a) HOLD is justified by verified fact (launch-team.ps1 + HUMAN_paste-* live at repo root, the install.sh deploy unit is the skill subtree only, so deploying as-is would hand a consumer an inert skill). All load-bearing claims verified against repo state: install.sh diff is exactly +1 line; the 10-entry SKILL_NAMES validates and deploys correctly; decision-surface/team-launcher are genuinely landed-but-not-listed-and-not-deployed; Zod is non-strict so the DRAFT status key passes; all 3 authors = Denson Smith; threat-class not-threat-ratified is confirmed. Zero load_bearing:true risks. The three surfaced items are: r1 a harmless off-by-one in SS2.4 prose (the printed array and probes are correct, install.sh is unaffected); r2 a noted-for-completeness --dangerously-skip-permissions documentation line in the held-out team-launcher (no new attack path); r3 the team-launcher hold leaves an un-generalized skill whose mitigation is the follow-up ticket DAEDALUS commits to filing as an arc deliverable. Overall posture: CLEAN, ready for build. Ratifications for PLINY to restate at the A1 beat: (1) the not-threat-ratified SS35.5 carve-out (additive skill landing, no runtime attack path); (2) the team-launcher generalize follow-up ticket is a required arc deliverable filed before hand-back (r3 mitigation).
gap_or_blocker: none