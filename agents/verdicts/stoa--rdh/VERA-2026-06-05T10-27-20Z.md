status: completed
ticket: stoa--rdh
verdict: pass
design_artifact_verified_against: agents/design/stoa--rdh/design.md (+ substrate/arcs/arc-60-build-directive.md)
build_verified: arc-60/build @ 3a889dc (in-place on main working tree; branch NOT pushed — local committed state)
probes_executed:
- probe_id: P1
  description: The Marianne / fresh-clone install path — dry-run + real install.sh --target project to a scratch parent; every SKILL_NAMES entry resolves and deploys.
  quadrant_classification: easy-easy
  command_or_method: bash substrate/install.sh --target project --project-dir /tmp/stoa-arc60-scratch --dry-run THEN (for real); then test -f <scratch>/.claude/skills/<name>/SKILL.md for all 10 SKILL_NAMES entries.
  expected: dry-run exit 0; real exit 0; validation passes (every SKILL_NAMES dir+SKILL.md resolves); all 10 listed skills deployed; no obsolete-file warning for the new skills.
  observed: dry-run exit 0 (plan = "10 skills"); real exit 0 ("install.sh: done (applied)"); 10/10 deployed-skill lines emitted ending at interactive-html-preview; zero error/fail/warn/obsolete lines in output; all 10 SKILL.md files present in scratch deploy (P1_MISSING_FLAG=0); scratch skills dir contains EXACTLY the 10 (no decision-surface, no team-launcher).
  result: pass
- probe_id: P2
  description: decision-surface land-but-don't-deploy — committed+tracked in-repo, absent from SKILL_NAMES, absent from scratch deploy.
  quadrant_classification: easy-easy
  command_or_method: test -f substrate/skills/decision-surface/SKILL.md; git ls-files --error-unmatch <same>; grep decision-surface substrate/install.sh; test -e <scratch>/.claude/skills/decision-surface.
  expected: source present + git-tracked; NOT in install.sh; NOT in scratch deploy.
  observed: P2a committed-source OK; P2a' tracked OK; P2b absent from install.sh OK (grep no match); P2c absent from deploy OK.
  result: pass
- probe_id: P3
  description: interactive-html-preview deploys — present in SKILL_NAMES and landed in scratch deploy.
  quadrant_classification: easy-easy
  command_or_method: grep '^  interactive-html-preview$' substrate/install.sh; test -f <scratch>/.claude/skills/interactive-html-preview/SKILL.md.
  expected: line-anchored match in SKILL_NAMES; SKILL.md present in deploy.
  observed: P3a in SKILL_NAMES OK (line 236, exact anchored match); P3b deployed OK.
  result: pass
- probe_id: P4
  description: team-launcher matches the HELD decision (DAEDALUS option-a) — committed+tracked in-repo, absent from SKILL_NAMES, not deployed; launch-team.ps1 absent from consumer (dangling-ref guarantee).
  quadrant_classification: easy-easy
  command_or_method: test -f substrate/skills/team-launcher/SKILL.md; git ls-files --error-unmatch <same>; grep team-launcher substrate/install.sh; test -e <scratch>/.claude/skills/team-launcher; test -e <scratch>/launch-team.ps1.
  expected: source present + tracked; NOT in SKILL_NAMES; NOT deployed; launcher absent from consumer tree.
  observed: P4a committed-source OK; P4a' tracked OK; P4b absent from SKILL_NAMES OK; P4c not deployed OK; P4d launcher absent from consumer OK.
  result: pass
- probe_id: P5
  description: gen-data Zod-clean + all 3 skills in agents.ts + npm run build clean (no app regression).
  quadrant_classification: easy-easy
  command_or_method: cd app && npm run gen-data; grep -c each of 3 skill names in app/src/data/generated/agents.ts; git diff --quiet agents.ts (idempotency); npm run build.
  expected: gen-data exit 0, no Zod error; all 3 skill names present in agents.ts; build exit 0.
  observed: gen-data exit 0 (no Zod error; "roster: 2 MAJOR, 12 CAPTAIN, 12 LIEUTENANT"); decision-surface count=4, interactive-html-preview count=5, team-launcher count=2 (all >0, all 3 present); gen-data re-run produced NO diff vs committed agents.ts (committed output is exactly reproducible/Zod-clean); npm run build exit 0 (tsc -b + vite build; 1595 modules transformed; "built in 3.87s"; chunk-size note is pre-existing advisory, not a regression).
  result: pass
- probe_id: P6
  description: authorship invariant — author=Denson Smith in all 3 SKILL.md frontmatters and unchanged; commit diff touches no author-like field set to a non-Denson value and changes no pre-existing author field.
  quadrant_classification: easy-easy
  command_or_method: grep '^author:' on 3 SKILL.md (non-Denson filter); git show 3a889dc — line-anchored grep for added (+) and removed (-) author-like field assignments (author/authors/owner/creator/created_by/maintainer/maintainers/copyright/holder/vendor/publisher); git show 3a889dc header for commit Author identity.
  expected: all 3 SKILL.md author=Denson Smith; the only added author-field lines are the expected net-new "author: Denson Smith" lines; zero non-Denson values; zero changed pre-existing author fields; commit Author = PRINCIPAL git identity (not overridden).
  observed: P6a all 3 SKILL.md author = Denson Smith (non-Denson filter empty); P6b/d 4 added author-field lines, ALL "author: Denson Smith" (3 SKILL.md + 1 embedded in regenerated agents.ts), zero non-Denson additions; P6c zero removed/changed pre-existing author-field lines; commit Author = denson <densonsmith2@gmail.com> (not overridden); Co-Authored-By CAPTAIN_ADA_the-stoa seat trailer present. The net-new Denson additions are EXPECTED+CORRECT per the design interpretation note (§5 weak-point 1) — NOT a finding.
  result: pass
threat_coverage: []
methodology_concerns: |
  None of consequence. Notes for the record:
  (1) §35.5 carve-out CONFIRMED independently: this arc is additive skill-landing + install-manifest wiring with no runtime attack path / no credentialed-network / no PRINCIPAL-gate surface introduced. threat_coverage is empty by carve-out, not by omission. No threat-anchored probe was required or manufactured.
  (2) P6 design weak-point 1 (the coarse author-field tripwire fires on legitimate net-new Denson lines) was handled by reading the diff with a tightened line-anchored grep split into added-vs-removed and a non-Denson filter — confirming the 4 added lines are all Denson and zero pre-existing fields changed. No false finding.
  (3) The P1 "fresh-clone" probe tested the LOCAL committed branch state (3a889dc), not origin (branch is not pushed, per dispatch). Working tree == committed state for the in-scope paths, so install.sh run from the working tree is equivalent to a consumer install of the committed deliverable. This is the dispatch-stated intent.
falsifying_evidence_summary: ""
verification_artifacts_path: agents/verification/stoa--rdh/ (probe outputs captured inline in this verdict; scratch install dir /tmp/stoa-arc60-scratch created, asserted-against, and removed)
summary: |
  The Arc 60 deliverable (commit 3a889dc on arc-60/build) was exercised by a full install.sh smoke-test against a throwaway scratch parent plus mechanical source/diff assertions. The load-bearing probe is the per-skill deploy SPLIT (P1+P2+P3+P4 together): a real install deploys EXACTLY the 10 SKILL_NAMES entries — the 9 pre-existing + interactive-html-preview — while decision-surface (DRAFT-held) and team-launcher (the-stoa-internal-held, DAEDALUS option-a) are committed and git-tracked in-repo but NOT deployed to the consumer, and the launcher's dangling repo-root references never reach a consumer tree because the skill itself is not deployed. install.sh validation passed (exit 0 dry-run + real); gen-data is Zod-clean and reproduces the committed agents.ts byte-for-byte with all 3 skills discovered for the app (the SKILL_NAMES-vs-app-visibility decoupling the design imports, verified); npm run build exits 0 with no app regression. Authorship is clean: every author field = Denson Smith, the only author-field additions in the diff are the expected net-new Denson lines, no pre-existing author field was altered, and the commit Author is the PRINCIPAL's git identity (not overridden) with the ADA seat-identity trailer present. All six probes PASS. No falsifying evidence found.
