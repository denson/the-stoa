status: completed
ticket: stoa--xyb.8
verdict: PASS-WITH-RISKS
design_artifact_audited: C:/Users/denso/claude_projects/the-stoa/.claude/worktrees/arc-47-build/agents/design/arc-47/design-rev1.md

audit_block:
  headline_cross_ref_result: ONE broken cross-ref class found, but NOT inside substrate/ (the design section-3.1 grep covered substrate/ correctly; I re-verified it clean). The break is in SPECIFICATION.md at repo root, a live op-disc cross-ref consumer the design never grepped. Empirically confirmed by running the live validate-spec resolver (spec_refs.py): 7 currently-PASSING op-disc references (S7.1, S7.2, S7.4 x4, S7.5) flip PASS->FAIL after the S7 relocation, because spec_refs.py resolves by EXACT heading-line match and the S7.x subsection HEADINGS move into the module. Every in-substrate cross-ref resolves post-cut.

  risks:
  - id: r1
    description: The design cross-ref-preservation check (section 3.1) scoped its grep to substrate/ ONLY and never grepped SPECIFICATION.md (repo root), a live op-disc cross-ref consumer via the shipped validate-spec mechanical check. The S7 relocation moves the S7.1/S7.2/S7.4/S7.5 HEADING lines into the module, leaving only a S7 stub whose PROSE names them (not heading lines). spec_refs.py resolves section-refs by exact heading-line match, so 7 currently-PASSING op-disc references in SPECIFICATION.md flip PASS->FAIL in the Pass-9 mechanical check. P-XREF does NOT catch this (it excludes SPECIFICATION.md and greps stub prose, not headings).
    evidence: Ran spec_refs.py --spec SPECIFICATION.md --repo-root . on current source. S7.4 (spec line 67) PASS->op-disc 180; S7.1 (244)->121; S7.2 (252)->142; S7.4 (256/370/659)->180; S7.5 (260)->200. All currently PASS, all target S7.x subsection headings the relocation moves into modules/two-polybius-coordination.md. spec_refs.py line 11 + _find_anchor_in_target (line 230) require an exact heading match. Design section-3.1 grep scoped substrate/ (design lines 148, 331-333); SPECIFICATION.md is at repo root, never grepped. P-XREF (design 328-339) globs substrate/ only.
    load_bearing: true
    quadrant_classification: hard-easy
  - id: r2
    description: The A4 rationale for RELOCATING S15 (verification-complexity) is factually wrong. A4 claims S15 relocates because verifier CAPTAINs are dispatched-with-brief and PLINY names verification-complexity.md in the dispatch brief. In fact VERA 5.7, CATO 6.7, ZENO 6.6, ARGUS 6.6 each instruct the seat to consult op-disc S15 DIRECTLY from their own role files, on every dispatch -- identical mechanics to S25.5, which the design KEEPs inline under A3 (read-direct, no orchestrator dispatches the module). PLINY does NOT name the module in verifier briefs. By the design own A3 principle S15 is a KEEP candidate; S15 and S25.5 sit on opposite sides of a boundary that does not actually distinguish them.
    evidence: CAPTAIN_VERA.md:98; CAPTAIN_CATO.md:122; CAPTAIN_ZENO.md:103; CAPTAIN_ARGUS.md 6.6 all point at operating-disciplines.md S15 from the role file. Compare S25.5 consumers VERA 5.10 (line 159) + DAEDALUS 6.7, cited by the design as the A3-KEEP justification (design 104-105). Same direct-role-file-pointer mechanism for both.
    load_bearing: false
    quadrant_classification: easy-easy
  - id: r3
    description: design section 6.4 is internally inconsistent about the highest-risk function it modifies. It states Check A stays GLOBAL and the change is only to how _module_basenames is built. But Check A (install.sh:916, name in exists) shares the SAME exists[] array Check B iterates -- both built from module_list (install.sh:909). Narrowing module_list to the owned-set silently narrows Check A too, contradicting the stated intent and weakening Check A guarantee (marker references a real module source regardless of owner). ADA would implement imprecisely or deviate from the spec (unspecified delta).
    evidence: install.sh:906-909 BEGIN builds exists[] from module_list; :916 Check A tests name in exists; :945 Check B iterates the same exists[]. design 6.4 (486-520) asserts only _module_basenames changes and that Check A is naturally global. The two roles of exists[] (global existence for A vs owned-consumption for B/D) require two distinct sets; the design specifies one.
    load_bearing: false
    quadrant_classification: easy-easy
  - id: r4
    description: The S20 partial-section relocation re-inlines S20.1 (canonical pattern) AFTER S20.4 at subproject tier (the marker sits post-S20.4), so S20.1 physically follows S20.4 -- out of numeric order. At user/project tier the visible S20 sequence jumps intro->S20.2 with S20.1 pointed-to-not-present. Not lost canon (all content recoverable; external cross-refs are to S20/S20.3 which stay inline); a numbering-order wrinkle the design honestly flags as the one non-clean-stub relocation.
    evidence: design 3.2 note (lines 184, 194) places the marker after S20.4; live S20 (op-disc 1242-1329) has S20.1 before S20.2/3/4. External citers S20 (SPECIFICATION.md:569, BARTLEBY:130) and S20.3 (SPECIFICATION.md:568, MAJOR_POLYBIUS) both KEEP inline.
    load_bearing: false
    quadrant_classification: easy-easy

  non_findings:
  - Whole-team LOST-CANON: all 34 top-level sections dispositioned (KEEP-TIGHTEN or RELOCATE); independently walked S1-S34, no orphan. Ledger covers the whole file.
  - 18 C-1 provenance deletions (brief highest-drop-risk): spot-checked 17/18 standalone C-1 ids (nax/7kg/uc7/14u/148/p5g/v2o/a5q/odh/ioy/ezj/53u/ads/dxw/kjo/32b.3) -- ALL resolve and materially carry the deleted N=1 story. Per-deletion bw-show gate + VERA P-C1 re-verify downstream.
  - WP4 stoa--ntn (S10 progression, design did NOT confirm): RESOLVES and carries the operating-mode progression table. C-2 fallback not needed; build-Step-2 per-deletion content-check gate still correct.
  - Q2 (Check E literal MODULE-INLINE in a relocated body): op-disc source has ZERO literal MODULE-INLINE strings; no verbatim-extracted body can carry one; Check E (install.sh:922) catches ADA-introduced ones at deploy. CONCUR non-issue.
  - In-substrate cross-ref preservation: re-grepped whole substrate (excl arcs). Every cited op-disc SN + every cited subsection of a relocated section (S7.1/.2/.4/.5/.7, S15.4, S22.2, S27.2) is named in its parent stub prose; all top-level stubs preserve numbers. P-XREF passes for substrate/.
  - WP2 runtime-script citers: stop-self-check.sh:18, _hooklib.sh:28, pretooluse-*.sh, inspect-script-output/check.sh:176 cite SN in COMMENTS; _save_verdict.py:221/232/239 + check-bw-release/check.sh:161 cite SN in human-readable string MESSAGES; spec_refs.py:42 in a format-doc comment. NONE resolve SN as a runtime path. CONCUR no runtime break.
  - WP1 module-ownership partition: the Check-B global-glob false-positive is REAL and correctly diagnosed (install.sh:878 glob -> :945 Check B would fail 13 unmarked op-disc modules during POLYBIUS recompose -> abort). Per-call owned-set is the minimal correct partition; owner-subdirs correctly ruled out (flat-glob + Read-path convention + Arc-29 modules/custom reservation). Precision gap in how it is specified -> r3.
  - Ambient-vs-conditional A1 (S7), S11, S16, S17, S30: all genuinely conditional/episodic, not always-on-for-the-team. RELOCATE calls CORRECT. S7 cross-ref mechanism gap -> r1.
  - C-2 mechanism (bw create --parent -d): superior to README 5.2 bw attach for recoverability (titled id discoverable from the Anchor; attach needs git-log archaeology under bw-0.13.0 no-attachment-read). Sound.
  - KEEP always-on core: S6 thesis, S10 triggers, S12 cookbook keep-home (correctly NOT relocated despite length), S18 heartbeat, S19 confabulation, S20 credential structural core + 5-anti-pattern list, S25 PRINCIPAL-gate, S28 trailer, S33/S34 thin rules all stay inline. P-KEEP guards each. ~700-850 floor is honest output, not cut-too-shallow.
  - Ledger precision (buildability): line refs S21(1359)/S28.7(1800)/S12(800)/S14(820)/S32(2044) all exact vs live source. Buildable.

  q_adjudications:
    Q1: CONCUR. op-disc is not an orchestrator; relocation index (0.5) + per-stub Read pointers is the correct shape; no routing map needed. The per-stub Read pointer IS the point-of-need dispatch-time signal (confirmed by the S15 consumer pattern: verifier seats reach S15 via their own role-file pointers; on hitting a stub the stub Read pointer routes them). Aligned with modules/README 4.2. The A4 case does NOT argue for a routing-map-equivalent in op-disc; see r2.
    Q2: CONCUR (non-finding). Zero literal MODULE-INLINE strings in source; Check E catches ADA-introduced ones.
    Q3: PARTIAL CONCUR. A1 (S7 RELOCATE), A2 (S8.3-S8.5 KEEP), A3 (S25.5 KEEP) are correct. A4 (S15 RELOCATE) has a FACTUALLY WRONG rationale (r2): the A3/A4 boundary as drawn does not distinguish S15 from S25.5 -- both read-direct-via-role-file. PLINY adjudicates: flip S15 to KEEP (consistent with A3), or keep RELOCATE but drop the false PLINY-names-it-in-the-brief rationale and accept the extra-hop. S15 is conditional-by-audience, not always-on-for-the-team; SHOULD-FIX consistency, not lost canon.
    Q4: CONCUR with caveat (r4). The partial-section split is the honest call; losslessness holds; external cross-refs (S20, S20.3) preserved. Only wrinkle is numbering-order. CONSIDER, not blocking.

summary: design-rev1 is a structurally sound, buildable, lossless-on-canon cut of the worst-offender universal file. I independently walked all 34 op-disc sections, spot-checked 17/18 C-1 provenance cites (all carry their stories), cleared WP4 (stoa--ntn resolves), cleared Q2 (zero literal markers), re-verified in-substrate cross-ref preservation (clean), and confirmed the WP1 module-ownership partition correctly diagnoses a real install.sh Check-B regression. The provenance-deletion drop risk -- the brief primary concern -- is well-managed. ONE load-bearing finding: the design cross-ref check never grepped SPECIFICATION.md (repo root), a live op-disc cross-ref consumer via the shipped validate-spec mechanical check; the S7 relocation flips 7 currently-PASSING S7.x references to FAIL there (I ran the live resolver to confirm). Content-level canon is preserved (the S7 body moves intact to the module) but it is a real regression in the spec-validation surface that P-XREF cannot catch as written. Three SHOULD-FIX/CONSIDER items: a factually wrong A4 rationale for relocating S15, an internal inconsistency in 6.4 about whether Check A narrows with the owned-set, and a S20-partial numbering-order wrinkle. Posture: minor revisions -- cut method, ledger, recompose extension, and the lossless-completeness bar are all met; the gaps are cross-ref-coverage scope plus two precision/consistency points, none requiring re-architecting.

smallest_change_per_finding:
  r1: Add SPECIFICATION.md (repo root) to the cross-ref grep scope and to P-XREF. For the 7 S7.x refs (SPECIFICATION.md lines 67/244/252/256/260/370/659): (a) make the S7 stub keep S7.1-S7.7 as real HEADING lines so spec_refs.py resolves them -- preserves cross-ref AND the stub-relocate shape, ~7 stub heading-lines; OR (b) accept the FAILs as known drift (spec_refs.py exits 0) AND record them in the validate-spec known-drift set so Pass-9 does not regress silently. (a) is the lossless-cross-ref option.
  r2: Flip S15 to KEEP-TIGHTEN (consistent with S25.5/A3), OR keep RELOCATE and (i) delete the false PLINY-names-verification-complexity.md-in-the-brief rationale from 2.6/8/weak-point-3, replacing it with verifier-role-file-S15-pointers-hit-a-stub-that-Read-routes-to-the-module, and (ii) note the extra-hop in the S15 stub. The fix is correcting the rationale, not necessarily the disposition.
  r3: Specify in 6.4 that Check A must test global file existence (a separate global all-modules set, or a direct test -f modules_dir/name.md per marker) while Check B/D use the owned-set -- two distinct sets, not one. One added sentence + correct the claim that Check A is automatically global under single-arg-narrowing.
  r4: Optional: place the S20 marker so S20.1 re-inlines in numeric position, or relabel the relocated detail so the recomposed order reads coherently. Lowest priority; lossless either way.
