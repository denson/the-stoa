status: completed
ticket: stoa--xyb.8
verdict: PASS-WITH-RISKS
design_artifact_audited: agents/design/arc-47/design-rev2.md (committed 30bb8fb)
scope: FOCUSED rev2 re-confirm (r1 BLOCKING fix + r2/r3/r4 + Q2 adjudication + new-risk sweep). NOT a re-walk of the whole cut; rev1 audited PASS-WITH-RISKS, lossless-completeness MET whole-team, held.

audit_block:
  r1_through_r4_disposition:
  - id: r1
    finding: SPECIFICATION.md cross-ref regression via validate-spec exact-heading resolver (S7 relocation flips 7 PASSing S7.x cites to FAIL under rev1 prose-only stubs)
    rev2_fix: S7 stub keeps S7.1/S7.2/S7.3/S7.4/S7.5/S7.7 as REAL markdown heading lines + bodies relocate; new probe P-SPEC-XREF runs shipped resolver + asserts ZERO at-risk refs flip PASS to FAIL
    argus_disposition: CLEARED
    independent_verification: |
      Re-ran spec_refs.py against LIVE source (212 refs).
      (a) HEADING SHAPE: imported spec_refs, ran the heading-pattern matcher against each of the 6
          proposed S7 stub heading lines (### 7.1 Radio-check protocol, etc.) - ALL 6 MATCH. The
          Relocated-to-module pointer lines do NOT match (correct). The S7 stub shape is sound.
      (b) CENSUS: the COMPLETE set of subsection cites targeting a RELOCATED section = exactly
          S7.1 (L244) / S7.2 (L252) / S7.4 (L67,256,370,659) / S7.5 (L260) = 7 PASS cites, ALL S7.
          CONFIRMED no other relocated section (S14/S16/S17/S22/S27/S29/S30/S31/S32) has a PASS
          subsection cite. S11. (L581) and S20.3 (L568) are top-level/KEEP, safe. DAEDALUS census
          (S3.1.2) is correct. S7.3 cites at SPEC L258/L533 resolve to SPECIFICATION.md-self and
          MAJOR_POLYBIUS, NOT op-disc; the S7 stub keeps S7.3 heading for uniformity anyway.
      (c) P-SPEC-XREF runs the shipped resolver + asserts the 7 at-risk refs PASS. Correct mechanical
          guard; the probe at-risk set matches the live census exactly.
      (d) PRE-EXISTING FAIL: S7.1. (L129, trailing-period) matches NO current op-disc heading (pre-cut
          FAIL) and would NOT match the post-cut S7.1 heading either (stays FAIL). Genuinely
          pre-existing, correctly scoped OUT (S9). P-SPEC-XREF correctly excludes it.
  - id: r2
    finding: S15 wrongly RELOCATE in rev1 on a FACTUALLY WRONG A4 rationale (PLINY-names-module-in-brief)
    rev2_fix: S15 flipped RELOCATE to KEEP-TIGHTEN inline; false A4 deleted; A3 generalized to read-direct-via-role-file means KEEP, covering both S15 and S25.5
    argus_disposition: CLEARED
    independent_verification: |
      Confirmed live: VERA S5.7, CATO S6.7, ZENO S6.6, ARGUS S6.6 ALL cite operating-disciplines.md
      S15 DIRECTLY from their own role files (path in backticks; grepped substrate CAPTAIN files).
      These are read-direct dispatch-time reads, NOT a module any orchestrator hands them - identical to
      S25.5 (A3 KEEP). A3-generalization correct; false A4 correctly deleted. No cross-ref effect: S15
      spec cite is top-level only (L202 PASS, no S15.x subsection spec cite); S15.4 cited from
      save-verdict SKILL + script resolves to the live S15.4 heading either way. Ledger self-consistent.
  - id: r3
    finding: rev1 S6.4 internally inconsistent - Check A stays global only B/D change, but Check A shares the single exists array narrowed by module_list
    rev2_fix: S6.4 specifies TWO distinct sets - GLOBAL global_exists (Check A) + per-call OWNED owned/consumed/nowned (Checks B/D); new probe P-OWNERSHIP-CHECKA
    argus_disposition: CLEARED
    independent_verification: |
      Verified against LIVE install.sh L871-957: current function takes ONE arg, builds ONE
      module-basenames from the global glob, single exists array backing Check A (L916), Check B (L945),
      Check D (L944). r3 diagnosis correct. The two-set fix is sound: Check A tests name-in-global-exists
      (owner-agnostic, does NOT narrow with arg2); Checks B/D iterate/count owned. The consumed=1 on
      every OPEN marker (L926) is harmless for a cross-owner name (Check B only iterates over owned).
      Basenames unique (5 POLYBIUS + 12 op-disc, no collision). Mis-authored-cross-owner-marker
      correctly scoped OUT (cannot arise from the data-driven call sites). P-OWNERSHIP-CHECKA +
      P-OWNERSHIP + P-RECOMPOSE + P-RECOMPOSE-NEG exercise it.
  - id: r4
    finding: S20 partial-relocation places detail-marker after S20.4, so relocated S20.1 re-inlines out of numeric order at subproject tier
    rev2_fix: confirmed lossless, left as-is with explicit do-not-renumber note (S3.2)
    argus_disposition: CLEARED
    independent_verification: |
      Only external citers of S20 are S20 (L569 PASS) and S20.3 (L568 PASS), both KEEP inline. The
      relocated detail (S20.1/S20.5/S20.6) has NO external cross-ref, so the out-of-order re-inline is
      cosmetic and lossless. The do-not-renumber note correctly prevents a churn-renumber that would
      risk future cross-ref drift. CONSIDER, resolved.

  q2_adjudication:
    question: census-driven heading-preserve (real heading lines ONLY for S7, the only spec-cited subsections TODAY) vs defensively preserving ALL relocated subsection headings (robust to FUTURE spec cites)
    recommendation: CONCUR census-driven. Do NOT blanket-preserve.
    reasoning: |
      (1) The validate-spec resolver is the ONLY mechanical exact-heading consumer; all other citers
      (S22.2/S27.2 from check.sh; comment/message lines) are human-prose, resolving against stub PROSE
      DAEDALUS already provides. Blanket heading-lines would bloat every stub for a hypothetical the
      live census shows does not exist. (2) P-SPEC-XREF is a STANDING gauntlet check - a future
      SPECIFICATION.md cite to S22.2/S27.2 surfaces as a NEW FAIL on the next validate-spec run, NOT
      silently; drift is caught at introduction. (3) The universal-file risk is about losing always-on
      RULES (KEEP bucket + P-KEEP cover that), not future spec cites to relocated CONDITIONAL
      subsections. Minimal-but-guarded is correct. One refinement: see r5.

  risks:
  - id: r5
    description: polling-cron-prompt-template.md L263 prose-points readers at op-disc S7.6 (the empirical-lineage subsection); S7.6 relocates to a PROVENANCE Anchor with NO stub heading (design line 166 + S3.3), so the pointer dangles post-cut.
    evidence: substrate/templates/polling-cron-prompt-template.md:263 (op-disc S7.6 prose pointer); design-rev2.md line 166 (S7.6 gets NO stub heading) + S3.3 (S7.6 rolled into the S7 move / Anchor, not a slim-core stub)
    load_bearing: false
    quadrant_classification: easy-easy
    rationale: |
      Non-load-bearing: the S7.6 empirical lineage is PRESERVED (moves with the S7 body / to an Anchor;
      recoverable canon, not LOST canon). The break is a dangling DOC pointer in a substrate template,
      not a mechanical FAIL (the resolver only walks SPECIFICATION.md, not templates, so P-SPEC-XREF
      does not flag it). Same shape as the S22.2/S27.2 prose-named approach DAEDALUS accepted - but
      unlike those (S22/S27 stub PROSE re-anchors them), this S7.6 pointer has no slim-core anchor to
      land on once S7.6 ceases to be a heading. Fix is one line: re-point template L263 to the S7.6
      Anchor during the Step-5 cross-ref sweep. Flagged per fix-known-bugs-now (cheap in-arc; should
      not cross the build boundary unaddressed). ADA: add a re-point-L263-S7.6-to-Anchor item to the
      Step-5 sweep checklist.

  non_findings:
  - "r1 census completeness - independently re-ran the resolver; the 7 S7.x at-risk cites are the COMPLETE set; no missed relocated-section subsection cite."
  - "S7 stub heading SHAPE - all 6 proposed heading lines match the resolver heading pattern; shapes correct as written in S2.7.1 (not merely a transcription risk)."
  - "S7.1. (L129) malformed cite - genuine pre-existing FAIL (pre- AND post-cut); correctly scoped OUT to S9."
  - "S7.3 spec-cite ambiguity - L258 self-resolves to SPECIFICATION.md, L533 is MAJOR_POLYBIUS S7.3; neither targets op-disc S7.3; S7 stub keeps S7.3 heading anyway."
  - "r2 verifier-seat read-direct claim - confirmed all 4 (VERA/CATO/ZENO/ARGUS) cite op-disc S15 direct via role file; A3 generalization sound; A4 correctly deleted."
  - "S15.4 cross-ref under KEEP - cited from save-verdict skill + script; resolves to live S15.4 inline; no module-vs-inline effect (answers DAEDALUS Q4)."
  - "r3 consumed cross-owner case - harmless (Check B iterates owned only); mis-authored case correctly out of scope."
  - "S22.2/S27.2 prose-named - citers are check.sh comment/message lines, not the resolver; prose-naming sufficient; census-driven scope correct (Q2)."
  - "Ledger count self-consistency - 12 CONDITIONAL + 27 PROVENANCE + 0 DUPLICATE = 39; 19 standalone C-1 + 3 C-2 + 5 SPLIT + 7 move-with; consistent post-S15-flip."
  - "Module basename uniqueness - 5 POLYBIUS + 12 op-disc, zero overlap; GLOBAL existence set = 17."

summary: |
  rev2 is a clean, targeted fix of rev1 r1 (BLOCKING) + r2/r3/r4 - NOT a re-cut, exactly as briefed.
  All four findings CLEAR under independent live verification: r1 S7 stub heading-preserve is
  mechanically sound (all 6 proposed heading lines match the shipped resolver pattern; the census of
  spec-cited relocated subsections is exactly the 7 S7.x cites DAEDALUS claims, re-run live), r2
  S15-flip-to-KEEP is grounded (all 4 verifier seats cite op-disc S15 direct via role file; false A4
  deleted; no cross-ref effect), r3 two-set recompose is the correct minimal fix to the real
  single-array narrowing (verified against live install.sh L871-957), and r4 S20 numbering-order is
  genuinely lossless. On Q2 I CONCUR with census-driven (not blanket) subsection-heading preserve: it
  is the minimal lossless scope and P-SPEC-XREF is a standing guard catching any future drift at
  introduction. ONE new risk, NON-load-bearing: an existing substrate template
  (polling-cron-prompt-template.md L263) prose-points at op-disc S7.6, which relocates to an Anchor with
  no stub heading, so that pointer dangles post-cut (recoverable canon, not lost canon; one-line
  re-point during the Step-5 sweep). No new LOST-CANON vector, no new mechanical cross-ref break, no new
  always-on-rule drop. rev2 is ADA-BUILDABLE; the r5 re-point should be added to the Step-5 sweep
  checklist before build close. Overall posture: clean, minor-revision (one-line doc re-point,
  non-blocking).
