status: completed
ticket: stoa--xyb.4
verdict: revise
design_artifact_audited: .claude/worktrees/arc-44-build/agents/design/arc-44/design-rev1.md
audit_block:
  risks:
  - id: r1
    description: Arc-2 lossless-home taxonomy covers ONLY the CONDITIONAL bucket (5 disk modules for POLYBIUS sec 5/10/11/12/14). The .3 method has THREE relocation buckets; the PROVENANCE-to-bw-cite destination (POLYBIUS sec 4/16.6/16.7/17.5/17.6/18.5/18.6 N=1-provenance + Cross-references subsections) is treated only as a delivery CHANNEL, not as a provenance-archive destination with an authoring/cite-back discipline. Arc 2 hits ~330 lines of provenance content (per .3 LINE MATH) whose lossless home is bw-cite; the mechanism does not define how it is archived to bw, the cite-back convention, or where the cite lives in slim core.
    evidence: design sec 3.G + sec 2 channel table vs bw show stoa--xyb.3 (PROVENANCE archived in bw CITED not inlined; N=1-provenance + Cross-references in 4/16-19 are tier-3 in always-loaded path) + epic sequence (cite-provenance 4/16/17/18/19); shipped at MAJOR_POLYBIUS.md sec16.6 L1022 sec16.7 L1034 sec17.5/17.6 sec18.5/18.6.
    load_bearing: true
    quadrant_classification: easy-easy
  - id: r2
    description: Routing-map format (Decision D) is task-type -> module(s) -> channel. It expresses CONDITIONAL relocations + inline/bw rows, but NOT the DUPLICATE->consolidate+pointer bucket (sec 7.3 bw-cookbook -> op-disc sec 12) nor the PROVENANCE->bw-cite bucket as first-class entries. A reader of the slim-core routing map cannot see deleted-content-lives-at-op-disc-sec12 or provenance-archived-at-bw-X. If the routing map is the index of what-was-relocated-where (REFINEMENT 1 intent), it is incomplete for 2 of 3 buckets.
    evidence: design sec 2.4 / sec 3.D routing-map columns vs bw show stoa--xyb.3 three-bucket method; REFINEMENT 1 (epic 2026-05-23T05:52:53Z) = routing map / module INDEX stays in always-loaded operational core.
    load_bearing: true
    quadrant_classification: easy-easy
  - id: r3
    description: op-disc sec 33 insertion-point is underspecified and the stated EOF-append is structurally wrong. operating-disciplines.md does NOT end at sec 32 with numbered content -- sec 32 ends L2087, followed by UN-numbered trailing sections Agent-regime-inverses (L2089) and Empirical-lineage (L2100-2110). Appending sec 33 at EOF (design + DAEDALUS heartbeat both say ends-L2110-appends-as-sec33) places numbered canon AFTER un-numbered closing matter, breaking structure. sec 33 must go after sec 32 (L2087) and BEFORE Agent-regime-inverses (L2089). Build spec sec 4 + probe P6 (header-grep only) do not name this.
    evidence: operating-disciplines.md L2042 (sec32 start) L2087 (sec32 end) L2089 (## Agent-regime inverses) L2100 (## Empirical lineage) L2110 (EOF) vs design sec1 + sec3.B + DAEDALUS heartbeat 06:01:30Z + probe P6 (grep header only).
    load_bearing: true
    quadrant_classification: easy-easy
  - id: r4
    description: Decision A explicit MODULE_NAMES array creates a real silent-deploy-gap AND fights the growing-library thesis more than conceded. Author a module source, forget the array line, it never deploys, no error (existence-check only fires for entries already IN the array). DAEDALUS argues same-risk-as-CAPTAIN/template/skill -- but those are stable rarely-grown classes, whereas modules are the ONE class the epic is designed to grow continuously (Decision E accretion = recurrence -> author module -> step3 add to MODULE_NAMES). Manual array on the most-frequently-grown class maximizes exposure to its weakest failure. Smoke-beat P4 greps for README.md TODAY; cannot catch a future authored-but-not-arrayed module.
    evidence: design sec3.A + sec8 weak-point1 + probe P4 (greps README.md) vs Decision E sec3.E step3 + epic growing-library framing.
    load_bearing: uncertain
    quadrant_classification: hard-easy
  - id: r5
    description: Subproject-tier module deploy (deploy at all 3 tiers like skills) rests on a contested platform assumption the design rates ~80%. The skills-subproject precedent (install.sh L52-60) is about SKILL TOOL discovery resolution NOT Read-tool path resolution -- modules are Read-by-explicit-path, so the precedent is not directly transferable. Web search confirms Read-relative-path resolution in sub-agents/subprojects is actively contested/buggy (claude-code 4754 15627 18200 11374). NOT load-bearing for Arc 1 (the one real README deploys+verifies at project tier regardless; subproject correctness only matters when Arc2+ subproject orchestrators dispatch module paths). Should be a TRACKED OPEN QUESTION gating Arc 2, not an Arc-1 blocker.
    evidence: design sec3.A subproject para + sec8 weak-point4 (~80%); install.sh L52-60 (skills = Skill-tool discovery) + L595 vs L594 (DEST_TEMPLATES_DIR empty); WebSearch claude-code 4754/15627/18200/11374.
    load_bearing: false
    quadrant_classification: easy-hard
    domain_blind_spot: Exact Claude Code Read-tool path-resolution root for a sub-agent in a subproject is platform behavior ARGUS cannot fully verify from repo+docs; docs/issues describe it as unsettled. Warrants a live probe before Arc 2 relies on subproject modules.
  - id: r6
    description: sec 33 minimality vs the debloat thesis is a genuine but bounded tension. Adding ~25-40 lines to the 2110-line worst-offender is net-additive to the file the epic most wants to shrink. The sec32 cross-ref house style (HTML cite-comments + bullet list ~7 lines at L2079-2085) pushes sec33 toward 40. Self-flagged (weak point2). Mitigating: sec33 is operational-core canon legitimately always-loaded, and op-disc own future cut (Arc4) is where it shrinks. Bounded: trim cross-ref block to one pointer line if sec33 exceeds ~30 lines.
    evidence: design sec2.2 + sec3.B + sec8 weak-point2 vs operating-disciplines.md sec32 cross-ref style L2079-2085; epic LINE MATH.
    load_bearing: false
    quadrant_classification: easy-easy
  - id: r7
    description: Thin-rule/detail split decision-time-availability check -- discharged as non-finding-grade. Load-bearing operational rules DO stay in sec33 always-loaded (3 channels, routing-map-stays-in-core rule, recurrence-authoring signal). The one arguably-decision-time item in README (channel-selection table) is redundantly covered by sec33 naming all 3 channels with one-line use-when; README detail is for authoring not routing.
    evidence: design sec2.2 / sec3.B + sec3.C selection table in README.
    load_bearing: false
    quadrant_classification: easy-easy
  non_findings:
  - IA-1 routing-map-in-orchestrator-core vs REFINEMENT 1 CONSISTENT. Populated map inline in orchestrator slim core IS in-core; convention in op-disc sec33 universal. Holds.
  - write_substrate_manifest (L396-453) needs NO modules entry CONFIRMED. Records only substituted files (MAJORs+CAPTAINs); templates+skills absent (verbatim). Modules verbatim-like-templates correct.
  - Refinement-2 clean-diff sound -- additions to install.sh + op-disc sec33 + new modules/README.md + design file only; P9 probeable. No role-file CONTENT cut. Holds subject to ADA honoring P9.
  - No-Skill/no-MCP (stoa--xyb.1) respected -- all 3 channels Bash/Read/bw; modules use Read not Skill tool; sec7 excludes Skill mechanism.
  - Credential discipline genuinely N/A -- install.sh deploys local files, no tokened op. P10 states N/A explicitly.
  - Authorship clean -- design header Author Denson Smith; README no author frontmatter (consistent with templates). No author-like field names anyone else.
  - install.sh line-ref drift in sec4 (SKILL_NAMES cited L142-151 actual L142; staleness ~L1013 actual L987-1014) -- minor citation drift, idioms hold; locate by idiom. Not a finding.
  - Modules staleness block sec4.6 omits custom-asterisk skip unlike skills -- correct for Arc1, no custom-module convention exists yet.
  summary: Well-grounded mostly-sound composition-layer mechanism (modules/ deploy class parallel to TEMPLATE_NAMES; thin op-disc sec33 + on-demand modules/README.md dogfood split; three Bash/Read/bw channels; routing-map inline per REFINEMENT 1). Clean-diff, no-Skill/no-MCP, credential-N/A, authorship, manifest-writer, IA-1 all check out. Posture MINOR-TO-STRUCTURAL, not a rebuild. Top risk r1: Arc-1 to Arc-2 contract incomplete on EPIC terms -- Arc 1 must give Arc 2 lossless homes for ALL THREE .3 buckets but taxonomy+routing-map only fully home the CONDITIONAL bucket. PROVENANCE-to-bw-cite (~330 lines) is a generic channel not an archive destination with cite-back; routing map cannot express duplicate-pointer or provenance-cite buckets as index entries (r2) -- at Arc 2 two of three relocation classes lack a first-class home/index slot, risking content-drop or an incomplete routing index that defeats REFINEMENT 1. Plus build-correctness defect r3: sec33 EOF-append places numbered canon after un-numbered trailing matter. r1/r2/r3 blockers; r4 (array vs growing-library uncertain), r5 (subproject Read-resolution = tracked Arc-2-gating open question NOT Arc-1 blocker), r6 (sec33 minimality) weaker. Smallest fixes: (a) extend sec3.G/sec3.C to define bw-as-provenance-archive + inline cite-back for PROVENANCE and state DUPLICATE home = existing consolidation target (no module), giving all 3 buckets explicit homes; (b) extend routing-map (or add sibling relocation-index) so duplicate-pointer + provenance-cite relocations are expressible index entries; (c) specify sec33 insertion after-sec32/before-Agent-regime-inverses + add placement assertion to P6.
