status: completed
ticket: stoa--xyb.4
verdict: pass
design_artifact_verified_against: agents/design/arc-44/design-rev2.md
build_verified: arc-44/build @ d3735c6f0de78cae89e7c2b549bdd0d54a526530 (worktree .claude/worktrees/arc-44-build)
probes_executed:
- probe_id: P0
  description: install.sh syntax check on the built script.
  quadrant_classification: easy-easy
  command_or_method: "bash -n substrate/install.sh"
  expected: exit 0
  observed: exit 0
  result: pass
- probe_id: P1
  description: source file substrate/modules/README.md exists (the one real Arc-1 content file).
  quadrant_classification: easy-easy
  command_or_method: "test -f substrate/modules/README.md"
  expected: exit 0
  observed: exit 0; ls shows README.md is the ONLY *.md in substrate/modules/ (no Arc-2 content files).
  result: pass
- probe_id: P2
  description: install.sh --dry-run shows the module deploy plan line AND the run_or_print cp line.
  quadrant_classification: easy-easy
  command_or_method: "bash <clone>/substrate/install.sh --target project --project-dir <throwaway> --dry-run | grep 'deploy modules|[dry-run] cp'"
  expected: stdout contains 'deploy modules : yes (1 module(s) ...)' AND '[dry-run] cp ... modules/README.md'
  observed: BOTH present. 'deploy modules   : yes (1 module(s) to <target>/.claude/modules)' and '[dry-run] cp "<src>/modules/README.md" "<target>/.claude/modules/README.md"'.
  result: pass
- probe_id: P3
  description: real deploy (no --dry-run) lands README.md at project-tier .claude/modules/ destination.
  quadrant_classification: easy-easy
  command_or_method: "bash <clone>/substrate/install.sh --target project --project-dir <throwaway> ; test -f <throwaway>/.claude/modules/README.md"
  expected: install exit 0; dest README exists.
  observed: install exit 0; 'deploy module: README.md' enumerate/log line in stdout (r4 observability confirmed); <throwaway>/.claude/modules/README.md exists (exit 0).
  result: pass
- probe_id: P4
  description: deploy COMPLETENESS — every substrate/modules/*.md source basename present in dest.
  quadrant_classification: easy-easy
  command_or_method: "for s in <src>/modules/*.md; do test -f <dest>/.claude/modules/$(basename $s) || fail; done"
  expected: every source *.md present in dest; fail=0.
  observed: fail=0 (README.md OK in dest).
  result: pass
- probe_id: P4b
  description: GLOB PROOF — a 2nd *.md dropped into a COPY of the source auto-deploys with NO install.sh edit (confirms deploy is glob-based, not a hardcoded array). Brief-mandated.
  quadrant_classification: easy-easy
  command_or_method: "echo > <CLONE>/substrate/modules/zz-vera-probe-module.md ; re-run install --target project ; test -f <dest>/.claude/modules/zz-vera-probe-module.md"
  expected: plan line counts 2 modules; new file gets its own 'deploy module:' log line; lands in dest; zero install.sh edit.
  observed: plan line went '1 module(s)' -> '2 module(s)'; 'deploy module: zz-vera-probe-module.md' logged; file landed in dest exit 0. Glob discovery confirmed (NOT array). (Done on a throwaway CLONE, never the tracked source.)
  result: pass
- probe_id: P5
  description: empty source modules dir errs loudly with nonzero exit (glob loud-fail-on-empty escape hatch).
  quadrant_classification: easy-easy
  command_or_method: "rm -f <CLONE2>/substrate/modules/*.md ; bash install.sh --target project --project-dir <t2>"
  expected: 'error: no module sources found: .../modules/*.md'; nonzero exit.
  observed: 'install.sh: error: no module sources found: <...>/modules/*.md'; exit=2.
  result: pass
- probe_id: P6a
  description: op-disc §33 exists.
  quadrant_classification: easy-easy
  command_or_method: "grep -nE '^## 33\\. Composition layer' substrate/operating-disciplines.md"
  expected: matches at one line.
  observed: line 2089 '## 33. Composition layer — instruction modules + orchestrator routing'.
  result: pass
- probe_id: P6b
  description: "§33 PLACEMENT (r3 fix) — §33 sits AFTER §32 content AND BEFORE the un-numbered '## Agent-regime inverses' section (NOT EOF-append)."
  quadrant_classification: easy-easy
  command_or_method: "grep -nE '^## 32\\.|^## 33\\.|^## Agent-regime inverses' substrate/operating-disciplines.md ; compare line numbers"
  expected: L(§33) > L(§32) AND L(§33) < L(Agent-regime inverses).
  observed: L(§32)=2042, L(§33)=2089, L(Agent-regime inverses)=2115. 2042 < 2089 < 2115. Correctly placed between §32 and the un-numbered trailing matter; NOT EOF (EOF=2136). r3 fix confirmed.
  result: pass
- probe_id: P6c
  description: "§33 ≤30 lines (r6 hard cap) AND cross-ref is a single pointer (not a §32-style block)."
  quadrant_classification: easy-easy
  command_or_method: "awk 'NR>=2089 && NR<2115' op-disc | wc -l ; grep -c '<!--|Cross-ref' in §33 range"
  expected: §33 section ≤30 lines; zero HTML-comment/Cross-references block in §33; single README pointer line.
  observed: §33 = 26 lines (header through line before next section; content-only 18-23). UNDER ≤30 cap, no trim needed. ZERO HTML comments / zero 'Cross-references' block in §33 range (contrast §32 has 4 block markers). Single README pointer line present (line 2111). Single-pointer constraint (r6) satisfied.
  result: pass
- probe_id: P7
  description: canon internal consistency — §33 ↔ README agree on 3 channels, 3 relocation classes, README pointer.
  quadrant_classification: easy-easy
  command_or_method: "read §33 + README; compare channel names, class names, pointer target."
  expected: §33's 3 channels == README §2's 3 channels; §33's 3 classes == README §5's 3 classes; §33 points to .claude/modules/README.md.
  observed: MATCH. §33 channels (inline / disk module Read / bw show|attach) == README §2 (Bash/Read/bw). §33 classes (CONDITIONAL/PROVENANCE/DUPLICATE) == README §5 table + §5.1/§5.2/§5.3. §33 line 2111 points to .claude/modules/README.md. No contradiction.
  result: pass
- probe_id: P3-readme-a
  description: "ARGUS-carry-1 — per-C-1 bw show content-check losslessness gate present in README PROVENANCE section."
  quadrant_classification: easy-easy
  command_or_method: "grep -nE 'REQUIRED losslessness gate|content-check that the cited ticket|MUST run per-deletion' substrate/modules/README.md"
  expected: gate text present in §5.2 PROVENANCE section.
  observed: README lines 176-181 — 'REQUIRED losslessness gate for the C-1 common case (probe-requirement carried into the cut): ... content-check that the cited ticket actually contains the full story ... it MUST run per-deletion before any inline prose is removed.' Present in §5.2. ARGUS-carry-1 SHIPPED.
  result: pass
- probe_id: P3-readme-b
  description: "3 relocation classes each have a worked template; routing-map + relocation-index formats present; Arc-2 taxonomy names exactly 5 modules with no content files."
  quadrant_classification: easy-easy
  command_or_method: "grep section headers/templates/taxonomy rows in README; count *.md rows; count source *.md files."
  expected: §5.1/§5.2/§5.3 each worked; routing-map (§4.1) + relocation-index (§4.2) code-fence templates present (relocation-index has 1 worked row per class); §6 taxonomy = 5 module rows; source has only README.md.
  observed: §5.1 CONDITIONAL (template ref §4.1), §5.2 PROVENANCE (C-1/C-2 + Anchor format template), §5.3 DUPLICATE (bw-cookbook worked example). Routing-map template at README line 92, relocation-index template at line 110 with 3 worked rows (one per class). §6 taxonomy = exactly 5 module rows (onboarding, sub-project-spawning, pair-programmer-authoring, pair-programming-prototyping, substrate-update-check). Source substrate/modules/ contains ONLY README.md (1 *.md). No content files for the 5 named modules.
  result: pass
- probe_id: P8
  description: staleness scan (glob-based) flags a non-source file in dest .claude/modules/.
  quadrant_classification: easy-easy
  command_or_method: "echo > <dest>/.claude/modules/bogus.md ; re-run install ; grep 'Obsolete files detected' output for bogus.md"
  expected: bogus.md (not in source glob) listed under 'Obsolete files detected at destination'.
  observed: 'Obsolete files detected at destination (not in current substrate): - <dest>/.claude/modules/bogus.md' AND the removed-from-source probe module both flagged. Glob-based staleness (dest vs source glob) confirmed.
  result: pass
- probe_id: P-subproject
  description: subproject tier deploys NO modules (DEST_MODULES_DIR='').
  quadrant_classification: easy-easy
  command_or_method: "bash install.sh --target subproject --parent-dir <p> --subproject subx --dry-run | grep 'deploy modules|modules deployment skipped'"
  expected: plan line 'deploy modules : no (subproject mode ...)'; deploy step 'modules deployment skipped (subproject mode ...)'.
  observed: 'deploy modules   : no (subproject mode — see stoa--xyb.4 §6 open question)' AND '[dry-run] modules deployment skipped (subproject mode ...)'. Subproject deploys zero modules, mirrors DEST_TEMPLATES_DIR='' pattern. Confirmed.
  result: pass
- probe_id: P9
  description: "clean diff — zero deletions of role-file CONTENT; only the 4 expected files touched; no Arc-2 module content files; no author-like field changed."
  quadrant_classification: easy-easy
  command_or_method: "git diff --name-status base..d3735c6 ; git diff base..d3735c6 -- 'substrate/MAJOR_*.md' 'substrate/CAPTAIN_*.md' | grep -c '^[-+]' ; author-field audit of added lines."
  expected: only install.sh (M) + op-disc (M) + modules/README.md (A) + design docs (A) touched; zero role-file changes; only 'Author: Denson Smith' added.
  observed: changed = design-rev1.md (A), design-rev2.md (A), install.sh (M), modules/README.md (A), operating-disciplines.md (M). Role files (MAJOR_*.md / CAPTAIN_*.md) entirely untouched (grep count 0). install.sh's 3 deletions are header doc-comment rewording (not content cuts). op-disc diff is additions-only (0 deletions). Only author-like field added is 'Author: Denson Smith' (design docs); README carries no author frontmatter (consistent with templates/*.md). No Arc-2 module content files. Clean diff held.
  result: pass
- probe_id: P-drift
  description: "LOAD-BEARING — verify ADA's drift call: design §3.C cited 'bw show --attachments <id>' which ADA found does NOT exist in shipped bw 0.13.0; ADA built README §5.2 to ship reality (git-on-beadwork-ref recovery for bw attach'd files, bw show <id> for comment prose). Confirm the drift call is correct AND the documented recovery path works."
  quadrant_classification: easy-hard
  command_or_method: "bw --version ; bw show --help ; bw --help ; bw attach --help ; isolated attach round-trip: bw create -> bw attach <id> <file> --name story/x.txt -> git show beadwork:attachments/<id>/story/x.txt"
  expected: no --attachments flag on bw show; no attachment-read subcommand; documented git-recovery path recovers the exact bytes.
  observed: "bw 0.13.0. 'bw show --help' has only --json and --only (NO --attachments). 'bw attach --help' is write-only (Read <file-path> from disk and store its bytes at attachments/<id>/<stored-path>; no read-back subcommand exists in the full command list: create/show/list/update/start/close/reopen/delete/comment/label/defer/undefer/history/attach/ready/blocked/dep/sync/export/import/recap/registry/init/config/upgrade/onboard/prime). DRIFT CALL CORRECT. Recovery round-trip PROVEN in an isolated throwaway bw repo: bw attach stored bytes at attachments/<id>/story/provenance-story.txt on the beadwork ref; 'git show beadwork:attachments/<id>/story/provenance-story.txt' recovered the EXACT bytes (exit 0). README §5.2's documented git-on-beadwork-ref recovery WORKS."
  result: pass
methodology_concerns:
- "README §5.2 minor inaccuracy (NON-BLOCKING, but a concrete documentation defect with a concrete fix). Lines 196-197 say: 'the attach intent comment in bw show <id> names the stored path.' Live test against bw 0.13.0 shows this is FALSE: the attach intent is recorded as a git COMMIT MESSAGE on the beadwork ref ('attach <id> <stored-path>', discoverable via 'git log beadwork'), NOT surfaced in 'bw show <id>' (verified: 'bw show <id> --only comments' is empty; 'bw show <id> --json' carries no attach/stored-path field). A future Arc-2 cutter following the README literally to find the stored path via 'bw show <id>' would not find it. The CORE recovery instruction (git show beadwork:attachments/<id>/<stored-path>) is correct and proven; only the stored-path-DISCOVERY pointer is wrong. Recommended one-line fix for the next dispatch (NOT a VERA edit — independence): change 'the attach intent comment in bw show <id> names the stored path' to 'the attach intent commit on the beadwork ref (git log beadwork: \"attach <id> <stored-path>\") names the stored path.' Per CLAUDE.md fix-known-bugs-now, route to ADA for a trailing one-line README fix on this branch."
- "P-drift quadrant is easy-hard (behavioral claim about a tool): the no-flag/no-subcommand facts are easy-verify (help output is deterministic), and the round-trip recovery was proven for the canonical case. Not marked INCOMPLETE because the recovery behavior is deterministic for the documented path (git show on the beadwork ref), not configuration-dependent — a single round-trip is conclusive evidence for the documented mechanism. Confidence: HIGH on the drift call and the core recovery path; the one inaccuracy above is the only sub-claim that failed and it is documentation, not mechanism."
falsifying_evidence_summary: ""
verification_artifacts_path: "All probes run against THROWAWAY clones/dirs under the OS temp dir (git clone --no-local of the build branch + scratch --project-dir deploy targets + an isolated bw repo for the attach round-trip), per op-disc §25.5 — the real worktree was never mutated. All throwaway artifacts cleaned up post-run; no probe scripts retained on disk (probes were inline shell, recorded verbatim in probes_executed above)."
summary: "Verified the Arc 44 (debloat Arc 1) composition-layer build at d3735c6 against design-rev2's full probe set (P1-P10) plus the brief's 4 explicit items and the load-bearing ADA --attachments drift call. PASS. The load-bearing behavioral claim — install.sh module deploy — was exercised end-to-end against a throwaway clone: dry-run, real deploy lands README at project-tier .claude/modules/, completeness, GLOB PROOF (a 2nd *.md added to a clone auto-deploys with zero install.sh edit, plan 1->2 modules), empty-source loud-fail (exit 2), staleness flags non-source files, and subproject tier deploys NO modules — all confirmed. op-disc §33 is correctly placed (L2089, between §32 and the un-numbered Agent-regime-inverses, NOT EOF — the r3 fix), is 26 lines (under the ≤30 r6 cap), and uses a single README pointer not a §32-style block. README internal consistency holds: the ARGUS-carry-1 per-C-1 bw-show content-check gate is present in §5.2, all 3 relocation classes have worked templates, both routing-map and relocation-index formats are present (index has one worked row per class), and the Arc-2 taxonomy names exactly 5 modules with zero content files. Clean diff held: zero role-file content deletions, only the 4 expected files touched, author field = Denson Smith only. The ADA --attachments DRIFT CALL IS CORRECT — bw 0.13.0 has no --attachments flag on show and no attachment-read subcommand; the README's git-on-beadwork-ref recovery path was proven by an isolated attach round-trip (exact bytes recovered, exit 0). ONE non-blocking documentation inaccuracy found: README §5.2 lines 196-197 claim 'bw show <id>' names the attach stored path, but live bw records that intent only as a git commit message on the beadwork ref (recover the stored-path name via 'git log beadwork', not 'bw show'). The core recovery mechanism is correct and proven; only the stored-path-discovery pointer is wrong — a one-line ADA fix, surfaced as a methodology_concern, not a falsified probe."
gap_or_blocker: ""
