status: completed
ticket: stoa--xyb.4
verdict: pass
design_artifact_audited: .claude/worktrees/arc-44-build/agents/design/arc-44/design-rev2.md
audit_block:
  prior_blockers_recheck:
  - r1 (3-bucket lossless homes) -- CLEARED. CONDITIONAL->disk module, PROVENANCE->bw-cite (C-1 already-in-bw delete+cite / C-2 archive-via-bw-attach-then-cite), DUPLICATE->pointer to existing keep-target. Each home concrete + verified live (sec16.6/16.7 DO already cite stoa--dxw/p5g/32b.3 + handoff/retro; sec16.8 bw attach REAL at L1048/1052; DUPLICATE target op-disc sec12 L727 real, dupes POLYBIUS sec7.3 L423 + PLINY sec6.1 L586). Anchor cite-back format defined (sec3.C). Not hand-waved. Lossless contract complete for all 3 buckets.
  - r2 (relocation index expresses all 3 classes) -- CLEARED. Sibling Relocation Index table (content->home->class) alongside dispatch-time routing map; Class column makes CONDITIONAL/PROVENANCE/DUPLICATE first-class (sec2.6, sec3.E-2), worked row per class. rev1 2-of-3-inexpressible defect resolved.
  - r3 (op-disc sec33 insertion point) -- CLEARED + RE-VERIFIED LIVE. sec32 content ends with the --- at L2087; un-numbered Agent-regime-inverses L2089; Empirical-lineage L2100; EOF L2110. sec4.0 inserts AFTER L2087 BEFORE L2089 (anchor literal ## Agent-regime inverses), NOT EOF. P6(b) asserts L(sec33) between L(sec32) and L(Agent-regime inverses). Build-correctness defect resolved.
  - r4 (glob deploy + completeness probe) -- CLEARED. Glob over substrate/modules/*.md, nullglob, source-existence + non-empty assert (sec4.2), enumerate/log (sec4.5), staleness vs source-glob (sec4.6). P4 = deploy-COMPLETENESS, stronger than rev1 array-grep. Glob idiom CONFIRMED consistent with shipped install.sh nullglob single-segment (L942/958/969/984); rev1 only-glob claim was mistaken, corrected.
  - r5 (subproject = Arc-2 open question) -- CLEARED. sec6 = explicit Arc-2-gating OPEN QUESTION (web-confirmed contested: claude-code #24987/#2571/#4754/#15627/#18200/#11374). Arc 1 wires user+project only (DEST_MODULES_DIR empty subproject, mirrors DEST_TEMPLATES_DIR empty L594). NOT an Arc-1 gate.
  - r6 (sec33 <=30 lines) -- CLEARED w/ caveat. Hard cap; cross-ref collapsed to single pointer line. Outline-as-written = 27 lines BUT [~2 sentences] placeholder unexpanded; real prose may push count. Trim ladder named (L175). P6(c) enforces. See nf1.
  residual_question_adjudications:
  - "(a) per-C-1-deletion bw-show content-check: MANDATE in THIS design as a stated Arc-2 probe-requirement. It is the only thing that turns the C-1 judgment call (DAEDALUS weak point #2) into a lossless guarantee for the COMMON case. Cheap now (one sentence: each C-1 deletion MUST bw show <id> and confirm the ticket carries the full deleted detail before removing inline prose), expensive to omit (wrong C-1 = silent content drop). Arc-1->Arc-2 CONTRACT job; Arc 1 names the targets (sec3.H-2) so it should name the safety gate. ADA: add one line to modules/README.md PROVENANCE section. NOT a rev2 blocker (content in the README ADA builds); SHOULD-FIX."
  - "(b) one merged table vs two: KEEP TWO. The merge is what made PROVENANCE/DUPLICATE inexpressible in rev1 -- the dispatch-time routing map has no task-type slot for an audit-time relocation with no dispatch trigger. Cardinality differs (one task loads N modules; one relocated section serves N task-types; PROVENANCE/DUPLICATE have NO dispatch-time task-type). A merged class-column table carries empty cells per row -- strictly worse. sec3.E rationale sound."
  - "(c) source-side module-shape gate: DO NOT add in Arc 1 (over-engineering for a one-file arc). The stray-file failure is symmetric-and-smaller than the array silent-non-deploy (visible in P4 enumerated log + flagged by staleness scan); templates/ and skills/ run the same ungated single-dir discipline without regression; a frontmatter gate adds an authoring tax to the ONE class meant to be friction-free (fights r4). Revisit ONLY on empirical stray-file incidents in Arc 2+. Tracked-thought, not an Arc-1 requirement."
  new_findings:
  - id: nf1
    description: "[CONSIDER] sec33 outline-as-written is 27 lines but contains unexpanded placeholder [~2 sentences]. Expanding to real prose may push toward/over 30. ADA must run P6(c) wc-check AFTER expanding the placeholder and apply the named trim ladder (collapse 3-channel sub-bullets, then 3-class sub-bullets) if over 30 -- do NOT expand the README pointer line into a sec32-style block."
    evidence: design-rev2.md L146-175; P6(c) sec5.
    load_bearing: false
    quadrant_classification: easy-easy
  - id: nf2
    description: "[CONSIDER] P1 asserts test -f substrate/modules/README.md but that dir does NOT exist yet (ADA builds it). Correct sequencing -- P1 is a POST-build VERA probe, not a precondition on the design. Confirms clean-diff (design commit 34d8f41 = design-file-only, 493 insertions, zero role-file touches)."
    evidence: ls substrate/modules/ = absent; git diff --stat d11e3e9..34d8f41 = 1 file 493 insertions; P1 sec5.
    load_bearing: false
    quadrant_classification: easy-easy
  - id: nf3
    description: "[CONSIDER] Glob stray-file surface (DAEDALUS weak point #1) is the genuine NEW risk rev2 introduces vs rev1 array -- a non-module *.md in substrate/modules/ deploys with no source-side gate. Adjudicated under residual (c): accept for Arc 1 (smaller+more-visible failure; staleness scan + enumerated deploy log mitigate; templates/skills run the same ungated discipline). Not load-bearing for a one-file arc. On record as considered-and-discharged."
    evidence: design-rev2.md sec9 weak-point-1 + sec4.5/4.6; install.sh templates staleness CITE L957-965 (file-only single-segment, mirrored by sec4.6).
    load_bearing: false
    quadrant_classification: hard-easy
  non_findings:
  - PROVENANCE C-1/C-2 split internal consistency HOLDS. Coherent two-case mechanism; sec16.8 bw attach real; C-1 already-in-bw verified live. The classification-judgment risk is an ARC-2 EXECUTION risk addressed by residual-(a), NOT an Arc-1 design defect.
  - Two-table core-cost (weak point #3) discharged. Few terse index rows enable removing ~360 always-loaded content lines -- strongly net-negative. REFINEMENT 1 mandates index-in-core. Holds subject to Arc-2 keeping rows one-line-each.
  - DUPLICATE-is-NOT-a-module explicit + correct (sec3.H-3): home = existing keep-target (op-disc sec12), no new file.
  - sec11/sec12 two-distinct-modules CONFIRMED live (POLYBIUS sec11 L638 authoring; sec12 L729 prototyping Mode 2). Two homes superset of one.
  - modules staleness block (sec4.6) file-only single-segment glob correctly mirrors templates CITE (L957-965), no recurse into future custom/.
  - Clean-diff / no-Skill-no-MCP / credential-N-A / authorship / manifest-writer-no-change / IA-1-vs-REFINEMENT-1 all held from rev1, re-confirmed clean against rev2.
  summary: Re-audit of design-rev2 against LIVE substrate files. All three rev1 BLOCKING findings genuinely cleared, not hand-waved -- each ground-truth claim verified directly (sec32 ends L2087 then un-numbered L2089; bw cookbook triplicated sec12/sec7.3/sec6.1; sec16.8 bw attach real; sec16.6/16.7 cite tickets; install.sh uses the nullglob idiom). r4/r5/r6 correctly handled. Posture CLEAN, no load-bearing risks remain. Residuals: (a) MANDATE the per-C-1 bw-show content-check as a stated Arc-2 probe-requirement in modules/README.md (SHOULD-FIX); (b) KEEP TWO tables; (c) DO NOT add a module-shape gate in Arc 1. New findings all CONSIDER-grade. rev2 is BUILDABLE by ADA without further design ambiguity. ADA must carry: add the residual-(a) C-1 content-check to modules/README.md PROVENANCE section, and run P6(c) AFTER expanding the sec33 placeholder, applying the trim ladder if over 30 lines.
gap_or_blocker: none
