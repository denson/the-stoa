status: completed
ticket: stoa--xyb.4
verdict: pass
diff_reviewed: 1fb5e04..d3735c6 (arc-44/build) — shipped build only: substrate/modules/README.md (NEW), substrate/operating-disciplines.md (§33 insert), substrate/install.sh (glob module-deploy class). Design/verdict artifacts under agents/ excluded per brief.
design_artifact_compared_against: agents/design/arc-44/design-rev2.md
concerns:
- id: c1
  category: citation
  description: README §5.2 C-2 recovery prose contains an inaccurate clause — "the `attach` intent comment in `bw show <id>` names the stored path." The attach intent is a GIT COMMIT MESSAGE on the beadwork ref, NOT a bw comment; `bw show <id>` does not surface it. A future agent following this recovery instruction runs `bw show <id>`, fails to find the stored path, and cannot construct the `git show beadwork:attachments/...` argument. Correct discovery path is `git log beadwork --oneline | grep attach` (or `git show beadwork:attachments/<id>/` to list).
  evidence: substrate/modules/README.md:196-197; live probe — `bw attach` help states "Commits a single-line intent"; `git log beadwork --oneline` shows "attach <id> <stored-path>" commits; `bw show <id>` comment stream does NOT contain the intent.
  severity: recommended-revision
  quadrant_classification: easy-easy
- id: c2
  category: consistency
  description: The modules staleness scan carries no cite-comment referencing §23 (base-vs-custom) + MAJOR_POLYBIUS §17 at its scoping site, whereas the sibling templates and skills scans both do (§23.5 says cite-comments appear "at every scoping site"). Separately, §23.2's per-class path-convention table is not extended with a Modules row. The deploy BEHAVIOR is correct (empirically verified — a custom module under .claude/modules/custom/ is NOT flagged obsolete; the file-only single-segment scan skips the subdir, mirroring templates), so this is doc-completeness drift, not a functional gap. Properly an Arc-2 follow-up (no custom modules exist yet).
  evidence: substrate/install.sh modules-staleness block (~L1078-1093, no §23 cite); cf. templates scan (carries §23 cite) + skills scan; substrate/operating-disciplines.md §23.2 table (no Modules row). Empirical: custom/ subdir module survived `--prune` scan (exit 0, not flagged).
  severity: minor
  quadrant_classification: easy-easy
follow_ups:
- Extend op-disc §23.2 per-class path-convention table with a Modules row (base `.claude/modules/*.md`, custom `.claude/modules/custom/*.md`) and add the §23 + POLYBIUS §17 cite-comment at the modules staleness scoping site in install.sh — reason: doc-completeness for the future custom-modules convention; no custom modules exist yet, so not Arc-1-blocking. Pairs naturally with Arc 2 (first arc to populate modules).
verifier_coverage_assessment: VERA exercised the load-bearing cases well; its empirical findings match my independent §6.8 probes exactly. VERA Probe-1 confirmed P1 source-exists, P2 dry-run plan+cp, P3 real-deploy lands README exit 0, P4 deploy-completeness, GLOB-PROOF (2nd *.md added to a throwaway clone auto-deployed with zero install.sh edit — the load-bearing r4 contract), P5 empty-source errs loudly (exit 2), P8 staleness flags stray + removed module, subproject-tier deploys NO modules. I independently reproduced project-tier deploy, subproject-skip, and stray-file staleness. No coverage gap against the design's weak points (glob stray-file surface = DAEDALUS WP1, discharged by ARGUS nf3 + VERA P5/P8; PROVENANCE C-1 classification = WP2, an Arc-2 execution risk gated by the README §5.2 content-check). The ONE case neither VERA nor the design probe spec exercised end-to-end is the C-2 recovery instruction itself (bw attach -> git-show-on-beadwork-ref round-trip) — which is how I caught c1. C-2 archival is an Arc-2 activity (no C-2 content archived this arc), so the gap is not load-bearing for Arc 1, but it is the reason c1 went uncaught upstream. quadrant: easy-easy (an end-to-end CLI round-trip, cheap to run). Not a coverage_concern severe enough to block — naming it for the record.
summary: Clean mechanism-only build. Scope discipline held exactly — zero changes to any role file (POLYBIUS/PLINY/CAPTAINs), op-disc is +26/-0 (pure §33 insert; REFINEMENT 2 satisfied, no content cut). §33 placement correct (after §32's --- at L2087, before the un-numbered ## Agent-regime inverses), 19 non-blank content lines, well under the ≤30 cap. install.sh glob-deploy faithfully mirrors the shipped nullglob single-segment templates idiom; user/project tiers deploy, subproject tier sets DEST_MODULES_DIR="" and skips cleanly (mirrors DEST_TEMPLATES_DIR); deploy observable (plan line + per-module log); bash -n exit 0; no quoting/portability bug (verb-only `author` strings, no `set -u` unbound surface — nullglob loops + `:-` defaults in the associative-array staleness idiom). README clear, internally consistent, all load-bearing cross-refs verify (§16.8 = bw attach home with the exact signature; §12 + POLYBIUS §7.3 + PLINY §6.1 = the real triplicated bw cookbook for the DUPLICATE worked example). Both ARGUS-carry items present (per-C-1 bw-show content-check gate in §5.2; P6(c) line-count run). Security/hygiene clean: no secrets, no author-like field touched, Author = PRINCIPAL (denson <densonsmith2@gmail.com>), both Co-Authored-By trailers present (CAPTAIN_ADA_the-stoa + Claude Opus 4.7). Most important finding is c1 (recommended-revision): one inaccurate clause in the C-2 recovery prose. Overall posture: pass with one recommended-revision (c1) + one minor doc follow-up (c2).

INDEPENDENT READ — ADA --attachments drift call: CORRECT, and a good catch at the writing seat. The design (Decision C) literally wrote `bw show --attachments <id>`; I confirmed against live bw 0.13.0 that `bw show` has only `--json` and `--only` (no `--attachments`) and there is no attachment-read subcommand. ADA documented ship-reality (git on the beadwork ref). I exercised the replacement end-to-end on a throwaway bw repo: `bw attach <id> <file> [--name story/prov.md]` stores at attachments/<id>/<stored-path> on the beadwork ref (slash-in-name verbatim; basename default — both confirmed); `git show beadwork:attachments/<id>/<stored-path>` recovers the exact bytes. The primary recovery command is accurate. ADA fixed the big problem (a flag that does not exist) and introduced a smaller one (c1 — the "via bw show" discovery clause). Net strongly positive: building the design as written would have shipped a recovery instruction that fails outright.

INDEPENDENT READ — §33 on the 2110-line worst-offender file: NOT self-undercutting; the +26 lines are the enabling investment, not bloat. §33 is the always-loaded RULE that defines the composition layer + the three relocation classes (CONDITIONAL->module, PROVENANCE->bw-cite, DUPLICATE->pointer) precisely so Arc 2 can move hundreds of CONDITIONAL/PROVENANCE/DUPLICATE lines OUT of always-loaded core into on-demand homes. The DUPLICATE class alone targets a concrete future cut (bw cookbook triplicated across op-disc §12 / POLYBIUS §7.3 / PLINY §6.1 — keep one, delete two). §33 is bounded at 19 content lines and pushes all detail to the on-demand README (the dogfood is deliberate). This is the canonical "spend a little always-loaded budget to unlock a large always-loaded reduction" pattern; net-negative line count is the explicit design goal. My independent read agrees with ARGUS's discharge of the two-table core-cost non-finding.
gap_or_blocker: none
