status: completed
ticket: stoa--pk4
verdict: pass
diff_reviewed: 3435fe3..ec72ed5 (8f3f2f9 main build + ec72ed5 rev3 C1 re-fix; 10 files)
design_artifact_compared_against: agents/design/stoa--pk4/design-rev2.md + design-rev3.md (rev3 supersedes rev2 §2.6 + §4 P6); directive substrate/arcs/arc-68-build-directive.md
concerns: (no blocking concerns; one design-doc-only follow-up, two minor nits)
- id: c1
  category: consistency
  description: design-rev3 §4 P6(b) bucket-2 literal trigger ("remove git from PATH") does not faithfully reach the resolution-failure bucket — record-seat.ps1 line 93 runs git show first; with git absent that aborts before the bucket. Shipped CODE is correct (VERA proved the bucket fires with the faithful trigger: git resolvable, no bw-bash derivable). Design-prose-only.
  evidence: design-rev3.md §4 P6(b) bucket-2 spec vs record-seat.ps1:93; VERA-20260620T130710Z.md methodology_concerns[0]
  severity: minor
  quadrant_classification: easy-easy
- id: c2
  category: hygiene
  description: Intermediate-commit trailer inconsistency — ec72ed5 carries a Co-Authored-By Claude Opus 4.8 line that 8f3f2f9 does not. Both carry the correct Author=denson + CAPTAIN_ADA_the-stoa seat trailer. Washes out in the close-gate squash. (Already noted by the FM.)
  evidence: git log 3435fe3..ec72ed5
  severity: minor
follow_ups:
- Fold the c1 design-rev3 P6(b) bucket-2 wording fix into the close-gate design-canon audit — already routed by PLINY/FM. Doc-only, not a code defect.
- (Optional) c2 trailer consistency is a close-gate squash decision for user-tier; no action needed in the arc branch.
verifier_coverage_assessment: VERA exercised every load-bearing case. P6(a) — the C1 gate rev2 owed — ran through the REAL WSL-shadow PATH (Get-Command bw=null, Get-Command bash=system32 WSL shadow, git un-shadowed) with the false-pass guard genuinely held (no git-bash fronting, no prepend ahead of system32, no bw shim), so the pass is through the real Resolve-GitBashWithBw derivation; a real probeFallback row round-tripped at exit 0. P6(b) drove both the new resolution-failure bucket and the bw-error bucket and asserted distinctness — CONSEQUENCE 2 killed. P4 re-ran DC4 round-trip + idempotent re-record THROUGH the new fallback. I independently re-verified cleanup (not relayed): registry = exactly 2 real rows, sha256 f29a2166, 0 stray attachments. VERA's lone methodology concern is design-doc-only and matches c1. No coverage gap.
attach_status: PENDING
summary: Arc 68 rev3 (launcher-correctness) is a clean, well-scoped diff. The three structural guarantees (chain-of-command-at-launch via L1 preamble on arc/paste paths + L2 role-file canon under Option B; gauntlet-by-default with -GauntletWaiver + the Stop-hook clause-E advisory detector; variable composition adding CHIRON/HAMILTON) plus the record-seat PATH-defect fix are all delivered and match design-rev2/rev3 exactly. Both PowerShell files parse clean under pwsh 7; the fresh stop-hook test suite is 2/2 green; install.sh correctly untouched. RQ2' security re-confirm clears independently: every value reaching the bash -lc invocation (ticket, tmpBash, attachName) is system-controlled/launcher-set and passed POSITIONALLY (no script-body interpolation); the only caller-supplied params (Seat/Name/Role/SessionId) reach ConvertTo-Json structurally, never the shell; the launcher never passes -Ticket; so the §35.5 not-threat-ratified carve-out STILL holds after rev3 (matches ARGUS caller-trace + VERA empty threat_coverage). The 4-bucket fix(b) is genuinely 4 distinct outcomes; rev3 NEVER invokes the WSL bash (derives candidates from git, gates each on command -v bw), so a PATH/resolution failure can never again be mis-attributed as the benign no-ticket / bw-error case — original mis-attribution hazard killed, proven by VERA distinctness probe. Authorship audit clean: only author field (team-launcher/SKILL.md author: Denson Smith) correct; both commit Author: = PRINCIPAL (denson) + CAPTAIN_ADA_the-stoa seat trailer. DoD bullets all met (dry-run shape, real composition-record round-trip, chain text in role files + §37, gauntlet-default honestly documented as Option A detection-not-prevention, canon greps clean — the only co-equal-panes hits are NEW text negating it; NOMOS-pending). Scope held inside out-of-scope lines (no Arc-67 rebuild, no u--9s2 build, install.sh unedited so ENABLE_HOOKS untouched, stoa--waa not pulled in). Posture: PASS with two minor nits — clean to advance to the close gate.
