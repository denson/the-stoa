status: completed
ticket: stoa--pk4
verdict: pass
verdict_label_for_orchestrator: PASS (build-ready)
design_artifact_audited: C:\Users\denso\claude_projects\the-stoa\.claude\worktrees\arc-68-build\agents\design\stoa--pk4\design-rev2.md
re_check_scope: TARGETED re-check of design-rev2 deltas vs rev1 conditions C1-C7 + RQ1-RQ4 (NOT a from-scratch re-audit). Prior structural clears (DC0 layered model, DC3 placement, S35.5 carve-out) carried forward; verified undisturbed by the rev2 deltas.

audit_block:
  risks:
  - id: r1
    description: C4 rev2 corrected rationale at S2.6 L243 is STILL false. rev1-r2 dropped the false "tmp has no spaces by construction"; rev2 replaced it with the claim that a Windows path cannot contain a single-quote character, so the single-quoted interpoland is safe. That replacement is ALSO false -- the apostrophe is NOT a Windows-reserved char (reserved set = lt gt colon double-quote slash backslash pipe question-mark asterisk), so a Windows username/dir CAN contain a single quote (e.g. an apostrophe surname like O-Brien -> C:\Users\[apostrophe-name]\...\Temp\...). If GetTempPath() resolves through such a profile, tmpBash carries a literal apostrophe that terminates the bash single-quoted string inside bash -lc, reintroducing the word-splitting break the wrap is meant to prevent -- AND the design asserts the quotes are load-bearing not decorative on a false premise a future maintainer would trust. Same error CLASS as rev1-r2 (a confident false safety-rationale); bounded because the common case (no apostrophe in path) is safe and the practical trigger is a rare username shape. The code is not made WORSE than rev1; the residual is the still-false documented rationale.
    evidence: design-rev2.md S2.6 L243; record-seat.ps1 L108 (GetTempPath); web-verified Windows reserved-char set this turn (apostrophe not reserved).
    load_bearing: false
    quadrant_classification: easy-easy
  - id: r2
    description: Minor internal inconsistency in the C6 line-number claim between the S0 changelog and the S2.5 prose. S0 (L18) says S36 ends L1659; S2.5 (L181) says S36 content ends at L1656, --- at L1658, appendix at L1660, insert at L1659. Ground truth confirms S2.5 is PRECISELY correct. The S0 ends-L1659 is loose shorthand for the INSERTION line (1659), not S36 end; the operative instruction (insert at L1659, before the appendices, NOT EOF) is CORRECT and P9 guards it structurally. Cosmetic changelog imprecision, not a build hazard.
    evidence: design-rev2.md S0 L18 vs S2.5 L181; ground-truth grep this turn (operating-disciplines.md L1656/L1658/L1660/L1671, 1681 lines).
    load_bearing: false
    quadrant_classification: easy-easy

  non_findings:
  - RQ2 (NEW injection surface from C5 -Ticket param) -- DISCHARGED; carve-out STANDS. Traced ALL record-seat.ps1 callers: (1) launcher launch-team.ps1 L241 does NOT pass -Ticket (defaults stoa--reg); (2) desktop self-record (whoami skill) does NOT pass -Ticket (defaults); (3) VERA P6(b) passes a FIXED literal -Ticket bogus--nope. No agent/caller-untrusted value reaches -Ticket in any flow; inside bash -lc the ticket is single-quote-wrapped and a bw ticket-id contains no single quote. The C5 param is launcher-set/defaulted, not adversary-reachable. S35.5 not-threat-ratified (process change, no runtime attack path) carve-out CONFIRMED post-parameterization.
  - C1/r1 fidelity (W5) -- DISCHARGED. rev2 P6(a) is rewritten to FORCE the bash-fallback via a pwsh-bw-unreachable shim (remove the bw-carrying dir from PATH; assert Get-Command bw == null AND Get-Command bash != null) so the Get-Command branch CANNOT satisfy it, and it asserts a REAL row round-trips. The fix(a) cygpath / C-colon-to-slash-c transform is present; the unexecuted transform is honestly named W5 with P6(a) as its proof obligation. P6(a) can no longer pass via the Get-Command branch. Sound.
  - C2/Option B fidelity -- DISCHARGED. S2.1 Edit B L83-89: say path returns the BARE word unchanged (no preamble, no -Layout Windows force); preamble prepends ONLY on arc (seat.Prompt, L170) + paste paths. Verified against launcher: Get-SeatPrompt L169-173; panes path L190 passes the prompt UNQUOTED (confirms say must stay single-token); arc path forces Windows L125 -> the quoted Windows path L209. P3 falsifies a say-path regression. Option B faithfully implemented.
  - Say-path-not-thinner (FM dependency) -- DISCHARGED. The PLINY role file ALREADY substantively carries the chain pre-edit (MAJOR_PLINY.md L17 receive directives from POLYBIUS, L41 do not converse with PRINCIPAL directly surface to POLYBIUS via beadwork, L134 gauntlet/CAPTAIN role); POLYBIUS L21/L154 carry chain content; the say-trigger (install.sh L1625 read that file and assume the role) loads the role file first. A bare-word say launch inherits the chain SUBSTANTIVELY via L2 canon even before the S2.5 edit; the S2.5 substantive paragraphs (anchored at POLYBIUS S9/L282, PLINY S17/S41 area) make established-at-launch + gauntlet-default-waiver-gated + S37 explicit. P8 asserts substance beyond the S37 token; patterns align with the rev2 prose. NOT a stub. Dependency genuinely met.
  - C3/r5 + RQ4 (no selective AR-7 detection) -- DISCHARGED. Every occurrence of selective/transcript/classifier/AR-7-shape in rev2 (S0 L15, S2.0b L59, S2.2 L113, S3 L303, P5 L329, W1 L374) is in the NEGATING context. Consistent honest framing: clause (E) fires on EVERY orchestrator Stop turn via the accepted-advisory branch; DoD bullet 4 met via clause-text-reaches-model, not selective firing. Clause (E) text (S2.2 L104-110) is a CONDITIONAL model self-check -- correct reminder framing, no over-claim that the hook detects the condition. No residual selective-detection wording.
  - C6 placement (EXACT line numbers, verified this turn) -- DISCHARGED. operating-disciplines.md = 1681 lines. S36 starts L1476; content ends L1656; trailing --- at L1658; ## Agent-regime inverses at L1660; ## Empirical lineage at L1671. S37 inserts at L1659 (between the --- and the first appendix), NOT EOF. rev2 S2.5 prose is exactly right; P9 guards placement structurally. (See r2 for the cosmetic S0-vs-S2.5 shorthand.)
  - C5 (parameterize ticket) -- DISCHARGED. record-seat.ps1 L58 currently hardcodes the ticket constant; rev2 adds a [string]Ticket=stoa--reg param + sets ticket=Ticket, giving P6(b) a real injection point distinct from the PATH-miss branch. Default preserves every existing caller. Sound.
  - C7 (fresh stop-hook fixture) -- DISCHARGED + runnable. tests dir has ONLY run-author-gate-tests.sh + fixtures/(control,fp,tp) + README.md (no stop-self-check test to extend -- rev2 correctly authors fresh). S2.8 fixture (synthetic Stop event JSON, stop_hook_active:false so it does not early-allow at L48-49) + runner (assert decision:block + clause-(E) sentinel; second same-turn call -> allow proving the L82-88 once-per-turn guard) respects the infinite-block guard + turn-key (session + transcript size, L74-77). Build-detail note: the runner must ensure both calls resolve to the SAME sentinel dir (L58 CWD/.claude/hooks else HOME else TMPDIR) and a stable-size fixture transcript so the turn-key matches -- a runner-correctness detail for ADA/VERA; the spec is aware of it. Sound + runnable.
  - C4 single-quote-wrap mechanism (the CODE) -- the single-quote-wrap is the correct mechanism for the common case; only the stated RATIONALE is false (see r1). The code is not worse than rev1.
  - RQ1 (row-recorded waiver, no in-prompt waiver on bare-say) -- DISCHARGED as ACCEPTABLE without pulling stoa--waa. A -GauntletWaiver is an explicit POLYBIUS/PRINCIPAL action that in practice carries a brief (arc/paste path, which DOES carry the waiver line); a waived bare-say launch is the unusual case and the row records gauntlet=waived authoritatively + the role-file/S37 canon carries gauntlet-by-default context. Folding stoa--waa solely to put the waiver text in a bare-say prompt is not warranted; W2 names it honestly and it stays a separate P3 ticket. Row + canon sufficient.
  - RQ3 (ack-isolation clean) -- DISCHARGED. All prevention-vs-detection (ack-sensitive) wording is confined to S2.0b (the L3/Residual enforcement-class rows in the S2.0 table defer to S2.0b; S2.0 itself states the LAYER STRUCTURE is ack-independent). A prevention-demand ack would touch ONLY S2.0b (and the named localized rework: swap clause (E) for a PreToolUse gate); the C1/C2/C4-C7 folds in S2.1/S2.5/S2.6/S2.8 are self-contained + ack-independent. No prevention-vs-detection wording leaks into the ack-independent sections. Isolation is clean.

  threat_coverage_assessment: N/A -- Arc 68 is launcher-tooling + process/role-file hardening with no runtime attack path. ARGUS RE-CONFIRMS the S35.5 carve-out not-threat-ratified (process change, no runtime attack path) AFTER the C5 -Ticket parameterization (RQ2 traced: launcher-set/defaulted + single-quote-wrapped, no untrusted reach). AR-7 is a correctness/discipline failure shape, not a security threat; no threat-ratified mitigation -> no threat-anchored probe required. Honest-claim boundary (S35.5): coverage assessed, not threat-defeat-in-general.

attach_status: PLACEHOLDER
summary: TARGETED re-check of design-rev2. rev2 cleanly folds all seven rev1 conditions (C1-C7) + the BLESSED Option B (C2) + the FM say-path-not-thinner dependency, and RQ1-RQ4 all resolve. Ground truth re-verified line-by-line this turn: record-seat.ps1 (L58 ticket-constant, L108 GetTempPath, L118-128 bw block); launch-team.ps1 (Get-SeatPrompt L169-173, arc force-Windows L125, panes-unquoted L190, Windows-quoted L209); operating-disciplines.md (S36 content ends L1656, --- L1658, appendices L1660/L1671 -> S37 inserts L1659 NOT EOF -- C6 CORRECT); stop-self-check.sh (clause-D append pattern L106-115, decision:block channel L120, infinite-block guard L48-49/L82-88 -- C7 fixture spec sound, no existing test to extend); role files (PLINY L17/L41/L134 + POLYBIUS L21/L154 already carry the chain substantively, say-trigger install.sh L1625 loads the role file -- say-path genuinely NOT thinner). RQ2 (the gating question): the C5 -Ticket param does NOT open an injection surface -- every production caller defaults it, only the VERA probe sets a fixed literal, and it is single-quote-wrapped; the S35.5 carve-out STANDS post-parameterization. RQ1 (row-recorded waiver sufficient), RQ3 (ack-isolation clean -- only S2.0b ack-sensitive), RQ4 (no selective-AR-7 wording) all confirmed. ONLY new observations, both NON-load-bearing: r1 -- C4 corrected rationale at S2.6 L243 (a Windows path cannot contain a single-quote) is STILL false (apostrophe is not a Windows-reserved char, web-verified; an apostrophe-surname profile would break the single-quote-wrap), same CLASS as rev1-r2, bounded; r2 -- a cosmetic S0-vs-S2.5 line-number shorthand (S2.5 precise + correct, P9 guards placement). NO rev2 delta introduces a NEW load-bearing flaw. VERDICT PASS (build-ready). The two bounded observations are honesty/clarity items ADA can fold trailing; neither blocks the build. ADA releasing gate remains the PRINCIPAL prevention-vs-detection ack on S2.0b (ack-independent of this PASS).
gap_or_blocker: none (status completed)
