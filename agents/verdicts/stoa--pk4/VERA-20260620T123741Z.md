status: completed
ticket: stoa--pk4
verdict: fail
design_artifact_verified_against: .claude/worktrees/arc-68-build/agents/design/stoa--pk4/design-rev2.md
build_verified: arc-68/build @ 8f3f2f9
probes_executed:
- probe_id: p1
  description: DryRun custom-agent composition adds CHIRON seat + forces Windows + chain preamble on the arc-class prompt.
  quadrant_classification: easy-easy (mechanical: run DryRun, grep printed output for the three asserted shapes).
  command_or_method: pwsh launch-team.ps1 -ProjectDir <tmp> -Slug probe -Composition custom-agent -DryRun
  expected: CHIRON_probe seat listed w/ space-free --name + --session-id uuid; -Layout Windows forced; CHIRON prompt contains "CHAIN OF COMMAND (Stoa, established at launch" + team-architect ROLE_LINE.
  observed: CHIRON_probe listed (--name CHIRON_probe, --session-id f054bce4-...); Windows-force warning fired (all seats Windows); CHIRON prompt carries the chain preamble + "MAJOR_CHIRON: design-time team-architect. You answer to POLYBIUS...". Say-seats stayed bare-word polybius/pliny (Option B, no preamble on Say seats).
  result: pass
- probe_id: p2
  description: Arc launch -- PLINY surfaces to POLYBIUS not PRINCIPAL; POLYBIUS supervises PLINY.
  quadrant_classification: easy-easy (DryRun + grep the printed PLINY/POLYBIUS prompts).
  command_or_method: pwsh launch-team.ps1 -ProjectDir <tmp> -Slug probe -ArcId stoa--test -DryRun
  expected: PLINY prompt contains "take direction from + surface to POLYBIUS via bw, NOT the PRINCIPAL"; no "surface to PRINCIPAL"/"PRINCIPAL addresses you" line in PLINY prompt; POLYBIUS prompt contains "you SUPERVISE PLINY".
  observed: PLINY prompt "...surface to POLYBIUS via bw, NOT the PRINCIPAL." present; no mis-routing line; POLYBIUS prompt "you SUPERVISE PLINY (direct + independently verify hand-backs via bw). The PRINCIPAL/user-tier addresses YOU; you never dispatch CAPTAINs yourself." present.
  result: pass
- probe_id: p3
  description: BLESSED C2/Option B -- standard SAY launch stays bare-word + preserves Panes (no Windows force, no preamble).
  quadrant_classification: easy-easy (DryRun + inspect the printed wt command).
  command_or_method: pwsh launch-team.ps1 -ProjectDir <tmp> -Slug probe -Activation say -DryRun
  expected: does NOT force -Layout Windows (stays Panes); POLYBIUS positional prompt = bare word "polybius"; PLINY = bare word "pliny".
  observed: printed "wt new-tab ... split-pane -V" (Panes), no Windows-force warning, both seats show (Panes); trailing positional tokens are exactly "polybius" and "pliny" (no preamble). Panes default preserved exactly.
  result: pass
- probe_id: p4
  description: REAL composition-record round-trip + idempotency (no live agents).
  quadrant_classification: easy-easy (real record-seat call, read back via git show, count rows).
  command_or_method: record-seat.ps1 -Seat CHIRON_probe ... -Composition custom-agent -Gauntlet required -ChainRole team-architect; read back from beadwork:attachments/stoa--reg/seat-registry.jsonl; re-record w/ a new sid; count CHIRON_probe rows.
  expected: row carries composition=custom-agent, gauntlet=required, chain_role=team-architect; re-record yields no duplicate (idempotent replace).
  observed: first record round-tripped all three DC4 fields; re-record (new sid 7e185fef-...) => CHIRON_probe row count stayed 1, surviving row carries the NEW sid (replace-not-append). CLEANUP: probe row removed (stoa--reg restored to baseline sha f29a2166).
  result: pass
- probe_id: p5
  description: Gauntlet-by-default detector -- clause (E) present + reaches model on the working Stop channel + infinite-block guard (advisory bar / C3 honest scope).
  quadrant_classification: easy-easy (run the fresh runner + a direct hook emission check).
  command_or_method: bash substrate/hooks/tests/run-stop-self-check-tests.sh; plus a direct one-shot of stop-self-check.sh asserting the emitted JSON.
  expected: emitted JSON decision:block, reason contains clause (E) sentinel "solo-with-one-checker close is the AR-7 failure shape"; second same-turn call allows (empty stdout).
  observed: runner 2 passed / 0 failed; direct emission decision=block, has clause (E)=True, has sentinel=True, has gauntlet/waiver language=True; second call empty stdout (guard intact). Honest scope confirmed: proves the clause reaches the model on every orchestrator Stop turn (the accepted advisory branch), NOT selective AR-7 detection.
  result: pass
- probe_id: p6a
  description: NON-NEGOTIABLE C1 -- FORCE + PROVE the bash-fallback writes a real stoa--reg row on a pwsh-bw-unreachable shim, exercising the REAL pwsh Get-Command bash resolution (per the FM false-pass guard on this ticket).
  quadrant_classification: easy-hard -> the bug surfaces obviously once the REAL resolution is exercised; the false-pass risk made detecting a VALID pass the hard part.
  command_or_method: ran record-seat.ps1 under (1) a BASH_ENV shim that fronted git-bash [FALSE PASS, rejected] and (2) the REAL registry-backed Machine+User PATH = what a freshly-launched pwsh terminal gets [the valid test].
  expected: a real probe row round-trips through the fallback AND the fallback exercised the real Get-Command bash resolution.
  observed: REAL resolution on this machine => Get-Command bw=NULL, Get-Command bash=C:\windows\system32\bash.exe (the WSL launcher, distro-less/BROKEN), Get-Command cygpath=NULL. record-seat.ps1 took the fallback, invoked WSL bash, which emitted "WSL ERROR execvpe(/bin/bash) failed: No such file or directory", exited 1 => NO row written; fix(b) MISLABELED the resolution failure as "bw attach to 'stoa--reg' exited 1 (... benign no-registry-ticket case)". My earlier BASH_ENV-shim pass was a FALSE PASS (it fronted git-bash, bypassing the real WSL resolution) -- REJECTED per the FM guard.
  result: fail
- probe_id: p6b
  description: Error-attribution -- PATH-miss vs missing-ticket/bw-error are distinct + the old benign-unconditional string no longer fires for a PATH failure.
  quadrant_classification: easy-easy (drive each branch, read the warning).
  command_or_method: (i) PATH-miss shim PATH so both bw AND bash are pwsh-unreachable; (ii) bw-error -Ticket "not a valid id!!!" with bw resolvable (bw exits 1 on an invalid id -- the design's -Ticket bogus--nope did NOT error on this bw version, see methodology_concerns).
  expected: PATH-miss => "PATH/resolution failure, NOT a missing-ticket case"; bw-error => "bw attach to '<ticket>' exited <N>"; distinct.
  observed: PATH-miss case emitted exactly "bw NOT FOUND on the pwsh PATH or via bash - this is a PATH/resolution failure, NOT a missing-ticket case." (exit 1); bw-error case emitted "bw attach to 'not a valid id!!!' exited 1 ..." (exit 1). The two messages are distinct; the PATH failure does NOT emit the old benign-unconditional string. NOTE: this branch-distinction holds in isolation, but see falsifying_evidence_summary -- the REAL affected-machine failure shape (WSL-broken) is routed to the bw-error branch, so the mis-attribution hazard partially survives in practice.
  result: pass
- probe_id: p6c
  description: Grep record-seat.ps1 -- the old collapsed string is not the sole/unconditional attribution.
  quadrant_classification: easy-easy (grep).
  command_or_method: grep "expected on a workspace without the registry ticket" and "benign" in record-seat.ps1.
  expected: the exact old string absent; "benign" only inside the exit-<N> branch's conditional guidance.
  observed: exact old string "expected on a workspace without the registry ticket" = 0 matches. "benign" appears only in (1) two comment lines describing the OLD bug, (2) the exit-<N> branch's conditional guidance ("If '$ticket' genuinely does not exist ... benign ...; otherwise a real attach failure"). Conforms.
  result: pass
- probe_id: p7
  description: install.sh count stability + consistency sweep (no stale framing).
  quadrant_classification: easy-easy (diff install.sh; compare source inventories; grep docs).
  command_or_method: git diff main 8f3f2f9 -- substrate/install.sh; compare skills/ + hooks/*.sh inventories main vs branch; install.sh --dry-run; grep touched docs for stale framing.
  expected: install.sh diff empty; skill/hook counts unchanged; no "two-seat default"/"PRINCIPAL coordinates the panes"/co-equal-panes framing.
  observed: install.sh diff EMPTY; skills/ dir 12 entries identical main vs branch; hooks/*.sh 8 identical; the two NEW files are both under hooks/tests/ (non-recursive *.sh glob => never deploy); dry-run reported 7 SKILL_NAMES + 4 carve-out skills + 8 hooks. Consistency sweep gauntlet-setup/SKILL.md + launch-team.ps1 clean; team-launcher/SKILL.md only match is the NEW correct framing "seats are not co-equal panes" (states the chain correctly, not the anti-pattern).
  result: pass
- probe_id: p8
  description: SAY-path chain established SUBSTANTIVELY via canon (the FM say-path-not-thinner dependency).
  quadrant_classification: easy-easy (Select-String the §37 body + both role files).
  command_or_method: assert od §37 title present + body carries PRINCIPAL+POLYBIUS+PLINY+gauntlet; MAJOR_PLINY carries "surface to POLYBIUS"+"NOT the PRINCIPAL"+gauntlet+default+§37; MAJOR_POLYBIUS carries "supervise PLINY"+CHIRON/HAMILTON+§37.
  expected: substance present BEYOND a bare §37 token in both role files.
  observed: od §37 title=1, body has PRINCIPAL+POLYBIUS+PLINY+gauntlet=True. MAJOR_PLINY surface-to-POLYBIUS=True, NOT-the-PRINCIPAL=True, gauntlet+default=True, §37 cross-ref=True (a full substantive paragraph, not a stub). MAJOR_POLYBIUS supervise-PLINY=True, CHIRON+HAMILTON=True, §37 cross-ref=True (full paragraph). Say-path-not-thinner genuinely met.
  result: pass
- probe_id: p9
  description: od §37 placement before the trailing un-numbered appendices (C6).
  quadrant_classification: easy-easy (Select-String line numbers).
  command_or_method: compare line numbers of "## 37. Launcher-correctness" vs "## Agent-regime inverses" vs "## Empirical lineage".
  expected: §37 line < both appendix lines.
  observed: §37 at L1660; "Agent-regime inverses" at L1746; "Empirical lineage" at L1757. §37 precedes both. Numbered sequence intact.
  result: pass
- probe_id: full-suite
  description: Banked-lesson backstop -- full app/gen-data + author-gate corpus IN ADDITION to P1-P9 (shared-machinery edit record-seat.ps1 + substrate frontmatter).
  quadrant_classification: easy-easy (run the existing suites; assert from full-run results, not inference).
  command_or_method: npm install (app/); npm run gen-data; npm test (vitest); npm run typecheck; bash substrate/hooks/tests/run-author-gate-tests.sh.
  expected: all green; gen-data re-derives the whole roster with a valid schema; no regression.
  observed: gen-data PASS (16 role files => 4 MAJOR/12 CAPTAIN/8 LIEUTENANT, schema valid); vitest 41/41 PASS (3 files); typecheck PASS; author-gate corpus 16/16 PASS. The regenerated app/src/data/generated/agents.ts showed as modified (expected regen artifact; reverted post-run, not in scope).
  result: pass
threat_coverage:
- (empty list -- VALID per §35.5 self-reference carve-out) Arc 68 is classified "not threat-ratified (process change, no runtime attack path)" by DAEDALUS (§5) and CONFIRMED by ARGUS (rev1 + rev2 re-check). It is launcher-tooling + process/role-file hardening; there is no attacker and no runtime attack path for an A3 M<n> map to defeat. The bash -c interpolands in fix(a) are launcher-set (the -Ticket param is single-quote/positional-safe, ARGUS re-confirmed no injection after the C5 parameterization). No threat-anchored probe is required.
methodology_concerns:
- P6(a) FALSE-PASS (self-caught + rejected): my FIRST P6a attempt used a BASH_ENV shim that fronted git-bash and made Get-Command bash resolve git-bash, bypassing the REAL pwsh resolution. That produced a PASS (a real probeFallback row round-tripped through git-bash bw). Per the FM's explicit false-pass guard on stoa--pk4 ("a P6a PASS is valid ONLY if it exercised the REAL pwsh Get-Command bash resolution and STILL wrote+round-tripped a real row"), I REJECTED that pass and re-ran against the registry-backed Machine+User PATH (the real fresh-terminal resolution), which FAILS. The honest verdict is the real-resolution result.
- P6(b) injection-point drift: the design's intended bw-error injection (-Ticket bogus--nope) does NOT error on this bw version (bw 0.13.1 auto-creates the attachment path => exit 0, leaving a stray bogus--nope attachment I cleaned via a forward commit). I drove the exit-<N> branch with a syntactically-invalid ticket id ("not a valid id!!!"), which bw rejects with exit 1 -- a faithful exercise of the same branch. DAEDALUS may want to update the P6(b) spec's injection example.
- P6(b)/fix(b) mis-attribution residual (consequence of the C1 fail, not a separate bug): on the affected machine the REAL failure shape (Get-Command bash => broken WSL) is routed into fix(b)'s exit-<N> branch and labeled a "bw attach exited N (... benign no-registry-ticket case)" -- so the mis-attribution hazard fix(b) was meant to KILL partially SURVIVES. fix(b) has no bucket for "the resolved bash is the wrong/broken one." This is the FM's CONSEQUENCE 2.
falsifying_evidence_summary: The load-bearing C1 probe P6(a) is FALSIFIED on the affected (target) machine. record-seat.ps1 fix(a) assumes pwsh Get-Command bash resolves git-bash; on this machine the REAL registry-backed PATH (what a freshly-launched pwsh terminal gets) resolves Get-Command bash to C:\windows\system32\bash.exe -- the WSL launcher, which is distro-less and BROKEN (emits "WSL ERROR execvpe(/bin/bash) failed: No such file or directory" and exits 1). Get-Command bw and Get-Command cygpath are both NULL on the real PATH. Consequence 1: the registry-write STILL FAILS on the very machine the original bug manifests -- fix(a) resolves the WRONG bash (broken WSL), never reaching git-bash where bw lives at /c/Users/denso/bin/bw, so no real stoa--reg row is written. Consequence 2: fix(b) buckets this resolution failure into its exit-<N> branch and mislabels it as a bw/ticket error ("benign no-registry-ticket case"), so the mis-attribution hazard the fix was built to kill partially survives. Root cause (the verify-the-tooling-PREMISE lesson): the design/build assumed a capability (Get-Command bash => git-bash) that is false on the target machine because WSL's system32\bash.exe shadows git-bash. A correct fix must resolve a bash where `command -v bw` actually succeeds (git-bash explicitly, not naive Get-Command bash), with a POSIX path-transform matching THAT bash's mount form (git-bash /c/, lowercase drive -- NOT WSL /mnt/c/), and fix(b) must bucket wrong-bash/WSL-broken as a RESOLUTION error, not a bw/ticket error. All other probes (P1-P5, P6b, P6c, P7-P9) and the full-suite backstop (gen-data, vitest 41/41, typecheck, author-gate 16/16) PASS -- the failure is isolated to C1/P6(a) and its fix(b) attribution residual.
verification_artifacts_path: .claude/worktrees/arc-68-build/agents/verification/stoa--pk4/ (p6a-fallback.ps1 [the false-pass shim, retained for transparency], p6a-REAL-resolution.ps1 [the valid real-resolution probe], p6b-pathmiss.ps1)
attach_status: OK
summary: I exercised the built launcher + record-seat + stop-hook + canon directly. DryRun probes (P1-P3) confirmed the three structural launch shapes incl. the BLESSED Option B (say stays bare-word + Panes; preamble only on arc/paste). Real round-trips (P4) confirmed the DC4 composition fields persist + idempotent-replace. The stop-hook clause (E) reaches the model on the working channel with the infinite-block guard intact (P5). Canon + placement + say-path-not-thinner (P7-P9) all hold substantively. The full-suite backstop is green (gen-data re-derived the whole roster; vitest 41/41; author-gate 16/16). The ONE load-bearing probe -- C1/P6(a), which the whole arc hinges on -- FAILS: on the affected machine the real pwsh Get-Command bash resolves to a broken WSL launcher, not git-bash, so the bash-fallback never writes a real row and fix(b) mislabels the resolution failure as a benign bw/ticket error. I self-caught a FALSE PASS (a BASH_ENV shim that fronted git-bash) and rejected it per the FM's guard. CLEANUP verified: stoa--reg restored to exact baseline (sha f29a2166, the 2 real rows), stray bogus--nope attachment removed via a forward commit, temp shims cleaned, the regen agents.ts reverted. Verdict FAIL -- route back to DAEDALUS/ADA for a C1 fix-cycle (resolve git-bash explicitly where bw lives; transform to git-bash mount form; bucket wrong-bash as a resolution error), then re-prove P6(a) against the real resolution before CATO.
