status: completed
ticket: stoa--q7f
verdict: pass
design_artifact_audited: agents/design/stoa--q7f/design-rev2.md
re_audit_of: agents/verdicts/stoa--q7f/argus-dc1-security.md (rev1 REVISE, conditions r1-r6)
audit_block:
  overall: PASS. All six rev1 conditions GENUINELY CLOSED in rev2 (not merely claimed). The DC1 shape was confirmed correct in rev1 and is NOT re-litigated. Each fold traced to the actual mechanism + probe, not the changelog assertion. The DC3 mock-emit is carried clean (verified by direct section diff). The single new weak point is the one DAEDALUS self-named; no unnamed regression. This is the gate-relevant security verdict: CLEAN PASS on the DC1 crux.
  per_condition:
  - id: r1
    disposition: CLOSED
    description: INV-DEST genuinely makes a caller-influenced destination UNCONSTRUCTABLE across the whole registry. url_slots accept ONLY FIXED/ENUM (free-form slot type not representable in the schema type); param_schema + skill_id namespaces are disjoint from url_slots by construction; the startup loader fail-loud refuses any op whose URL template references a param/skill_id key; redirect-following is OFF (closes the response-side SSRF-via-redirect variant raised in rev1 hard-hard honest-claim boundary). P-M1 rewritten to assert the CROSS-REGISTRY invariant via (a) STRUCTURAL enumeration over EVERY op PLUS a planted violating op that MUST refuse-to-load, plus (b) INSTANCE and (c) LEGIT. No residual path to a caller destination.
    evidence: rev2 s1.2.1, s1.3, s1.5 P-M1 row. Closes rev1 r1.
    load_bearing_in_rev1: true
  - id: r2
    disposition: CLOSED
    description: The rev1 contradiction (M2 closed-by-construction WHILE conceding redaction completeness open) is resolved by the honest split-claim. Channel = closed BY CONSTRUCTION (server-side cred, no caller-URL). Response redaction = BUILD-TIME-VERIFIED per-op allow-list (response_schema emits ONLY declared fields, DROPS everything else; allow-list not deny-list, so an un-anticipated echoed credential is dropped because never declared). INV-RESP forbids opaque pass-through (op with no response_schema refuses to load). SSE bounded via per-chunk chunk_schema allow-list. P-M2 broadened to the FULL Vertex surface (generateContent/predict/groundingMetadata/SSE/error bodies) + two-phase audit-record grep + the refuse-to-load assertion. The key-echo path is closed: no opaque escape hatch survives.
    evidence: rev2 s1.3.1, s1.5 P-M2 row, s5. Closes rev1 r2.
    load_bearing_in_rev1: true
  - id: r3
    disposition: CLOSED
    description: rev2 encodes the STRABO-verified mechanism correctly - keys off Tailscale-App-Capabilities (NOT User-Login, empty for tagged nodes); control-plane-resolved-from-grants/not-self-asserted; strip-protected incl. underscore/case-normalization defense, SAME guarantee as User-Login under the SAME 0600/loopback bind precondition; grant shape + --accept-app-caps opt-in/fail-closed serve invocation. V-ENC correctly scoped as a SINGLE build-time VERA encoding confirmation, NOT designed-around (the requirement - decode per the same rules as Tailscale-User-* then JSON-parse - is FIXED; only the on-the-wire byte encoding is re-confirmed against the pinned TS version). Independently web-confirmed against CURRENT (2026) Tailscale primary docs - the App-Capabilities header shape (JSON keyed by reverse-DNS cap name), the --accept-app-caps opt-in flag, AND the RFC-2047 Q-encoding for non-ASCII - so V-ENC is CONSERVATIVELY scoped (a build-time re-confirm, not an open design gap).
    evidence: rev2 s0, s1.4.1, s1.4.3, s5. STRABO r3 Items 1-4 (strabo-dc2-premises.md L246-345). Web-confirm of header shape + Q-encoding (tailscale.com/blog/app-capabilities, kb/1537). Closes rev1 r3.
    load_bearing_in_rev1: true
    version_floor_disposition: NOT a contradiction; JUSTIFIED + SOURCED. rev2 states v1.92+ as the APP-CAPS-FEATURE floor (STRABO r3 Item 4) at s0/s1.4.1/changelog, AND derives the EFFECTIVE DEPLOY floor as v1.94.1+ at s1.4.3 because THIS design ALSO requires unix-socket serve (supported TS >= 1.94.1, newswire field-note via STRABO L184). Binding floor = max(1.92, 1.94.1) = 1.94.1. Both numbers correct for their respective constraints; internally consistent. ONE honest nit (NOT load-bearing, NOT a reopen) - the 1.94.1 unix-socket-serve floor traces to a precedent field-note, not a primary doc re-confirmable via web search this re-audit; it sits correctly INSIDE the V-ENC build-time version-pinning discipline, so VERA pins + re-confirms the exact floor at build. No DAEDALUS action required.
  - id: r4
    disposition: CLOSED
    description: Two-phase audit genuinely guarantees no executed credentialed call is ever unrecorded. INTENT record written BEFORE egress (after validation gate, before the outbound call leaves the box), value-free (params_digest one-way, NEVER values; no credential; no response). OUTCOME update written AFTER egress, same audit_id, still value-free. P-M5 crash-inject point now sits in the egress->outcome window and asserts the INTENT record SURVIVES. The event that most needs attribution - the egress - can never occur un-recorded.
    evidence: rev2 s1.4.2, s1.5 P-M5 row. Closes rev1 r4.
    load_bearing_in_rev1: true
  - id: r5
    disposition: CLOSED
    description: INV-BIND genuinely makes a 0.0.0.0 bind un-shippable. At startup, BEFORE accepting any connection, the handler inspects its OWN listener and asserts 0600 AF_UNIX or loopback; if bound to ANY routable address it logs a fatal identity-trust violation and EXITS non-zero / refuses to serve. P-M3 (a) asserts the IN-PROCESS refuse, not only external ss/netstat. Protects BOTH identity classes - the strip-and-reinject guarantee for User-Login AND App-Capabilities both depend on this bind (s1.4.1), and P-M3 (b)/(c) exercise both.
    evidence: rev2 s1.4.3, s1.5 P-M3 row. Closes rev1 r5.
    load_bearing_in_rev1: true
  - id: r6
    disposition: CLOSED
    description: The honest residual is stated correctly with NO over-claim. skill_id is caller-declared = ATTRIBUTION (audited on the INTENT record) + optional SOFT per-(identity,skill_id) sub-bucket; explicitly NOT a security boundary. The per-identity (tag) bucket stays the HARD boundary - a hostile builder under the trusted tag could mislabel skill_id but cannot exceed that identity bucket (same trusted tag either way; mislabel only muddies intra-identity attribution). A hard per-skill quota is named as an out-of-scope follow-up needing a server-trusted skill identity. No claim that skill_id is a quota boundary.
    evidence: rev2 s1.3 envelope, s1.4.4, s6.1 weak-point-2, s7 out-of-scope. Closes rev1 r6 (was load_bearing:false).
    load_bearing_in_rev1: false
  dc3_carried_unchanged: CONFIRMED by direct s2 section diff. The two catalog TOMLs (vertex-gemini.toml, tailscale.toml) are BYTE-IDENTICAL in their code blocks (the only s2 diff lines are surrounding-prose arrow-glyph normalization + reflow; no TOML/spec change). The omitted Vertex key_bearing_pairing row is PRESERVED (decision unchanged, now annotated ARGUS CONFIRMED). The byte-identical resolve.py assertion is preserved; assert_value_free preserved; the ProvisioningSpec (s2.5) is field-identical. The ONLY substantive s2 add is the one ARGUS-suggested SA-bootstrap prose line (s2.1 last bullet) - additive prose, exactly as flagged in rev1 non-findings. Clean.
  new_regression_check: NONE beyond the one DAEDALUS self-named. The single new weak point (INV-RESP allow-list closed-by-correct-maintenance vs an evolving upstream Vertex response surface, named s5/s6.1) is HONESTLY BOUNDED and does NOT secretly reopen M2 - widening the allow-list to surface a new upstream field is an explicit build-time change that must be re-proven not to carry a credential echo, and opaque pass-through stays structurally forbidden (INV-RESP). The build-time framing is the honest ceiling for an allow-list over an upstream the core does not control. No fold introduced an unnamed new security gap.
  threat_coverage_assessment: PROBE-SPEC ADEQUACY (design-time, s6.9 tier-i) RE-CONFIRMED and STRENGTHENED. rev1 PASSED the mechanical presence-check but flagged three probes at insufficient STRENGTH (P-M1 single-injection / P-M2 single-echo / P-M5 after-egress crash-point). rev2 RAISES all three: P-M1 asserts the cross-registry STRUCTURAL invariant + planted-violation refuse-to-load; P-M2 covers the FULL Vertex response surface + INV-RESP refuse-to-load; P-M5 crash-injects in the egress->outcome window asserting INTENT survives. No mapless-mitigation smell, no map-present/probe-absent smell. THREAT-ENUMERATION completeness remains unmechanized judgment (honest-claim boundary below); coverage is not represented as proof of total threat-defeat.
  honest_claim_boundary: |
    (hard-hard, s6.6, not a numbered condition.) Verified COVERAGE of the named threats M1..M6 and that the six folds close the six conditions; did NOT exhaust threat-ENUMERATION. For this all-keys internet-egress box the residual enumeration items CONSIDERED and which rev2 addresses or honestly bounds: response-side SSRF-via-redirect (now CLOSED by INV-DEST redirect-OFF); key confusion across providers (each op binds one provider auth server-side - bounded, not mechanically proven cross-op); timing/error-class side-channel exfil of key bytes (rev2 returns only status + provider_error_class on error - bounded, not eliminated). M1..M6 NOT represented as a proven-complete threat set - the enumeration is the gate judgment, surfaced so it is not mistaken for exhausted. None of these UNVERIFIABLE residuals is a reason to withhold PASS on the six folds, which are the scope of this re-audit.
summary: |
  RE-AUDIT VERDICT: PASS. design-rev2.md genuinely closes all six rev1 conditions - each fold traced to the actual mechanism and probe, not the changelog claim. r1: INV-DEST makes a caller-influenced destination UNCONSTRUCTABLE across the whole registry and P-M1 asserts the cross-registry invariant via a planted-violation refuse-to-load. r2: the closed-by-construction-vs-open contradiction is resolved by the honest split-claim (channel by-construction; response redaction a build-time-verified per-op allow-list) with INV-RESP forbidding opaque pass-through and P-M2 broadened to the full Vertex surface. r3: the tagged-builder path is encoded as STRABO verified (App-Capabilities, control-plane-trusted, strip-protected, --accept-app-caps opt-in/fail-closed) with V-ENC correctly scoped as a single build-time confirm; the header shape + RFC-2047 Q-encoding web-confirmed against current Tailscale docs. r4: two-phase intent-before-egress/outcome-after with a value-free INTENT record and P-M5 crash-inject moved into the egress->outcome window guarantees no executed credentialed call is unrecorded. r5: INV-BIND in-process fail-loud refuse-if-routable makes a 0.0.0.0 bind un-shippable and protects BOTH identity classes. r6: the honest residual is stated (skill_id is attribution, not a security boundary; per-identity tag stays the hard boundary) with no over-claim.
  The v1.92-vs-v1.94.1 floor the brief flagged is NOT a contradiction: v1.92 is the app-caps-FEATURE floor (STRABO-verified) and v1.94.1 is the EFFECTIVE DEPLOY floor because the design also requires unix-socket serve (newswire field-note); binding floor max(1.92,1.94.1)=1.94.1 and rev2 states both correctly. The one honest nit - the 1.94.1 unix-socket floor rests on a precedent field-note, not a primary doc re-confirmable now - sits correctly inside the V-ENC build-time version-pinning discipline; no DAEDALUS action required. The DC3 mock-emit is carried CLEAN (direct section diff: two TOMLs byte-identical, omitted pairing row + byte-identical resolve.py preserved, one additive SA-bootstrap line only). The one new weak point is DAEDALUS-self-named and honestly bounded (INV-RESP allow-list maintenance vs evolving Vertex); it does not reopen M2. No unnamed regression. This is a clean security PASS on the DC1 crux - the gate-relevant go signal the FM turns on. The threat-ENUMERATION-completeness residual (hard-hard, above) is surfaced honestly and is not a reason to withhold PASS on the six folds.
gap_or_blocker: none - rev2 + rev1 + rev1 verdict + STRABO r3 follow-up all read in full; DC3 carried-unchanged verified by section diff; r3 header/encoding web-confirmed against current Tailscale docs.
