status: completed
ticket: stoa--q7f
verdict: pass-with-nits
diff_reviewed: worktree arc-76-build (uncommitted DC3 build) — tracked M: tests/test_dataload.py; untracked NEW: builder_deploy_core/data/catalog/vertex-gemini.toml, builder_deploy_core/data/catalog/tailscale.toml, demo/sos_core_emit_demo.py
design_artifact_compared_against: agents/design/stoa--q7f/design-rev2.md §2 (DC3 mock-emit)
concerns:
- id: c1
  category: craft
  description: Test function name test_load_detection_hints_loads_four_services now asserts SIX services (roster grew 4->6); the docstring + comment were updated to say "every cataloged service" but the function NAME still says four_services, so the name contradicts its own docstring and assertion. Cosmetic/readability only — behavior is correct and STRICTER (set-equality). ADA should rename to _loads_all_services (or _six_services).
  evidence: tests/test_dataload.py:88 (def test_load_detection_hints_loads_four_services) vs the in-body set-equality asserting the 6-member roster (lines ~92-95)
  severity: nit
  quadrant_classification: easy-easy
- id: c2
  category: consistency
  description: DESIGN-CANON DRIFT (design-vs-shipped) — design-rev2 §2.6 step 2 instructs "Builds the resolved set ... via a category=\"none\" manifest with delta.add, calls resolve(...) -> asserts the resolved set equals §2.4." Against the shipped FROZEN resolver this is UNEXECUTABLE: resolve.py:78-80 raises ResolutionError("unknown category: none") because "none" is not in the library category set {document-consuming, document/data, geospatial}. ADA built to ship reality (compose baseline UNION delta.add directly == §2.4; this is the category-FREE set §2.5 confirms) — the RIGHT engineering call, NOT a defect in the build. The drift is in the DESIGN PROSE: §2.6 step 2 should be corrected (follow-up) to describe direct baseline∪delta composition for a category-less core stand-up, not a resolve() call. Surfacing now (cheaper than the per-arc design-canon audit catching it later). The Grand also needs this expressiveness finding at the real provision gate (resolve() cannot express a category-less manifest).
  evidence: design-rev2.md §2.6 step 2 ("calls resolve(...)") vs resolve.py:78-80 (raises on category=none); demo step2 (sos_core_emit_demo.py) ground-checks the raise and composes directly; ADA + VERA + POLYBIUS all surfaced D1 up-chain
  severity: recommended-revision
  quadrant_classification: easy-easy
follow_ups:
- c1 rename of test_load_detection_hints_loads_four_services -> reason: cosmetic test-name accuracy, not load-bearing; ADA can fold into the same revise pass as c2 or a trailing commit.
- c2 design-rev2 §2.6 step 2 correction (resolve() call -> direct baseline∪delta composition for category-less stand-up) -> reason: design-canon drift, not a build defect; the build is correct; the DESIGN doc should be reconciled to shipped reality as a follow-up, and the expressiveness finding relayed to the Grand at the provision gate (already surfaced up-chain by ADA/VERA/POLYBIUS).
verifier_coverage_assessment: VERA genuinely FALSIFIED — did not re-assert ADA. The load-bearing meta-verifier move is p5: VERA independently re-composed the resolved set from the loaded catalog+baseline (NOT demo code) and re-derived emit_spec RAW, explicitly bypassing the demo hand-typed EXPECTED_SPEC, and named the exact failure mode this closes (a demo that mistyped EXPECTED_SPEC to match a buggy emit would pass its own asserts). p9-D1: VERA called resolve(category=none) itself, observed the raise, enumerated the library categories, and confirmed no new-catalog leak into the 3 real categories — D1 disposition (real finding for the Grand, structurally forced, not papered over) is sound. p7/p10: VERA read the test diff against both functions and correctly judged set-equality STRICTER-than-count => mandatory-not-weakening. threat_coverage: [] is correctly justified under the §35.5 carve-out (NOT threat-ratified, ARGUS CONFIRMED, design §2.7) — no threat-ratified mitigation in scope, so my §6.1 item-11 cross-check is a no-op. I independently re-ran the demo (exit 0, all §2.6 assertions) and the full suite (105 passed/0 failed) and reproduced VERA every result. No coverage gap; nothing VERA passed is a latent problem. VERA verdict SOUND.
summary: Clean DC3 mock-emit build with two minor surfacings, neither blocking. The diff is exactly 3 authored files (2 catalog TOMLs byte-identical to the design §2.2/§2.3 EXACT blocks — verified by byte-comparison — plus a high-craft demo) and one in-scope test modification. SCOPE: the test_dataload.py edit is APPROPRIATE, not creep — it is a forced roster-pin (two set-equality assertions grew 4->6 to admit the additive records); set-equality is STRICTER than a count so the update is mandatory, and every other assertion (the load_catalog no-detection_hints-leak shape assertion, per-token isinstance, spot-checks) is byte-unchanged. No core-service code, no INV-* impl, no frozen-file edits (resolve/spec/port/mock/dataload/baseline/kinds all zero-diff). CONSISTENCY: both TOMLs follow the established catalog shape EXACTLY (service-id/gcp_api/category + [[entries]] + the CLOSED [detection_hints] sub-key set); tailscale gcp_api=none follows the spatial-db precedent; the omitted Vertex key_bearing_pairing row is consistent with the §2.1 SA-auth justification (ARGUS CONFIRMED). SECURITY/value-free: the TOMLs hold slot NAMES only (zero credential values), the demo makes ZERO real network/cred calls (MockProvisioner + load_catalog only, every populated=False), author=Denson Smith on all three authored files (seat-built-by ADA is a compliant seat-identity layer on top, not a foreign author field). The most important surfacing is c2: a design-canon drift (design §2.6 step 2 describes a resolve() call that the shipped frozen resolver rejects for category=none) — the BUILD is correct (ADA composed directly, the right call), the DESIGN doc should be reconciled as a follow-up. Overall posture: minor revisions (a test rename + a design-doc correction), ship-ready as built. VERDICT: PASS-WITH-NITS.