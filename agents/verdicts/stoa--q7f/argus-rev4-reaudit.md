status: completed
ticket: stoa--q7f
verdict: pass
design_artifact_audited: agents/design/stoa--q7f/design-rev4.md (arc-77/build worktree) — RE-AUDIT of the rev3->rev4 delta only (the four folds); the rev3 SHAPE was cleared in argus-rev3-security.md and is not re-litigated.

audit_block:
  risks:
  - id: r1-residual
    description: NON-LOAD-BEARING probe-precision observation on P-M4(a.2). Condition r1 load-bearing failure mode is CLOSED (see r1 close-status below). P-M4(a.2) asserts the observable response triple (403 + ZERO egress + audited forbidden) on the unmapped-cap and absent-header paths, but does NOT pin the audited REASON to the no-resolvable-principal string. Consequence: a permissive-default build whose default principal is out-of-scope for the probed op would ALSO produce 403 + zero egress (denied at the scopes gate with reason out-of-scope, not at resolution) and would PASS P-M4(a.2). Every permissive default that actually GRANTS reach (default principal in some op scopes) still produces egress and FAILS the probe, so the dangerous class is caught. The uncaught residue is a functionally-fail-closed sentinel default that grants zero reach today — latent fragility (a later scopes edit could silently make that sentinel reachable), not an open cross-lane hole. The design claim in section 5.3 step 3 (a .get(cap, DEFAULT)-style resolver FAILS P-M4(a.2)) is therefore true for reach-granting defaults and slightly over-stated for a zero-reach sentinel default.
    evidence: design-rev4 section 2.5 P-M4(a.2) (asserts 403/zero-egress/audited-forbidden, does not assert the audit reason string); section 2.5 audit bullet (distinct reasons available: no-resolvable-principal / ambiguous-multi-lane-principal / out-of-scope); section 5.3 step 3.
    load_bearing: false
    quadrant_classification: easy-easy
  non_findings:
  - r1 [was LOAD-BEARING] request-time fail-closed principal resolution — CLOSED. rev4 adds INV-PRINCIPAL (section 2.4 clauses 4-6) as a distinct request-time invariant sibling to the startup INV-LANE, plus the explicit resolve_principal reference implementation (section 2.1). Traced every no-resolvable-principal case the condition names: (a) absent/empty/malformed App-Capabilities -> caps is the empty set -> lane_principals empty -> falls past multi-cap and single-lane -> operator check fails (no allowlisted login) -> raise Forbidden -> 403. (b) a cap outside LANE_REGISTRY -> the membership test is false -> not added (no default) -> Forbidden -> 403. (c) unlisted operator login -> not in CORE_OPERATORS -> Forbidden -> 403. There is NO constructable default/permissive fallback: the lane set is built by explicit-membership set-comprehension (a miss is simply not added), the .get(cap, DEFAULT) shape and any empty-header->benign-class shape are explicitly forbidden, and the handler denies on Forbidden rather than falling through. P-M4(a.2) FALSIFIES the reach-granting permissive-default build. The r1 failure mode named in rev3 (a permissive default that is cross-lane-BROKEN) is closed by construction. See r1-residual for the one non-load-bearing precision caveat.
  - r2 [was LOAD-BEARING] synthetic-marker bypass — CLOSED. rev4 section 3.1 constrains the SEAL_AUDIT_SYNTHETIC_ exemption to the INTERSECTION of path-bound AND shape-clean, with an explicit precedence rule: the shape test is applied to the demarked body (marker prefix stripped) and a real-secret-shape match FAILS the build REGARDLESS of the marker or path. (a) A real-SHAPED value carrying the marker cannot pass — the demarked body matches a real shape -> FAIL. So the marker is genuinely necessary-but-not-sufficient. (b) P-M7(a.2) probes the bypass: it plants (i) a marked real-shaped value inside the fixtures path AND (ii) a marked value outside the fixtures path, and asserts BOTH still FAIL — so a build treating the marker as an unconditional pass token fails this probe. (c) New-bypass check: the path-bound + shape-clean rule cannot itself become a laundering surface. Shape-clean is required and applied unconditionally to the demarked body, so no real-shaped secret is ever exempted regardless of path; an UNMARKED real secret placed inside the fixtures path fails the base rule (the exemption requires the marker, and section 3.1 scans every tracked file including the fixtures path). The only exemptible object is a shape-clean (by definition non-real) marked fixture in the designated path. The shape-clean half is the load-bearing bypass-closer; the path-bound half is defense-in-depth. The base rule also tightened from any UNMARKED secret-value match (rev3) to any secret-value match (rev4) with the constrained exemption — a tightening, not a regression.
  - r3 [was scope/uncertain] multi-cap resolution fail-closed — CLOSED. section 2.1 clause 3 + INV-PRINCIPAL clause 6: if more than one distinct lane principal resolves AND the cap-set frozenset is not in MULTI_LANE_NODES -> raise Forbidden -> 403, no egress. This denies a 2-lane non-registered node rather than silently unioning two lanes scopes. The explicit-exception registry MULTI_LANE_NODES is deny-by-default (only an explicitly-registered cap-set resolves) and returns ONE declared principal (not a union of scopes), whose scopes are separately configured. A registered multi-lane node carrying an extra unmapped cap would not match its registered key and is denied — fail-closed (stricter), acceptable.
  - r4 [was scope/uncertain] store-DATA tenancy scoped out + ticketed — CLOSED. section 7 first bullet explicitly states the multi-lane authz closes cross-lane API-EGRESS ONLY, states cross-lane store-DATA isolation is NOT silently assumed closed, cites tracking ticket stoa--exn, and states it does NOT block this pass-through build. I resolved stoa--exn: it EXISTS and carries a concrete plan (blocker named — the lane principal must be carried from the pass-through identity layer into the store-access path, today a separate private DATABASE_URL; next-action named — design row-level/schema/per-lane-namespace isolation at Phase-3 store stand-up with candidate mechanisms Postgres RLS / per-lane schema namespacing / lane column+enforced predicate; owner to be assigned at Phase-3). Correct scope-out discipline.
  - rev3->rev4 delta is FOLDS-ONLY (no carried-section regression) — CONFIRMED by full unified diff. Every change maps to r1/r2/r3/r4 or a lineage/cross-reference note. Carried UNCHANGED: INV-DEST/INV-RESP/INV-BIND, the two-phase audit, M1-M3/M5/M6 and P-M1..P-M3/P-M5/P-M6 (section 5.1/5.2). scopes class->lane remains a TIGHTENING. workflow-vs-app-code = application code UNCHANGED (INV-PRINCIPAL correctly added to the section 4 pure-code list). The three A3 classifications carry with rev4 confirmations (seal-audit threat-ratified; DC3 mock-emit + workflow-call NOT-threat-ratified, carve-outs correct not self-asserted); the r2 tightening is explicitly noted to NOT change the seal-audit classification (it closes a bypass IN the already-threat-ratified mitigation). Authorship frontmatter = Denson Smith. Scope fence held (section 0/7; worktree carries design/ + verdicts/ only).
  threat_coverage_assessment: Design-time probe-SPEC adequacy (re-audit of the two folds that touched the threat-anchored probe set). M4: the mapped mitigation now carries an A3 map row citing P-M4 EXTENDED, and section 2.5 specs a threat-anchored probe with three attack sub-paths (a.1 mapped cross-lane, a.2 unresolvable-principal, a.3 loader/multi-cap) plus (b) legit-unaffected — the r1/r3 gap in rev3 (no spec-d probe for the unresolvable/ambiguous path) is closed at the SPEC level. M7: the mapped mitigation carries P-M7 EXTENDED with (a.1) unmarked, (a.2) marked-bypass, and (b) legit-unaffected — the r2 gap (marked-bypass unprobed) is closed at the SPEC level. Both folds add a threat-anchored probe exercising the named attack path and asserting both attack-blocked and legit-unaffected, so no mapped mitigation is left probe-absent. Boundary on this claim (honest): this is probe-SPEC adequacy read pre-build; whether the EXECUTED probe genuinely exercises each attack path (and, for the r1-residual note, whether the executed P-M4(a.2) pins the audit reason) is VERA/CATO tier-ii at/after build. Threat-ENUMERATION completeness stays unmechanized ARGUS judgment: the design honestly does not claim M1-M7 is exhaustive, and r4 is the one shared-surface the reframe introduces, now explicitly scoped out and ticketed rather than silent.


summary: RE-AUDIT of the rev3->rev4 delta. All four ARGUS-rev3 conditions are CLOSED, not merely mentioned. The two load-bearing folds hold up under trace: r1 is closed by construction — INV-PRINCIPAL plus the explicit resolve_principal reference implementation force a DENY on every no-resolvable-principal case with no constructable permissive default, and P-M4(a.2) falsifies every reach-granting permissive-default build; r2 is closed by the shape-clean precedence rule — the demarked body is shape-tested unconditionally so no real-shaped value is ever exempted regardless of marker or path, and P-M7(a.2) probes the bypass. The two scope folds are correct: r3 reject-ambiguous genuinely denies a 2-lane non-registered node (deny-by-default MULTI_LANE_NODES, single declared principal, never a scope-union), and r4 is explicitly scoped out in section 7 with the store-layer boundary stated and the tracking ticket stoa--exn verified to exist with a concrete plan. The rev3->rev4 diff is folds-only with no carried-section regression, and the base seal-audit rule tightened rather than weakened. One NON-load-bearing precision observation (r1-residual): P-M4(a.2) asserts the response triple but not the audited reason, so a zero-reach sentinel-default resolver (functionally fail-closed, grants no reach) would pass the probe indistinguishably from a true resolution-denial — latent fragility a later scopes edit could surface, not an open cross-lane hole. This does NOT hold the design open. Posture: CLEAN — design-rev4 is cleared for ADA. Note for the downstream gauntlet, not a blocker: VERA at build time should confirm the EXECUTED P-M4(a.2) observes denial at RESOLUTION (assert the audit reason is no-resolvable-principal), which would also retire the r1-residual precision note.

daedalus_residual_questions_answered:
  - (1) Does P-M4(a.2) FALSIFY a permissive-default build? YES for the dangerous class — any default that grants reach produces egress and fails the probe; INV-PRINCIPAL plus the reference resolver forbid the .get(cap, DEFAULT) shape by construction. Caveat (non-load-bearing, see r1-residual): the probe asserts the 403/zero-egress response but not the audit reason, so a zero-reach sentinel default that is out-of-scope for the probed op would also pass — latent fragility, not an open hole. r1 load-bearing failure mode CLOSED.
  - (2) Does reject-ambiguous actually DENY a 2-lane non-registered node? YES — resolve_principal raises Forbidden (403, no egress) when more than one lane principal resolves and the cap-set frozenset is not a registered MULTI_LANE_NODES entry; deny-by-default, never a silent union; the registered exception returns a single declared principal, not a scope-union.
  - (3) Can the path-bound + shape-clean marker constraint itself become a new bypass? NO — shape-clean is required and applied unconditionally to the demarked body, so no real-shaped secret is ever exempted regardless of path; an unmarked real secret in the fixtures path fails the base rule; the only exemptible object is a shape-clean (non-real) marked fixture. No laundering surface.

condition_close_status: r1 CLOSED (load-bearing); r2 CLOSED (load-bearing); r3 CLOSED (scope); r4 CLOSED (scope, ticketed stoa--exn verified). Verdict: PASS — design-rev4 cleared for ADA. One non-load-bearing probe-precision note carried forward to VERA (not a blocker).

gap_or_blocker: none (status completed)
