<!-- author: Denson Smith -->
<!-- seat: CAPTAIN_ARGUS_the_stoa (PLAN-CRITIC) — focused re-audit of the rev5 to rev6 delta: the r1 (SCALAR recursive-dict-scan) + r2 (PASS_WHOLE_ALLOWLIST) folds of ARGUS rev5 conditions -->
<!-- ticket: stoa--q7f | arc-77 | coordination stoa--po5 | u--9s2 Phase-1 increment 2.4 -->

# ARGUS re-audit verdict — arc-77 rev6 (r1 + r2 fold of the ARGUS rev5 conditions) — stoa--q7f

```
status: completed
ticket: stoa--q7f
verdict: pass
design_artifact_audited: agents/design/stoa--q7f/design-rev6.md (rev5 to rev6 delta ONLY — the r1 SCALAR-recursive-dict-scan fold + the r2 PASS_WHOLE_ALLOWLIST fold; the already-cleared rev5/rev4 shape NOT re-litigated)

audit_block:
  scope: FOCUSED re-audit confirming closure of the two ARGUS rev5 PASS-WITH-CONDITIONS residuals (argus-rev5-inv-resp.md r1 + r2). Not a fresh cold-audit — the rev5 OBJECT-recursion delta and the rev4 shape (multi-lane authz, INV-PRINCIPAL, seal-audit, INV-DEST/BIND/LANE, M1/M3-M7, workflow=app-code, scope fence) were verified UNCHANGED, not re-audited.

  r1_closed: YES — CONFIRMED. rev6 makes the SCALAR node a BY-CONSTRUCTION RECURSIVE DICT-SCAN: the SCALAR handler recurses into every list / nested container under the value and refuses to emit ANY mapping found at ANY depth; only true scalar leaves and (recursively) arrays-of-scalars pass; the whole value is dropped if a mapping is found anywhere in it (rev6 sec 5.1a node 2). A shallow top-level check is declared NON-CONFORMANT. P-M2(a.3) plants an object/dict carrying a credential-shaped value under a SCALAR-declared field BOTH directly (finishReason = a dict) AND nested inside a list (webSearchQueries = [ok, dict], doubly-nested [[ok, dict]]) and asserts the planted object is DROPPED from the caller-facing output AND absent from BOTH audit phases. The LIST-NESTED case is the load-bearing distinguisher: a shallow-SCALAR build (drops a top-level dict; passes a list-containing-a-dict) EMITS the list-nested credential and FAILS the absence-assertion; the recursive dict-scan DROPS it and PASSES. Genuine falsification of a shallow build, specifically via the list-nested case as the brief requires. The (b) legit clause (a legit scalar AND a legit list-of-scalars pass intact) pins the complement, so the a.3/(b) pair forces the exact recursive-dict-scan behavior and forbids a lazy drop-any-list impl. No mapping can ride a SCALAR slot at any depth. Buildable: contains_mapping(v) = isinstance(v,dict) or (isinstance(v,list) and any(contains_mapping(e) for e in v)); JSON emits only dict/list/scalar, so list is the only container to recurse. CLOSED.

  r2_closed: YES — CONFIRMED. rev6 adds server-side PASS_WHOLE_ALLOWLIST = frozenset({candidates.content}) and extends the INV-RESP loader self-check (_check_inv_resp) to walk the schema node tree tracking the dotted path to each node and REFUSE TO LOAD (LoaderError, fail-CLOSED AT LOAD) any schema marking a PASS_WHOLE field whose dotted path is not in the allowlist (rev6 sec 5.1a). P-M2(a.4) loads a schema marking a NON-allowlisted field PASS_WHOLE and asserts refuse-to-load; a node-types-only loader that loads it anyway FAILS the clause. This is deny-by-default and caught AT LOAD (refuse-to-serve), not at runtime — a future op mis-marking a credential-bearing field PASS_WHOLE is stopped at startup, consistent with the INV-LANE / INV-PRINCIPAL / seal-audit deny-by-default posture. Fail-open is not constructible for the realistic surface: whole-pass treatment occurs ONLY at a PASS_WHOLE leaf, and EVERY PASS_WHOLE leaf is path-checked at load, so the load-time and runtime views are consistent (no field is whole-passed at runtime without being a load-checked PASS_WHOLE leaf). CLOSED.

  dotted_path_convention_buildable: YES for the realistic field-name surface. The convention (schema-root-relative field path; the OBJECT node candidates applied element-wise still yields candidates.content; list indices are NOT part of the path; the walk keys on field NAMES not container indices) is unambiguously buildable by accumulating node keys along the walk and comparing against the allowlist. For the Vertex / JSON identifier field-name space (camelCase keys, no embedded dots) it cannot be evaded: a PASS_WHOLE leaf is honored only if its computed path EXACTLY matches an allowlist entry, else the loader refuses (fail-closed). See non_findings for the one theoretical corner considered and discharged (embedded-dot field names).

  grammar_complete_no_r3: YES — CONFIRMED. The response_schema NODE grammar has exactly three node types and the loader enforces that closed set (every node must be an OBJECT dict, a SCALAR, or a PASS_WHOLE; a bare list or any unrecognized node refuses to load). Each type is now fully guarded: OBJECT recurses and drops undeclared keys at every level (rev5); SCALAR drops any mapping at any depth via a recursive dict-scan (rev6 r1); PASS_WHOLE placement is deny-by-default, loader-enforced against an allowlist at load (rev6 r2). A list value under an OBJECT node is applied element-wise (non-dict element dropped); a scalar under an OBJECT node is dropped. There is no fourth node type and no unguarded type-combination — the grammar is a fixed point. No r3 in the node grammar; the expectation of convergence holds.

  no_regression: CONFIRMED. Mechanical git diff --no-index rev5 vs rev6 = 156 insertions / 42 deletions. Every changed line is r1/r2-fold-consistent: header/changelog, the rev6 changelog table + classification paragraph, the sec 1 M2 threat row + honest-claim rewrite, sec 5.1a SCALAR grammar + PASS_WHOLE_ALLOWLIST block + dotted-path paragraph + loader-guard extension, sec 5.1a P-M2 (a.3)/(a.4) additions, sec 5.2 M2 map row, sec 5.3 DoD, sec 6 wp3. No line weakens sec 2 multi-lane authz, INV-PRINCIPAL, seal-audit sec 3.1, INV-DEST/BIND/LANE, M1/M3-M7, the sec 4 workflow=app-code call, or the sec 7 scope fence. The 42 deletions are all intended rewrites of the lines the folds replace.

  classification_correct: CONFIRMED (op-disc sec 6.9 clause 2, A4 CONFIRM). DAEDALUS PROPOSES both r1 and r2 are TIGHTENINGS inside the already-threat-ratified M2 mitigation (INV-RESP) — no new M#, M2 A3 map row (sec 5.2) reused, threat-anchored probe remains P-M2 (extended with a.3 + a.4), verdict defeats_via_probe V-M2-RESP unchanged. ARGUS CONFIRMS: neither fold introduces a new attack path or threat — r1 probes an attack path (object rides a SCALAR slot) the rev5 grammar already named but left unprobed; r2 mechanizes a deny-by-default guard on the PASS_WHOLE escape hatch the rev5 grammar already declared never-a-default. PASS_WHOLE_ALLOWLIST is a bounded guard on the existing content carve-out, not a new mitigation. Classification CORRECT.

  risks: []

  non_findings:
  - Dotted-path embedded-dot theoretical collision (the brief evasion question, considered + DISCHARGED). The r2 allowlist compares a dot-joined path string against {candidates.content}. A single node key literally named candidates.content (a dot INSIDE one field name) would compute the same joined string as the two-segment path candidates then content, so the string convention has a theoretical ambiguity. DISCHARGED as NOT load-bearing for three reasons: (1) it requires a provider field literally named with an embedded dot AND an upstream body carrying a credential under it — not a realistic Vertex/JSON identifier shape; (2) the actor who could exploit it is a future server-side registry.py author, who already has full code access and could edit PASS_WHOLE_ALLOWLIST directly — the guard threat model is mistake-prevention + audit-forcing, not defense against a malicious registry author; (3) the collision direction is contrived and the guard remains fail-closed for every realistic field name. The convention unambiguity rests on the assumption that provider field names are dot-free identifiers, which holds for the Vertex surface this arc wires. Named for completeness; ARGUS does not prescribe a remediation.
  - content PASS_WHOLE remains correctly bounded to candidates.content ALONE (the sole allowlist entry); groundingMetadata is a recursed OBJECT node (undeclared sub-keys dropped), not whole-passed. content is the model product and is not credential-bearing (the only core credential is the request-side ADC bearer, never in a Vertex response body). Carried from rev5, unchanged. Discharged.
  - KEY-allowlist vs VALUE-content-scanner residual (sec 6 wp3 residual i): a declared SCALAR leaf string value or the content product passes whole; this is the correct boundary, not a hole — value-content artifact scanning is the seal-audit (M7) job. Carried, discharged.
  - P-M2(a.3) absence-assertion sufficiency: like P-M2(a.2), a.3 relies on the absence-assertion, which is logically sufficient to falsify any emitting build. The (b) legit clause additionally guards over-drop. Non-load-bearing.

  threat_coverage_assessment: COMPLETE at design-time probe-SPEC adequacy (op-disc sec 6.13). M2 is a threat-ratified mitigation with an A3 map row (sec 5.2). A threat-anchored probe is now specified for EVERY node-type attack surface of the M2 INV-RESP mitigation: OBJECT-node depth (P-M2 a.2, rev5), SCALAR-slot object smuggle (P-M2 a.3, rev6 r1), and PASS_WHOLE mis-placement (P-M2 a.4, rev6 r2). The one probe-spec gap ARGUS rev5 flagged (SCALAR-slot smuggle unprobed) is now closed by a.3; the r2 placement guard is probed by a.4. No executed probe exists yet (design-time read) — this assessment is probe-SPEC adequacy only; the tier-ii does-the-executed-probe-exercise-the-attack-path judgment is VERA/CATO downstream. Threat-ENUMERATION completeness stays ARGUS unmechanized judgment: the M-set is unchanged from the ratified rev4/rev5 model (no new threat introduced by either fold), so enumeration is not re-opened by this delta.

  items_1_to_4_explicit_answers:
  - item 1 (r1 closed; probe falsifies a shallow-SCALAR build via the LIST-NESTED case; no mapping rides a SCALAR slot at any depth): YES. Recursive dict-scan drops any mapping at any depth; P-M2(a.3) list-nested case emits under a shallow build and is dropped under the recursive build, so it genuinely distinguishes shallow from recursive; the (b) legit clause forbids over-drop. Closed.
  - item 2 (r2 closed; PASS_WHOLE placement caught AT LOAD deny-by-default; dotted-path convention buildable and not evadable): YES. Loader refuses-to-load a non-allowlisted PASS_WHOLE (fail-closed at load); the convention is unambiguously buildable for the identifier field-name surface and not evadable there (exact-match-or-refuse); the sole theoretical corner (embedded-dot field name) is discharged as non-load-bearing (see non_findings).
  - item 3 (convergence; node grammar complete; no r3): YES. Three loader-enforced node types, each fully guarded (OBJECT recurses, SCALAR drops objects at any depth, PASS_WHOLE placement-guarded). No fourth type, no unguarded combination — a fixed point. No third by-construction hole.
  - item 4 (no rev5 regression; classification correct): CONFIRMED both. Delta mechanically confined to r1+r2 (156ins/42del, no weakening of the carried shape); tightening-inside-M2 with no-new-M# classification confirmed (A4).

summary: Focused re-audit of the rev5 to rev6 delta, confirming closure of the two ARGUS rev5 conditions. Both are CLOSED. r1: the SCALAR node is now a by-construction RECURSIVE DICT-SCAN that drops any mapping found at any depth (including nested inside a list), a shallow top-level check is declared non-conformant, and P-M2(a.3) genuinely falsifies a shallow-SCALAR build specifically via the list-nested case (a shallow build emits the list-nested credential-bearing dict; the recursive scan drops it) with a legit clause pinning the no-over-drop complement — so no mapping can ride a SCALAR slot at any depth. r2: PASS_WHOLE placement is now deny-by-default via a server-side PASS_WHOLE_ALLOWLIST = {candidates.content} that the INV-RESP loader enforces by refusing to load any schema marking a non-allowlisted field PASS_WHOLE (fail-closed AT LOAD, not runtime), probed by P-M2(a.4); a future op mis-marking a credential-bearing field PASS_WHOLE is caught at startup. The dotted-path convention is unambiguously buildable and non-evadable for the realistic identifier field-name surface (exact-match-or-refuse); the sole theoretical corner (a field name literally containing a dot) is discharged as non-load-bearing. The response_schema NODE grammar is now COMPLETE: exactly three loader-enforced node types, each fully guarded (OBJECT recurses, SCALAR drops objects at any depth via r1, PASS_WHOLE placement-guarded via r2) — a fixed point with no third by-construction hole, so there is no r3, matching the expected convergence. The rev5 to rev6 delta is mechanically confined to the r1+r2 folds (156 insertions / 42 deletions; no weakening of multi-lane authz, INV-PRINCIPAL, the seal-audit, INV-DEST/BIND/LANE, M1/M3-M7, the workflow=app-code call, or the scope fence), and the tightening-inside-already-threat-ratified-M2 classification (no new M#) is CONFIRMED. Posture: PASS — r1 and r2 closed, the node grammar is complete, no residual load-bearing risk. Cleared for ADA to build the complete hardened version. ARGUS surfaces; ARGUS does not fix.
```
