# build / test artifacts — never committed
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/
dist/
# per-worktree virtualenv (stoa--fdf inc 2.2): created for editable-install self-exercise; never committed
.venv/
