# author: Denson Smith
# §22 seed record — document-parsing. Validated fail-closed by dataload.load_catalog (§2.4.1).

service-id = "document-parsing"
gcp_api = "document-parsing"
category = "document-consuming"

[[entries]]
kind = "gcp_api"
name = "document-parsing"

# §25 detection_hints — ADDITIVE, ADVISORY (stoa--fdf inc 2.2; verbatim from prototype catalog_hints).
# load_catalog ignores it (byte-unchanged); load_detection_hints validates it. (§25.1 mis-propose-only.)
[detection_hints]
sdk_imports  = ["google.cloud.documentai", "google-cloud-documentai"]
url_patterns = ["documentai.googleapis.com"]
config_keys  = ["DOCAI_PROCESSOR_ID", "DOCUMENT_AI_LOCATION"]
data_signals = ["pdf-ingest", "ocr-extract", "document-parse"]
