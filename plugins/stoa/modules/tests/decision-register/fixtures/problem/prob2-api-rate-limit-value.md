SCENARIO: "At directive-lock the agent needed the third-party API's current rate limit. The classifier returned PROBLEM (read the published docs / measure it) and the agent went and got the number."
EXPECT: no-write
WHY: PROBLEM, not DILEMMA -- condition 2 fails. A findable factual value is not a value-tradeoff; there is a single right answer recoverable from the source. No register entry. Logging this would train the PRINCIPAL to distrust a register full of non-decisions.
